// Junior + Mid on-ramp interview bank.
// Fundamentals asked in real early-career MLE/DS screens, framed as interview
// questions with the common trap named — not textbook definitions.
export const EXTRA_FOUNDATIONS = [
  // ─────────────────────────── Statistics (6) ───────────────────────────
  {
    id: 3001,
    cat: 'Statistics',
    company: 'Any',
    level: 'Junior',
    q: 'Your A/B test comes back with p = 0.03. A teammate says "so there\'s a 97% chance the new variant is better." Is that right?',
    answer: 'No. The p-value is the probability of seeing an effect this large (or larger) IF the null hypothesis were true — it is a statement about the data given "no effect," not about the probability that any hypothesis is true. p = 0.03 means "if there were truly no difference, we\'d see data this extreme only 3% of the time," so we reject the null at the 5% level. It says nothing directly about the chance the variant is better; that would require a prior (a Bayesian posterior).',
    whatsTested: 'Correct interpretation of a p-value and the null-hypothesis framing.',
    antiPattern: 'Reading the p-value as P(hypothesis is true | data). It inverts the conditional — the p-value is P(data this extreme | null true), and flipping those two is the classic inverse-probability fallacy.',
    staffFraming: 'A stronger candidate adds that a p-value ignores effect size and business impact: with a huge sample even a trivial 0.1% lift is "significant," so you always pair significance with a confidence interval on the effect and a minimum-detectable-effect you decided on before the test.',
  },
  {
    id: 3002,
    cat: 'Statistics',
    company: 'Any',
    level: 'Junior',
    q: 'What does a 95% confidence interval actually mean? Someone tells you "there\'s a 95% probability the true mean is inside [4.1, 5.3]." True or false?',
    answer: 'Technically false under the frequentist definition. The 95% refers to the procedure: if you repeated the experiment many times and built an interval each time, about 95% of those intervals would contain the true fixed mean. For any single computed interval, the true mean is either in it or not — there is no probability attached to that one interval. In practice we still use it as our best plausible range for the parameter.',
    whatsTested: 'Understanding what the confidence level describes (the procedure, not one interval).',
    antiPattern: 'Assigning a 95% probability to the parameter landing in one specific interval. The parameter is fixed, not random, so the probability lives in the repeated-sampling procedure, not the single result.',
    staffFraming: 'A stronger candidate notes that a Bayesian credible interval DOES support the intuitive "95% probability the mean is in here" reading, and that with wide, uninformative priors the two often nearly coincide — so the distinction matters most with small samples or strong priors.',
  },
  {
    id: 3003,
    cat: 'Statistics',
    company: 'Any',
    level: 'Junior',
    q: 'Why do we usually reach for the mean, but sometimes the median? Give a concrete case where the mean would mislead a stakeholder.',
    answer: 'The mean uses every value and is efficient when data is roughly symmetric, but it is pulled by outliers and skew. The median is the middle value and is robust to extremes. Classic case: user session lengths or revenue-per-user are heavily right-skewed — a few power users spending $10k drag the mean way above what a typical user experiences. Reporting "average revenue per user = $85" when the median is $12 tells leadership a story most users never live.',
    whatsTested: 'Choosing a summary statistic that matches the data\'s distribution and the decision.',
    antiPattern: 'Always defaulting to the mean. It hides skew, so on long-tailed metrics like income, latency, or spend it overstates the typical case and can lead to bad product decisions.',
    staffFraming: 'A stronger candidate reports both plus a percentile (median and p90/p95) — especially for latency, where the average is fine but the p99 is what actually breaks the user experience, so you monitor the tail, not the center.',
  },
  {
    id: 3004,
    cat: 'Statistics',
    company: 'Any',
    level: 'Mid',
    q: 'You run 20 independent A/B comparisons at the 0.05 level and one comes back significant. Should you ship that winner?',
    answer: 'Be skeptical — this is the multiple-comparisons problem. If you test 20 truly-null hypotheses each at α = 0.05, the chance of at least one false positive is 1 − 0.95^20 ≈ 64%. So one "significant" result out of 20 is roughly what pure noise produces. Before shipping, correct for multiplicity (Bonferroni divides α by the number of tests; Benjamini-Hochberg controls the false discovery rate more powerfully) or, better, pre-register the primary metric and treat the rest as exploratory to be confirmed in a follow-up.',
    whatsTested: 'Recognizing inflated false-positive rates across many tests and how to control them.',
    antiPattern: 'Celebrating the one significant metric and ignoring the other 19. Cherry-picking the winner from many tests is exactly how noise gets shipped as a "result."',
    staffFraming: 'A stronger candidate distinguishes controlling family-wise error (Bonferroni, conservative) from false discovery rate (Benjamini-Hochberg, better power when you expect several real effects) and picks based on the cost of a false ship versus a missed win.',
  },
  {
    id: 3005,
    cat: 'Statistics',
    company: 'Any',
    level: 'Mid',
    q: 'Explain the Central Limit Theorem to a PM, then tell me one thing it does NOT let you assume.',
    answer: 'The CLT says the distribution of the sample MEAN becomes approximately normal as sample size grows, regardless of the shape of the underlying data — which is why we can build confidence intervals and run t-tests on skewed data once n is decently large. What it does NOT say: the raw data becomes normal (it stays as skewed as ever), and it does not rescue you when the sample is tiny or the tails are extreme, or when observations are not independent (e.g., correlated users), where convergence is slow or biased.',
    whatsTested: 'Grasping that the CLT is about the sampling distribution of the mean, plus its limits.',
    antiPattern: 'Claiming the CLT makes "the data normal" or that any n works. It is about the mean\'s distribution, needs adequate n, and assumes independent observations — violating those breaks the inference.',
    staffFraming: 'A stronger candidate flags that dependence (clustered/repeated users) inflates the effective variance, so the naive standard error is too small — you need clustered standard errors or a bootstrap at the user level, not the row level.',
  },
  {
    id: 3006,
    cat: 'Statistics',
    company: 'Any',
    level: 'Mid',
    q: 'A newer neighborhood in a city shows a higher crime rate than an older one, so a stakeholder concludes the new area is more dangerous. What confound would you check first?',
    answer: 'Check whether you are comparing rates or raw counts, and whether a lurking variable explains the gap — this smells like a base-rate or Simpson\'s-paradox trap. Population size, age distribution, policing intensity, and reporting rates all differ between neighborhoods. A "higher crime rate" can flip once you normalize per capita or stratify by a confounder like income or population density. The right move is to condition on the plausible confounders and see if the effect survives.',
    whatsTested: 'Spotting confounding and base-rate issues before accepting a causal claim from observational data.',
    antiPattern: 'Taking the raw comparison at face value and calling it causal. Aggregated observational differences routinely reverse after controlling for a confounder — correlation here is not the danger claim it looks like.',
    staffFraming: 'A stronger candidate names the identification strategy: stratify or regress on confounders, and if a key confounder is unmeasured, the observational estimate is not identified — you\'d want a natural experiment or an instrument, not just more controls.',
  },
  // ─────────────────────────── Evaluation (6) ───────────────────────────
  {
    id: 3007,
    cat: 'Evaluation',
    company: 'Any',
    level: 'Junior',
    q: 'Your fraud model is 99.5% accurate. The stakeholder is thrilled. Why might you not be?',
    answer: 'Because fraud is rare — maybe 0.5% of transactions — so a model that predicts "not fraud" for everything is already 99.5% accurate while catching zero fraud. Accuracy is misleading on imbalanced data. What matters is how well we catch fraud (recall) and how many false alarms we cause (precision). I\'d look at precision, recall, and the confusion matrix, and probably the PR curve, instead of accuracy.',
    whatsTested: 'Knowing that accuracy is deceptive under class imbalance and choosing better metrics.',
    antiPattern: 'Treating high accuracy as success on a rare-event problem. The majority-class baseline already scores near-perfect accuracy while being useless, so accuracy hides total failure on the class you care about.',
    staffFraming: 'A stronger candidate ties the metric to the cost matrix: a missed fraud costs the chargeback amount, a false positive costs a blocked good customer, so you optimize expected cost and pick the decision threshold from that — not a generic F1.',
  },
  {
    id: 3008,
    cat: 'Evaluation',
    company: 'Any',
    level: 'Junior',
    q: 'Precision vs recall — explain the difference, and give one product where you\'d favor recall and one where you\'d favor precision.',
    answer: 'Precision = of the items we flagged positive, how many really were positive (controls false alarms). Recall = of all the true positives out there, how many we caught (controls misses). Favor recall for a cancer-screening or fraud-detection flow, where missing a true case is catastrophic and a false alarm just triggers a second look. Favor precision for a spam filter or a "block this account" action, where wrongly flagging a legitimate item annoys or harms real users and is costly.',
    whatsTested: 'Defining precision/recall correctly and mapping them to the asymmetric cost of the use case.',
    antiPattern: 'Swapping the definitions or saying "you always want both high." They trade off at a fixed model quality, so the real skill is knowing which error your product can least afford.',
    staffFraming: 'A stronger candidate frames it as choosing an operating point on the PR curve driven by the cost of a false negative versus a false positive, and mentions that you tune the threshold to hit a target on the metric the business cares about, not 0.5 by default.',
  },
  {
    id: 3009,
    cat: 'Evaluation',
    company: 'Any',
    level: 'Junior',
    q: 'What is an F1 score and why not just average precision and recall directly?',
    answer: 'F1 is the harmonic mean of precision and recall: 2·P·R / (P + R). We use the harmonic mean instead of a plain average because it punishes imbalance — if precision is 0.95 but recall is 0.05, the arithmetic mean is a comfortable 0.5, but F1 is about 0.095, which honestly reflects that the model is failing on recall. So F1 rewards models that are good at BOTH, not lopsided ones.',
    whatsTested: 'Understanding F1 and why the harmonic mean is the right combiner.',
    antiPattern: 'Believing F1 is the plain average of P and R. That average lets a model hide a terrible recall behind a great precision; the harmonic mean refuses to.',
    staffFraming: 'A stronger candidate notes F1 weights precision and recall equally, which is rarely what the business wants — they reach for F-beta (β > 1 to favor recall, β < 1 to favor precision) or report the full PR curve so the reviewer can choose the point.',
  },
  {
    id: 3010,
    cat: 'Evaluation',
    company: 'Any',
    level: 'Junior',
    q: 'A colleague compares two models on the same test set and picks the one with higher AUC. When is AUC the wrong thing to optimize?',
    answer: 'AUC measures ranking quality — the probability a random positive scores higher than a random negative — averaged over all thresholds. It is threshold-agnostic and insensitive to class imbalance in its ROC form, which is exactly the problem when positives are rare: ROC-AUC can look strong while the model still produces mostly false positives at any usable threshold. If you operate at one fixed decision point, or positives are scarce, PR-AUC or the metric at your actual operating threshold is more honest than ROC-AUC.',
    whatsTested: 'Knowing what AUC does and does not capture, and when to prefer PR-AUC or point metrics.',
    antiPattern: 'Treating higher ROC-AUC as universally better. It rewards overall ranking but can mask poor precision on the rare class and ignores the single threshold you\'ll actually deploy at.',
    staffFraming: 'A stronger candidate distinguishes ROC-AUC (uses the true-negative-heavy majority, so it stays optimistic under imbalance) from PR-AUC (focuses on the positive class), and picks the metric whose curve matches the deployment: fixed threshold, imbalanced data → PR-AUC and calibration.',
  },
  {
    id: 3011,
    cat: 'Evaluation',
    company: 'Any',
    level: 'Mid',
    q: 'What does it mean for a model to be "calibrated," and why might a well-ranked model still need calibration before you ship it?',
    answer: 'Calibration means the predicted probabilities match observed frequencies: among cases the model scores 0.8, about 80% should actually be positive. A model can rank perfectly (high AUC) yet be miscalibrated — e.g., gradient-boosted trees and SVMs often output over-confident or squashed scores. You need calibration whenever the actual probability drives a decision: expected-value pricing, risk thresholds, or blending scores across models. Fixes: Platt scaling (logistic) or isotonic regression on a held-out set.',
    whatsTested: 'Distinguishing discrimination (ranking) from calibration and knowing when calibration matters.',
    antiPattern: 'Assuming a high-AUC model automatically has trustworthy probabilities. Ranking and calibration are separate — you can ace one and fail the other — so a "0.9" from an uncalibrated model may not mean 90%.',
    staffFraming: 'A stronger candidate calibrates on a separate held-out slice (not the training or the same test used for selection) to avoid leakage, and checks a reliability diagram, noting that resampling for imbalance or a shifted base rate can itself decalibrate the model.',
  },
  {
    id: 3012,
    cat: 'Evaluation',
    company: 'Any',
    level: 'Mid',
    q: 'Your model scores 0.92 F1 in offline evaluation but underperforms in production. Name the most likely offline-evaluation mistakes.',
    answer: 'The usual suspects: (1) train/test leakage — a feature encodes the label or the split isn\'t clean, so offline is optimistic; (2) a non-representative test set — you evaluated on data that doesn\'t match the live distribution (different time period, users, or a rebalanced test that changes the base rate); (3) temporal leakage — random splitting on time-ordered data lets the model peek at the future; and (4) train/serving skew — features computed differently offline vs. online. I\'d start by checking the split logic and comparing offline vs. live feature distributions.',
    whatsTested: 'Diagnosing why offline metrics don\'t transfer — leakage, distribution shift, and serving skew.',
    antiPattern: 'Blaming the model and retraining harder. The gap is usually an evaluation or data-plumbing bug (leakage, wrong split, skew), not model capacity — retraining on the same flawed setup reproduces the same illusion.',
    staffFraming: 'A stronger candidate builds a time-based split and a shadow/online-offline consistency check, and treats a persistent offline-online gap as a monitoring signal for distribution shift rather than a one-time debugging chore.',
  },
  // ─────────────────────────── Regression (5) ───────────────────────────
  {
    id: 3013,
    cat: 'Regression',
    company: 'Any',
    level: 'Junior',
    q: 'In a linear regression, a coefficient on "number of bathrooms" is 15,000. A stakeholder says "so each bathroom adds $15k to the price." What caveat do you add?',
    answer: 'The coefficient means: holding all other features in the model fixed, a one-unit increase in bathrooms is ASSOCIATED with a $15k higher predicted price. Two caveats: it is associational, not causal — bathrooms correlate with size, neighborhood, and other things, so it is not "add a bathroom, gain $15k." And it is conditional on the other features being held constant, which may be unrealistic (you rarely add a bathroom without adding square footage). The units and any feature scaling also change the number.',
    whatsTested: 'Correctly interpreting a regression coefficient — conditional and associational, not causal.',
    antiPattern: 'Reading the coefficient as a causal, standalone dollar effect. It is a partial association given the other predictors, and correlated features make the "all else equal" clause fragile.',
    staffFraming: 'A stronger candidate flags multicollinearity: when bathrooms and square footage move together, the coefficient becomes unstable and can even flip sign, so a large coefficient may be an artifact of correlated inputs rather than a real per-bathroom effect.',
  },
  {
    id: 3014,
    cat: 'Regression',
    company: 'Any',
    level: 'Junior',
    q: 'When would you use logistic regression instead of linear regression?',
    answer: 'Use logistic regression when the target is a class/probability (yes-no, churn or not, click or not), and linear regression when the target is a continuous number (price, revenue, temperature). Linear regression on a 0/1 target is a bad fit — it can predict probabilities below 0 or above 1, and its residuals violate the model\'s assumptions. Logistic regression runs the linear combination through a sigmoid so outputs stay in [0, 1] and can be read as probabilities.',
    whatsTested: 'Matching the model family to the target type (continuous vs. categorical).',
    antiPattern: 'Fitting linear regression to a binary label. It produces nonsensical out-of-range predictions and mis-modeled error structure, which is exactly why the logistic link exists.',
    staffFraming: 'A stronger candidate adds that logistic regression optimizes log-loss (cross-entropy), not squared error, and that its coefficients are log-odds — exponentiate to get an odds ratio — which is a cleaner way to explain effects to a stakeholder.',
  },
  {
    id: 3015,
    cat: 'Regression',
    company: 'Any',
    level: 'Junior',
    q: 'What is R-squared, and why can adding more features never lower it — and why is that a trap?',
    answer: 'R-squared is the fraction of variance in the target explained by the model, from 0 to 1. Adding any feature — even pure noise — can only keep R-squared the same or nudge it up, because the model can always fit the extra column a little to reduce training error. That is the trap: a rising R-squared as you pile on features does not mean the model is better; it may just be memorizing noise (overfitting). Use adjusted R-squared, which penalizes extra features, or judge on held-out data instead.',
    whatsTested: 'Understanding R-squared and why it monotonically rewards complexity on training data.',
    antiPattern: 'Chasing higher R-squared by adding features. Because it never decreases on the training set, it is not a valid model-selection criterion — it will happily reward overfitting.',
    staffFraming: 'A stronger candidate evaluates fit on held-out/cross-validated data and compares adjusted R-squared or information criteria (AIC/BIC), noting that a high training R-squared with a poor test R-squared is the signature of overfitting.',
  },
  {
    id: 3016,
    cat: 'Regression',
    company: 'Any',
    level: 'Junior',
    q: 'Explain L1 vs L2 regularization. If you wanted the model to drop irrelevant features automatically, which would you pick and why?',
    answer: 'Both add a penalty on coefficient size to fight overfitting. L2 (Ridge) penalizes the sum of squared coefficients and shrinks them smoothly toward zero but rarely to exactly zero. L1 (Lasso) penalizes the sum of absolute values and drives some coefficients exactly to zero, performing automatic feature selection. So to drop irrelevant features automatically, pick L1 — its penalty has corners at zero, so the optimum lands weights on zero, producing a sparse model.',
    whatsTested: 'Knowing the geometry and effect of L1 vs L2 and their different use cases.',
    antiPattern: 'Saying both "just prevent overfitting" without the sparsity distinction. Missing that only L1 zeroes coefficients means missing the entire reason you\'d choose it for feature selection.',
    staffFraming: 'A stronger candidate notes L1 struggles with correlated features (it arbitrarily keeps one and zeroes the rest) and reaches for Elastic Net, which blends L1 and L2 to get sparsity while distributing weight across correlated predictors.',
  },
  {
    id: 3017,
    cat: 'Regression',
    company: 'Any',
    level: 'Mid',
    q: 'You suspect the relationship between a feature and the target is not linear. What are your options before jumping to a neural network?',
    answer: 'Plenty of simpler options first. (1) Transform the feature — log, square-root, or polynomial terms let a linear model bend. (2) Bin the feature into buckets and treat it categorically to capture non-monotonic shapes. (3) Add interaction terms. (4) Use splines/GAMs for smooth non-linear fits while staying interpretable. (5) Or switch model family to a tree-based model (gradient boosting), which captures non-linearity and interactions without manual transforms. A neural net is overkill for tabular data of modest size and costs you interpretability.',
    whatsTested: 'Reaching for interpretable, lower-cost ways to model non-linearity before deep learning.',
    antiPattern: 'Defaulting to a neural network for tabular non-linearity. It adds complexity, data hunger, and opacity when a log transform, splines, or gradient boosting usually wins on tabular data.',
    staffFraming: 'A stronger candidate points out that gradient-boosted trees remain the practical default on tabular data — they handle non-linearity and interactions automatically and typically beat deep nets there — while GAMs give you the non-linear fit AND a readable per-feature shape for stakeholders.',
  },
  // ─────────────────────── Trees & Ensembles (4) ─────────────────────────
  {
    id: 3018,
    cat: 'Trees & Ensembles',
    company: 'Any',
    level: 'Junior',
    q: 'Why does a single deep decision tree overfit, and how does a Random Forest fix it?',
    answer: 'A single tree grown deep keeps splitting until leaves are nearly pure, so it memorizes noise in the training data — high variance, poor generalization. A Random Forest trains many trees on bootstrap samples of the rows AND a random subset of features at each split, then averages their predictions. The trees make different, decorrelated errors, and averaging cancels much of that variance, so the ensemble generalizes far better than any single deep tree.',
    whatsTested: 'Understanding tree overfitting and how bagging plus feature randomness reduces variance.',
    antiPattern: 'Saying a forest is better "because more trees are always better" without the variance-reduction mechanism. The point is decorrelation via row bootstrapping and per-split feature sampling, not just quantity.',
    staffFraming: 'A stronger candidate quantifies it: averaging T correlated trees gives variance ρσ² + (1−ρ)σ²/T, so the feature subsampling that lowers ρ matters more than piling on trees — and adds that forests reduce variance but do little for bias, which is why individual trees are grown deep.',
  },
  {
    id: 3019,
    cat: 'Trees & Ensembles',
    company: 'Any',
    level: 'Junior',
    q: 'Do decision trees need feature scaling (normalization/standardization)? Explain.',
    answer: 'No. Trees split on thresholds one feature at a time — "is age > 30?" — and that decision is unaffected by whether the feature is measured in years, standardized, or scaled by 1000; the ordering of values is all that matters. This is unlike distance- or gradient-based models (kNN, SVM, logistic regression, neural nets), which are sensitive to feature magnitudes and do need scaling. So for tree-based models, scaling is wasted effort.',
    whatsTested: 'Knowing which model families are scale-sensitive and why trees are invariant to monotonic scaling.',
    antiPattern: 'Blindly standardizing every feature "because preprocessing." For trees it does nothing, and reflexively applying it signals not understanding why scaling helps distance/gradient methods in the first place.',
    staffFraming: 'A stronger candidate generalizes the rule: tree models are invariant to any monotonic transform of a single feature, so log-transforming a skewed input also won\'t change a tree\'s splits — the payoff of such transforms shows up in linear and distance-based models instead.',
  },
  {
    id: 3020,
    cat: 'Trees & Ensembles',
    company: 'Any',
    level: 'Mid',
    q: 'Bagging vs boosting — what is the core difference, and which one is more prone to overfitting if you\'re not careful?',
    answer: 'Bagging (e.g., Random Forest) trains many independent models in parallel on bootstrap samples and averages them; it mainly reduces variance. Boosting (e.g., gradient boosting, XGBoost) trains models sequentially, each new one focusing on the errors of the ensemble so far; it reduces bias and can push accuracy higher. Because boosting keeps fitting the residuals, it can chase noise and overfit if you add too many rounds or set the learning rate too high — so it needs careful regularization (early stopping, shrinkage, tree-depth limits). Bagging is more robust out of the box.',
    whatsTested: 'Distinguishing the parallel/variance-reduction vs sequential/bias-reduction mechanisms and their overfitting risk.',
    antiPattern: 'Treating bagging and boosting as interchangeable "ensembles." Missing that boosting is sequential and residual-fitting means missing why it overfits more readily and needs early stopping.',
    staffFraming: 'A stronger candidate names the key boosting knobs — learning rate (shrinkage), number of trees, max depth, subsampling — and explains the classic trade: a smaller learning rate with more trees plus early stopping on a validation set usually generalizes best.',
  },
  {
    id: 3021,
    cat: 'Trees & Ensembles',
    company: 'Any',
    level: 'Mid',
    q: 'Your gradient-boosted model reports "feature X" as most important. Before telling the stakeholder X drives the outcome, what would you check?',
    answer: 'Be careful — tree "feature importance" is easy to misread. Default (gain/split-count) importances are biased toward high-cardinality and continuous features, which get more chances to split, so a noisy ID-like column can look important. Importance also doesn\'t tell you direction or whether the relationship is real versus leakage. I\'d cross-check with permutation importance on held-out data, look at SHAP values for direction and per-feature effect, and confirm X isn\'t a leaked proxy for the label before making any causal-sounding claim.',
    whatsTested: 'Skepticism about tree importance metrics and knowing more reliable attribution tools.',
    antiPattern: 'Reporting default gain importance as ground truth about what "drives" the outcome. It is biased toward high-cardinality features, ignores direction, and says nothing about causality or leakage.',
    staffFraming: 'A stronger candidate distinguishes impurity-based importance (fast but biased, computed on training data) from permutation importance (measured on held-out data, reflects real predictive value) and SHAP (consistent, per-prediction, directional) — and never equates any importance with causation.',
  },
  // ─────────────────────────── Features (4) ──────────────────────────────
  {
    id: 3022,
    cat: 'Features',
    company: 'Any',
    level: 'Junior',
    q: 'When encoding a categorical feature like "country," when do you use one-hot vs label (integer) encoding?',
    answer: 'One-hot encoding makes a 0/1 column per category — use it for nominal features with no order (country, color) in linear/distance-based models, because label encoding would falsely imply Germany(3) > France(2) and inject a fake ordering the model would exploit. Label (integer) encoding is fine for ORDINAL features that have a real order (low < medium < high) or for tree-based models, which split on thresholds and don\'t assume the integers are on a meaningful scale. Watch out for high-cardinality features — one-hot explodes the dimensionality.',
    whatsTested: 'Choosing an encoding that respects whether a category is ordinal and the model family.',
    antiPattern: 'Label-encoding a nominal feature for a linear model. It invents a numeric ordering the model treats as real, so "country = 5" gets treated as bigger than "country = 2," corrupting the fit.',
    staffFraming: 'A stronger candidate handles high cardinality with target/mean encoding (with cross-fold smoothing to avoid leakage) or hashing/embeddings, and knows one-hot is fine for trees too but wastes splits, so integer encoding is often preferred there.',
  },
  {
    id: 3023,
    cat: 'Features',
    company: 'Any',
    level: 'Junior',
    q: 'You have a feature that is missing for 30% of rows. A teammate says "just fill it with the column mean." What could go wrong?',
    answer: 'Mean imputation is a common default but has real downsides. It shrinks the feature\'s variance and distorts its distribution (a spike at the mean), it ignores that the value might be Missing-Not-At-Random — the fact that it\'s missing may itself be informative — and it can bias relationships with other features. Better options: add a binary "was_missing" indicator alongside the imputed value so the model can learn from the missingness, use median for skewed data, or use a model-based imputation. And always fit the imputer on the training set only.',
    whatsTested: 'Understanding the pitfalls of naive imputation and that missingness can be signal.',
    antiPattern: 'Reflexively mean-imputing and moving on. It distorts variance and throws away the possibly-informative fact that the value was missing, and if fit on the full data it leaks.',
    staffFraming: 'A stronger candidate asks WHY the data is missing (MCAR vs MAR vs MNAR) — if missingness correlates with the target, the "was_missing" flag can be one of your strongest features, and the imputation strategy should follow the mechanism, not a default.',
  },
  {
    id: 3024,
    cat: 'Features',
    company: 'Any',
    level: 'Mid',
    q: 'A model uses "average_purchase_amount_last_30_days" and performs amazingly offline but poorly live. What feature-engineering mistake would you suspect?',
    answer: 'I\'d suspect data leakage / a look-ahead feature. If "last 30 days" was computed relative to a fixed dataset snapshot rather than relative to each prediction\'s point-in-time, it can include information from AFTER the moment we\'d actually predict — the offline feature "sees the future." At serving time you only have data up to now, so the feature is weaker or computed differently, and performance collapses. The fix is point-in-time correct feature computation: build features using only data available as of the prediction timestamp, ideally via a feature store or time-based joins.',
    whatsTested: 'Diagnosing temporal/look-ahead leakage in time-windowed features and the train/serving gap.',
    antiPattern: 'Assuming the model "just needs retraining." The offline-online gap here is a leakage bug in how the time window was built, so retraining on the same leaky feature reproduces the same fake performance.',
    staffFraming: 'A stronger candidate insists on point-in-time joins and a single feature-computation path shared by training and serving (a feature store), so the same code produces the feature offline and online — eliminating both look-ahead leakage and train/serving skew at once.',
  },
  {
    id: 3025,
    cat: 'Features',
    company: 'Any',
    level: 'Mid',
    q: 'You standardize your features for a logistic regression. Do you fit the scaler on all the data before splitting, or after? Why does it matter?',
    answer: 'Fit the scaler on the TRAINING split only, then apply those learned parameters (mean/std) to transform the validation and test sets. If you fit on all the data first, the scaler\'s mean and standard deviation are computed using test rows — so information from the test set leaks into training, and your evaluation is optimistically biased. The same rule applies to any fitted preprocessing: imputation, target encoding, feature selection. Inside cross-validation, refit the scaler within each fold.',
    whatsTested: 'Preventing preprocessing leakage by fitting transforms only on training data.',
    antiPattern: 'Scaling (or imputing/encoding) on the full dataset before splitting. It quietly leaks test statistics into training, inflating offline metrics that won\'t hold up in production.',
    staffFraming: 'A stronger candidate wraps preprocessing and the model in a single Pipeline so the scaler is refit inside every cross-validation fold automatically — making leakage structurally impossible rather than a thing you have to remember.',
  },
  // ─────────────────────────── Coding (3) ────────────────────────────────
  {
    id: 3026,
    cat: 'Coding',
    company: 'Any',
    level: 'Junior',
    q: 'In pandas, you have a DataFrame df with columns "user_id" and "amount." Write code to get total amount per user, sorted highest first, and explain it.',
    answer: 'df.groupby("user_id")["amount"].sum().sort_values(ascending=False). Read it left to right: groupby("user_id") buckets rows by user; selecting ["amount"] limits the aggregation to that column (so you get a clean Series, not every column); .sum() totals within each group; .sort_values(ascending=False) orders users by total, biggest first. If you want a DataFrame back with user_id as a column rather than the index, add .reset_index().',
    whatsTested: 'Basic pandas group-by aggregation fluency and understanding each step.',
    antiPattern: 'Looping over rows in Python to accumulate per-user totals. It is slow and non-idiomatic — vectorized groupby is both faster and clearer, and hand-rolling it signals unfamiliarity with the library.',
    staffFraming: 'A stronger candidate reaches for .agg() when multiple aggregations are needed (e.g., sum and count together), knows groupby drops NaN keys by default, and uses reset_index() deliberately depending on whether downstream code expects the group key as an index or a column.',
  },
  {
    id: 3027,
    cat: 'Coding',
    company: 'Any',
    level: 'Junior',
    q: 'Write a function that returns True if a string is a palindrome, ignoring case. Then tell me its time and space complexity.',
    answer: 'def is_palindrome(s): s = s.lower(); return s == s[::-1]. Lowercase the string, then compare it to its reverse; if they match it reads the same both ways. Time complexity is O(n) — building the reversed copy and comparing both scan the n characters. Space is O(n) because s[::-1] creates a new reversed string. A two-pointer version (walk from both ends inward) is also O(n) time but O(1) extra space since it compares in place.',
    whatsTested: 'Basic string manipulation plus correctly reasoning about time and space complexity.',
    antiPattern: 'Giving the slice solution but saying it is O(1) space, or being unable to state complexity at all. The reversed slice allocates a new string, so it is O(n) space — the two-pointer variant is what gets you to O(1).',
    staffFraming: 'A stronger candidate proactively asks about edge cases and scope — empty string (usually True), whether to strip non-alphanumeric characters and spaces ("A man a plan..."), Unicode — and offers the O(1)-space two-pointer version when memory matters.',
  },
  {
    id: 3028,
    cat: 'Coding',
    company: 'Any',
    level: 'Mid',
    q: 'You need to count occurrences of each word in a large list. Walk me through your approach and why you\'d avoid a naive one.',
    answer: 'Use a hash map: collections.Counter(words), or a defaultdict(int) incrementing per word — both are O(n) single-pass over the list, since each insert/lookup in a dict is O(1) on average. The naive approach to avoid is, for each unique word, scanning the whole list with list.count() — that is O(n) per word and O(n·k) overall, which blows up on large inputs. The hash-map approach trades a bit of memory (O(k) for k unique words) for linear time.',
    whatsTested: 'Choosing a hash-map counting approach and articulating the time/space trade vs. a quadratic naive one.',
    antiPattern: 'Calling list.count() in a loop over unique words. It re-scans the list for every word, turning an O(n) job into O(n·k) — fine on toy inputs, disastrous at scale.',
    staffFraming: 'A stronger candidate mentions Counter\'s built-in most_common(k) for top-k, and that if the data doesn\'t fit in memory the right answer shifts to a streaming/count-min-sketch approach or a distributed map-reduce word count rather than a single dict.',
  },
  // ─────────────────────────── SQL (2) ───────────────────────────────────
  {
    id: 3029,
    cat: 'SQL',
    company: 'Any',
    level: 'Junior',
    q: 'Write a query for the top 5 products by total revenue from an "orders" table (columns: product_id, revenue). Then: why is WHERE vs HAVING a common gotcha here?',
    answer: 'SELECT product_id, SUM(revenue) AS total_revenue FROM orders GROUP BY product_id ORDER BY total_revenue DESC LIMIT 5. Group by product, sum revenue per group, order descending, take the top 5. The WHERE-vs-HAVING gotcha: WHERE filters individual rows BEFORE grouping, so it can\'t reference an aggregate like SUM(revenue) — that\'s illegal. To filter on the aggregated total (say, only products with SUM(revenue) > 1000), you must use HAVING, which runs AFTER the GROUP BY.',
    whatsTested: 'Group-by aggregation with ordering/limit and the WHERE-before-group vs HAVING-after-group rule.',
    antiPattern: 'Putting an aggregate condition in WHERE (WHERE SUM(revenue) > 1000). It errors because WHERE is evaluated per-row before aggregation exists — aggregate filters belong in HAVING.',
    staffFraming: 'A stronger candidate knows the logical order of evaluation (FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT), which explains why SELECT aliases usually can\'t be used in WHERE or HAVING but can in ORDER BY, and why ties at the 5th spot may need a window function (RANK) instead of LIMIT.',
  },
  {
    id: 3030,
    cat: 'SQL',
    company: 'Any',
    level: 'Mid',
    q: 'You join a "users" table to an "orders" table and the row count is higher than the number of users. A teammate expected one row per user. What happened, and how would you fix it?',
    answer: 'A one-to-many join fanned out the rows: each user has multiple orders, so an INNER/LEFT JOIN produces one row per matching order, not per user — that\'s expected, not a bug. If the goal is one row per user with, say, their order count or total spend, you aggregate: join then GROUP BY user, or pre-aggregate orders in a subquery (SELECT user_id, COUNT(*) ... GROUP BY user_id) before joining. If instead you truly expected one order per user and got duplicates, check the join key for non-uniqueness or an unintended many-to-many.',
    whatsTested: 'Understanding join cardinality (fan-out) and how to collapse to the intended grain with aggregation.',
    antiPattern: 'Slapping SELECT DISTINCT on the result to "remove duplicates." It hides the fan-out instead of understanding it, silently drops legitimately distinct order rows, and gives wrong sums/counts once you aggregate.',
    staffFraming: 'A stronger candidate reasons explicitly about the grain of each table and the resulting join, pre-aggregates the many-side to the target grain before joining (avoiding double-counted metrics), and checks join-key uniqueness to catch an unintended many-to-many that would multiply rows further.',
  },
];

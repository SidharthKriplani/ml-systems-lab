// data.js — normalized JUDGE drills for subject 'data' (feature engineering / leakage / skew / windows).
// Migrated from src/tabs/FeatureEngTab.jsx. SIM widgets (Feature Store Designer,
// Window Aggregation Builder, Architecture Diagram) are intentionally excluded — they
// configure/generate, they don't pose a judgment call. Reveal-card modules with a clear
// correct verdict were converted to 'mcq'; the training-serving skew set, which has no
// single right answer across its four bugs, is kept as a 'rubric'.

export const DATA_DRILLS = [
  // ─── Training-Serving Skew (SkewSimulator BUGS — reveal cards, no single right answer) ───
  {
    id: 'data-skew-simulator',
    subject: 'data', subtopic: 'skew', level: 'senior', type: 'rubric',
    title: 'Four training-serving skew bugs — which hurts most, and why',
    context: [
      'Future data leak — training: avg(purchase_amount OVER last_7_days); serving: same window but WHERE ts <= NOW(). Training window accidentally includes future events; at serve time future data does not exist, so the feature distribution shifts.',
      'Different null handling — training: df["age"].fillna(df["age"].mean()) (~34); serving: if age is None: age = 0. A 0-age customer looks like a child to the model.',
      'Scaler fitted on wrong data — training: scaler.fit_transform(X_train); serving: scaler.fit_transform([single_row]) re-fitting every request. A single row has mean = its own value, std → 0.',
      'Window timezone mismatch — training: calendar-day window (midnight boundaries); serving: rolling 7×24h window (UTC vs local). At midnight, serving may include 8 days while training expected 7.',
    ],
    question: 'Rank these four training-serving skew bugs by production impact and give the fix for each. Which is the single most severe, and why?',
    diagnosis: 'The scaler re-fit-per-request bug is the most severe: every scaled feature is wrong for every request, so the model is effectively random (Critical). The future-data leak is next (High — AUC drops 8–15pp once the leaked signal disappears at serve time). Different null handling is medium-high (affects the ~12% of traffic with missing age). The timezone/window mismatch is low-medium but the hardest to detect — metrics look fine while behaviour is subtly wrong.',
    explanation: 'Zero-variance or single-row transforms corrupt every prediction, so scaler re-fitting dominates. Distribution-shift bugs (leak, null handling) degrade a slice of traffic. Boundary/timezone bugs are insidious precisely because offline metrics stay green — the damage is silent.',
    fix: 'Scaler: serialise the fitted scaler (joblib.dump) and call transform() at serve time, never fit_transform(). Future leak: use RANGE/ROWS BETWEEN N PRECEDING AND CURRENT ROW with explicit ts < event_ts; unit-test feature values against known-good history. Null handling: store training-time imputation values as artefacts and load them at serving. Timezone: standardise all timestamps to UTC at ingest, define windows in code with explicit UTC boundaries, add per-feature PSI monitoring.',
    levels: {
      ic3: 'Notices one bug (usually the visible null-handling one) and patches it in the serving code, re-introducing skew.',
      ic5: 'Ranks the four correctly, identifies scaler re-fitting as Critical, and gives the store-fitted-artefact fix for each.',
      staff: 'Treats all four as instances of one root cause — training and serving compute features by different code paths. Mandates a shared feature-transformation library + offline/online parity tests in CI, so skew fails the build instead of shipping.',
    },
    source: 'FeatureEng · Training-Serving Skew Simulator',
  },

  // ─── Feature Leakage Zoo (LEAKAGE_SCENARIOS — reveal cards, clear diagnosis per type) ───
  {
    id: 'data-leakage-target',
    subject: 'data', subtopic: 'leakage', level: 'mid', type: 'mcq',
    title: 'Fraud model: is_flagged_by_ops as a feature — Training AUC 0.99, prod 0.62',
    context: [
      'Predicting fraud, the feature set includes `is_flagged_by_ops`, which ops sets *after* the fraud review.',
      'Training AUC 0.99, production AUC 0.62. Severity: Critical.',
    ],
    question: 'Which leakage pattern is this, and what is the fix?',
    options: [
      'Group / split leakage — the same entity is in both train and test; use GroupKFold',
      'Target leakage — a feature is populated after the label event; enforce a strict point-in-time join',
      'Imputation leakage — impute stats leaked from test rows; fit imputers on train only',
      'Meta-feature leakage — a row-id/index encodes split identity; drop it before training',
    ],
    answer: 1,
    diagnosis: 'Target leakage. `is_flagged_by_ops` is set after the fraud review, so it is label-adjacent — the model only works when that post-hoc signal is available, which it is not at prediction time in production.',
    explanation: 'A feature with implausibly high importance whose generation timestamp is after the label timestamp is the classic target-leakage signature. Offline AUC is inflated; production collapses the moment the label-adjacent feature is unavailable.',
    fix: 'Enforce a strict point-in-time join: every feature must be computable using only data available before the prediction event. Audit feature-generation timestamps vs label timestamps.',
    source: 'FeatureEng · Leakage Zoo (target)',
  },
  {
    id: 'data-leakage-temporal',
    subject: 'data', subtopic: 'leakage', level: 'senior', type: 'mcq',
    title: 'Rolling 7-day avg computed with future rows — backtest great, live loses money',
    context: [
      'A rolling 7-day average is computed using future rows because of a wrong sort order before the window.',
      'Backtest P&L looks great; live trading loses money from day 1. No error thrown. Severity: Critical.',
    ],
    question: 'What is the leakage pattern and how do you detect it?',
    options: [
      'Aggregation leakage — target mean-encoding includes the current row; use out-of-fold encoding',
      'Temporal leakage — a time-window feature consumes future rows; recompute row-by-row in time order and diff against the batch values',
      'Proxy label leakage — a lagging indicator stands in for the label; use only leading indicators',
      'Join fanout leakage — a slowly-changing dim is joined without snapshotting; use SCD Type 2',
    ],
    answer: 1,
    diagnosis: 'Temporal leakage. The window pulls in rows from the future because the data was not correctly ordered by time before the window function ran. Backtests look great because future information is embedded in each feature; live trading has no future, so it fails immediately.',
    explanation: 'Sort the dataset by time and recompute features row-by-row, then compare against the batch-computed values — any mismatch is temporal leakage. A no-error-but-loses-money-from-day-1 profile is the giveaway.',
    fix: 'Compute time-based features with explicit ORDER BY + ROWS BETWEEN N PRECEDING AND CURRENT ROW. Avoid RANGE unless you fully understand its boundary semantics.',
    source: 'FeatureEng · Leakage Zoo (temporal)',
  },
  {
    id: 'data-leakage-group',
    subject: 'data', subtopic: 'leakage', level: 'mid', type: 'mcq',
    title: 'User appears in both train and test — offline strong, prod 10–15pp lower',
    context: [
      'A recommendation model random-splits interactions; the same user appears in both train and test, so the model memorises user-level signal.',
      'Offline eval looks strong; production metrics are 10–15pp lower and an A/B test shows no lift. Severity: High.',
    ],
    question: 'Which leakage pattern is this, and what is the correct fix?',
    options: [
      'Group / split leakage — entities span train and test; use group-aware splits (GroupKFold, entity/time split), never random-split rows that share an entity',
      'Target leakage — a post-label feature is included; enforce a point-in-time join',
      'Meta-feature leakage — a dataset-id feature leaks split identity; drop index columns',
      'Imputation leakage — imputation stats saw the full dataset; fit on train only',
    ],
    answer: 0,
    diagnosis: 'Group / split leakage. When rows share an entity (a user) and you random-split, the model memorises user-level signal that appears in both train and test. Offline metrics look strong; production and A/B tests reveal there was no generalisable lift.',
    explanation: 'Check the overlap between train and test entity IDs. For recommendation systems you must split by user, not by interaction — otherwise the eval is measuring memorisation.',
    fix: 'Use group-aware splits (GroupKFold, time-based split, entity-based split). Never random-split when rows share an entity.',
    source: 'FeatureEng · Leakage Zoo (group)',
  },
  {
    id: 'data-leakage-proxy',
    subject: 'data', subtopic: 'leakage', level: 'senior', type: 'mcq',
    title: 'Churn model uses support_tickets_last_30d — fine at launch, degrades over time',
    context: [
      'Predicting churn using `support_tickets_last_30d`; tickets spike because the user is already churning.',
      'Performance is fine at launch, then degrades as behaviour and ticket patterns shift. Severity: High.',
    ],
    question: 'What is the leakage pattern, and what distinction fixes it?',
    options: [
      'Group leakage — split by user instead of interaction',
      'Proxy label leakage — a lagging indicator (caused by the outcome) is used as a feature; keep only leading indicators available before the outcome',
      'Aggregation leakage — target encoding includes the current row; use out-of-fold estimates',
      'Meta-feature leakage — drop dataset-identity columns',
    ],
    answer: 1,
    diagnosis: 'Proxy label leakage. Support tickets spike *because* the user is already churning — the feature is a lagging indicator correlated with the outcome but caused by it, not a leading risk signal.',
    explanation: 'Plot feature values stratified by label; near-perfect separation is suspicious. You must understand why a feature separates the classes so cleanly — here it is because the behaviour is downstream of the outcome.',
    fix: 'Distinguish leading indicators (available before the outcome) from lagging indicators (correlated with but caused by the outcome). Use only leading indicators.',
    source: 'FeatureEng · Leakage Zoo (proxy)',
  },
  {
    id: 'data-leakage-aggregation',
    subject: 'data', subtopic: 'leakage', level: 'mid', type: 'mcq',
    title: 'Mean-encoding the target across all rows including the current row',
    context: [
      'A target-encoded feature is built by mean-encoding the target across all rows, including the current row.',
      'Target-encoded features look great in training; at serving the encoding lookup does not exist and silently falls back to the global mean. Severity: Medium.',
    ],
    question: 'Which leakage pattern is this, and how should the encoding be computed?',
    options: [
      'Temporal leakage — reorder by time before windowing',
      'Aggregation leakage — the encoding uses the target of the current row; use out-of-fold target encoding (fit on k-1 folds, encode fold k) and a held-out global estimate at serving',
      'Join fanout leakage — snapshot the dimension table',
      'Group leakage — use GroupKFold',
    ],
    answer: 1,
    diagnosis: 'Aggregation leakage. Mean-encoding that includes the current row lets each row see its own label through the encoded value, inflating training metrics; at serving the per-key lookup is missing and silently falls back to the global mean, so the feature behaves differently in production.',
    explanation: 'Check whether the encoding uses the target column. If it does, it must be computed with out-of-fold estimates only, never on the full data including the row being encoded.',
    fix: 'Use out-of-fold target encoding: fit on k-1 folds and encode fold k. At serving time use only the held-out global estimate.',
    source: 'FeatureEng · Leakage Zoo (aggregation)',
  },
  {
    id: 'data-leakage-meta',
    subject: 'data', subtopic: 'leakage', level: 'mid', type: 'mcq',
    title: 'Row index / file path / dataset id used as a feature',
    context: [
      'The feature set includes a row index, file path, or dataset ID; the model learns dataset-split identity.',
      'The model appears to generalise during CV, fails on a new data source, and row_id shows up in the top-10 feature importances. Severity: Medium.',
    ],
    question: 'What is the leakage pattern and the fix?',
    options: [
      'Proxy leakage — replace with a leading indicator',
      'Meta-feature leakage — an auto-generated/index column encodes dataset identity; drop row IDs, file metadata, split timestamps, and data-source identifiers before training',
      'Imputation leakage — fit imputers on train only',
      'Temporal leakage — order by time before windowing',
    ],
    answer: 1,
    diagnosis: 'Meta-feature leakage. Row IDs, file paths, and dataset identifiers correlate with the split or source and let the model learn dataset identity rather than signal — it looks fine in CV and fails on a genuinely new source.',
    explanation: 'row_id appearing in the top feature importances is the tell. Audit all features, including auto-generated and index columns, and drop anything that encodes dataset identity.',
    fix: 'Before training, explicitly drop: row IDs, file metadata, timestamps used for splitting, and data-source identifiers.',
    source: 'FeatureEng · Leakage Zoo (meta)',
  },
  {
    id: 'data-leakage-join-fanout',
    subject: 'data', subtopic: 'leakage', level: 'senior', type: 'mcq',
    title: 'Left-join events to a slowly-changing dim without snapshotting',
    context: [
      'Historical events are left-joined to a slowly-changing dimension without snapshotting, so historical rows get today\'s dim value.',
      'The model trained on historical events uses current attribute values; serving matches training only because serving also uses current values. Severity: Medium.',
    ],
    question: 'Which leakage pattern is this, and how do you join correctly?',
    options: [
      'Aggregation leakage — use out-of-fold target encoding',
      'Join fanout leakage — a slowly-changing dim is joined without a snapshot; use SCD Type 2 and join on (entity_id, event_ts) with dim.valid_from <= event_ts < dim.valid_to',
      'Group leakage — split by entity',
      'Meta-feature leakage — drop dataset-id columns',
    ],
    answer: 1,
    diagnosis: 'Join fanout leakage. Joining historical events to a dimension without snapshotting stamps today\'s attribute values onto old events. It hides in eval because serving also uses current values, but the feature no longer reflects what was true at event time.',
    explanation: 'Compare feature distributions for old vs new events; if old events carry "current" attribute values, you have fanout leakage.',
    fix: 'Snapshot dimension tables or use SCD Type 2. Always join on (entity_id, event_ts) with dim.valid_from <= event_ts < dim.valid_to.',
    source: 'FeatureEng · Leakage Zoo (join_fanout)',
  },
  {
    id: 'data-leakage-imputation',
    subject: 'data', subtopic: 'leakage', level: 'mid', type: 'mcq',
    title: 'Impute missing values with stats computed on the full dataset (incl. test)',
    context: [
      'Missing values are imputed using statistics computed on the full dataset, including test rows.',
      'Cross-validation shows stable metrics; on future data the null handling differs and the score distribution shifts. Severity: Low-Medium.',
    ],
    question: 'What is the leakage pattern, and where must imputation statistics be computed?',
    options: [
      'Imputation leakage — impute stats saw test/full data; fit imputers on the train split only, store them as artifacts, and apply them at serving',
      'Target leakage — enforce a point-in-time join',
      'Group leakage — use GroupKFold',
      'Join fanout leakage — snapshot the dimension',
    ],
    answer: 0,
    diagnosis: 'Imputation leakage. Computing mean/median/mode on the full dataset lets test-row information into the imputed values, so CV looks stable but future data (which the stats never saw) shifts the score distribution.',
    explanation: 'Trace where imputation statistics come from — they should only ever see training data. Fit inside a Pipeline or explicitly on the train split.',
    fix: 'Fit imputers inside a Pipeline or on the train split only. Store fitted imputers as artifacts; serving loads and applies them.',
    source: 'FeatureEng · Leakage Zoo (imputation)',
  },

  // ─── Online vs Offline Feature Decision (ONLINE_OFFLINE_SCENARIOS — clear verdict) ───
  {
    id: 'data-serving-reco-ctr',
    subject: 'data', subtopic: 'feature-eng', level: 'senior', type: 'mcq',
    title: 'Real-time recommendation CTR — where do the features live?',
    context: [
      'Real-time recommendation CTR model. Latency <50ms, freshness in seconds, high load, medium complexity.',
      'The model needs fresh user session context (last click, dwell time).',
    ],
    question: 'What feature-serving architecture fits this workload?',
    options: [
      'Offline batch only (data warehouse) — no latency constraint',
      'Online feature store (Redis/DynamoDB) — pre-compute aggregates in streaming (Flink/Kafka) and push to an online store',
      'Precompute + edge cache (CDN) with query-time personalization only',
      'Offline embedding index + online vector search',
    ],
    answer: 1,
    diagnosis: 'Online feature store. CTR needs fresh session context and a sub-50ms budget leaves no room for batch reads, so aggregates are pre-computed in a streaming engine and pushed to an online store.',
    explanation: 'The sub-50ms budget rules out any synchronous batch read. Pre-compute in streaming (Flink/Kafka), serve from Redis/DynamoDB.',
    fix: 'Anti-pattern to avoid: calling a feature-computation service synchronously at inference — it adds 20-40ms and a new failure mode. Tags: Redis, Flink, Feast.',
    source: 'FeatureEng · Online vs Offline (reco_ctr)',
  },
  {
    id: 'data-serving-fraud-txn',
    subject: 'data', subtopic: 'feature-eng', level: 'staff', type: 'mcq',
    title: 'Fraud scoring at payment — needs fresh AND historical signals',
    context: [
      'Fraud scoring at payment. Latency <100ms, freshness in seconds, very high load, high complexity.',
      'Needs fresh signals (last 5 transactions, velocity) and historical signals (30-day spend pattern).',
    ],
    question: 'What feature-serving architecture fits?',
    options: [
      'Compute velocity counts in the serving path at request time',
      'Dual-write: pre-compute historical nightly, stream velocity features in real-time, and join at serving from the online store only',
      'Offline batch only — score nightly in the warehouse',
      'Precompute + CDN edge cache',
    ],
    answer: 1,
    diagnosis: 'Dual-write. Fraud needs both real-time velocity and historical spend patterns, so you pre-compute history nightly, stream velocity in real time, and read both from the online store at serving.',
    explanation: 'Computing velocity in the serving path gives unbounded latency under spike traffic — one slow DB query times out the payment.',
    fix: 'Anti-pattern to avoid: computing velocity counts synchronously in the serving path. Tags: Dual-write, Kafka, Redis, Snowflake.',
    source: 'FeatureEng · Online vs Offline (fraud_txn)',
  },
  {
    id: 'data-serving-batch-risk',
    subject: 'data', subtopic: 'feature-eng', level: 'senior', type: 'mcq',
    title: 'Monthly credit risk scoring — how much infra do you need?',
    context: [
      'Monthly credit risk scoring. Latency in hours, freshness daily, low load, high complexity.',
      'Complex features (12-month payment history, peer comparisons) are expensive to compute.',
    ],
    question: 'What feature-serving architecture fits?',
    options: [
      'Online feature store (Redis) for sub-ms reads',
      'Offline batch only (data warehouse) — run the expensive features once nightly; no online store needed',
      'Dual-write to online + offline stores',
      'Precompute + CDN edge cache',
    ],
    answer: 1,
    diagnosis: 'Offline batch only. Monthly scoring has no latency constraint and the features are expensive, so you run them once nightly in the warehouse — an online store adds operational overhead with zero latency benefit.',
    explanation: 'Over-engineering feature serving is the expensive mistake here: building online infra for a batch use case buys nothing.',
    fix: 'Anti-pattern to avoid: building an online store for a batch use case. Tags: Snowflake, dbt, Spark.',
    source: 'FeatureEng · Online vs Offline (batch_risk)',
  },
  {
    id: 'data-serving-search-rank',
    subject: 'data', subtopic: 'feature-eng', level: 'senior', type: 'mcq',
    title: 'Search result ranking — mixing slow and query-time features',
    context: [
      'Search result ranking. Latency <200ms, freshness in hours, high load, medium complexity.',
      'Query-independent features (document popularity, quality) change slowly; query-dependent ones (BM25, personalization) are needed per request.',
    ],
    question: 'What feature-serving architecture fits?',
    options: [
      'Recompute document-level features at query time for freshness',
      'Precompute + cache (CDN/local cache): precompute slow query-independent features daily; compute query-dependent features at request time from precomputed signals',
      'Offline batch only — hours of latency is fine',
      'Dual-write to online and offline stores',
    ],
    answer: 1,
    diagnosis: 'Precompute + cache. Slow-changing, query-independent features are precomputed daily and cached at the edge; query-dependent features (BM25, personalization) are computed at request time from those precomputed signals.',
    explanation: 'Recomputing document-level features per request is O(docs) per request and kills latency.',
    fix: 'Anti-pattern to avoid: recomputing document-level features at query time. Tags: CDN cache, Elasticsearch, Redis.',
    source: 'FeatureEng · Online vs Offline (search_rank)',
  },
  {
    id: 'data-serving-churn-pred',
    subject: 'data', subtopic: 'feature-eng', level: 'mid', type: 'mcq',
    title: 'Daily churn propensity — consumed by CRM, not real-time serving',
    context: [
      'Daily churn propensity. Latency in minutes, freshness daily, medium load, high complexity.',
      'Scores are consumed by CRM tools, not real-time serving.',
    ],
    question: 'What feature-serving architecture fits?',
    options: [
      'Serve churn scores through an API with live feature computation',
      'Offline batch + feature snapshot table — run the full pipeline nightly, write scores + a feature snapshot to a table that downstream systems read',
      'Online feature store (Redis) for sub-ms reads',
      'Offline embedding index + online vector search',
    ],
    answer: 1,
    diagnosis: 'Offline batch + snapshot table. Churn scores feed CRM tools, so you run the full feature pipeline nightly and write scores plus a feature snapshot to a table that downstream systems read.',
    explanation: 'A daily-fresh model gains nothing from live feature computation behind an API — you would pay for online infra with no benefit.',
    fix: 'Anti-pattern to avoid: serving daily-fresh churn scores through an API with live feature computation. Tags: Airflow, dbt, BigQuery.',
    source: 'FeatureEng · Online vs Offline (churn_pred)',
  },
  {
    id: 'data-serving-llm-rag',
    subject: 'data', subtopic: 'feature-eng', level: 'senior', type: 'mcq',
    title: 'LLM retrieval-augmented generation — where do embeddings get computed?',
    context: [
      'LLM RAG. Latency <500ms, freshness in hours-days, medium load, high complexity.',
      'Document embeddings are expensive to compute but change infrequently.',
    ],
    question: 'What feature-serving architecture fits?',
    options: [
      'Recompute document embeddings at query time',
      'Offline embedding index + online vector search — batch-compute document embeddings, index them in a vector DB, then embed the query online and search the precomputed index',
      'Offline batch only — no online component',
      'Dual-write velocity features to an online store',
    ],
    answer: 1,
    diagnosis: 'Offline embedding index + online vector search. Document embeddings are computed in batch and indexed in a vector DB (Pinecone, Weaviate, pgvector); at query time you embed the user query online and search the precomputed index.',
    explanation: 'Recomputing document embeddings at query time is 100ms+ per doc × corpus size — unusable latency.',
    fix: 'Anti-pattern to avoid: recomputing document embeddings at query time. Tags: Pinecone, pgvector, Batch embed.',
    source: 'FeatureEng · Online vs Offline (llm_rag)',
  },

  // ─── Feature Store Time-Travel Bug (FEATURE_STORE_SCENARIOS — AccordionMCQ) ───
  {
    id: 'data-featstore-pit-join',
    subject: 'data', subtopic: 'windows', level: 'mid', type: 'mcq',
    title: 'Point-in-time join returns future features',
    context: [
      'A credit risk model is trained via a feature store with point-in-time correct joins. Training spans 24 months; each row is a loan application with a label (default/no-default) known 90 days after application. The store is queried with as_of = application_date.',
      'Offline evaluation shows AUC = 0.89 — much higher than the previous model\'s 0.81.',
    ],
    question: 'The 8-point AUC jump on a "point-in-time correct" model is unusually large. What single check determines whether the lift is real or leakage before you promote it?',
    options: [
      'Verify that AUC > 0.85 on the held-out test set.',
      'Confirm the point-in-time join uses application_date, not label_date. If it joins on any timestamp derived from the outcome window, post-default features become available at training time — look-ahead leakage disguised as feature-store correctness.',
      'Retrain with a larger dataset to confirm the AUC holds.',
      'Check that the feature store has no missing values for the training period.',
    ],
    answer: 1,
    diagnosis: 'Point-in-time correctness is only as good as the timestamp used. If the store accidentally joins on label_available_date instead of application_date, post-outcome features (e.g., account behaviour after default) enter training. An 8-point AUC jump is a red flag — legitimate feature gains rarely produce lift this large without leakage.',
    explanation: 'Feature-store "point-in-time correctness" is a promise about the join key, not a guarantee. The join key must be the event timestamp, never a timestamp derived from the label window.',
    fix: 'Audit every feature\'s computation timestamp against the join event timestamp: verify each feature used only pre-application data and that the join key is the application event timestamp, not a derived one. A temporal holdout (train months 1–18, evaluate 19–24) should match the full-period AUC if no leakage exists.',
    source: 'FeatureEng · Feature Store Time-Travel (fst1)',
  },
  {
    id: 'data-featstore-staleness',
    subject: 'data', subtopic: 'skew', level: 'mid', type: 'mcq',
    title: 'Feature store staleness in real-time serving',
    context: [
      'A real-time fraud model reads from an online feature store. user_transaction_count_24h refreshes every 5 minutes via streaming. The model was trained with this feature reflecting exact counts at prediction time.',
      'In production the false-negative rate is 40% higher than offline evaluation predicted. Card-testing attacks complete in under 2 minutes.',
    ],
    question: 'False negatives are 40% higher than offline predicted. What is failing, and why did offline eval miss it?',
    options: [
      'The model was trained on too little data.',
      'The online store has a 5-minute lag; for sub-2-minute card-testing attacks, user_transaction_count_24h is stale at prediction time. The model trained on exact counts but serves on lagged counts — systematic offline-online skew.',
      'False negative rate increases as fraud patterns evolve.',
      'The feature store needs to be replaced with a real-time database.',
    ],
    answer: 1,
    diagnosis: 'Training-serving skew from feature staleness. The model learned that a high user_transaction_count_24h predicts card testing, but in production the store still shows the count from 5 minutes ago, so the fraud signal is invisible at serving time.',
    explanation: 'Offline eval used exact counts, so it never saw the lag. The distribution the model serves on differs from the one it trained on precisely because of the refresh interval.',
    fix: 'Measure actual feature staleness at serving (log the delta between feature-computation time and serving time). If staleness exceeds the attack window, add a direct DB lookup at inference for high-velocity features, bypassing the cache. In training, inject realistic staleness by lagging feature values by the observed serving lag so the model trains on the production distribution.',
    source: 'FeatureEng · Feature Store Time-Travel (fst2)',
  },
  {
    id: 'data-featstore-backfill',
    subject: 'data', subtopic: 'windows', level: 'senior', type: 'mcq',
    title: 'Backfill overwrites historical point-in-time features',
    context: [
      'The data engineering team backfills 6 months of user_lifetime_value using an improved computation, writing the updated values into the feature store under the original event timestamps.',
      'A model trained last month and currently in production was promoted based on offline eval that used the old LTV values.',
    ],
    question: 'The backfill overwrote 6 months of LTV history under the original timestamps. What has actually broken?',
    options: [
      'The model will need to be retrained to use the improved LTV computation.',
      'The backfill invalidates the offline evaluation that justified promotion. If the backfilled values differ from the originals, the model was selected/tuned on a feature distribution that no longer exists in the store, so online-performance predictions are now unreliable.',
      'Backfills have no effect on models already deployed to production.',
      'The model\'s AUC will improve because LTV computation is now more accurate.',
    ],
    answer: 1,
    diagnosis: 'Immutability violation. A feature store should be append-only for historical data. Backfilling updated values under the original timestamps breaks the audit trail and invalidates every model evaluation that used those historical features — you can no longer reproduce the training/eval dataset the model was promoted on.',
    explanation: 'The promotion decision was made against a specific feature distribution. Overwriting history means that distribution is gone, so the offline eval that justified the model is no longer reproducible.',
    fix: 'Make historical records immutable: backfills should write new records with a backfill_timestamp alongside the original event_timestamp, not overwrite. Retrain and re-evaluate on the new backfilled data explicitly; keep the deployed model\'s original evaluation reproducible via the original values.',
    source: 'FeatureEng · Feature Store Time-Travel (fst3)',
  },

  // ─── Interaction Features & Leakage (INTERACTION_LEAKAGE_SCENARIOS — AccordionMCQ) ───
  {
    id: 'data-interaction-ratio-leak',
    subject: 'data', subtopic: 'leakage', level: 'junior', type: 'mcq',
    title: 'Manual interaction feature introduces label leakage',
    context: [
      'For churn prediction, an engineer adds support_calls_per_dollar_spent = support_call_count / revenue_30d.',
      'This interaction tests as the #1 feature by SHAP importance; AUC improves from 0.74 to 0.82.',
    ],
    question: 'An 8-point AUC jump from a single hand-crafted ratio is a red flag. What in the construction of this feature makes it partially encode the label?',
    options: [
      'There is no leakage — the feature uses legitimate historical data.',
      'Customers about to churn reduce spending first, so revenue_30d in the denominator captures end-of-tenure behaviour. The ratio amplifies a post-churn signal that would not be available when a retention action should be taken.',
      'The feature should be computed as revenue_per_call, not calls_per_revenue.',
      'SHAP importance does not indicate leakage.',
    ],
    answer: 1,
    diagnosis: 'Near-future leakage. Churning customers often stop spending in the 30 days before cancelling, so revenue_30d falls and the ratio spikes — but the spike is because the customer is already leaving, not because they are at risk. The model partly learns post-churn behaviour rather than pre-churn risk.',
    explanation: 'A large single-feature lift from a hand-crafted ratio should trigger a look at whether the denominator or numerator moves *because* of the outcome.',
    fix: 'Shift revenue to earlier windows (revenue_60d/90d) to reduce sensitivity to end-of-tenure spend drops, or keep support_call_count and revenue_30d as separate features and let the model learn the interaction — a genuine interaction is found without a hand-built ratio that amplifies leakage.',
    source: 'FeatureEng · Interaction & Leakage (ifl1)',
  },
  {
    id: 'data-interaction-model-vs-manual',
    subject: 'data', subtopic: 'feature-eng', level: 'mid', type: 'mcq',
    title: 'Let the model learn interactions vs. manual engineering',
    context: [
      'A senior engineer argues manually engineering age × income and tenure × product_count is unnecessary for a gradient boosting model — trees learn interactions automatically.',
      'A junior engineer counters that explicit interaction features always improve performance.',
    ],
    question: 'Who is right, and in what specific scenario would manually adding this interaction actually matter?',
    options: [
      'Always — explicit interactions reduce the number of splits and improve training speed and generalisation.',
      'Never — gradient boosting always discovers interactions at least as well as manual engineering.',
      'When the interaction needs a transformation (ratio, product, difference) that splits approximate poorly, or involves high-cardinality features that would need exponentially many splits to capture jointly.',
      'Only when the dataset has fewer than 10,000 rows.',
    ],
    answer: 2,
    diagnosis: 'Trees learn piecewise interactions via splits and can approximate age × income through a sequence of splits, but may need many to do so accurately with continuous features. Ratio and difference features are hardest for splits. Manual engineering is justified when the transformation is non-linear and the tree would need exponential depth.',
    explanation: 'Neither absolute claim is right. The nuanced answer is transformation- and cardinality-dependent.',
    fix: 'For gradient boosting, hand-engineer ratio/difference features with known business meaning that splits approximate poorly; skip polynomial products (trees handle those). Always A/B the engineered feature on a held-out set and keep it only on consistent cross-fold lift, confirming with SHAP that it learns signal, not noise.',
    source: 'FeatureEng · Interaction & Leakage (ifl2)',
  },
  {
    id: 'data-interaction-target-encoding',
    subject: 'data', subtopic: 'leakage', level: 'junior', type: 'mcq',
    title: 'Target encoding leaks label into cross-validation',
    context: [
      'Target encoding is applied to a high-cardinality categorical (merchant_id, 8,000 values) using the full training set before cross-validation.',
      'Validation AUC is 0.91; deployed production AUC is 0.76.',
    ],
    question: 'A 15-point val→prod gap appeared on day one; the only unusual step was target-encoding merchant_id on the full training set before CV. What broke, and why did CV not catch it?',
    options: [
      'The model overfits to the training data due to high cardinality.',
      'Target encoding on the full training set before CV leaks label information into the validation folds — encoded values for validation merchants already incorporate those merchants\' labels, inflating validation AUC and giving an overoptimistic estimate.',
      'High-cardinality categoricals should always use one-hot encoding.',
      'The production data has different merchant IDs than the training data.',
    ],
    answer: 1,
    diagnosis: 'Target encoding leakage. Computing mean(target | merchant_id) on the full training set means the encoding for a validation merchant already incorporates that merchant\'s labels, so the feature implicitly encodes the label — extremely predictive in CV but unavailable for new or rare merchants in production.',
    explanation: 'CV cannot catch it because the contamination is baked into the feature before the folds are split. The honest estimate needs out-of-fold predictions.',
    fix: 'Apply target encoding inside the CV loop: for each fold compute encodings only from that fold\'s training portion. Use smoothed encoding (blend fold-level with global mean) for low-frequency merchants. For production, use the encoding from the full training set, but always report out-of-fold AUC.',
    source: 'FeatureEng · Interaction & Leakage (ifl3)',
  },
]

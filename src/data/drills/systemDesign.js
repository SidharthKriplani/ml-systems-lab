// systemDesign.js — normalized JUDGE drills migrated from SystemDesignTab.jsx.
// subject = 'system_design'. Conforms to the drillPool.js schema.
//
// Source modules migrated:
//   - "Do We Even Need ML?"      → mcq,       subtopic 'framing'
//   - "Retrieval System Failures"→ mcq,       subtopic 'retrieval'
//   - "Design Review"            → rubric,    subtopic 'design-review' (tiered options, no single right answer)
//   - "Incident Scenarios"       → rubric,    subtopic 'incidents'     (tiered prose responses)
//   - "ML Incident Room"         → multistep, subtopic 'incidents'     (evidence → diagnosis)
//
// SKIPPED (per migration brief): Two-Tower Explorer, Two-Tower Diagram,
// Serving Tradeoffs (SIM widgets), and DS Ownership Chain (tiered prose, not a drill).

export const SYSDESIGN_DRILLS = [
  // ─── Do We Even Need ML? (framing) ─────────────────────────────────────────
  {
    id: 'sd-framing-churn-email',
    subject: 'system_design', subtopic: 'framing', level: 'staff', type: 'mcq',
    title: 'Churn prediction email campaign',
    context: 'A growth PM requests a churn prediction model. Users predicted as high-churn will receive a retention email. Your data team has 18 months of historical churn labels and 40 features. Training will take ~2 weeks.',
    question: 'The PM wants a churn model. The intervention is a retention email. You have 18 months of labels and 40 features — the model is buildable. But should you build it? What question do you ask before committing 2 weeks of data team time?',
    options: [
      'Yes — a trained model will identify high-churn users more precisely than rules.',
      'No — if the action is sending an email to high-churn users, send it to everyone. The incremental lift from targeting is likely smaller than the cost of building the model.',
      'Yes — but only if model AUC exceeds 0.80 on the holdout set.',
      'No — use a rules engine based on days-since-last-login and purchase frequency.',
    ],
    answer: 1,
    diagnosis: 'The counterfactual action is "send everyone the email." If the email is cheap and the churn cost is high, the ROI of precision targeting is often negative once model-building cost is included. The question is not "can we build a model?" but "does the model create more value than the baseline action?"',
    fix: 'Before scoping any ML project, ask: what is the counterfactual action if we have no model? What does the model let us do differently? If the difference in action between top-decile and bottom-decile predicted users is small, the model ROI is negative. Ship the email campaign first; add targeting only when you have signal that untargeted outreach has diminishing returns.',
    levels: {
      staff: 'The staff move is to challenge the framing itself: apply the counterfactual test before scoping. Most ML projects fail it — a cheap universal action can dominate precise targeting once build and maintenance cost are counted.',
    },
    source: 'System Design · Do We Even Need ML? · dwml1',
  },
  {
    id: 'sd-framing-support-classifier',
    subject: 'system_design', subtopic: 'framing', level: 'staff', type: 'mcq',
    title: 'Support ticket classifier',
    context: 'A support team of 3 agents handles ~8 tickets per day across 6 categories (billing, technical, returns, complaints, general, escalation). A PM wants an ML classifier to auto-route tickets to the right agent.',
    question: 'A PM wants a BERT classifier to auto-route support tickets across 6 categories. Volume is 8 tickets per day across 3 agents. The model is technically buildable. Do you build it?',
    options: [
      'Yes — even at low volume, a classifier eliminates manual routing overhead.',
      'Yes — train a fine-tuned BERT model; it will generalise better as volume grows.',
      'No — at 8 tickets/day, a human reads the ticket in seconds. Regex + keyword rules for the 2 common categories covers 80% of routing. ML ROI is negative at this volume.',
      'No — use a decision tree trained on ticket text, not a neural model.',
    ],
    answer: 2,
    diagnosis: 'At 8 tickets/day, a human agent routes each ticket in under 30 seconds. Building, deploying, and maintaining a text classifier takes weeks and ongoing effort. The break-even volume for ML routing is typically 500+ tickets/day where manual routing becomes the bottleneck.',
    fix: 'Apply the volume test: ML routing pays off when routing is the bottleneck, not when it is a minor inconvenience. For low-volume queues, implement keyword rules for the top-2 categories and let agents handle the rest. Revisit when volume exceeds 200 tickets/day or routing errors cause measurable SLA misses.',
    levels: {
      staff: 'Staff-level judgment sizes the bottleneck before greenlighting: ML routing only pays off when routing (not resolution) is the constraint. At 8 tickets/day it never is.',
    },
    source: 'System Design · Do We Even Need ML? · dwml2',
  },
  {
    id: 'sd-framing-fraud-base-rate',
    subject: 'system_design', subtopic: 'framing', level: 'staff', type: 'mcq',
    title: 'Fraud flagging at 0.001% base rate',
    context: 'A fintech product processes 500,000 transactions per day. Historical fraud rate is 0.001% — about 5 fraudulent transactions per day. The fraud team manually reviews flagged transactions. A data scientist proposes an XGBoost fraud classifier.',
    question: '500,000 transactions per day, 5 actual frauds per day (0.001% rate), and a 3-person review team. A data scientist proposes an XGBoost classifier. Before approving the sprint, you run the precision-recall math — what does it tell you about whether this model can actually work at this base rate?',
    options: [
      'XGBoost is the wrong algorithm — neural networks handle imbalanced data better.',
      'The dataset is too small to train a reliable model.',
      'At 0.001% base rate, even a 99% precise model flags ~500 false positives per day for every 5 real frauds. The review queue is overwhelmed and the precision economics are broken.',
      'Fraud detection always requires real-time inference, which XGBoost cannot support.',
    ],
    answer: 2,
    diagnosis: 'Precision-recall economics break at extreme class imbalance. A model with 99% precision on 500k transactions flags 5,000 transactions/day as fraud — 4,995 false positives for 5 real frauds. Unless the review team can scale to 5,000 daily reviews, the model creates more work than it saves.',
    fix: 'Before building: compute the maximum tolerable false positive rate given review team capacity, then back-calculate the precision the model needs. At 0.001% base rate with a 3-person team reviewing 50 flags/day, you need >90% precision. Validate whether that precision is achievable before committing. Consider a rules engine for the highest-signal fraud patterns (impossible geolocation, known BIN ranges) as a cheaper first layer.',
    levels: {
      staff: 'The staff move is to back-calculate the required precision from review-team capacity and base rate before approving the sprint — proving feasibility (or its absence) on paper first.',
    },
    source: 'System Design · Do We Even Need ML? · dwml3',
  },

  // ─── Retrieval System Failures (retrieval) ─────────────────────────────────
  {
    id: 'sd-retrieval-embedding-drift',
    subject: 'system_design', subtopic: 'retrieval', level: 'senior', type: 'mcq',
    title: 'Two-tower embedding drift',
    context: 'A two-tower recommendation model was trained 6 months ago. Item embeddings are recomputed weekly; user embeddings are recomputed daily. Users are complaining that recommendations feel stale — items they already purchased keep appearing.',
    question: 'The two-tower model was trained 6 months ago. User embeddings refresh daily, item embeddings weekly. Users report already-purchased items keep appearing. Your embedding pipeline shows no errors and ANN index is current. What is broken in the system design?',
    options: [
      'The ANN index is using cosine similarity; switch to dot product.',
      'Item embeddings are updated weekly but user behaviour shifts faster — the embedding space mismatch means user vectors point toward item clusters that no longer reflect current inventory or user state.',
      'The model needs more negative samples during training.',
      'Daily user embedding recomputation is too frequent and causing instability.',
    ],
    answer: 1,
    diagnosis: 'Embedding drift: user embeddings are recomputed daily against a model trained 6 months ago. The embedding space has not shifted, but item inventory, pricing, and availability have. Items the user purchased are still in the same embedding cluster as items they might buy — the model cannot distinguish "already owned" from "relevant."',
    fix: 'Add a purchased-item filter as a post-retrieval exclusion layer (fastest fix). Longer-term: retrain the two-tower model more frequently, or add explicit negative feedback signals (purchases, skips) to the training data. Monitor embedding space drift with periodic random sample cosine similarity audits.',
    source: 'System Design · Retrieval System Failures · ret1',
  },
  {
    id: 'sd-retrieval-hnsw-staleness',
    subject: 'system_design', subtopic: 'retrieval', level: 'senior', type: 'mcq',
    title: 'HNSW index staleness under writes',
    context: 'A product search system uses HNSW (Hierarchical Navigable Small World) for approximate nearest-neighbour retrieval over 2M product embeddings. New products are added daily (~500/day). Engineers report that newly added products rarely appear in search results for the first 48 hours after ingestion.',
    question: '500 new products ingested daily, but they don\'t appear in search results for 48 hours after ingestion. Embeddings compute correctly, no pipeline errors, HNSW index shows all items present. What is the structural reason HNSW fails for newly added nodes, and what is the fastest fix?',
    options: [
      'New product embeddings are lower quality because they have fewer purchase signals.',
      'HNSW graph connectivity degrades when new nodes are inserted without a full index rebuild — new items are sparsely connected and rarely reached during beam search.',
      'The embedding model was not fine-tuned on new product categories.',
      'The retrieval system is caching results for 48 hours.',
    ],
    answer: 1,
    diagnosis: 'HNSW is an approximation structure optimised for static datasets. Incremental inserts add nodes with limited graph connectivity — the new node connects to a small neighbourhood but is not back-linked from existing nodes that should point to it. Beam search rarely reaches these weakly-connected new nodes.',
    fix: 'Schedule a full HNSW index rebuild nightly (incremental inserts accumulate connectivity debt). For time-sensitive new items, add a recency-boosted brute-force fallback layer: exact-search over the last 72 hours of new products and merge results with HNSW output before re-ranking. Monitor recall@100 on a held-out new-item test set.',
    source: 'System Design · Retrieval System Failures · ret2',
  },
  {
    id: 'sd-retrieval-domain-mismatch',
    subject: 'system_design', subtopic: 'retrieval', level: 'senior', type: 'mcq',
    title: 'Query-document domain mismatch',
    context: [
      'A legal document search system embeds queries and documents using a general-purpose sentence-transformer (trained on web text and news). Retrieval precision is 0.41 on the legal domain benchmark.',
      'The same model achieves 0.78 precision on a general news retrieval benchmark.',
    ],
    question: 'Precision is 0.41 on legal documents but 0.78 on general news — same model, same index, same infrastructure. Legal queries use "force majeure," "indemnification," Latin terms. What does this 37-point gap tell you about where the model is failing, and what is the fix?',
    options: [
      'The HNSW index needs more ef_construction parameter tuning.',
      'Legal queries use domain-specific terminology and Latin phrases not present in the model\'s training distribution — query and document embeddings land in different regions of the embedding space for the same legal concept.',
      'The model\'s max token length is too short for long legal documents.',
      'Legal documents need TF-IDF retrieval, not dense embeddings.',
    ],
    answer: 1,
    diagnosis: 'Domain mismatch: "force majeure" in a query and "force majeure" in a document should be near-neighbours in embedding space. For a web-trained model, they may not be — the model has seen these terms in different contexts and assigns them different representations. Precision collapses when query-document vocabulary diverges from training distribution.',
    fix: 'Fine-tune the sentence transformer on legal query-document pairs using contrastive loss (positive pairs: query + relevant document; hard negatives: query + plausible-but-wrong document). Even 5,000–10,000 domain-specific pairs significantly improve in-domain precision. Evaluate on a held-out legal retrieval benchmark before and after fine-tuning.',
    source: 'System Design · Retrieval System Failures · ret3',
  },
  {
    id: 'sd-retrieval-bm25-short-query',
    subject: 'system_design', subtopic: 'retrieval', level: 'senior', type: 'mcq',
    title: 'BM25 recall collapse on short queries',
    context: 'An e-commerce search system uses BM25 as the sole retrieval stage. Single-word queries ("shoes", "laptop") have good recall. But two-word queries ("running shoes", "gaming laptop") show recall@100 dropping from 0.92 to 0.61. No infrastructure changes were made.',
    question: 'BM25 recall drops from 0.92 on 1-word queries to 0.61 on 2-word queries. Corpus and index are unchanged. What is the structural reason BM25 behaves this way on multi-word queries, and what retrieval architecture change fixes it?',
    options: [
      'BM25 needs retuning of the k1 and b parameters for multi-word queries.',
      'BM25 uses AND-style matching by default in many implementations — multi-word queries require all terms to be present, eliminating documents that contain synonyms or rephrased versions of the query terms.',
      'The inverted index needs to be rebuilt with position information for phrase matching.',
      'Multi-word queries require a different tokenizer.',
    ],
    answer: 1,
    diagnosis: 'BM25 is a bag-of-words model. With AND semantics (common in Elasticsearch defaults), "running shoes" requires both "running" AND "shoes" in the document. A product titled "Nike Air jogger footwear" contains neither exact term — BM25 misses it completely. Recall collapses as query length increases because the intersection of matching documents shrinks. Vocabulary mismatch between query and document is invisible to BM25.',
    fix: 'Add a dense retrieval stage alongside BM25 (hybrid search): embed the query with a bi-encoder, retrieve top-K dense candidates, merge with BM25 results via RRF (Reciprocal Rank Fusion). For e-commerce specifically: add product synonym expansion at query time and title synonym normalisation at index time. Switch Elasticsearch BM25 operator from AND to OR with field boosting for titles.',
    source: 'System Design · Retrieval System Failures · ret4',
  },
  {
    id: 'sd-retrieval-reranker-distribution-shift',
    subject: 'system_design', subtopic: 'retrieval', level: 'staff', type: 'mcq',
    title: 'Recall collapse after embedding model upgrade',
    context: 'Your team upgrades the bi-encoder from sentence-transformers/all-MiniLM-L6-v2 (384 dims) to sentence-transformers/all-mpnet-base-v2 (768 dims). Offline cosine similarity scores improve. But NDCG@10 in the A/B test drops 8 points. The HNSW index was rebuilt with new embeddings before the test.',
    question: 'New embedding model shows better offline cosine scores but NDCG@10 drops 8 points in A/B. HNSW rebuilt cleanly. What is the most likely cause and fix?',
    options: [
      'The new model is too slow and latency is degrading user experience.',
      'The cross-encoder re-ranker was trained on relevance signals from the old embedding space. Retrieved candidates from the new model are different enough that the re-ranker assigns scores poorly — the ranking stage is trained on a different retrieval distribution than it now receives.',
      'The HNSW ef_search parameter was not tuned for 768-dimensional embeddings.',
      'The new model produces embeddings in a different norm range, breaking cosine similarity.',
    ],
    answer: 1,
    diagnosis: 'Two-stage pipeline distribution shift: the cross-encoder re-ranker was trained on (query, candidate) pairs where candidates came from the old retrieval system. The old system retrieved a specific distribution of candidates. The new bi-encoder retrieves a different set of candidates — semantically richer but unfamiliar to the re-ranker. The re-ranker\'s calibration breaks on out-of-distribution candidates, producing incorrect score orderings even when the retrieved set is actually more relevant.',
    fix: 'Re-collect training data for the cross-encoder using the new bi-encoder\'s retrieval outputs. Fine-tune the cross-encoder on (query, new-bi-encoder-retrieved-candidates, relevance-labels) before running the A/B test. Alternatively: use a distillation-based approach where the old cross-encoder scores candidates from the new retrieval to create soft labels, then train the new cross-encoder on those. Always treat retrieval and re-ranking as a joint system.',
    levels: {
      staff: 'The staff insight is that retrieval and re-ranking form one joint system: upgrading either stage in isolation shifts the candidate distribution the other stage was calibrated on.',
    },
    source: 'System Design · Retrieval System Failures · ret5',
  },
  {
    id: 'sd-retrieval-query-intent-drift',
    subject: 'system_design', subtopic: 'retrieval', level: 'staff', type: 'mcq',
    title: 'Query intent drift across sessions',
    context: 'A recipe search engine\'s NDCG@5 is 0.84 at 09:00 and degrades to 0.71 by 19:00. The model, index, and serving infrastructure are unchanged throughout the day. Traffic volume is similar across time windows.',
    question: 'NDCG@5 degrades from 0.84 to 0.71 between morning and evening. Same model, same index, similar traffic. What phenomenon explains time-of-day metric degradation in a search system and what monitoring would catch it?',
    options: [
      'The HNSW index is degrading under write load from new recipe additions.',
      'Query intent distribution shifts across the day — morning queries ("breakfast smoothie", "overnight oats") are navigational/known-item; evening queries ("what to cook with chicken and rice") are exploratory/multi-intent. The model was optimised for morning-style precision but fails on exploratory evening queries.',
      'Caching effects cause stale results to be served more frequently in the evening.',
      'The embedding model has worse performance on longer queries, which are more common in the evening.',
    ],
    answer: 1,
    diagnosis: 'Query intent shift: morning users have narrow, specific intents (navigational). Evening users have broad, multi-intent queries ("dinner ideas for four people"). A ranking model trained on pooled daily data implicitly weights the majority query distribution. If morning queries dominate training data (volume or signal quality), the model excels at navigational queries but degrades on exploratory ones. This shows up as a NDCG cliff at intent transition time.',
    fix: 'Slice query logs by intent type (navigational / informational / exploratory — classify using query length + click-through pattern). Train and evaluate the ranking model separately per intent type. Add query intent classification as a first-stage router: short known-item queries → BM25+title boosting; long exploratory queries → dense retrieval + diversification re-ranking. Monitor NDCG by time-of-day cohort as a standing dashboard metric.',
    levels: {
      staff: 'The staff move is to instrument NDCG by intent-type cohort so aggregate metrics stop hiding a real per-segment cliff, then route by intent rather than train a single pooled model.',
    },
    source: 'System Design · Retrieval System Failures · ret6',
  },

  // ─── Design Review (design-review) ─────────────────────────────────────────
  // Every option is valid at a different seniority tier — no single right answer.
  {
    id: 'sd-design-review-objective',
    subject: 'system_design', subtopic: 'design-review', level: 'staff', type: 'rubric',
    title: 'ML Objective',
    context: 'Design review for a recommendation system. Each option below reflects a different level of production maturity, from junior to staff.',
    question: 'What is your primary ML objective for this recommendation system?',
    options: [
      'Maximize CTR — we optimize for click-through rate as the main signal.',
      'Optimize engagement (watch time / listen time) as a single north-star metric.',
      'Multi-objective: engagement + satisfaction signal, with a constraint on diversity minimum.',
      'Multi-objective: listen-rate + 30-day return + diversity, with explicit product-defined tradeoff weights and a living document that records why those weights were chosen.',
    ],
    levels: {
      junior: 'Maximize CTR. Creates Goodhart\'s Law failure: the model learns to recommend clickbait. Users click, don\'t engage, and churn. CTR is a proxy; optimizing only for it breaks the actual goal.',
      analyst: 'Optimize engagement (watch/listen time) as a single north-star. Better than CTR — watch time is harder to game. But a single metric still creates blind spots: engagement can be maximized by showing addictive content at the expense of diversity and long-term satisfaction.',
      senior: 'Multi-objective with an explicit diversity constraint. Good production thinking — the constraint prevents filter bubbles. But without product-defined tradeoff weights, the model finds unexpected Pareto optima that satisfy the constraint technically but not spiritually.',
      staff: 'Multi-objective (listen-rate + 30-day return + diversity) with explicit product-defined tradeoff weights and a living rationale document. Three signals cover short-term engagement, long-term retention, and ecosystem health. Explicit, recorded weights mean the objective can be debated, audited, and updated — the model optimizes what the business actually wants.',
    },
    explanation: 'Ranked design-review question: each option maps to a seniority tier. The staff-level answer (option index 3) is the target — three signals plus explicit, documented tradeoff weights. CTR-only (0) is junior; single engagement metric (1) is analyst; multi-objective with a diversity constraint but no weights (2) is senior.',
    source: 'System Design · Design Review · objective',
  },
  {
    id: 'sd-design-review-cold-start',
    subject: 'system_design', subtopic: 'design-review', level: 'staff', type: 'rubric',
    title: 'Cold Start',
    context: 'Design review for a recommendation system. Each option below reflects a different level of production maturity, from junior to staff.',
    question: 'How do you handle the cold-start problem for new items with no interaction history?',
    options: [
      'Show popular items to all users when a new item has no embedding.',
      'Use item metadata (genre, tags, description) to build a content-based embedding for new items.',
      'Two-stage: content-based retrieval for new items using metadata embeddings, transition to collaborative filtering after ~20 interactions.',
      'Two-stage: content-based retrieval using metadata embeddings, merge with collaborative signal after 50+ interactions, monitor the cold-start cohort separately with its own metrics dashboard, and validate that the transition improves downstream metrics.',
    ],
    levels: {
      junior: 'Popular-item fallback. Guarantees the rich get richer — new items never accumulate interactions, never enter the recommendation loop, and the catalog goes stale. This defers the problem forever rather than solving it.',
      analyst: 'Content-based embedding from metadata. Correct first step — solves cold-start. But without a transition strategy you stay in content-based mode too long, even after real interaction signal exists.',
      senior: 'Two-stage with transition to collaborative filtering after ~20 interactions. Good — but the threshold matters; 20 interactions may be too low for stable embeddings depending on domain, and you must monitor whether the transition improves or regresses quality.',
      staff: 'Two-stage with a 50+ interaction merge, separate monitoring for the cold-start cohort, and validation that the transition improves downstream metrics. Separate cohort monitoring is the insight most engineers miss — without it, poor cold-start performance is averaged away in aggregate metrics and never fixed.',
    },
    explanation: 'Ranked design-review question. Staff answer (option index 3) adds separate cold-start cohort monitoring — the piece most engineers omit. Popular-item fallback (0) is junior; metadata embeddings without transition (1) is analyst; transition at ~20 interactions without cohort monitoring (2) is senior.',
    source: 'System Design · Design Review · cold_start',
  },
  {
    id: 'sd-design-review-recall-drop',
    subject: 'system_design', subtopic: 'design-review', level: 'staff', type: 'rubric',
    title: 'Recall Drop',
    context: 'Design review for a recommendation system. Retrieval recall@100 dropped from 87% to 64% overnight. Each option reflects a different level of debugging maturity.',
    question: 'Your retrieval recall@100 dropped from 87% to 64% overnight. What are your first three things to check?',
    options: [
      'Check if a new model version was deployed yesterday.',
      'Check model version, then look at latency graphs, then check if A/B traffic split changed.',
      'Check embedding freshness, then ANN index rebuild status, then training data for distribution shift.',
      'Systematic: (1) embedding freshness/coverage — are item embeddings current and does the ANN index match? (2) ANN index staleness — when was it last rebuilt and with what data? (3) training data composition shift — did the label distribution or interaction data pipeline change?',
    ],
    levels: {
      junior: 'Check the model version. Reasonable as one signal, but it is a single hypothesis, not a process. If no model change happened you are stuck. A recall drop has many causes — you need a checklist.',
      analyst: 'Model version, then latency, then A/B split. Better — multiple signals. But latency rarely explains a recall drop and A/B split is low-probability. You are not checking the most likely retrieval-specific failure modes.',
      senior: 'Embedding freshness, ANN index rebuild status, training-data distribution shift. Retrieval-system-specific thinking — exactly right. Stale embeddings and a stale ANN index are the two most common causes of an overnight recall drop in a two-tower system.',
      staff: 'The same three hypotheses stated systematically and in priority order, with the specific sub-questions for each. Embedding coverage and index staleness are fastest to check and most likely; label distribution shift requires more work but rules out training issues.',
    },
    explanation: 'Ranked design-review question. Staff answer (option index 3) is a prioritized, retrieval-native checklist. Single-hypothesis model-version check (0) is junior; multi-signal but generic (1) is analyst; correct retrieval-specific hypotheses (2) is senior.',
    source: 'System Design · Design Review · recall_drop',
  },
  {
    id: 'sd-design-review-skew',
    subject: 'system_design', subtopic: 'design-review', level: 'staff', type: 'rubric',
    title: 'Training-Serving Skew',
    context: 'Design review for a two-tower model. Each option reflects a different level of production maturity for detecting and preventing training-serving skew.',
    question: 'How do you detect and prevent training-serving skew in your two-tower model?',
    options: [
      'Compare offline AUC to online AUC — if they diverge significantly, there\'s skew.',
      'Log features at serving time, sample them, and periodically compare the distribution to training features.',
      'Continuous feature distribution monitoring with PSI thresholds, plus a scheduled validation run that scores a labeled holdout set and alerts if online score distribution drifts from training.',
      'Feature parity test (assert training and serving compute the same feature values for identical inputs) + continuous score distribution monitoring (KL divergence alert) + scheduled test on a labeled holdout — all three gates must pass before any model promotion.',
    ],
    levels: {
      junior: 'Compare offline vs online AUC. This is a lagging indicator — by the time you notice divergence you have served a skewed model for days or weeks. It detects skew after it causes harm, not before.',
      analyst: 'Log and periodically compare serving vs training feature distributions. Good direction, but "periodically compare" is not production-grade — you need continuous monitoring with thresholds and alerts, not ad hoc analysis.',
      senior: 'Continuous PSI-gated feature monitoring plus scheduled holdout validation. Production-quality — PSI alerts catch data drift early and scheduled validation catches model degradation, giving early warning on both.',
      staff: 'Deterministic feature parity test + continuous score-distribution monitoring (KL divergence) + scheduled holdout test, all three as blocking promotion gates. Parity tests catch code-level skew before deployment; distribution monitoring catches runtime drift; the holdout catches model degradation. No single point of failure slips through.',
    },
    explanation: 'Ranked design-review question. Staff answer (option index 3) makes three complementary checks blocking promotion gates. Offline-vs-online AUC (0) is a lagging junior signal; ad hoc periodic comparison (1) is analyst; continuous PSI + holdout (2) is senior.',
    source: 'System Design · Design Review · skew',
  },
  {
    id: 'sd-design-review-diversity',
    subject: 'system_design', subtopic: 'design-review', level: 'staff', type: 'rubric',
    title: 'Diversity Collapse',
    context: 'Design review for a recommendation system. Diversity has dropped 40% over 6 weeks with no model changes. Each option reflects a different level of root-cause understanding.',
    question: 'Your recommendation diversity has dropped 40% over 6 weeks with no model changes. What is the root cause?',
    options: [
      'The model has overfit to recent data. Retrain it on a longer history window.',
      'The training data has become less diverse over time. Augment it with more diverse examples.',
      'Feedback loop: model recommendations influenced user behaviour, user behaviour became training labels, model reinforced popular items. Break the loop by adding a diversity regularization term in the loss.',
      'Feedback loop: model shaped behaviour → behaviour became labels → model reinforced itself. Fix requires: (1) a long-term behavioral holdout (never touched by the recommender) for unbiased training signal, (2) diversity constraint in objective, (3) causal correction or inverse propensity weighting to debias historical labels.',
    ],
    levels: {
      junior: 'Retrain on a longer history window. This is not the root-cause fix — it is the exact mechanism that reproduces the problem. Retraining on data generated by a biased model yields a more biased model.',
      analyst: 'Training data became less diverse; augment it. Correct observation but it is effect, not cause. Why did the data become less diverse? Augmentation treats the symptom without addressing the feedback loop.',
      senior: 'Identifies the feedback loop and adds diversity regularization. The loop identification is the key insight. But regularization alone does not fix data already corrupted by the loop — you also need a clean holdout to measure ecosystem health.',
      staff: 'Feedback loop plus a full fix: a long-term behavioral holdout (never touched by the recommender), a diversity constraint in the objective, and IPW / causal correction to debias historical labels. The behavioral holdout is the critical piece most engineers omit — without unbiased data you cannot break the loop. IPW is the statistically correct way to reuse logged data when the logging policy was your own model.',
    },
    explanation: 'Ranked design-review question. Staff answer (option index 3) diagnoses the feedback loop and prescribes a behavioral holdout + diversity constraint + IPW debiasing. Retrain-on-longer-history (0) is junior; augment-the-data (1) is analyst; loop identification with regularization only (2) is senior.',
    source: 'System Design · Design Review · diversity',
  },

  // ─── Incident Scenarios (incidents) — tiered prose responses ───────────────
  {
    id: 'sd-scenario-late-pipeline',
    subject: 'system_design', subtopic: 'incidents', level: 'staff', type: 'rubric',
    title: 'Feature Pipeline Late',
    context: 'You run a real-time fraud model. Features include transaction velocity (1h, 24h windows), device fingerprint match, and user risk score. The pipeline that refreshes user risk scores failed silently at 04:00. Serving is using a stale fallback. Traffic is 40% of daily peak. Business pressure: launch is in 2 hours, the pipeline failed 3 hours ago, the model is serving 6-hour-old features, and the PM is asking if it is fine to proceed.',
    question: 'What do you do?',
    levels: {
      junior: 'Restart the pipeline and wait. Tell the PM "we\'re working on it."',
      analyst: 'Quantify staleness impact: which features are affected, by how much. Estimate expected precision degradation. Give the PM a risk number, not just "working on it."',
      senior: 'Implement a serving bypass that flags stale-feature predictions as low-confidence. Enforce a conservative threshold for flagged predictions. Communicate: "risk score features are 6h stale, we\'re raising the fraud threshold temporarily to compensate, expected FP rate increases by ~X%."',
      staff: 'All of the above plus: escalate the silent-failure root cause (why did no alert fire?). Post-incident, require pipeline health gates before any serving traffic is allowed, and define a "feature freshness SLA breach" protocol in the runbook.',
    },
    explanation: 'Tiered incident-response drill. The lesson: quantify degradation before deciding. A confident "here\'s the risk number" is more useful than "we\'re fixing it." Degraded service with known bounds beats unknown service. Staff-level response also fixes the class of failure (silent pipeline failure with no alert), not just the instance.',
    source: 'System Design · Incident Scenarios · late_pipeline',
  },
  {
    id: 'sd-scenario-srm-failure',
    subject: 'system_design', subtopic: 'incidents', level: 'staff', type: 'rubric',
    title: 'A/B Test SRM Failure',
    context: 'An A/B test ran 14 days. Primary metric CTR: +2.1% (p=0.002). Secondary: add-to-cart +0.8% (ns), revenue per user +1.2% (ns). Population: all US users. But the SRM (sample ratio mismatch) check shows a 52:48 split instead of 50:50, SRM p-value 0.003. Business pressure: the PM wants to ship today.',
    question: 'Do you ship? What do you tell the PM?',
    levels: {
      junior: '"The test passed, let\'s ship."',
      analyst: '"We have an SRM — the assignment ratio is wrong, so the result is statistically invalid. We need to find the cause and rerun before shipping."',
      senior: 'Investigate the SRM cause before deciding: logging bug (affects metric), assignment bug (biased sample), or novelty effect (unlikely at 14 days). Tell the PM: "The result cannot be trusted as-is — a 2% assignment imbalance. If it\'s a control-side logging bug, the CTR lift may be inflated. Recommend fix and rerun, 7-day minimum. I can give a bias-corrected point estimate while we wait, but I won\'t call it valid."',
      staff: 'All of the above plus: root-cause the SRM systematically, fix the assignment/logging system, add a mandatory SRM-check gate to the experimentation platform that blocks result reads if SRM p < 0.01, and set an org-wide policy: "SRM = invalid experiment, no exceptions."',
    },
    explanation: 'Tiered incident-response drill. The lesson: SRM invalidates the randomisation assumption. A significant p-value on a biased sample is not evidence — it is noise. The correct answer is always "rerun," not "ship." Staff response institutionalizes a blocking SRM gate so the failure cannot recur.',
    source: 'System Design · Incident Scenarios · srm_failure',
  },
  {
    id: 'sd-scenario-offline-online-gap',
    subject: 'system_design', subtopic: 'incidents', level: 'staff', type: 'rubric',
    title: 'Offline/Online Performance Gap',
    context: 'A new fraud model was trained on 90 days of data and promoted this morning after shadow metrics looked comparable. Offline validation: AUC 0.94, precision 0.88. Production (6h): AUC 0.62, precision 0.31. Champion (still in shadow): AUC 0.92, precision 0.69. Fraud ops is overwhelmed.',
    question: 'What do you do right now and what was the likely cause?',
    levels: {
      junior: 'Roll back. Investigate later.',
      analyst: 'Roll back immediately — the champion is still in shadow and performing correctly, so rollback risk is low. Likely cause: label leakage (offline precision 0.88 was too good) or a feature available in training but not at serving time.',
      senior: 'Rollback in <15 min. Compare top feature importances between champion and challenger. Check if any feature has >0.15 importance and was derived from post-event data. Run a calibration check — AUC 0.94 offline vs 0.62 online means fundamentally different score distributions, pointing to feature leakage or training-serving skew. Post-mortem in 24h.',
      staff: 'All of the above plus: establish a promotion gate — if shadow AUC < champion AUC * 0.95, block automated promotion. Require an offline-to-online parity test before any fraud model promotion. Review why the shadow instrumentation did not catch this.',
    },
    explanation: 'Tiered incident-response drill. The lesson: offline AUC 0.94 → online AUC 0.62 is not a generalisation gap, it is a data-integrity failure. Suspiciously good offline metrics are a red flag, not a green light. Staff response adds a shadow-vs-champion promotion gate.',
    source: 'System Design · Incident Scenarios · offline_online_gap',
  },
  {
    id: 'sd-scenario-drift-alert',
    subject: 'system_design', subtopic: 'incidents', level: 'senior', type: 'rubric',
    title: 'Feature Drift Alert',
    context: 'A 6-month-old churn predictor. Feature user_age_bucket is the 3rd most important. PSI has been stable at 0.05–0.08 for 5 months, then spiked to 0.41 overnight (alert threshold 0.2). Product says no changes were made and business metrics look normal. Labels are delayed by 14 days, so there is no performance signal yet.',
    question: 'How do you respond? What are the possible causes and what do you investigate first?',
    levels: {
      junior: '"Alert fired, looking into it."',
      analyst: 'Check the feature computation code and the upstream data source. Plot the distribution shift — new bucket, missing bucket, or shifted proportions? Rule out a pipeline bug before concluding it is real drift.',
      senior: 'Systematic triage: (1) data pipeline bug? Check upstream table row counts and schema. (2) Population composition change? Check if a new cohort was onboarded. (3) Real behavioural shift? Check if the change tracks a product launch or external event. Escalate to the model team only after ruling out pipeline cause. Set a 14-day performance watch given label delay.',
      staff: 'All of the above plus: if it is real drift, initiate the retraining protocol. Define a "PSI triage playbook" so any analyst can follow the same steps, and review whether the monitoring thresholds need calibration for this model\'s feature distributions.',
    },
    explanation: 'Tiered incident-response drill. The lesson: high PSI does not automatically mean model degradation — it means something changed. Pipeline bugs cause more high-PSI alerts than real drift. Always rule out instrumentation before concluding the world changed.',
    source: 'System Design · Incident Scenarios · drift_alert',
  },
  {
    id: 'sd-scenario-delayed-labels',
    subject: 'system_design', subtopic: 'incidents', level: 'senior', type: 'rubric',
    title: 'Retraining with Delayed Labels',
    context: 'The model predicts 30-day conversion; labels resolve 30 days after the event. Training data spans 12 months. The most recent 30 days have 40% label coverage (still inside the observation window). The model has not been retrained in 90 days and PSI alerts are firing on two features. Your manager says "just use what\'s available."',
    question: 'How do you handle the label delay? What do you tell your manager?',
    levels: {
      junior: 'Train on the 40% labelled subset from the last 30 days.',
      analyst: 'Exclude the last 30 days from training (not enough labels resolved). Train on events from 30+ days ago where labels are fully resolved. Tell the manager: "Using partial labels introduces survivorship bias — the 40% that resolved early may be the easiest cases. I\'ll use t-30 as the training cutoff."',
      senior: 'Use the t-30 cutoff, but also check the label resolution curve: how many labels resolve by day 7, 14, 21, 30? If 80% resolve by day 7, train a proxy model on day-7 labels with recalibration on fully-resolved labels. Quantify the bias from partial labels vs the staleness cost of the t-30 cutoff and give the manager a concrete recommendation with the tradeoff.',
      staff: 'All of the above plus: define an org standard for label readiness (minimum 90% resolved before an event is used in training). Design a two-model architecture — early-signal model (day-7 labels) plus full-window model (day-30 labels) — serving the early model where latency matters and the full model for long-horizon decisions.',
    },
    explanation: 'Tiered incident-response drill. The lesson: training on partially-resolved labels is not "using what\'s available" — it is training on a biased sample. Events that resolve early are systematically different from the full population.',
    source: 'System Design · Incident Scenarios · delayed_labels',
  },
  {
    id: 'sd-scenario-model-rollback',
    subject: 'system_design', subtopic: 'incidents', level: 'senior', type: 'rubric',
    title: 'Model Rollback Decision',
    context: 'A recommendation-system update (6 new item features) has been live for 8 hours at 50% traffic. Primary A/B metric CTR is neutral (+0.1%, p=0.8). Secondary metric: support ticket volume is up 34% since launch, tickets about "irrelevant recommendations." The old champion still serves the other 50%. On-call asks whether to roll back.',
    question: 'Do you roll back? What is your decision framework?',
    levels: {
      junior: '"Primary metric is neutral, let it run."',
      analyst: '"Support tickets +34% is a guardrail breach. Roll back. A neutral primary metric doesn\'t override a significant user-harm signal."',
      senior: 'Roll back within 30 minutes. Decision rule: guardrail metrics trump primary-metric neutrality when there is a significant user-harm signal (support tickets are lagging but real). Post-rollback, investigate whether the ticket increase concentrated on a specific segment or item category, and which of the 6 new features most correlates with low-quality recommendations.',
      staff: 'All of the above plus: enforce that guardrail metrics are pre-registered and blocking (not advisory). Add support ticket rate as a formal guardrail for all recommendation experiments, and define a rollback SLA: guardrail breach → decision within 30 min, execution within 60 min.',
    },
    explanation: 'Tiered incident-response drill. The lesson: a neutral primary metric with a breached guardrail means "we stopped something bad, not proved something good." Roll back and understand before redeploying.',
    source: 'System Design · Incident Scenarios · model_rollback',
  },

  // ─── ML Incident Room (incidents) — multistep evidence → diagnosis ─────────
  {
    id: 'sd-incident-stale-embed',
    subject: 'system_design', subtopic: 'incidents', level: 'staff', type: 'multistep',
    title: 'Silent Recommendation Degradation',
    context: [
      'P1 incident. CTR dropped 18% over 4 days. No alerts fired.',
      'You run a two-tower recommendation system. Item embeddings refresh daily from a feature pipeline; user embeddings refresh hourly. Monitoring tracks accuracy@10 and latency. It is Tuesday 2am — PagerDuty fires for the first time.',
      'Metrics: CTR -18% DoD, accuracy@10 -12%, P50 45ms and P99 210ms (both normal), model v2.3 unchanged for 5 days, no A/B running.',
      'Feature pipeline logs: item_embedding_pipeline SUCCESS on days 1–2, then FAILED days 3–5 with "S3 path not found: s3://features/items/...". No alert configured on pipeline failure; serving fell back to last successful embeddings silently.',
      'Feature freshness: user_embeddings 2h (fresh), item_embeddings 3d 21h (stale, SLA breach at 26h threshold, no consumer alert), user_context 45m (fresh), item_metadata 6h (fresh).',
      'Catalog drift: 14,200 new items in last 3 days have no embedding; coverage 73% of catalogue (down from 99%); 27% of eligible items cannot be retrieved — model defaults to popular-item fallback for all users.',
    ],
    question: 'Diagnose the root cause of the silent 18% CTR drop and prescribe the fix.',
    steps: [
      {
        question: 'CTR dropped 18% over 4 days with no alerts, model unchanged, latency normal. What is the root cause?',
        options: [
          'Model regression — v2.3 introduced a bug',
          'Feature pipeline failure — item embeddings are 4 days stale',
          'Infrastructure issue — serving cluster degraded',
          'Input distribution shift — user behaviour changed',
        ],
        answer: 1,
        finding: 'The item_embedding_pipeline has failed for 3 straight days ("S3 path not found") and serving fell back to stale embeddings silently. Item embeddings are ~4 days stale; 14,200 new items have no embedding; catalogue coverage fell from 99% to 73%. The model is fine — the features feeding it went stale without any alert.',
      },
      {
        question: 'You have confirmed stale item embeddings. What is the immediate fix and the class-of-bug fix?',
        options: [
          'Retrain the model on fresh data',
          'Re-run the item embedding pipeline with a manual backfill; add pipeline-failure alerts, a feature-freshness monitor that pages on >26h staleness, and a serving health check comparing embedding coverage to catalogue size',
          'Lower the decision threshold to spread predictions out',
          'Roll back to the previous model version',
        ],
        answer: 1,
        finding: 'Immediately backfill the item embedding pipeline. Then close the class of failure: configure pipeline-failure alerts, add a feature-freshness monitor that pages on-call if item_embeddings exceed 26h stale, and add a serving-layer health check comparing embedding coverage against catalogue size. Silent fallbacks are silent failures — a system that degrades without alerting is worse than one that crashes loudly.',
      },
    ],
    diagnosis: 'Feature pipeline failure — item embeddings are ~4 days stale and serving fell back silently.',
    explanation: 'Silent fallbacks are silent failures. A serving system that degrades gracefully without alerting is worse than one that crashes loudly — you lose days of recovery time. The model (v2.3) is healthy; the failure is an unmonitored stale feature pipeline.',
    fix: 'Immediately re-run the item embedding pipeline with a manual backfill. Configure pipeline-failure alerts. Add a feature-freshness monitor that pages on-call if item_embeddings > 26h stale. Add a serving-layer health check that compares embedding coverage against catalogue size.',
    source: 'System Design · ML Incident Room · stale_embed',
  },
  {
    id: 'sd-incident-label-leak',
    subject: 'system_design', subtopic: 'incidents', level: 'staff', type: 'multistep',
    title: 'Fraud Model Performance Cliff',
    context: [
      'P0 incident. Precision dropped from 0.91 to 0.43 in production overnight.',
      'A gradient-boosted fraud model was retrained last night on 60 days of data. Offline: AUC 0.97, precision 0.91. Promoted this morning. By noon fraud ops is overwhelmed by false positives.',
      'Online vs offline: offline AUC 0.97 / precision 0.91 / recall 0.88 vs online AUC 0.61 / precision 0.43 / recall 0.71. Offline metrics computed on a held-out validation set carved from the same dataset (random split).',
      'Top features by importance: transaction_count_30d (0.22), avg_txn_amount_30d (0.19), is_disputed_resolved (0.17, POST-EVENT), chargeback_filed_within_7d (0.14, POST-EVENT), velocity_1h (0.11). Features #3 and #4 derive from dispute outcomes only available AFTER fraud is confirmed; during training they were joined on transaction_id without timestamp filtering.',
      'Feature engineering diff: the previous version filtered dispute_outcomes with resolved_at < transaction_at (time guard); the new version removed that time guard in a refactor.',
      'Shadow comparison: v4.1 (champion, still shadow) AUC 0.93 / precision 0.87; v4.2 (promoted) AUC 0.43 / precision 0.43. Shadow mode was running but NOT configured to block promotion if shadow AUC < champion; promotion was manual.',
    ],
    question: 'Diagnose why offline metrics were excellent but production precision collapsed, and prescribe the fix.',
    steps: [
      {
        question: 'Offline AUC 0.97 but online 0.61, and two top features derive from dispute outcomes. What is the root cause?',
        options: [
          'Model overfitting — too many trees, too deep',
          'Label leakage — post-event features used during training',
          'Distribution shift — March fraud patterns differ from Jan–Feb',
          'Decision threshold not calibrated for production',
        ],
        answer: 1,
        finding: 'Two of the top features (is_disputed_resolved, chargeback_filed_within_7d) are outcome-derived and only known after fraud is confirmed. A refactor removed the resolved_at < transaction_at time guard, so training joined future information into the features. Offline metrics looked excellent because the model was reading the answer; at serving time those features are unavailable and precision collapses.',
      },
      {
        question: 'You have confirmed label leakage from removed time guards. What ships now and what prevents the class of bug?',
        options: [
          'Increase regularization and reduce tree depth',
          'Roll back to v4.1; re-run feature engineering with timestamp filtering on all dispute-outcome joins; retrain and validate on a time-ordered hold-out; add an automated leakage check flagging any feature with > 0.3 correlation to the label in a causality-excluded window; add a shadow gate that blocks promotion if shadow AUC < champion',
          'Lower the decision threshold to reduce false positives',
          'Wait for more production data and recalibrate the threshold',
        ],
        answer: 1,
        finding: 'Roll back to v4.1 immediately (champion is still healthy in shadow). Restore timestamp filtering on all dispute-outcome joins, retrain, and validate on a time-ordered hold-out (never a random split for fraud). Add an automated leakage check and make the shadow comparison a blocking promotion gate. Suspiciously good offline metrics are the tell — never use random splits for fraud or churn.',
      },
    ],
    diagnosis: 'Label leakage — post-event dispute-outcome features leaked into training after a refactor removed the time guard.',
    explanation: 'Label leakage shows up as suspiciously good offline metrics. If a model\'s top features are outcome-derived (disputes, chargebacks, resolutions), run a temporal validity check before training. Never use random train/test splits for fraud or churn — always use time-ordered splits.',
    fix: 'Immediately roll back to v4.1. Re-run feature engineering with timestamp filtering on all dispute-outcome joins. Retrain, validate on a time-ordered hold-out. Add an automated leakage check: verify no feature has > 0.3 correlation with the label in a causality-excluded window. Make shadow AUC < champion a blocking promotion gate.',
    source: 'System Design · ML Incident Room · label_leak',
  },
  {
    id: 'sd-incident-skew-serving',
    subject: 'system_design', subtopic: 'incidents', level: 'staff', type: 'multistep',
    title: 'Search Ranking Latency Explosion',
    context: [
      'P1 incident. P99 latency hit 8 seconds while P50 stayed fine at 120ms.',
      'Two-stage search ranking: ANN retrieval (fast) + cross-encoder reranking (slow). A new reranker deployed yesterday; P99 started climbing 6 hours later.',
      'Latency: P50 120ms (baseline 115) ok, P95 890ms (baseline 340) warning, P99 7,800ms (baseline 450) critical, P99.9 28,000ms. By stage: ANN P99 45ms normal, reranker P99 7,750ms (up from 400), feature fetch 12ms normal. By query length: <5 tokens (67% traffic) P99 180ms ok; 5–20 tokens (28%) P99 650ms; >20 tokens (5%) P99 28,000ms.',
      'Model change log: v3.1 deployed yesterday — BERT-base → BERT-large (110M → 340M params), max_len 128 → 512, batch size fixed 32 → dynamic (fills to max_len), top-100 reranking candidates unchanged. Dynamic batching fills sequences to max_len=512, so long queries trigger full 512-token computation for ALL 100 candidates.',
      'Serving infra: A10G 24GB at 100% utilisation on long-query instances, CPU fallback when GPU queue > 500ms, 10s hard timeout. Long-query queue depth 40–120 items. GPU memory 21/24 GB (up from 14). 3 OOM events in last hour.',
      'Traces: a 13-token query and a 1-token query both process 51,200 tokens (512 × 100) because the batch is padded to max_len=512 regardless of actual query length.',
    ],
    question: 'Diagnose the P99 latency explosion and prescribe the fix.',
    steps: [
      {
        question: 'P50 is fine but P99 is 8s, concentrated in long queries, and traces show every query padded to 512 tokens. What is the root cause?',
        options: [
          'Model too large — BERT-large cannot fit on A10G',
          'Dynamic batching pads all sequences to max_len=512 — long queries create massive tensors',
          'Too many reranking candidates (100) for BERT-large',
          'GPU thermal throttling under sustained load',
        ],
        answer: 1,
        finding: 'Dynamic batching fills every sequence to max_len=512, so a 1-token query and a 13-token query both process 51,200 tokens across 100 candidates. BERT attention is O(n2) in sequence length, so long-query batches produce enormous tensors — P99 explodes for the 5% of queries over 20 tokens while P50 stays fine. This is a distribution problem in the tail, not a mean problem.',
      },
      {
        question: 'You have confirmed padding-induced quadratic compute. What is the short-term and medium-term fix?',
        options: [
          'Add more GPUs and raise the timeout',
          'Short-term: roll back to BERT-base or set max_len to 128. Medium-term: sequence-length bucketing (batch similar-length queries, pad only to the longest in the bucket), a per-query latency budget with a reranker bypass over budget, and per-length-bucket P99 monitoring',
          'Reduce reranking candidates from 100 to 10 for all queries',
          'Move the reranker to CPU to avoid GPU OOM',
        ],
        answer: 1,
        finding: 'Short-term, roll back to BERT-base or reduce max_len to 128. Medium-term, implement sequence-length bucketing so queries of similar length batch together and pad only to the bucket max, add a per-query latency budget with a reranker bypass for queries already over budget, and monitor P99 separately per query-length bucket. P99 issues are usually distribution problems — always segment latency by input characteristics.',
      },
    ],
    diagnosis: 'Dynamic batching pads all sequences to max_len=512, so long queries create massive tensors and BERT-large\'s O(n2) attention explodes P99.',
    explanation: 'P99 latency issues are often distribution problems, not mean problems. Always segment latency by input characteristics (query length, candidate count, user tier). Padding-induced quadratic compute is a classic reranker trap — BERT attention scales as O(n2) in sequence length.',
    fix: 'Short-term: roll back to BERT-base or reduce max_len to 128. Medium-term: implement sequence-length bucketing — batch queries of similar length, pad only to the longest in the bucket. Add a per-query latency budget and a reranker bypass for queries exceeding it. Monitor P99 separately for each query-length bucket.',
    source: 'System Design · ML Incident Room · skew_serving',
  },
]

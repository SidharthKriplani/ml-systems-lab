// classical.js — normalized JUDGE drills for subject 'classical_ml'.
// Migrated from src/tabs/ClassicalMLTab.jsx. Only the DRILL modules are carried
// over: Ensemble Decision Lab, Hyperparameter Priority, Naive Bayes Failures.
// The SIM / exposition widgets (Model Failure Zoo, Decision Boundary Lab,
// Bias-Variance Tradeoff) are not judgment drills and are excluded.
//
// Normalization notes:
//   - Ensemble + Hyperparameter items used string `answer` values; those are
//     converted to the integer index of the option they name.
//   - subtopic: ensembles | naive-bayes | optimization. Hyperparameter-Priority
//     items are tagged subtopic:'optimization' per the migration spec.
//   - Ensemble items had no title/question/diagnosis/fix fields; those are
//     synthesized from situation/detail/reasoning so the drill renders under the
//     shared schema. whyNot / whatsTested / antiPattern / staffFraming are folded
//     into explanation. Raw <svg> markup (none present in these items) is stripped.
// Shape: see drillPool.js header.

export const CLASSICAL_DRILLS = [
  // Ensemble Decision Lab
  {
    id: 'classical-ensemble-1',
    subject: 'classical_ml', subtopic: 'ensembles', level: 'mid', type: 'mcq',
    title: 'High-variance model that overfits on a 5k sample dataset',
    context: [
      'High-variance model that overfits on your 5k sample dataset.',
      'Decision tree with max_depth=None. Val accuracy keeps jumping plus/minus 8% across splits.',
    ],
    question: 'Which ensembling strategy fixes this?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Blending', 'Single model', 'Voting Classifier'],
    answer: 0,
    diagnosis: 'High variance maps to bagging — parallel trees on bootstrap samples reduce variance without adding bias.',
    explanation: "Bagging's parallel trees each see different bootstrap samples — reduces variance directly. Boosting would make overfitting worse by doubling down on hard examples. The problem is high variance, not high bias. Why not the others: boosting increases model complexity and would amplify the variance issue; stacking/blending needs more data to be reliable than 5k. Anti-pattern: choosing boosting for a high-variance model makes it worse — boosting doubles down on hard examples and increases model complexity.",
    fix: 'High variance = model is too complex for the data. Bagging averages across diverse samples; boosting adds more complexity. Never use boosting to fix overfitting.',
    source: 'Classical ML - Ensemble Decision Lab',
  },
  {
    id: 'classical-ensemble-2',
    subject: 'classical_ml', subtopic: 'ensembles', level: 'mid', type: 'mcq',
    title: 'High bias — RMSE 0.42 on both train and val, barely beats a mean predictor',
    context: [
      'Model has high bias — RMSE 0.42 on both train and val, barely beats a mean predictor.',
      'Train and val error are nearly identical — the model is underfitting, not overfitting.',
    ],
    question: 'Which ensembling strategy fixes this?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Blending', 'Single model', 'Voting Classifier'],
    answer: 1,
    diagnosis: 'High bias (train approx val, both poor) maps to boosting — sequential residual correction directly reduces bias.',
    explanation: "Boosting sequentially fits residuals — it directly attacks bias. Each new model corrects where the previous one was wrong. Bagging averages parallel models and won't help when the underlying model is underfitting. Why not bagging: it reduces variance, not bias — if every tree underfits, averaging them still underfits. Anti-pattern: bagging when a model underfits — you average 100 underfitting trees and still get an underfitting ensemble.",
    fix: 'Bias-variance diagnosis is gate zero. Train approx val both poor = bias. Train much better than val = variance. Boosting for bias. Bagging for variance. Never mix them up.',
    source: 'Classical ML - Ensemble Decision Lab',
  },
  {
    id: 'classical-ensemble-3',
    subject: 'classical_ml', subtopic: 'ensembles', level: 'mid', type: 'mcq',
    title: '3 strong models from different families: XGBoost, LightGBM, Neural Net',
    context: [
      'You have 3 strong models from different algorithm families: XGBoost, LightGBM, Neural Net.',
      'Each performs similarly on val set. Errors appear uncorrelated — when XGBoost fails, the Neural Net often gets it right.',
    ],
    question: 'Which ensembling strategy extracts the most value?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Blending', 'Single model', 'Voting Classifier'],
    answer: 2,
    diagnosis: 'Uncorrelated errors from diverse model families is exactly where stacking pays off — a meta-learner learns when to trust each base model.',
    explanation: 'Uncorrelated errors from diverse model families = high stacking value. A meta-learner learns when to trust each base model. Simple blending leaves signal on the table — the meta-learner can learn the conditional relationship. Why not blending: it is simpler but treats all models equally; the meta-learner in stacking can discover context-dependent model trust. Anti-pattern: simple averaging (blending) when errors are uncorrelated — you leave the conditional trust signal on the table.',
    fix: 'Stacking payoff = diversity of base models times uncorrelatedness of errors. Same family = correlated errors = low stacking value. XGBoost + LightGBM + linear = high value.',
    source: 'Classical ML - Ensemble Decision Lab',
  },
  {
    id: 'classical-ensemble-4',
    subject: 'classical_ml', subtopic: 'ensembles', level: 'mid', type: 'mcq',
    title: 'Kaggle: 12 models, deadline in 1 hour, no time for a meta-learner',
    context: [
      'Kaggle competition. You have 12 models, deadline in 1 hour, no time to train a meta-learner.',
      'All 12 models have been tuned. You need to combine them fast.',
    ],
    question: 'Which ensembling strategy fits the time budget?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Blending', 'Single model', 'Voting Classifier'],
    answer: 3,
    diagnosis: 'Under time pressure, blend — rank-average or probability average with no meta-learner training required.',
    explanation: "Rank-average or mean of probabilities. Fast, surprisingly effective, no overfitting risk from a meta-learner. Rule of thumb: weight models by (val_AUC - 0.5) squared. Stacking would need a held-out set and time you don't have. Why not stacking: it requires training a meta-learner on out-of-fold predictions — no time for that; single model throws away 11 models worth of work. Anti-pattern: attempting stacking with 1 hour remaining — the meta-learner requires out-of-fold predictions which need the full CV to generate.",
    fix: 'Kaggle end-game: blend first, always. Stacking is for when you have days, not hours. Rank-average weighted by (val_AUC - 0.5) squared is the standard fast ensemble.',
    source: 'Classical ML - Ensemble Decision Lab',
  },
  {
    id: 'classical-ensemble-5',
    subject: 'classical_ml', subtopic: 'ensembles', level: 'senior', type: 'mcq',
    title: 'Binary classifier, tiny dataset (2k samples), maximize signal',
    context: [
      'Binary classifier, dataset is small (2k samples), you want to maximize signal from limited data.',
      "You're tempted to build an ensemble but the dataset is tiny.",
    ],
    question: 'What is the right call?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Blending', 'Single model', 'Voting Classifier'],
    answer: 4,
    diagnosis: 'On 2k samples the ensemble itself overfits — a well-regularized single model + stratified k-fold extracts more signal.',
    explanation: 'Ensembles add complexity and require more data to be reliable. A well-regularized single model + stratified k-fold will outperform a hastily assembled ensemble on 2k samples. With small N, ensemble overfitting risk outweighs the variance reduction benefit. Why not an ensemble: any ensemble on 2k samples risks overfitting the ensemble structure itself — use your data budget for cross-validation, not stacking layers. Anti-pattern: building an ensemble on 2k samples — the ensemble itself can overfit, and you have no data budget left for a reliable stacking holdout set.',
    fix: 'Rule of thumb: need at least 10k samples for stacking to be reliable. Below that: well-regularized single model + stratified k-fold extracts more signal.',
    source: 'Classical ML - Ensemble Decision Lab',
  },
  {
    id: 'classical-ensemble-6',
    subject: 'classical_ml', subtopic: 'ensembles', level: 'mid', type: 'mcq',
    title: '3 specialist classifiers with a majority-vote policy',
    context: [
      '3 specialist classifiers: high-precision tuned, high-recall tuned, balanced. Majority vote policy.',
      'Production requirement: flag fraud if the majority of models say fraud. You want a democratic decision policy.',
    ],
    question: 'Which ensembling strategy implements this?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Blending', 'Single model', 'Voting Classifier'],
    answer: 5,
    diagnosis: 'A democratic majority-vote policy maps to a Voting Classifier, not probability averaging.',
    explanation: 'Each model votes — majority wins. Perfect when you have specialists with complementary objectives and you want a democratic decision policy. Hard voting is ideal here: 2-of-3 models must agree to flag. Why not blending/stacking: blending averages probabilities and can be dominated by the most confident model; stacking would lose the intentional specialization of each model. Anti-pattern: blending (probability average) when you have specialist models — the most confident model dominates and the specialization is effectively lost.',
    fix: 'Hard voting = 2-of-3 agree = decision. Soft voting = averaged probabilities = confidence-weighted. When the policy is democratic by design, hard voting is the implementation.',
    source: 'Classical ML - Ensemble Decision Lab',
  },
  {
    id: 'classical-ensemble-7',
    subject: 'classical_ml', subtopic: 'ensembles', level: 'senior', type: 'mcq',
    title: 'XGBoost plateaued at 0.79 AUC after 3 weeks of feature work',
    context: [
      'Your XGBoost model has plateaued at 0.79 AUC after 3 weeks of feature work. New data arrives daily. Training time is already 4 hours.',
      'Bias is moderate (train AUC 0.81, val 0.79). You need +2 points AUC or the model goes to production as-is. Another team suggests switching from XGBoost (boosting) to a Random Forest (bagging) ensemble.',
    ],
    question: 'Which ensembling strategy has the best shot at +2 points?',
    options: ['Bagging', 'Boosting', 'Stacking', 'Blending', 'Single model', 'Voting Classifier'],
    answer: 2,
    diagnosis: 'A single-family plateau requires cross-family stacking, not a switch from boosting to bagging.',
    explanation: 'A 2-point plateau with moderate bias is not a signal to switch families — it is a signal that a single algorithm family has been exhausted. Stacking across families (XGBoost + LightGBM + a linear model) will extract incremental signal that no single-family ensemble can. Switching XGBoost for Random Forest replaces one boosted ensemble with a bagged ensemble — both are tree-based, errors are correlated, and the gain is marginal. Blending is a weaker version of stacking. The 4-hour training time rules out running boosting with dramatically more trees. Anti-pattern: switching XGBoost for Random Forest at a plateau — both are tree-based, errors are highly correlated, the gain is marginal and not worth 4 more hours of training.',
    fix: 'Single-family plateau = exhausted the algorithm. Stacking across families (trees + linear + neural) extracts residual signal that no tree family can capture. Training budget is the only constraint.',
    source: 'Classical ML - Ensemble Decision Lab',
  },

  // Hyperparameter Priority
  {
    id: 'classical-hyperparam-1',
    subject: 'classical_ml', subtopic: 'optimization', level: 'senior', type: 'mcq',
    title: 'XGBoost: validation AUC plateau at 0.82 after 100 trees',
    context: [
      'Model: XGBoost.',
      'Validation AUC plateau at 0.82 after 100 trees.',
      "Learning curves flatten early. You haven't hit a wall on max_depth.",
    ],
    question: 'Which hyperparameter is the highest-leverage move?',
    options: ['max_depth', 'learning_rate + more trees', 'subsample', 'colsample_bytree'],
    answer: 1,
    diagnosis: 'learning_rate + n_estimators is the highest-leverage pair for a plateau — not architectural knobs. Impact: HIGH.',
    explanation: "Plateau means insufficient model capacity or too-early stopping. Lower LR + more trees (with early stopping) almost always helps. Reducing learning_rate from 0.1 to 0.01 and setting n_estimators=1000 with early_stopping_rounds=20 is the highest-leverage move. Why not the others: max_depth is secondary — tree structure isn't the bottleneck when you plateau at 100 trees; subsample and colsample_bytree are regularization knobs for overfitting, not underfitting. Anti-pattern: tuning max_depth when the model stops improving at 100 trees is looking in the wrong place — depth is not the constraint.",
    fix: 'Plateau at N trees = too few iterations or LR too high. Lower LR, raise n_estimators, use early stopping. That is the default first move on any boosted model plateau.',
    source: 'Classical ML - Hyperparameter Priority',
  },
  {
    id: 'classical-hyperparam-2',
    subject: 'classical_ml', subtopic: 'optimization', level: 'senior', type: 'mcq',
    title: 'Random Forest: inference too slow — 800ms per request in production',
    context: [
      'Model: Random Forest.',
      'Inference too slow — 800ms per request in production.',
      'Accuracy is fine. Pure latency problem.',
    ],
    question: 'Which hyperparameter fixes the latency?',
    options: ['n_estimators', 'max_depth', 'min_samples_leaf', 'max_features'],
    answer: 0,
    diagnosis: 'Random Forest inference time scales linearly with n_estimators, not with depth. Impact: HIGH.',
    explanation: 'Halving n_estimators halves inference time linearly — it is a direct 1:1 relationship. If 500 trees to 800ms, try 100 trees to ~160ms. Check if accuracy degrades acceptably. max_depth has a smaller effect on inference. Why not the others: max_depth affects tree complexity but not the count of trees being traversed; min_samples_leaf changes tree structure but does not reduce the number of trees; max_features affects training, not inference. Anti-pattern: reducing max_depth to improve inference speed — depth affects the number of nodes visited per tree, but traversing 500 shallow trees is still 500 traversals.',
    fix: 'Profile inference before tuning. n_estimators is the only knob with a near-1:1 relationship to latency. Halve it, benchmark, then decide if the accuracy tradeoff is acceptable.',
    source: 'Classical ML - Hyperparameter Priority',
  },
  {
    id: 'classical-hyperparam-3',
    subject: 'classical_ml', subtopic: 'optimization', level: 'senior', type: 'mcq',
    title: 'Logistic Regression: large train/val gap (overfitting) on 1k samples',
    context: [
      'Model: Logistic Regression.',
      'Large train/val gap (overfitting) on 1k samples.',
      'Train accuracy 94%, val accuracy 72%. Only 1,000 training samples.',
    ],
    question: 'Which hyperparameter controls the overfitting?',
    options: ['C (regularization)', 'solver', 'max_iter', 'class_weight'],
    answer: 0,
    diagnosis: 'C is the only model-complexity knob in Logistic Regression — everything else is a convergence or class-distribution parameter. Impact: HIGH.',
    explanation: 'C is the only knob that directly controls overfitting in LR. Decrease C (increase regularization). Default C=1 — try C=0.01 or C=0.1 first. solver and max_iter do not affect model capacity at all. Why not the others: solver is about convergence algorithm and does not change what the model learns; max_iter controls whether optimization converges, not model complexity; class_weight is for imbalance, not overfitting. Anti-pattern: changing solver or increasing max_iter when a model is overfitting — those control how you optimize, not what you optimize for.',
    fix: 'Logistic Regression with a 22-point train/val gap on 1k samples needs one thing: lower C. Start at C=0.01, double until val accuracy stops improving.',
    source: 'Classical ML - Hyperparameter Priority',
  },
  {
    id: 'classical-hyperparam-4',
    subject: 'classical_ml', subtopic: 'optimization', level: 'senior', type: 'mcq',
    title: 'SVM (RBF kernel): underfitting — decision boundary too smooth',
    context: [
      'Model: SVM (RBF kernel).',
      'Model is underfitting — decision boundary too smooth.',
      "Both train and val accuracy are mediocre. The model isn't fitting the training data.",
    ],
    question: 'Which hyperparameter is the primary complexity lever?',
    options: ['C', 'gamma', 'kernel', 'degree'],
    answer: 1,
    diagnosis: 'gamma controls RBF kernel complexity and is the primary lever for underfitting in SVMs. Impact: HIGH.',
    explanation: "Gamma controls how far each training example's influence reaches. Too-small gamma = model too smooth = underfitting. Increase gamma first (try gamma='scale' to 'auto' to explicit values), then increase C. gamma is the primary complexity lever for RBF. Why not the others: C controls the margin penalty — relevant after gamma is set correctly; kernel change is drastic — exhaust RBF tuning first; degree only applies to the polynomial kernel. Anti-pattern: tuning C before gamma on an underfitting SVM — C controls the soft-margin penalty, which is irrelevant when the model cannot fit the training data at all.",
    fix: 'RBF SVM tuning order: gamma first (complexity), then C (regularization). Too-small gamma makes every training point look far from every other — the model sees a flat landscape.',
    source: 'Classical ML - Hyperparameter Priority',
  },
  {
    id: 'classical-hyperparam-5',
    subject: 'classical_ml', subtopic: 'optimization', level: 'mid', type: 'mcq',
    title: 'Decision Tree: massive overfitting — train acc 99%, val acc 61%',
    context: [
      'Model: Decision Tree.',
      'Massive overfitting — train acc 99%, val acc 61%.',
      'Classic overfit profile. The tree has memorized the training set.',
    ],
    question: 'Which hyperparameter is the most direct regularization lever?',
    options: ['max_depth', 'min_samples_split', 'min_samples_leaf', 'criterion'],
    answer: 0,
    diagnosis: 'max_depth is the most direct regularization lever for decision trees — it caps the entire tree structure in one parameter. Impact: HIGH.',
    explanation: "Most direct lever for overfitting in trees. Set max_depth=5 and re-evaluate. This immediately constrains the tree's ability to create arbitrarily fine splits. min_samples_leaf is secondary — useful after you have a reasonable max_depth. Why not the others: min_samples_split helps but is weaker than max_depth at controlling overfit; min_samples_leaf is secondary; criterion (gini vs entropy) almost never matters — 0.1% accuracy difference at most. Anti-pattern: tuning criterion (gini vs entropy) when a tree has memorized the training set — this changes the split metric by 0.1%, not the overfitting.",
    fix: 'Decision tree with 99/61 split: set max_depth=5 immediately. Everything else is secondary. If accuracy is still poor after depth is constrained, then consider min_samples_leaf.',
    source: 'Classical ML - Hyperparameter Priority',
  },
  {
    id: 'classical-hyperparam-6',
    subject: 'classical_ml', subtopic: 'optimization', level: 'senior', type: 'mcq',
    title: 'Neural Net (MLP): loss not converging after 50 epochs',
    context: [
      'Model: Neural Net (MLP).',
      'Loss not converging after 50 epochs.',
      'Loss curve is jagged and oscillating. No clear downward trend.',
    ],
    question: 'Which hyperparameter do you tune first?',
    options: ['learning_rate', 'hidden_layer_sizes', 'activation', 'batch_size'],
    answer: 0,
    diagnosis: 'Jagged oscillating loss is a learning rate diagnostic, not an architecture diagnostic. Impact: HIGH.',
    explanation: 'Non-convergence is almost always an LR problem first. Jagged loss = LR too high. Try 10x lower LR before changing architecture. This is the single highest-impact knob for convergence. Layer sizes are secondary. Why not the others: hidden_layer_sizes affects capacity but will not fix oscillation; activation function rarely causes non-convergence — ReLU is nearly always fine; batch_size affects noise in gradients but LR should be adjusted proportionally anyway. Anti-pattern: changing hidden layer sizes or activation when loss is oscillating — the model is not suffering from insufficient capacity, it is taking steps that are too large.',
    fix: 'Jagged loss = LR too high. Reduce by 10x, observe one epoch. If loss smooths, you found the problem. If loss vanishes, you went too low. Binary search from there.',
    source: 'Classical ML - Hyperparameter Priority',
  },
  {
    id: 'classical-hyperparam-7',
    subject: 'classical_ml', subtopic: 'optimization', level: 'mid', type: 'mcq',
    title: 'k-NN: predictions feel noisy and inconsistent across similar inputs',
    context: [
      'Model: k-NN.',
      'Predictions feel noisy and inconsistent across similar inputs.',
      'Small input changes cause big prediction swings.',
    ],
    question: 'Which hyperparameter controls the bias-variance tradeoff here?',
    options: ['n_neighbors (k)', 'weights', 'algorithm', 'leaf_size'],
    answer: 0,
    diagnosis: 'k (n_neighbors) controls the bias-variance tradeoff in k-NN — small k = high variance = noise. Impact: HIGH.',
    explanation: "Small k = high variance = noise. Increase k (try k = sqrt(n_samples) as a starting rule). More neighbors = smoother decision boundary. weights='distance' is the secondary improvement — closer neighbors get more say. Why not the others: weights affects how neighbors are combined but does not reduce the noise from having too few neighbors; algorithm (ball_tree, kd_tree, brute) and leaf_size only affect speed — zero impact on predictions. Anti-pattern: tuning algorithm or leaf_size when predictions are noisy — those are speed parameters with zero effect on prediction quality.",
    fix: "k-NN with noisy predictions: increase k. Rule of thumb starting point: k = sqrt(n_samples). Then add weights='distance' for a secondary improvement. Speed parameters are irrelevant to quality.",
    source: 'Classical ML - Hyperparameter Priority',
  },
  {
    id: 'classical-hyperparam-8',
    subject: 'classical_ml', subtopic: 'optimization', level: 'senior', type: 'mcq',
    title: 'GradientBoostingClassifier: training takes 4 hours on 100k samples',
    context: [
      'Model: GradientBoostingClassifier.',
      'Training takes 4 hours on 100k samples.',
      'sklearn GradientBoostingClassifier. Pure speed problem.',
    ],
    question: 'Which hyperparameter reduces training time with the least accuracy cost?',
    options: ['n_estimators', 'learning_rate', 'subsample', 'max_features'],
    answer: 2,
    diagnosis: 'Stochastic GBM (subsample < 1.0) reduces training time and often improves generalisation — and the real fix is switching to LightGBM. Impact: MEDIUM (switch to LightGBM for HIGH).',
    explanation: "Setting subsample=0.8 activates stochastic GBM — reduces training time by ~20% while often improving generalisation. But the real fix: switch to LightGBM entirely. It's 10-20x faster than sklearn GBM on the same data via histogram-based splitting. Why not the others: n_estimators reduces trees but hurts accuracy proportionally; learning_rate alone doesn't affect speed; max_features helps but less than subsample for GBM. Anti-pattern: reducing n_estimators to speed up training — it works but hurts accuracy proportionally, whereas subsample reduces training work per tree without proportional accuracy loss.",
    fix: "sklearn GBM at 4 hours = wrong tool. Switch to LightGBM first (10-20x faster, histogram-based). If stuck with sklearn GBM: subsample=0.8, then reduce n_estimators only if subsample isn't enough.",
    source: 'Classical ML - Hyperparameter Priority',
  },

  // Naive Bayes Failures
  {
    id: 'classical-naive-bayes-1',
    subject: 'classical_ml', subtopic: 'naive-bayes', level: 'mid', type: 'mcq',
    title: 'Text classification with correlated features',
    context: 'You are building a spam classifier using Multinomial Naive Bayes on bag-of-words features. Training accuracy is 97%. In production, performance drops to 71%. Investigation shows that spam emails consistently contain both "free" and "money" together, while your model treats them as independent features.',
    question: 'Why does Naive Bayes fail here, and what is the correct fix?',
    options: [
      'Naive Bayes requires more training data — 97% training accuracy means the model is underfitted.',
      'The conditional independence assumption is violated: P("money" | spam) and P("free" | spam) are not independent — they co-occur systematically. Naive Bayes multiplies these probabilities as if they are independent, amplifying the spam signal far beyond its true strength and making the model overconfident on correlated features.',
      'Bag-of-words features should be replaced with TF-IDF before using Naive Bayes.',
      'Naive Bayes cannot handle binary classification — use logistic regression instead.',
    ],
    answer: 1,
    diagnosis: 'The independence assumption is the core limitation of Naive Bayes. When features co-occur systematically (as in spam: "free" + "money" + "click"), the model multiplies their conditional probabilities, treating them as independent evidence. This causes probability estimates to become extremely overconfident — Naive Bayes will assign probabilities near 1.0 to spam when correlated features appear, making it poorly calibrated even when directionally correct.',
    explanation: 'The independence assumption is the core limitation of Naive Bayes. When features co-occur systematically (as in spam: "free" + "money" + "click"), the model multiplies their conditional probabilities, treating them as independent evidence. This causes probability estimates to become extremely overconfident — Naive Bayes will assign probabilities near 1.0 to spam when correlated features appear, making it poorly calibrated even when directionally correct.',
    fix: 'Three options: (1) Accept the limitation and use Naive Bayes for its speed/simplicity, monitoring calibration separately. (2) Add feature selection to remove highly correlated features, reducing redundant evidence. (3) Switch to a model that handles feature dependencies: logistic regression, random forest, or gradient boosting — all of which model feature interactions without assuming independence.',
    source: 'Classical ML - Naive Bayes Failures',
  },
  {
    id: 'classical-naive-bayes-2',
    subject: 'classical_ml', subtopic: 'naive-bayes', level: 'mid', type: 'mcq',
    title: 'Gaussian Naive Bayes on skewed numeric features',
    context: 'A fraud detection model uses Gaussian Naive Bayes on transaction amount, time since last transaction, and account age. Training AUC is 0.82. In production, high-value transactions (>$10,000) are almost never flagged as fraud, even when other signals are strong. Investigation reveals transaction amounts follow a heavy-tailed Pareto distribution.',
    question: 'What is the core failure mode?',
    options: [
      'Gaussian Naive Bayes requires feature standardisation (z-score normalisation) before training.',
      'The model needs more fraud examples for high-value transactions (class imbalance).',
      'Gaussian Naive Bayes assumes each feature follows a Gaussian (normal) distribution. Transaction amounts follow a heavy-tailed Pareto distribution — the Gaussian assumption severely underestimates the probability of high-value amounts under the fraud class, making the model systematically ignore high-value transactions.',
      'Naive Bayes cannot handle financial data — use a neural network instead.',
    ],
    answer: 2,
    diagnosis: 'The Gaussian NB likelihood P(amount | fraud) is computed assuming the feature is normally distributed. For a Pareto-distributed feature, the Gaussian fit places nearly zero probability mass on values in the tail (>$10,000). This means even if $10,000 transactions are disproportionately fraudulent, the model assigns near-zero P(amount=$10,000 | fraud), dominating the posterior and suppressing the fraud prediction.',
    explanation: 'The Gaussian NB likelihood P(amount | fraud) is computed assuming the feature is normally distributed. For a Pareto-distributed feature, the Gaussian fit places nearly zero probability mass on values in the tail (>$10,000). This means even if $10,000 transactions are disproportionately fraudulent, the model assigns near-zero P(amount=$10,000 | fraud), dominating the posterior and suppressing the fraud prediction.',
    fix: 'Replace Gaussian assumption with a distribution that fits the data: log-transform the transaction amount (making it approximately log-normal) before training, or use a kernel density estimator (KDE NB) which makes no parametric assumption about the feature distribution. Alternatively, bucket the amount feature into quantile bins and use Categorical/Multinomial NB, which makes no distributional assumption.',
    source: 'Classical ML - Naive Bayes Failures',
  },
  {
    id: 'classical-naive-bayes-3',
    subject: 'classical_ml', subtopic: 'naive-bayes', level: 'junior', type: 'mcq',
    title: 'Zero-frequency problem in production',
    context: 'A product category classifier uses Multinomial Naive Bayes trained on 50,000 product descriptions. In production, it fails with a probability of 0.0 for any product containing a word not seen during training — even if every other word strongly indicates the category.',
    question: 'What causes this and what is the standard fix?',
    options: [
      'The training vocabulary needs to be expanded — the model did not see enough training examples.',
      'The zero-frequency problem: if a word never appeared in training, P(word | class) = 0. Since Naive Bayes multiplies all feature probabilities, a single zero collapses the entire posterior to 0.0, regardless of how strong the other signals are. One unseen word destroys the prediction.',
      'Out-of-vocabulary words should be removed from the input before prediction.',
      'The model needs to be retrained with TF-IDF instead of raw counts.',
    ],
    answer: 1,
    diagnosis: 'Multinomial Naive Bayes computes the product of P(word_i | class) for all words in the document. If any word has P(word_i | class) = 0 (because it never appeared in training), the entire product evaluates to 0.0 regardless of how many other words strongly indicate the class. This is a fundamental failure mode for any real deployment where the input vocabulary will differ from training.',
    explanation: 'Multinomial Naive Bayes computes the product of P(word_i | class) for all words in the document. If any word has P(word_i | class) = 0 (because it never appeared in training), the entire product evaluates to 0.0 regardless of how many other words strongly indicate the class. This is a fundamental failure mode for any real deployment where the input vocabulary will differ from training.',
    fix: 'Apply Laplace smoothing (add-1 smoothing): add 1 to every word count in training, including words that never appeared. This gives every possible word a small non-zero probability. In sklearn: MultinomialNB(alpha=1.0) (default). For stronger smoothing on rare words, tune alpha: higher alpha = more smoothing = more conservative probability estimates. Also implement an OOV (out-of-vocabulary) token for unknown words.',
    source: 'Classical ML - Naive Bayes Failures',
  },
]

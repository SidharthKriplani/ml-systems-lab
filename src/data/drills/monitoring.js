// monitoring.js — normalized JUDGE drills for subject 'monitoring'.
// Migrated from src/tabs/MonitoringTab.jsx. Only the DRILL modules are carried
// over: Coverage Audit (MonitorCoverageAudit blind-spot cards), Alerting
// Decisions (AlertingDecisionTree AccordionMCQ), Drift Attribution
// (DriftAttribution AccordionMCQ), and Incident Triage (reveal cards → mcq).
//
// SKIPPED (interactive SIM / lab widgets, not drills): Drift Dashboard, PSI Lab,
// KS Test Explorer, Alert Tuner, Live Drift Lab.
//
// One monitoring item already lives in drillPool.js
// (monitoring-incident-zero-variance) and is NOT duplicated here.
//
// Coverage Audit blind spots have no native MCQ; each is framed as an mcq where
// the correct answer is the real blind-spot issue and the distractors are
// sibling blind spots or wrong framings. Raw <svg>/JSX (cloudMap, icons) is
// stripped. Shape: see drillPool.js header.

export const MONITORING_DRILLS = [
  // ─── Coverage Audit (subtopic: coverage) ──────────────────────────────────
  {
    id: 'mon-coverage-raw-value-range',
    subject: 'monitoring', subtopic: 'coverage', level: 'mid', type: 'mcq',
    title: 'Raw ingestion monitors volume, schema, freshness — what silently slips through?',
    context: [
      'Pipeline stage: Raw Data Ingestion',
      'Typically monitored: row count per batch, schema validation (names + types), freshness (time since last ingest)',
      'Example: age = -5, revenue = 10^15 flow straight into the feature store',
    ],
    question: 'Volume, schema, and freshness are all green. Which blind spot lets garbage into the model?',
    options: [
      'Value range violations — no min/max bounds checks, so out-of-range values are used silently',
      'Latency — the ingest job is running too slowly',
      'Schema drift — a column type changed',
      'Freshness — the batch is stale',
    ],
    answer: 0,
    diagnosis: 'Value range violations — schema checks pass on type but not on plausibility',
    explanation: 'Row count, schema, and freshness all pass while age = -5 or revenue = 10^15 sail through as valid floats. The model silently consumes garbage values because nothing asserts each column stays within physical bounds.',
    fix: 'Add min/max bounds checks per column. Alert if >0.1% of rows violate bounds.',
    source: 'Monitor · Coverage Audit · Raw Data Ingestion',
  },
  {
    id: 'mon-coverage-raw-referential-integrity',
    subject: 'monitoring', subtopic: 'coverage', level: 'senior', type: 'mcq',
    title: 'Nulls appear downstream but the ingest job reports success',
    context: [
      'Pipeline stage: Raw Data Ingestion',
      'Typically monitored: row count, schema validation, freshness',
      'Example: a user_id in the events table has no matching row in the users table',
    ],
    question: 'The ingest job is green but downstream joins produce nulls. What is the uncovered failure mode?',
    options: [
      'Freshness — events arrived late',
      'Referential integrity — orphaned keys cause silent join failures / null features downstream',
      'Value range — a numeric feature went out of bounds',
      'Schema — a column was renamed',
    ],
    answer: 1,
    diagnosis: 'Referential integrity gap — orphaned foreign keys fail joins silently',
    explanation: 'A user_id in events with no matching users row is structurally valid per schema and row-count checks, but the downstream join drops or nulls the row. Nothing monitors the join match rate, so the feature quietly goes null.',
    fix: 'Run daily uniqueness + FK checks. Alert if match rate drops below 98%.',
    source: 'Monitor · Coverage Audit · Raw Data Ingestion',
  },
  {
    id: 'mon-coverage-feature-psi',
    subject: 'monitoring', subtopic: 'coverage', level: 'senior', type: 'mcq',
    title: 'Feature pipeline is healthy but scores compress after a campaign',
    context: [
      'Pipeline stage: Feature Pipeline',
      'Typically monitored: null rates per feature, pipeline run time, output row count',
      'Example: mean_purchase_7d shifted from $45 to $12 after a discount campaign',
    ],
    question: 'Null rates, run time, and row count are all normal, yet model scores compress. What is the blind spot?',
    options: [
      'Pipeline run time regressed',
      'Distribution shift (PSI) on a key feature is unmonitored — the feature moved without any null or row-count signal',
      'Row count dropped',
      'A column type changed',
    ],
    answer: 1,
    diagnosis: 'Unmonitored distribution shift — PSI on high-weight features is missing',
    explanation: 'mean_purchase_7d moving from $45 to $12 keeps null rate, row count, and runtime perfectly stable. Only PSI (or an equivalent distribution monitor) on the feature would surface the shift; without it, scores compress with no explanation.',
    fix: 'Monitor PSI weekly for top-10 features by model weight. Page if PSI > 0.2.',
    source: 'Monitor · Coverage Audit · Feature Pipeline',
  },
  {
    id: 'mon-coverage-feature-window-boundary',
    subject: 'monitoring', subtopic: 'coverage', level: 'staff', type: 'mcq',
    title: 'Feature values spike on specific days and nobody can see why',
    context: [
      'Pipeline stage: Feature Pipeline',
      'Typically monitored: null rates, run time, row count',
      'Example: a rolling 7-day window accidentally includes 8 days after a DST transition',
    ],
    question: 'A rolling aggregate spikes on isolated days with clean nulls and row counts. What is the uncovered bug class?',
    options: [
      'Distribution shift on the raw input',
      'Aggregation window boundary bugs — off-by-one windows (e.g. DST) inflate values without tripping standard monitors',
      'Referential integrity between tables',
      'Serving-side feature computation returning a default',
    ],
    answer: 1,
    diagnosis: 'Aggregation window boundary bug — off-by-one at DST / calendar edges',
    explanation: 'A 7-day window silently spanning 8 days after a DST change inflates the aggregate on specific days. Null rate and row count stay clean, so the spike is easy to miss and hard to attribute.',
    fix: 'Unit test window boundaries against known reference dates. Include DST edge cases.',
    source: 'Monitor · Coverage Audit · Feature Pipeline',
  },
  {
    id: 'mon-coverage-serving-score-distribution',
    subject: 'monitoring', subtopic: 'coverage', level: 'senior', type: 'mcq',
    title: 'CTR drops with no latency or error spike',
    context: [
      'Pipeline stage: Model Serving',
      'Typically monitored: request latency (P50, P99), error rate, request volume',
      'Example: scores compressed to 0.48–0.52 — the model outputs near-prior for every user',
    ],
    question: 'Latency, error rate, and volume are all fine, but CTR falls. What is serving not watching?',
    options: [
      'Score distribution — mean/std/percentiles of predictions are unmonitored, so a collapse to the prior is invisible',
      'Request latency at P99',
      'Error rate on the serving endpoint',
      'Request volume into the model',
    ],
    answer: 0,
    diagnosis: 'Missing score-distribution monitoring — prediction diversity collapse is silent',
    explanation: 'When scores compress to 0.48–0.52 the model is emitting the prior for everyone. Latency, errors, and volume are unaffected, so the only signal is the shape of the score distribution itself — which nothing is tracking.',
    fix: 'Monitor score distribution mean, std, and percentiles. Alert if std drops >50% from baseline.',
    source: 'Monitor · Coverage Audit · Model Serving',
  },
  {
    id: 'mon-coverage-serving-calibration',
    subject: 'monitoring', subtopic: 'coverage', level: 'staff', type: 'mcq',
    title: 'High-confidence predictions are wrong more often than their score implies',
    context: [
      'Pipeline stage: Model Serving',
      'Typically monitored: latency, error rate, request volume',
      'Example: the model says 90% confidence but is only right 60% of the time',
    ],
    question: 'Serving metrics are green, but downstream systems over-trust the scores. What is uncovered?',
    options: [
      'Score distribution mean has shifted',
      'Prediction confidence calibration — no ECE / reliability check, so miscalibrated confidence goes undetected',
      'Request volume dropped',
      'Latency at P99 regressed',
    ],
    answer: 1,
    diagnosis: 'Calibration is unmonitored — confidence and accuracy have diverged',
    explanation: 'A model claiming 90% confidence while only 60% correct will pass every latency/error/volume check. Downstream systems act on the inflated confidence. Only a periodic calibration check surfaces the gap.',
    fix: 'Run calibration checks (ECE, reliability diagram) on a holdout set monthly.',
    source: 'Monitor · Coverage Audit · Model Serving',
  },
  {
    id: 'mon-coverage-labels-delay',
    subject: 'monitoring', subtopic: 'coverage', level: 'staff', type: 'mcq',
    title: 'Training data looks healthy but retraining makes the model worse',
    context: [
      'Pipeline stage: Label Collection',
      'Typically monitored: label volume, label pipeline freshness',
      'Example: conversion labels take 7–30 days to arrive; a model trained on the last 7 days sees mostly null labels',
    ],
    question: 'Label volume and freshness look fine, yet performance drops after retraining. What is the blind spot?',
    options: [
      'Label distribution shift',
      'Label delay tracking — recent cohorts have low label coverage, so retraining learns from mostly-null labels',
      'Referential integrity in the events table',
      'Serving latency during label writes',
    ],
    answer: 1,
    diagnosis: 'Unmonitored label delay — recent cohorts lack mature labels',
    explanation: 'If conversion labels take 7–30 days, the last 7 days of "training data" is dominated by nulls. Label volume and freshness both look healthy, so retraining silently learns from immature cohorts and degrades.',
    fix: "Track label coverage rate by prediction cohort. Don't train on cohorts with <80% label coverage.",
    source: 'Monitor · Coverage Audit · Label Collection',
  },
  {
    id: 'mon-coverage-labels-distribution',
    subject: 'monitoring', subtopic: 'coverage', level: 'senior', type: 'mcq',
    title: 'Model "improves" (lower FPR) but precision drops',
    context: [
      'Pipeline stage: Label Collection',
      'Typically monitored: label volume, label pipeline freshness',
      'Example: fraud rate dropped from 1.2% to 0.4% after an upstream rule-based filter was added',
    ],
    question: 'FPR looks better after an upstream change, but precision falls. What is uncovered in label monitoring?',
    options: [
      'Label delay in recent cohorts',
      'Label distribution shift — the positive rate changed upstream, so metrics move for the wrong reason',
      'Feature PSI on a top feature',
      'Serving score compression',
    ],
    answer: 1,
    diagnosis: 'Unmonitored label distribution shift — the base rate moved upstream',
    explanation: 'An upstream rule filter dropping fraud rate from 1.2% to 0.4% makes the model look like it improved (lower FPR) while precision falls because true positives are being filtered before they reach the model. Only tracking the label positive rate exposes this.',
    fix: 'Monitor label positive rate as a KPI. Alert if positive rate changes >20% week-over-week.',
    source: 'Monitor · Coverage Audit · Label Collection',
  },
  {
    id: 'mon-coverage-business-correlation',
    subject: 'monitoring', subtopic: 'coverage', level: 'staff', type: 'mcq',
    title: 'AUC is stable but revenue is dropping',
    context: [
      'Pipeline stage: Business Metrics',
      'Typically monitored: daily/weekly KPI dashboard',
      'Example: model AUC is stable but conversion rate is dropping — the model optimizes the wrong proxy',
    ],
    question: 'The model looks fine and the KPI dashboard exists, yet ML and stakeholders disagree on health. What is missing?',
    options: [
      'Segment-level breakdowns of the KPI',
      'Model-to-metric correlation monitoring — nothing tracks whether the model score still moves the business metric',
      'Score distribution monitoring at serving',
      'Label coverage by cohort',
    ],
    answer: 1,
    diagnosis: 'No model-to-metric correlation monitoring — proxy has decoupled from value',
    explanation: 'AUC can hold steady while the model optimizes a proxy that has drifted away from the real business outcome. Without tracking the correlation between model score and the KPI over time, the ML team says the model is fine while revenue falls.',
    fix: 'Track correlation between model score and business metric weekly. Alert if correlation drops >0.1.',
    source: 'Monitor · Coverage Audit · Business Metrics',
  },
  {
    id: 'mon-coverage-business-segment',
    subject: 'monitoring', subtopic: 'coverage', level: 'senior', type: 'mcq',
    title: 'Overall metrics stable, but a mobile complaint arrives first',
    context: [
      'Pipeline stage: Business Metrics',
      'Typically monitored: daily/weekly KPI dashboard',
      'Example: overall metrics are stable but mobile users degraded 15pp — hidden by desktop volume',
    ],
    question: 'Aggregate KPIs look flat but a segment is failing. What is the coverage gap?',
    options: [
      'Model-to-metric correlation over time',
      'Segment-level monitoring — aggregate KPIs mask per-segment (device/geo/cohort) failures',
      'Calibration on a holdout set',
      'Value-range checks at ingestion',
    ],
    answer: 1,
    diagnosis: 'No segment-level monitoring — aggregates hide localized degradation',
    explanation: 'A 15pp drop for mobile users disappears inside a desktop-dominated aggregate. The first signal becomes a complaint from the mobile product team, because no monitor breaks the KPI out by segment.',
    fix: 'Monitor key segments (device, geo, user cohort) separately. Aggregate metrics hide segment failures.',
    source: 'Monitor · Coverage Audit · Business Metrics',
  },

  // ─── Alerting Decisions (subtopic: alerting) ──────────────────────────────
  {
    id: 'mon-alerting-psi-maintenance-window',
    subject: 'monitoring', subtopic: 'alerting', level: 'senior', type: 'mcq',
    title: 'PSI spike on user_age at 3am',
    context: 'Your monitoring system fires at 3:12am: PSI = 0.31 on user_age for the fraud detection model. Model AUC has dropped from 0.83 to 0.79 over the last 6 hours. P95 serving latency is unchanged. The upstream user profile pipeline had a scheduled maintenance window from 2am–3am.',
    question: 'The PSI spike and AUC drop overlap exactly with the 2am–3am maintenance window. Do you page on-call and roll back, or hold?',
    options: [
      'Page the on-call ML engineer and roll back the model immediately.',
      'The PSI spike and AUC drop are consistent with a data pipeline issue during the maintenance window — not a model failure. Log the alert, check whether the upstream pipeline has recovered post-maintenance, and monitor for 30 minutes before escalating.',
      'Retrain the model on the most recent 7 days of data.',
      'Disable the fraud model and fall back to the rules engine.',
    ],
    answer: 1,
    diagnosis: 'Temporal correlation matters: PSI spike overlapping exactly with a scheduled maintenance window is almost certainly a pipeline artifact, not a model failure. Rollback and retraining are expensive and irreversible actions. The correct response is to determine whether the root cause is transient (maintenance window data gap) before escalating.',
    fix: 'Build alert context into your monitoring: tag each alert with any overlapping maintenance windows, recent deployments, or known data pipeline events. If PSI spikes coincide with a known infrastructure event, auto-suppress the page and create a watch-and-log entry instead. Escalate only if PSI remains elevated >1 hour after the maintenance window closes.',
    source: 'Monitor · Alerting Decisions · alert1',
  },
  {
    id: 'mon-alerting-auc-drop-batch-coverage',
    subject: 'monitoring', subtopic: 'alerting', level: 'senior', type: 'mcq',
    title: 'AUC drops 8 points — batch vs. real-time serving',
    context: 'A content recommendation model shows AUC dropping from 0.76 to 0.68 over 48 hours. PSI is stable across all input features. The model serves batch pre-computed recommendations refreshed every 4 hours. Investigation shows that a new content category was launched 3 days ago and now represents 18% of user interactions.',
    question: 'All input feature PSI values are stable, but AUC dropped 8 points in 48 hours after a new content category launched. PSI shows nothing wrong — so where is the model failing?',
    options: [
      'PSI is calculated incorrectly — the monitoring system has a bug.',
      'Batch serving means the model never saw the new content category — it cannot recommend items it was never trained on. PSI measures input feature distributions, which are stable. AUC degradation comes from missing coverage, not feature drift.',
      'AUC degradation always lags PSI changes by 48 hours.',
      'The new content category is reducing engagement across all recommendations.',
    ],
    answer: 1,
    diagnosis: 'PSI and AUC measure different things. PSI monitors input feature distributions — which are stable because user features have not changed. AUC monitors prediction quality — which degrades because the model has no coverage for a new content category that now drives 18% of interactions. You need output distribution monitoring, not just input monitoring.',
    fix: 'Add coverage monitoring: track the percentage of serving requests where the model has low confidence (prediction score < 0.5) or returns items from a category absent from training data. When a new content category launches, trigger a model evaluation on the new-category slice immediately. For batch serving, shorten the refresh cycle or add a real-time fallback for cold-start categories.',
    source: 'Monitor · Alerting Decisions · alert2',
  },
  {
    id: 'mon-alerting-latency-breach-rollback',
    subject: 'monitoring', subtopic: 'alerting', level: 'senior', type: 'mcq',
    title: 'Latency breach at peak — auto-rollback or hold?',
    context: 'P95 serving latency for your real-time pricing model breaches SLA (>200ms) for 8 consecutive minutes during peak load at 6pm. The breach started immediately after a model update was promoted to 100% traffic. PSI and AUC are both stable. Infrastructure team confirms no server-side issues.',
    question: 'P95 latency breached 200ms SLA at exactly the moment the new model went to 100% traffic. PSI and AUC are both stable. Do you rollback now or investigate first?',
    options: [
      'Hold — latency breaches are infrastructure problems, not model problems.',
      'Auto-rollback immediately — any SLA breach warrants rollback.',
      "The timing correlation (latency breach = model update) and stable PSI/AUC strongly suggest the new model has higher computational complexity. Rollback to the previous model to restore SLA, then profile the new model's inference path before re-promoting.",
      'Scale up serving infrastructure and keep the new model live.',
    ],
    answer: 2,
    diagnosis: 'When a latency breach starts precisely at a model update and infrastructure is healthy, the model is the most likely cause — larger model size, more features, or a more complex decision tree depth. Holding and investigating while users experience SLA breaches is wrong. Rollback first, profile second.',
    fix: 'Add latency to the model promotion gate: run a load test at expected peak QPS before promoting any model to 100% traffic. Gate on P95 < 150ms (giving 50ms headroom to SLA). For the specific incident: rollback, then profile the new model — compare feature count, tree depth, and inference time against the previous version. Re-promote only after confirming P95 at peak load.',
    source: 'Monitor · Alerting Decisions · alert3',
  },

  // ─── Drift Attribution (subtopic: drift) ──────────────────────────────────
  {
    id: 'mon-drift-attribution-impact-score',
    subject: 'monitoring', subtopic: 'drift', level: 'senior', type: 'mcq',
    title: 'PSI elevated — which feature is actually driving it?',
    context: 'Your aggregate monitoring dashboard shows overall PSI = 0.22 across 47 input features for a credit scoring model. The alert threshold is 0.20. You need to identify which features are driving the drift before filing an incident.',
    question: 'Aggregate PSI just crossed your 0.20 alert threshold across 47 features. You have 5 minutes before the incident ticket auto-escalates. How do you find which feature is actually threatening model performance?',
    options: [
      'Compute PSI for each feature individually and rank by PSI value descending.',
      'PSI is already computed per feature — sort by PSI descending, focus on features with PSI > 0.10, then cross-reference with feature importance from the trained model. A high-PSI feature that is also high-importance is the real risk; a high-PSI feature with near-zero importance is noise.',
      'Retrain the model and compare performance to identify which features degraded.',
      'File the incident based on the aggregate PSI alone — individual feature analysis is too slow for on-call response.',
    ],
    answer: 1,
    diagnosis: 'PSI alone is not enough — it measures distributional shift but not impact. A feature with PSI = 0.45 that contributes 0.1% to model predictions is less dangerous than a feature with PSI = 0.12 that is the top feature by importance. The intersection of drift magnitude and model dependence determines actual risk.',
    fix: 'Build a drift impact score: drift_impact = PSI_feature × feature_importance_rank. Sort by drift impact, not raw PSI. In your incident report, list the top 3 features by drift impact, their PSI values, their importance percentile, and the upstream data source responsible. This immediately focuses the data engineering response on the right pipeline.',
    source: 'Monitor · Drift Attribution · drift1',
  },
  {
    id: 'mon-drift-covariate-vs-label',
    subject: 'monitoring', subtopic: 'drift', level: 'staff', type: 'mcq',
    title: 'Covariate drift vs. label drift — different responses',
    context: 'A loan default model shows: (1) PSI = 0.28 on debt_to_income_ratio over the last 30 days — users applying for loans have higher DTI than training distribution. (2) Observed default rate has dropped from 3.2% to 2.1% over the same period. Model predicted default rate is still 3.1%.',
    question: 'debt_to_income_ratio PSI is 0.28 (high-DTI applicants are up). Simultaneously, observed default rate dropped from 3.2% to 2.1% while the model still predicts 3.1%. Are these the same problem or two separate failures requiring different fixes?',
    options: [
      'Covariate drift only — the feature distribution shifted. Retrain on recent data.',
      'Label drift only — the default rate changed. Update the decision threshold.',
      'Both: covariate drift (DTI distribution shifted) and label drift (true default rate dropped). These require different responses — feature drift requires pipeline investigation, label drift requires threshold recalibration and potentially retraining with recent labels.',
      'Neither — a 1% drop in default rate is within normal seasonal variation.',
    ],
    answer: 2,
    diagnosis: 'Covariate and label drift are independent failure modes that happen to co-occur here. Covariate drift (DTI shift) means the input distribution has changed — model predictions may be less reliable in the new region of feature space. Label drift (true default rate drop) means the model is now miscalibrated — it still predicts 3.1% default when the true rate is 2.1%, causing systematic over-rejection of creditworthy applicants.',
    fix: 'Respond to each independently: (1) For covariate drift — investigate the DTI pipeline, check if loan application eligibility criteria changed, assess whether the model has enough training data in the new DTI range. (2) For label drift — recalibrate the decision threshold using recent outcome data (Platt scaling or isotonic regression on 90-day labels). Schedule a retrain on data from the post-shift period once enough labels accumulate.',
    source: 'Monitor · Drift Attribution · drift2',
  },
  {
    id: 'mon-drift-concept-without-input',
    subject: 'monitoring', subtopic: 'drift', level: 'staff', type: 'mcq',
    title: 'Concept drift without input drift',
    context: 'A fraud detection model shows stable input feature distributions (all PSI < 0.10) and stable serving latency. But precision on confirmed fraud cases has dropped from 0.81 to 0.63 over 6 weeks. The fraud team reports that fraud patterns have changed — new account takeover techniques using legitimate-looking session behaviour.',
    question: 'All 47 input feature PSI values are below 0.10 — your monitoring shows nothing wrong. But fraud precision has collapsed from 0.81 to 0.63 over 6 weeks. What is your monitoring blind spot, and why can PSI not detect this failure?',
    options: [
      'The monitoring system computed PSI incorrectly.',
      "Concept drift: the relationship between features and the fraud label has changed, even though the feature distributions are stable. New fraud techniques produce inputs that look identical to legitimate behaviour — the model's learned boundary is no longer valid.",
      'The model needs more training data.',
      'Precision dropped because more legitimate transactions are being processed.',
    ],
    answer: 1,
    diagnosis: 'Concept drift is invisible to input monitoring. PSI measures whether features look different — it cannot detect whether the same feature values now mean something different. New fraud techniques specifically exploit this: they produce feature distributions indistinguishable from legitimate behaviour, making PSI-based monitoring blind to the change.',
    fix: 'Add outcome-based monitoring: track precision, recall, and F1 on confirmed fraud labels as they come in (typically 2–4 week delay). When outcome metrics degrade with stable input distributions, the diagnosis is concept drift. Response: increase human review sampling rate to build a fresh labeled dataset, then retrain on recent data. Add adversarial test cases representing known new fraud patterns to the model evaluation suite.',
    source: 'Monitor · Drift Attribution · drift3',
  },

  // ─── Incident Triage (subtopic: incident) — reveal cards → mcq ─────────────
  {
    id: 'mon-incident-acc-drop-no-deploy',
    subject: 'monitoring', subtopic: 'incident', level: 'senior', type: 'mcq',
    title: 'Accuracy dropped 8pp overnight, no deployment in 48h',
    context: [
      'Alert: Accuracy dropped 8pp overnight. No deployment in the last 48h.',
      'Severity: high',
      'Hypothesis: data distribution shift (seasonal, upstream schema change, or pipeline failure).',
    ],
    question: 'What is your immediate action?',
    options: [
      'Immediately roll back to the previous model.',
      'Investigate first — do not rollback yet. Check PSI on all input features before touching the model.',
      'Retrain the model on the last 7 days.',
      'Lower the decision threshold to recover accuracy.',
    ],
    answer: 1,
    diagnosis: 'Data distribution shift (seasonal, upstream schema change, or data pipeline failure). Check PSI on all input features before touching the model.',
    explanation: 'Triage: (1) Check data freshness — is the feature pipeline producing stale or incomplete data? (2) Run PSI on top-10 features and isolate which shifted. (3) Check if the label distribution changed (delayed labels, label schema change). (4) If input features are clean but predictions are wrong → the model has degraded; only then consider retraining or rollback.',
    fix: "Anti-pattern to avoid: immediately rolling back to the previous model. If the cause is data drift, rollback won't help — the old model is equally blind to the new distribution.",
    source: 'Monitor · Incident Triage · acc_drop',
  },
  {
    id: 'mon-incident-null-spike',
    subject: 'monitoring', subtopic: 'incident', level: 'senior', type: 'mcq',
    title: 'Null rate on user_session_duration spiked 2% → 34%',
    context: [
      'Alert: Null rate on feature user_session_duration spiked from 2% to 34%.',
      'Severity: high',
      'Hypothesis: upstream event-tracking change or ingestion failure; the model is now imputing nulls and producing biased predictions.',
    ],
    question: 'What is your immediate action?',
    options: [
      'Treat it as a model problem and retrain.',
      'Page data engineering immediately — this is a pipeline incident.',
      'Lower the decision threshold to compensate.',
      'Roll back the model to the previous version.',
    ],
    answer: 1,
    diagnosis: 'Upstream event tracking change or ingestion failure. The model is now imputing nulls, producing systematically biased predictions.',
    explanation: 'Triage: (1) Check the ingestion pipeline — was there a deploy or schema change in the event tracker? (2) Check whether nulls correlate with a user segment, device, or geography. (3) Assess model impact: what does the model output when this feature is null — is the imputation value causing score collapse? (4) Hotfix: if imputation is causing harm, route affected traffic to a fallback model that does not use this feature.',
    fix: 'Anti-pattern to avoid: treating this as a model problem. The model is behaving correctly given the null — the problem is upstream.',
    source: 'Monitor · Incident Triage · null_spike',
  },
  {
    id: 'mon-incident-score-collapse',
    subject: 'monitoring', subtopic: 'incident', level: 'senior', type: 'mcq',
    title: 'Score distribution compressed to 0.45–0.55',
    context: [
      'Alert: Model score distribution compressed — 90% of users scoring 0.45–0.55 (was 0.15–0.85).',
      'Severity: medium',
      'Hypothesis: a high-weight feature has gone constant or very low-variance; the model is outputting the prior.',
    ],
    question: 'What is your immediate action?',
    options: [
      'Retrain the model immediately.',
      'Investigate — prediction diversity loss, likely a feature issue. Check feature variance for top features by weight.',
      'Roll back to yesterday\'s model.',
      'Widen the decision threshold band.',
    ],
    answer: 1,
    diagnosis: 'A high-weight feature has gone to a constant value or has very low variance. The model is essentially outputting the prior.',
    explanation: 'Triage: (1) Check feature variance for the top-5 features by model weight — look for features that became constant. (2) Check whether a categorical feature encoding changed (e.g. a new category ID mapping to an OOV token). (3) Check serving-side feature computation — is a real-time feature service returning a default value? (4) If the feature is fine but scores are still collapsed, check the serving code for sigmoid/softmax being applied twice.',
    fix: "Anti-pattern to avoid: retraining immediately. The feature issue will persist through retraining and you'll waste compute.",
    source: 'Monitor · Incident Triage · score_collapse',
  },
  {
    id: 'mon-incident-latency-spike-p99',
    subject: 'monitoring', subtopic: 'incident', level: 'senior', type: 'mcq',
    title: 'P99 latency spiked 45ms → 380ms, P50 unchanged',
    context: [
      'Alert: P99 inference latency spiked from 45ms to 380ms. P50 unchanged.',
      'Severity: high',
      'Hypothesis: P50 unchanged but P99 spiked = tail-latency issue, not a global slowdown (co-located batch job, GC pause, or a slow feature-query path).',
    ],
    question: 'What is your immediate action?',
    options: [
      'Add more replicas without diagnosing the cause.',
      'Check for resource contention — likely a noisy neighbor or co-located batch job.',
      'Roll back the model.',
      'Retrain on fresh data.',
    ],
    answer: 1,
    diagnosis: 'P50 unchanged but P99 spiked = a tail-latency issue, not a global slowdown. Likely a co-located batch job, GC pause, or a specific feature query hitting a slow path.',
    explanation: 'Triage: (1) Check CPU/memory utilization on serving nodes — is a batch job co-located? (2) Profile which requests are slow — do they share a feature, user segment, or input size? (3) Check whether a feature-store query has a slow path (e.g. a cache miss falling back to DB). (4) Check whether model warmup is causing cold-start latency on recent pod restarts.',
    fix: "Anti-pattern to avoid: adding more replicas without diagnosing the cause. If it's a co-located batch job, scaling serving won't help.",
    source: 'Monitor · Incident Triage · latency_spike',
  },
  {
    id: 'mon-incident-label-delay-proxy',
    subject: 'monitoring', subtopic: 'incident', level: 'staff', type: 'mcq',
    title: 'Model accuracy great but conversion rate dropped 4%',
    context: [
      'Alert: Model accuracy looks great but the business metric (conversion rate) dropped 4%.',
      'Severity: medium',
      'Hypothesis: the model optimizes a proxy label that has decoupled from the true business metric (common after a product change).',
    ],
    question: 'What is your immediate action?',
    options: [
      'Ignore the business metric because model metrics are green.',
      'Investigate the label pipeline — a metric-model disconnect suggests label delay or proxy mismatch.',
      'Retrain immediately on recent data.',
      'Roll back the model.',
    ],
    answer: 1,
    diagnosis: 'The model is optimizing a proxy label that has decoupled from the true business metric. Common after a product change.',
    explanation: 'Triage: (1) Check the correlation between model score and the business metric over time — has it broken recently? (2) Check whether a product change altered user behavior in a way the proxy label does not capture. (3) Check label delay: if conversion labels take 7 days to arrive, your "accurate" model is trained on stale feedback. (4) Run a hold-out analysis comparing the business metric for high-score vs low-score predictions in the current period.',
    fix: 'Anti-pattern to avoid: ignoring the business metric because the model metric is green. Model metrics are proxies — they can all look fine while value collapses.',
    source: 'Monitor · Incident Triage · label_delay',
  },
  {
    id: 'mon-incident-volume-crash',
    subject: 'monitoring', subtopic: 'incident', level: 'senior', type: 'mcq',
    title: 'Prediction volume dropped 60% in 30 min, no errors',
    context: [
      'Alert: Prediction volume dropped 60% in 30 minutes. No errors in logs.',
      'Severity: critical',
      'Hypothesis: traffic is not reaching the model — not a model failure. Suspect load balancer, upstream service, feature flag, or A/B routing config.',
    ],
    question: 'What is your immediate action?',
    options: [
      'Blame the model and roll it back.',
      'Page on-call immediately — this is likely an upstream traffic or routing failure.',
      'Retrain the model.',
      'Lower the decision threshold.',
    ],
    answer: 1,
    diagnosis: 'Traffic is not reaching the model — not a model failure. Check the load balancer, upstream service, feature flag, and A/B routing config.',
    explanation: 'Triage: (1) Check whether the upstream caller is healthy — is the service that calls the model seeing errors? (2) Check A/B or feature-flag config — was the model\'s traffic allocation changed? (3) Check load balancer health checks — did a pod fail readiness and get removed from rotation? (4) Check whether the drop is in all regions or just one, which suggests a geographic routing issue.',
    fix: "Anti-pattern to avoid: blaming the model. A 60% volume drop with no errors is almost never a model issue — it's a routing/infra issue.",
    source: 'Monitor · Incident Triage · volume_crash',
  },
  {
    id: 'mon-incident-concept-drift-precision',
    subject: 'monitoring', subtopic: 'incident', level: 'staff', type: 'mcq',
    title: 'PSI stable, features normal, precision dropped 12pp over 3 weeks',
    context: [
      'Alert: PSI stable, feature distributions normal, but precision dropped 12pp over 3 weeks.',
      'Severity: medium',
      'Hypothesis: the feature→label relationship has changed (concept drift), not the feature distribution (data drift). PSI won\'t catch this.',
    ],
    question: 'What is your immediate action?',
    options: [
      'Wait for PSI to trigger before acting.',
      'This is concept drift — the model needs retraining, not further input investigation. Plot precision/recall over time and retrain on recent data.',
      'Roll back the model.',
      'Add more serving replicas.',
    ],
    answer: 1,
    diagnosis: 'The relationship between features and labels has changed (concept drift), not the feature distribution (data drift). PSI will not catch this.',
    explanation: 'Triage: (1) Plot precision/recall over time — is it a gradual decline (drift) or a step change (incident)? (2) Check whether the label definition or collection process changed around when the decline started. (3) Run segment error analysis — which user cohort or feature slice degraded most? (4) Retrain on recent data; consider a time-weighted loss or short training window to prioritize recent patterns.',
    fix: 'Anti-pattern to avoid: waiting for PSI to trigger before acting. PSI measures input drift, not concept drift — you need outcome monitoring too.',
    source: 'Monitor · Incident Triage · concept_drift',
  },
  {
    id: 'mon-incident-schema-mismatch-fallback',
    subject: 'monitoring', subtopic: 'incident', level: 'senior', type: 'mcq',
    title: 'Silent errors for 8% of requests, returning fallback scores',
    context: [
      'Alert: Model serving throwing silent errors for 8% of requests, returning fallback scores.',
      'Severity: high',
      'Hypothesis: a feature pipeline or upstream service changed a field name, type, or encoding; serving catches the error silently and returns a default score.',
    ],
    question: 'What is your immediate action?',
    options: [
      'Accept 8% silent errors as normal operating noise.',
      'Find the schema mismatch — a feature has changed type or name in the serving payload.',
      'Retrain the model on fresh data.',
      'Roll back to the previous model.',
    ],
    answer: 1,
    diagnosis: 'A feature pipeline or upstream service changed a field name, type, or encoding. Serving code catches the error silently and returns a default score.',
    explanation: 'Triage: (1) Sample the 8% of requests getting fallback scores — what do their feature payloads look like? (2) Compare the feature schema at training time vs current serving schema — look for renamed columns, int→float coercions, or new categorical values. (3) Check whether a recent upstream deploy changed the event schema or feature computation logic. (4) Fix: add schema validation at the model entry point that raises loudly, not silently — a silent fallback hides the problem.',
    fix: 'Anti-pattern to avoid: accepting 8% silent errors as normal. Every silent fallback is a real user getting a degraded experience and a missed opportunity to catch the root cause.',
    source: 'Monitor · Incident Triage · schema_mismatch',
  },
]

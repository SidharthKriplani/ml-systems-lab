// timeSeries.js — normalized JUDGE drills for subject 'time_series'.
// Migrated from src/tabs/TimeSeriesTab.jsx (Forecast Failure Zoo,
// Stationarity & Transforms, Anomaly Detection Tiers, TS Model Selector,
// TS Feature Engineering). Raw <svg> markup in the source is not drill data
// and is excluded. Shape: see drillPool.js header.
//
// Conversion notes:
// - ForecastFailureZoo options were { id, label } with a `correct` id string;
//   converted to plain-string options with answer = index of the option whose
//   id matched `correct`. Source fields answer/fix/lesson mapped to
//   diagnosis/explanation/fix.
// - StationaritySelector scenarios were a diagnostic selector (no MCQ). Each is
//   reframed as an MCQ over candidate transforms (the correct transform vs
//   transforms of sibling scenarios). issue → diagnosis, desc+check →
//   explanation, transform+dont → fix.
// - AnomalyDetectionTiers options were { id, label } with a `correct` id
//   (rule|stat|ml|hybrid); answer = index where option id === correct. The
//   'label' (fraud) scenario used options_override for display, so that set is
//   used here.
// - TSModelSelector / TSFeatureEngineering already used plain-string options
//   with an integer `answer`, `diagnosis`, and `fix` — carried through as-is.

export const TIMESERIES_DRILLS = [
  // ─── Forecast Failure Zoo (subtopic: forecasting) ────────────────────────
  {
    id: 'ts-ffz-leakage',
    subject: 'time_series', subtopic: 'forecasting', level: 'senior', type: 'mcq',
    title: 'Revenue Forecast Collapses at Month Boundary',
    context: [
      'Your revenue forecast was 97% accurate on the test set. In production, it started over-predicting by 30–40% at every month-end. The model uses 30-day rolling features, lag-1 revenue, and day-of-week dummies.',
      'Clue: Test set was carved by random 80/20 split from a 2-year dataset.',
    ],
    question: 'What is the root cause?',
    options: [
      'Target leakage — future revenue leaked into rolling features during training',
      'Distribution shift — month-end revenue patterns changed after training',
      'Wrong train/test split — random split allowed future data into training features',
      'Overfitting — model memorized monthly patterns in training data',
    ],
    answer: 2,
    diagnosis: 'Wrong train/test split — a random split leaks future data into training features',
    explanation: 'The random 80/20 split is the root cause. With time-series data, a random split allows future observations into the training set. Your 30-day rolling feature at time T was computed using data that included future time points T+1, T+2, etc. — which were in the training set despite being temporally after the test point. In production those future points don\'t exist, so the features look completely different. Random splits are catastrophic for time-series: the 97% test accuracy was meaningless because it measured performance on future-contaminated features.',
    fix: 'Always use a time-ordered split for time-series: train on the first 80% of the timeline, test on the last 20%. Never random-shuffle a time series before splitting.',
    source: 'TimeSeries · Forecast Failure Zoo',
  },
  {
    id: 'ts-ffz-seasonality',
    subject: 'time_series', subtopic: 'forecasting', level: 'senior', type: 'mcq',
    title: 'Holiday Spike Completely Missed',
    context: [
      'Your demand forecast missed Black Friday by 4×. The model was trained on 18 months of data and uses ARIMA with weekly seasonality. It performs well 48 weeks of the year.',
      'Clue: Black Friday and Cyber Monday are in the 4 weeks it misses. Training data had only one prior Black Friday (last year).',
    ],
    question: 'What is the root cause?',
    options: [
      'ARIMA cannot model multiplicative seasonality — use SARIMA instead',
      'One training example of Black Friday is not enough to learn the pattern',
      'The model\'s seasonal lag window only covers 7 days, missing the 52-week annual cycle',
      'ARIMA forecasts degrade beyond 4 weeks ahead — the holiday was too far in the future to predict',
    ],
    answer: 1,
    diagnosis: 'One training example of a rare high-magnitude event is statistically insufficient',
    explanation: 'One training example of Black Friday is statistically insufficient — the model has no reliable estimate of how extreme the holiday effect is. ARIMA\'s seasonal component learned from a single Black Friday is highly unstable. Option A is a real ARIMA limitation but not the primary cause — the model performs fine 48/52 weeks. Option C is worth fixing (a 52-week lag should be included) but even with it, one data point for the holiday effect is still unreliable. Rare high-magnitude events (Black Friday, Super Bowl, launches) are underrepresented in any training set and must be modeled explicitly with calendar features, not from seasonal patterns.',
    fix: 'Add a binary holiday indicator as an external regressor. Use Prophet or a SARIMAX model with explicit holiday effects. Or use a hybrid: statistical model + holiday multipliers from business knowledge.',
    source: 'TimeSeries · Forecast Failure Zoo',
  },
  {
    id: 'ts-ffz-nonstationarity',
    subject: 'time_series', subtopic: 'forecasting', level: 'senior', type: 'mcq',
    title: 'Sales Forecast Drifts Upward Over 3 Months',
    context: [
      'Your sales forecast was accurate in January. By March, it was systematically underpredicting by 25%. The model was trained on 2 years of historical data. No model changes were made.',
      'Clue: The company launched an aggressive marketing campaign in February. User acquisition rate tripled.',
    ],
    question: 'What is the root cause?',
    options: [
      'Concept drift — the data-generating process changed (new marketing campaign)',
      'Model decay — the model parameters need re-estimation',
      'Missing feature — marketing spend is not in the model',
      'All of the above — they describe the same root cause at different levels',
    ],
    answer: 3,
    diagnosis: 'A regime change from the marketing campaign — concept drift, model decay, and a missing feature are one root cause',
    explanation: 'All three describe the same root cause: a regime change caused by the marketing campaign. This is concept drift (the relationship between features and target shifted), model decay (parameters trained on pre-campaign data are now biased), and a missing feature (marketing spend is a causal driver not in the model). The distinction matters for the fix: feature addition solves the problem permanently; retraining solves it temporarily until the next regime change. Forecast drift after a business change is almost always concept drift — monitoring residuals with a control chart catches it 2–4 weeks earlier than waiting for stakeholder complaints.',
    fix: 'Add marketing spend as a feature. Retrain on recent data with the campaign period included. Implement drift monitoring (PSI on predictions vs actuals) to detect future regime changes earlier.',
    source: 'TimeSeries · Forecast Failure Zoo',
  },
  {
    id: 'ts-ffz-granularity',
    subject: 'time_series', subtopic: 'forecasting', level: 'staff', type: 'mcq',
    title: 'Aggregate Forecast Accurate, SKU Forecast Useless',
    context: [
      'Your demand forecast at the total category level (all products) has 5% MAPE. But when you break down by individual SKU (3,000 products), MAPE is 85%. The supply chain team needs SKU-level forecasts for inventory planning.',
      'Clue: You trained one global model on all SKUs combined with SKU ID as a feature.',
    ],
    question: 'What is the root cause?',
    options: [
      'Individual SKU time series are too noisy for any model to forecast accurately',
      'SKU-level data is too sparse — most SKUs have intermittent demand',
      'Hierarchical reconciliation is needed — aggregate forecasts don\'t decompose to SKU level correctly',
      'Missing SKU-level features — the model doesn\'t have enough information to differentiate SKUs',
    ],
    answer: 2,
    diagnosis: 'A hierarchical forecasting problem — aggregate and SKU forecasts are not coherent',
    explanation: 'This is a hierarchical forecasting problem. When you sum individual SKU forecasts they don\'t equal the (accurate) category total, and disaggregating the category total doesn\'t give accurate SKU forecasts. You need hierarchical reconciliation (bottom-up, top-down, or optimal reconciliation via MinT/WLS) to ensure forecasts are coherent across all levels. Aggregate accuracy masks SKU-level accuracy: good category forecasts are often achieved by cancellation of errors that individually matter at the SKU level.',
    fix: 'Use hierarchical forecasting frameworks (statsforecast HierarchicalForecast, Prophet with hierarchies). Apply optimal reconciliation (MinT) to make SKU forecasts sum to category forecasts while minimizing error at each level.',
    source: 'TimeSeries · Forecast Failure Zoo',
  },
  {
    id: 'ts-ffz-autocorrelation',
    subject: 'time_series', subtopic: 'forecasting', level: 'senior', type: 'mcq',
    title: 'Confidence Intervals Too Narrow — Actuals Constantly Outside',
    context: [
      'Your forecast model produces 95% prediction intervals, but actual values fall outside those intervals 40% of the time (should be ~5%). The point forecast is reasonably accurate but the uncertainty estimates are badly underconfident.',
      'Clue: You computed prediction intervals assuming IID residuals: interval = forecast ± 1.96 * σ.',
    ],
    question: 'What is the root cause?',
    options: [
      'Residuals are autocorrelated — naive σ underestimates true uncertainty',
      'Residuals have heavy tails — normal distribution assumption wrong',
      'The model is underfitting — residuals contain predictable signal',
      'The σ estimate is too small — should use a larger multiplier than 1.96',
    ],
    answer: 0,
    diagnosis: 'Autocorrelated residuals — the IID assumption underestimates uncertainty',
    explanation: 'Autocorrelated residuals are the root cause. IID residuals are an assumption for the ±1.96σ interval formula. When residuals are correlated across time — almost always true in time-series forecasts — the effective sample size is much smaller than the nominal size, and uncertainty is systematically underestimated. Naive interval formulas that assume independence always produce overconfident (too narrow) intervals; correct uncertainty estimation requires modeling or testing for residual autocorrelation.',
    fix: 'Check autocorrelation with ACF/PACF of residuals. Use conformal prediction for valid intervals without distributional assumptions. Alternatively use block bootstrap (preserves temporal structure) or a model that captures residual autocorrelation (ARIMA on residuals, GARCH for volatility).',
    source: 'TimeSeries · Forecast Failure Zoo',
  },
  {
    id: 'ts-ffz-trend-break',
    subject: 'time_series', subtopic: 'forecasting', level: 'staff', type: 'mcq',
    title: 'Forecast Was Great for 18 Months, Then Broke Instantly',
    context: [
      'A product usage forecast that had been running reliably for 18 months suddenly became useless. Actual values dropped 60% below forecast in a single week and never recovered. No retraining, no model changes.',
      'Clue: The company discontinued a major product bundle that had been driving 60% of usage.',
    ],
    question: 'What is the root cause?',
    options: [
      'Structural break — the data-generating process changed abruptly',
      'Temporary anomaly — should be treated as an outlier and excluded',
      'Model needs retraining on more recent data',
      'Need to add product bundle as a feature',
    ],
    answer: 0,
    diagnosis: 'A structural break — a permanent, irreversible change in the data-generating process',
    explanation: 'This is a structural break — a permanent, irreversible change in the data-generating process. The old time series ended; a new one began. Retraining on pre-break data will not help; the model must be rebuilt on post-break data. Structural breaks cannot be handled by standard forecasting models — they require intervention modeling, Bayesian change-point detection, or manual regime detection. They are not concept drift: retraining on old data is useless or harmful, and you need a process to detect and respond to breaks in near-real-time.',
    fix: 'Detect the break point (CUSUM test, Bayesian change-point detection). Discard pre-break training data entirely or use it only as a prior. Rebuild the model on post-break regime data. Add operational event features (product launches/discontinuations) as external regressors.',
    source: 'TimeSeries · Forecast Failure Zoo',
  },
  {
    id: 'ts-ffz-intermittent',
    subject: 'time_series', subtopic: 'forecasting', level: 'senior', type: 'mcq',
    title: 'Forecast Predicts 0 for High-Value Product Spike',
    context: [
      'A medical device has sales of 0 for 47 out of 52 weeks, then occasionally spikes to 200–500 units. Your model consistently predicts near-zero (correctly, most weeks) but completely misses the spikes that matter most to the business.',
      'Clue: Model is a standard ARIMA trained on weekly totals.',
    ],
    question: 'What is the root cause / right approach?',
    options: [
      'Intermittent demand requires specialized methods (Croston\'s method, IMAPA)',
      'This should be a two-stage model: classify spike vs no-spike, then predict magnitude',
      'Need leading indicators (orders in pipeline, sales calls) as features',
      'Intermittent demand is genuinely hard — some combination of all approaches is needed',
    ],
    answer: 3,
    diagnosis: 'Intermittent demand is genuinely hard — no single clean solution',
    explanation: 'Intermittent demand is a genuinely hard problem with no single clean solution. Croston\'s method and its variants (SBA, TSB, IMAPA) are designed for this and outperform ARIMA. A two-stage classifier (will we sell anything this week?) + regressor (how much?) is often more useful for business decisions. Leading indicators (pipeline data, customer orders) are the most valuable signal if available. In practice the best approach combines all three. Standard methods assume reasonably continuous demand; the business rarely needs an accurate point forecast — it needs to know how much safety stock to hold.',
    fix: 'Try Croston\'s method or IMAPA as a baseline. If you have pipeline data (purchase orders, quotes), use those as leading features. Frame it as: probability of a spike this week, and if so, expected magnitude. Service-level optimization (stock to meet 95% demand) is often more useful than point forecasting for intermittent series.',
    source: 'TimeSeries · Forecast Failure Zoo',
  },
  {
    id: 'ts-ffz-multivariate-lag',
    subject: 'time_series', subtopic: 'forecasting', level: 'senior', type: 'mcq',
    title: 'Adding More Features Made the Forecast Worse',
    context: [
      'You added 15 macroeconomic indicators as features to your sales forecast model, expecting improvement. RMSE on the test set increased by 20%. The model was a gradient boosted tree.',
      'Clue: The macroeconomic indicators are released monthly, but your forecast is weekly. You used the most recent monthly value for all 4 weeks of each month.',
    ],
    question: 'What is the root cause?',
    options: [
      'Look-ahead bias — some macro features may not be released before the forecast date',
      'Feature timestamp mismatch — using a monthly feature as if it\'s contemporaneous introduces noise',
      'Curse of dimensionality — too many features relative to training samples',
      'The macro features are not actually predictive of this specific product\'s sales',
    ],
    answer: 1,
    diagnosis: 'Feature timestamp mismatch — monthly indicators used as if contemporaneous',
    explanation: 'The fundamental problem is feature timestamp mismatch. Monthly macro indicators are typically released with a 4–6 week lag. If your model uses the "current month\'s" GDP growth but that figure won\'t be published until 6 weeks later, you have look-ahead bias. Even if the release timing is correct, treating a monthly value as weekly ignores within-month variation and creates artificial precision. External features can introduce look-ahead bias if their availability isn\'t audited — the question isn\'t "what does this feature measure?" but "when is this feature actually available in production?"',
    fix: 'For all external features, check when the data is actually available at prediction time; use publication date, not reference date. Align macro features with their real release schedule. If weekly macro data is unavailable, use daily/weekly proxies (stock indices, search trends) instead of lagged monthly series.',
    source: 'TimeSeries · Forecast Failure Zoo',
  },

  // ─── Stationarity & Transforms (subtopic: stationarity) ──────────────────
  {
    id: 'ts-stat-trend',
    subject: 'time_series', subtopic: 'stationarity', level: 'junior', type: 'mcq',
    title: 'Upward trend, no seasonality',
    context: [
      'Time series steadily increases over time. Mean is not constant. Variance appears stable.',
      'Diagnosis: Non-stationary (mean non-constant).',
    ],
    question: 'What transform makes this series stationary?',
    options: [
      'First differencing: yt − yt-1, removing the trend',
      'Log transform alone to stabilize variance',
      'Seasonal differencing: yt − yt-s',
      'No transformation needed — fit ARMA directly',
    ],
    answer: 0,
    diagnosis: 'Non-stationary (mean non-constant)',
    explanation: 'The mean rises over time while variance is stable, so this is trend non-stationarity. First differencing (yt − yt-1) removes a linear trend and makes the series stationary. A log transform alone won\'t remove a linear trend — it only stabilizes variance. Confirm with an ADF (Augmented Dickey-Fuller) test: p < 0.05 → stationary after differencing.',
    fix: 'Apply first differencing, then confirm with ADF (p < 0.05). Don\'t use a log transform alone — it won\'t remove a linear trend, only stabilize variance.',
    source: 'TimeSeries · Stationarity & Transforms',
  },
  {
    id: 'ts-stat-seasonal',
    subject: 'time_series', subtopic: 'stationarity', level: 'mid', type: 'mcq',
    title: 'Clear weekly/yearly seasonality',
    context: [
      'Same pattern repeats every week or year. Mean is relatively constant, but seasonal variation is large.',
      'Diagnosis: Seasonally non-stationary.',
    ],
    question: 'What is the correct transform?',
    options: [
      'First differencing: yt − yt-1',
      'Log transform to stabilize variance',
      'Seasonal differencing: yt − yt-s (s = seasonal period), or STL decomposition + model the residuals',
      'No transformation needed — fit ARMA directly',
    ],
    answer: 2,
    diagnosis: 'Seasonally non-stationary',
    explanation: 'The pattern repeats on a fixed period, so the non-stationarity is seasonal. Seasonal differencing (yt − yt-s, with s = 7 for weekly or 52 for yearly) removes the repeating structure; alternatively STL decomposition isolates the seasonal component so you can model the residuals. Confirm with a KPSS test for seasonal stationarity and inspect the ACF — spikes at lag s confirm seasonality. Don\'t model the raw series and ignore seasonality; the model will be confused by repeating patterns.',
    fix: 'Seasonal differencing (yt − yt-s) or STL decomposition + model the residuals. Check with KPSS and the ACF (spikes at lag s confirm seasonality).',
    source: 'TimeSeries · Stationarity & Transforms',
  },
  {
    id: 'ts-stat-hetero',
    subject: 'time_series', subtopic: 'stationarity', level: 'mid', type: 'mcq',
    title: 'Growing variance with level',
    context: [
      'As the series grows, the amplitude of fluctuations also grows. Variance is proportional to the mean.',
      'Diagnosis: Heteroscedastic (non-constant variance).',
    ],
    question: 'What is the correct transform?',
    options: [
      'First differencing alone: yt − yt-1',
      'Log transform: log(yt), or a Box-Cox transformation',
      'Seasonal differencing: yt − yt-s',
      'No transformation needed — fit ARMA directly',
    ],
    answer: 1,
    diagnosis: 'Heteroscedastic (non-constant variance)',
    explanation: 'Variance grows in proportion to the level — multiplicative variance. A log transform stabilizes variance when it grows proportionally with the level; Box-Cox generalizes this. First differencing alone won\'t fix multiplicative variance growth — log first, then difference if a trend remains. Diagnose by plotting the series and its absolute residuals over time: increasing spread signals heteroscedasticity.',
    fix: 'Log transform (or Box-Cox) to stabilize variance; then difference if a trend remains. Don\'t use first differencing alone — it won\'t fix multiplicative variance growth.',
    source: 'TimeSeries · Stationarity & Transforms',
  },
  {
    id: 'ts-stat-stationary',
    subject: 'time_series', subtopic: 'stationarity', level: 'junior', type: 'mcq',
    title: 'Stable mean and variance, no trend',
    context: [
      'Series fluctuates around a constant mean. No trend, no seasonality. Variance appears constant.',
      'Diagnosis: Already stationary.',
    ],
    question: 'What is the correct next step?',
    options: [
      'First difference the series before modeling',
      'Apply a log transform to stabilize variance',
      'No transformation needed — fit ARMA/ARIMA(p,0,q) directly',
      'Seasonal differencing to remove hidden seasonality',
    ],
    answer: 2,
    diagnosis: 'Already stationary',
    explanation: 'Constant mean, constant variance, no trend or seasonality — the series is already stationary. No transformation is needed: fit ARMA/ARIMA(p,0,q) directly and identify p and q from the ACF/PACF. Confirm with ADF (p < 0.05) and KPSS (p > 0.05), which together indicate stationarity. Don\'t over-transform: differencing a stationary series introduces unnecessary autocorrelation and makes modeling harder.',
    fix: 'Fit ARMA/ARIMA(p,0,q) directly; identify p and q from ACF/PACF. Confirm with ADF (p < 0.05) and KPSS (p > 0.05). Don\'t over-transform a stationary series.',
    source: 'TimeSeries · Stationarity & Transforms',
  },
  {
    id: 'ts-stat-structural',
    subject: 'time_series', subtopic: 'stationarity', level: 'senior', type: 'mcq',
    title: 'Sudden mean shift (structural break)',
    context: [
      'Series is stable, then abruptly jumps to a new level and stays there. Two distinct regimes.',
      'Diagnosis: Structural break — not classical non-stationarity.',
    ],
    question: 'What is the correct approach?',
    options: [
      'First difference the series to remove the shift',
      'Detect the break point (CUSUM / Bayesian change-point) and fit separate models per regime, or add a level-shift indicator at the break',
      'Log transform to stabilize the two levels',
      'Seasonal differencing at the shift point',
    ],
    answer: 1,
    diagnosis: 'Structural break — not classical non-stationarity',
    explanation: 'An abrupt, persistent jump to a new level is a structural break, not classical non-stationarity. Detect the break point (CUSUM test, Bayesian change-point detection) and either fit separate models per regime or add a level-shift indicator variable at the break. Don\'t just difference or detrend — differencing a level-shift series creates a spike at the break point and is wrong, and standard stationarity tests may incorrectly reject stationarity. Use a Chow test for a break at a known date, CUSUM for unknown break dates.',
    fix: 'Detect the break (CUSUM, Bayesian change-point, Chow test at a known date); fit separate models per regime or add a level-shift indicator. Don\'t just difference — that creates a spike at the break.',
    source: 'TimeSeries · Stationarity & Transforms',
  },
  {
    id: 'ts-stat-unit-root',
    subject: 'time_series', subtopic: 'stationarity', level: 'senior', type: 'mcq',
    title: 'Random walk (unit root)',
    context: [
      'Series wanders with no clear trend but never mean-reverts. Each step is independent. Could go anywhere.',
      'Diagnosis: Unit root — first-order integrated I(1).',
    ],
    question: 'What is the correct transform?',
    options: [
      'Log transform to stabilize variance',
      'First differencing removes the unit root; then fit ARMA to the differenced series (the "I" in ARIMA(p,d,q), d=1)',
      'Seasonal differencing: yt − yt-s',
      'No transformation needed — fit ARMA directly',
    ],
    answer: 1,
    diagnosis: 'Unit root — first-order integrated I(1)',
    explanation: 'A series that wanders without mean-reverting has a unit root and is integrated of order one, I(1). First differencing removes the unit root; then fit ARMA to the differenced series — this is the "I" in ARIMA(p,d,q), where d=1 means one round of differencing. Diagnose with ADF (p > 0.05 → fail to reject a unit root) together with KPSS (p < 0.05 → reject stationarity); both tests together give stronger evidence. Don\'t forecast a random walk far into the future — forecast uncertainty grows linearly with horizon, so wide intervals are correct, not a model failure.',
    fix: 'First difference to remove the unit root, then fit ARMA to the differenced series (ARIMA with d=1). Confirm with ADF (p > 0.05) and KPSS (p < 0.05). Expect wide intervals far out — that is correct, not a failure.',
    source: 'TimeSeries · Stationarity & Transforms',
  },

  // ─── Anomaly Detection Tiers (subtopic: anomaly) ─────────────────────────
  {
    id: 'ts-anom-threshold',
    subject: 'time_series', subtopic: 'anomaly', level: 'junior', type: 'mcq',
    title: 'CPU usage on 200 production servers',
    context: [
      'Each server\'s CPU should stay below 80%. Any server above this threshold for >5 minutes needs to be paged.',
    ],
    question: 'Which anomaly-detection tier is correct?',
    options: [
      'Rule-based threshold (CPU > 80% for 5 min)',
      'Statistical z-score over rolling window',
      'Isolation Forest on multivariate server metrics',
    ],
    answer: 0,
    diagnosis: 'Use rule-based detection',
    explanation: 'Rule-based is correct here. The threshold is known, fixed, and operationally meaningful. A statistical or ML approach would add complexity with no benefit — and might miss the specific threshold the SLA is defined against. Use the right tool for the right job.',
    fix: 'Upgrade to statistical if the threshold varies by server type, time of day, or load profile. Upgrade to ML if correlations between metrics (CPU + memory + network) define anomalies better than any single metric.',
    source: 'TimeSeries · Anomaly Detection Tiers',
  },
  {
    id: 'ts-anom-stat',
    subject: 'time_series', subtopic: 'anomaly', level: 'mid', type: 'mcq',
    title: 'Revenue per hour across a marketplace',
    context: [
      'Revenue varies significantly by hour of day, day of week, and season. You want to detect anomalies (sudden drops or spikes) that aren\'t explained by normal patterns.',
    ],
    question: 'Which anomaly-detection tier is correct?',
    options: [
      'Rule-based threshold (revenue < $X/hour)',
      'ARIMA/STL residual-based anomaly detection',
      'Deep autoencoder on hourly feature vectors',
    ],
    answer: 1,
    diagnosis: 'Use statistical (residual-based) detection',
    explanation: 'Statistical residual-based is the right tier. Fit an ARIMA or STL model to capture seasonal/trend patterns, then flag residuals beyond ±3σ as anomalies. This accounts for the known seasonality without requiring ML complexity. A fixed threshold ignores normal hourly variation and fires constantly during off-peak hours. An autoencoder is overkill for a univariate series with known seasonality.',
    fix: 'Upgrade to ML when you have multivariate correlated metrics where no single threshold or simple decomposition captures the anomaly (e.g., simultaneously low revenue + low order count + high cart abandonment).',
    source: 'TimeSeries · Anomaly Detection Tiers',
  },
  {
    id: 'ts-anom-ml',
    subject: 'time_series', subtopic: 'anomaly', level: 'senior', type: 'mcq',
    title: 'Manufacturing sensor array (50 sensors per machine)',
    context: [
      '50 sensors per machine capturing temperature, pressure, vibration, flow rate. Normal operating range varies by machine type and production mode. Failures are characterized by subtle correlations breaking down across sensors.',
    ],
    question: 'Which anomaly-detection tier is correct?',
    options: [
      'Rule-based threshold per sensor',
      'Univariate z-score per sensor independently',
      'Multivariate anomaly detection (Isolation Forest, Autoencoder)',
    ],
    answer: 2,
    diagnosis: 'Use ML-based multivariate detection',
    explanation: 'ML-based multivariate anomaly detection is appropriate here. Failures in manufacturing equipment manifest as correlated deviations across multiple sensors — no single sensor exceeds its individual threshold, but the combination is anomalous. Isolation Forest or an LSTM autoencoder trained on normal operating data can detect these inter-sensor correlation breaks. Rule-based thresholds per sensor would miss the subtle cross-sensor patterns entirely.',
    fix: 'Upgrade to an LSTM autoencoder if the anomaly patterns are temporal (sequences of correlated sensor values over time), not just instantaneous cross-sensor correlations.',
    source: 'TimeSeries · Anomaly Detection Tiers',
  },
  {
    id: 'ts-anom-label',
    subject: 'time_series', subtopic: 'anomaly', level: 'senior', type: 'mcq',
    title: 'Fraud transactions in payment processing',
    context: [
      'You have labeled historical fraud data. Fraudulent transactions represent 0.2% of traffic. You want to detect new fraud patterns as they emerge.',
    ],
    question: 'Which anomaly-detection approach is correct?',
    options: [
      'Rules only',
      'Unsupervised anomaly detection (Isolation Forest)',
      'Supervised model trained on labeled fraud + rule-based pre-filters',
    ],
    answer: 2,
    diagnosis: 'Use a hybrid: supervised model + rule-based pre-filters',
    explanation: 'Hybrid is correct. Fraud detection with labeled data is a supervised learning problem at its core — use those labels. But rule-based pre-filters from domain experts are fast, interpretable, and catch known fraud patterns without burning model capacity on obvious cases. The supervised model handles the subtle cases that rules miss. Pure unsupervised anomaly detection ignores your labeled data, which is almost always the wrong call when labels exist.',
    fix: 'Add unsupervised components specifically for detecting novel fraud patterns that differ from historical fraud — your supervised model won\'t generalize to genuinely new attack vectors.',
    source: 'TimeSeries · Anomaly Detection Tiers',
  },
  {
    id: 'ts-anom-sparse',
    subject: 'time_series', subtopic: 'anomaly', level: 'mid', type: 'mcq',
    title: 'API latency spikes for a low-traffic endpoint',
    context: [
      'An internal API endpoint gets ~50 requests per day. You want to detect latency anomalies without having 100,000s of normal samples to calibrate a statistical model.',
    ],
    question: 'Which anomaly-detection tier is correct?',
    options: [
      'Rule-based: alert if p99 > 2× 30-day p99 baseline',
      'Statistical: rolling mean ± 3σ on daily aggregates',
      'ML-based: train on historical latency distribution',
    ],
    answer: 0,
    diagnosis: 'Use rule-based detection for sparse data',
    explanation: 'Rule-based is correct for sparse data. With only 50 requests/day, there\'s insufficient data to reliably fit a statistical model or train an ML model. A simple relative threshold (2× baseline) is robust, interpretable, and doesn\'t require enough data to estimate a distribution. Statistical methods need at minimum hundreds of observations per window to produce reliable σ estimates.',
    fix: 'Aggregate with similar endpoints and train a shared model once you have sufficient data volume. Or use Bayesian methods with strong priors for small-sample settings.',
    source: 'TimeSeries · Anomaly Detection Tiers',
  },
  {
    id: 'ts-anom-concept-drift',
    subject: 'time_series', subtopic: 'anomaly', level: 'senior', type: 'mcq',
    title: 'Detecting model prediction drift over time',
    context: [
      'You want to detect when your ML model\'s prediction distribution has shifted significantly from what it was at deployment — without waiting for labels.',
    ],
    question: 'Which anomaly-detection approach is correct?',
    options: [
      'Rule: alert if mean prediction changes by >5% week-over-week',
      'Statistical: PSI / KS test on prediction score distribution',
      'Train a drift detection model on historical prediction distributions',
    ],
    answer: 1,
    diagnosis: 'Use a statistical distribution test (PSI / KS)',
    explanation: 'PSI/KS statistical test is the correct approach. Prediction drift is fundamentally a distribution comparison problem — you\'re asking whether the distribution of scores today matches the training distribution. PSI (Population Stability Index) and KS test are purpose-built for this. A fixed threshold on mean prediction is too crude (distribution can shift in shape without changing mean). Training a separate drift detection model is circular and fragile.',
    fix: 'Use multivariate drift detection (MMD, Maximum Mean Discrepancy) when you want to detect drift across the joint feature distribution, not just the marginals or predictions.',
    source: 'TimeSeries · Anomaly Detection Tiers',
  },

  // ─── TS Model Selector (subtopic: model-selection) ───────────────────────
  {
    id: 'ts-model-arima-stationary',
    subject: 'time_series', subtopic: 'model-selection', level: 'mid', type: 'mcq',
    title: 'Short stationary series, no seasonality',
    context: [
      'You have 3 years of weekly sales data for a single product. The series is stationary (ADF p < 0.05). No obvious seasonal pattern. You need next-12-week point forecasts.',
    ],
    question: 'Which model family is the best starting point?',
    options: [
      'Prophet — it handles stationarity and autocorrelation automatically without manual parameter selection.',
      'ARIMA — stationary data with no seasonality is a natural ARIMA use case.',
      'LSTM — deep learning will capture non-linear autocorrelation structures better at this horizon.',
      'Exponential smoothing (ETS) — it models autocorrelated stationary series with fewer parameters than ARIMA.',
    ],
    answer: 1,
    diagnosis: 'ARIMA is purpose-built for stationary univariate series. Box-Jenkins ACF/PACF analysis gives you the p,d,q parameters directly. Prophet adds trend and seasonality components you don\'t need here — it will overfit to noise. LSTMs need far more data (thousands of timesteps) to outperform ARIMA. ETS (option D) is a legitimate alternative: simple ETS (SES) handles stationary series and works well, but ARIMA is preferred because ACF/PACF analysis directly diagnoses the autocorrelation structure rather than treating it as a hyperparameter.',
    fix: 'Fit ARIMA(p,0,q): d=0 because series is already stationary. Use auto_arima (pmdarima) or plot ACF/PACF to select p and q. Validate with expanding-window cross-validation, not random split. Baseline: ARIMA(1,0,1) as starting point.',
    source: 'TimeSeries · TS Model Selector',
  },
  {
    id: 'ts-model-prophet-seasonality',
    subject: 'time_series', subtopic: 'model-selection', level: 'mid', type: 'mcq',
    title: 'Strong weekly + yearly seasonality',
    context: [
      'E-commerce site visits: clear weekly cycle (weekend spikes) and yearly holiday seasonality. You have 2 years of daily data (730 points). The series is non-stationary (upward trend).',
    ],
    question: 'Which model handles multi-period seasonality most naturally?',
    options: [
      'SARIMA with seasonal differencing.',
      'Prophet — built for multiple seasonality components + trend with additive/multiplicative modes.',
      'Exponential smoothing (Holt-Winters) — designed for trend + seasonality.',
      'ARIMA with Fourier terms as external regressors.',
    ],
    answer: 1,
    diagnosis: 'Prophet\'s core strength is decomposable multi-period seasonality + trend changes. It handles weekly + yearly seasonality automatically with Fourier series. SARIMA can handle one seasonal period cleanly but two seasonalities require hacks. Holt-Winters supports one seasonal period. 730 points is enough for Prophet\'s Bayesian inference.',
    fix: 'from prophet import Prophet; m = Prophet(seasonality_mode="multiplicative"); m.fit(df). Add holiday effects with m.add_country_holidays(). Use multiplicative mode if the amplitude of seasonal swings grows with trend level. Evaluate with cross_validation() from prophet.diagnostics.',
    source: 'TimeSeries · TS Model Selector',
  },
  {
    id: 'ts-model-global-lightgbm',
    subject: 'time_series', subtopic: 'model-selection', level: 'senior', type: 'mcq',
    title: 'Many short related series',
    context: [
      'You need to forecast demand for 10,000 SKUs across 50 stores. Each SKU-store combination has 18 months of weekly data (78 points). You have shared product features (category, price, weight) and store features (region, size).',
    ],
    question: 'What is the most scalable forecasting approach?',
    options: [
      'Fit 500,000 individual ARIMA models — one per SKU-store combination.',
      'Fit a global LightGBM model on all series simultaneously, using lag features + cross-series features.',
      'Fit a Prophet model per SKU (ignore store dimension to reduce models).',
      'Use a single LSTM trained on all series with one sequence per SKU-store.',
    ],
    answer: 1,
    diagnosis: '500k individual ARIMA models are computationally infeasible and statistically poor on 78 points each. A global ML model (LightGBM, XGBoost) trained across all series learns cross-series patterns and leverages product/store features. 78-point series → strong reliance on tabular lag features. Tree models consistently outperform individual deep learning models at this scale with this data volume.',
    fix: 'Create a flat feature table: one row per (sku, store, week) with lag_1, lag_2, lag_4, lag_52 (if available), rolling_mean_4w, rolling_std_4w, plus static product/store features and calendar features. Train LightGBM with time-based train/val split. Tune with Optuna. This approach generalises better than fitting 500k separate models and scales trivially.',
    source: 'TimeSeries · TS Model Selector',
  },
  {
    id: 'ts-model-neural-conditions',
    subject: 'time_series', subtopic: 'model-selection', level: 'staff', type: 'mcq',
    title: 'Long sequence, non-linear patterns',
    context: [
      'You are forecasting energy consumption at 5-minute intervals over 3 years (315,000 timesteps). The series has complex non-linear patterns, multiple driver variables (temperature, humidity, day_of_week), and regime changes (COVID lockdowns).',
    ],
    question: 'When does a neural time series model beat classical methods here?',
    options: [
      'When the series is long enough that gradient descent converges reliably — 315k points is more than sufficient.',
      'When the sample size is large (100k+), multivariate inputs are available, and non-linear interactions between drivers are expected.',
      'When the series has regime changes — neural models handle changepoints better than ARIMA because they learn from context.',
      'Neural models are not appropriate here because the series is non-stationary and needs differencing first.',
    ],
    answer: 1,
    diagnosis: 'The three conditions for neural TS models to win: (1) large training data (100k+ points), (2) multivariate inputs with non-linear interactions, (3) complex patterns that parametric models cannot represent. All three hold here. 315k timesteps is sufficient for TFT or N-BEATS training. Option A confuses sample size with the correct justification — data volume is necessary but not sufficient; the advantage comes from multi-variate non-linear modeling. Option C is a common practitioner belief but is wrong: regime changes are hard for all model families, and neural models don\'t handle them better without explicit indicators.',
    fix: 'Temporal Fusion Transformer (TFT) or N-BEATS for this use case. TFT natively handles multi-horizon forecasting with multi-variate inputs and produces interpretable attention weights. Add lockdown as a binary covariate. Use PyTorch Forecasting library. Validate on the last 6 months only — not a random split.',
    source: 'TimeSeries · TS Model Selector',
  },
  {
    id: 'ts-model-croston-intermittent',
    subject: 'time_series', subtopic: 'model-selection', level: 'senior', type: 'mcq',
    title: 'Intermittent demand',
    context: [
      'A spare parts retailer forecasts demand for 5,000 low-velocity SKUs. Many SKUs sell 0 units in most weeks — demand is sporadic and lumpy. Traditional MAPE blows up on zero-demand weeks.',
    ],
    question: 'Which approach is designed for intermittent/lumpy demand?',
    options: [
      'Prophet with logistic growth and floor = 0 to prevent negative forecasts.',
      'Croston\'s method — separates non-zero demand size from inter-demand interval.',
      'LightGBM with Tweedie loss objective — handles zero-inflated count-like distributions natively.',
      'SARIMA with seasonal differencing to remove the periodic zero-demand pattern.',
    ],
    answer: 1,
    diagnosis: 'Intermittent demand violates ARIMA and Prophet assumptions (both assume relatively continuous observations). Croston\'s method models the non-zero demand level and the average inter-demand interval separately — purpose-built for this distribution. Option C (LightGBM + Tweedie) is a legitimate production approach and can outperform Croston at scale — it earns credit as a strong alternative. Option A (Prophet with floor) is a common team decision but Prophet\'s Fourier-based seasonality is poorly suited to series that are zero 90% of the time.',
    fix: 'Use Croston\'s method or its improved variant ADIDA (Aggregate-Disaggregate Intermittent Demand Approach). In Python: statsforecast library has CrostonClassic and CrostonOptimized. Evaluate with Mean Absolute Scaled Error (MASE) which handles zeros, not MAPE. For the ML approach: Tweedie regression (LightGBM with tweedie objective) handles zero-inflated continuous approximations of count data.',
    source: 'TimeSeries · TS Model Selector',
  },
  {
    id: 'ts-model-coldstart-global',
    subject: 'time_series', subtopic: 'model-selection', level: 'senior', type: 'mcq',
    title: 'Very short series cold start',
    context: [
      'A new product launched 6 weeks ago. You need a week-8 forecast. You have 6 data points and no historical precedent for this exact product. You do have 3 years of data from 200 similar existing products.',
    ],
    question: 'How do you forecast for a new series with minimal history?',
    options: [
      'ARIMA requires at least 2× the seasonal period — 6 points is too short. Refuse to forecast.',
      'Use cross-series learning: train a global model on the 200 similar products and apply it to the new product at inference time.',
      'Exponential smoothing with a very low smoothing parameter to avoid overfitting to 6 points.',
      'Naive seasonal: copy the pattern from the most similar existing product.',
    ],
    answer: 1,
    diagnosis: 'With 6 data points, any univariate model overfits trivially. The correct approach exploits the cross-series signal from 200 similar products. A global model trained on all historical data applies immediately to new products — it has learned the typical demand trajectory for this product category even if this specific product is new.',
    fix: 'Train a global LightGBM or N-BEATS model on the 200 existing products. At inference, pass the 6 available lags as features. The model generalises from similar product patterns. Add product attributes (category, price tier, launch campaign size) as static covariates — these drive early-lifecycle behavior. Monitor: compare week 7–10 actuals vs forecast to detect if this product is an outlier from the cluster.',
    source: 'TimeSeries · TS Model Selector',
  },

  // ─── TS Feature Engineering (subtopic: features) ─────────────────────────
  {
    id: 'ts-feat-lag-selection',
    subject: 'time_series', subtopic: 'features', level: 'mid', type: 'mcq',
    title: 'Lag feature selection',
    context: [
      'You are building a tabular ML forecasting model for weekly retail sales. You have 3 years of data. The series has a strong 52-week annual cycle.',
    ],
    question: 'Which lag features are most important to include?',
    options: [
      'lag_1, lag_2, lag_3 only — recent history is always the best predictor.',
      'lag_1, lag_2, lag_4, lag_52 — recent lags plus the seasonal lag to capture annual patterns.',
      'All lags from 1 to 52 — let the model select which matter.',
      'lag_52 only — the same week last year is the best predictor for seasonal data.',
    ],
    answer: 1,
    diagnosis: 'Using only lag_1/2/3 misses the seasonal signal entirely. Including all 52 lags causes the curse of dimensionality, multicollinearity, and training instability. lag_52 alone ignores recent short-term dynamics. The right set: lag_1 (momentum), lag_2 (2-week autocorrelation), lag_4 (monthly cycle), lag_52 (same-week-last-year seasonal anchor).',
    fix: 'Start with lag_1, lag_2, lag_4, lag_13 (quarterly), lag_52. Add rolling statistics: rolling_mean_4w, rolling_std_4w, rolling_mean_52w. Use feature importance from your model to prune. For SHAP: the relative importance of lag_52 vs lag_1 tells you whether the series is more seasonally driven or momentum-driven.',
    source: 'TimeSeries · TS Feature Engineering',
  },
  {
    id: 'ts-feat-cyclical-encoding',
    subject: 'time_series', subtopic: 'features', level: 'mid', type: 'mcq',
    title: 'Cyclical calendar encoding',
    context: [
      'You are adding "day of week" (0–6) as a feature to a daily demand model. You encode it as a raw integer (0 = Monday, 6 = Sunday). Your LightGBM model performs poorly on weekends.',
    ],
    question: 'What is wrong with raw integer encoding for cyclical features?',
    options: [
      'LightGBM cannot use integer features — use one-hot encoding.',
      'Raw integer encoding implies Sunday (6) is far from Monday (0) — but they are adjacent in the weekly cycle. Use sin/cos encoding.',
      'The feature needs to be standardised to [0, 1] before use.',
      'Day of week is not useful — remove it and use a weekend binary flag instead.',
    ],
    answer: 1,
    diagnosis: 'Raw integer encoding imposes a false linear order: day 6 (Sunday) is numerically far from day 0 (Monday) even though they are 1 day apart in the weekly cycle. Tree models handle this via splits, but linear components cannot represent the cyclic proximity. Sin/cos encoding maps the cycle onto a unit circle — preserving the true distance between days.',
    fix: 'day_sin = sin(2π × day_of_week / 7); day_cos = cos(2π × day_of_week / 7). Same for month_of_year (/12), hour_of_day (/24). These two features together encode any cyclic position correctly. For tree models, one-hot encoding of day_of_week is also fine (7 binary features); for neural/linear models, sin/cos is strongly preferred.',
    source: 'TimeSeries · TS Feature Engineering',
  },
  {
    id: 'ts-feat-target-leakage',
    subject: 'time_series', subtopic: 'features', level: 'mid', type: 'mcq',
    title: 'Target leakage via future information',
    context: [
      'A data scientist adds "sales for the same store, next week" as a feature to predict demand for the current week. The model achieves 99% accuracy in offline eval. In production, accuracy drops to 55%.',
    ],
    question: 'What caused the offline-production gap?',
    options: [
      'The model overfit — add dropout or L2 regularisation.',
      '"Next week\'s sales" is a future value — it is not available at prediction time. This is feature leakage.',
      'Production data has a different distribution than the training data.',
      'The 99% accuracy was computed on the test set, which the model also trained on.',
    ],
    answer: 1,
    diagnosis: 'This is a textbook example of temporal leakage: a feature value from the future (next week) was used to predict the present week. During offline eval, the future value exists in the dataset so the model uses it directly — trivially accurate. At serving time, next week has not happened yet, so the feature is absent or zero, and the model fails.',
    fix: 'Enforce strict point-in-time joins: all features used to predict week T must be computed from data available at T-1 or earlier. Audit every feature with the question: "Could I have computed this value at prediction time?" Use the lag feature pattern: if you want "same store performance", use lag_1 (last week\'s sales), not next week\'s. In your data pipeline, use as-of joins.',
    source: 'TimeSeries · TS Feature Engineering',
  },
  {
    id: 'ts-feat-rolling-window',
    subject: 'time_series', subtopic: 'features', level: 'senior', type: 'mcq',
    title: 'Rolling statistics window choice',
    context: [
      'You add rolling_mean_1w, rolling_mean_4w, rolling_mean_13w, rolling_mean_52w as features. Feature importance shows rolling_mean_1w has 80% of the importance weight. Your model is overfit and generalises poorly.',
    ],
    question: 'What is likely happening and how do you fix it?',
    options: [
      'rolling_mean_1w has high importance because it is the best feature — keep it and remove the others.',
      'rolling_mean_1w is highly correlated with the target (it is last week\'s average) — high importance signals that the model is overfitting to recent noise rather than learning the seasonal pattern.',
      'The rolling windows need to be standardised before use.',
      'Use an exponentially weighted mean instead of a rolling mean.',
    ],
    answer: 1,
    diagnosis: 'When rolling_mean_1w dominates importance, the model is essentially predicting "next week ≈ this week" — which is often true in the short run but fails to capture seasonal and structural patterns. The model appears accurate in walk-forward validation but degrades for multi-step horizons. High importance on very short windows is a sign of horizon mismatch.',
    fix: 'For horizon h (forecasting h steps ahead), the shortest lag you can use without leakage is lag_h. If forecasting 4 weeks out, rolling_mean_1w uses information from 1 week ago — valid, but ensure the window doesn\'t overlap with the forecast horizon. Balance importance: if rolling_mean_1w dominates, consider dropping it and forcing the model to rely on seasonal lags. Or use a minimum horizon-aware lookback: rolling_mean_4w as the shortest window.',
    source: 'TimeSeries · TS Feature Engineering',
  },
  {
    id: 'ts-feat-known-future-regressor',
    subject: 'time_series', subtopic: 'features', level: 'senior', type: 'mcq',
    title: 'External regressor for known future events',
    context: [
      'You are forecasting hourly electricity demand. You know the weather forecast for the next 48 hours (temperature, humidity). You want to include this as a feature in your model.',
    ],
    question: 'How do you correctly include known-future external regressors in a forecasting model?',
    options: [
      'Do not include weather — it creates a dependency on forecast accuracy that degrades your model.',
      'Include future weather values directly as features at forecast time — these are known-future regressors, not leakage.',
      'Include only historical weather (lagged by 24h) to avoid any future information.',
      'Create a separate model that predicts weather, then chain the two models together.',
    ],
    answer: 1,
    diagnosis: 'Known-future regressors are a different category from leakage: the weather forecast IS available at prediction time (you are using the forecast, not the actual future). This is the correct pattern for exogenous variables with external forecasts — the information is genuinely available when you need to make your prediction.',
    fix: 'In ARIMAX, Prophet, and TFT, add weather as a "future covariate" or exogenous variable. Key: document explicitly that your model depends on weather forecast quality. Degrade analysis: what happens if the weather forecast is off by 5°C? Run sensitivity analysis. For neural models (TFT): weather is a "known future input" — the architecture explicitly separates these from historical-only features.',
    source: 'TimeSeries · TS Feature Engineering',
  },
  {
    id: 'ts-feat-cross-series-embedding',
    subject: 'time_series', subtopic: 'features', level: 'senior', type: 'mcq',
    title: 'Cross-series features for global model',
    context: [
      'You are training a global model across 5,000 SKUs. Each SKU is a different time series. You want to add features that encode each SKU\'s identity so the model can learn SKU-specific patterns.',
    ],
    question: 'What is the best way to encode SKU identity in a global ML forecasting model?',
    options: [
      'Use the SKU string as a label-encoded integer — the model will learn the mapping.',
      'Learn SKU embeddings: either pre-trained from product metadata, or learned end-to-end as a trainable embedding layer alongside the forecasting objective.',
      'One-hot encode all 5,000 SKUs — this gives the model full expressivity per SKU.',
      'Add SKU category as a single categorical feature and ignore SKU-level identity.',
    ],
    answer: 1,
    diagnosis: 'Label encoding (integer ID) forces the model to treat SKU identity as a linear feature — SKU 1000 is implied to be halfway between SKU 500 and 1500. One-hot encoding of 5,000 SKUs creates 5,000 sparse features — the model sees very few training examples per SKU feature. Embedding representations learn a dense, low-dimensional representation of each SKU\'s demand pattern.',
    fix: 'Option 1: pre-train SKU embeddings on product metadata (category, price tier, weight) using word2vec or a small neural network, then use as static features. Option 2: learn embeddings end-to-end in a neural forecasting model (TFT or N-BEATS with static covariate input). Option 3: for tree models, use target encoding of SKU ID computed with out-of-fold means to avoid leakage — this is a lightweight approximation of embeddings.',
    source: 'TimeSeries · TS Feature Engineering',
  },
]

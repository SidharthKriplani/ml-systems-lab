// gapfill2.js — hand-authored judgment drills filling thin subject×level cells.
// Targets: classical_ml junior, system_design junior + mid, monitoring junior + mid.
// Interview-grade fundamentals (junior) and one-step-deeper tradeoffs (mid).
// Shape: see drillPool.js header. Mostly mcq, with a few code drills where a
// bug is the natural framing.

export const GAPFILL2_DRILLS = [
  // ══════════════════════════════════════════════════════════════
  // classical_ml · junior (9)
  // ══════════════════════════════════════════════════════════════
  {
    id: 'gf2-cml-baseline-first',
    subject: 'classical_ml', subtopic: 'workflow', level: 'junior', type: 'mcq',
    title: 'Reaching for XGBoost before a baseline exists',
    context: [
      'New churn-prediction project, 40k labelled rows, 30 features.',
      'A junior opens with a tuned XGBoost and reports 0.86 AUC as the headline result.',
      'Nobody knows what a trivial model scores on this data.',
    ],
    question: 'What is the problem with leading with the tuned XGBoost?',
    options: [
      'XGBoost is the wrong algorithm for churn — logistic regression is always better',
      'Without a baseline (majority-class / simple logistic regression) you cannot tell whether 0.86 is good or whether a one-line model already gets 0.85; the baseline is what makes the number interpretable',
      '0.86 AUC is too low to report at all',
      'You must always tune hyperparameters before measuring AUC',
    ],
    answer: 1,
    diagnosis: 'A metric without a baseline is uninterpretable',
    explanation: 'AUC of 0.86 in isolation says nothing. If a plain logistic regression on the same features already scores 0.85, the 20 hours of XGBoost tuning bought 0.01 — likely noise across CV folds. A baseline (majority-class predictor for a floor, a simple linear/logistic model for a reasonable reference) frames every later number: it tells you how much headroom exists and whether complexity is paying its way.',
    fix: 'Always ship a baseline first: majority-class for the absolute floor, then a plain logistic regression. Report the complex model as a delta over the baseline, not as a standalone number.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-logreg-linear-in-what',
    subject: 'classical_ml', subtopic: 'logistic-regression', level: 'junior', type: 'mcq',
    title: 'What logistic regression is actually linear in',
    context: [
      'A junior says "logistic regression can only fit linear relationships, so it is useless when probability rises non-linearly with a feature".',
      'The target is fraud; the feature is transaction amount, and fraud rate rises sharply only above $5,000.',
    ],
    question: 'What is the correct understanding?',
    options: [
      'The junior is right — logistic regression cannot model a threshold effect at all',
      'Logistic regression is linear in the log-odds, not in probability, and only linear in the features you give it — a bucketed or non-linear amount feature (e.g. an amount>$5k indicator) lets it capture the threshold cleanly',
      'You must switch to a neural network for any non-linear feature effect',
      'The sigmoid already makes the model non-linear in the raw features, so no feature engineering is needed',
    ],
    answer: 1,
    diagnosis: 'Linearity is in log-odds and in the supplied features',
    explanation: 'log(p/(1-p)) = w·x + b is linear in x, and the sigmoid maps that to a probability — so the model is already non-linear in probability space (an S-curve). What it cannot do is bend with respect to a raw feature it was never given the right form of. A threshold at $5k is captured by adding an amount>$5000 indicator or amount buckets; the weight on that feature then encodes the jump. The limitation is the feature representation, not the model class.',
    fix: 'Engineer the feature: add threshold indicators, buckets, or a log-transform of amount. Logistic regression then models the effect. Reach for a non-linear model only when interactions between many features dominate.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-accuracy-imbalance',
    subject: 'classical_ml', subtopic: 'class-imbalance', level: 'junior', type: 'mcq',
    title: '99.2% accuracy on a 0.8%-positive fraud set',
    context: [
      'Fraud model: 0.8% of transactions are fraud.',
      'A junior reports 99.2% accuracy and calls the model excellent.',
      'The confusion matrix shows the model predicts "not fraud" for essentially every transaction.',
    ],
    question: 'Why is the 99.2% accuracy misleading, and what should be reported instead?',
    options: [
      'Accuracy is fine — 99.2% is genuinely strong for fraud detection',
      'A model that always predicts the majority class already scores 99.2% here because positives are 0.8%; accuracy is dominated by the majority class, so report precision/recall (or PR-AUC) which measure performance on the rare positive class',
      'The model needs more training epochs to break past 99.2%',
      'Accuracy is low because fraud is hard — 99.2% means the model is barely working',
    ],
    answer: 1,
    diagnosis: 'Accuracy is a majority-class mirror under heavy imbalance',
    explanation: 'With 0.8% positives, "always predict negative" gets 99.2% accuracy while catching zero fraud. Accuracy simply reflects the base rate. What matters is performance on the rare class: recall (what fraction of fraud did we catch), precision (of flagged transactions, how many were fraud), and PR-AUC which summarises the tradeoff at the prevalence that actually exists. ROC-AUC can also look flattering under extreme imbalance, so PR-based metrics are the honest read.',
    fix: 'Drop accuracy for imbalanced problems. Report precision, recall, and PR-AUC; set the threshold from the precision/recall the business needs (e.g. recall floor for fraud caught).',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-leakage-target-encode',
    subject: 'classical_ml', subtopic: 'feature-leakage', level: 'junior', type: 'code',
    title: 'Target leakage from fitting the scaler on all the data',
    context: [
      'A junior standardises features and reports 0.94 CV AUC, but production AUC is 0.81.',
      'The preprocessing is fit once on the full dataset, then cross-validation runs on top.',
    ],
    question: 'What is leaking, and how do you fix it?',
    options: [
      'Nothing leaks — the CV/production gap is just normal distribution shift',
      'The scaler is fit on the entire dataset (including validation folds) before CV splits, so each fold\'s "held-out" data influenced the mean/std used to transform it; fit preprocessing inside the CV loop via a Pipeline so each fold only sees its own training statistics',
      'The learning rate is too high, inflating CV AUC',
      'AUC is the wrong metric — switch to accuracy',
    ],
    answer: 1,
    code: `# reported CV AUC 0.94, production 0.81
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)          # fit on ALL rows

scores = cross_val_score(
    LogisticRegression(), X_scaled, y, cv=5, scoring='roc_auc'
)
print(scores.mean())   # 0.94 — optimistic

# fix: keep preprocessing inside the fold
pipe = make_pipeline(StandardScaler(), LogisticRegression())
scores = cross_val_score(pipe, X, y, cv=5, scoring='roc_auc')  # honest`,
    diagnosis: 'Preprocessing fit before the split leaks validation stats into training',
    explanation: 'fit_transform on the whole X computes the mean and std using every row — including the rows each CV fold will later "hold out". The transformed held-out rows are therefore not truly unseen; the model gets a peek at their distribution. That inflates CV AUC (0.94) above what production (0.81) delivers, where you genuinely cannot see future data. Wrapping the scaler in a Pipeline makes cross_val_score refit the scaler on each fold\'s training rows only.',
    fix: 'Never fit any preprocessing (scaling, target encoding, imputation, feature selection) on the full dataset before CV. Put every fitted step in a Pipeline so it is refit per fold.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-tree-no-scaling',
    subject: 'classical_ml', subtopic: 'trees', level: 'junior', type: 'mcq',
    title: 'Do gradient-boosted trees need feature scaling?',
    context: [
      'A junior spends a day building a StandardScaler + XGBoost pipeline "because scaling always helps".',
      'The features range from ages (0-100) to income (0-500,000).',
    ],
    question: 'What is the correct view on scaling for tree-based models?',
    options: [
      'Trees always need scaling, exactly like logistic regression and SVMs',
      'Tree splits are based on thresholds on one feature at a time, so any monotonic rescaling leaves the split points and the tree identical — scaling is unnecessary for XGBoost/trees (it matters for distance- and gradient-based models like SVM, k-NN, logistic regression)',
      'Scaling makes trees train faster but changes nothing about accuracy',
      'You must scale so the trees do not overweight income',
    ],
    answer: 1,
    diagnosis: 'Trees are invariant to monotonic feature transforms',
    explanation: 'A decision tree asks "is feature f > threshold t?". If you rescale f monotonically (e.g. standardise it), the optimal threshold rescales too and the resulting partition of the data is identical — same splits, same predictions. So trees and tree ensembles (Random Forest, XGBoost, LightGBM) are invariant to feature scaling. Scaling matters where distances or gradient magnitudes depend on feature units: k-NN, SVM, and (for convergence speed) logistic regression / neural nets.',
    fix: 'Skip scaling for tree models. Spend the effort where it pays: handling missing values, encoding categoricals, and feature construction. Scale only for distance/gradient-based models.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-regularization-what',
    subject: 'classical_ml', subtopic: 'regularization', level: 'junior', type: 'mcq',
    title: 'What L2 regularization actually trades',
    context: [
      'Logistic regression: train accuracy 93%, validation accuracy 74%, on 1,500 rows with 200 features.',
      'A junior wants to add features to close the gap.',
    ],
    question: 'What is the right move and why?',
    options: [
      'Add more features — the model needs more signal to reach validation parity',
      'The 19-point train/val gap is overfitting; increase L2 regularization (lower C), which shrinks weights toward zero, adds bias but cuts variance, and typically raises validation accuracy on a 1.5k-row, 200-feature problem',
      'Switch to accuracy-weighted boosting',
      'Increase the number of training iterations',
    ],
    answer: 1,
    diagnosis: 'A large train/val gap is a variance problem — regularize, do not add features',
    explanation: 'With 200 features and only 1,500 rows, the model has enough capacity to memorise noise — hence train 93% vs val 74%. Adding features widens the gap. L2 regularization penalises the sum of squared weights, so the optimizer keeps weights small unless a feature earns its magnitude with real signal. This deliberately raises bias (predictions pulled toward the mean) to lower variance; on a small, wide dataset the variance reduction dominates and validation accuracy rises. In sklearn, lower C = stronger regularization.',
    fix: 'Lower C (e.g. 1.0 to 0.1 to 0.01) and watch validation accuracy. Consider L1 if you also want sparsity/feature selection. Add features only once the gap is controlled.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-calibration-why',
    subject: 'classical_ml', subtopic: 'calibration', level: 'junior', type: 'mcq',
    title: 'Ranking is good but the probabilities are wrong',
    context: [
      'A model powers an expected-value bid: bid = P(convert) × value.',
      'AUC is a healthy 0.88, but among items the model scores "0.30", the actual conversion rate is 0.12.',
    ],
    question: 'What is wrong, and what does the fix target?',
    options: [
      'AUC 0.88 guarantees the probabilities are correct — the discrepancy is noise',
      'The model ranks well but is miscalibrated: a predicted 0.30 should mean 30% convert, but only 12% do; calibrate (Platt scaling / isotonic regression) so predicted probabilities match observed frequencies, because the EV bid multiplies by the raw probability',
      'Raise the AUC — it must reach 0.95 for the probabilities to be usable',
      'Switch from probability to a fixed bid for every item',
    ],
    answer: 1,
    diagnosis: 'Good discrimination does not imply good calibration',
    explanation: 'AUC only measures ranking — whether positives get higher scores than negatives. It is unchanged by any monotonic squashing of the scores, so a model can rank perfectly yet output probabilities that are systematically too high. Here 0.30 predictions convert at 0.12, so the EV bid (P × value) overpays by ~2.5x on those items. Calibration fixes the mapping from score to probability: Platt scaling fits a logistic on the scores, isotonic regression fits a monotonic step function — both on a held-out set.',
    fix: 'Fit a calibrator (isotonic if you have enough held-out data, Platt for smaller sets) and validate with a reliability diagram / expected calibration error. Only calibrated probabilities are safe to multiply into a decision.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-onehot-highcard',
    subject: 'classical_ml', subtopic: 'feature-engineering', level: 'junior', type: 'mcq',
    title: 'One-hot encoding a 50,000-value ID column',
    context: [
      'A junior one-hot encodes a user_id column with 50,000 distinct values as a feature for a click model.',
      'The dataset has 200k rows; training slows to a crawl and validation AUC drops versus dropping the column.',
    ],
    question: 'What went wrong and what is the better encoding?',
    options: [
      'One-hot is always correct — the AUC drop means the model needs more depth',
      'One-hot on a 50k-cardinality ID explodes the feature space to 50k mostly-empty columns, and each ID appears in only a few rows, so the model memorises IDs (overfits) with no generalisation; use target/frequency encoding or a learned embedding, or drop the raw ID',
      'The problem is that user_id should be scaled, not one-hot encoded',
      'One-hot is fine but the learning rate is too high',
    ],
    answer: 1,
    diagnosis: 'One-hot on high-cardinality IDs = sparsity plus memorization',
    explanation: 'One-hot turns one column into 50,000, almost all zeros. With 200k rows, each user_id shows up on average 4 times — far too few to estimate a reliable per-ID effect, so the model latches onto individual IDs and overfits, and unseen IDs at serving time are all-zero (no signal). Target encoding replaces the ID with the (smoothed, out-of-fold) mean of the label for that ID; frequency encoding uses its count; a learned embedding maps IDs to a dense low-dimensional vector. All three keep dimensionality sane and generalise.',
    fix: 'For high-cardinality categoricals use target encoding (with smoothing + out-of-fold to avoid leakage), frequency encoding, or embeddings. Reserve one-hot for low-cardinality columns.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-cml-cv-single-split',
    subject: 'classical_ml', subtopic: 'evaluation', level: 'junior', type: 'mcq',
    title: 'Trusting a single train/test split on 3k rows',
    context: [
      'On 3,000 rows a junior does one 80/20 split, gets 0.79 AUC, and picks the winning model from a set of five.',
      'Re-running with a different random seed swings the AUC between 0.74 and 0.83.',
    ],
    question: 'What is the core issue and the fix?',
    options: [
      'The model is unstable — pick a different algorithm entirely',
      'A single split on a small dataset has high variance in the estimate (±0.05 here), so which model "wins" is partly luck; use k-fold cross-validation to average over folds and report a mean ± std, giving a stable, comparable estimate',
      'AUC is unreliable — switch to accuracy on the single split',
      '3,000 rows is too few to model at all',
    ],
    answer: 1,
    diagnosis: 'One split on small data has high estimation variance',
    explanation: 'With only 600 validation rows, the AUC estimate wobbles ±0.05 just from which rows landed in the test fold. Choosing the best of five models on one split rewards whichever model got the friendliest split, not the best model. k-fold CV (e.g. 5-fold, stratified for imbalance) evaluates each model on 5 different held-out folds and averages, shrinking the variance of the estimate and letting you compare mean±std across models. The std itself flags whether a difference between models is real.',
    fix: 'Use stratified k-fold CV; report mean and std of the metric. Prefer a model only when its mean beats another by more than the fold-to-fold std.',
    source: 'Authored · gapfill',
  },

  // ══════════════════════════════════════════════════════════════
  // system_design · junior (9)
  // ══════════════════════════════════════════════════════════════
  {
    id: 'gf2-sd-two-stage-latency',
    subject: 'system_design', subtopic: 'two-stage', level: 'junior', type: 'mcq',
    title: 'Why not run the ranker over the whole catalogue',
    context: [
      'Search-ranking system: 2M documents, p99 latency budget of 150ms per query.',
      'The cross-feature ranker costs ~2ms per document scored.',
      'A junior proposes scoring all 2M documents with the ranker on every query.',
    ],
    question: 'Why is a retrieve-then-rank split necessary here?',
    options: [
      'Two stages are a convention that makes the design doc look thorough',
      'Scoring 2M docs at 2ms each is ~4,000 seconds per query — impossible under a 150ms budget; a cheap retrieval stage (inverted index / ANN) narrows 2M to a few hundred, and the ranker scores only those',
      'The ranker cannot output more than a few hundred scores at once',
      'Retrieval and ranking are the same model, run twice for redundancy',
    ],
    answer: 1,
    diagnosis: 'Latency forces a cheap recall stage before the expensive precision stage',
    explanation: '2,000,000 docs × 2ms = 4,000,000ms ≈ 67 minutes per query — nowhere near 150ms. The heavy ranker can only afford to score a few hundred candidates (a few hundred × 2ms ≈ sub-second, and in practice much less with batching). Retrieval is the cheap, high-recall filter: an inverted index or ANN over embeddings returns the top few hundred in a few ms. The ranker then spends its budget only on survivors. Right-size each stage to its job.',
    fix: 'Stage 1: cheap high-recall retrieval over the full corpus (inverted index / ANN). Stage 2: expensive high-precision ranking over a few hundred candidates. Budget latency per stage.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-batch-vs-online',
    subject: 'system_design', subtopic: 'serving', level: 'junior', type: 'mcq',
    title: 'Batch precompute vs online scoring',
    context: [
      'A "you might also like" module shows recommendations on a product page.',
      'Catalogue: 500k items, 10M daily active users; recommendations change slowly (based on purchase history).',
      'A junior wants to score the model live on every page view.',
    ],
    question: 'What is the better serving pattern and why?',
    options: [
      'Score online on every page view — it is always freshest and simplest',
      'Because recommendations change slowly and reads vastly outnumber meaningful updates, precompute them in a nightly (or hourly) batch and serve from a key-value store; this cuts request-time latency to a lookup and removes per-request model cost',
      'Precompute is impossible at 500k items — only online scoring scales',
      'Batch and online produce different math, so batch is inherently less accurate',
    ],
    answer: 1,
    diagnosis: 'Slow-changing outputs + read-heavy traffic favour batch precompute',
    explanation: 'If a user\'s recommendations only shift when their purchase history changes (rarely within a day), recomputing them on every page view wastes model compute and adds tail latency. Batch: run the model per user overnight, write results to a KV store (Redis/DynamoDB); serving becomes a single-digit-ms lookup. Online scoring earns its cost only when inputs change per request (e.g. the query, the current session context) or freshness within seconds matters. The decision hinges on how fast the inputs and outputs actually change.',
    fix: 'Batch-precompute slow-changing, read-heavy outputs into a KV store. Reserve online scoring for request-dependent or freshness-critical features; a hybrid (batch base + light online re-rank) is common.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-embedding-precompute',
    subject: 'system_design', subtopic: 'embeddings', level: 'junior', type: 'mcq',
    title: 'Where the item embeddings should live at serving time',
    context: [
      'A two-tower retrieval model: a user tower and an item tower producing 128-d embeddings.',
      'Catalogue of 3M items. Retrieval must return top-200 in under 20ms.',
      'A junior plans to run the item tower on all 3M items at request time to get their embeddings.',
    ],
    question: 'What is the standard serving design?',
    options: [
      'Run the item tower live for all 3M items on each request — it guarantees fresh embeddings',
      'Precompute all 3M item embeddings offline and load them into an ANN index; at request time run only the user tower once and do a nearest-neighbour lookup, so serving cost is one user embedding + one ANN query, not 3M forward passes',
      'Store item embeddings in a SQL table and scan it per request',
      'Concatenate user and item features and run one model per item at request time',
    ],
    answer: 1,
    diagnosis: 'Item embeddings are query-independent — precompute and index them',
    explanation: 'The item tower does not depend on the current user, so its 3M outputs can be computed once (batch job), refreshed on a schedule, and stored in an ANN index (HNSW / IVF). At request time you run only the user tower (one forward pass) and query the index for the 200 nearest item vectors — a few ms. Running the item tower for 3M items per request would be millions of forward passes, blowing the 20ms budget by orders of magnitude. This is exactly why the two towers stay separate until a final dot product.',
    fix: 'Precompute item embeddings offline into an ANN index; refresh on a cadence. Serve: user tower once, then ANN nearest-neighbour lookup. Monitor index staleness so new items get indexed.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-feature-store-why',
    subject: 'system_design', subtopic: 'feature-store', level: 'junior', type: 'mcq',
    title: 'Why a feature store instead of recomputing features twice',
    context: [
      'A team computes "7-day purchase count" in a training pipeline (SQL over a warehouse) and again, separately, in the serving service (a different code path over a live DB).',
      'The two implementations drift; offline AUC is 0.84 but online is 0.78.',
    ],
    question: 'What problem does a feature store solve here?',
    options: [
      'It makes training faster by caching gradients',
      'It provides a single definition of each feature shared by training and serving, eliminating training-serving skew from two divergent implementations of the same feature',
      'It replaces the model with a lookup table',
      'It is only useful for deep learning, not classical models',
    ],
    answer: 1,
    diagnosis: 'Two feature implementations = training-serving skew',
    explanation: 'When "7-day purchase count" is coded once for training and again for serving, subtle differences (timezone boundaries, inclusive vs exclusive windows, how refunds count) make the serving feature systematically differ from the trained-on feature. The model then sees inputs at serving time it never saw in training — offline 0.84 collapses to online 0.78. A feature store defines each feature once and materialises it to both an offline store (for training) and an online store (for low-latency serving), so both paths consume identical logic. It also handles point-in-time correctness to avoid leakage.',
    fix: 'Define features once in a feature store feeding both offline (training) and online (serving) stores. Add point-in-time joins to prevent leakage, and monitor for skew between the two.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-cache-recommendations',
    subject: 'system_design', subtopic: 'caching', level: 'junior', type: 'mcq',
    title: 'Caching model predictions safely',
    context: [
      'A pricing model is called for the same product+region many times per minute.',
      'A junior adds a cache keyed on product_id only, TTL of 24h, to cut model calls.',
      'Prices start looking stale and wrong for some regions.',
    ],
    question: 'What is the cache-design mistake?',
    options: [
      'Caching model output is never acceptable — always recompute',
      'The cache key omits inputs that change the prediction (region, and any time-sensitive features), so different-region requests collide on one entry; the key must include every input that affects the output, and the TTL must match how fast those inputs change',
      'The TTL is too short — make it 7 days',
      'Caching adds latency, so it should be removed',
    ],
    answer: 1,
    diagnosis: 'Cache key must cover all inputs that change the prediction',
    explanation: 'A cache is a function output store: the key must include every argument that changes the output. Keying on product_id alone means product X in region A and region B share one cached price — whichever request lands first poisons the other. And a 24h TTL is only safe if the inputs (demand, competitor prices) really are stable for 24h; if prices move hourly, the TTL must be an hour or less. Get the key wrong and you serve confidently wrong values; get the TTL wrong and you serve stale ones.',
    fix: 'Key the cache on the full input tuple (product_id + region + any bucketed time-sensitive features). Set TTL to how fast the underlying inputs change; consider event-based invalidation.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-timeout-fallback',
    subject: 'system_design', subtopic: 'reliability', level: 'junior', type: 'mcq',
    title: 'No fallback when the model service times out',
    context: [
      'A ranking service calls a model server synchronously with no timeout and no fallback.',
      'The model server has a GC pause; p99 latency spikes to 5s and the whole feed page hangs, then errors.',
    ],
    question: 'What is the missing design element?',
    options: [
      'Nothing — model calls should always block until they return',
      'A latency budget with a timeout plus a graceful fallback (e.g. serve a cached or popularity-based ranking) so a slow/failed model degrades the experience instead of taking the whole page down',
      'The model server just needs more CPU — no design change required',
      'Retry the model call indefinitely until it succeeds',
    ],
    answer: 1,
    diagnosis: 'Synchronous dependency with no timeout or fallback is a single point of failure',
    explanation: 'A downstream model call with no timeout inherits the model\'s worst-case latency: one GC pause and every request behind it stacks up, exhausting threads and taking the page from "unranked" to "down". Production ML serving sets a per-call timeout inside the latency budget and, on timeout/error, falls back to something cheap and always-available: a cached ranking, a popularity ordering, or the raw retrieval order. The user sees a slightly worse feed instead of an error page. Retrying indefinitely just amplifies load during an incident.',
    fix: 'Set a timeout within the latency budget; on timeout/error fall back to a cached or heuristic ranking. Add a circuit breaker so a struggling model server sheds load instead of collapsing.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-idempotent-scoring',
    subject: 'system_design', subtopic: 'pipelines', level: 'junior', type: 'code',
    title: 'A batch scoring job that double-counts on retry',
    context: [
      'A nightly batch job scores users and appends predictions to an output table.',
      'When the job is retried after a partial failure, some users appear twice with conflicting scores.',
    ],
    question: 'What is the design flaw and the fix?',
    options: [
      'The model is non-deterministic — set a random seed and the duplicates vanish',
      'Appending is not idempotent: a retry re-runs already-completed partitions and appends duplicate rows; write with an upsert/overwrite keyed on (user_id, run_date) so re-running a partition replaces rather than duplicates its rows',
      'Batch jobs cannot be retried — switch to online scoring',
      'The output table needs a bigger disk',
    ],
    answer: 1,
    code: `# nightly scoring — appends, so a retry duplicates rows
def score_partition(users, run_date):
    preds = model.predict(users)
    df = to_frame(users, preds, run_date)
    db.append("predictions", df)          # NOT idempotent

# fix: idempotent write keyed on (user_id, run_date)
def score_partition(users, run_date):
    preds = model.predict(users)
    df = to_frame(users, preds, run_date)
    db.upsert("predictions", df,
              keys=["user_id", "run_date"])   # re-run replaces, no dup`,
    diagnosis: 'Append-based writes make retries non-idempotent',
    explanation: 'Batch jobs fail partway and get retried — that is normal. If the write is a plain append, the retry re-processes partitions that already succeeded and appends their rows a second time, so a user ends up with two conflicting scores and any downstream join fans out. The fix is to make the write idempotent: upsert (or overwrite the partition) keyed on the logical identity of a row — here (user_id, run_date). Re-running the same partition then replaces its rows instead of stacking new ones, so the output is the same whether the job ran once or five times.',
    fix: 'Make writes idempotent: upsert/merge on a natural key, or overwrite per-partition. Then retries and backfills are safe. Track per-partition completion so you can resume rather than restart.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-capacity-qps',
    subject: 'system_design', subtopic: 'capacity', level: 'junior', type: 'mcq',
    title: 'Sizing replicas from QPS and latency',
    context: [
      'A model server handles one request in 20ms on a single worker (one in-flight at a time).',
      'Peak traffic is 2,000 requests per second.',
      'A junior provisions 2 replicas "because that feels like enough".',
    ],
    question: 'Roughly how many workers are needed, and how do you reason about it?',
    options: [
      '2 replicas — QPS does not matter, only that the service is redundant',
      'One worker at 20ms/request serves ~50 req/s (1000ms / 20ms); 2,000 req/s ÷ 50 ≈ 40 workers, then add headroom for p99 and failover — capacity comes from throughput per worker, not a guess',
      '2,000 workers — one per request per second',
      '20 workers — one per millisecond of latency',
    ],
    answer: 1,
    diagnosis: 'Capacity = QPS ÷ per-worker throughput, with headroom',
    explanation: 'A single-threaded worker that takes 20ms to handle one request can complete at most 1000/20 = 50 requests per second. To serve 2,000 req/s you need at least 2,000/50 = 40 such workers just to break even at average latency. You then add headroom: p99 latency is higher than the mean, traffic is bursty, and you need spare capacity to survive a replica failure — so you provision meaningfully above 40 (and use batching / concurrency per worker to raise the 50 req/s figure). Two replicas would be overwhelmed at ~100 req/s, 20x under peak.',
    fix: 'Compute per-worker throughput (1000ms ÷ latency, times concurrency), divide peak QPS by it, then add headroom for p99, bursts, and failover. Load-test to validate, and autoscale on utilisation.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-log-features-served',
    subject: 'system_design', subtopic: 'training-data', level: 'junior', type: 'mcq',
    title: 'Reconstructing training features from raw logs after the fact',
    context: [
      'A team logs the raw request and the model\'s prediction, but not the exact feature vector that was scored.',
      'To retrain, they rebuild features from the warehouse "as they are now".',
      'The retrained model behaves worse online than offline.',
    ],
    question: 'What is the systemic mistake?',
    options: [
      'Logging predictions is unnecessary — only labels matter',
      'Rebuilding features "as they are now" reconstructs values that differ from what the model actually saw at request time (feature values change, definitions evolve), causing training-serving skew; log the exact feature vector used at serving time and train on those logged features',
      'The model needs a larger learning rate on retrain',
      'Warehouses cannot store features, so this is unavoidable',
    ],
    answer: 1,
    diagnosis: 'Reconstructed features ≠ served features (point-in-time skew)',
    explanation: 'Features are time-sensitive: "7-day purchase count" or "current inventory" for a user at 2pm last Tuesday is not what the warehouse returns today. Rebuilding features from current tables gives the model training inputs that never matched the inputs it will see live, so offline metrics look fine but online performance sags. Logging the actual served feature vector (or doing rigorous point-in-time joins) guarantees the training distribution equals the serving distribution. This is the same skew a feature store\'s point-in-time correctness is designed to prevent.',
    fix: 'Log the exact feature vector scored at serving time (feature logging), and train on it. If reconstructing from a warehouse, use strict point-in-time joins so each row reflects state as of the request timestamp.',
    source: 'Authored · gapfill',
  },

  // ══════════════════════════════════════════════════════════════
  // system_design · mid (6)
  // ══════════════════════════════════════════════════════════════
  {
    id: 'gf2-sd-ann-recall-latency',
    subject: 'system_design', subtopic: 'ann', level: 'mid', type: 'mcq',
    title: 'Tuning HNSW when recall and latency fight',
    context: [
      'HNSW index over 20M embeddings. At the current efSearch, recall@100 vs a brute-force baseline is 0.90 and p99 is 8ms.',
      'Product wants recall@100 ≥ 0.97; raising efSearch to hit it pushes p99 to 22ms, over the 15ms budget.',
    ],
    question: 'What is the correct way to reason about this tradeoff?',
    options: [
      'HNSW is broken — switch to exact brute-force nearest neighbour',
      'efSearch trades recall for latency along one curve; to get more recall at the same latency you must move the whole curve — rebuild with a higher efConstruction / M (denser graph), shard to parallelise search, or accept a lower recall target — you cannot get 0.97 at 8ms for free by only changing efSearch',
      'Recall and latency are independent — raise recall without touching latency',
      'Lower M to speed things up; recall is unaffected by graph degree',
    ],
    answer: 1,
    diagnosis: 'Recall and latency sit on one operating curve set by index build params',
    explanation: 'efSearch controls how many candidates HNSW explores at query time: higher efSearch = higher recall = more work = higher latency. It slides you along a fixed curve. To beat the curve — more recall at the same latency — you change how the graph was built: higher M (more edges per node) and efConstruction produce a better-connected graph that reaches high recall at lower efSearch, at the cost of more memory and slower build. Sharding parallelises the search across machines to cut wall-clock latency. Brute force gives recall 1.0 but is far too slow at 20M. The honest framing is: pick two of {recall, latency, cost}.',
    fix: 'Rebuild with higher M/efConstruction to shift the recall-latency curve, and/or shard to parallelise. Re-measure recall@k against a brute-force sample. Treat it as a three-way tradeoff with memory/cost.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-online-offline-consistency',
    subject: 'system_design', subtopic: 'feature-store', level: 'mid', type: 'mcq',
    title: 'When online and offline feature freshness legitimately differ',
    context: [
      'A feature "sessions in last 1 hour" is served online from a streaming aggregate updated every few seconds.',
      'For training, the same feature is computed from batch logs bucketed hourly.',
      'The model underperforms specifically on users whose behaviour changed within the hour.',
    ],
    question: 'What is the subtle skew, and the fix?',
    options: [
      'There is no skew — hourly and streaming aggregates are equivalent',
      'The online value reflects sub-minute freshness while the training value is coarsely bucketed to the hour, so the model was trained on a staler, lumpier version of the feature than it sees live; recompute the training feature from the same streaming logic (or log the served value) so both paths share freshness semantics',
      'The streaming pipeline is simply wrong — delete it and serve hourly batch online too',
      'Add more layers to the model to absorb the difference',
    ],
    answer: 1,
    diagnosis: 'Same feature name, different freshness = training-serving skew',
    explanation: 'Two pipelines can compute a feature with the same name but different temporal granularity. Online, "sessions in last 1 hour" updates continuously; in training it was snapped to hourly buckets. For a user who spiked activity at minute 55, the online feature reads the spike while the hourly training feature averaged it away — so the model never learned to react to what it now sees. The fix is semantic consistency: either replay the streaming computation to generate point-in-time-correct training values, or log the exact served feature and train on those logs. A feature store with a single materialisation feeding both stores is the durable answer.',
    fix: 'Make training and serving share one feature definition and freshness. Prefer logging served feature values, or replay the streaming aggregation for training. Monitor online-vs-offline distributions per feature.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-embedding-versioning',
    subject: 'system_design', subtopic: 'embeddings', level: 'mid', type: 'mcq',
    title: 'Retraining the tower without re-indexing every item',
    context: [
      'The user tower is retrained weekly and redeployed.',
      'The item embeddings in the ANN index were produced by the previous item tower and are refreshed only monthly.',
      'Retrieval quality quietly degrades after each user-tower deploy.',
    ],
    question: 'What is the root cause and the correct discipline?',
    options: [
      'The ANN index is corrupt — rebuild it from scratch each request',
      'User and item embeddings must come from the same model version to share a vector space; deploying a new user tower against item vectors from an old item tower breaks the dot-product geometry, so version the towers together and re-index items whenever the tower changes',
      'Weekly retraining is too frequent — retrain the user tower yearly',
      'The item tower is irrelevant once the index exists',
    ],
    answer: 1,
    diagnosis: 'Mismatched embedding versions break the shared vector space',
    explanation: 'A two-tower model works because the user and item towers are trained jointly to place related users and items near each other in one vector space. The dot product is only meaningful when both vectors come from that same trained space. Ship a new user tower and its embeddings live in a slightly different geometry than the month-old item vectors in the index — the nearest neighbours it retrieves are no longer the intended ones, and quality drifts down after each deploy. The discipline is atomic versioning: a model version = (user tower + item tower); when it changes, re-embed and re-index the items, and cut over both together (often via a shadow index and an atomic swap).',
    fix: 'Version towers as a unit. On any tower change, re-embed all items into a new index and swap atomically. Tag embeddings with the model version and refuse cross-version dot products.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-write-read-skew-labels',
    subject: 'system_design', subtopic: 'training-data', level: 'mid', type: 'mcq',
    title: 'Label attribution window vs feature timestamp',
    context: [
      'A conversion model labels an impression positive if the user converts within 7 days.',
      'The team builds a training set by joining today\'s impressions to today\'s conversion table.',
      'Offline metrics are strong; the deployed model over-predicts conversion.',
    ],
    question: 'What point-in-time mistake is inflating the offline metrics?',
    options: [
      'Nothing — a 7-day window is standard and cannot cause leakage',
      'Recent impressions have not had the full 7 days to convert, so their labels are censored (many true positives still pending are recorded as negatives) — and joining to the current conversion table can also leak future conversions into features; exclude impressions younger than the attribution window and use point-in-time joins',
      'The conversion table is too small — collect more conversions',
      'The model needs calibration, not a data fix',
    ],
    answer: 1,
    diagnosis: 'Label censoring and future leakage from ignoring the attribution window',
    explanation: 'Two coupled time bugs. First, label maturity: an impression from yesterday cannot yet be labelled — its 7-day window is not closed, so users still on their way to converting are wrongly stamped negative (censored labels), biasing the positive rate. Second, if features are joined against the current state of tables, a feature like "user\'s lifetime conversions" can include conversions that happened after the impression — leaking the future. Offline the model looks great because it half-sees the answer; online it over-predicts. The fix is temporal hygiene: only train on impressions old enough for the window to close, and compute every feature as of the impression timestamp via point-in-time joins.',
    fix: 'Drop impressions younger than the attribution window so labels are mature. Compute all features with point-in-time joins as of the event timestamp. Re-measure — the offline number should drop to something honest.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-shadow-vs-canary',
    subject: 'system_design', subtopic: 'deployment', level: 'mid', type: 'mcq',
    title: 'Shadow deployment vs canary for a new model',
    context: [
      'A new ranking model is ready. The team wants to de-risk the launch.',
      'They must catch both (a) serving bugs / latency regressions and (b) whether the model actually improves the business metric.',
    ],
    question: 'How do shadow and canary each contribute, and why use both?',
    options: [
      'Shadow and canary are the same thing — pick either one',
      'Shadow runs the new model on live traffic without serving its output, catching latency/crash/skew issues at full volume with zero user risk but no business-metric read; canary serves the new model to a small % of real users to measure the business metric — use shadow to validate the plumbing, then canary to validate the value',
      'Canary first to check latency, then shadow to measure the metric',
      'Neither is needed if offline AUC improved',
    ],
    answer: 1,
    diagnosis: 'Shadow validates the system; canary validates the outcome',
    explanation: 'They answer different questions. Shadow (dark launch) mirrors real requests to the new model but discards its responses — you get production-scale signal on latency, errors, memory, and training-serving skew (compare shadow predictions to what you expected) with no user ever seeing the output, so zero business risk. But because its output is not served, shadow cannot tell you whether the model improves clicks/conversions. Canary serves the new model to, say, 1-5% of users and measures the actual business metric with a control group, catching harm early and quantifying lift before full rollout. The mature sequence is shadow to prove it runs correctly, then canary to prove it helps.',
    fix: 'Shadow-deploy first to validate latency, errors, and skew at scale with no user impact. Then canary to a small traffic slice with a control to measure business lift before ramping to 100%.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-sd-hot-key-feature',
    subject: 'system_design', subtopic: 'serving', level: 'mid', type: 'mcq',
    title: 'A hot key melts the online feature store',
    context: [
      'The online feature store is a sharded KV store keyed by entity id.',
      'One viral item receives 40% of all reads; the shard holding it saturates while others idle, and p99 spikes.',
    ],
    question: 'What is happening and how do you mitigate it?',
    options: [
      'The whole store is undersized — double every shard',
      'A hot key concentrates load on a single shard regardless of total capacity; mitigate by caching the hot entity\'s features in-process/at the edge, replicating hot keys across shards, or adding a small local cache with a short TTL — sharding alone cannot balance a single dominant key',
      'Switch the store from KV to SQL',
      'Hot keys are impossible in a sharded store',
    ],
    answer: 1,
    diagnosis: 'Skewed access creates a hot shard that more total capacity cannot fix',
    explanation: 'Sharding balances load only when access is spread across keys. When one key takes 40% of reads, its shard does 40% of the work while the rest sit idle — adding shards does not help because the hot key still lives on exactly one of them. The fixes attack concentration, not total size: an in-process or edge cache absorbs repeated reads of the hot entity (a few-second TTL is fine since features change slowly); replicating hot keys across multiple shards spreads their reads; request coalescing collapses duplicate concurrent lookups. Detecting hot keys (top-N read counters) lets you promote them to cache automatically.',
    fix: 'Cache hot entities near the app (short-TTL local/edge cache), replicate hot keys across shards, and coalesce duplicate reads. Add hot-key detection to promote keys to cache automatically.',
    source: 'Authored · gapfill',
  },

  // ══════════════════════════════════════════════════════════════
  // monitoring · junior (6)
  // ══════════════════════════════════════════════════════════════
  {
    id: 'gf2-mon-inputs-not-just-accuracy',
    subject: 'monitoring', subtopic: 'what-to-monitor', level: 'junior', type: 'mcq',
    title: 'Why monitoring accuracy alone is not enough',
    context: [
      'A fraud model is monitored only by weekly accuracy, computed once labels arrive.',
      'Fraud labels take 30 days to confirm (chargebacks).',
      'A junior asks why the team also monitors the input feature distributions daily.',
    ],
    question: 'Why monitor inputs and prediction distributions, not just accuracy?',
    options: [
      'Input monitoring is redundant — accuracy captures everything',
      'Labels are delayed 30 days, so accuracy tells you about a problem a month late; input drift and prediction-distribution shifts are observable immediately and act as an early warning that something changed before the labels confirm the damage',
      'Inputs never change once a model is deployed',
      'Prediction distributions are only useful during training',
    ],
    answer: 1,
    diagnosis: 'Delayed labels make accuracy a lagging indicator — inputs lead',
    explanation: 'If chargeback labels arrive 30 days late, an accuracy-only dashboard cannot flag a regression until a month after it starts — a month of bad fraud decisions. Inputs are available the instant a request arrives, so watching feature distributions (drift) and the prediction distribution gives an immediate, leading signal: if a key feature\'s distribution shifts or predicted-fraud rate suddenly halves, you investigate now rather than after 30 days of losses. Input/output monitoring does not replace accuracy — it front-runs it while you wait for labels.',
    fix: 'Monitor at three layers: input feature distributions (drift), prediction distribution, and delayed labels/accuracy. Alert on the leading (input/prediction) signals; confirm with accuracy when labels land.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-data-vs-concept-drift',
    subject: 'monitoring', subtopic: 'drift', level: 'junior', type: 'mcq',
    title: 'Data drift vs concept drift',
    context: [
      'Case A: after a marketing push, far more young users arrive; the relationship between features and churn is unchanged.',
      'Case B: a competitor launches, and users who used to be loyal (same features) now churn at a higher rate.',
    ],
    question: 'Which is data drift and which is concept drift?',
    options: [
      'Both are concept drift',
      'A is data drift (P(X) changed — the input distribution shifted, but P(y|X) is stable); B is concept drift (P(y|X) changed — the same inputs now map to a different outcome), which is the one that necessarily degrades the learned mapping',
      'A is concept drift, B is data drift',
      'Both are data drift',
    ],
    answer: 1,
    diagnosis: 'Data drift moves P(X); concept drift moves P(y|X)',
    explanation: 'Data drift (covariate shift) means the input distribution P(X) changes while the true input-to-label relationship P(y|X) stays fixed — Case A: more young users, but young users still churn according to the same rule the model learned. The model may be under-tested in the new region of input space, but its logic is still correct. Concept drift means P(y|X) itself changes — Case B: identical feature values now correspond to higher churn because the world changed (a competitor appeared). Concept drift invalidates the learned mapping and typically forces a retrain; pure data drift may only need coverage checks or reweighting.',
    fix: 'Distinguish them: track input distributions (PSI/KS) for data drift and, once labels arrive, track error/calibration by segment for concept drift. Concept drift usually needs a retrain; data drift may only need reweighting or expanded training coverage.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-psi-basics',
    subject: 'monitoring', subtopic: 'psi', level: 'junior', type: 'mcq',
    title: 'Reading a PSI value',
    context: [
      'A feature\'s Population Stability Index between the training distribution and this week\'s serving distribution is 0.28.',
      'A junior is unsure whether that is fine.',
    ],
    question: 'What does PSI measure and how is 0.28 typically read?',
    options: [
      'PSI measures model accuracy; 0.28 means 28% accuracy',
      'PSI measures how much a feature\'s distribution has shifted between two populations by comparing binned proportions; common rules of thumb: <0.1 stable, 0.1–0.25 moderate shift worth watching, >0.25 significant shift — so 0.28 flags meaningful drift to investigate',
      'PSI is the p-value of a t-test; 0.28 means not significant',
      'PSI only applies to the target variable, never to features',
    ],
    answer: 1,
    diagnosis: 'PSI quantifies distribution shift; >0.25 is a significant-drift flag',
    explanation: 'PSI bins a feature (e.g. deciles from the reference/training distribution) and sums (actual% − expected%) × ln(actual%/expected%) across bins — a symmetric measure of how far this week\'s proportions moved from the reference. The standard interpretation: below 0.1 the population is stable, 0.1 to 0.25 is a moderate shift worth monitoring, and above 0.25 signals a significant shift that warrants investigation. A PSI of 0.28 is over that threshold, so this feature has drifted meaningfully — check whether it is a data pipeline change, a real population change, or upstream breakage.',
    fix: 'Track PSI per important feature against the training distribution. Alert at ~0.25; investigate whether it is a pipeline bug, genuine population change, or upstream feature breakage before deciding to retrain.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-training-serving-skew-signal',
    subject: 'monitoring', subtopic: 'training-serving-skew', level: 'junior', type: 'mcq',
    title: 'Offline great, online bad, inputs "look normal"',
    context: [
      'A new model scores 0.87 AUC offline but underperforms online from day one (not a gradual decay).',
      'Feature drift monitors versus training data show nothing alarming.',
    ],
    question: 'What is the most likely cause and how do you catch it?',
    options: [
      'Concept drift — the world changed overnight',
      'Training-serving skew: the serving pipeline computes features differently from training, so the model gets inputs it never trained on; catch it by logging served feature vectors and comparing them, feature by feature, against the training-time values for the same entities',
      'The model simply needs more training epochs',
      'The online metric is miscomputed — trust the offline number',
    ],
    answer: 1,
    diagnosis: 'Immediate (not gradual) online gap points to training-serving skew',
    explanation: 'Drift degrades a model gradually as the world moves; a gap that is present from the very first day of serving — with training-vs-serving drift monitors quiet — is the signature of training-serving skew: the serving code computes a feature differently from the training code (different default for nulls, different window boundary, a units mismatch). Standard drift monitors compare serving to training aggregate distributions and can miss this if the marginal distribution looks similar while individual values are wrong. The direct test is to log the exact feature vector served for specific entities and diff it against the training-pipeline value for those same entities; mismatches localise the skew.',
    fix: 'Log served feature vectors and diff them, per feature and per entity, against training-pipeline values. Fix the divergent computation (ideally by sharing one feature definition via a feature store).',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-prediction-collapse',
    subject: 'monitoring', subtopic: 'prediction-distribution', level: 'junior', type: 'mcq',
    title: 'Predicted-positive rate drops to near zero overnight',
    context: [
      'A recommendation CTR model normally predicts a mean score around 0.08.',
      'After a deploy, the mean predicted score collapses to 0.002 and barely varies across items.',
      'No labels are in yet.',
    ],
    question: 'What does a collapsed prediction distribution most likely indicate?',
    options: [
      'The model suddenly got much better at rejecting bad items',
      'A near-constant, near-zero prediction distribution usually signals a broken feature (a key input became all-null/all-zero) or a bad model artifact, not a genuine quality jump — monitor the prediction distribution so this is caught before labels arrive',
      'Nothing to worry about — mean prediction is not a useful monitor',
      'The threshold needs lowering — the model is fine',
    ],
    answer: 1,
    diagnosis: 'Collapsed prediction variance is a breakage signal, not an improvement',
    explanation: 'A healthy model produces a spread of scores; when every prediction squashes to ~0.002 with almost no variance, the model has effectively stopped discriminating. The usual causes are mechanical: an important feature started returning null/zero after a pipeline or schema change (so the model sees a constant input), or the wrong/corrupt model artifact got deployed. Because this shows up in the prediction distribution instantly — long before delayed labels reveal the CTR damage — monitoring the mean and variance (or full histogram) of predictions is a cheap, fast tripwire. A real quality improvement would not zero out variance across all items.',
    fix: 'Monitor prediction mean and variance (and the histogram) with alerts on sudden collapse or spikes. On trigger, check for null/constant features and verify the deployed model artifact and version before it does damage.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-label-delay-proxy',
    subject: 'monitoring', subtopic: 'label-delay', level: 'junior', type: 'mcq',
    title: 'Monitoring a model whose labels arrive weeks late',
    context: [
      'A loan-default model only gets ground-truth labels after 60–90 days.',
      'The team wants to know within days if something is wrong.',
    ],
    question: 'What is the right monitoring approach given the label delay?',
    options: [
      'Just wait 90 days for accuracy — nothing else is trustworthy',
      'Use fast proxy signals while labels mature: input drift (PSI), prediction-distribution shift, and early proxy outcomes (e.g. early-payment behaviour); reserve the delayed labels for the eventual ground-truth accuracy check',
      'Retrain the model every day to avoid needing labels',
      'Label delay means the model cannot be monitored at all',
    ],
    answer: 1,
    diagnosis: 'Bridge label delay with leading proxy signals',
    explanation: 'When ground truth is 60-90 days out, an accuracy-only strategy is blind for a quarter. The standard approach is a layered one: watch what is available immediately — input feature drift (PSI/KS), the prediction distribution, and business proxies that correlate with the eventual label but resolve sooner (e.g. first-payment-missed as an early signal of eventual default). These catch pipeline breakage and drift within days. The delayed labels still come, and you use them for the authoritative accuracy/calibration read once they mature — but they confirm, they do not lead.',
    fix: 'Layer monitoring: immediate input/prediction drift, medium-term proxy outcomes, and delayed ground-truth accuracy. Alert on the fast signals; backfill accuracy and recalibrate when true labels arrive.',
    source: 'Authored · gapfill',
  },

  // ══════════════════════════════════════════════════════════════
  // monitoring · mid (6)
  // ══════════════════════════════════════════════════════════════
  {
    id: 'gf2-mon-psi-aggregation-trap',
    subject: 'monitoring', subtopic: 'psi', level: 'mid', type: 'mcq',
    title: 'Global PSI is calm but a segment is broken',
    context: [
      'Overall PSI on a key feature is 0.06 (stable).',
      'But the model\'s error has spiked for one country that is 4% of traffic.',
      'That country\'s feature pipeline changed; its local PSI is 0.9.',
    ],
    question: 'Why did global PSI miss it, and what is the discipline?',
    options: [
      'Global PSI cannot miss anything — the segment issue is unrelated',
      'A small segment\'s large shift is diluted in the global aggregate (4% weight against a stable 96%), so global PSI stays low; monitor drift per meaningful segment, not just globally, to catch localized breakage',
      'PSI of 0.9 is fine — only global PSI matters',
      'The fix is to raise the global PSI alert threshold',
    ],
    answer: 1,
    diagnosis: 'Aggregation hides segment-level drift',
    explanation: 'PSI computed over all traffic is a weighted average; a segment that is only 4% of volume contributes little, so even a catastrophic local shift (PSI 0.9 for that country) barely moves the global number from 0.06. The population as a whole looks stable while one segment is broken and its users get bad predictions. The discipline is to monitor drift and performance sliced by meaningful segments — geography, platform, new-vs-returning, key customer tiers — so localized pipeline breakage or drift surfaces instead of being averaged into calm. This mirrors why aggregate accuracy can hide a subgroup regression.',
    fix: 'Compute drift (and error, once labels land) per meaningful segment, not just globally. Alert on any segment breaching threshold, weighted by that segment\'s business importance rather than only its traffic share.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-alert-threshold-tradeoff',
    subject: 'monitoring', subtopic: 'alerting', level: 'mid', type: 'mcq',
    title: 'Drift alerts fire every day and get ignored',
    context: [
      'A per-feature drift alert triggers whenever daily PSI > 0.1 on any of 300 features.',
      'It fires ~20 times a day; the on-call has muted the channel.',
      'A real breakage went unnoticed for three days inside the noise.',
    ],
    question: 'What is the threshold-design problem and how do you fix it?',
    options: [
      'The threshold is too high — lower it to 0.05 to catch more',
      'Alerting on every feature at a low threshold guarantees false-positive fatigue (300 features × daily noise), which trains responders to ignore alerts; tie alerts to impact — alert on features the model actually weights, require persistence over multiple windows, and route by severity — so alerts stay trustworthy',
      'Add more features to the alert to increase coverage',
      'Alerts should never be tuned — noise is unavoidable',
    ],
    answer: 1,
    diagnosis: 'Low-threshold, per-everything alerting causes fatigue and misses real events',
    explanation: 'With 300 features and a sensitive daily threshold, natural day-to-day variation trips alarms ~20 times a day. Humans respond to a high false-positive rate by muting — so the one true breakage drowns in noise and sits for three days. The fix is to make each alert carry information: alert primarily on features with high model importance (drift on a feature the model barely uses rarely matters), require the drift to persist across multiple windows (rules out one-day blips), rank/route by severity, and consider a model-output or performance-proxy alert as the top-level tripwire with feature drift as diagnostic detail. Fewer, higher-precision alerts keep the channel credible.',
    fix: 'Alert on high-importance features, require persistence over N windows, and page by severity. Use a small set of high-precision top-level alerts (prediction/performance-proxy) with per-feature drift as drill-down, not as the pager.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-canary-metric-choice',
    subject: 'monitoring', subtopic: 'canary', level: 'mid', type: 'mcq',
    title: 'What a canary should gate on when labels are slow',
    context: [
      'A new model is canaried to 5% of traffic.',
      'The business metric (7-day retention) will not be measurable for a week.',
      'The team wants an automated gate to halt the canary fast if it is harmful.',
    ],
    question: 'What should the automated canary gate monitor?',
    options: [
      'Wait a full week for 7-day retention before any gate can act',
      'Gate on fast, safe-to-measure guardrails available immediately — error rate, latency, prediction-distribution sanity vs control, and short-horizon proxy engagement — and use the slow business metric as the final promotion criterion, not the fast kill switch',
      'Gate only on offline AUC of the new model',
      'Gate on nothing automated — humans should watch a dashboard',
    ],
    answer: 1,
    diagnosis: 'Fast guardrails kill; the slow business metric promotes',
    explanation: 'A canary needs a kill switch that acts in minutes, but the metric you ultimately care about (7-day retention) takes a week — so it cannot be the automated halt. The resolution is a two-tier gate. Fast guardrails available immediately catch acute harm: elevated error/latency, prediction distribution diverging from the control group, and short-horizon proxies (immediate CTR/engagement) with a guardrail that they do not crater. These auto-halt the canary. The slow business metric is then the promotion criterion once enough time has passed — it decides whether to ramp to 100%, but it is too laggy to be the emergency stop.',
    fix: 'Split gates: fast auto-halt on error/latency/prediction-sanity/short-horizon proxies vs a control; slow business metric (with enough exposure and a control) as the promote-to-100% decision. Never make the laggy metric the kill switch.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-feedback-loop',
    subject: 'monitoring', subtopic: 'feedback-loops', level: 'mid', type: 'mcq',
    title: 'The model only sees labels for what it recommended',
    context: [
      'A ranking model is retrained on click logs.',
      'Users can only click items the model chose to show, so items it ranks low are rarely shown and rarely get positive labels.',
      'Over successive retrains, the feed narrows and a "the model is always right" pattern appears in the metrics.',
    ],
    question: 'What is this failure mode and how do you monitor/mitigate it?',
    options: [
      'The model is genuinely improving — narrowing is good',
      'A feedback loop: training labels are collected only on items the model surfaced, so under-shown items never get a chance to prove relevance and the model self-confirms; mitigate with exploration (log randomized/less-shown items), inverse-propensity weighting, and monitor catalog coverage/exposure, not just click metrics',
      'The click logs are corrupt — delete them',
      'Add more features to break the loop',
    ],
    answer: 1,
    diagnosis: 'Selection bias in the labels creates a self-reinforcing feedback loop',
    explanation: 'The model chooses what to show, and you only observe labels for what was shown — so items ranked low get no impressions, no clicks, and no positive labels, and the next retrain concludes they are correctly low. The loop tightens each cycle: the feed narrows, popular items entrench, and offline metrics look great because the model is graded only on the distribution it created. Mitigations inject counterfactual signal: a small exploration budget shows randomized or under-exposed items to gather unbiased labels; inverse-propensity weighting reweights logged data by how likely each item was to be shown; and monitoring must include catalog coverage / exposure diversity, not only CTR, so the narrowing is visible.',
    fix: 'Add an exploration budget (randomized/epsilon-greedy impressions) to collect unbiased labels, apply inverse-propensity weighting in training, and monitor catalog coverage and exposure entropy alongside click metrics.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-drift-without-perf-drop',
    subject: 'monitoring', subtopic: 'drift', level: 'mid', type: 'mcq',
    title: 'A feature drifted hard but performance is fine',
    context: [
      'PSI on one feature is 0.4 (large drift).',
      'Model accuracy and calibration, measured on freshly arrived labels, are unchanged.',
      'The on-call wants to know whether to retrain.',
    ],
    question: 'How should the team interpret drift without a performance drop?',
    options: [
      'Any PSI > 0.25 mandates an immediate retrain regardless of performance',
      'Drift is a warning, not a verdict — a feature can shift without hurting the model if the model relies on it little or the shift is within a region it handles well; when labels confirm performance is stable, treat the drift as monitored-but-tolerated rather than auto-retraining, while watching for the point where it does start to bite',
      'The performance metric must be wrong — trust PSI and retrain',
      'Drift never matters if it does not immediately drop accuracy',
    ],
    answer: 1,
    diagnosis: 'Drift is a leading proxy, not proof of harm — confirmed performance rules',
    explanation: 'PSI measures input shift, not model harm. A feature can drift substantially yet leave performance intact: the model may weight it lightly, the shift may fall in a region the model already generalises over, or other features may compensate. When you actually have labels showing accuracy and calibration are stable, that ground truth outranks the drift signal — retraining on every drift alert wastes effort and can even destabilise a working model. The correct posture is to keep the drifted feature under closer watch (drift often precedes eventual degradation) and set a trigger tied to a real performance or calibration threshold, not to PSI alone.',
    fix: 'Do not auto-retrain on drift alone. Confirm with labels/calibration; if performance holds, mark it monitored-and-tolerated and set a retrain trigger on an actual performance or calibration threshold, keeping the feature under heightened watch.',
    source: 'Authored · gapfill',
  },
  {
    id: 'gf2-mon-latency-p50-vs-p99',
    subject: 'monitoring', subtopic: 'latency', level: 'mid', type: 'mcq',
    title: 'Mean latency looks fine but users complain',
    context: [
      'A model serving dashboard shows mean latency at 25ms, comfortably under a 100ms SLO.',
      'Users still report intermittent slowness, and 1 in 50 requests feels sluggish.',
    ],
    question: 'What is the monitoring blind spot?',
    options: [
      'Mean latency is the correct SLO metric — the complaints are unfounded',
      'The mean hides the tail: with p99 possibly at hundreds of ms, ~1 in 100 requests is slow even while the mean is 25ms; monitor and alert on tail percentiles (p95/p99), since users experience the tail, not the average',
      'The SLO should be raised so the mean fits comfortably',
      'Latency does not affect ML systems, only web servers',
    ],
    answer: 1,
    diagnosis: 'The mean masks tail latency; users feel p99',
    explanation: 'Averages are dominated by the common fast case and say nothing about the tail. A service with a 25ms mean can still have a p99 of 300ms — meaning 1% of requests are slow, which at scale is millions of bad experiences and, for a page that fans out to many model calls, a near-certainty that some call on each page hits the tail. GC pauses, cold caches, hot keys, and queueing all live in the tail. Monitor p95/p99 (and p99.9 for fan-out-heavy paths), set the SLO on a percentile not the mean, and alert on tail regressions — that is what correlates with the complaints.',
    fix: 'Set SLOs and alerts on tail percentiles (p95/p99, p99.9 for high-fan-out paths), not the mean. Investigate tail causes: GC pauses, cache misses, hot keys, queueing; add timeouts/fallbacks so the tail degrades gracefully.',
    source: 'Authored · gapfill',
  },
]

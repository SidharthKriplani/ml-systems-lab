// gapfill.js — hand-authored drills targeting thin subject×level coverage cells.
// Same shape and voice as authored.js: written to how the question is actually
// posed in interviews, with a mid-level trap distractor in every item.

export const GAPFILL_DRILLS = [
  // ── monitoring · junior (3) ────────────────────────────────────
  {
    id: 'gap-mon-drift-basics', subject: 'monitoring', subtopic: 'drift', level: 'junior', type: 'mcq',
    title: 'The model is unchanged but predictions got worse',
    context: ['A deployed model has not been retrained or redeployed in three months.', 'Its offline metrics were fine; live performance has quietly degraded.'],
    question: 'If the model code is identical, what most likely changed?',
    options: [
      'The model weights decayed over time',
      'The world moved: the incoming data distribution drifted away from what the model was trained on, so the same weights now score a different population',
      'Nothing — a static model cannot get worse',
      'The server needs a restart',
    ],
    answer: 1,
    diagnosis: 'Static model + shifting data = drift',
    explanation: 'Weights do not decay. A model is a fixed function of its inputs, so if the same function performs worse, the inputs (or the input→label relationship) must have moved. That is drift, and it is the default suspect for silent degradation on an unchanged model.',
    fix: 'Baseline the training feature and score distributions, then track them live; when the incoming distribution diverges, investigate before performance craters.',
    source: 'Authored',
  },
  {
    id: 'gap-mon-alert-fatigue', subject: 'monitoring', subtopic: 'alerting', level: 'junior', type: 'mcq',
    title: 'The drift alert fires every day and everyone ignores it',
    context: ['A monitor pages the on-call any time daily PSI on any feature exceeds 0.1.', 'It fires most days; the team has muted the channel.'],
    question: 'What is the real failure here, and the first fix?',
    options: [
      'The threshold is too high — lower it to catch more',
      'Alert fatigue: an over-sensitive, no-persistence alert trained the team to ignore it — raise the bar and require the shift to persist before paging',
      'The monitoring tool is broken',
      'Drift monitoring is not worth doing',
    ],
    answer: 1,
    diagnosis: 'A muted alert is worse than no alert',
    explanation: 'An alert that fires on normal daily noise carries no information; once muted, the one real incident is invisible. The problem is not the metric but the trigger design — no persistence window and a threshold set at the "moderate" band rather than the actionable one.',
    fix: 'Require the shift to persist (e.g. 3 consecutive days or a rolling window), page only above the significant band, and route weaker signals to a dashboard instead of the pager.',
    source: 'Authored',
  },
  {
    id: 'gap-mon-silent-staleness', subject: 'monitoring', subtopic: 'coverage', level: 'junior', type: 'mcq',
    title: 'Every dashboard is green but the feature is a week old',
    context: ['A model reads a "7-day spend" feature from a table refreshed by a nightly job.', 'The job silently failed a week ago; the table still returns its last-good values, so no error is thrown.'],
    question: 'Why did all the health checks stay green, and what should you have monitored?',
    options: [
      'The checks are correct — stale data is fine',
      'Nothing errored because a stale value is still a valid value; you needed a freshness/recency check on the feature, not just error and latency monitors',
      'The model should have crashed',
      'Latency monitoring would have caught it',
    ],
    answer: 1,
    diagnosis: 'Stale but valid data passes every error check',
    explanation: 'Latency, error rate, and null checks all pass on a value that is simply old — nothing is malformed. Silent staleness only shows up if you explicitly track how fresh the data is (last-updated timestamp vs now).',
    fix: 'Add a freshness SLA: alert when a feature\'s max timestamp lags wall-clock beyond its refresh window, and treat pipeline success/heartbeat as a monitored signal, not an assumption.',
    source: 'Authored',
  },

  // ── monitoring · mid (2) ───────────────────────────────────────
  {
    id: 'gap-mon-canary-metric', subject: 'monitoring', subtopic: 'alerting', level: 'mid', type: 'mcq',
    title: 'Which single number gives the earliest warning',
    context: ['A new fraud model is live. True default/fraud labels arrive weeks later, so accuracy is not observable now.', 'The team wants one leading "canary" metric to watch in the first hours.'],
    question: 'What makes a good canary metric here?',
    options: [
      'Weekly precision — the metric you ultimately care about',
      'The live score distribution (e.g. flag rate / mean predicted probability) vs the training baseline — observable instantly and a fast proxy for the model behaving off-distribution',
      'Server CPU utilisation',
      'The number of predictions served',
    ],
    answer: 1,
    diagnosis: 'A canary must be observable before the true label is',
    explanation: 'Precision is the goal but arrives too late to canary a rollout. The score/flag-rate distribution is available the instant traffic flows, and a sudden jump in flag rate is an early tell that the model is scoring a different population than it trained on.',
    fix: 'Gate the ramp on the prediction distribution and a few input-feature summaries against the training baseline; hold the delayed label metrics as the confirming, not the leading, signal.',
    source: 'Authored',
  },
  {
    id: 'gap-mon-what-to-watch', subject: 'monitoring', subtopic: 'coverage', level: 'mid', type: 'mcq',
    title: 'You can only afford to monitor a handful of features',
    context: ['A model has 400 input features. Monitoring drift on all 400 is noisy and expensive.', 'You must pick a small set to watch closely.'],
    question: 'Which features deserve the tightest monitoring?',
    options: [
      'The ones with the most missing values',
      'The highest-importance features plus any upstream-owned or externally-sourced ones — drift there moves predictions most and is likeliest to break silently',
      'A random 10% sample, rotated weekly',
      'The features that are cheapest to compute',
    ],
    answer: 1,
    diagnosis: 'Monitor by impact and fragility, not uniformly',
    explanation: 'Drift on a low-importance feature barely moves the output, while drift on a top-importance feature is a real threat — so weight monitoring by how much the model relies on each feature. Features owned by another team or a third party are also fragile: their upstream can change without telling you.',
    fix: 'Rank features by importance × fragility; put a tight drift + freshness monitor on the top handful and a cheap aggregate check on the long tail.',
    source: 'Authored',
  },

  // ── system_design · mid (4) ────────────────────────────────────
  {
    id: 'gap-sd-feature-caching', subject: 'system_design', subtopic: 'framing', level: 'mid', type: 'mcq',
    title: 'A slow feature lookup is blowing the latency budget',
    context: ['A ranking model needs a "user 30-day aggregate" feature at request time.', 'Computing it live from the events table takes 300ms; the total SLA is 100ms.'],
    question: 'What is the standard fix, and what does it cost you?',
    options: [
      'Compute it live but on a bigger machine',
      'Precompute it offline and cache it in a low-latency store, reading it at request time — trading some feature freshness (staleness bounded by the refresh cadence) for the latency budget',
      'Drop the feature',
      'Move the whole model to batch',
    ],
    answer: 1,
    diagnosis: 'Precompute-and-cache trades freshness for latency',
    explanation: 'An expensive aggregate cannot be recomputed inside a 100ms budget on every request. Precomputing it on a schedule and serving it from a fast KV store meets the budget; the price is that the value is as stale as your refresh interval, which is usually fine for a 30-day aggregate.',
    fix: 'Materialise heavy aggregates in an online store keyed for O(1) lookup, size the refresh cadence to the tolerable staleness, and reserve on-the-fly computation for cheap, must-be-fresh features.',
    source: 'Authored',
  },
  {
    id: 'gap-sd-batch-vs-streaming', subject: 'system_design', subtopic: 'framing', level: 'mid', type: 'mcq',
    title: 'Does this feature need a streaming pipeline?',
    context: ['Two features feed a fraud model: "account age in days" and "count of transactions in the last 5 minutes".', 'A streaming pipeline is far costlier to build and operate than a batch job.'],
    question: 'How should you decide batch vs streaming per feature?',
    options: [
      'Stream everything — real-time is always better',
      'Match the pipeline to how fast the feature actually changes: account age can be a nightly batch, but the 5-minute velocity feature is worthless unless computed in near-real-time — so stream only what needs it',
      'Batch everything to save money',
      'Use streaming for the model with more features',
    ],
    answer: 1,
    diagnosis: 'Freshness requirement, not fashion, picks the pipeline',
    explanation: 'A feature\'s pipeline should be as fresh as the signal it carries and no fresher. Account age changes by one per day — a nightly batch is exact enough. A 5-minute transaction count is a fraud signal precisely because it is recent; batching it destroys its value. Streaming is justified only for the second.',
    fix: 'Classify each feature by its freshness requirement; stream the genuinely time-sensitive ones and batch the rest, rather than paying streaming\'s operational cost across the board.',
    source: 'Authored',
  },
  {
    id: 'gap-sd-registry-rollback', subject: 'system_design', subtopic: 'design-review', level: 'mid', type: 'mcq',
    title: 'A bad model is live and rollback means retraining the old one',
    context: ['A regression ships to production. To revert, the team has to re-run training on last month\'s pipeline to reproduce the previous model.', 'That takes hours; the bad model keeps scoring meanwhile.'],
    question: 'What piece of infrastructure was missing?',
    options: [
      'A faster training cluster',
      'A model registry that versions and stores the actual trained artifact (weights + metadata), so rollback is redeploying a known-good version, not retraining',
      'More unit tests on the model code',
      'A bigger canary percentage',
    ],
    answer: 1,
    diagnosis: 'No versioned artifact = rollback by retraining',
    explanation: 'If the only way back to the previous model is to reproduce it from code and data, you do not have a rollback — you have a rebuild, and it is slow and not guaranteed identical. A registry stores each trained artifact with its version and lineage, so reverting is a redeploy of the exact bytes that were live before.',
    fix: 'Register every model artifact with a version, metrics, and the data/code lineage; make deployment a pointer swap so rollback is instant and reproducible.',
    source: 'Authored',
  },
  {
    id: 'gap-sd-load-shedding', subject: 'system_design', subtopic: 'design-review', level: 'mid', type: 'mcq',
    title: 'Traffic spikes and the whole service falls over',
    context: ['A model API is sized for normal load. A traffic surge pushes every request into a growing queue.', 'Latencies climb until timeouts cascade and the service becomes fully unavailable for everyone.'],
    question: 'What design pattern keeps the service partially up under overload?',
    options: [
      'Autoscale and hope new instances spin up in time',
      'Load shedding: once the queue passes a limit, reject or degrade excess requests fast (fail cheap / serve a fallback) so accepted requests stay within SLA instead of everyone timing out',
      'Increase every request timeout so nothing fails',
      'Add a retry on every failed request',
    ],
    answer: 1,
    diagnosis: 'Better to serve some than to drown serving all',
    explanation: 'An unbounded queue under overload means every request waits longer until all of them time out — total collapse. Load shedding caps the in-flight work: excess requests are rejected quickly or given a cheap fallback, so the requests you do accept complete within SLA. Retries and bigger timeouts make it worse by adding load.',
    fix: 'Bound queue depth / concurrency, shed or degrade beyond it (return a cached or default prediction), and add backpressure so upstream slows rather than piling on retries.',
    source: 'Authored',
  },

  // ── classical_ml · senior (2) ──────────────────────────────────
  {
    id: 'gap-classical-linear-wins', subject: 'classical_ml', subtopic: 'model-selection', level: 'senior', type: 'mcq',
    title: 'When the boring linear model is the right call',
    context: ['A lender must ship a credit model. Regulators require every decision to be explainable per-applicant.', 'A GBM scores 0.86 AUC; a regularised logistic regression scores 0.84.'],
    question: 'Why might the linear model be the correct production choice?',
    options: [
      'Linear models are always more accurate',
      'The 0.02 AUC gap is outweighed by the requirement for per-decision explainability and monotonic, auditable coefficients — a logistic model gives adverse-action reasons directly; the GBM does not without heavy post-hoc tooling',
      'GBMs cannot be used in finance',
      'Logistic regression trains faster, which is the deciding factor',
    ],
    answer: 1,
    diagnosis: 'Constraints, not just AUC, choose the model',
    explanation: 'In regulated lending, each denial needs a defensible reason. A logistic model exposes signed, monotone-constrainable coefficients that map straight to adverse-action codes; a GBM needs SHAP-style approximations that are harder to defend and can be non-monotone. A 2-point AUC edge rarely outweighs that. (Latency and simpler monitoring are secondary bonuses, not the driver — the trap answer picks one of those as decisive.)',
    fix: 'When explainability or monotonicity is a hard requirement, prefer the interpretable model unless the accuracy gap maps to material, defensible business value; otherwise reach for constrained GBMs only if the tooling can satisfy the audit.',
    source: 'Authored',
  },
  {
    id: 'gap-classical-weight-vs-resample', subject: 'classical_ml', subtopic: 'optimization', level: 'senior', type: 'mcq',
    title: 'Class weights or resample the minority class?',
    context: ['A fraud model trains on 1% positives. A teammate proposes SMOTE-oversampling the positives to 50/50.', 'Another proposes just setting class_weight to upweight positives in the loss.'],
    question: 'What is the key practical difference to reason about?',
    options: [
      'They are identical — pick either',
      'Class weights re-scale the loss on the real data (no synthetic points, calibration shift is predictable); oversampling/SMOTE fabricates or duplicates minority points, which can leak via resampling before the split and distorts the base rate so scores need recalibration',
      'SMOTE is always superior because it adds data',
      'Resampling is the only way to handle imbalance',
    ],
    answer: 1,
    diagnosis: 'Reweighting vs resampling changes different things',
    explanation: 'Both push the model to attend to the minority, but by different mechanisms. class_weight leaves the data untouched and adjusts the gradient, so it is simple and its effect on the decision threshold is predictable. SMOTE/oversampling invents or repeats minority rows — which must happen strictly inside the training fold (do it before the CV split and you leak), and it inflates the apparent positive rate, so predicted probabilities need recalibrating back to the true prior.',
    fix: 'Start with class weights (or an adjusted decision threshold) as the low-risk default; if you resample, do it inside the CV fold only, and recalibrate scores to the true base rate before they feed any decision.',
    source: 'Authored',
  },

  // ── classical_ml · staff (3) ───────────────────────────────────
  {
    id: 'gap-classical-reg-at-scale', subject: 'classical_ml', subtopic: 'optimization', level: 'staff', type: 'mcq',
    title: 'L1 vs L2 on a wide, sparse feature space',
    context: ['A linear model has 2M mostly-sparse features (hashed IDs, interactions).', 'Serving cost scales with the number of non-zero weights that must be stored and looked up.'],
    question: 'How should a staff engineer reason about the regularisation choice here?',
    options: [
      'Use L2 — it always generalises better',
      'L1 (or elastic-net leaning L1) drives many weights to exact zero, which both regularises and prunes the model to a serveable size; pure L2 shrinks but keeps all 2M weights, so it costs more to serve for no sparsity benefit',
      'Skip regularisation and rely on more data',
      'Use L2 with a very large lambda to zero out weights',
    ],
    answer: 1,
    diagnosis: 'On a wide sparse space, L1 doubles as feature selection',
    explanation: 'The two penalties do different things: L2 shrinks all weights smoothly but toward — not to — zero, so a 2M-feature model stays 2M-feature at serve time. L1\'s corner geometry produces exact zeros, so it prunes the model and shrinks the serving footprint as a side effect. Large-lambda L2 shrinks everything but still never yields the exact zeros the trap answer assumes.',
    fix: 'Prefer L1/elastic-net when sparsity is a serving requirement, tune lambda against both AUC and the resulting non-zero count, and remember L2 is the better default only when you want to keep all features and just tame their magnitude.',
    source: 'Authored',
  },
  {
    id: 'gap-classical-selection-leakage', subject: 'classical_ml', subtopic: 'optimization', level: 'staff', type: 'mcq',
    title: 'Feature selection ran once, on all the data',
    context: ['A pipeline selects the top-200 features by correlation with the target on the full dataset, then cross-validates the model on those 200.', 'CV AUC looks strong; production AUC is markedly lower.'],
    question: 'Where did the leakage enter?',
    options: [
      'Nowhere — selecting before CV is standard',
      'The selection used the whole dataset, so it peeked at the CV validation folds when choosing features; the CV then evaluates a model whose feature set already saw those folds, inflating the estimate — selection must live inside the CV loop',
      'The model overfit because 200 features is too many',
      'The target was miscoded',
    ],
    answer: 1,
    diagnosis: 'Selecting on all data leaks the validation folds',
    explanation: 'Feature selection is part of training, so it must only ever see the training portion of each fold. Ranking features on the full dataset lets the choice absorb information from the very rows CV later uses to validate — the estimate is optimistic and does not survive to fresh data. The trap answer blames capacity, but the gap would persist even with few features because the leak is in the protocol, not the count.',
    fix: 'Wrap selection (and any target-aware transform) inside each CV fold so it only sees that fold\'s training rows; nest CV if you are also tuning the number selected.',
    source: 'Authored',
  },
  {
    id: 'gap-classical-tree-vs-gbm-prod', subject: 'classical_ml', subtopic: 'ensembles', level: 'staff', type: 'mcq',
    title: 'A single deep tree or a boosted ensemble in production',
    context: ['A team debates one deep decision tree (fully interpretable, one artifact) vs a gradient-boosted ensemble (higher accuracy, hundreds of trees).', 'The use case tolerates a black box but has a strict retrain SLA and drift.'],
    question: 'What is the load-bearing tradeoff a staff engineer weighs?',
    options: [
      'Always ship the GBM — more accurate',
      'A single deep tree is high-variance and unstable (small data shifts flip splits), so it drifts poorly; a GBM averages many shallow trees for far lower variance and better accuracy, at the cost of interpretability and a heavier retrain — usually worth it unless interpretability is required',
      'A single tree is more accurate because it is simpler',
      'They perform identically once tuned',
    ],
    answer: 1,
    diagnosis: 'Single tree = low bias, brutal variance',
    explanation: 'One deep tree is easy to read but notoriously unstable: reshuffle the data slightly and the top splits change, which is exactly the wrong property under drift and frequent retraining. Boosting many shallow trees trades that instability and some accuracy in for a robust, lower-variance model. The interpretability loss is the real cost — reach for the single tree only when a human must trace every decision.',
    fix: 'Default to the GBM for accuracy and stability under retraining; keep a single tree (or a distilled surrogate) only where a fully traceable decision path is a hard requirement.',
    source: 'Authored',
  },

  // ── time_series · staff (3) ────────────────────────────────────
  {
    id: 'gap-ts-hierarchical-reconcile', subject: 'time_series', subtopic: 'forecasting', level: 'staff', type: 'mcq',
    title: 'The store forecasts do not add up to the region forecast',
    context: ['You forecast demand per SKU, per store, and per region independently.', 'The sum of the store-level forecasts does not match the region-level forecast the business reports.'],
    question: 'What is the issue and the accepted fix?',
    options: [
      'Just report the region forecast and ignore the stores',
      'Forecasts made independently at each level are not coherent; reconciliation (e.g. MinT / optimal reconciliation) adjusts them so lower levels sum to their parents while borrowing strength across the hierarchy',
      'Average the store and region forecasts together',
      'Forecast only at the bottom level and always sum up',
    ],
    answer: 1,
    diagnosis: 'Independent per-level forecasts are incoherent',
    explanation: 'When each level is modelled on its own, nothing forces the parts to sum to the whole, so the numbers contradict each other and the business loses trust. Bottom-up summing is coherent but throws away the smoother top-level signal; reconciliation methods (optimal/MinT) combine all levels into a single coherent set, usually improving accuracy at every level.',
    fix: 'Fit base forecasts at each level, then apply optimal reconciliation so the hierarchy is coherent and each level borrows strength from the others; do not just pick one level and derive the rest naively.',
    source: 'Authored',
  },
  {
    id: 'gap-ts-intermittent-demand', subject: 'time_series', subtopic: 'model-selection', level: 'staff', type: 'mcq',
    title: 'Most days the item sells zero',
    context: ['A spare-parts SKU sells 0 units on most days and a small burst occasionally.', 'A standard forecaster (ARIMA/ETS on the raw series) predicts a smooth near-zero line and RMSE looks fine.'],
    question: 'Why is the standard approach and RMSE misleading, and what fits better?',
    options: [
      'It is fine — near-zero is the right forecast',
      'Intermittent demand needs methods built for it (Croston / SBA, or modelling demand size and inter-arrival separately); RMSE rewards the flat near-zero line yet gives zero guidance on when the sporadic demand actually lands, which is the whole decision',
      'Just forecast the yearly total and divide by 365',
      'Switch to a deeper neural net on the raw series',
    ],
    answer: 1,
    diagnosis: 'Intermittent series break smooth forecasters and RMSE',
    explanation: 'A mostly-zero series has demand in two parts — how often it occurs and how big it is when it does. Continuous forecasters smear it into a flat near-zero line that minimises squared error while being operationally useless: it never tells you when to hold stock. Croston-family methods model the size and the inter-arrival gap separately, and you should judge them on stocking/service-level metrics, not RMSE.',
    fix: 'Use Croston/SBA or a size-and-interval decomposition (or a count/zero-inflated model), and evaluate on inventory-relevant metrics (fill rate, holding vs stockout cost), not RMSE on a zero-heavy series.',
    source: 'Authored',
  },
  {
    id: 'gap-ts-backtest-horizon', subject: 'time_series', subtopic: 'forecasting', level: 'staff', type: 'mcq',
    title: 'The backtest looks great but reports 1-step error for a 14-day decision',
    context: ['The business acts on a 14-day-ahead forecast.', 'The backtest refits daily and reports one-step-ahead error, which looks excellent.'],
    question: 'Why is the backtest dishonest, and how do you fix it?',
    options: [
      'It is honest — lower error is always better',
      'It measures the wrong horizon: one-step error is far smaller than 14-step error, so the report flatters a model that must actually predict 14 days out; backtest at the deployment horizon with a rolling origin and no refit inside the horizon',
      'Refit less often to raise the error and be safe',
      'Use a bigger test set to fix it',
    ],
    answer: 1,
    diagnosis: 'Backtest the horizon you deploy, not a shorter one',
    explanation: 'Error grows with horizon, so a one-step backtest systematically understates the error the business will actually experience at 14 days. It is a horizon mismatch, not a size problem. An honest backtest fixes an origin, forecasts the full 14-day horizon before advancing, and never lets information from within that window leak into the forecast.',
    fix: 'Use rolling-origin (walk-forward) evaluation at the true deployment horizon, aggregate error across origins, and report the horizon-specific error the decision depends on.',
    source: 'Authored',
  },

  // ── deep_learning · mid (3) ────────────────────────────────────
  {
    id: 'gap-dl-batchnorm-eval', subject: 'deep_learning', subtopic: 'training-failures', level: 'mid', type: 'mcq',
    title: 'Great training accuracy, garbage at inference — batch size 1',
    context: ['A net with BatchNorm trains well. At inference it serves one request at a time (batch size 1) and predictions are wildly off.', 'A colleague forgot to switch the model out of training mode.'],
    question: 'What is going wrong with BatchNorm here?',
    options: [
      'BatchNorm cannot be used at inference at all',
      'In train mode BatchNorm normalises using the current batch\'s stats — with batch size 1 that is meaningless; in eval mode it must use the running mean/variance learned during training. The model was left in train mode',
      'The learning rate is too high at inference',
      'The single input needs to be duplicated to form a batch',
    ],
    answer: 1,
    diagnosis: 'BatchNorm needs eval mode to use running stats',
    explanation: 'BatchNorm behaves differently in the two modes by design. During training it normalises with the statistics of the live batch and updates a running estimate; at inference it must switch to those frozen running statistics, because a single-example "batch" has no usable variance. Leaving it in train mode (or forgetting model.eval()) makes each prediction depend on nonsense per-request stats.',
    fix: 'Call model.eval() (and disable grad) at inference so BatchNorm/Dropout use their evaluation behaviour; if batches at train time are tiny, consider GroupNorm/LayerNorm which do not depend on batch statistics.',
    source: 'Authored',
  },
  {
    id: 'gap-dl-lr-warmup', subject: 'deep_learning', subtopic: 'optimizers', level: 'mid', type: 'mcq',
    title: 'Loss explodes in the first few hundred steps, then would be fine',
    context: ['A transformer trained with a large target learning rate and Adam diverges in the opening steps.', 'The same target LR is stable if the model gets past the start.'],
    question: 'What does learning-rate warmup do, and why does it help here?',
    options: [
      'It trains faster by starting at the highest LR',
      'It ramps the LR up from near-zero over the first N steps, so early updates are small while Adam\'s variance estimates and the model are still unstable — avoiding the large early steps that blow up the loss',
      'It lowers the final LR to improve accuracy',
      'It replaces the need for gradient clipping',
    ],
    answer: 1,
    diagnosis: 'Warmup tames the unstable opening steps',
    explanation: 'At the very start, weights are random and Adam\'s second-moment estimates are noisy, so a full-size step can send the loss to infinity. Warmup starts the LR near zero and ramps it to the target over a few hundred/thousand steps, keeping early updates gentle until the optimiser state settles; the peak LR is unchanged. It is not a substitute for clipping — they address related but distinct failure modes.',
    fix: 'Add a linear (or short cosine) warmup over the first few hundred–thousand steps, then decay; keep gradient clipping as an independent safety net for spikes later in training.',
    source: 'Authored',
  },
  {
    id: 'gap-dl-mixed-precision', subject: 'deep_learning', subtopic: 'optimizers', level: 'mid', type: 'mcq',
    title: 'Mixed precision made training NaN',
    context: ['Switching to fp16 mixed-precision training speeds things up but the loss becomes NaN within an epoch.', 'The engineer removed the GradScaler to "simplify" the loop.'],
    question: 'What is the GradScaler for, and why did removing it break things?',
    options: [
      'It scales the learning rate for fp16',
      'Small gradients underflow to zero in fp16\'s narrow range; GradScaler multiplies the loss by a large factor before backward so gradients stay representable, then unscales before the optimizer step — removing it lets tiny gradients vanish and destabilise training',
      'It converts the model weights to fp16',
      'It is only needed for very large batch sizes',
    ],
    answer: 1,
    diagnosis: 'fp16 underflow needs loss scaling',
    explanation: 'fp16 has a much smaller dynamic range than fp32, so the smallest gradients round to zero and learning silently stalls or destabilises. GradScaler scales the loss up before backprop so those gradients land in fp16\'s representable range, then unscales them (and skips the step on inf/NaN) before the optimizer updates. It is not about learning rate or converting weights — it is purely about keeping gradients representable.',
    fix: 'Keep the GradScaler (or use bf16, which has fp32-like range and often needs no scaling); it dynamically tunes the scale and skips steps that overflow, so leave it in the loop.',
    source: 'Authored',
  },

  // ── deep_learning · mid — gradient clipping (rounds DL·mid to what the spread needs) ──
  {
    id: 'gap-dl-grad-clipping', subject: 'deep_learning', subtopic: 'gradients', level: 'mid', type: 'mcq',
    title: 'Occasional loss spikes wreck an otherwise-stable run',
    context: ['An RNN/transformer trains fine mostly, but every so often a batch produces a huge gradient and the loss jumps, sometimes to NaN.'],
    question: 'What does gradient clipping do about this?',
    options: [
      'It lowers the learning rate permanently',
      'It caps the gradient norm (or value) at a threshold before the optimizer step, so a single exploding-gradient batch cannot take a giant destabilising step — bounding the update size without changing the LR',
      'It removes the batches that cause spikes',
      'It normalises the inputs to prevent large gradients',
    ],
    answer: 1,
    diagnosis: 'Clipping bounds the worst-case update',
    explanation: 'Some architectures occasionally produce gradients orders of magnitude larger than typical, and one such step can throw the model off a good trajectory or overflow to NaN. Clipping rescales the gradient so its norm never exceeds a set threshold, capping how far any single step can move the weights. It leaves the learning rate and normal-sized gradients untouched — it only trims the rare giants.',
    fix: 'Use clip-by-global-norm at a threshold tuned from observed gradient norms; keep it alongside warmup — warmup prevents early blowups, clipping catches spikes throughout.',
    source: 'Authored',
  },

  // ── data · mid (2) ─────────────────────────────────────────────
  {
    id: 'gap-data-encoding-leakage', subject: 'data', subtopic: 'leakage', level: 'mid', type: 'mcq',
    title: 'Target encoding on a high-cardinality column looked amazing',
    context: ['A zip-code feature (thousands of values) is target-encoded: each zip replaced by the mean target over all rows with that zip.', 'CV AUC jumped; production AUC did not.'],
    question: 'What went wrong with the encoding?',
    options: [
      'Target encoding is always invalid — use one-hot',
      'Encoding each zip with the target mean over all rows leaks the label into the feature, especially for rare zips where the mean is basically the row\'s own target — it must be computed out-of-fold (or with smoothing/leave-one-out) so a row never sees its own label',
      'The cardinality is too high for any encoding',
      'It overfit because zip is not predictive',
    ],
    answer: 1,
    diagnosis: 'Target encoding on all rows leaks the label',
    explanation: 'Target encoding is powerful for high-cardinality columns but is a direct leakage trap: computing the per-category target mean over the whole dataset lets each row peek at its own label through the feature. For rare categories the mean is dominated by that very row, so the encoded value practically is the target — inflating CV and collapsing in production where new zips have no such shortcut.',
    fix: 'Compute target encodings out-of-fold (leave-one-out or K-fold), add smoothing toward the global mean for rare categories, and fit the encoder inside the CV loop like any other target-aware transform.',
    source: 'Authored',
  },
  {
    id: 'gap-data-imbalance-choice', subject: 'data', subtopic: 'skew', level: 'mid', type: 'mcq',
    title: 'The first move on a 2%-positive dataset',
    context: ['A new classifier trains on 2% positives. A teammate\'s first instinct is to oversample to 50/50 before anything else.'],
    question: 'What should you check before reaching for resampling?',
    options: [
      'Immediately oversample — balance is always required',
      'First fix the metric and threshold: a threshold-agnostic model with the right loss/eval (PR-AUC, class weights, a tuned threshold) often handles 2% fine — resampling is one tool, not the mandatory first step, and it distorts calibration',
      'Delete most negatives to balance the classes',
      'Collect data until the classes are 50/50',
    ],
    answer: 1,
    diagnosis: 'Imbalance ≠ automatic resampling',
    explanation: 'Class imbalance is usually a metric-and-threshold problem before it is a data problem. Accuracy misleads at 2% positives, but PR-AUC plus class weights and a threshold chosen from the cost tradeoff often solves it without touching the data. Resampling changes the base rate and calibration, so it is a considered choice — not the reflexive first move the trap answer makes it.',
    fix: 'Start with the right metric (PR-AUC/recall at a fixed precision), class weights, and a cost-driven threshold; only add resampling if that is insufficient, and recalibrate afterwards.',
    source: 'Authored',
  },

  // ── data · staff (1) ───────────────────────────────────────────
  {
    id: 'gap-data-pit-feature-store', subject: 'data', subtopic: 'windows', level: 'staff', type: 'mcq',
    title: 'Training features that quietly used the future',
    context: ['A feature store joins each training label to the current value of "customer lifetime value", which is recomputed nightly and overwritten.', 'For a label from six months ago, training reads today\'s CLV, not the CLV as of that date.'],
    question: 'What is the failure, and what does a point-in-time feature store fix?',
    options: [
      'Nothing — the latest feature value is the best value',
      'The join uses the feature\'s current value instead of its value as of the label timestamp, leaking future information into old training rows; a point-in-time-correct join reconstructs each feature as it was when the label occurred',
      'The feature should just be recomputed more often',
      'The label timestamps are wrong',
    ],
    answer: 1,
    diagnosis: 'Non-point-in-time join leaks the future into history',
    explanation: 'A feature that is overwritten in place has no history, so joining a six-month-old label to today\'s value trains the model on information that did not exist at prediction time. It scores well offline and fails live, because at serving there is no future to borrow. Point-in-time correctness means the store retains feature history and joins each label to the feature value as of that label\'s timestamp.',
    fix: 'Store feature history with valid-from/valid-to timestamps and do an as-of (point-in-time) join on label time; audit every feature for whether its training value was actually knowable at prediction time.',
    source: 'Authored',
  },
]

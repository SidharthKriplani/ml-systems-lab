// production.js — normalized JUDGE drills for the Production / serving subject.
// Migrated from src/tabs/MLOpsDeployTab.jsx + src/tabs/MLOpsPipelinesTab.jsx
// (the two MLOps tabs were merged into one "MLOps: Deploy, Pipelines & Registry" tab).
//
// Every item conforms to the drillPool.js schema:
//   { id, subject:'production', subtopic, level, type:'mcq',
//     title, context, question, options[str], answer(int),
//     diagnosis?, explanation?, fix?, source }
//
// Subtopics: deployment | rollback | registry | ci-cd | infra | schema
// awsCallout content is folded into `explanation`; raw <svg> stripped from all text.

export const PRODUCTION_DRILLS = [
  // ─── Deployment Strategy (from DEPLOY_SCENARIOS) ────────────────────────────
  {
    id: 'production-deploy-fraud-canary',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'Fraud detection model — cannot test offline, must limit blast radius',
    context: "New fraud detection model. Can't test offline — fraud patterns require live traffic. Need to limit blast radius if it fires too aggressively.",
    question: 'Which deployment strategy fits — testing against real traffic while controlling exposure, with automatic halt if a business metric breaches a threshold?',
    options: ['Canary', 'Shadow Mode', 'Blue-Green', 'Feature Flag', 'Rolling Update', 'Immediate Full Deploy'],
    answer: 0,
    diagnosis: 'Canary — % traffic ramp with metric gates',
    explanation: "Start at 1%, watch false positive rate and block rate vs champion. Shadow would generate no real decisions so you can't measure business impact. Feature flag requires explicit user targeting which doesn't make sense for fraud. AWS in production: SageMaker Deployment Guardrails configure automatic traffic shifting with metric-gated rollback — halt the canary ramp if false positive rate exceeds your threshold.",
    fix: 'Deploy as a canary starting at 1%, gate the ramp on false-positive/block-rate vs champion, and configure automatic rollback if the metric breaches threshold.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 1)',
  },
  {
    id: 'production-deploy-embedding-shadow',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'New embedding architecture — completely different score distribution',
    context: 'Updated recommendation model. New embedding architecture — completely different score distribution. You need to compare engagement metrics over 2 weeks.',
    question: 'A completely different score distribution means offline metrics cannot predict live behaviour. Which strategy collects live predictions without serving them to users?',
    options: ['Canary', 'Shadow Mode', 'Blue-Green', 'Feature Flag', 'Rolling Update', 'Immediate Full Deploy'],
    answer: 1,
    diagnosis: 'Shadow Mode first, then Canary',
    explanation: 'Shadow lets you collect predictions without impacting users. After offline comparison passes, canary 5% → 20% → 50% → 100% with engagement guardrails. Never go straight to canary on a completely different score distribution. AWS in production: SageMaker Shadow Testing routes a copy of live traffic to the shadow variant and logs predictions without serving them — the managed AWS primitive for this exact pattern.',
    fix: 'Run shadow mode to collect live predictions, compare offline, then canary-ramp with engagement guardrails.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 2)',
  },
  {
    id: 'production-deploy-bugfix-rolling',
    subject: 'production', subtopic: 'deployment', level: 'mid', type: 'mcq',
    title: 'Critical serving code bug fix — identical model weights',
    context: 'Critical bug fix in the model serving code (NaN handling). Identical model weights, just a one-line code fix.',
    question: 'No model change means no need for a gradual traffic ramp. Which strategy fits when the only risk is infrastructure continuity, not prediction quality?',
    options: ['Canary', 'Shadow Mode', 'Blue-Green', 'Feature Flag', 'Rolling Update', 'Immediate Full Deploy'],
    answer: 4,
    diagnosis: 'Rolling Update (or Blue-Green)',
    explanation: 'No model change means no need for gradual traffic ramp. Rolling is standard; Blue-Green if you want instant rollback capability. AWS in production: SageMaker Endpoints support rolling updates natively; pair with CodeDeploy blue/green if you need instant rollback without instance churn.',
    fix: 'Ship a rolling update; use Blue-Green if you want instant rollback capability.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 3)',
  },
  {
    id: 'production-deploy-multisurface-featureflag',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'Multi-surface ranking model — teams own different surfaces',
    context: 'New content ranking model. Different teams own different user surfaces (homepage, search, notifications). You want team leads to control when their surface gets the new model.',
    question: 'Traffic-percentage ramps do not express team or surface ownership. Which strategy is designed for surface-level rollout with independent owner control?',
    options: ['Canary', 'Shadow Mode', 'Blue-Green', 'Feature Flag', 'Rolling Update', 'Immediate Full Deploy'],
    answer: 3,
    diagnosis: 'Feature Flag — segment by surface',
    explanation: "Each team lead can flip their surface independently. Canary can't express this ownership structure. AWS in production: SageMaker Multi-Model Endpoints let each surface route to a distinct model variant on the same endpoint — team leads swap their variant without touching others.",
    fix: 'Use feature flags segmented by surface so each team lead controls their own rollout.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 4)',
  },
  {
    id: 'production-deploy-internal-immediate',
    subject: 'production', subtopic: 'deployment', level: 'junior', type: 'mcq',
    title: 'Internal analyst tool — 20 users, no external customers',
    context: 'Model passed all offline evals. Deploying to an internal tool used by 20 data analysts. No external customers affected.',
    question: 'Does the blast radius justify the overhead of a gradual rollout for 20 internal users?',
    options: ['Canary', 'Shadow Mode', 'Blue-Green', 'Feature Flag', 'Rolling Update', 'Immediate Full Deploy'],
    answer: 5,
    diagnosis: 'Immediate Full Deploy',
    explanation: 'Low blast radius, known user base, internal only. Over-engineering deployment here wastes time. Ship it, monitor it. AWS in production: SageMaker Serverless Inference is ideal for low-traffic internal tools — zero idle cost, auto-scaling, no instance management overhead.',
    fix: 'Ship it directly and monitor; a gradual rollout is over-engineering for this blast radius.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 5)',
  },
  {
    id: 'production-deploy-llm-shadow-humaneval',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'LLM serving upgrade — automated metrics unreliable',
    context: "LLM serving upgrade: new version produces slightly different token probabilities. Need to compare response quality but automated metrics aren't reliable for this.",
    question: 'LLM quality requires human judgment, not just automated metrics. Which strategy lets you collect outputs for human preference evaluation before any live traffic?',
    options: ['Canary', 'Shadow Mode', 'Blue-Green', 'Feature Flag', 'Rolling Update', 'Immediate Full Deploy'],
    answer: 1,
    diagnosis: 'Shadow Mode with human eval',
    explanation: 'LLM quality requires human judgment, not just automated metrics. Run both versions, collect shadow outputs, run human preference evaluation before any live traffic. AWS in production: SageMaker Shadow Testing captures challenger outputs alongside champion responses without serving them — feed those logs into a human evaluation pipeline before promoting the new version.',
    fix: 'Run shadow mode, collect challenger outputs, and gate promotion on human preference evaluation.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 6)',
  },
  {
    id: 'production-deploy-batch-500m-emr',
    subject: 'production', subtopic: 'infra', level: 'staff', type: 'mcq',
    title: 'Batch inference at scale — 500M users, 90-minute SLA',
    context: '500M user daily batch scoring job. Models are XGBoost (5MB). Must complete within 90 minutes. Current approach: a single SageMaker Batch Transform job takes 6.5 hours. Budget is constrained.',
    question: 'Which architecture gives fine-grained parallelism control, lowest cost-per-inference, and fits within 90 minutes for 500M rows?',
    options: [
      'Keep a single SageMaker Batch Transform job and add instances',
      'Step Functions fan-out over Lambda',
      'Spark on EMR (spot instances) with the model broadcast as a UDF',
      'Blue-Green live-serving endpoints',
    ],
    answer: 2,
    diagnosis: 'Spark on EMR with the model broadcast as a UDF',
    explanation: 'At 500M rows, Spark with spot instances (r5.4xlarge × 20) scores in 45–60 minutes and costs ~$12/run. SageMaker Batch Transform launches one container per mini-batch — orchestration and instance startup overhead dominates when each inference is sub-millisecond (XGBoost 5MB model). Step Functions fan-out over Lambda is capped at 15 minutes per invocation and needs complex DLQ + aggregation logic. Blue-Green and canary are live-serving strategies, not batch architectures. AWS in production: EMR Spot + SageMaker Model Registry — pull the registered XGBoost artifact from S3, broadcast as a Spark UDF across r5.4xlarge spot workers; ~50 minutes at ~$12/run vs ~$80 for equivalent on-demand Batch Transform.',
    fix: 'Broadcast the registered XGBoost artifact as a Spark UDF on EMR spot workers; completes in ~50 minutes at ~$12/run.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 8)',
  },
  {
    id: 'production-deploy-churn-registry-canary',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'Retrained churn model — needs governed production promotion',
    context: 'Monthly retrain of a churn model passed offline evaluation (AUC +0.03 vs champion). The artifact is in S3. You need zero-downtime promotion with rollback capability within 60 seconds if business metrics degrade.',
    question: 'Which deployment strategy gives metric-gated rollback at the endpoint level after registering the artifact?',
    options: ['Canary', 'Shadow Mode', 'Blue-Green', 'Feature Flag', 'Rolling Update', 'Immediate Full Deploy'],
    answer: 0,
    diagnosis: 'Register → canary with Deployment Guardrails',
    explanation: 'SageMaker train → Model Registry (register with approval gate) → canary endpoint with Deployment Guardrails. The register step creates an immutable versioned artifact; the canary ramp gates on business metrics (churn call rate, revenue impact) before full promotion. Blue-Green would flip all traffic instantly — no metric gate during ramp. Shadow cannot roll back on business metrics because it serves no users. Immediate deploy skips the registry and audit trail. AWS in production: SageMaker Model Registry + Deployment Guardrails — register with approval status, then deploy with automatic traffic shifting and metric-gated rollback.',
    fix: 'Register the artifact with an approval gate, then canary-ramp with Deployment Guardrails for automatic metric-gated rollback.',
    source: 'MLOps Deploy · Deployment Strategy (scenario 7)',
  },

  // ─── Champion-Challenger (from CC_QUESTIONS) ────────────────────────────────
  {
    id: 'production-cc-initial-recommendation',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'Champion-Challenger — initial promotion recommendation',
    context: [
      'Challenger vs champion after 2 weeks in shadow/canary.',
      'AUC-PR 0.847 → 0.861 (better) · ECE 0.043 → 0.038 (better) · False positive rate 4.2% → 3.8% (better)',
      'p50 latency 12ms → 18ms (worse) · p99 latency 45ms → 89ms (bad) · Memory 420MB → 780MB (worse) · Training time 4h → 7h (worse)',
    ],
    question: 'Based on these metrics, what is your initial recommendation?',
    options: [
      'Promote challenger — AUC-PR, ECE, and false positive rate all improved',
      'Keep champion — latency and memory regressions outweigh accuracy gains',
      'Promote with conditions — resolve the p99 latency issue before shipping',
      'Reject and iterate — training time increase signals over-engineering',
    ],
    answer: 2,
    diagnosis: 'Promote with conditions — fix p99 latency first',
    explanation: "The p99 latency jump from 45ms → 89ms is a red flag if there's an SLA. Accuracy gains are real but you need to resolve the latency issue first before promoting. Keeping the champion would be right only if the latency regression were unresolvable — but the right move is to investigate and fix, not surrender the accuracy gains. Training time is not a ship gate. AWS in production: SageMaker Inference Recommender benchmarks challenger latency across instance types so you have real numbers before committing to a conditional promotion.",
    fix: 'Preserve the accuracy gains but gate promotion on resolving the p99 latency regression first.',
    source: 'MLOps Deploy · Champion-Challenger (Q1)',
  },
  {
    id: 'production-cc-sla-check',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'Champion-Challenger — does the challenger meet the p99 SLA?',
    context: ['SLA for this service is p99 < 100ms.', 'Challenger measures p99 = 89ms on current traffic.'],
    question: 'Does the challenger meet the SLA?',
    options: [
      'Yes, 89ms < 100ms, it passes',
      'No, 98% increase is too close to the limit',
      'Depends on traffic variance',
      'Need more data',
    ],
    answer: 2,
    diagnosis: 'Depends — 89ms on current traffic may be 110ms at peak',
    explanation: '89ms on current traffic might be 110ms under peak load. 11ms headroom at p99 is insufficient. You need load testing under peak traffic, not just current traffic. AWS in production: SageMaker Inference Recommender runs load tests at configured peak traffic levels and reports p99 latency and throughput — use this before trusting any single-traffic-level latency number.',
    fix: 'Load-test at peak traffic before trusting a single-traffic-level p99 number.',
    source: 'MLOps Deploy · Champion-Challenger (Q2)',
  },
  {
    id: 'production-cc-over-sla-profile',
    subject: 'production', subtopic: 'deployment', level: 'senior', type: 'mcq',
    title: 'Champion-Challenger — p99 = 103ms under peak, just over SLA',
    context: ['Load test shows p99 = 103ms under peak load — 3ms over the 100ms SLA.', 'Accuracy gains are real and worth preserving.'],
    question: 'What do you do?',
    options: [
      'Reject the challenger — 3ms over SLA means it fails the promotion gate',
      'Profile and optimize the hot path before making a ship/no-ship decision',
      'Deploy to low-traffic segments only where p99 is within SLA',
      'Upgrade to a larger instance type to bring p99 within SLA',
    ],
    answer: 1,
    diagnosis: 'Profile the hot path before deciding',
    explanation: "The accuracy gains are real and worth preserving. First, profile what's causing the extra ~50ms (new features? model size? I/O?). Often 80% of latency gain comes from one inefficiency. Don't throw away a better model because of an undiagnosed bottleneck. Low-traffic-only is a reasonable interim step but leaves most users on the inferior champion. A larger instance treats the symptom before diagnosing the cause. AWS in production: SageMaker Endpoints expose CloudWatch metrics per variant (CPU/GPU utilisation, invocation time) — start profiling the hot path there before reaching for a heavier profiling tool.",
    fix: 'Profile the hot path to find the dominant latency source before rejecting or over-provisioning.',
    source: 'MLOps Deploy · Champion-Challenger (Q3)',
  },
  {
    id: 'production-cc-final-promote',
    subject: 'production', subtopic: 'deployment', level: 'staff', type: 'mcq',
    title: 'Champion-Challenger — p99 = 67ms after optimization, promote?',
    context: ['After optimization the challenger runs at p99 = 67ms under peak — well within the 100ms SLA.'],
    question: 'How do you promote?',
    options: [
      'Yes — promote to 100% immediately, latency is now well within SLA',
      'Yes — canary ramp 5%→20%→50%→100% with manual approval gates',
      'Yes — canary ramp with automatic rollback trigger on metric breach',
      'No — run another week of shadow mode to confirm the optimization holds',
    ],
    answer: 2,
    diagnosis: 'Canary ramp with automatic rollback triggers',
    explanation: 'Ramp 5% → 20% → 50% → 100% with an automatic rollback trigger: if p99 > 80ms OR false positive rate > 5% over a 1-hour window → auto-rollback to champion. Canary with manual gates is better than immediate full deploy but worse than automatic triggers — human approvals introduce delay and overnight gaps. Automatic rollback is the production standard because metric breaches can happen at 3am. AWS in production: SageMaker Deployment Guardrails automate the canary ramp and fire automatic rollback to the champion endpoint if your CloudWatch alarm thresholds are breached during promotion.',
    fix: 'Canary-ramp with automatic rollback triggers on p99 and false-positive thresholds — no reliance on overnight human approval.',
    source: 'MLOps Deploy · Champion-Challenger (Q4)',
  },

  // ─── Rollback Decision (from ROLLBACK_SCENARIOS) ────────────────────────────
  {
    id: 'production-rollback-error-spike-npe',
    subject: 'production', subtopic: 'rollback', level: 'mid', type: 'mcq',
    title: 'Error rate spike — NullPointerExceptions right after deploy',
    context: 'Model error rate jumped from 0.1% to 2.3% within 5 minutes of deploy. Errors are NullPointerExceptions in the feature pipeline.',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 0,
    diagnosis: 'Rollback immediately',
    explanation: "Sharp spike immediately post-deploy = deploy caused it. Don't investigate first — you're actively erroring on 2% of traffic. Roll back, then investigate safely. AWS in production: SageMaker Deployment Guardrails with automatic rollback triggers — configure a CloudWatch alarm on error rate > 1% to roll back the endpoint to the previous variant automatically, no on-call page required.",
    fix: 'Roll back first to stop the active errors, then investigate the NPE in the feature pipeline safely.',
    source: 'MLOps Deploy · Rollback Decision (alert 1)',
  },
  {
    id: 'production-rollback-aucpr-drift',
    subject: 'production', subtopic: 'rollback', level: 'senior', type: 'mcq',
    title: 'AUC-PR degradation over 3 days — no recent deploys',
    context: 'Model AUC-PR dropped from 0.847 to 0.831 over the past 3 days. No recent deploys.',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 1,
    diagnosis: 'Investigate — this is drift, not a code bug',
    explanation: "Gradual metric degradation with no deploy is data drift, not a code bug. Rolling back won't help. Investigate: which features drifted? Is this seasonal? What changed upstream? AWS in production: SageMaker Model Monitor schedules continuous data quality and model quality monitoring jobs — set up a baseline from production traffic and alert when feature distributions exceed PSI thresholds.",
    fix: 'Investigate feature drift and upstream changes; rollback would not address a data-distribution shift.',
    source: 'MLOps Deploy · Rollback Decision (alert 2)',
  },
  {
    id: 'production-rollback-monday-latency',
    subject: 'production', subtopic: 'rollback', level: 'mid', type: 'mcq',
    title: 'Latency increase — Monday-morning traffic ramp',
    context: 'p99 latency increased from 45ms to 62ms over the past 2 hours. Currently 8am Monday — traffic ramping up for start of business.',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 2,
    diagnosis: 'Monitor only — expected traffic-correlated increase',
    explanation: "Traffic-correlated latency increase at start-of-business is expected. 62ms is still within SLA. The alert is noisy. No action until you're outside SLA or the pattern persists at stable traffic. AWS in production: SageMaker Endpoints auto-scaling policies adjust instance count based on InvocationsPerInstance — ensure your scaling policy warms up before Monday peaks instead of treating the ramp as an incident.",
    fix: 'Monitor; tune auto-scaling warm-up so the expected Monday ramp is not treated as an incident.',
    source: 'MLOps Deploy · Rollback Decision (alert 3)',
  },
  {
    id: 'production-rollback-constant-half',
    subject: 'production', subtopic: 'rollback', level: 'mid', type: 'mcq',
    title: '40% of predictions returning exactly 0.5000',
    context: '40% of predictions are returning score exactly 0.5000 since the last feature pipeline deploy 30 minutes ago.',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 0,
    diagnosis: 'Rollback the pipeline deploy — broken features',
    explanation: 'Predictions collapsing to a single constant value = feature pipeline is broken, likely returning null/zero features. Not a model problem — a data problem. Rollback the pipeline deploy. AWS in production: SageMaker Feature Store version-controlled feature groups let you roll back the pipeline to a previous ingestion snapshot while you debug the broken feature compute logic.',
    fix: 'Roll back the feature pipeline deploy to a previous snapshot; the model is fine, the features are broken.',
    source: 'MLOps Deploy · Rollback Decision (alert 4)',
  },
  {
    id: 'production-rollback-business-metric-down',
    subject: 'production', subtopic: 'rollback', level: 'senior', type: 'mcq',
    title: 'Business metric down 8%, model metrics unchanged',
    context: 'Business metric (conversion rate on ranked items) dropped 8% over the past week. Model metrics unchanged.',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 1,
    diagnosis: 'Investigate — model is technically correct',
    explanation: "Model metrics unchanged but business metric down = either (a) the feature the model optimizes doesn't correlate with conversion as expected, or (b) an external factor (seasonality, competitor). Rollback won't fix it if the model is technically correct. AWS in production: SageMaker Experiments track the A/B split between model variants alongside business metrics in one place — makes it easier to rule out model causality before chasing external explanations.",
    fix: 'Investigate the metric-business linkage and external factors; rollback would not help a technically-correct model.',
    source: 'MLOps Deploy · Rollback Decision (alert 5)',
  },
  {
    id: 'production-rollback-canary-2x-fpr',
    subject: 'production', subtopic: 'rollback', level: 'senior', type: 'mcq',
    title: 'Canary 5% showing 2x false positive rate',
    context: 'New model version deployed to canary 5% shows false positive rate 2x the champion on the 5% slice.',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 0,
    diagnosis: 'Rollback the canary — it caught it at 5%',
    explanation: 'This is what canary is for — caught it at 5%. The 2x false positive rate would cause real user pain if promoted. Rollback the canary, investigate the eval-set vs production-distribution mismatch. AWS in production: SageMaker Deployment Guardrails canary mode with a false-positive-rate alarm would have triggered the automatic rollback before you needed a human decision — the automated form of what you just did manually.',
    fix: 'Roll back the canary and investigate the eval-set vs production distribution mismatch.',
    source: 'MLOps Deploy · Rollback Decision (alert 6)',
  },
  {
    id: 'production-rollback-training-failed-stale',
    subject: 'production', subtopic: 'rollback', level: 'mid', type: 'mcq',
    title: 'Training job failed — model 26 hours old',
    context: 'Model training job failed — no new model produced today. Current model is 26 hours old (daily retrain schedule).',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 2,
    diagnosis: 'Monitor staleness — nothing better to roll back to',
    explanation: "A single missed retrain rarely causes quality degradation. Monitor staleness. If model age > 48h and drift is detected, then escalate. Don't rollback — there's nothing to roll back to that's better. AWS in production: SageMaker Pipelines conditional step logic can skip promotion and alert when a training job fails, keeping the current production model live and paging on-call only after the staleness threshold is crossed.",
    fix: 'Monitor model staleness; escalate only if age > 48h with detected drift.',
    source: 'MLOps Deploy · Rollback Decision (alert 7)',
  },
  {
    id: 'production-rollback-gpu-memory-40',
    subject: 'production', subtopic: 'rollback', level: 'senior', type: 'mcq',
    title: 'GPU memory up 40% after deploy — no OOM yet',
    context: 'GPU memory usage on the serving cluster increased 40% after the latest deploy. No OOM errors yet but trending toward it.',
    question: 'What is the right move?',
    options: ['Rollback immediately', 'Investigate before deciding', 'Monitor only', 'No action needed'],
    answer: 1,
    diagnosis: 'Investigate — pre-failure signal, not yet a rollback trigger',
    explanation: "Not a rollback trigger yet (no errors) but it's a pre-failure signal. Profile: did the new model use more memory? Is it a memory leak? Act before OOM hits. AWS in production: SageMaker Inference Recommender — run a right-sizing job on the new model artifact to confirm its memory footprint before the next deploy, catching GPU memory bloat in pre-production rather than at 2am.",
    fix: 'Profile the memory increase (leak vs larger model) and act before OOM; not yet a rollback situation.',
    source: 'MLOps Deploy · Rollback Decision (alert 8)',
  },

  // ─── CI/CD Gate Design (from GATES configurator) ────────────────────────────
  {
    id: 'production-cicd-schema-metric-block',
    subject: 'production', subtopic: 'ci-cd', level: 'senior', type: 'mcq',
    title: 'Which CI/CD gates should reliably BLOCK an ML deploy?',
    context: [
      'Designing a CI/CD pipeline for an ML model deploy. Each gate can be include/exclude and block/warn.',
      'Gates: unit tests, schema validation, offline metric threshold, calibration check, performance regression, training-data freshness, feature drift, shadow comparison.',
    ],
    question: 'ML CI/CD differs from software CI/CD — you cannot exhaustively unit-test model quality. Which pair should reliably block a ship?',
    options: [
      'Calibration check and feature drift detection',
      'Training-data freshness and feature drift detection',
      'Schema mismatch and offline metric regression',
      'Every gate should block — no gate is merely informational',
    ],
    answer: 2,
    diagnosis: 'Only schema mismatch and metric regression should reliably block',
    explanation: "Schema mismatch = guaranteed production failure; offline metric regression = you're shipping a worse model. Both must block. Calibration, freshness, and drift are important signal but rarely block a deploy on their own — warn and document. Shadow mode is your integration test and canary ramp is your production smoke test; they catch what unit tests cannot. AWS in production: SageMaker Projects provides MLOps templates that wire these gates — schema validation, offline metrics, and shadow comparison — into a CodePipeline workflow with automatic approval steps.",
    fix: 'Block on schema validation and offline-metric regression; set calibration, freshness, and drift to warn-and-document.',
    source: 'MLOps Pipelines · CI/CD Gate Design',
  },
  {
    id: 'production-cicd-drift-warn-not-block',
    subject: 'production', subtopic: 'ci-cd', level: 'mid', type: 'mcq',
    title: 'Feature drift gate — block or warn?',
    context: 'A CI/CD gate reports PSI < 0.2 on key features vs last month. Reviewers are deciding whether a drift breach should block the deploy or merely warn.',
    question: 'What severity should feature drift detection carry in the gate?',
    options: [
      'Block — any drift is a guaranteed production failure',
      'Warn — drift is informational context for reviewers, not a hard ship gate',
      'Exclude the gate entirely — drift is unmeasurable pre-deploy',
      'Block, but only if PSI > 0.5',
    ],
    answer: 1,
    diagnosis: 'Warn — drift is signal, rarely a block',
    explanation: "Drift is informational context for reviewers. Warn, don't block. The only two things that should reliably block a ship are schema mismatch and metric regression; everything else is signal — worth knowing, rarely worth blocking. AWS in production: SageMaker Projects wires drift and metric gates into a CodePipeline workflow with approval steps so warnings surface to reviewers without halting the pipeline.",
    fix: 'Set feature-drift detection to warn-and-document so reviewers see the signal without blocking the ship.',
    source: 'MLOps Pipelines · CI/CD Gate Design',
  },

  // ─── Infrastructure Decision (from INFRA_OPTIONS configurator) ──────────────
  {
    id: 'production-infra-batch-scoring',
    subject: 'production', subtopic: 'infra', level: 'mid', type: 'mcq',
    title: 'Serving infra — batch request pattern, freshness not required',
    context: 'Request pattern: batch (minutes/hours). Predictions do not need to be fresh at request time. Scale is large.',
    question: 'Which serving infrastructure fits best?',
    options: [
      'REST API + single instance (FastAPI/Flask)',
      'Batch inference (Spark / Databricks)',
      'Triton Inference Server',
      'Serverless (Lambda + SageMaker)',
    ],
    answer: 1,
    diagnosis: 'Batch inference on Spark/Databricks',
    explanation: 'Batch inference handles massive scale and scores that do not need request-time freshness. It is useless if predictions need to be fresh at request time — but here they do not. REST/single-instance breaks at >1k RPM; Triton is GPU-optimised overkill for this; serverless hits execution-time limits on large batch jobs.',
    fix: 'Use batch inference (Spark/Databricks) since predictions do not need request-time freshness.',
    source: 'MLOps Pipelines · Infrastructure Decision',
  },
  {
    id: 'production-infra-llm-large-model',
    subject: 'production', subtopic: 'infra', level: 'senior', type: 'mcq',
    title: 'Serving infra — large LLM (>2GB), real-time generation',
    context: 'Model size: large (>2GB / LLM). Request pattern: real-time text generation. High scale.',
    question: 'Which serving infrastructure is purpose-built for this?',
    options: [
      'REST API + single instance (FastAPI/Flask)',
      'Batch inference (Spark / Databricks)',
      'vLLM / TGI (Text Generation Inference)',
      'Ray Serve / BentoML',
    ],
    answer: 2,
    diagnosis: 'vLLM / TGI — purpose-built for LLMs',
    explanation: 'vLLM/TGI provide continuous batching and KV-cache management, purpose-built for LLMs. They are GPU-only and complex to deploy, and not suitable for non-LLM models. A single REST instance cannot scale, batch inference kills real-time, and Ray Serve is Python-native but not LLM-specialised the way vLLM/TGI are.',
    fix: 'Serve large LLMs with vLLM/TGI for continuous batching and KV-cache management.',
    source: 'MLOps Pipelines · Infrastructure Decision',
  },
  {
    id: 'production-infra-early-stage-small',
    subject: 'production', subtopic: 'infra', level: 'junior', type: 'mcq',
    title: 'Serving infra — early-stage team, small model, low scale',
    context: 'Team infra maturity: early stage (no k8s). Model size: small (<100MB). Scale: low (<1k RPM). Real-time request pattern.',
    question: 'Which serving infrastructure ships fastest with the least overhead?',
    options: [
      'Containerized REST + horizontal scaling (Docker + k8s)',
      'REST API + single instance (FastAPI/Flask)',
      'Triton Inference Server',
      'vLLM / TGI',
    ],
    answer: 1,
    diagnosis: 'REST API + single instance',
    explanation: 'A single REST instance is fastest to ship with zero infra overhead — the right call for an early-stage team with a small model at low scale. It breaks at >1k RPM or if the instance goes down, so revisit as scale grows. k8s needs a platform team, and Triton/vLLM are overkill for a small model.',
    fix: 'Ship a single-instance REST API now; migrate to containerized horizontal scaling as scale approaches ~1k RPM.',
    source: 'MLOps Pipelines · Infrastructure Decision',
  },

  // ─── Registry Patterns (from REGISTRY_SCENARIOS) ────────────────────────────
  {
    id: 'production-registry-gates-passed-promote',
    subject: 'production', subtopic: 'registry', level: 'mid', type: 'mcq',
    title: 'All gates passed, canary metrics nominal',
    context: 'Challenger model passed all offline evals and 2-week shadow. Canary at 5% for 3 days shows metrics matching champion.',
    question: 'What registry action do you take?',
    options: ['Register as staging', 'Promote to production', 'Rollback to previous', 'Archive', 'Flag for review', 'Delete'],
    answer: 1,
    diagnosis: 'Promote to production',
    explanation: 'All gates passed. Canary data confirms the offline eval held up. This is the normal promotion path. AWS in production: SageMaker Model Registry transitions the model version status from Staging → Production via a SageMaker Pipelines approval step — the registry records who approved, when, and which eval metrics passed.',
    fix: 'Promote to production via a registry approval step that records the approver and passing metrics.',
    source: 'MLOps Pipelines · Registry Patterns (scenario 1)',
  },
  {
    id: 'production-registry-stale-staging-archive',
    subject: 'production', subtopic: 'registry', level: 'mid', type: 'mcq',
    title: 'Stale staging model, 6 months old, never deployed',
    context: "A model trained 6 months ago is still registered as 'staging'. It was never deployed — a better version was trained instead.",
    question: 'What registry action do you take?',
    options: ['Register as staging', 'Promote to production', 'Rollback to previous', 'Archive', 'Flag for review', 'Delete'],
    answer: 3,
    diagnosis: 'Archive — valid but superseded',
    explanation: "Don't delete (you might want the weights for comparison or rollback reference). Archive indicates 'valid but superseded.' Keeps the registry clean without losing history. AWS in production: SageMaker Model Registry supports Archived status natively — pair with an S3 lifecycle policy to move the artifact to Glacier after 90 days if storage cost matters.",
    fix: "Archive it — keeps history and weights available without cluttering the active registry.",
    source: 'MLOps Pipelines · Registry Patterns (scenario 2)',
  },
  {
    id: 'production-registry-data-leakage-rollback',
    subject: 'production', subtopic: 'registry', level: 'senior', type: 'mcq',
    title: 'Data leakage found in training set — model is live',
    context: "The production model's training data was found to contain test-set labels (data leakage). The model is currently serving live traffic.",
    question: 'What registry action do you take?',
    options: ['Register as staging', 'Promote to production', 'Rollback to previous', 'Archive', 'Flag for review', 'Delete'],
    answer: 2,
    diagnosis: 'Rollback immediately — results are invalid',
    explanation: 'Leakage = model results are invalid. Roll back immediately, then audit: how long was the leaky model live? What decisions did it influence? Flag for incident review. AWS in production: SageMaker Pipelines maintains a full lineage graph from raw S3 training data through to deployed endpoint — use the lineage API to pinpoint when the leaky data first entered training and which model versions are affected.',
    fix: 'Roll back to the previous version, then use lineage to audit affected versions and the leak window.',
    source: 'MLOps Pipelines · Registry Patterns (scenario 3)',
  },
  {
    id: 'production-registry-schema-mismatch-stage',
    subject: 'production', subtopic: 'registry', level: 'senior', type: 'mcq',
    title: 'Schema mismatch caught in CI — feature renamed',
    context: "A new model version failed schema validation in CI — the feature 'user_country' was renamed to 'country_code' in the feature pipeline.",
    question: 'What registry action do you take?',
    options: ['Register as staging', 'Promote to production', 'Rollback to previous', 'Archive', 'Flag for review', 'Delete'],
    answer: 0,
    diagnosis: 'Keep as staging — do not promote',
    explanation: "Don't promote. The schema mismatch would cause production errors. Fix the schema alignment, re-run CI, then promote if it passes. AWS in production: SageMaker Feature Store feature-group schema is versioned and immutable — a rename requires creating a new feature group, making the breaking change explicit and blocking accidental promotion through CI.",
    fix: 'Hold it in staging, align the schema, re-run CI, then promote only if it passes.',
    source: 'MLOps Pipelines · Registry Patterns (scenario 4)',
  },
  {
    id: 'production-registry-2yr-old-40gb-archive',
    subject: 'production', subtopic: 'registry', level: 'mid', type: 'mcq',
    title: '2-year-old model consuming 40GB in registry',
    context: 'A model version from 2 years ago is taking up 40GB in the registry. It has been superseded by 12 newer versions.',
    question: 'What registry action do you take?',
    options: ['Register as staging', 'Promote to production', 'Rollback to previous', 'Archive', 'Flag for review', 'Delete'],
    answer: 3,
    diagnosis: 'Archive (delete only if retention policy allows)',
    explanation: 'If retention policy says keep 6 months, delete it. If no formal policy, archive. Never delete a model that was in production without confirming audit requirements are met. AWS in production: SageMaker Model Registry combined with S3 Intelligent-Tiering shifts archived artifacts to lower-cost storage tiers — set a lifecycle rule to expire artifacts beyond your compliance retention window.',
    fix: 'Archive by default; delete only after confirming the retention/audit policy permits it.',
    source: 'MLOps Pipelines · Registry Patterns (scenario 5)',
  },
  {
    id: 'production-registry-fairness-flag',
    subject: 'production', subtopic: 'registry', level: 'staff', type: 'mcq',
    title: 'Better engagement, worse fairness metric',
    context: 'An A/B test shows the challenger is better on engagement but worse on a fairness metric (higher false positive rate on a minority subgroup).',
    question: 'What registry action do you take?',
    options: ['Register as staging', 'Promote to production', 'Rollback to previous', 'Archive', 'Flag for review', 'Delete'],
    answer: 4,
    diagnosis: 'Flag for review — not a pure technical call',
    explanation: "This is not a pure technical decision — it requires product/legal/policy review. Don't promote, don't rollback the canary, but don't just reject either. Escalate with the data. AWS in production: SageMaker Clarify generates a bias report per subgroup for both pre-training data and post-training predictions — attach the Clarify report as a registry artifact so the product/legal review has structured evidence to evaluate.",
    fix: 'Flag for product/legal review and attach a per-subgroup bias report as structured evidence.',
    source: 'MLOps Pipelines · Registry Patterns (scenario 6)',
  },

  // ─── Model Registry (from MODEL_REGISTRY_SCENARIOS, AccordionMCQ) ────────────
  {
    id: 'production-model-registry-semver-hash',
    subject: 'production', subtopic: 'registry', level: 'senior', type: 'mcq',
    title: 'Semantic versioning breaks under continuous training',
    context: 'Your team uses semantic versioning (1.0.0, 1.1.0, 2.0.0) for model artifacts in S3. Models are retrained weekly. After 6 months you have 26 versions. A downstream team pinned their serving config to "model >= 1.x" and accidentally picked up a model with a breaking input schema change released as v1.9.0 instead of v2.0.0.',
    question: 'What versioning strategy prevents this class of failure?',
    options: [
      'Use stricter semantic versioning discipline — major version for any schema change.',
      'Hash-based versioning (content-addressed by model artifact hash) with an explicit schema contract file checked into the registry alongside every artifact.',
      'Require all downstream consumers to pin exact versions, not ranges.',
      'Add a model changelog document that engineers read before upgrading.',
    ],
    answer: 1,
    diagnosis: 'Semantic versioning requires human judgment to correctly classify breaking vs. non-breaking changes. Under weekly retraining with multiple engineers, version bumping discipline degrades. Hash-based versioning removes the judgment call: the hash is the version. Schema contracts checked into the registry make breaking changes detectable programmatically.',
    explanation: 'AWS in production: SageMaker Model Registry stores model metadata including custom schema contracts alongside each artifact version — attach schema.json as a registry property and enforce the compatibility check in a SageMaker Pipelines approval step.',
    fix: 'Move to content-addressed artifact versioning: `sha256(model_artifact)` as the canonical version ID. Register a `schema.json` with every artifact (input feature names + dtypes, output schema). Add a compatibility check in the promotion gate: if `schema.json` differs from the currently deployed version, block auto-promotion and require explicit sign-off.',
    source: 'MLOps Pipelines · Model Registry Patterns (mreg1)',
  },
  {
    id: 'production-model-registry-shadow-traffic',
    subject: 'production', subtopic: 'registry', level: 'senior', type: 'mcq',
    title: 'Shadow mode promotion gate fails silently',
    context: 'Your MLOps platform runs new models in shadow mode for 48 hours before promoting to canary. Shadow mode logs predictions but does not serve them to users. After promotion, a model that performed well in shadow mode shows 12% higher error rate in canary. Investigation reveals shadow traffic was sampled from cached requests, not live traffic.',
    question: 'What was the root failure in the shadow mode gate?',
    options: [
      'Shadow mode should have been run for 2 weeks, not 48 hours — insufficient time to capture weekly traffic cycles.',
      'The canary rollout percentage was too high — 5% canary is the standard maximum for initial promotion.',
      'Shadow traffic did not represent the live distribution — cached requests have different feature distributions than real-time requests. The gate passed a model that was never tested on actual live traffic.',
      'The shadow mode comparison metric (error rate) should have been AUC-PR, not mean absolute error.',
    ],
    answer: 2,
    diagnosis: 'Shadow mode is only as valid as its traffic sample. Cached requests have higher hit rates, different session lengths, and different feature value distributions than real-time traffic. A model that performs well on cached traffic may degrade on real-time traffic with cold-start features, fresh user state, or recent inventory changes.',
    explanation: 'AWS in production: SageMaker Shadow Testing intercepts live endpoint invocations and mirrors them to the shadow variant in real time — eliminates the cached-replay problem because traffic is sourced directly from the production request path.',
    fix: 'Verify shadow traffic provenance before trusting gate results. Live shadow mode must intercept actual inference requests, not replay cached ones. Add a traffic distribution check to the gate: compare feature value distributions between shadow traffic and the last 7 days of production traffic. Flag divergence > 10% PSI as a gate failure.',
    source: 'MLOps Pipelines · Model Registry Patterns (mreg2)',
  },
  {
    id: 'production-model-registry-rollback-vs-retrain',
    subject: 'production', subtopic: 'registry', level: 'staff', type: 'mcq',
    title: 'Rollback trigger: when to rollback vs. retrain',
    context: 'Your production recommendation model shows a sudden AUC drop from 0.81 to 0.74 at 2am. Monitoring also shows PSI = 0.31 on the `user_age` feature (upstream data pipeline changed bucketing logic). P95 serving latency is unchanged.',
    question: 'What is the correct immediate response?',
    options: [
      'Rollback to the previous model version immediately.',
      'Retrain the model on recent data and promote to production.',
      'The AUC drop is caused by the upstream feature distribution shift, not model degradation. Fix the upstream pipeline first. Rollback would restore the model but not fix the feature — the rollback model would also degrade on the same corrupted feature.',
      'Increase canary traffic to the previous model version while investigating.',
    ],
    answer: 2,
    diagnosis: 'Rollback addresses model failures, not data failures. When PSI on an input feature exceeds 0.25, the feature distribution has fundamentally shifted — both the current and previous model will produce degraded outputs on the same bad feature. Rollback without fixing the upstream pipeline is a no-op.',
    explanation: 'AWS in production: SageMaker Model Monitor data quality jobs compute PSI on incoming feature distributions continuously — the PSI > 0.25 alarm is your signal to page data engineering rather than triggering a model rollback.',
    fix: 'Classify the failure before responding: if monitoring shows feature PSI > 0.25 alongside metric degradation, the root cause is upstream, not the model. Escalate to the data engineering team for pipeline fix. If the fix takes >2 hours, consider temporarily routing traffic to a model that does not depend on the degraded feature. Reserve model rollback for cases where the model artifact itself is the failure source.',
    source: 'MLOps Pipelines · Model Registry Patterns (mreg3)',
  },

  // ─── Schema Cascade (from SCHEMA_CASCADE_SCENARIOS, AccordionMCQ) ────────────
  {
    id: 'production-schema-column-rename-silent',
    subject: 'production', subtopic: 'schema', level: 'senior', type: 'mcq',
    title: 'Upstream column rename breaks four models silently',
    context: 'A data engineering team renames `user_tenure_days` to `account_age_days` in the feature store as part of a schema cleanup. The change passes CI because no tests check column names against downstream consumers. Four production models that use `user_tenure_days` as a top-5 feature continue serving — they silently receive NaN for the missing column and fall back to imputed mean values.',
    question: 'Why did model monitoring not catch this immediately?',
    options: [
      'The models were retrained after the rename and learned to ignore the missing column via feature selection.',
      'NaN imputation with mean values keeps the feature distribution superficially stable — PSI stays low because the imputed mean resembles historical distribution. Prediction quality degrades without triggering a distribution alert.',
      'The monitoring system was configured to track prediction drift, not feature-level null rates.',
      'PSI monitoring only detects shifts in non-null values — silent NaN injection requires a dedicated null-rate monitor.',
    ],
    answer: 1,
    diagnosis: 'Mean imputation is the silent killer: replacing a missing column with its historical mean produces a feature value that looks statistically valid. PSI compares the new distribution to the reference — if the reference also included some mean-imputed values, the shift is masked. Model performance degrades because the signal is gone, but the distribution appears stable.',
    explanation: 'AWS in production: SageMaker Feature Store enforces schema on ingestion and tracks which feature groups each model consumed — a consumer scan before merging a column rename surfaces every downstream model that will silently receive nulls.',
    fix: 'Add null-rate monitoring as a first-class signal alongside PSI. A null rate jump from 0.2% to 100% for any feature should trigger an immediate alert, regardless of imputation strategy. In CI/CD, add a schema compatibility check: before merging any feature store schema change, run a consumer scan that lists all models depending on the affected columns.',
    source: 'MLOps Pipelines · Schema Cascade (sc1)',
  },
  {
    id: 'production-schema-dbt-rename-cascade',
    subject: 'production', subtopic: 'schema', level: 'senior', type: 'mcq',
    title: 'dbt model rename cascades to serving pipeline',
    context: [
      'A dbt model `fct_user_events` is renamed to `fct_events_v2` and the old model is deprecated. The dbt CI passes.',
      'Three days later, the real-time feature computation service — which reads from the dbt output table directly via a Spark job — begins returning stale features. The Spark job silently fails to find the renamed table and falls back to a cached snapshot from 3 days prior.',
    ],
    question: 'What gating mechanism would have prevented this?',
    options: [
      'The dbt model should have maintained a backward-compatible view under the old name during a deprecation window.',
      'A cross-system lineage check in the dbt CI pipeline: before deprecating any model, verify no downstream consumers (Spark jobs, ML pipelines, dashboards) reference the old table name. Block merge if live consumers exist.',
      'The Spark job should have used a table alias registered in a central catalog, not a hardcoded table name.',
      'Feature store caching TTL should be set to zero to prevent stale fallback hiding the failure.',
    ],
    answer: 1,
    diagnosis: 'dbt CI only validates the dbt DAG. It has no visibility into Spark jobs, ML serving pipelines, or dashboards that read from dbt output tables directly. A rename that passes dbt CI can still silently break every downstream consumer that was not part of the dbt graph.',
    explanation: 'AWS in production: AWS Lake Formation combined with AWS Glue Data Catalog tracks table-level lineage across Glue jobs, Spark, and Athena — use the catalog to enumerate all consumers of a table before approving a rename in dbt.',
    fix: 'Build cross-system lineage: maintain a registry of all consumers of each dbt model (Spark jobs, ML features, dashboards). Integrate this registry into dbt CI — any rename or deprecation triggers a consumer scan. Block merge until all consumers are either updated or explicitly acknowledged. Tools like DataHub, OpenLineage, or a simple internal YAML consumer manifest work for this.',
    source: 'MLOps Pipelines · Schema Cascade (sc2)',
  },
  {
    id: 'production-schema-dtype-score-inflation',
    subject: 'production', subtopic: 'schema', level: 'staff', type: 'mcq',
    title: 'Feature dtype change causes silent score inflation',
    context: 'A feature pipeline changes `purchase_count_30d` from `int32` to `float32` as part of a standardisation pass. The change is backward-compatible in Python. Model serving normalises all features by dividing by their historical max. The historical max for `purchase_count_30d` was computed as integer 847 — stored as int in the normalisation config. After the dtype change, floating-point division produces slightly different normalised values due to precision differences.',
    question: 'What is the most production-dangerous aspect of this failure?',
    options: [
      'Float features use more memory than int features.',
      'The score change is small enough to stay within monitoring thresholds — no alert fires. But the model was trained on int-normalised values, so the serving distribution has silently diverged from the training distribution for every request.',
      'The normalisation config should be recomputed after every dtype change.',
      'Int32 to float32 is a breaking schema change that should require a major version bump.',
    ],
    answer: 1,
    diagnosis: 'Training-serving skew without an alert: the dtype change introduces a tiny, consistent numerical difference in every normalised feature value. The PSI on the normalised feature stays near zero (the distribution shape is identical, just shifted by a small constant). But the model was never trained on float-normalised values — the skew is real and permanent.',
    explanation: 'AWS in production: SageMaker Model Monitor model quality monitoring jobs can detect training-serving skew by comparing live prediction distributions against a baseline captured at training time — catches the silent normalisation drift before it affects business metrics.',
    fix: 'Add a training-serving consistency check to the serving pipeline: periodically sample a batch of serving requests, run the same features through the training-time normalisation pipeline, and compare outputs. Any consistent difference > 1e-4 in feature value should trigger a review. Lock normalisation configs to the training pipeline version and require recomputation — not just dtype-safe migration — when any input schema changes.',
    source: 'MLOps Pipelines · Schema Cascade (sc3)',
  },
]

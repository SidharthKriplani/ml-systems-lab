// recsys.js — hand-authored recommender-systems judgment drills.
// Flagship subject. Written to how RecSys is actually interrogated in industry
// interviews (Meta / YouTube / Netflix / Spotify / TikTok style), not trivia.
//
// Coverage spread across subtopics — retrieval, candidate-generation, ranking,
// multi-objective/value-models, cold-start, evaluation, embeddings/ANN ops,
// re-ranking/diversity, feedback-loops/popularity — and across all four levels,
// with a deliberately heavy junior + mid on-ramp.
//
// Does NOT duplicate the system_design recsys drills (embedding drift, HNSW
// staleness, re-ranker distribution shift, design-review cold-start, silent
// stale-embedding incident) — those live in systemDesign.js.

export const RECSYS_DRILLS = [
  // ── retrieval / candidate-generation ───────────────────────────
  {
    id: 'recsys-two-stage-why', subject: 'recsys', subtopic: 'candidate-generation', level: 'junior', type: 'mcq',
    title: 'Why a recommender has two stages at all',
    context: ['Home feed: 8M item catalogue, must return 20 items in under 120ms.', 'A junior asks why the team does not just run the ranking model over all 8M items.'],
    question: 'What is the reason for the retrieve-then-rank split?',
    options: [
      'Two models look more impressive in a design doc',
      'Running the heavy ranker over 8M items per request blows the latency budget; a cheap retrieval stage narrows 8M → a few hundred, then the expensive ranker scores only those',
      'Retrieval and ranking are the same model run twice for redundancy',
      'The ranker cannot output more than 20 scores at once',
    ],
    answer: 1,
    diagnosis: 'Latency forces a recall-then-precision funnel',
    explanation: 'A cross-feature ranker costs maybe 1–5ms per item. Over 8M items that is thousands of seconds — impossible per request. Retrieval is a cheap, high-recall filter (embedding lookup + ANN) that cuts millions to hundreds; the ranker then spends its budget on the survivors.',
    fix: 'Stage 1 retrieval maximises recall over the full catalogue cheaply; stage 2 ranking maximises precision over a few hundred. Right-size each stage to its job and its latency slice.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-two-tower-shape', subject: 'recsys', subtopic: 'retrieval', level: 'junior', type: 'mcq',
    title: 'Why the two towers never touch until the very end',
    context: ['A two-tower retrieval model has a user tower and an item tower.', 'A junior proposes concatenating user and item features early and passing them through one shared MLP "so they can interact more".'],
    question: 'Why must the towers stay separate until a final dot product?',
    options: [
      'It is a stylistic convention with no real effect',
      'Item embeddings must be precomputed and indexed offline; if user and item features mix early, every item score depends on the live user and nothing can be precomputed or served by ANN',
      'Separate towers train faster on GPUs',
      'Concatenation would make the embeddings too large to store',
    ],
    answer: 1,
    diagnosis: 'Late interaction is what makes retrieval indexable',
    explanation: 'The whole point of two towers is that item embeddings are query-independent: compute all 8M once, put them in an ANN index, and at request time embed only the user and do a nearest-neighbour lookup. An early-fusion model would have to re-score every item per user — that is a ranker, not a retriever.',
    fix: 'Keep towers independent with a dot-product (or cosine) at the top so item vectors precompute into the ANN index. Save rich cross-feature interaction for the ranking stage, where the candidate set is small.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-inbatch-negatives', subject: 'recsys', subtopic: 'retrieval', level: 'mid', type: 'mcq',
    title: 'In-batch negatives make popular items look bad',
    context: ['Two-tower model trained with in-batch negatives: each user\'s positive item is the label, every other item in the batch is a negative.', 'After launch, popular items are under-retrieved relative to their true engagement.'],
    question: 'What is the mechanism, and the standard correction?',
    options: [
      'The learning rate is too high — lower it',
      'Popular items appear as in-batch negatives far more often, so the model is penalised for scoring them highly; correct with a logQ (sampled-softmax) popularity correction to the logits',
      'The item tower is too small — add layers',
      'In-batch negatives are always wrong — switch to purely random negatives',
    ],
    answer: 1,
    diagnosis: 'Sampling bias: frequent items become frequent negatives',
    explanation: 'With in-batch negatives, an item\'s chance of being a negative is proportional to its popularity. The model learns to suppress popular items to avoid the penalty, skewing retrieval. The sampled-softmax / logQ correction subtracts log(sampling probability) from the logit to undo the frequency bias.',
    fix: 'Apply the logQ correction (subtract log of the estimated sampling probability) so popular items are not over-penalised. Optionally mix in a stream of random/uniform negatives to cover the tail the batch never contains.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-recall-ceiling', subject: 'recsys', subtopic: 'retrieval', level: 'mid', type: 'mcq',
    title: 'Ranker is great offline, feed still feels narrow',
    context: ['Offline the ranker scores 0.82 NDCG on the candidate sets it is fed.', 'Users still complain the feed is repetitive and misses obvious relevant items.', 'Retrieval returns 400 candidates; recall@400 against a labelled relevant set is 0.55.'],
    question: 'Where is quality actually being lost?',
    options: [
      'The ranker — retrain it with more features',
      'Retrieval — if the relevant item is not in the 400 candidates, no ranker can surface it; recall@400 of 0.55 caps final quality regardless of ranker skill',
      'The UI — increase the number of rows shown',
      'The loss function — switch NDCG to MRR',
    ],
    answer: 1,
    diagnosis: 'Retrieval recall is the hard ceiling on the whole system',
    explanation: 'Ranking can only reorder what retrieval hands it. A recall@400 of 0.55 means 45% of relevant items never reach the ranker — the ranker\'s 0.82 NDCG is measured on an already-truncated set. A brilliant ranker on a weak candidate pool still ships a narrow feed.',
    fix: 'Audit and lift retrieval recall first: add complementary retrieval sources (a second tower, a graph/co-visitation retriever, a popularity + recency source), widen K, then re-measure. Only then tune the ranker.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-multi-source-retrieval', subject: 'recsys', subtopic: 'candidate-generation', level: 'senior', type: 'mcq',
    title: 'One tower cannot cover every intent',
    context: ['A single two-tower retriever powers the whole feed.', 'It is strong on "more like your recent watches" but weak on fresh content, on niche interests, and on re-engagement after a lapse.'],
    question: 'What is the standard senior-level fix to broaden candidate generation?',
    options: [
      'Make the single tower much larger and train it longer',
      'Run multiple retrieval sources in parallel (behavioural two-tower, content/embedding for freshness, co-visitation/graph, popularity+recency) and merge their candidates before ranking',
      'Increase K on the one tower to 5,000',
      'Replace the tower with a large language model',
    ],
    answer: 1,
    diagnosis: 'Candidate generation should be a blend of sources, not one model',
    explanation: 'Each retrieval source has an intent blind spot. One behavioural tower over-indexes on recent behaviour and starves fresh/niche/lapsed content. Production feeds run a handful of specialised retrievers in parallel and union their outputs, so the ranker sees a diverse, higher-recall pool.',
    fix: 'Stand up several retrieval sources with complementary strengths, union+dedupe their candidates, and monitor per-source contribution to final impressions so a silently-dead source is caught early.',
    source: 'Authored · RecSys',
  },

  // ── ranking (pointwise / pairwise / listwise, position bias) ───
  {
    id: 'recsys-pointwise-vs-pairwise', subject: 'recsys', subtopic: 'ranking', level: 'junior', type: 'mcq',
    title: 'What "learning to rank" changes vs a plain classifier',
    context: ['A junior trains a pointwise click model (binary click / no-click, log-loss) and is surprised interviewers ask about pairwise and listwise objectives.'],
    question: 'What do pairwise/listwise losses optimise that a pointwise classifier does not?',
    options: [
      'They predict a better-calibrated absolute click probability',
      'They optimise the relative order of items in a list — pairwise learns "A should rank above B", listwise optimises a whole-list metric — which is what the user actually experiences',
      'They train faster than pointwise',
      'They remove the need for labels',
    ],
    answer: 1,
    diagnosis: 'Ranking cares about order, not absolute probability',
    explanation: 'A user sees an ordered list; only relative order matters for what gets seen and clicked. Pointwise optimises each item\'s probability in isolation and can get the ordering wrong even with good per-item AUC. Pairwise (RankNet-style) optimises correct A-over-B orderings; listwise (e.g. LambdaMART/ListNet) optimises a list metric like NDCG directly.',
    fix: 'Use pointwise when you need a calibrated probability (e.g. for a value model). Use pairwise/listwise when the deliverable is an ordering — and evaluate with a ranking metric (NDCG/MRR), not accuracy.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-lambdamart-what', subject: 'recsys', subtopic: 'ranking', level: 'mid', type: 'mcq',
    title: 'Why LambdaMART "weights" pairs by NDCG change',
    context: ['A team uses LambdaMART for ranking.', 'A mid engineer knows it is gradient-boosted trees on pairwise gradients but cannot articulate the "lambda" part.'],
    question: 'What is the lambda in LambdaMART doing?',
    options: [
      'It is the learning rate of the boosting',
      'Each misordered pair\'s gradient is scaled by the |ΔNDCG| that swapping them would cause — so fixing pairs near the top of the list (which move the metric most) is prioritised',
      'It is an L2 regulariser on the tree weights',
      'It converts the trees into a neural network',
    ],
    answer: 1,
    diagnosis: 'Lambda ties the pairwise gradient to the list metric',
    explanation: 'Pure pairwise loss treats every misordered pair equally, but a swap at ranks 1–2 changes NDCG far more than a swap at ranks 40–41. LambdaMART multiplies each pair\'s gradient by the |ΔNDCG| from swapping it, so the model spends capacity where the metric actually moves — the top of the list.',
    fix: 'Reach for LambdaMART when you have strong tabular ranking features and NDCG is the target; it directly bakes the metric into the gradient. Keep an eye on the gain (relevance) and discount definitions used to compute ΔNDCG.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-position-bias-ipw', subject: 'recsys', subtopic: 'ranking', level: 'senior', type: 'mcq',
    title: 'The ranker keeps re-learning that position 1 is best',
    context: ['Ranking model trained on logged clicks.', 'Position 1 gets far more clicks than position 5 partly because it is seen first, not because it is more relevant.', 'Each retrain, the model leans harder on whatever it ranked first last time.'],
    question: 'What is the failure and the principled correction?',
    options: [
      'Nothing wrong — high positions really are more relevant',
      'Position bias: clicks confound relevance with exposure, and the feedback loop compounds it; correct by modelling P(examine|position) and training with inverse-propensity weighting (IPW) or a position/examination term the model can factor out',
      'Add more trees so the model can memorise positions',
      'Shuffle the final list randomly on every request',
    ],
    answer: 1,
    diagnosis: 'Clicks confound relevance with position exposure',
    explanation: 'A click at position 1 is P(examine|pos1) × P(relevant). Training on raw clicks teaches "top position = good", and because tomorrow\'s training data is today\'s rankings, the bias self-reinforces. IPW reweights each click by 1/P(examine|position); a two-tower "examination × relevance" model (position-as-feature at train, dropped at serve) factors exposure out.',
    fix: 'Estimate position propensities (result randomisation or an intervention-harvested EM estimate), then train with IPW or an examination model that isolates relevance. Serve with the position feature set to a constant so the model does not depend on it.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-listwise-eval-mismatch', subject: 'recsys', subtopic: 'ranking', level: 'mid', type: 'mcq',
    title: 'Great AUC, mediocre feed',
    context: ['Ranker: pointwise, trained and selected on click AUC (0.79).', 'NDCG@10 barely improved over the old model and the feed feels the same to users.'],
    question: 'What is the most likely mismatch?',
    options: [
      'AUC 0.79 is simply too low — train longer',
      'AUC measures global pairwise separability across all impressions, not the ordering quality within each user\'s list; a model can improve AUC while barely changing the top-of-list order that users see',
      'NDCG@10 is the wrong metric — use accuracy',
      'The feed is fine; users are unreliable',
    ],
    answer: 1,
    diagnosis: 'Optimising a global metric that is not the list metric',
    explanation: 'AUC pools pairs across every impression and rewards separating clicks from non-clicks anywhere. What the user experiences is the order within their own short list. You can move AUC by fixing easy cross-list pairs while the within-list top order — which drives NDCG — hardly changes.',
    fix: 'Select and early-stop on the metric you ship: per-query NDCG@K / MRR, not global AUC. If ordering is the product, move to a pairwise/listwise objective.',
    source: 'Authored · RecSys',
  },

  // ── multi-objective / value models ─────────────────────────────
  {
    id: 'recsys-ctr-only-clickbait', subject: 'recsys', subtopic: 'multi-objective', level: 'junior', type: 'mcq',
    title: 'Optimising pure CTR fills the feed with clickbait',
    context: ['A new ranker maximises predicted click-through rate only.', 'CTR is up 9%, but average watch time per session is down and week-4 retention is falling.'],
    question: 'What went wrong, and what is the standard fix?',
    options: [
      'Nothing — CTR up is a clear win, ship it',
      'CTR is a proxy: optimising it alone rewards clickbait that gets the click but not the satisfaction; combine multiple objectives (CTR + dwell/watch-time + explicit signals) into a value model that reflects real value',
      'Retention is unrelated to the ranker',
      'Lower the learning rate to reduce CTR',
    ],
    answer: 1,
    diagnosis: 'Goodhart: a proxy optimised alone corrupts the real goal',
    explanation: 'CTR measures the click, not what happens after. Optimised in isolation it selects thumbnails/titles that bait the click and disappoint after — clicks up, satisfaction and retention down. The fix is a value/utility model that blends the signals that actually track long-term value.',
    fix: 'Predict several heads (click, dwell/watch-time, like/share, hide/skip) and combine into a single utility score with tuned weights; validate against a north-star like retention, not CTR alone.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-value-model-weights', subject: 'recsys', subtopic: 'multi-objective', level: 'senior', type: 'mcq',
    title: 'Combining CTR, dwell and share into one score',
    context: ['Three ranking heads: pCTR, predicted dwell seconds, and pShare.', 'A mid engineer proposes final_score = pCTR + dwell + pShare and adds them directly.'],
    question: 'What is wrong with summing the heads raw, and how should they be combined?',
    options: [
      'Nothing — summing predictions is standard',
      'The heads live on wildly different scales/units (a probability, seconds, a probability) and different business value; combine as a weighted sum of calibrated heads, e.g. w1·pCTR + w2·(dwell) + w3·pShare, with weights tuned to a north-star and each head calibrated first',
      'You must multiply them, never add',
      'Use only the head with the highest AUC and drop the others',
    ],
    answer: 1,
    diagnosis: 'Multi-objective needs calibrated heads and value-tuned weights',
    explanation: 'pCTR∈[0,1], dwell is unbounded seconds, pShare∈[0,1] — summed raw, dwell dominates purely by scale, not importance. Each head must be calibrated (so its number means what it says), then combined with weights that encode relative business value, tuned via online experiments against the north-star.',
    fix: 'Calibrate each head, normalise to comparable ranges, then weight by business value learned from A/B sweeps (or a learned combiner). Re-tune weights as objectives shift; monitor each head\'s calibration separately.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-head-calibration', subject: 'recsys', subtopic: 'multi-objective', level: 'senior', type: 'mcq',
    title: 'The share head quietly stopped mattering',
    context: ['Value model = w_ctr·pCTR + w_dwell·dwell + w_share·pShare with fixed weights.', 'The pShare head has become overconfident — its outputs cluster near 0.9 for most items after a retrain.', 'Feed diversity of shareable content dropped even though w_share is unchanged.'],
    question: 'Why did a mis-calibrated head change the blend even with fixed weights?',
    options: [
      'Fixed weights guarantee the blend cannot change',
      'A head that collapses toward a narrow high range loses its ability to discriminate between items, so its contribution to the ranking flattens — calibration of each head, not just the weights, governs its real influence',
      'The dwell head must have broken instead',
      'w_share must have been secretly set to zero',
    ],
    answer: 1,
    diagnosis: 'Miscalibration silently reweights a multi-objective blend',
    explanation: 'A weight only matters if the head varies across items. If pShare saturates near 0.9 for everything, w_share·pShare is nearly constant and drops out of the ordering — the model effectively stops using share, no weight change required. Per-head calibration is what keeps each objective\'s influence honest.',
    fix: 'Monitor each head\'s calibration (reliability curve, output spread) as a standing metric; recalibrate (Platt/isotonic) after retrains. Alert when a head\'s output variance collapses — it is silently leaving the blend.',
    source: 'Authored · RecSys',
  },

  // ── cold-start ─────────────────────────────────────────────────
  {
    id: 'recsys-coldstart-newuser', subject: 'recsys', subtopic: 'cold-start', level: 'junior', type: 'mcq',
    title: 'A brand-new user with zero history',
    context: ['A collaborative-filtering recommender relies on a user\'s past interactions.', 'A user signs up 10 seconds ago and has clicked nothing.'],
    question: 'What is the sound way to recommend to this user, versus the naive one?',
    options: [
      'Return an empty feed until they interact',
      'Bridge with what you do know — popularity/trending as a floor, plus any signup context (declared interests, geo, device, referrer) — then switch to personalised signal as interactions arrive',
      'Assign them a random existing user\'s history',
      'Wait 24 hours before showing anything',
    ],
    answer: 1,
    diagnosis: 'Cold-start is bridged with content/context, not left empty',
    explanation: 'With no interaction vector, collaborative filtering has nothing to latch onto. But you are rarely at zero information: trending items give a reasonable floor, and signup context (stated interests, location, device, referral source) is content the item side can match against. Personalisation ramps in as behaviour accrues.',
    fix: 'Serve a popularity/trending baseline blended with content-based matching on any available context; monitor the new-user cohort separately and validate that the hand-off to personalised signal improves their metrics.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-coldstart-newitem', subject: 'recsys', subtopic: 'cold-start', level: 'mid', type: 'mcq',
    title: 'New items never get their first impression',
    context: ['A pure collaborative retriever ranks items by learned interaction embeddings.', 'A freshly uploaded item has no interactions, so its embedding is untrained and it is never retrieved — so it never gets interactions.'],
    question: 'What breaks the chicken-and-egg loop for new items?',
    options: [
      'Wait — good items will eventually surface on their own',
      'Give new items a content-based embedding from metadata (text, category, creator, media features) so they are retrievable on day zero, and add an exploration budget so some impressions go to under-served items to gather signal',
      'Boost every new item to position 1 permanently',
      'Delete items that get no clicks in the first hour',
    ],
    answer: 1,
    diagnosis: 'Content bridge + exploration escape the cold-start loop',
    explanation: 'A collaborative-only embedding for a zero-interaction item is noise, so it is never shown, so it never earns interactions — a self-locking loop. A content-based embedding makes it retrievable from metadata immediately, and a small exploration budget guarantees enough impressions to collect the behavioural signal that eventually trains a real embedding.',
    fix: 'Compute content embeddings from item metadata for retrieval on day zero; reserve an exploration slice (e.g. epsilon-greedy or Thompson sampling on the value model) for under-explored items; monitor time-to-first-100-impressions for new items.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-coldstart-explore-cost', subject: 'recsys', subtopic: 'cold-start', level: 'senior', type: 'mcq',
    title: 'How much of the feed can exploration safely take',
    context: ['To warm up new items, a team floods 30% of every user\'s feed with unexplored content.', 'Engagement drops noticeably; the exploration is too aggressive.'],
    question: 'What is the disciplined way to run exploration here?',
    options: [
      'Explore on 0% — never show unproven items',
      'Frame it as explore/exploit: cap the exploration budget small (single-digit %), target it (new/under-served items, users tolerant of variety), and use an uncertainty-aware policy (Thompson sampling / UCB) so exploration concentrates where information gain is highest',
      'Keep 30% — engagement loss is the cost of freshness',
      'Explore only on power users who click the most',
    ],
    answer: 1,
    diagnosis: 'Exploration is a budgeted explore/exploit trade-off',
    explanation: 'Uniform 30% random exploration pays a large engagement tax for information you do not uniformly need. The value of exploring an item scales with model uncertainty about it. A small, uncertainty-targeted budget (Thompson/UCB) buys most of the learning at a fraction of the cost, and should be aimed at items/users where it hurts least.',
    fix: 'Keep exploration to a small budget, drive it with an uncertainty-aware bandit policy, target under-explored items and variety-tolerant users, and track the exploration cost (engagement delta) against catalogue coverage gained.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-coldstart-platform', subject: 'recsys', subtopic: 'cold-start', level: 'staff', type: 'mcq',
    title: 'Launching recommendations in a brand-new country',
    context: ['A working recommender launches in a new market with almost no local interaction data and a different content catalogue.', 'A staff engineer must decide the launch strategy.'],
    question: 'What is the strongest framing for platform/market cold-start?',
    options: [
      'Copy the home-market model as-is and hope behaviour transfers',
      'Bootstrap with transferable signal (content-based models, cross-market pretraining/transfer, universal features like recency and popularity), instrument the new market as its own cohort, and design a data flywheel so local signal quickly replaces borrowed priors',
      'Wait a year to collect data before turning on personalisation',
      'Only ever show globally-popular items in the new market',
    ],
    answer: 1,
    diagnosis: 'Whole-platform cold-start = transfer now, flywheel to local signal',
    explanation: 'A new market has no collaborative signal and a partly different catalogue, so behavioural models trained elsewhere transfer poorly on specifics but content and universal features (recency, popularity, session dynamics) still generalise. The staff move is to bootstrap on transferable signal while explicitly engineering the loop that accumulates local data and hands off to a locally-trained model.',
    fix: 'Launch on content-based + cross-market transfer + universal features; monitor the market as its own cohort; prioritise the logging/labelling flywheel so local interaction data can retrain a market-specific model as fast as it accrues.',
    source: 'Authored · RecSys',
  },

  // ── evaluation (offline↔online, counterfactual) ────────────────
  {
    id: 'recsys-recall-at-k', subject: 'recsys', subtopic: 'evaluation', level: 'junior', type: 'mcq',
    title: 'Recall@K vs NDCG@K — which question does each answer',
    context: ['A junior reports both recall@50 and NDCG@10 and is asked which to use for which stage.'],
    question: 'Which metric fits retrieval, and which fits ranking, and why?',
    options: [
      'Both measure the same thing at different K',
      'Recall@K (did the relevant items make it into the top-K set, order-agnostic) fits retrieval; NDCG@K (are relevant items ordered high, with a position discount) fits ranking',
      'NDCG for retrieval, recall for ranking',
      'Only accuracy matters for both',
    ],
    answer: 1,
    diagnosis: 'Retrieval cares about presence; ranking cares about order',
    explanation: 'Retrieval\'s job is to not miss relevant items — recall@K measures exactly that and ignores order, which is correct because the ranker will reorder anyway. Ranking\'s job is to put the best items highest — NDCG@K rewards relevant items near the top via a position discount, matching what the user sees.',
    fix: 'Evaluate retrieval with recall@K (and coverage/diversity); evaluate ranking with NDCG@K / MRR. Using a ranking metric to grade retrieval, or vice versa, hides the failure that actually matters at that stage.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-offline-online-gap', subject: 'recsys', subtopic: 'evaluation', level: 'senior', type: 'mcq',
    title: 'Offline NDCG says win, the A/B says flat',
    context: ['A new ranker beats the old one by +6% offline NDCG on logged data.', 'The A/B test shows no engagement lift — and slightly worse novelty.', 'Offline evaluation replays the new model over impressions logged by the old model.'],
    question: 'What is the core reason offline and online disagree here?',
    options: [
      'The A/B sample size is too small — ship on the offline number',
      'Offline replay only scores the candidates the OLD model chose to show; the new model\'s best picks may never appear in the logs, so offline NDCG is measured on a biased, old-policy-selected set — a counterfactual/off-policy gap',
      'NDCG is broken and should never be trusted',
      'The A/B was contaminated; rerun it',
    ],
    answer: 1,
    diagnosis: 'Offline replay is off-policy: you only see the old policy\'s choices',
    explanation: 'Logged data contains only impressions the deployed model surfaced. Replaying a new model over those logs asks "how would it order what the old model already picked?" — it cannot credit items the old model never showed. So offline gains can be illusory (or hidden), and only an online test exposes the new policy to its own candidate distribution.',
    fix: 'Use counterfactual/off-policy estimators (IPS/doubly-robust) with logged propensities to de-bias offline evaluation, keep exploration data for coverage, and treat the online A/B as the ground truth — offline is a filter, not the verdict.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-counterfactual-ips', subject: 'recsys', subtopic: 'evaluation', level: 'staff', type: 'mcq',
    title: 'Estimating a new policy from old logs without shipping it',
    context: ['Before A/B-testing a new ranking policy, a team wants an offline estimate of its online reward from logged data.', 'The logs record which item was shown, the reward, and the logging policy\'s propensity for that action.'],
    question: 'What makes an inverse-propensity-scoring (IPS) estimate valid, and its main weakness?',
    options: [
      'IPS just averages rewards; propensities are irrelevant',
      'IPS reweights each logged reward by (new-policy prob / logging-policy prob) to correct for which actions the old policy over/under-showed — valid only if the logging policy had non-zero probability on the actions the new policy favours (full support); its weakness is high variance when the two policies diverge',
      'IPS requires the two policies to be identical',
      'IPS works only for classification, not ranking',
    ],
    answer: 1,
    diagnosis: 'Off-policy estimation needs support + propensities, and fights variance',
    explanation: 'IPS is a counterfactual estimator: importance-weight each logged outcome by the ratio of new-to-logging action probabilities so the sample looks like it came from the new policy. It is unbiased only under full support (the logger must sometimes take the actions the new policy prefers) and known propensities. When the policies disagree strongly, weights explode and variance blows up — hence clipping and doubly-robust variants.',
    fix: 'Log propensities, guarantee support via exploration, and prefer doubly-robust or clipped/self-normalised IPS to tame variance. Use the estimate to gate which candidates reach an A/B — never as the sole ship decision.',
    source: 'Authored · RecSys',
  },

  // ── embeddings & ANN operations ────────────────────────────────
  {
    id: 'recsys-ann-recall-latency', subject: 'recsys', subtopic: 'ann-ops', level: 'mid', type: 'mcq',
    title: 'The ANN knob that trades recall for latency',
    context: ['Retrieval uses an HNSW index.', 'P99 retrieval latency is over budget, so an engineer wants to lower efSearch (the search-time candidate list size).'],
    question: 'What is the trade-off being made, and what must be watched?',
    options: [
      'Lowering efSearch is free — it only speeds things up',
      'efSearch trades recall for latency: a smaller search list is faster but explores fewer graph nodes, so approximate recall drops — you must watch retrieval recall@K, not just latency, when tuning it',
      'efSearch controls index build time only, not queries',
      'It changes the embedding dimension',
    ],
    answer: 1,
    diagnosis: 'ANN is an approximation: the speed knob costs recall',
    explanation: 'ANN indexes (HNSW efSearch, IVF nprobe) expose a search-effort knob. Turning it down visits fewer candidates — lower latency, but the nearest neighbours it returns are more approximate, so recall against exact search falls. Tuning latency in isolation silently degrades retrieval quality that no downstream metric attributes back to the index.',
    fix: 'Tune efSearch/nprobe against a recall-vs-latency curve measured on a held-out set; pick the point that meets the latency SLA at acceptable recall, and monitor approximate recall@K continuously — not just P99 latency.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-embedding-staleness-rebuild', subject: 'recsys', subtopic: 'ann-ops', level: 'senior', type: 'mcq',
    title: 'Fresh embeddings the index has never heard of',
    context: ['Item embeddings are recomputed hourly by a fresh pipeline.', 'But the ANN index is rebuilt only nightly, and incremental inserts between rebuilds degrade its graph connectivity.', 'New and updated items are under-retrieved during the day even though their embeddings are current.'],
    question: 'What is the operational gap, and how is it closed?',
    options: [
      'The embeddings must be wrong — retrain the tower',
      'Embedding freshness and index freshness are two different clocks: current vectors are useless if the ANN structure that indexes them is stale or degraded by inserts; rebuild/refresh the index on a cadence matched to update rate, with a fresh-item fallback path',
      'Switch cosine similarity to dot product',
      'Increase the embedding dimension',
    ],
    answer: 1,
    diagnosis: 'Index freshness is a separate SLA from embedding freshness',
    explanation: 'An embedding being current does not mean it is well-indexed. HNSW/IVF structures accumulate connectivity debt from incremental inserts and only fully heal on rebuild, so items updated since the last rebuild are reachable but poorly connected. The two freshness clocks — vector recompute and index rebuild — must both be monitored.',
    fix: 'Match index rebuild cadence to the item-update rate (or use a proper incremental index with periodic full rebuilds), add a brute-force/exact fallback over the most-recent items merged before ranking, and monitor recall@K on a fresh-item hold-out.',
    source: 'Authored · RecSys',
  },

  // ── re-ranking, diversity, feedback loops / popularity bias ────
  {
    id: 'recsys-diversity-rerank', subject: 'recsys', subtopic: 'diversity', level: 'mid', type: 'mcq',
    title: 'Top 10 are all near-duplicates of each other',
    context: ['The ranker sorts purely by predicted engagement.', 'The top 10 are ten almost-identical items (same creator, same topic) — each individually high-scoring but redundant as a set.'],
    question: 'Why does a pure relevance sort do this, and what fixes it?',
    options: [
      'The ranker is buggy — retrain it',
      'A greedy relevance sort has no notion of a slot depending on what is already shown; add a diversity-aware re-rank (e.g. MMR or a DPP) that penalises redundancy so each new slot trades relevance against novelty vs the items already placed',
      'Show fewer items so duplicates are less visible',
      'Increase the candidate count K',
    ],
    answer: 1,
    diagnosis: 'Point-scoring ignores the marginal value of a slot given the set',
    explanation: 'Ranking by per-item score treats slots independently, so ten copies of the same great item all win. But a list has diminishing returns — the second near-duplicate adds little user value. Diversity re-ranking (Maximal Marginal Relevance, or a Determinantal Point Process) scores the marginal contribution of an item given what is already selected.',
    fix: 'Add a re-rank stage after the ranker that optimises list-level utility: MMR (relevance − λ·max similarity to chosen) or a DPP for set diversity; tune the diversity weight against downstream engagement/retention, not just top-1 relevance.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-popularity-feedback-loop', subject: 'recsys', subtopic: 'feedback-loops', level: 'senior', type: 'mcq',
    title: 'The rich get richer, and the tail dies',
    context: ['Over months, the model concentrates impressions ever more tightly on already-popular items.', 'Catalogue coverage in the feed keeps shrinking; the long tail gets almost no exposure.', 'The training data is the model\'s own logged impressions.'],
    question: 'What is the dynamic, and how do you counter it?',
    options: [
      'This is optimal — popular items are just better',
      'A popularity feedback loop: the model shows popular items → they get more interactions → next training reinforces them → tail items starve; break it with exposure/popularity de-biasing, an exploration budget for the tail, and monitoring catalogue coverage/Gini over time',
      'The catalogue is simply too large — shrink it',
      'Add more layers to the ranker',
    ],
    answer: 1,
    diagnosis: 'Self-reinforcing popularity bias collapses catalogue coverage',
    explanation: 'The model trains on impressions it chose, so items it favours accumulate more interaction data and look even better next cycle, while unshown items never get a chance to prove themselves. Left unchecked, the feed narrows to a popularity monoculture — bad for discovery, creators, and long-run retention.',
    fix: 'De-bias for exposure/popularity in training (e.g. IPW on impression propensity), reserve an exploration budget for tail items, and track catalogue coverage, impression Gini, and tail-item exposure as standing health metrics — not just aggregate engagement.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-data-flywheel', subject: 'recsys', subtopic: 'business-framing', level: 'staff', type: 'mcq',
    title: 'Why the incumbent feed is so hard to beat',
    context: ['A staff engineer is asked why a mature recommender has a durable advantage over a new entrant with the same model architecture.'],
    question: 'What is the strongest structural answer?',
    options: [
      'The incumbent simply has bigger GPUs',
      'The data flywheel: more users → more interaction logs → better-trained retrieval/ranking → better recommendations → more engagement and users → more data; the moat is the compounding interaction data, not the architecture, so protecting logging quality and the exploration that feeds it is a strategic concern',
      'The incumbent has a secret loss function',
      'Architecture is everything; data is a commodity',
    ],
    answer: 1,
    diagnosis: 'The moat is the compounding interaction-data flywheel',
    explanation: 'Recommender quality is dominated by the quantity and coverage of interaction data, which the same architecture cannot conjure. Each turn of the loop — impressions, clicks, dwell — feeds the next model, so the incumbent\'s advantage compounds. That reframes logging integrity, label quality, and exploration coverage from plumbing into strategy.',
    fix: 'Treat the flywheel as a first-class asset: invest in clean logging with propensities, guard exploration coverage so the tail keeps generating signal, and measure data-efficiency (quality per unit interaction), since that — not model tweaks — is where durable advantage accrues.',
    source: 'Authored · RecSys',
  },

  // ── incident-style multistep drills ────────────────────────────
  {
    id: 'recsys-incident-retrieval-recall-cliff', subject: 'recsys', subtopic: 'retrieval', level: 'staff', type: 'multistep',
    title: 'Overnight Recall Cliff in Candidate Generation',
    context: [
      'P1 incident. Feed engagement dropped 14% overnight; users report "everything is the same popular stuff".',
      'Two-tower retrieval → LambdaMART ranker → MMR diversity re-rank. Item embeddings recompute nightly; the ANN index rebuilds after the embedding job.',
      'Metrics: ranker NDCG@10 on served candidates unchanged (0.81); retrieval recall@400 fell from 0.86 to 0.52 overnight; catalogue coverage in impressions dropped from 94% to 61%.',
      'Pipeline logs: the nightly item_embedding job changed its output schema (embedding dim 128 → 256) after a model upgrade, but the ANN index build config still specifies dim=128 and silently ingested only the first 128 dims of each vector.',
      'Ranker and re-ranker are unchanged; latency normal; no A/B running.',
    ],
    question: 'Diagnose why engagement fell with the ranker untouched, and prescribe the fix.',
    steps: [
      {
        question: 'Engagement −14%, ranker NDCG unchanged, but recall@400 fell 0.86 → 0.52 and coverage 94% → 61%. Where is the failure?',
        options: [
          'The ranker regressed — retrain LambdaMART',
          'Retrieval — the candidate set collapsed toward popular items, so the ranker is reordering a much worse pool',
          'The diversity re-rank is over-diversifying',
          'User behaviour shifted overnight',
        ],
        answer: 1,
        finding: 'The ranker is healthy — its NDCG on the candidates it receives is unchanged. The damage is upstream: recall@400 halved and coverage crashed, so retrieval is handing the ranker a narrow, popularity-skewed pool. A ranker cannot surface what retrieval never returned; the recall ceiling dropped and dragged everything with it.',
      },
      {
        question: 'You trace it to the retrieval stage. Embedding dim changed 128 → 256 but the index still builds at dim=128. What happened and what is the fix?',
        options: [
          'Nothing — dimension mismatches are handled automatically',
          'The index silently ingested only the first 128 of each 256-dim vector, corrupting every distance computation; roll the embedding job back (or rebuild the index at dim=256), and add a schema/dim-compatibility check plus a recall@K canary that blocks the index swap on a regression',
          'Increase efSearch to compensate for the mismatch',
          'Retrain the two-tower model from scratch',
        ],
        answer: 1,
        finding: 'A dim mismatch is not a crash here — the index build truncated each 256-d vector to 128 dims, so every nearest-neighbour distance is computed on half a vector and retrieval returns near-garbage that collapses onto popular items. Immediate fix: roll back the embedding schema or rebuild the index at dim=256. Class-of-bug fix: a dim/schema compatibility gate between the embedding job and the index build, and a recall@K canary on a held-out set that must pass before the new index is promoted. Silent truncation is the tell — validate contracts between pipeline stages.',
      },
    ],
    diagnosis: 'Retrieval recall collapsed: an embedding dimension change (128→256) was silently truncated to 128 by the ANN index build, corrupting all distances.',
    explanation: 'The ranker was blamed by instinct, but its metric was unchanged — the ceiling above it dropped. Contracts between pipeline stages (embedding dim, schema) fail silently unless validated; a recall canary on the index and a dim-compatibility check would have blocked the bad swap.',
    fix: 'Roll back the embedding schema or rebuild the index at the correct dimension. Add a dim/schema compatibility gate between the embedding job and index build, and a recall@K canary on a held-out set that blocks index promotion on regression. Alert on catalogue-coverage drops in impressions as a leading indicator.',
    source: 'Authored · RecSys',
  },
  {
    id: 'recsys-incident-multiobjective-drift', subject: 'recsys', subtopic: 'multi-objective', level: 'senior', type: 'multistep',
    title: 'CTR Up, Satisfaction Down After a Retrain',
    context: [
      'P2 incident. After a routine value-model retrain, session CTR rose 7% but average dwell time fell 11% and "not interested" taps rose 20%.',
      'Value model = w_ctr·pCTR + w_dwell·dwell + w_share·pShare, weights fixed for 6 months. Only the pCTR and dwell heads were retrained on the last 30 days.',
      'Calibration check: pCTR reliability is good; the dwell head\'s output range collapsed — post-retrain it predicts a narrow band around a high value for almost all items (variance down 8×).',
      'The 30-day training window overlapped a period where a logging bug zeroed out dwell for a subset of long-watch sessions, so the dwell head learned to ignore dwell variation.',
    ],
    question: 'Diagnose why CTR rose while satisfaction fell, and prescribe the fix.',
    steps: [
      {
        question: 'CTR up but dwell down and "not interested" up, after retraining pCTR and dwell heads with fixed weights. What is the mechanism?',
        options: [
          'The weights were secretly changed',
          'The dwell head lost its ability to discriminate (variance collapsed), so its term became near-constant and dropped out of the blend — the model effectively reverted to CTR-dominated ranking, which selects clickbait',
          'The share head is broken',
          'Users simply changed behaviour',
        ],
        answer: 1,
        finding: 'Fixed weights do not guarantee fixed influence. The dwell head\'s output variance collapsed 8×, so w_dwell·dwell is now nearly the same for every item and drops out of the ordering. The blend silently became CTR-dominated — which is exactly the clickbait regime: clicks up, dwell and satisfaction down. A weight only matters if the head still varies across items.',
      },
      {
        question: 'You find the dwell head\'s collapse traces to a logging bug that zeroed dwell for long-watch sessions in the training window. What ships and what prevents recurrence?',
        options: [
          'Bump w_dwell up to compensate for the flat head',
          'Roll back to the previous value model; exclude/repair the corrupted dwell logs and retrain the dwell head; add per-head calibration + output-variance monitors that alert when a head flattens, and a data-quality check on dwell before it enters training',
          'Remove the dwell head entirely and rely on CTR',
          'Lower w_ctr until CTR stops rising',
        ],
        answer: 1,
        finding: 'Rolling back restores the healthy blend immediately. The real fix is upstream: repair or exclude the corrupted dwell logs (a logging bug zeroed long-watch dwell), retrain the dwell head, and — the class-of-bug fix — monitor each head\'s calibration and output variance so a flattened head pages someone, plus a data-quality gate on dwell before training. Bumping w_dwell would not help: a constant head contributes nothing to ordering no matter its weight.',
      },
    ],
    diagnosis: 'A corrupted-dwell training window collapsed the dwell head\'s variance, so it dropped out of the multi-objective blend and ranking reverted to CTR-dominated clickbait.',
    explanation: 'Multi-objective blends are governed by head calibration, not just weights — a head that stops varying silently leaves the ranking. The trigger was a data-quality bug (zeroed dwell) that no metric caught because per-head variance was not monitored.',
    fix: 'Roll back the value model. Repair/exclude corrupted dwell logs and retrain the head. Add per-head calibration and output-variance monitoring that alerts on flattening, and a data-quality check on each signal before it enters training. Track dwell and "not interested" alongside CTR, never CTR alone.',
    source: 'Authored · RecSys',
  },
]

// authored.js — hand-authored drills that fill the coverage holes the migration
// left: the junior on-ramp (nearly empty) and a few bare subject×level cells.
// Written to how the question is actually posed in interviews, not trivia.

export const AUTHORED_DRILLS = [
  // ── eval · junior ──────────────────────────────────────────────
  {
    id: 'auth-eval-confusion-basics', subject: 'eval', subtopic: 'metric-choice', level: 'junior', type: 'mcq',
    title: 'Reading a confusion matrix under a cost asymmetry',
    context: ['Spam filter, 1000 emails: TP 40, FP 5, FN 10, TN 945.', 'A false positive (real mail sent to spam) annoys users far more than a false negative.'],
    question: 'Which number should you push down first, and what does it map to?',
    options: [
      'False negatives — raise recall',
      'False positives — raise precision, since a legit email in spam is the costly error',
      'True negatives — they should be higher',
      'Accuracy is already 98.5%, nothing to fix',
    ],
    answer: 1,
    diagnosis: 'Optimize precision when false positives carry the higher cost',
    explanation: 'Precision = TP/(TP+FP) = 40/45 ≈ 0.89. Each FP is a real email lost to spam — the expensive mistake here. Accuracy hides this because the 945 true negatives dominate it.',
    fix: 'Raise the decision threshold to cut FPs (precision up, recall down), and report precision/recall, not accuracy, when classes and costs are imbalanced.',
    source: 'Authored',
  },
  {
    id: 'auth-eval-accuracy-trap', subject: 'eval', subtopic: 'metric-choice', level: 'junior', type: 'mcq',
    title: '99.2% accuracy on a fraud model — is it good?',
    context: ['Fraud is 0.8% of transactions.', 'A new model reports 99.2% accuracy and the team wants to ship.'],
    question: 'What is the first thing to check?',
    options: [
      'Ship it — 99.2% is excellent',
      'Compare to the all-negative baseline: predicting "not fraud" every time already scores 99.2%',
      'Increase the training set size',
      'Switch to a deeper model',
    ],
    answer: 1,
    diagnosis: 'Accuracy is meaningless at this class balance',
    explanation: 'At 0.8% fraud, always predicting "not fraud" gives 99.2% accuracy for free. The metric rewards the majority class and tells you nothing about caught fraud.',
    fix: 'Use precision/recall or PR-AUC on the minority class; pick a threshold from the cost of a missed fraud vs a false block.',
    source: 'Authored',
  },
  {
    id: 'auth-eval-heldout-why', subject: 'eval', subtopic: 'validation traps', level: 'junior', type: 'mcq',
    title: 'Why report a number on data the model never trained on',
    context: ['A junior reports 0.98 accuracy — measured on the same rows the model trained on.'],
    question: 'Why is that number not trustworthy?',
    options: [
      'It is trustworthy — same data is fine',
      'The model can memorise training rows, so training accuracy overstates how it does on new data',
      'Accuracy is always wrong',
      'You need more decimal places',
    ],
    answer: 1,
    diagnosis: 'Training-set accuracy overstates generalisation',
    explanation: 'A model can fit (even memorise) the rows it saw. The only honest estimate of future performance is on a held-out set it never touched.',
    fix: 'Split first, evaluate on the held-out test set, and never let any fitting (scalers, imputers, feature stats) see the test rows.',
    source: 'Authored',
  },

  // ── monitoring · junior/mid ────────────────────────────────────
  {
    id: 'auth-mon-drift-what', subject: 'monitoring', subtopic: 'drift', level: 'junior', type: 'mcq',
    title: 'Accuracy looks fine but you have no labels yet',
    context: ['A credit model scores loans in real time.', 'Ground-truth default labels take 30–90 days to arrive, so live accuracy is unknowable today.'],
    question: 'What can you monitor now to catch trouble early?',
    options: [
      'Nothing — wait for labels',
      'The input feature distributions and the score distribution vs the training baseline (data drift)',
      'Only the server CPU',
      'Retrain daily regardless',
    ],
    answer: 1,
    diagnosis: 'Monitor input/prediction distributions when labels are delayed',
    explanation: 'You cannot measure accuracy without labels, but you can watch whether the inputs and the score distribution have shifted away from training — the earliest available signal of trouble.',
    fix: 'Track PSI/KS on key features and on the prediction distribution against the training baseline; alert on shift, investigate before accuracy can even be measured.',
    source: 'Authored',
  },
  {
    id: 'auth-mon-psi-bands', subject: 'monitoring', subtopic: 'drift', level: 'mid', type: 'mcq',
    title: 'PSI on a key feature reads 0.18',
    context: ['Population Stability Index between training and this week on the income feature = 0.18.'],
    question: 'What does that value tell you to do?',
    options: [
      'Nothing — under 0.25 is always fine',
      'It is in the 0.1–0.25 band: a real shift worth investigating, not yet an automatic retrain',
      'Retrain immediately — any PSI means retrain',
      'PSI cannot be computed on income',
    ],
    answer: 1,
    diagnosis: 'PSI 0.1–0.25 = investigate; >0.25 = major shift',
    explanation: 'Conventional bands: <0.1 stable, 0.1–0.25 moderate shift (investigate), >0.25 significant. 0.18 is a genuine change worth a look, but not a fire-alarm on its own.',
    fix: 'Investigate what moved (a pipeline change vs real population shift), and pair the drift alert with a persistence requirement so one noisy week does not page anyone.',
    source: 'Authored',
  },

  // ── system_design · junior/mid ─────────────────────────────────
  {
    id: 'auth-sd-batch-vs-online', subject: 'system_design', subtopic: 'framing', level: 'junior', type: 'mcq',
    title: 'Batch scoring or online inference?',
    context: ['A churn model powers a weekly retention email campaign. Predictions are consumed once a week.'],
    question: 'Which serving mode fits, and why?',
    options: [
      'Online, real-time API — always the modern choice',
      'Batch scoring — the consumer is weekly, so precompute; real-time adds cost and latency for no benefit',
      'On-device inference',
      'Streaming with sub-50ms SLA',
    ],
    answer: 1,
    diagnosis: 'Match serving mode to when predictions are consumed',
    explanation: 'A weekly campaign has no latency requirement. Batch scoring a table on a schedule is simpler, cheaper, and easier to monitor than a real-time endpoint. Real-time is for when a user is waiting on the prediction.',
    fix: 'Reserve online inference for interactive, latency-bound use (fraud at checkout, recs on page load); use batch when the downstream consumer is periodic.',
    source: 'Authored',
  },
  {
    id: 'auth-sd-feature-store-why', subject: 'system_design', subtopic: 'framing', level: 'junior', type: 'mcq',
    title: 'Why teams put a feature store in the middle',
    context: ['Two teams each compute "30-day average spend" — one in Spark for training, one in Python at serving. Their models disagree in production.'],
    question: 'What core problem does a feature store solve here?',
    options: [
      'It makes models train faster',
      'It computes each feature once and serves the identical value to training and serving — killing training-serving skew',
      'It replaces the model',
      'It stores model weights',
    ],
    answer: 1,
    diagnosis: 'Feature store = one definition for train and serve',
    explanation: 'Two independent implementations of the "same" feature drift apart (nulls, timezones, windows), so the model sees different values at train vs serve time. A feature store makes the feature a single computed, point-in-time-correct definition.',
    fix: 'Define the feature once, register it, and have both training (point-in-time history) and serving (low-latency lookup) read the same source.',
    source: 'Authored',
  },
  {
    id: 'auth-sd-two-stage-why', subject: 'system_design', subtopic: 'retrieval', level: 'mid', type: 'mcq',
    title: 'Why not run the good ranker over all 10M items',
    context: ['A recommender must return 20 items from a 10M catalogue in under 100ms.'],
    question: 'Why is a two-stage (retrieve then rank) design used instead of ranking everything?',
    options: [
      'It looks more sophisticated',
      'Scoring 10M items with a good ranker cannot fit the latency budget; a cheap retrieval stage cuts to hundreds first, then the expensive ranker scores those',
      'Ranking all items is more accurate and always worth it',
      'Retrieval and ranking are the same model',
    ],
    answer: 1,
    diagnosis: 'Recall-then-precision funnel forced by latency',
    explanation: 'A good ranker over 10M items is orders of magnitude over any real-time budget. Cheap retrieval maximises recall over millions; the expensive ranker maximises precision over the few hundred survivors. Different jobs, different stages.',
    fix: 'Two-tower/ANN retrieval → cross-feature ranker → re-rank for diversity; retrieval recall caps final quality, so audit it first when results look weak.',
    source: 'Authored',
  },

  // ── deep_learning · junior/mid ─────────────────────────────────
  {
    id: 'auth-dl-overfit-signs', subject: 'deep_learning', subtopic: 'training-failures', level: 'junior', type: 'mcq',
    title: 'Train loss keeps dropping, val loss started climbing',
    context: ['Epoch 2 onward: train loss ↓ steadily, validation loss ↑.', '45M-param model, 12k training samples, no dropout, no weight decay.'],
    question: 'What is happening and the first fix?',
    options: [
      'Underfitting — train a bigger model',
      'Overfitting — the model is memorising; add regularisation (dropout/weight decay), reduce capacity, or get more data',
      'Learning rate too low',
      'The data loader is broken',
    ],
    answer: 1,
    diagnosis: 'Overfitting — diverging train/val loss',
    explanation: 'A high-capacity model on little data with no regularisation memorises the training set: train loss falls while val loss rises. The gap is the signature.',
    fix: 'Add dropout and weight decay, consider early stopping, reduce model size or use a pretrained backbone, and get more data if you can.',
    source: 'Authored',
  },
  {
    id: 'auth-dl-relu-why', subject: 'deep_learning', subtopic: 'gradients', level: 'junior', type: 'mcq',
    title: 'Why deep nets switched from sigmoid to ReLU',
    context: ['A 20-layer network with sigmoid activations barely trains — early layers stop learning.'],
    question: 'What does ReLU fix?',
    options: [
      'It makes the network smaller',
      "Sigmoid's slope maxes at 0.25 and vanishes through depth; ReLU's slope is 1 for active units, so the gradient survives to early layers",
      'ReLU removes the need for a loss function',
      'It only helps on images',
    ],
    answer: 1,
    diagnosis: 'ReLU keeps gradients from vanishing through depth',
    explanation: 'Backprop multiplies each layer\'s slope. Sigmoid (≤0.25) compounds to near zero over many layers, so early layers get no signal. ReLU passes a slope of 1 for active neurons, keeping the gradient alive.',
    fix: 'Use ReLU/GELU in hidden layers; add residual connections and normalisation for very deep nets. Reserve sigmoid for a probability output.',
    source: 'Authored',
  },

  // ── causal · junior ────────────────────────────────────────────
  {
    id: 'auth-causal-corr-cause', subject: 'causal', subtopic: 'framing', level: 'junior', type: 'mcq',
    title: 'Users who open the app more churn less — so nudge opens?',
    context: ['Observational data: users with more weekly app-opens have much lower churn.', 'A PM proposes push notifications to force more opens.'],
    question: 'What is the flaw in concluding "more opens cause less churn"?',
    options: [
      'The correlation is too weak',
      'Engaged users both open more and churn less — opens may be a symptom of engagement, not a cause of retention; a confounder drives both',
      'Nothing — correlation proves it',
      'Churn is the wrong metric',
    ],
    answer: 1,
    diagnosis: 'Correlation with a confounder ≠ causation',
    explanation: 'Underlying engagement/intent drives both opens and retention. Forcing opens via notifications need not move retention — you may just be measuring the symptom, or even annoy users into leaving.',
    fix: 'Run an experiment: randomly send the nudge to a treatment group and measure the causal effect on retention, not the observational correlation.',
    source: 'Authored',
  },

  // ── data · junior ──────────────────────────────────────────────
  {
    id: 'auth-data-leakage-basic', subject: 'data', subtopic: 'leakage', level: 'junior', type: 'mcq',
    title: 'A feature that makes the model suspiciously perfect',
    context: ['A model predicting whether a support ticket will be escalated hits 0.99 AUC.', 'Top feature: "resolution_team" — which is only assigned after escalation is decided.'],
    question: 'What kind of bug is this?',
    options: [
      'A great feature — keep it',
      'Target leakage — the feature encodes information only available after the outcome, so it will be missing at prediction time',
      'Class imbalance',
      'Overfitting to noise',
    ],
    answer: 1,
    diagnosis: 'Target leakage from a post-outcome feature',
    explanation: 'resolution_team is set after the escalation decision, so at real prediction time it does not exist. The model is peeking at the future; the 0.99 AUC evaporates in production.',
    fix: 'For every feature, ask "is this value known at the moment of prediction?" Drop anything computed at or after the label event; enforce point-in-time correctness.',
    source: 'Authored',
  },
  {
    id: 'auth-data-scaling-why', subject: 'data', subtopic: 'feature-eng', level: 'junior', type: 'mcq',
    title: 'kNN says two customers are "near" — but only income mattered',
    context: ['Features: age (0–100) and income (0–500,000), unscaled.', 'A distance-based model (kNN) treats them as equally weighted inputs.'],
    question: 'Why does income dominate the distance, and what fixes it?',
    options: [
      'It does not — both count equally',
      'Distance sums squared differences; income spans thousands of units so its gap dwarfs age — scale each feature to a comparable range first',
      'Drop age entirely',
      'Use a bigger k',
    ],
    answer: 1,
    diagnosis: 'Unscaled features let the large-range one dominate distance',
    explanation: 'Euclidean distance is a sum of squared raw differences. Income (0–500k) swamps age (0–100), so "nearest neighbour" silently means "closest income" regardless of age.',
    fix: 'Standardise/normalise features before any distance- or gradient-based model. (Tree models split on thresholds and are unaffected.)',
    source: 'Authored',
  },

  // ── production · junior ────────────────────────────────────────
  {
    id: 'auth-prod-canary-what', subject: 'production', subtopic: 'deployment', level: 'junior', type: 'mcq',
    title: 'Shipping a new model to 100% of traffic at once',
    context: ['A team wants to replace the live model with a new version in one switch.'],
    question: 'What safer rollout pattern reduces blast radius, and how does it work?',
    options: [
      'Just deploy to everyone — faster',
      'Canary: send a small % of traffic to the new model, watch metrics, then ramp up if healthy or roll back if not',
      'Train longer before deploy',
      'Only deploy on weekends',
    ],
    answer: 1,
    diagnosis: 'Canary limits blast radius on rollout',
    explanation: 'A full cutover means a bad model hits everyone instantly. A canary exposes a small slice first, so a regression shows up on 1–5% of traffic and can be rolled back before it spreads.',
    fix: 'Route a small % to the new version, gate the ramp on live guardrail metrics (latency, error rate, business KPI), and keep an automatic rollback trigger.',
    source: 'Authored',
  },

  // ── time_series · junior ───────────────────────────────────────
  {
    id: 'auth-ts-split-why', subject: 'time_series', subtopic: 'features', level: 'junior', type: 'mcq',
    title: 'Random train/test split on a sales forecast',
    context: ['A junior random-splits 3 years of daily sales 80/20 and reports a great backtest.'],
    question: 'Why is a random split wrong for time series?',
    options: [
      'It is fine — random is always safest',
      'It lets the model train on future days and test on past ones — leaking the future; forecasts must be validated forward in time',
      'The test set is too small',
      'Sales cannot be forecast',
    ],
    answer: 1,
    diagnosis: 'Random split leaks the future into training',
    explanation: 'A random split puts some future dates in the training set and past dates in test, so the model "sees" the future it is supposed to predict. The backtest is optimistic and meaningless.',
    fix: 'Use a forward (rolling-origin) split: train on the past, test on the immediately-following window, and backtest at the horizon you will actually deploy.',
    source: 'Authored',
  },
  {
    id: 'auth-ts-stationarity-what', subject: 'time_series', subtopic: 'stationarity', level: 'junior', type: 'mcq',
    title: 'What "stationary" is trying to tell you',
    context: ['A series has a clear upward trend and growing seasonal swings over the years.'],
    question: 'What does non-stationarity mean here, and why does it matter for a classical model?',
    options: [
      'It means the data is missing',
      'The mean/variance change over time; models like ARIMA assume stable statistics, so you difference/transform to stabilise them first',
      'Nothing — stationarity is optional',
      'It means the series is random',
    ],
    answer: 1,
    diagnosis: 'Non-stationary = statistics drift over time',
    explanation: 'Trend and growing seasonal amplitude mean the mean and variance are not constant. Classical models (ARIMA) assume stationarity, so their estimates are off until you stabilise the series.',
    fix: 'Difference to remove trend, log/Box-Cox to stabilise variance, and de-seasonalise; confirm with a unit-root test (ADF/KPSS) before fitting.',
    source: 'Authored',
  },

  // ── classical_ml · staff (bare cell) ───────────────────────────
  {
    id: 'auth-classical-model-roi', subject: 'classical_ml', subtopic: 'model-selection', level: 'staff', type: 'mcq',
    title: 'A 5-model stacked ensemble buys +0.4% AUC',
    context: ['Current: one gradient-boosted tree, 0.882 AUC, 8ms inference, one artifact to monitor.', 'Proposed: a 5-model stack, 0.886 AUC, 60ms inference, 5 artifacts + a meta-learner to retrain and monitor.'],
    question: 'How should a staff engineer frame the decision?',
    options: [
      'Ship the stack — higher AUC always wins',
      'Weigh the +0.4% against 7× latency and 5× operational surface; unless that AUC maps to real business value beyond the added serving + monitoring + retraining cost, keep the single model',
      'Always avoid ensembles',
      'Decide purely on offline AUC',
    ],
    answer: 1,
    diagnosis: 'Model choice is a total-cost decision, not an AUC contest',
    explanation: 'A 0.4% AUC gain rarely justifies 7× latency and a 5× jump in things that can break and must be retrained/monitored. Staff-level judgment prices the operational and latency cost against the actual business value of the metric delta.',
    fix: 'Translate the AUC delta into business terms (revenue/loss), compare to the fully-loaded cost of serving + monitoring + retraining the stack, and prefer the simplest model that clears the bar.',
    source: 'Authored',
  },
]

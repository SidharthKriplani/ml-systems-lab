export const PRODUCTION_MODULES = [
  {
    id: 'training_serving_skew',
    title: 'Training-Serving Skew',
    subtitle: 'Definition, causes, detection, remediation',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['training-serving skew', 'production ML', 'feature drift'],
    summary: `Training-serving skew occurs when the feature distribution or feature computation at training time differs from serving time. The model was trained on one distribution and deployed into another — even if the data collection process is the same, subtle differences in preprocessing, timing, or data pipeline implementation can cause the model to behave unexpectedly in production. Training-serving skew is one of the top causes of model underperformance post-deployment.`,
    keyPoints: [
      'Root causes: (1) Different code paths for training vs serving feature computation. (2) Training uses ground truth future data that is not available at serving time (data leakage → skew at serving). (3) Feature freshness difference — training on daily batch features, serving on stale features.',
      'Timestamp leakage: training features computed with respect to a future reference time. E.g., "account_age_days" computed as of the label date rather than as of the inference date — the model sees artificially older accounts during training.',
      'Detection: log production features alongside predictions. Compare feature distributions between (a) training data and (b) production features used for predictions. Use PSI or KS test per feature. Large divergence = skew.',
      'Shadow mode validation: before launch, run new model in shadow mode, log features and predictions. Compare feature distributions to training distribution. Catch skew before it affects users.',
      'Remediation: enforce a single feature computation library used in both training and serving. Point-in-time correct feature extraction during training (look up feature values as they existed at the label timestamp, not today). Feature store with serving API enforces consistency.',
      'Preprocessing skew: fitting scaler/imputer on training data, applying identical parameters at serving (store mean, std from training in model artifact). Never re-fit at serving time.',
      'Continuous monitoring: track feature-level distribution metrics (mean, std, null rate, PSI) per deployment. Alert when PSI > 0.2 for any feature — indicates significant skew.',
    ],
    checkQuestions: [
      { q: 'Your model predicts churn well in offline evaluation but performs randomly in production. Feature distributions look similar. What else could cause this?', a: '(1) Label definition mismatch: offline label defined differently from the production outcome being measured. (2) Prediction horizon skew: model trained to predict 30-day churn but serving environment fires prediction at a different time relative to observation window. (3) Feature ordering or encoding mismatch: categorical feature encoded differently in training (alphabetical order) vs serving (database insert order). (4) Missing feature: a high-importance feature present in training is null or absent in serving. (5) Model serialisation issue: model saved and loaded with version mismatch. Debug by logging raw features at serving time and comparing against training examples for the same entity.' },
    ],
  },
  {
    id: 'feature_engineering_prod',
    title: 'Feature Engineering in Production',
    subtitle: 'Online vs offline features, point-in-time joins, backfill risk',
    difficulty: 'intermediate',
    estimatedMin: 25,
    tags: ['feature engineering', 'online features', 'point-in-time', 'backfill'],
    summary: `Production feature engineering must balance richness (complex features improve model performance) with reliability (complex pipelines introduce more failure modes). Features fall into two categories: offline features (computed in batch from historical data, stored, retrieved at serving time) and online features (computed in real-time from live data streams). The most dangerous error in feature engineering is failing to enforce point-in-time correctness during training, leading to data leakage.`,
    keyPoints: [
      'Offline features: computed in batch (hourly/daily). Low latency at serving (lookup from store). Can use rich historical aggregations. Risk: staleness — features may be hours/days old at serving time.',
      'Online features: computed in real-time from live events. Always fresh. High operational complexity: the computation must be fast (< 50ms), reliable (99.99% uptime), and match training computation exactly.',
      'Point-in-time correct feature extraction: for each training sample with label timestamp t, retrieve feature values as they existed at time t, not as they exist today. E.g., "customer revenue last 30 days" should be revenue in the 30 days ending at t, not at the time of training data extraction.',
      'Backfill risk: when recomputing historical features for training, the backfill pipeline may use information not available at the original time (e.g., schema changes, bug fixes, external data updates). The backfilled features then differ from what was available when predictions were made.',
      'Feature freshness SLA: define maximum acceptable staleness per feature. "User last 24h session count" may tolerate 1h staleness; "cart contents" may require <1s freshness. SLA violations should trigger alerts.',
      'Feature reuse: computing features once and reusing across models reduces computational cost and ensures consistency. Feature stores materialise features centrally. Without a feature store, each team reimplements feature computation — divergence is inevitable.',
      'Schema validation at serving: validate feature schema (types, ranges, null rates) at serving time. Reject or impute if features violate expected schema. Log schema violations for debugging.',
    ],
    checkQuestions: [
      { q: 'You are building a loan default prediction model. Describe how you would handle the feature "total_outstanding_loan_balance" for both training and serving.', a: 'Training: for each loan application in the training set with label date t, retrieve total_outstanding_loan_balance as it was at time t (point-in-time correct join on the balance history table: balance as of t). Never use today\'s balance — the customer may have paid off loans since then. Serving: query the balance from the production database at inference time (online feature). Ensure the query logic is identical to the training extraction logic. Store in feature store with timestamp. Test: verify that training and serving feature distributions match using shadow mode logging.' },
    ],
  },
  {
    id: 'feature_store',
    title: 'Feature Store Architecture',
    subtitle: 'Offline store, online store, registry, materialisation, latency SLAs',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['feature store', 'MLOps', 'architecture', 'feature serving'],
    summary: `A feature store is a centralised system for storing, computing, and serving ML features. It decouples feature engineering from model training and serving, enabling feature reuse across teams and models. A feature store has three core components: an offline store (batch historical features for training), an online store (low-latency serving for inference), and a feature registry (metadata, versioning, lineage). Materialisation pipelines keep offline and online stores in sync.`,
    keyPoints: [
      'Offline store: historical feature values, typically in a data warehouse (BigQuery, Snowflake) or object store (Parquet on S3). Used for training dataset generation with point-in-time correct joins.',
      'Online store: low-latency key-value store (Redis, DynamoDB, Cassandra) for serving features at inference time. P99 latency < 10ms is typical. Stores only current feature values.',
      'Feature registry: metadata catalog of all features — definition, data type, owner, freshness SLA, lineage (which models use this feature, which pipeline computes it). Enables discoverability and governance.',
      'Materialisation: the process of computing and writing features to the online store. Batch materialisation: scheduled jobs (Airflow/Spark). Stream materialisation: Kafka → Flink → online store. Stream enables low-staleness online features.',
      'Point-in-time joins in offline store: when generating training data, join label table to feature tables using event_timestamp conditions (feature_value.event_time ≤ label_timestamp). Feast and Tecton implement this natively.',
      'Feature versioning: feature definitions change over time. Version features to ensure reproducibility — a model trained on feature_v1 continues to work even after feature_v2 is deployed. Old model versions reference old feature versions.',
      'Open source options: Feast (lightweight, Kubernetes-native), Tecton (managed), Hopsworks, SageMaker Feature Store. Key selection criteria: latency SLAs, scale, stream support, point-in-time join, team size.',
    ],
    checkQuestions: [
      { q: 'A data scientist wants to reuse "user_30d_purchase_count" computed by another team. What does the feature store provide to make this safe?', a: 'The feature registry provides: (1) Feature definition — exact computation logic (SQL/PySpark) so the data scientist understands what they are using. (2) Data type and expected range — validates the feature is what they expect. (3) Freshness SLA — when was it last updated and how stale can it be? (4) Lineage — which upstream data sources it depends on. (5) Owner contact — who to ping if the feature breaks. Without the registry, the team would need to reverse-engineer the computation, risk using a stale version, and would have no recourse if the feature is discontinued. The feature store also handles the serving logistics — the data scientist just specifies the feature name and entity ID.' },
    ],
  },
  {
    id: 'feature_store_traps',
    title: 'Feature Store API Traps',
    subtitle: 'Stale features, cold-start, versioning, deprecation',
    difficulty: 'intermediate',
    estimatedMin: 15,
    tags: ['feature store', 'cold start', 'staleness', 'versioning'],
    summary: `Feature stores introduce subtle failure modes that are different from traditional software APIs. The most common issues: stale features that silently degrade model performance, cold-start entities with no historical features, version mismatches between feature definitions and model expectations, and premature deprecation of features used by production models. These issues are often silent — no exception is thrown, but predictions degrade.`,
    keyPoints: [
      'Staleness: online store features may not be updated due to pipeline failure, late-arriving data, or infrastructure issues. Model receives yesterday\'s features while thinking they are current. Monitor feature freshness at serving time.',
      'Cold-start: new entities (new users, new products) have no feature history. Feature store returns null or a default value. Model must handle nulls gracefully — impute with global mean, use a cold-start model, or apply fallback rules.',
      'Default values for cold-start: use the population median or the mode, not 0 (which may be an extreme value). For new users, a "new user" binary feature often helps the model adapt its behaviour.',
      'Version drift: model was trained on feature_definition_v1. Feature team updates to v2 (changes aggregation window from 7d to 14d). Online store starts serving v2 values while model expects v1 semantics. Silent performance degradation.',
      'Deprecation risk: a core feature used by 10 models is flagged for deprecation. Without lineage tracking in the registry, the feature team does not know all consumers — breaking downstream models silently.',
      'Backfill inconsistency: when the online store pipeline fails and is backfilled, the backfill may use different logic or data than the original computation, causing a distributional shift in the feature.',
      'Testing: shadow-test new feature versions before cutting over. A/B test models on old vs new feature version. Build integration tests that validate feature distributions match expected ranges on sample entities.',
    ],
    checkQuestions: [
      { q: 'Your recommendation model suddenly degrades in production. Feature distributions look normal on average, but P99 latency for feature retrieval spiked from 5ms to 800ms. What happened and what do you do?', a: 'The online store (Redis/DynamoDB) is likely experiencing a latency spike — possibly due to hot keys (many requests hitting the same key), cache miss (eviction pressure), or infrastructure issue. Impact: slow feature retrieval causes timeout or fallback to default/null features, model operates on missing data, predictions degrade. Immediate actions: (1) Check online store metrics — latency, hit rate, eviction rate, connection pool. (2) Check if fallback to defaults is occurring — model using null features? (3) Scale up online store or increase connection pool. (4) Implement circuit breaker: if feature retrieval > 50ms, use cached/default values. Long-term: implement client-side caching for high-request-rate features, shard hot keys.' },
    ],
  },
  {
    id: 'late_arriving_data',
    title: 'Late-Arriving Data',
    subtitle: 'Watermarking, streaming late data handling, impact on labels, remediation',
    difficulty: 'advanced',
    estimatedMin: 20,
    tags: ['late data', 'streaming', 'watermark', 'label quality'],
    summary: `In real-time ML systems, events often arrive late — a mobile event may arrive hours after it occurred due to network delays, device offline periods, or upstream pipeline lags. Late arrivals cause two problems: (1) streaming aggregations compute incorrect feature values (missing events), and (2) labels are corrupted (a conversion event that arrives after the label cutoff is classified as non-conversion). Both degrade model quality silently.`,
    keyPoints: [
      'Watermarks in streaming (Flink/Spark): a watermark at time t declares "all events with timestamp ≤ t have arrived." Events arriving after the watermark are late. Watermark policy: max_seen_event_time − allowed_lateness.',
      'Late data handling in Flink: late events can be routed to a side output stream for reprocessing, or they can trigger window recomputation (allowed lateness on windows). Tradeoff: too short allowed lateness = missed events; too long = processing delay.',
      'Impact on labels: for a click-through prediction model, a click event arriving after the label generation time is incorrectly counted as no-click. If click event lateness is 4 hours, labels generated within 4 hours of the observation window will have systematic false negatives — bias in training data.',
      'Delayed feedback problem: common in ad attribution. A conversion (purchase) may be attributed to an impression hours or days later. Training on features at impression time with labels available hours later creates a temporal mismatch.',
      'Label delay correction: (1) Only generate training labels after a sufficient "label incubation period" (waiting long enough for all late events to arrive). (2) Model the label delay explicitly — weight training samples by P(label complete | delay so far). (3) Online learning with label corrections as late events arrive.',
      'Practical mitigation: design feature pipelines to be recomputable. Store raw events, not just aggregations. Schedule recomputation at t+24h and t+7d to correct labels for late-arriving data. Use append-only event stores.',
      'Monitoring: track label completeness over time — plot fraction of expected events received as a function of hours after event occurrence. Visualise the lateness distribution to set appropriate watermarks and incubation periods.',
    ],
    checkQuestions: [
      { q: 'You are training a click model for mobile ads. Labels are generated 1 hour after the ad impression. You find your model has much lower precision for impressions on mobile devices. Why?', a: 'Mobile devices often go offline and batch-upload events when reconnected. Click events on mobile may arrive hours after the 1-hour label cutoff — classified as no-click (false negatives). The model sees a disproportionate number of false negatives for mobile impressions, learning that mobile traffic is less likely to click — a spurious pattern. Fix: (1) Extend label incubation period to 24-48h for mobile. (2) Measure click event lateness distribution by platform — validate how long to wait for 95%+ of events to arrive. (3) Add device_platform as a feature so the model can learn the actual CTR per platform. (4) Apply label correction weights based on expected completeness.' },
    ],
  },
  {
    id: 'data_quality',
    title: 'Data Quality for ML',
    subtitle: 'Schema drift, distribution shift in features, null rates, automated validation',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['data quality', 'schema drift', 'validation', 'Great Expectations'],
    summary: `Data quality is the foundation of reliable ML systems. Bad data silently degrades models — unlike software bugs, there is no exception thrown when a feature nulls out or a distribution shifts. Production ML requires automated, comprehensive data quality checks at every stage of the pipeline: ingestion, feature computation, training, and serving. The goal is to detect issues before they corrupt model predictions.`,
    keyPoints: [
      'Schema validation: enforce expected column names, data types, and value ranges. Any deviation (new column, type change, unexpected null) should trigger an alert. TensorFlow Data Validation (TFDV) and Great Expectations are standard tools.',
      'Distribution validation: compare current data distribution to a reference (training distribution). Statistical tests: PSI (population stability index) for continuous features, chi-squared for categoricals. PSI > 0.2 = significant shift.',
      'Null rate monitoring: track % null per feature over time. A sudden spike in nulls (e.g., 0.1% → 30%) indicates upstream pipeline failure. Set per-feature null rate SLAs.',
      'Value range checks: flag values outside [min_train, max_train] as out-of-range. For numerical features, values beyond 5σ from training mean are suspect. Clip or impute with caution — understand why they are OOR before discarding.',
      'Cardinality drift: for categorical features, new categories appearing (or expected categories disappearing) indicates upstream data change. New categories are typically OOV (out-of-vocabulary) for the model — handled as unknown/UNK.',
      'Referential integrity: features that are IDs (user_id, product_id) must exist in reference tables. Missing referential integrity = feature retrieval will return null at serving time.',
      'Expectation suites (Great Expectations): define expectations as code (expect_column_to_exist, expect_column_values_to_not_be_null, expect_column_mean_to_be_between). Run on each pipeline run, fail and alert on violation. Expectation suites are versioned and reviewed like code.',
    ],
    checkQuestions: [
      { q: 'Your training pipeline runs successfully every day, but model performance has been slowly degrading over 3 weeks with no code changes. How do you diagnose this?', a: 'This is a data quality or distribution drift issue. Diagnosis steps: (1) Check feature-level PSI over time — which features have PSI > 0.1 in recent weeks? (2) Check null rates per feature — any silent null rate increase? (3) Check label distribution — is the positive rate shifting (e.g., fewer fraud cases if fraud patterns changed)? (4) Check upstream data sources for schema changes (new ETL job, source system update). (5) Check data volume per day — any sudden drop indicating missing data? (6) Review feature importances across model retraining runs — did which features matter change? A 3-week slow degradation points to gradual distribution drift, not a sudden pipeline failure.' },
    ],
  },
  {
    id: 'label_generation',
    title: 'Label Generation',
    subtitle: 'Programmatic labeling, label noise, weak supervision, distant supervision',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['labeling', 'label noise', 'weak supervision', 'programmatic labeling'],
    summary: `Labels are the foundation of supervised learning, yet obtaining high-quality labels at scale is expensive and time-consuming. Programmatic labeling (weak supervision) uses heuristic labeling functions instead of human annotators — each function is noisy but combining many functions via a label model (Snorkel) produces accurate probabilistic labels. Understanding and handling label noise is essential for building reliable ML systems.`,
    keyPoints: [
      'Types of label noise: random noise (labels flipped uniformly, less harmful), systematic noise (specific patterns consistently mislabeled, more harmful — model learns the noise). Identify systematic noise via error analysis.',
      'Programmatic labeling (Snorkel): write labeling functions (LFs) — each LF is a heuristic (regex, distant supervision, ML model) that labels a subset of data. LFs have coverage (what fraction they label) and accuracy (how often correct). A label model aggregates LFs into probabilistic labels.',
      'Distant supervision: use external knowledge bases to generate labels heuristically. E.g., if company X and person Y appear together and the KB says X employs Y, label the sentence with relation "employed_by". Noisy but scalable.',
      'Label smoothing: instead of hard 0/1 labels, use soft labels (0.9/0.1). Reduces overconfidence of model on noisy labels. Equivalent to adding uniform noise to labels during training.',
      'Noise-robust loss functions: symmetric loss functions (e.g., symmetric cross-entropy) are more robust to label noise than standard cross-entropy. Bootstrap loss: use model\'s own predictions as proxy labels for noisy instances.',
      'Confident learning (CleanLab): identify and correct noisy labels using the model\'s predicted probabilities. For each class, estimate the noise transition matrix — what fraction of true class k labels are flipped to class j.',
      'Human annotation best practices: use multiple annotators and resolve disagreements via majority vote or adjudication. Cohen\'s kappa measures inter-annotator agreement. Low kappa → ambiguous task definition, not annotator carelessness.',
    ],
    checkQuestions: [
      { q: 'You are building a content moderation model. Human labeling is too expensive at scale. How do you approach label generation?', a: 'Programmatic labeling workflow: (1) Write labeling functions (LFs): regex patterns for profanity, keyword lists for hate speech, a small trained classifier on the labelled seed data, rule-based heuristics from policy documents. (2) Analyse LFs: coverage (what fraction of data each LF labels), conflict (when LFs disagree), accuracy on a small gold-labeled set. (3) Train a label model (Snorkel) that learns LF accuracies and correlations, outputs probabilistic labels. (4) Train a discriminative model (BERT) on the probabilistic labels — this "end model" generalises beyond the LF patterns. (5) Validate: sample 1000 examples from each label bucket and have humans verify. Refine LFs iteratively.' },
    ],
  },
  {
    id: 'pipelines',
    title: 'ML Pipeline Architecture',
    subtitle: 'Batch vs streaming ingestion, orchestration, idempotency, pipeline failures',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['MLOps', 'pipelines', 'orchestration', 'Airflow', 'idempotency'],
    summary: `An ML pipeline is the end-to-end computational graph from raw data to deployed model predictions. It includes data ingestion, feature computation, training, evaluation, and deployment — each with dependencies and failure modes. Pipeline reliability requires idempotent operations (re-running produces the same result), clear failure handling, and end-to-end lineage tracking. Orchestrators like Airflow, Prefect, and Kubeflow Pipelines manage dependencies and retries.`,
    keyPoints: [
      'Batch pipelines: run on a schedule (hourly, daily). Low operational complexity. Higher feature staleness. Suitable for offline features and model retraining. Airflow DAGs are the standard orchestration tool.',
      'Streaming pipelines: continuous processing of events. Low latency for real-time features. Higher complexity: stateful computation, exactly-once semantics, backpressure handling. Flink and Spark Streaming are standard.',
      'Idempotency: re-running a pipeline step with the same inputs produces the same outputs and does not create duplicate records. Achieved via upsert (not insert), deterministic feature computation, and idempotent write patterns.',
      'Exactly-once semantics in streaming: a message is processed exactly once even in the presence of failures. Flink achieves this via checkpointing with two-phase commit. Apache Kafka Streams also provides exactly-once via transactions.',
      'Pipeline failure modes: (1) Missing data (upstream source is late/empty). (2) Schema mismatch (source schema changed). (3) Compute failure (OOM, node failure). (4) Corrupt data (invalid values slip through). Each requires different handling: retry, alert, skip, or quarantine.',
      'Lineage tracking: record which data versions, code versions, and feature versions were used to train each model. Critical for reproducibility and debugging. MLflow, DVC, and Weights&Biases provide lineage tracking.',
      'Pipeline testing: unit test each transform (input → expected output), integration test the full pipeline on a small sample, data validation tests at each stage boundary. Run tests on every pipeline code change.',
    ],
    checkQuestions: [
      { q: 'Your daily retraining pipeline fails on day 3 due to an upstream data source being unavailable. How do you design the system to handle this gracefully?', a: '(1) Data availability check at pipeline start: before any computation, validate that all upstream data sources have data for the expected date range. If not, fail fast with a clear alert rather than training on incomplete data. (2) Idempotent pipeline steps: ensure that re-running the pipeline after the source recovers produces the correct result. (3) Graceful degradation: if today\'s data is unavailable, continue serving the model trained on yesterday\'s data (do not retrain on corrupted/incomplete data). (4) SLA monitoring: alert if the source is unavailable for >1h. (5) Automatic retry: Airflow retry with exponential backoff. (6) Backfill mechanism: once source recovers, backfill and retrain with the missed day\'s data.' },
    ],
  },
  {
    id: 'model_registry',
    title: 'Model Registry & Versioning',
    subtitle: 'Artifact storage, metadata, lineage, deployment gating, experiment tracking',
    difficulty: 'intermediate',
    estimatedMin: 15,
    tags: ['model registry', 'MLflow', 'versioning', 'deployment'],
    summary: `A model registry is a centralised repository for storing and managing trained model artifacts, their metadata, and their deployment status. It bridges experiment tracking (which model was best?) and deployment (which model is live?). A well-designed registry records full lineage — which data, code, and parameters produced each model — enabling reproducibility and rollback. MLflow Model Registry, SageMaker, and Vertex AI Model Registry are common implementations.`,
    keyPoints: [
      'Model artifacts: serialised model weights, preprocessing pipelines, feature schema, evaluation metrics, training metadata. Stored in object storage (S3, GCS) with version identifiers.',
      'Model stages: Staging (passed automated tests, ready for human review) → Production (live, serving traffic) → Archived (deprecated, no longer serving). Transitions require approval gates.',
      'Deployment gating: automated checks before promoting to production. Minimum performance thresholds (AUC > X, ECE < Y). Comparison against current production model — new model must beat champion on validation set. Bias/fairness metrics check.',
      'Experiment tracking: log hyperparameters, metrics, artifacts for every training run. MLflow and W&B are standard. Enables comparing 100 experiments to find the best model, reproducibility from any past run.',
      'Model lineage: for each model version, record the exact training dataset version, code commit hash, feature store feature versions, and hyperparameters. Full reproducibility and audit trail.',
      'Rollback mechanism: if production model degrades, one-click rollback to the previous production version. Model registry must retain previous production artifact and its metadata.',
      'Challenger model in registry: a model in "Staging" can be shadow-mode deployed alongside the production "Champion" model. Performance comparison from shadow logs informs the production promotion decision.',
    ],
    checkQuestions: [
      { q: 'Your production model is found to be biased against a demographic group after deployment. How does the model registry help you remediate?', a: '(1) Audit lineage: registry provides the training dataset version, feature set, and code commit. You can reproduce the exact training run and understand what went wrong. (2) Rollback: immediately promote the previous production model version from Archived → Production, minimising time with the biased model live. (3) Add bias metrics to deployment gate: update the gating criteria to include fairness metrics (demographic parity, equalised odds) as required conditions for production promotion — future models cannot be deployed without passing these checks. (4) Re-train: with fixed training data or reweighted samples, produce a new model version, log it in the registry with updated bias metrics, and promote through the updated gate.' },
    ],
  },
  {
    id: 'ab_infra',
    title: 'A/B Infrastructure',
    subtitle: 'Traffic splitting, treatment assignment, exposure logging, interaction effects',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['A/B testing', 'experimentation', 'traffic splitting', 'exposure logging'],
    summary: `A/B infrastructure is the engineering system that supports controlled experiments at scale. It handles: deterministic, reproducible treatment assignment for each user, correct exposure logging (only count users who actually experienced the treatment), traffic splitting with guaranteed balance, and mechanisms to detect and handle experiment interactions. Without reliable infrastructure, A/B test results are untrustworthy regardless of statistical methodology.`,
    keyPoints: [
      'Deterministic assignment: hash(user_id + experiment_id) mod 100 < treatment_percentage assigns users to treatment. Same user always gets same treatment — critical for consistent experience and reproducibility.',
      'Bucketing: assignment must be independent across experiments to avoid correlation. Disjoint experiment layers ensure orthogonality. Within a layer, each user is in only one bucket.',
      'Exposure logging: only log a user as "exposed" when they actually see the treatment (e.g., encounter the page with the new feature). Logging at assignment rather than exposure inflates denominator — dilutes the effect, reduces power.',
      'Sample Ratio Mismatch (SRM): expected 50/50 split but observe 52/48. Indicates a bug in assignment or logging. Never analyse an experiment with SRM > 1% until the root cause is fixed — results are invalid.',
      'Novelty effects: users interact with new UI elements simply because they are new, not because the design is better. Include a "novelty holdout" segment that sees the new feature for the first time 2 weeks after launch — use their behaviour as a long-run estimate.',
      'Network/interference effects: when users interact (social network, marketplace), treating some users affects control users. Geographic or network cluster randomisation with interference-aware estimators (HTE estimators).',
      'Multiple testing: running 10 simultaneous experiments at α=0.05 → expected 0.5 false positives by chance. Use Bonferroni or Benjamini-Hochberg correction for primary metrics. Keep guardrail metrics at family-wise error rate control.',
    ],
    checkQuestions: [
      { q: 'You run an A/B test with 50/50 split. Analysis shows 52% of unique users are in treatment, 48% in control. What do you do before looking at metrics?', a: 'This is a Sample Ratio Mismatch (SRM) — expected 50/50 but observed 52/48. SRM indicates a systematic bug in assignment, exposure logging, or data pipeline. DO NOT analyse the primary metrics — results are unreliable. Debug: (1) Check assignment logic — is the hash function correctly splitting? (2) Check for bot traffic that may be preferentially filtered in one arm. (3) Check exposure logging — is treatment exposure logged at assignment or on first page load? Different trigger points → SRM. (4) Check for session-level vs user-level assignment mismatch. Fix the bug, rerun the experiment, or use pre-experiment covariate adjustment to partially correct for the imbalance after ruling out fundamental assignment bugs.' },
    ],
  },
]

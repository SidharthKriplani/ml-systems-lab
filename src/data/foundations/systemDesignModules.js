export const SYSTEM_DESIGN_MODULES = [
  {
    id: 'design_framework',
    title: 'The 6-Step ML System Design Framework',
    subtitle: 'Clarify → scope → data → model → serving → monitoring',
    difficulty: 'foundational',
    estimatedMin: 20,
    tags: ['system design', 'framework', 'ML design interview'],
    summary: `ML system design interviews test whether you can translate a vague product requirement into a concrete, scalable architecture. A systematic framework prevents scope creep and shows seniority. The 6 steps are: (1) Clarify requirements and constraints, (2) Frame as an ML problem and scope, (3) Data strategy, (4) Model design, (5) Serving architecture, (6) Monitoring and iteration. Skipping steps — especially requirements clarification — is the most common mistake and signals junior thinking.`,
    keyPoints: [
      'Step 1 — Clarify: ask about scale (users/QPS), latency requirements, freshness needs, offline vs online, available labels, ML vs rule-based tradeoff, success metric (business vs ML metric).',
      'Step 2 — Frame as ML: define the target variable and problem type (classification, ranking, regression, generation). Supervised vs unsupervised? What are the input features? State the objective function explicitly.',
      'Step 3 — Data: identify data sources (logs, DB, user signals), labeling strategy, data volume and velocity, train/val/test split strategy, data freshness requirements, class balance, and label delay.',
      'Step 4 — Model: discuss candidate architectures for this problem type. Surface the real tradeoffs: complexity vs latency, interpretability vs accuracy, online vs batch training. Start with a baseline.',
      'Step 5 — Serving: decide online vs batch inference. Define the latency SLA. Cover caching, A/B infrastructure, model versioning and rollback, feature serving (feature store, online store), and fallback strategy.',
      'Step 6 — Monitoring: specify what to monitor (features, predictions, labels when available). Cover drift detection, alerting and runbooks, and retraining triggers and cadence.',
      'Common mistakes: jumping to model selection without clarifying requirements; ignoring latency; proposing complex models without a baseline; skipping monitoring; not asking about label availability.',
    ],
    checkQuestions: [
      { q: 'You are asked to "design a spam filter for email." What are the first 5 clarifying questions you ask?', a: '(1) Scale: how many emails per second must be classified? (100/s vs 1M/s changes architecture fundamentally). (2) Latency: does the result need to be synchronous (block sending) or async (tag after delivery)? (3) Available labels: do we have historical labelled spam/ham? How are they collected? (4) Precision vs recall: is a false positive (blocking legitimate email) or false negative (missing spam) more costly? (5) What defines spam: marketing, phishing, malware? Multiple models or one? These answers constrain every subsequent design decision.' },
    ],
  },
  {
    id: 'recsys_overview',
    title: 'Recommender System Overview',
    subtitle: 'Retrieval vs ranking vs diversity, architecture overview, data flywheel',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['recommender system', 'RecSys', 'retrieval', 'ranking'],
    summary: `Modern large-scale RecSys (Netflix, Spotify, TikTok) uses a multi-stage funnel: retrieval (candidate generation) narrows millions of items to thousands, ranking orders candidates by predicted engagement, and re-ranking applies diversity, freshness, and business logic constraints. Each stage is independently trained and monitored. The data flywheel — more users → more interaction data → better models → more engagement → more users — is the key competitive moat and the reason it is hard to displace incumbents.`,
    keyPoints: [
      'Why multi-stage: running a complex ranking model on all 10M items in under 50ms is not feasible. Retrieval is fast but imprecise (recall-focused). Ranking is precise but slow, so it must run on fewer than 1000 candidates.',
      'Retrieval goals: high recall — the relevant items must be in the candidate set for the ranker to find. Typical target is top-500 from millions in under 20ms, using approximate nearest neighbour search on learned embeddings.',
      'Ranking goals: high precision within the candidate set — correctly order the ~500 candidates by predicted engagement. Cross-feature interactions and expensive user history features are feasible here because you are making O(500) model calls, not O(10M).',
      'Re-ranking: after pure relevance ranking, apply diversity (avoid 10 identical items), freshness (inject new items), business rules (avoid out-of-stock products), and fairness constraints. Often rule-based or a lightweight model.',
      'Data flywheel: positive feedback loop — better recommendations drive more engagement, more engagement produces more behavioural data, better data builds a better model. Incumbents with this loop running are difficult to displace.',
      'Exploration vs exploitation: pure exploitation recommends what users have already shown interest in, creating a filter bubble. Exploration injects novel items to discover new preferences and collect signal on long-tail items.',
      'Cold start: new users (no history) and new items (no interactions) cannot be personalised with collaborative filtering. Strategies include onboarding questions, content-based features, popularity-based fallback, and fast online learning from initial interactions.',
    ],
    checkQuestions: [
      { q: 'TikTok shows you relevant videos even in your first session before any watch history. How?', a: `TikTok handles cold start via: (1) Context signals: device type, language, location, time of day — available immediately. (2) Content-based features: video metadata, hashtags, audio features, visual content — not dependent on user history. (3) Popularity signals: trending and viral content performs well as a fallback for new users. (4) Rapid exploitation of initial signals: the first few videos watched (even seconds of watch time) are used immediately to update the real-time user representation. TikTok's model updates the user embedding after each video, enabling quick adaptation from first session onward.` },
    ],
  },
  {
    id: 'recsys_stack',
    title: 'RecSys Stack Deep-Dive',
    subtitle: 'Candidate generation, ranking, re-ranking, diversity, A/B',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['RecSys', 'ranking', 'candidate generation', 're-ranking'],
    summary: `The full production RecSys stack integrates multiple specialised models and business logic layers, each independently trained, monitored, and updated. The stack must fit within a total latency budget (typically under 100ms end-to-end), handle cold start gracefully, serve personalised results to millions of users simultaneously, and support rapid A/B testing of any component in isolation without touching the others.`,
    keyPoints: [
      'Stage 1 — Candidate generation: multiple retrieval sources run in parallel. Collaborative filtering (user-item embeddings via ANN), content-based (item feature similarity), session-based (BERT4Rec on recent interactions), trending items, and editorial picks. Each source returns 100-500 candidates.',
      `Stage 2 — Candidate deduplication and merging: combine candidates from all sources (union), deduplicate, remove items in the user's seen or disliked history, and apply eligibility filters (available, age-appropriate). Result is roughly 1000 clean candidates.`,
      'Stage 3 — Ranking model: predict engagement probability for each candidate. Input includes user features, item features, context features, and interaction features. Architecture is typically gradient boosted trees for tabular features or a neural ranker (DCN, DLRM, MMOE for multi-task). Outputs a score per candidate.',
      'Stage 4 — Re-ranking: apply business constraints on top of relevance scores. Diversity injection (maximal marginal relevance to avoid redundancy), freshness boost, promoted content insertion, advertiser constraints, and fairness constraints. Often rule-based or a lightweight model.',
      'Stage 5 — Mixing and allocation: combine organic recommendations with ads, editorial content, and cross-surface promotions. Final slate is assembled respecting positional constraints and ad rate targets.',
      `Feedback loop management: the ranking model trains on user interactions, but those interactions are influenced by the previous ranking model's decisions (position bias, exposure bias). Counterfactual learning and IPS weighting address this.`,
      'Latency budget allocation: total budget 100ms. Candidate generation: <20ms (parallel ANN lookups). Ranking: <40ms (batch scoring 1000 items). Re-ranking and serving: <20ms. Feature retrieval: <10ms, overlapped with other stages via async fetching.',
    ],
    checkQuestions: [
      { q: 'Your ranking model has a 95ms inference latency for 1000 candidates. Total budget is 100ms. How do you bring latency down to 40ms?', a: '(1) Model compression: quantise from FP32 to INT8 — 2-4× speedup with minimal accuracy loss. Prune low-magnitude weights. (2) Architecture change: replace the neural ranker with a gradient boosted tree model (GBM is much faster for tabular features) — 10-50× speedup. Or use a smaller neural model via knowledge distillation. (3) Batch optimisation: ensure the 1000 candidates are scored in a single batched forward pass, not 1000 individual calls. (4) Hardware: move to GPU serving or a specialised inference accelerator. (5) Reduce candidate count: tighten retrieval to 300-500 candidates (accept slight recall loss to gain latency). (6) Early exit: use a fast two-stage ranker — coarse score all 1000, detailed score only top 100.' },
    ],
  },
  {
    id: 'two_tower',
    title: 'Two-Tower Models',
    subtitle: 'Architecture, dot-product vs cross-attention, embedding serving, ANN',
    difficulty: 'advanced',
    estimatedMin: 20,
    tags: ['two-tower', 'embeddings', 'ANN', 'retrieval'],
    summary: `Two-tower (dual encoder) models encode users and items independently into a shared embedding space, then use dot-product similarity for retrieval. Because item embeddings can be precomputed and indexed offline, retrieval reduces to approximate nearest neighbour (ANN) search — enabling sub-millisecond lookup across millions of items. The architecture is the foundation of large-scale retrieval at companies like Google, YouTube, and Meta.`,
    keyPoints: [
      'Architecture: user tower f_u(user_features) → u ∈ ℝ^d; item tower f_i(item_features) → v ∈ ℝ^d. Score(u,v) = uᵀv (dot product) or cosine(u,v). Both towers are typically MLPs or Transformers.',
      'Training objective: in-batch softmax (contrastive). For each (user, item) positive pair, the negatives are other items in the batch. Loss = -log[exp(uᵀv⁺)/Σⱼ exp(uᵀvⱼ)]. Hard negative mining improves representation quality significantly.',
      'Why dot product instead of cross-attention: cross-attention is more expressive, but it requires computing all (user, item) pairs jointly — O(n×m) at inference, not scalable. Dot product lets you precompute item embeddings offline and do ANN at inference time.',
      'ANN serving: precompute all item embeddings and index them (FAISS, ScaNN, HNSW). At inference, compute the user embedding in real-time (~2ms) and do an ANN lookup (~5ms). Total retrieval time is roughly 10ms for 10M items.',
      'HNSW (Hierarchical Navigable Small World): graph-based ANN with O(log n) query time and very high recall@k. Best for moderate-size indexes (fewer than 100M items). FAISS IVF-PQ uses inverted file indexing plus product quantisation and handles very large indexes with lower memory.',
      'Embedding staleness: item embeddings are computed offline. If item features change (price change, title update), embeddings must be refreshed. Delta updates recompute only changed items rather than rebuilding the full index.',
      'Limitations vs cross-attention ranking: two-tower misses feature interactions between user and item (a user who prefers cheap items requires comparing the user price preference to the item price, which needs both together). The standard solution is two-tower for retrieval and a cross-attention ranker on the retrieved candidates.',
    ],
    checkQuestions: [
      { q: 'You build a two-tower model and find that the recall@100 is 60% — only 60% of actually-clicked items are in the retrieved 100. How do you improve recall?', a: '(1) Hard negative mining: easy negatives (random items) produce weak gradients. Mine harder negatives — items that score highly for a user but were not clicked. In-batch hardest negatives or offline-mined negatives from recent ANN results. (2) Temperature tuning: the softmax temperature controls how peaked the distribution is. Lower temperature → sharper discrimination. (3) More embedding dimensions: increase d (e.g., 64→256) — more expressive representations. (4) More features: add user and item features that better capture preference (e.g., item category, user historical categories). (5) Dual softmax or temperature-corrected sampling: correct for popularity bias in negatives. (6) Expand ANN retrieved candidates (100→500) and rely on the ranker to filter.' },
    ],
  },
  {
    id: 'semantic_search',
    title: 'Semantic Search & Embeddings',
    subtitle: 'Bi-encoder vs cross-encoder, ANN indexes: FAISS/ScaNN/HNSW',
    difficulty: 'advanced',
    estimatedMin: 20,
    tags: ['semantic search', 'FAISS', 'HNSW', 'embeddings', 'retrieval'],
    summary: `Semantic search retrieves documents based on meaning rather than keyword overlap. A bi-encoder (two-tower) encodes query and document independently into a shared embedding space and measures similarity by distance. A cross-encoder jointly encodes the query-document pair for higher accuracy but cannot precompute document embeddings, making it unsuitable for first-stage retrieval at scale. ANN indexes (FAISS, HNSW, ScaNN) enable efficient nearest neighbour search in billion-scale embedding databases.`,
    keyPoints: [
      'Bi-encoder: encode query q and document d independently. Score = cosine(encode(q), encode(d)). Documents are precomputed and indexed. Fast retrieval, but less precise because there is no query-document interaction during encoding.',
      'Cross-encoder: input is [CLS] query [SEP] document [SEP] to BERT. A classification head outputs a relevance score. More precise but O(N) inference — must run for every query-document pair. Use for re-ranking retrieved candidates, not first-stage retrieval.',
      'BERT-based bi-encoders: Sentence-BERT (SBERT) adds mean pooling and contrastive loss training to BERT. E5, GTE, and BGE are state-of-the-art open-source bi-encoders for English retrieval.',
      'FAISS (Facebook AI Similarity Search): IVF (Inverted File Index) quantises documents into clusters and searches only nearby clusters. PQ (Product Quantisation) compresses embeddings to roughly 32 bytes for memory efficiency. FAISS IVF-PQ is standard for 100M+ item indexes.',
      `HNSW (Hierarchical Navigable Small World): builds a multi-layer graph with all documents at the bottom and progressively sparser long-range links at higher layers. Search starts at the top layer and greedily descends to the query's nearest neighbour. High recall@k (>0.99 achievable), sub-millisecond latency for 10M items, but high memory footprint.`,
      'Latency-recall tradeoff: more FAISS nprobe (clusters searched) = higher recall, higher latency. More HNSW ef_search = higher recall, higher latency. Calibrate for your SLA — aim for recall@100 > 0.95 at P99 latency < 10ms.',
      'Embedding quantisation: full FP32 embeddings (d=768) are 3KB per document. INT8 quantisation reduces to 0.75KB with roughly 1% recall drop. PQ (32 bytes per document) fits billions of embeddings in RAM. Quantise the index, not the model.',
    ],
    checkQuestions: [
      { q: 'You need to build a semantic search system for 50M product descriptions with P99 latency < 15ms and recall@10 > 0.95. Choose an architecture.', a: 'Architecture: bi-encoder for retrieval + cross-encoder for re-ranking. For retrieval: use a BERT-based bi-encoder (SBERT or E5) to encode all 50M product descriptions offline. For 50M items with d=256 embeddings: HNSW index (50M × 256 × 4 bytes = 50GB FP32) — fits on a 256GB index server. HNSW with ef_search=100 achieves recall@100 ≈ 0.97 at ~2ms per query. For re-ranking: run a cross-encoder on top-50 retrieved candidates to reorder by P10 precision — adds ~10ms on GPU. Total: ~12ms, within 15ms budget. For recall@10 > 0.95: retrieve top-50 with HNSW (recall@50 > 0.98), rerank to get top-10.' },
    ],
  },
  {
    id: 'ml_platform',
    title: 'ML Platform Design',
    subtitle: 'Feature store, training infra, model registry, serving infra, observability',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['ML platform', 'MLOps', 'infrastructure', 'system design'],
    summary: `An ML platform abstracts the common engineering problems — feature computation, experiment tracking, model serving — into shared, reliable infrastructure that data scientists and ML engineers can use without rebuilding from scratch. A well-designed platform reduces model development cycle time from weeks to days. The investment only makes sense at scale: for a single model with two data scientists, bespoke infrastructure is simpler. For ten models across multiple teams, a platform amortises its cost rapidly.`,
    keyPoints: [
      'Feature store: offline store (data warehouse for training data) + online store (Redis/DynamoDB for serving) + feature registry (metadata, versioning, lineage). It is the central piece — enables feature reuse and eliminates training-serving skew.',
      'Training infrastructure: distributed training on GPU clusters (Kubernetes + Kubeflow or Ray Train). Experiment tracking (MLflow, W&B). Data versioning (DVC, Delta Lake). Auto-scaling for bursty training jobs.',
      'Model registry: artifact storage (S3/GCS) + metadata (metrics, lineage) + versioning + deployment staging. Gates promotion from staging to production. Enables one-click rollback to any previous version.',
      'Serving infrastructure: real-time serving (Triton Inference Server, TorchServe, FastAPI) + batch scoring (Spark, Dataflow). Model-as-service with REST/gRPC APIs. Auto-scaling, canary deployments, blue-green deployments.',
      'Observability stack: feature monitoring (PSI, null rates), prediction monitoring (score distribution, entropy), performance monitoring (latency, throughput, error rate), and business metric monitoring. Centralised dashboards (Grafana) and alerting (PagerDuty).',
      'Retraining automation: triggered retraining (drift threshold exceeded), scheduled retraining (weekly), continuous training (online learning). Automated evaluation and promotion via CI/CD pipelines.',
      'Platform vs bespoke: build a shared platform when you have 10+ models across teams to amortise the cost. For 1-2 models, bespoke infrastructure is simpler. Platform investment typically breaks even at around 5 models with shared features.',
    ],
    checkQuestions: [
      { q: 'A startup has 3 data scientists building their first production ML model. Should they build an ML platform?', a: 'Not yet. With 3 data scientists and 1 model, the overhead of building a full ML platform (feature store, model registry, serving infra) is not justified. Start with: (1) A simple experiment tracking setup (MLflow or W&B — both have free tiers, < 1 day to set up). (2) Model serving via a simple FastAPI wrapper + Docker. (3) Feature computation in pandas/SQL, versioned in git. (4) Manual monitoring dashboards in Grafana or a simple Python script. Invest in platform infrastructure when: you have > 3 models in production, > 5 data scientists, or you are spending > 20% of engineering time on tooling rather than modelling.' },
    ],
  },
  {
    id: 'ranking_systems',
    title: 'Learning-to-Rank Systems',
    subtitle: 'Pointwise vs pairwise vs listwise, LambdaMART, position bias, online distillation',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['LTR', 'learning-to-rank', 'LambdaMART', 'position bias'],
    summary: `Learning-to-Rank (LTR) trains a model to order items by relevance, optimising ranking quality metrics (NDCG, MAP) rather than pointwise prediction accuracy. Three approaches exist: pointwise (treat each item independently), pairwise (compare pairs), and listwise (optimise the full list ordering). LambdaMART dominates tabular LTR tasks. Neural LTR using learned embeddings and cross-attention is increasingly used when feature interactions are complex.`,
    keyPoints: [
      'Pointwise LTR: predict a relevance score for each (query, item) pair independently. Loss is regression or classification on relevance labels. Simple to implement, but it ignores relative ordering — you can rank items in the wrong order even with good pointwise scores.',
      'Pairwise LTR: predict which of two items is more relevant. RankNet uses a sigmoid on score differences with binary cross-entropy loss. LambdaRank weights gradients by NDCG impact — items with larger ranking quality impact when swapped receive larger gradients.',
      'Listwise LTR: optimise a loss defined over the entire ranked list. ListNet uses cross-entropy between predicted ranking distribution and target. ListMLE maximises the likelihood of the correct full ranking. Most aligned with evaluation metrics but computationally expensive.',
      'LambdaMART: combines MART (Multiple Additive Regression Trees, equivalent to gradient boosted trees) with LambdaRank gradients. State of the art for tabular LTR — web search, ad ranking. LightGBM and XGBoost both implement it via objective="lambdarank".',
      'Position bias: users click results at higher positions more often regardless of relevance. Training on raw clicks teaches the model position effects rather than relevance. IPW (inverse propensity weighting) corrects for this by upweighting clicks at lower positions.',
      `Online distillation: train a large teacher model with expensive features offline, then distil its predictions into a smaller student model for serving. The student matches the teacher's ranking on held-out data. Expensive features drive the label generation without incurring serving latency cost.`,
      'Cascade ranking: a coarse model scores all candidates, the top-k go to a fine model. Train the coarse model to have high recall for items the fine model would rank highly. End-to-end joint optimisation of the cascade improves overall NDCG compared to training each stage independently.',
    ],
    checkQuestions: [
      { q: 'Your LTR model is trained on click data but evaluation shows much better precision@1 for bottom-ranked positions than top-ranked ones. What is the cause and fix?', a: 'This is position bias — items shown at top positions get more clicks regardless of relevance (users scan from top). Training on click labels without correcting for position teaches the model that top-positioned items are inherently more relevant — creating a feedback loop. Result: the model learns to score whatever was previously shown at position 1 higher, regardless of actual relevance. Fix: (1) Inverse Propensity Scoring — weight each (query, clicked_item, position) training example by 1/P(click|position) to correct for examination probability. (2) Randomisation experiment: occasionally serve random rankings to collect unbiased click data for training. (3) Trust lower-position clicks more (they represent stronger intent since users had to scroll).' },
    ],
  },
  {
    id: 'real_time_ml',
    title: 'Real-Time ML',
    subtitle: 'Streaming features, model latency SLAs, caching strategies, throughput batching',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['real-time ML', 'latency', 'streaming', 'caching', 'serving'],
    summary: `Real-time ML systems must deliver personalised predictions within milliseconds — fraud detection at payment time, ranking at search time, content recommendation on page load. The fundamental tension is that more features improve accuracy but increase latency, and more complex models improve accuracy but slow inference. Designing a real-time ML system means making these tradeoffs explicit and co-designing model architecture, feature engineering, and serving infrastructure together around a hard latency SLA.`,
    keyPoints: [
      'Latency budget breakdown: feature retrieval from the online store (5-10ms) + model inference (5-50ms) + network overhead (2-5ms) + serialisation (1-2ms). Total must fit within SLA: 50ms for recommendations, 10ms for fraud, 200ms for search.',
      'Feature computation strategies: pre-compute offline and serve from cache (lowest latency, highest staleness); compute on request (highest freshness, highest latency); streaming materialisation (intermediate — compute on event arrival, serve from store).',
      'Model architecture for low latency: gradient boosted trees (GBM) are fastest for tabular features (1-5ms for 1000 trees). Neural models with many layers and large embeddings are slower (20-200ms). Quantisation plus ONNX export reduces neural model latency 2-4×.',
      'Request batching: batch multiple prediction requests together for a single GPU forward pass. Batch size 32-128 increases throughput 10-50× at the cost of added latency waiting for the batch to fill. Use dynamic batching with a timeout and max size.',
      'Caching strategies: cache predictions for (user, context) combinations that are frequent and stable. TTL-based cache invalidation. The risk is stale predictions for users with recent actions — use caching for cold-start users and real-time scoring for active users.',
      'Async feature fetching: issue all feature store requests in parallel (fan-out), wait for all to complete (fan-in). Total feature retrieval time becomes max(individual feature latencies), not the sum. This is critical for meeting SLAs.',
      'Fallback strategy: if the prediction endpoint is too slow or fails, fall back to a popularity-based or rule-based response, or serve the previous cached prediction. Circuit breaker pattern: if error rate exceeds a threshold, route traffic to fallback immediately.',
    ],
    checkQuestions: [
      { q: 'Your payment fraud model has a 150ms P99 latency but the payment processing SLA requires < 50ms. What is your approach?', a: 'End-to-end latency reduction strategy: (1) Profile: break down the 150ms — feature retrieval: Xms, model inference: Yms, network: Zms. (2) Feature retrieval: ensure all features are served from a co-located Redis (< 2ms), not a remote database. Use async parallel feature fetching. (3) Model: if using a neural model, export to ONNX + TensorRT on GPU, or switch to a GBM (XGBoost/LightGBM — 5ms for 500 trees). Apply INT8 quantisation. (4) Architecture: precompute user risk scores in a streaming pipeline (Flink) updated on every transaction, cache in Redis. Serving becomes a simple cache lookup (< 5ms) + lightweight recency adjustment. (5) If none of the above achieves < 50ms: use async decision — let the transaction proceed, score in parallel, reverse if fraudulent within 30s.' },
    ],
  },
]

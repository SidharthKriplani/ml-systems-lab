export const SYSTEM_DESIGN_MODULES = [
  {
    id: 'design_framework',
    title: 'The 6-Step ML System Design Framework',
    subtitle: 'Clarify → scope → data → model → serving → monitoring',
    difficulty: 'foundational',
    estimatedMin: 20,
    tags: ['system design', 'framework', 'ML design interview'],
    summary: `Most failed ML projects don't fail on the model — they fail because the problem was never properly scoped. Engineers jump to architecture before clarifying latency requirements, label availability, or the actual business metric that matters. A junior engineer proposes a transformer for a problem that needed a decision tree, or designs offline batch scoring for a system that needs sub-50ms online inference. The 6-step framework forces the constraints to surface before any architectural decision is made: (1) Clarify requirements, (2) Frame as an ML problem, (3) Data strategy, (4) Model design, (5) Serving architecture, (6) Monitoring. Skipping to step 4 without steps 1-3 is the most common mistake in ML system design interviews — and in real projects.`,
    keyPoints: [
      `**Step 1 — Clarify: without these answers, every subsequent decision is a guess.** Ask: how many users and QPS? What is the latency requirement — synchronous (blocks the user) or asynchronous? Are labels available, and if so, with how much delay? What is the cost asymmetry between false positives and false negatives? Is this replacing a rule-based system, and does it need to beat it on day one? The answers to these questions constrain every architecture choice that follows.`,
      `**Step 2 — Frame as ML: a vague product requirement ("improve recommendations") must be translated into a concrete ML objective.** Define the target variable precisely. State the problem type: binary classification, multi-class, ranking, regression, generation. State the objective function explicitly — optimize for click-through rate, session length, revenue, or fraud recall? Misframing the objective function is the single most common cause of a model that trains well but produces no business value.`,
      `**Step 3 — Data: the model can only be as good as the labels allow.** Identify data sources (event logs, databases, user signals), confirm label availability and delay (a fraud label may arrive 30 days after the transaction), assess class balance, and plan the train/val/test split to prevent leakage. Training on future data is the most destructive form of leakage and the hardest to catch in production systems.`,
      `**Step 4 — Model: start with a baseline, not the most complex architecture.** A logistic regression or GBM baseline runs in a day and establishes a floor. Then surface the real tradeoffs for this problem: complexity vs latency, interpretability requirements, online vs batch training feasibility. A model that takes 200ms to score cannot serve recommendations at page load. The baseline forces you to understand the problem before adding complexity.`,
      `**Step 5 — Serving: online vs batch is the first architectural fork, and it changes everything downstream.** Online serving needs a feature store for low-latency feature retrieval, a hard latency SLA, a caching strategy, a fallback when the model is slow or unavailable, and an A/B infrastructure. Batch scoring needs a scheduler, an output store, and a staleness budget. Skipping the latency analysis here is how teams ship models that never reach production because they can't meet SLA.`,
      `**Step 6 — Monitoring: a deployed model has no mechanism to signal when it's failing.** Without monitoring, the first sign of degradation is a business metric moving — by then the model has been wrong for days or weeks. Specify upfront what to monitor: input feature distributions, prediction score distributions, and ground truth performance when labels arrive. Define retraining triggers — drift threshold, performance threshold, or time-based — before deployment, not after the first incident.`,
      `**Common mistakes: jumping to model selection without clarifying requirements; assuming labels are readily available when they arrive with a 14-day delay; proposing a complex architecture without a baseline; designing serving without a latency budget; shipping without a monitoring plan.** Each of these is a specific failure mode, not a general gap.`,
    ],
    checkQuestions: [
      { q: 'You are asked to "design a spam filter for email." What are the first 5 clarifying questions you ask?', a: '(1) Scale: how many emails per second must be classified? (100/s vs 1M/s changes architecture fundamentally). (2) Latency: does the result need to be synchronous (block sending) or async (tag after delivery)? (3) Available labels: do we have historical labelled spam/ham? How are they collected? (4) Precision vs recall: is a false positive (blocking legitimate email) or false negative (missing spam) more costly? (5) What defines spam: marketing, phishing, malware? Multiple models or one? These answers constrain every subsequent design decision.' },
    ],
  },
  {
    id: 'recsys_overview',
    title: 'Recommender System Overview',
    subtitle: 'Retrieval vs ranking vs diversity, architecture overview, data flywheel',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['recommender system', 'RecSys', 'retrieval', 'ranking'],
    summary: `A single ranking model cannot serve both the scale problem and the quality problem simultaneously. Running a high-quality ranking model over 10 million items in under 50ms is not computationally feasible — at even 1ms per item that's 10,000 seconds.

This is why every large-scale recommender system uses a staged funnel: retrieval (candidate generation) narrows millions to thousands using fast, approximate methods; ranking orders the candidates using expensive, precise models; re-ranking applies diversity, freshness, and business constraints on top. Each stage is a different engineering tradeoff. The data flywheel — more users produce more interaction data, which trains better models, which drives more engagement, which attracts more users — is why incumbent recommender systems are nearly impossible to displace once they reach scale.`,
    keyPoints: [
      `**Why multi-stage: the retrieval stage must be fast and high-recall — every relevant item that the ranker would surface must survive retrieval.** Missing a relevant item at retrieval means it can never be ranked, no matter how good the ranker is. The ranker must be high-precision — correctly ordering the 500 retrieved candidates — but it only needs to run 500 times, not 10 million. These are different optimization problems that require different models.`,
      `**Retrieval goals: maximize recall at a fixed candidate set size.** The target is typically top-500 from millions in under 20ms using approximate nearest neighbour (ANN) search on learned embeddings. ANN trades exact recall for speed — finding 490 of the top-500 items is good enough if the ranker can recover from small retrieval errors.`,
      `**Ranking goals: maximize precision within the retrieved candidate set.** At 500 candidates, the model can afford cross-feature interactions, expensive user history features, and slower inference — because it runs O(500) times, not O(10M). Ranking is where the real personalization happens.`,
      `**Re-ranking: a pure relevance ranker produces a list that maximizes per-item engagement but ignores the set.** Ten identical items back-to-back is technically high-relevance but terrible UX. Re-ranking applies diversity (avoid redundancy), freshness (inject new items to combat filter bubbles), business rules (suppress out-of-stock or restricted items), and fairness constraints. Often implemented as rule-based logic or a lightweight model because it runs after the expensive ranker.`,
      `**Data flywheel: the compounding advantage of incumbents.** Every recommendation served generates a feedback signal (click, skip, watch time). That signal trains better models. Better models drive more engagement. More engagement generates more signal. New entrants without interaction history cannot compete on collaborative filtering — they must rely on content-based signals until they build a user base. This is why early-stage recommender systems systematically underperform incumbents even with identical architecture.`,
      `**Exploration vs exploitation: a pure exploitation system recommends only what users have already shown interest in, shrinking their consumption set over time and starving the system of signal on new items.** Exploration injects novel items to discover new user preferences and collect signal on long-tail content. Without exploration, the system converges to a small subset of popular items — destroying long-tail coverage and eventually user satisfaction.`,
      `**Cold start: new users have no interaction history, so collaborative filtering cannot personalize.** Strategies include context signals available immediately (device type, location, time of day), content-based features that don't require history, popularity-based fallback (trending items work well for cold-start users), and rapid exploitation of the first few interactions to update the user representation within the same session.`,
    ],
    checkQuestions: [
      { q: 'TikTok shows you relevant videos even in your first session before any watch history. How?', a: `TikTok handles cold start via: (1) Context signals: device type, language, location, time of day — available immediately. (2) Content-based features: video metadata, hashtags, audio features, visual content — not dependent on user history. (3) Popularity signals: trending and viral content performs well as a fallback for new users. (4) Rapid exploitation of initial signals: the first few videos watched (even seconds of watch time) are used immediately to update the real-time user representation. TikTok's model updates the user embedding after each video, enabling quick adaptation from first session onward.` },
    ],
  },
  {
    id: 'recsys_stack',
    title: 'RecSys Stack Deep-Dive',
    subtitle: 'Candidate generation, ranking, re-ranking, diversity, A/B',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['RecSys', 'ranking', 'candidate generation', 're-ranking'],
    summary: `The gap between a working recommender prototype and a production recommender system is the full engineering stack that sits around the model. A prototype runs a ranker over a few thousand items and returns results. A production system must do this for millions of users simultaneously, within a 100ms total latency budget, with independent A/B testing for each stage, graceful fallback when any component fails, and position-bias correction to avoid the feedback loop where the ranking model learns to surface whatever the previous model already ranked highly. Each stage of the funnel — candidate generation, deduplication, ranking, re-ranking, mixing — is independently trained, monitored, and deployable. The stack is what allows a 10-person team to iterate on retrieval without touching ranking.`,
    keyPoints: [
      `**Stage 1 — Candidate generation: multiple retrieval sources run in parallel to maximize recall from different angles.** Collaborative filtering (user-item embeddings via ANN) captures interaction history. Content-based retrieval captures item-feature similarity. Session-based models (BERT4Rec on recent interactions) capture immediate intent. Trending items provide a freshness signal. Each source returns 100-500 candidates and the union ensures the ranker has broad coverage.`,
      `**Stage 2 — Candidate deduplication and merging: combine candidates from all sources, deduplicate, remove items in the user's seen or disliked history, and apply eligibility filters (in-stock, age-appropriate, geographically available).** This step reduces the union of ~2000 candidates to roughly 1000 clean candidates for the ranker. Items that pass this step but are ineligible waste ranker capacity and can produce embarrassing output.`,
      `**Stage 3 — Ranking model: predict engagement probability for each candidate using the full feature set.** Input includes user features (history, demographics), item features (category, popularity, freshness), context features (time of day, device), and interaction features (did this user engage with similar items?). Neural rankers (DCN, DLRM, MMOE for multi-task) handle feature interactions that gradient boosted trees miss. The output is a score per candidate.`,
      `**Stage 4 — Re-ranking: a list ranked purely by predicted engagement score tends to cluster similar items and show only what the user already likes.** Re-ranking applies maximal marginal relevance (diversity), freshness boosting, promoted content insertion, advertiser constraints, and fairness requirements. Getting this right prevents filter bubbles while preserving relevance — the wrong diversity algorithm makes lists feel incoherent rather than refreshing.`,
      `**Stage 5 — Mixing and allocation: the final slate combines organic recommendations, paid promotions, editorial content, and cross-surface promotions.** Position constraints, ad density targets, and brand-safety rules all apply here. This is often the stage where business logic creep accumulates — each team adds one rule at a time until the logic becomes an unmaintainable tangle that no one fully understands.`,
      `**Feedback loop management: the ranking model trains on user interactions, but those interactions are shaped by what the previous model chose to show.** A model shown at position 1 gets more clicks regardless of quality. Training on raw interaction data teaches the model to replicate position effects, not relevance. Counterfactual learning and inverse propensity score (IPS) weighting correct for this — each training example is weighted by the inverse of the probability it was shown, recovering an unbiased estimate of true relevance.`,
      `**Latency budget allocation: total budget 100ms.** Candidate generation: under 20ms (parallel ANN lookups across sources). Ranking: under 40ms (batch scoring ~1000 candidates). Re-ranking and mixing: under 20ms. Feature retrieval: under 10ms, overlapped with generation via async fetching. Any stage that blows its budget cascades — feature retrieval running 25ms instead of 10ms leaves only 15ms for ranking, which forces either fewer candidates or a simpler ranker.`,
    ],
    checkQuestions: [
      { q: 'Your ranking model has a 95ms inference latency for 1000 candidates. Total budget is 100ms. How do you bring latency down to 40ms?', a: '(1) Model compression: quantise from FP32 to INT8 — 2-4× speedup with minimal accuracy loss. Prune low-magnitude weights. (2) Architecture change: replace the neural ranker with a gradient boosted tree model (GBM is much faster for tabular features) — 10-50× speedup. Or use a smaller neural model via knowledge distillation. (3) Batch optimisation: ensure the 1000 candidates are scored in a single batched forward pass, not 1000 individual calls. (4) Hardware: move to GPU serving or a specialised inference accelerator. (5) Reduce candidate count: tighten retrieval to 300-500 candidates (accept slight recall loss to gain latency). (6) Early exit: use a fast two-stage ranker — coarse score all 1000, detailed score only top 100.' },
    ],
  },
  {
    id: 'two_tower',
    title: 'Two-Tower Models',
    subtitle: 'Architecture, dot-product vs cross-attention, embedding serving, ANN',
    difficulty: 'advanced',
    estimatedMin: 20,
    tags: ['two-tower', 'embeddings', 'ANN', 'retrieval'],
    summary: `The core retrieval problem at scale is: a cross-attention model that jointly encodes user and item would be the most accurate, but it requires computing a score for every (user, item) pair at inference time — O(n×m) operations, which is 10 billion computations for 10M items and 1000 users per second. That's not real-time retrieval, that's a data warehouse job. Two-tower models break the coupling: user and item are encoded independently into the same embedding space, and similarity is measured by dot product.

Because item embeddings don't depend on the query user, they can be precomputed offline and indexed for approximate nearest neighbour search. Retrieval becomes a 10ms operation even across 100M items.

The tradeoff is that the model cannot capture user-item feature interactions — that's left to the cross-attention ranker downstream.`,
    keyPoints: [
      `**Architecture: user tower f_u(user_features) → u ∈ ℝ^d; item tower f_i(item_features) → v ∈ ℝ^d.** Score(u,v) = uᵀv (dot product) or cosine(u,v). Both towers are typically MLPs or Transformers. The dimensionality d is a tradeoff: higher d captures more nuance but increases ANN index size and search time. d=64 is fast and memory-efficient; d=256 improves retrieval quality meaningfully; beyond d=512, returns diminish.`,
      `**Training objective: in-batch softmax (contrastive loss).** For each (user, item) positive pair in the batch, the other items in the batch serve as negatives. The model learns to push positive pairs together and push negative pairs apart. Hard negative mining — using items that score highly for a user but were not interacted with — provides stronger gradient signal and improves embedding quality significantly over random negatives.`,
      `**Why dot product instead of cross-attention: cross-attention is more expressive because it can model user-item feature interactions (a user who prefers cheap items can compare their price sensitivity to the item's price).** But cross-attention requires the user context at item encoding time, destroying the ability to precompute item embeddings. Dot product sacrifices that expressiveness for the ability to pre-index all item embeddings and do sub-millisecond lookup. Two-tower for retrieval, cross-attention for re-ranking — that's the standard split.`,
      `**ANN serving: precompute all item embeddings and index them offline (FAISS, ScaNN, HNSW).** At inference, compute the user embedding in real time (~2ms) and do an ANN lookup (~5ms). Total retrieval time is roughly 10ms for 10M items. The ANN index must be rebuilt or incrementally updated when item embeddings change — embedding staleness is an operational cost that scales with how frequently item features change.`,
      `**HNSW (Hierarchical Navigable Small World): builds a multi-layer proximity graph.** Query time is O(log n) with recall@k above 0.99 achievable for moderate index sizes (under 100M items). Memory-intensive — each item stores its graph edges. Best choice when recall is critical and RAM is available. FAISS IVF-PQ uses inverted file indexing plus product quantisation to compress embeddings to ~32 bytes each, enabling billion-scale indexes in reasonable memory at the cost of 2-5% recall.`,
      `**Embedding staleness: item embeddings are computed offline and indexed.** When item features change (price update, inventory change, title edit), the indexed embedding no longer reflects the current item. Delta updates recompute only changed items rather than rebuilding the full index. For fast-changing catalogs (e-commerce with frequent price updates), staleness is a real accuracy problem — users retrieve items at yesterday's price or with yesterday's description.`,
      `**Limitations vs cross-attention ranking: two-tower misses any feature interaction that requires seeing user and item together.** A user who prefers budget items cannot be matched to a low-price item on that dimension alone — the model doesn't know the user cares about price unless that signal is captured in the user embedding independently. The standard remedy is two-tower for retrieval (fast, approximate) and a cross-attention ranker on the retrieved candidates (slower, precise, sees full interaction).`,
    ],
    checkQuestions: [
      { q: 'You build a two-tower model and find that the recall@100 is 60% — only 60% of actually-clicked items are in the retrieved 100. How do you improve recall?', a: '(1) Hard negative mining: easy negatives (random items) produce weak gradients. Mine harder negatives — items that score highly for a user but were not clicked. In-batch hardest negatives or offline-mined negatives from recent ANN results. (2) Temperature tuning: the softmax temperature controls how peaked the distribution is. Lower temperature → sharper discrimination. (3) More embedding dimensions: increase d (e.g., 64→256) — more expressive representations. (4) More features: add user and item features that better capture preference (e.g., item category, user historical categories). (5) Dual softmax or temperature-corrected sampling: correct for popularity bias in negatives. (6) Expand ANN retrieved candidates (100→500) and rely on the ranker to filter.' },
    ],
  },
  {
    id: 'semantic_search',
    title: 'Semantic Search & Embeddings',
    subtitle: 'Bi-encoder vs cross-encoder, ANN indexes: FAISS/ScaNN/HNSW',
    difficulty: 'advanced',
    estimatedMin: 20,
    tags: ['semantic search', 'FAISS', 'HNSW', 'embeddings', 'retrieval'],
    summary: `Keyword search fails the moment the user's vocabulary doesn't match the document's vocabulary. A user searching "heart attack symptoms" misses documents that say "myocardial infarction presentation." BM25 has no concept of synonymy. Semantic search solves this by mapping queries and documents into an embedding space where meaning — not literal string overlap — determines similarity. But this creates a new engineering problem: the most accurate approach (cross-encoders that jointly process query and document) cannot scale beyond a few hundred documents per query. Bi-encoders (two-tower) enable precomputing document embeddings and doing ANN retrieval, scaling to billions of documents at the cost of some precision. The production answer is always bi-encoder for retrieval, cross-encoder for re-ranking the top results.`,
    keyPoints: [
      `**Bi-encoder: encode query and document independently.** Score = cosine(encode(q), encode(d)). Documents are precomputed and indexed offline. Fast retrieval at scale, but precision is limited because the query and document never interact during encoding — the model can't adapt its document representation based on the specific query context.`,
      `**Cross-encoder: the query and document are concatenated and jointly encoded by a transformer.** A classification head outputs a relevance score. More accurate because query-document interactions are fully modeled, but O(N) inference — must run for every query-document pair. With 50M documents and 1000 QPS, that's 50 billion inference calls per second. Cross-encoders are only feasible for re-ranking a pre-retrieved set of 50-200 candidates.`,
      `**BERT-based bi-encoders: raw BERT embeddings perform poorly for semantic similarity — BERT was trained with MLM, not similarity.** Sentence-BERT (SBERT) adds mean pooling and contrastive loss training to produce sentence embeddings that cluster by meaning. Modern bi-encoders (E5, GTE, BGE) are trained with carefully curated hard negatives and achieve much higher recall than first-generation SBERT models.`,
      `**FAISS IVF-PQ: for billion-scale indexes, exact nearest neighbour search is infeasible.** FAISS uses Inverted File indexing (IVF) to group documents into clusters and search only the nearest clusters at query time (controlled by nprobe). Product Quantisation (PQ) compresses each embedding to roughly 32 bytes for memory efficiency. The result: 1B 768-dim embeddings that would need 3TB in FP32 fit in roughly 40GB with IVF-PQ, at the cost of 2-5% recall.`,
      `**HNSW: builds a multi-layer proximity graph.** Each layer is a smaller, sparser graph; the top layer enables fast long-range navigation and the bottom layer enables precise local search. Query recall@k above 0.99 is achievable for indexes under 100M items, with sub-millisecond latency. The cost is memory — HNSW stores the graph edges alongside embeddings, roughly 2-4× the embedding storage cost. For 50M items at d=256, expect roughly 50GB for HNSW vs 12GB for flat FP32 embeddings.`,
      `**Latency-recall tradeoff: every ANN index has a parameter that trades speed for accuracy (nprobe for FAISS, ef_search for HNSW).** More search effort = higher recall, higher latency. Calibrate for your SLA: measure recall@100 and P99 latency as you sweep the parameter, then pick the operating point that meets both requirements. Calibrate on a representative query set — recall degrades more on tail queries than on head queries.`,
      `**Embedding quantisation: reducing embedding precision trades memory and speed for a small recall loss.** INT8 quantisation (FP32 → INT8) reduces memory 4× with roughly 1% recall drop. PQ reduces to 32 bytes per document with 3-5% recall drop. Quantise the index, not the model — the model must still compute high-precision query embeddings at inference time to get quality results from a quantised index.`,
    ],
    checkQuestions: [
      { q: 'You need to build a semantic search system for 50M product descriptions with P99 latency < 15ms and recall@10 > 0.95. Choose an architecture.', a: 'Architecture: bi-encoder for retrieval + cross-encoder for re-ranking. For retrieval: use a BERT-based bi-encoder (SBERT or E5) to encode all 50M product descriptions offline. For 50M items with d=256 embeddings: HNSW index (50M × 256 × 4 bytes = 50GB FP32) — fits on a 256GB index server. HNSW with ef_search=100 achieves recall@100 ≈ 0.97 at ~2ms per query. For re-ranking: run a cross-encoder on top-50 retrieved candidates to reorder by P10 precision — adds ~10ms on GPU. Total: ~12ms, within 15ms budget. For recall@10 > 0.95: retrieve top-50 with HNSW (recall@50 > 0.98), rerank to get top-10.' },
    ],
  },
  {
    id: 'ml_platform',
    title: 'ML Platform Design',
    subtitle: 'Feature store, training infra, model registry, serving infra, observability',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['ML platform', 'MLOps', 'infrastructure', 'system design'],
    summary: `Two teams building separate models both need user purchase history. One computes it in Spark, the other in Python, with slightly different null-handling and timezone logic. Their models see different feature values at training time. During serving, the real-time computation diverges further. Meanwhile, a third team is building the same feature for a third model. Each team is debugging a different version of the same thing. An ML platform solves the coordination problem: features are computed once, registered centrally, and consumed by any model.

Training pulls point-in-time correct historical features. Serving retrieves the same computation at low latency. The investment only makes sense at scale — for a single model with two data scientists, bespoke infrastructure is simpler. For ten models across multiple teams, the platform amortises its cost rapidly and the alternative becomes a coordination disaster.`,
    keyPoints: [
      `**Feature store: the central piece of ML platform infrastructure.** An offline store (data warehouse for training data with point-in-time correct joins) prevents training-serving skew. An online store (Redis or DynamoDB) serves features at low latency during inference. A feature registry provides metadata, versioning, and lineage. Without a feature store, every team reinvents the same features with subtle divergences — and debugging training-serving skew across three different implementations is a week of engineering time per incident.`,
      `**Training infrastructure: distributed training on GPU clusters (Kubernetes + Kubeflow or Ray Train) handles models that don't fit on a single machine.** Experiment tracking (MLflow, W&B) prevents the "which model version was this?" problem — losing the mapping between a deployment and its training run is how teams end up unable to reproduce their own results. Data versioning (DVC, Delta Lake) ensures that two training runs on the same code but different data produce different, identifiable artifacts.`,
      `**Model registry: artifact storage (S3/GCS) plus metadata (metrics, lineage, training data pointer) plus versioning plus deployment staging.** The registry is what enables one-click rollback. Without it, rolling back to a previous model version means finding the correct artifact manually, redoing configuration, and hoping nothing has changed in the serving environment. With it, rollback is a single API call.`,
      `**Serving infrastructure: real-time serving (Triton Inference Server, TorchServe, or FastAPI behind a load balancer) and batch scoring (Spark or Dataflow for large-scale offline inference).** Model-as-service with REST/gRPC APIs allows other teams to call the model without coupling to its implementation. Auto-scaling handles traffic spikes without manual intervention. Canary deployments and blue-green switchovers allow testing in production without full exposure — the key is defining rollback triggers upfront rather than making real-time judgment calls on degrading systems.`,
      `**Observability stack: feature monitoring (PSI, null rates, schema drift), prediction monitoring (score distribution, entropy), infrastructure monitoring (latency, throughput, error rate), and business metric monitoring.** Centralized dashboards (Grafana) with automated alerting (PagerDuty) so problems surface before a business stakeholder notices a metric drop. Without this stack, teams discover model problems from downstream business alerts — after significant damage has accumulated.`,
      `**Retraining automation: triggered retraining (drift threshold exceeded), scheduled retraining (weekly or monthly), or continuous training (online learning for fast-moving domains).** Automated evaluation and promotion via CI/CD pipelines prevent models from being deployed without validation. The most common failure mode here is manual retraining: engineers retrain on an ad hoc basis when someone complains, without consistent evaluation — some model versions are better than the previous one, some are worse, and there's no reliable way to tell.`,
      `**Platform vs bespoke: build a shared platform when you have 10+ models across teams.** For 1-2 models, the platform overhead exceeds its value. The break-even point is roughly 5 models with shared features, at which point the deduplication of feature computation, experiment tracking, and serving infrastructure starts saving more time than the platform itself costs to maintain.`,
    ],
    checkQuestions: [
      { q: 'A startup has 3 data scientists building their first production ML model. Should they build an ML platform?', a: 'Not yet. With 3 data scientists and 1 model, the overhead of building a full ML platform (feature store, model registry, serving infra) is not justified. Start with: (1) A simple experiment tracking setup (MLflow or W&B — both have free tiers, < 1 day to set up). (2) Model serving via a simple FastAPI wrapper + Docker. (3) Feature computation in pandas/SQL, versioned in git. (4) Manual monitoring dashboards in Grafana or a simple Python script. Invest in platform infrastructure when: you have > 3 models in production, > 5 data scientists, or you are spending > 20% of engineering time on tooling rather than modelling.' },
    ],
  },
  {
    id: 'ranking_systems',
    title: 'Learning-to-Rank Systems',
    subtitle: 'Pointwise vs pairwise vs listwise, LambdaMART, position bias, online distillation',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['LTR', 'learning-to-rank', 'LambdaMART', 'position bias'],
    summary: `Training a classifier to predict relevance and then sorting by score is not the same as training a ranker. A classifier optimized for per-item accuracy can assign correct absolute scores to every item individually but still produce the wrong ordering — because ranking is a relative problem, not an absolute one. Pointwise LTR misses this. Pairwise LTR fixes it for pairs but ignores that NDCG improvements at rank 1 matter far more than at rank 100. Listwise LTR optimizes the whole list but is computationally expensive and sensitive to label noise. The practical answer for tabular ranking (web search, ad ranking) is LambdaMART — gradient boosted trees with gradients derived from NDCG impact — which combines the training speed of GBMs with ranking-aware loss. The deeper problem that none of these fixes on their own is position bias: click data is contaminated by which position items were shown at, and training on raw clicks teaches the model position effects rather than relevance.`,
    keyPoints: [
      `**Pointwise LTR: predict a relevance score for each (query, item) pair independently using regression or classification.** Simple to implement and fast to train, but it ignores relative ordering. A model that assigns scores of [0.9, 0.8, 0.7] to items [A, B, C] ranks them correctly. A model that assigns [0.6, 0.5, 0.4] — same relative order but lower absolute values — is equally good by pointwise loss but may differ by pairwise loss depending on thresholds. Pointwise LTR is the floor, not the target.`,
      `**Pairwise LTR: instead of predicting absolute relevance, predict which of two items is more relevant.** RankNet uses sigmoid on score differences with binary cross-entropy loss. LambdaRank extends this by weighting each pair's gradient by the change in NDCG if those two items were swapped — a swap at rank 1 vs rank 2 receives a much larger gradient than a swap at rank 98 vs rank 99. This makes the training signal NDCG-aware without requiring NDCG to be differentiable.`,
      `**Listwise LTR: optimize a loss defined over the entire ranked list.** ListNet uses cross-entropy between predicted and target ranking distributions. ListMLE maximizes the likelihood of the correct full ranking. These are the most aligned with evaluation metrics but computationally expensive (require considering all items for each training example) and sensitive to noisy or incomplete relevance labels.`,
      `**LambdaMART: the production workhorse for tabular LTR.** It combines gradient boosted trees (MART — Multiple Additive Regression Trees) with LambdaRank gradients. At each boosting step, each item's gradient is the sum of LambdaRank gradients over all pairs involving that item, weighted by NDCG impact. Result: a GBM that directly optimizes a ranking objective. LightGBM and XGBoost both implement it via objective="lambdarank". Outperforms neural LTR on pure tabular features with significantly lower serving latency.`,
      `**Position bias: users click results at higher positions more often, regardless of relevance.** Position 1 gets 10× the clicks of position 10 on a typical search result page — not because position-1 results are 10× more relevant, but because users examine positions in order and stop once they find something satisfactory. Training on raw clicks teaches the model to reproduce this position bias rather than learning true relevance. The ranker learns to score items that were previously at position 1 highly — a self-reinforcing loop.`,
      `**Inverse Propensity Weighting (IPW): correct for position bias by weighting each training example by 1/P(click|position, not_relevant) — the inverse of the probability that the position alone caused the click.** Items shown at position 1 get a low weight (position explains most of their clicks); items shown at position 5 get a high weight (their clicks represent genuine relevance signal). IPW requires estimating P(click|position), which is done via randomization experiments or EM-based propensity estimation.`,
      `**Online distillation: training a large teacher model offline with expensive features (cross-attention, user full history), then distilling its ranking predictions into a smaller student model for serving.** The student matches the teacher's ranking on held-out data without requiring the expensive features at inference time. The teacher's expensive features drive the label quality without incurring serving latency — allowing the model quality of a slow model with the serving speed of a fast one.`,
    ],
    checkQuestions: [
      { q: 'Your LTR model is trained on click data but evaluation shows much better precision@1 for bottom-ranked positions than top-ranked ones. What is the cause and fix?', a: 'This is position bias — items shown at top positions get more clicks regardless of relevance (users scan from top). Training on click labels without correcting for position teaches the model that top-positioned items are inherently more relevant — creating a feedback loop. Result: the model learns to score whatever was previously shown at position 1 higher, regardless of actual relevance. Fix: (1) Inverse Propensity Scoring — weight each (query, clicked_item, position) training example by 1/P(click|position) to correct for examination probability. (2) Randomisation experiment: occasionally serve random rankings to collect unbiased click data for training. (3) Trust lower-position clicks more (they represent stronger intent since users had to scroll).' },
    ],
  },
  {
    id: 'real_time_ml',
    title: 'Real-Time ML',
    subtitle: 'Streaming features, model latency SLAs, caching strategies, throughput batching',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['real-time ML', 'latency', 'streaming', 'caching', 'serving'],
    summary: `A fraud detection model that returns results in 500ms is worthless — the transaction either completes in 200ms or the user abandons. A recommendation model that takes 2 seconds to load the page has already lost the session. Real-time ML has a hard constraint that batch ML does not: the prediction must be available before the user's patience runs out. Every additional feature improves accuracy but adds latency. Every additional model layer improves accuracy but adds latency. Designing a real-time ML system is an exercise in making these tradeoffs explicit with numbers — not "this adds some latency" but "this adds 15ms and the SLA is 50ms, so we have 35ms left for everything else." The fundamental challenge is that the people who build models optimize for accuracy and the people who build serving infrastructure optimize for latency, and they are often different people with different incentives.`,
    keyPoints: [
      `**Latency budget breakdown: every millisecond must be accounted for before deployment, not discovered in production.** A typical 50ms fraud detection budget: feature retrieval from online store (5-10ms), model inference (10-20ms), network overhead (2-5ms), serialization (1-2ms). If the model alone takes 40ms, there is no room for feature retrieval. Profile the end-to-end system before committing to a model architecture, not after.`,
      `**Feature computation strategies: pre-computed offline features served from cache (lowest latency: 1-2ms, highest staleness: hours to days); computed on request (highest freshness, highest latency: 20-100ms for complex features); streaming materialization (intermediate: computed on event arrival, served from store at 2-5ms, staleness measured in seconds).** The choice depends on how much the feature changes and how much stale values hurt model accuracy.`,
      `**Model architecture for low latency: gradient boosted trees (GBM) are the fastest option for tabular features — 1-5ms for 1000 trees on CPU.** Neural models with many layers and large embeddings are slower (20-200ms on CPU, 5-20ms on GPU). Quantization plus ONNX export reduces neural model latency 2-4×. For fraud at 10ms SLA, you almost always need GBM. For recommendations at 50ms SLA, a small neural model is feasible.`,
      `**Request batching: batch multiple prediction requests together for a single GPU forward pass.** Batch size 32-128 increases throughput 10-50× at the cost of added latency waiting for the batch to fill. Dynamic batching with a timeout (wait up to 5ms for more requests, then fire regardless of batch size) balances throughput and latency. This is how GPU serving stays economical at high QPS — individual requests are cheap, but batching is what makes GPU utilization high enough to justify the cost.`,
      `**Caching strategies: cache predictions for (user, context) combinations that are stable and frequently repeated.** TTL-based invalidation: set TTL equal to how long the prediction remains valid, not how long you want to save compute. The trap is serving stale predictions to users who just took an action that should change their score — a fraud model should not cache the "low risk" score for a user who just failed verification. Use caching for cold-start users and real-time scoring for recently active users.`,
      `**Async feature fetching: issue all feature store requests in parallel (fan-out), wait for all to complete (fan-in).** Total feature retrieval time is max(individual latencies), not their sum. Four features each taking 8ms in parallel cost 8ms total; in serial they cost 32ms. Async fan-out is non-negotiable for meeting latency SLAs when features come from multiple sources. Implement with a timeout: if one feature is slow, don't block on it — serve a default value rather than exceeding the latency SLA.`,
      `**Fallback strategy: every real-time ML system must have a documented response to "what happens when the model endpoint is slow or unavailable?" Without a predefined fallback, engineers improvise under pressure during incidents and consistently make it worse.** Define the fallback before deployment: popularity-based response, rule-based scoring, or the previous cached prediction. Circuit breaker pattern: if error rate exceeds a threshold for 30 seconds, route all traffic to the fallback immediately without waiting for individual requests to time out.`,
    ],
    checkQuestions: [
      { q: 'Your payment fraud model has a 150ms P99 latency but the payment processing SLA requires < 50ms. What is your approach?', a: 'End-to-end latency reduction strategy: (1) Profile: break down the 150ms — feature retrieval: Xms, model inference: Yms, network: Zms. (2) Feature retrieval: ensure all features are served from a co-located Redis (< 2ms), not a remote database. Use async parallel feature fetching. (3) Model: if using a neural model, export to ONNX + TensorRT on GPU, or switch to a GBM (XGBoost/LightGBM — 5ms for 500 trees). Apply INT8 quantisation. (4) Architecture: precompute user risk scores in a streaming pipeline (Flink) updated on every transaction, cache in Redis. Serving becomes a simple cache lookup (< 5ms) + lightweight recency adjustment. (5) If none of the above achieves < 50ms: use async decision — let the transaction proceed, score in parallel, reverse if fraudulent within 30s.' },
    ],
  },
]

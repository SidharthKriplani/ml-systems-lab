export const CLASSICAL_ML_MODULES = [
  {
    id: 'linear_regression',
    title: 'Linear Regression from First Principles',
    subtitle: 'OLS, normal equation, geometric interpretation, assumptions',
    difficulty: 'foundational',
    estimatedMin: 20,
    tags: ['regression', 'OLS', 'linear models'],
    summary: `Linear regression models the expected output as a linear function of inputs: ŷ = Xθ. The Ordinary Least Squares (OLS) estimator minimises the sum of squared residuals ‖y−Xθ‖². The closed-form solution is θ̂ = (XᵀX)⁻¹Xᵀy (normal equation). OLS has optimal properties under the Gauss-Markov theorem: among unbiased linear estimators, it has minimum variance.`,
    keyPoints: [
      'Normal equation θ̂ = (XᵀX)⁻¹Xᵀy: derived by setting ∂‖y−Xθ‖²/∂θ = −2Xᵀ(y−Xθ) = 0. Computationally O(nd² + d³) — expensive when d is large.',
      'Geometric interpretation: ŷ = Xθ̂ is the orthogonal projection of y onto the column space of X. Residuals e = y − ŷ are orthogonal to all columns of X: Xᵀe = 0.',
      'Gauss-Markov theorem: if errors have zero mean, constant variance, and are uncorrelated (not necessarily normal), OLS is BLUE — Best Linear Unbiased Estimator.',
      'OLS assumptions: (1) linearity, (2) no perfect multicollinearity (XᵀX invertible), (3) homoscedasticity (constant error variance), (4) no endogeneity (Cov(X,ε)=0), (5) normality for inference.',
      'Multicollinearity: when features are nearly linearly dependent, (XᵀX) is near-singular. θ̂ has high variance — small data changes cause large coefficient swings. Ridge regression is the fix.',
      'R² = 1 − SS_res/SS_tot ∈ [0,1]: fraction of variance explained. Adjusted R² penalises extra features: 1 − (1−R²)(n−1)/(n−p−1). Adding any feature increases R².',
      'Heteroscedasticity: non-constant error variance. OLS is still unbiased but no longer BLUE; standard errors are wrong. Fix: White\'s robust standard errors or WLS.',
    ],
    checkQuestions: [
      { q: 'Why is it problematic to invert XᵀX numerically, and what is the preferred computational approach?', a: 'Forming XᵀX squares the condition number: if cond(X)=κ, then cond(XᵀX)=κ². This amplifies numerical errors. The preferred approach is QR decomposition of X: X=QR, then θ̂=R⁻¹Qᵀy. For very large systems, iterative solvers (conjugate gradient) or SVD-based pseudoinverse are used. sklearn\'s LinearRegression uses SVD by default.' },
    ],
  },
  {
    id: 'logistic_regression',
    title: 'Logistic Regression',
    subtitle: 'Sigmoid, cross-entropy loss, decision boundary, calibration',
    difficulty: 'foundational',
    estimatedMin: 20,
    tags: ['classification', 'logistic regression', 'calibration'],
    summary: `Logistic regression models P(y=1|x) = σ(wᵀx + b) where σ(z) = 1/(1+e^{-z}). The log-odds (logit) is linear in x: log P(y=1)/P(y=0) = wᵀx + b. Training minimises cross-entropy loss, which is equivalent to MLE under the Bernoulli likelihood. Despite the name, logistic regression is a classification model and is well-calibrated by design.`,
    keyPoints: [
      'Sigmoid σ(z) = 1/(1+e^{-z}) ∈ (0,1). σ\'(z) = σ(z)(1−σ(z)) — smooth derivative enables gradient descent. Saturates for |z|>>0 (vanishing gradient).',
      'Cross-entropy loss: L = −Σᵢ [yᵢ log ŷᵢ + (1−yᵢ) log(1−ŷᵢ)]. Gradient wrt logit zᵢ: ∂L/∂zᵢ = ŷᵢ − yᵢ. Elegant — same form as linear regression gradient.',
      'Decision boundary: P(y=1|x) = 0.5 ⟺ wᵀx + b = 0. This is a hyperplane in feature space — logistic regression is a linear classifier.',
      'Multinomial (Softmax) regression: for K classes, P(y=k|x) = exp(wₖᵀx)/Σⱼ exp(wⱼᵀx). Cross-entropy loss generalises naturally.',
      'Calibration: logistic regression output probabilities are well-calibrated when the model is correct. After regularisation, minor miscalibration can occur — Platt scaling recalibrates.',
      'L2 regularisation for logistic regression: adds λ‖w‖² to loss. Equivalent to MAP with Gaussian prior. Prevents overfitting when features > samples.',
      'Multicollinearity affects coefficient interpretation but not predictions. Unlike linear regression, coefficients in logistic regression are log-odds ratios — interpretation is conditional on other features.',
    ],
    checkQuestions: [
      { q: 'Why does logistic regression fail when classes are perfectly linearly separable?', a: 'When data is perfectly separable, the MLE does not exist: the log-likelihood is maximised as ‖w‖→∞, driving the sigmoid output to exactly 0 or 1 on training data. Gradient descent diverges — weights grow without bound. Solutions: L2 regularisation creates a finite optimal ‖w‖; or use a Bayesian approach with a proper prior.' },
    ],
  },
  {
    id: 'regularization',
    title: 'Regularisation Geometry',
    subtitle: 'L1 vs L2 geometry, Lasso sparsity, Ridge shrinkage, elastic net',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['regularisation', 'L1', 'L2', 'Lasso', 'Ridge'],
    summary: `Regularisation adds a penalty to the loss to prevent overfitting. L2 (Ridge) adds λ‖w‖² — shrinking all weights toward zero proportionally. L1 (Lasso) adds λ‖w‖₁ — inducing exact sparsity by pushing some weights to exactly zero. The geometric insight: minimising loss subject to ‖w‖_p ≤ r has constraint boundaries that are round (L2) or diamond-shaped (L1), with the diamond's corners creating sparsity.`,
    keyPoints: [
      'Ridge (L2): θ̂_ridge = (XᵀX + λI)⁻¹Xᵀy. Always invertible (λ>0), even with multicollinearity. Shrinks all coefficients by factor 1/(1+λ·1/σₖ²) where σₖ are singular values.',
      'Lasso (L1): no closed form — requires coordinate descent or LARS. At the L1 ball\'s corners (coordinate axes), exactly k coefficients are non-zero. Performs automatic feature selection.',
      'Geometric intuition: minimise loss s.t. ‖w‖_p ≤ r. The loss contours are ellipses centred at OLS solution. The constraint boundary for L1 is a diamond — the "corner" of the diamond intersects the constraint boundary first, making the optimal solution sparse.',
      'When L1 wins over L2: when the true model is sparse — only a few features matter and you want automatic selection. Interpretable, sparse solutions for high-dimensional data (genomics, NLP bag-of-words).',
      'When L2 wins over L1: when many features contribute small but meaningful effects (dense signal). L2 has a closed-form solution, is differentiable everywhere, and handles correlated features better (Lasso arbitrarily picks one).',
      'Elastic net: α‖w‖₁ + (1−α)‖w‖₂². Combines sparsity (L1) with stability for correlated features (L2). Preferred in practice when both properties are desired.',
      'Regularisation as prior: λ_L2 ↔ Gaussian prior N(0,1/λ), λ_L1 ↔ Laplace prior. Elastic net ↔ normal-Laplace mixture prior.',
    ],
    checkQuestions: [
      { q: 'You have 200 samples and 500 features. Many features are likely irrelevant. Which regulariser do you choose and why?', a: 'L1 (Lasso) is preferred: it performs automatic feature selection by driving irrelevant feature weights to exactly zero, giving a sparse interpretable model. With n=200 << p=500, sparsity is a reasonable assumption and helps generalisation. If features are highly correlated, elastic net is better than pure Lasso, as Lasso arbitrarily picks among correlated features.' },
    ],
  },
  {
    id: 'generalization',
    title: 'Generalisation Theory',
    subtitle: 'Bias-variance, VC dimension, PAC learning, double descent',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['bias-variance', 'VC dimension', 'overfitting', 'double descent'],
    summary: `Generalisation measures how well a model performs on unseen data. The bias-variance decomposition splits expected MSE into: bias² (systematic error from underfitting) + variance (sensitivity to training data) + irreducible noise. Simpler models have high bias, low variance; complex models have low bias, high variance. The classical U-shaped test error curve is now known to be incomplete — overparameterised models exhibit double descent.`,
    keyPoints: [
      'Bias-variance decomposition: E[(y−ŷ)²] = Bias²(ŷ) + Var(ŷ) + σ². Bias = E[ŷ]−y (systematic). Variance = E[(ŷ−E[ŷ])²] (sensitivity to data).',
      'VC dimension: maximum number of points any arrangement of which the hypothesis class can shatter (label in all 2ⁿ ways). Linear classifiers in ℝᵈ: VC-dim = d+1.',
      'PAC learning bound: generalisation gap ≤ √(VC-dim·log(n) + log(1/δ)) / n. More expressive model → higher VC-dim → worse bound.',
      'Regularisation as variance reduction: reduces effective model complexity → lowers variance at cost of increased bias. The bias-variance tradeoff is controlled by regularisation strength.',
      'Double descent: test error drops as model size increases to interpolation threshold (zero training error), then rises (classical overfitting), then DECREASES again for very overparameterised models. Contradicts the classical bias-variance story.',
      'Double descent explanation: highly overparameterised models have many solutions with zero training error. Gradient descent with small LR finds the minimum-norm solution (implicit bias), which has low variance due to benign interpolation.',
      'Inductive bias: assumptions baked into the model architecture that guide generalisation (e.g., CNNs assume translation invariance, Transformers assume permutation equivariance in attention). Matching inductive bias to data structure is more important than just minimising training loss.',
    ],
    checkQuestions: [
      { q: 'A model achieves 99% training accuracy and 75% test accuracy. What does this tell you about bias and variance, and what would you do?', a: 'High training accuracy + much lower test accuracy = low bias (model fits training data well) + high variance (doesn\'t generalise — sensitive to the specific training set). Remedies: (1) More training data — reduces variance fastest. (2) Stronger regularisation (L2, dropout). (3) Simpler model architecture. (4) Ensemble methods (bagging reduces variance). Increasing model capacity would worsen the problem.' },
    ],
  },
  {
    id: 'trees',
    title: 'Decision Trees',
    subtitle: 'Information gain, Gini, pruning, depth-accuracy tradeoff',
    difficulty: 'foundational',
    estimatedMin: 20,
    tags: ['decision trees', 'Gini', 'information gain'],
    summary: `Decision trees recursively partition the feature space with axis-aligned splits, selecting the feature and threshold that maximally reduce impurity. Impurity is measured by Gini index G = 1 − Σ pₖ² or information gain (entropy reduction). Trees are interpretable, handle mixed types naturally, and require minimal preprocessing — but they overfit badly without pruning or ensemble methods.`,
    keyPoints: [
      'Splitting criterion: at each node, find feature j and threshold t that minimise weighted child impurity. Gini: G = 1−Σpₖ². Entropy: H = −Σpₖlog pₖ. Both give similar splits in practice.',
      'Greedy algorithm: splits are chosen locally optimal at each node — not globally optimal. The globally optimal tree is NP-hard to find.',
      'Depth vs accuracy: shallow trees underfit; deep trees overfit. Optimal depth is a hyperparameter tuned via CV. Trees can memorise training data: a leaf per sample achieves 100% training accuracy.',
      'Handling categorical features: trees split on subsets of categories (exponential search) — common implementations use one-hot encoding or optimal ordinal encoding for speed.',
      'Pruning: cost-complexity pruning (sklearn default) adds penalty α×(number of leaves) to error. Grow full tree, then prune back leaves that increase this penalised error.',
      'Feature importance from trees: Gini importance = total impurity decrease weighted by node sample fraction. Biased toward high-cardinality features — permutation importance is more reliable.',
      'Trees vs linear models: trees excel with non-linear, interaction effects and mixed types. Linear models win for extrapolation and when true relationship is approximately linear.',
    ],
    checkQuestions: [
      { q: 'Why do decision trees have high variance, and how does this motivate random forests?', a: 'Trees are unstable: small changes in training data can produce very different tree structures (different top splits propagate down). This is high variance. Random forests reduce variance by averaging many trees trained on bootstrap samples (bagging), with additional randomisation via feature subsampling at each split. The average of many high-variance, low-bias estimators has much lower variance while preserving low bias. Bias-variance: each tree has low bias; averaging reduces variance without increasing bias.' },
    ],
  },
  {
    id: 'random_forest',
    title: 'Random Forests',
    subtitle: 'Bagging, OOB error, feature importance, hyperparameter sensitivity',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['random forest', 'bagging', 'ensemble'],
    summary: `Random forests train an ensemble of decorrelated decision trees using bagging (bootstrap aggregating) and random feature subsampling. At each split, only a random subset of m ≤ d features are considered — reducing correlation between trees. The final prediction averages all trees (regression) or takes majority vote (classification). OOB (out-of-bag) samples not used in each tree\'s bootstrap provide a free validation set.`,
    keyPoints: [
      'Bagging: train T trees on T bootstrap samples (sample n points with replacement). Each bootstrap sample uses ~63.2% of data; remaining ~36.8% are OOB.',
      'Feature subsampling at each split: m = √d for classification, m = d/3 for regression (sklearn defaults). Decorrelates trees — key to variance reduction beyond simple bagging.',
      'Variance reduction: for T trees each with variance σ², pairwise correlation ρ: ensemble variance = ρσ² + (1−ρ)σ²/T. As T→∞, variance → ρσ². Decorrelation (low ρ) is critical.',
      'OOB error: for each sample, average predictions from trees that did NOT use it in training. Approximates CV without extra computation — free model validation.',
      'Variable importance: (1) Gini importance: mean decrease in impurity (fast, biased toward high-cardinality). (2) Permutation importance: mean decrease in OOB accuracy when feature is shuffled — unbiased. SHAP is most reliable.',
      'Hyperparameter sensitivity: n_estimators (more is always better, diminishing returns after ~200), max_features (most impactful — tune this), max_depth (usually left as None — pruned via min_samples_leaf), min_samples_leaf.',
      'Parallelisable: each tree is independent — embarrassingly parallel. Training time scales linearly with n_estimators.',
    ],
    checkQuestions: [
      { q: 'A random forest with 500 trees takes too long to train. What do you tune first, and what accuracy tradeoff do you expect?', a: 'Reduce n_estimators first (e.g., 100) — the OOB error curve shows diminishing returns past ~100-200 trees. Accuracy loss is typically small (<1%). Next, reduce max_features (faster splits) or set max_depth. Using parallel computation (n_jobs=-1) is the first step if compute is available. For very large datasets, subsample without replacement (max_samples parameter).' },
    ],
  },
  {
    id: 'gradient_boosting',
    title: 'Gradient Boosting & XGBoost',
    subtitle: 'Residual fitting, shrinkage, XGBoost regularisation, early stopping',
    difficulty: 'intermediate',
    estimatedMin: 25,
    tags: ['gradient boosting', 'XGBoost', 'LightGBM', 'ensemble'],
    summary: `Gradient boosting builds an additive model F(x) = Σₜ αₜhₜ(x) where each tree hₜ fits the negative gradient (pseudo-residuals) of the loss from the current ensemble. Unlike random forests, trees are added sequentially with each one correcting the errors of its predecessors. XGBoost adds second-order Taylor expansion of the loss, explicit regularisation on tree structure, and efficient split finding — making it the dominant tabular ML algorithm.`,
    keyPoints: [
      'Algorithm: initialise F₀ = argmin_γ Σ L(yᵢ,γ). For t=1..T: compute pseudo-residuals rᵢ = −∂L/∂F(xᵢ). Fit tree hₜ to {rᵢ}. Update F_t = F_{t-1} + η·hₜ.',
      'For MSE loss, pseudo-residuals are actual residuals. For log-loss, they are (yᵢ − ŷᵢ) — same form as logistic regression gradient. Gradient boosting generalises to any differentiable loss.',
      'Shrinkage (learning rate η < 1): scales each tree\'s contribution. Smaller η → better generalisation (more regularisation) but requires more trees. Typical range: 0.01–0.3.',
      'Depth in boosting vs RF: boosting trees are typically shallow (depth 3-8) — deep trees overfit the residuals. RF trees are often grown to full depth. Shallow trees in boosting are interpretable "stumps" at the extreme.',
      'XGBoost objective: Σ L(yᵢ,ŷᵢ) + Σₜ Ω(hₜ) where Ω(h) = γT + (λ/2)Σⱼ wⱼ². T = number of leaves, wⱼ = leaf weights. L2 on leaf weights shrinks predictions toward zero.',
      'XGBoost split finding: uses second-order Taylor: L(y, ŷ+f) ≈ L(y,ŷ) + g·f + (h/2)·f². Optimal weight wⱼ* = −Gⱼ/(Hⱼ+λ). Exact algorithm or approximate histogram method for speed.',
      'Early stopping: monitor validation loss; stop after k rounds with no improvement. Essential for finding optimal n_estimators without exhaustive grid search.',
    ],
    checkQuestions: [
      { q: 'XGBoost is overfitting the training set. List 5 hyperparameters you would tune and the direction of each change.', a: '(1) Lower learning_rate (e.g., 0.3→0.05) — more regularisation, slower learning. Increase n_estimators to compensate. (2) Reduce max_depth (e.g., 6→3) — shallower trees reduce variance. (3) Increase min_child_weight (e.g., 1→10) — prevents splits on small groups. (4) Add subsample < 1.0 (e.g., 0.8) — stochastic gradient boosting. (5) Add colsample_bytree < 1.0 (e.g., 0.8) — feature subsampling like RF. Also increase lambda (L2 on leaf weights).' },
    ],
  },
  {
    id: 'ensembles',
    title: 'Ensemble Methods',
    subtitle: 'Bagging vs boosting vs stacking, diversity principle',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['ensembles', 'stacking', 'bagging', 'boosting'],
    summary: `Ensemble methods combine multiple models to reduce overall prediction error. Bagging reduces variance by averaging high-variance low-bias models. Boosting reduces bias by sequentially fitting models to residuals. Stacking (stacked generalisation) learns how to combine diverse base models using a meta-learner. The diversity principle: ensemble gains are largest when constituent models make different errors.`,
    keyPoints: [
      'Bagging: train models on bootstrap samples; aggregate by averaging/voting. Reduces variance without increasing bias. Works best with high-variance base learners (deep trees).',
      'Boosting: sequential, reweight misclassified samples (AdaBoost) or fit residuals (gradient boosting). Reduces bias. Works best with weak learners (shallow trees).',
      'Stacking: train diverse base models on training data; use their OOF predictions as features for a meta-learner. OOF (out-of-fold) prevents leakage into meta-features.',
      'Diversity principle: for ensemble to help, models must disagree (make different errors). Identical models averaged = one model. Sources of diversity: different algorithms, different features, different training subsets, different random seeds.',
      'Simple averaging: robust baseline for stacking. Weighted averaging: if base models have unequal quality. Geometric mean: for probability outputs (avoids overconfidence).',
      'When stacking is worth it: typically 0.5-1% lift over best single model in competitions. In production: complexity cost (N models to maintain, N×latency) often outweighs gain. Use when marginal improvement is business-critical.',
      'Snapshot ensembles: for deep learning, save model checkpoints at different points in training (cyclical LR) and ensemble them — cheap diversity.',
    ],
    checkQuestions: [
      { q: 'Your stacking ensemble is overfitting. The meta-learner has very high training accuracy but low CV accuracy. What is the likely cause and fix?', a: 'Likely cause: data leakage in meta-features. If base models were trained on the full training set and their training-set predictions were used as meta-features, the meta-learner sees predictions that are optimistically good (base models have memorised labels). Fix: use out-of-fold (OOF) predictions — each base model is trained on k-1 folds and predicts on fold k. Meta-features are thus generated from held-out data, preventing leakage.' },
    ],
  },
  {
    id: 'svm',
    title: 'Support Vector Machines',
    subtitle: 'Maximum-margin hyperplane, kernel trick, soft margin',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['SVM', 'kernel', 'margin', 'dual'],
    summary: `SVMs find the maximum-margin hyperplane separating two classes. The margin is 2/‖w‖, maximised by minimising ‖w‖² subject to yᵢ(wᵀxᵢ+b) ≥ 1. Only support vectors (points on or inside the margin) determine the solution. The kernel trick replaces xᵢᵀxⱼ with k(xᵢ,xⱼ) = φ(xᵢ)ᵀφ(xⱼ), enabling non-linear decision boundaries without explicit feature mapping.`,
    keyPoints: [
      'Hard-margin SVM: min ½‖w‖² s.t. yᵢ(wᵀxᵢ+b) ≥ 1. Only feasible when data is linearly separable. Dual: max Σαᵢ − ½Σᵢⱼ αᵢαⱼyᵢyⱼxᵢᵀxⱼ s.t. αᵢ≥0, Σαᵢyᵢ=0.',
      'Soft-margin SVM: introduces slack ξᵢ ≥ 0. min ½‖w‖² + C·Σξᵢ. C controls tradeoff: large C = smaller margin, fewer violations (overfitting risk). C is the key hyperparameter.',
      'Kernel trick: in the dual, xᵢᵀxⱼ appears only in dot products → replace with k(xᵢ,xⱼ). The model learns a non-linear boundary without computing φ(x) explicitly.',
      'Common kernels: linear k(x,x\')=xᵀx\'. Polynomial k(x,x\')=(xᵀx\'+r)^d. RBF (Gaussian) k(x,x\')=exp(−γ‖x−x\'‖²). RBF is infinite-dimensional feature space, universal approximator.',
      'Support vectors: the training points with αᵢ > 0 (on or violating the margin). Decision function depends only on these — sparse representation. Number of SVs indicates model complexity.',
      'Hinge loss: max(0, 1−yᵢf(xᵢ)). The SVM objective with soft-margin is exactly hinge loss + L2 regularisation. This connects SVM to logistic regression (log-loss instead of hinge).',
      'Practical considerations: scale features before SVM (RBF kernel uses distances — sensitive to scale). SVMs don\'t scale well to large datasets (O(n²)-O(n³)). For large n, prefer linear SVM (liblinear) or SGD with hinge loss.',
    ],
    checkQuestions: [
      { q: 'Your SVM with RBF kernel underfits the training data. What do you adjust?', a: 'Increase C (allow fewer margin violations, fit more aggressively) and/or increase γ (RBF bandwidth — higher γ means tighter, more localised kernels that fit training data more closely but generalise less). The dual issue: if already overfitting with high C, decrease γ to get smoother decision boundaries. Grid search over (C, γ) on log scale is standard: C ∈ {0.01, 0.1, 1, 10, 100}, γ ∈ {0.001, 0.01, 0.1, 1, 10}.' },
    ],
  },
  {
    id: 'knn',
    title: 'K-Nearest Neighbours',
    subtitle: 'Distance metrics, curse of dimensionality, ANN indexes',
    difficulty: 'foundational',
    estimatedMin: 15,
    tags: ['KNN', 'distance metrics', 'ANN'],
    summary: `KNN classifies a new point by majority vote among its k nearest training points (using some distance metric). KNN is a non-parametric, lazy learner — it stores all training data and does no learning during training. Decision time is O(nd) per query for brute force. KNN performance degrades severely in high dimensions (curse of dimensionality) and benefits from approximate nearest neighbour (ANN) indexes for large datasets.`,
    keyPoints: [
      'Distance metrics: Euclidean ‖x−x\'‖₂ (assumes isotropic, scale-sensitive), Manhattan ‖x−x\'‖₁ (robust to outliers), Cosine similarity (scale-invariant, good for text/embeddings).',
      'Bias-variance in k: k=1 → 0 training error, high variance (overfit). Large k → smoother decision boundary, higher bias. Tune k via CV.',
      'Curse of dimensionality: in high dimensions, all points become approximately equidistant. Nearest neighbour is nearly as far as the farthest neighbour — distance loses discriminative power. Beyond ~20 dims, KNN degrades.',
      'Weighted KNN: weight votes by 1/distance — closer neighbours matter more. Reduces sensitivity to k choice.',
      'ANN indexes for large-scale KNN: FAISS (Facebook), ScaNN (Google), HNSW (Hierarchical Navigable Small World graphs). Trade small accuracy loss for O(log n) or sublinear query time.',
      'KNN in feature space vs embedding space: raw features often mislead distance; KNN on learned embeddings (from contrastive learning) is powerful for recommendation, semantic search.',
      'KNN for missing data imputation: replace missing value with weighted average of k nearest complete neighbours — a simple, often effective imputation strategy.',
    ],
    checkQuestions: [
      { q: 'Why does KNN fail in 1000 dimensions even with millions of training points?', a: 'Curse of dimensionality: the volume of a d-dimensional unit sphere shrinks relative to the unit cube as d increases. Almost all points concentrate near the surface of the sphere. For a test point, the distance to its nearest neighbour approaches the distance to its farthest neighbour — the ratio converges to 1. The k nearest neighbours are no longer geometrically "near" the query point, so the locality assumption underlying KNN breaks down. Dimensionality reduction or learned metric/embedding is needed.' },
    ],
  },
  {
    id: 'naive_bayes',
    title: 'Naïve Bayes',
    subtitle: 'Independence assumption, Gaussian NB, Laplace smoothing',
    difficulty: 'foundational',
    estimatedMin: 15,
    tags: ['Naive Bayes', 'independence', 'text classification'],
    summary: `Naïve Bayes classifiers apply Bayes\' theorem with the "naïve" assumption that all features are conditionally independent given the class: P(x|y) = Π P(xⱼ|y). Despite this strong and usually false assumption, NB is fast, works well with small data and high dimensions, and is particularly effective for text classification where the independence assumption is violated but feature-class associations still hold.`,
    keyPoints: [
      'Prediction: ŷ = argmax_k P(y=k) Π_j P(xⱼ|y=k). Using log-probabilities: argmax_k [log P(y=k) + Σⱼ log P(xⱼ|y=k)] — avoids underflow.',
      'Gaussian NB: P(xⱼ|y=k) = N(μⱼₖ, σⱼₖ²). Fit per-class mean and variance from training data. Good for continuous features.',
      'Multinomial NB: P(xⱼ|y=k) ∝ count of feature j in class k. Standard for text — each word is a feature, counts are frequencies.',
      'Laplace smoothing: add α=1 to all counts before normalising. Prevents P(unseen word|class) = 0 from zeroing out the entire product. MAP interpretation: Dirichlet prior with α.',
      'Conditional independence is almost always violated in practice, yet NB often achieves competitive accuracy. Why: classification requires only the right ranking of P(y|x), not accurate probability estimates. NB posteriors are miscalibrated but rankings are often correct.',
      'NB advantages: O(nd) training (just count statistics), O(d) prediction, handles missing features naturally (just omit term), works with very little data, highly interpretable.',
      'Complement NB: for text, model P(x|y≠k) and predict class with minimum complement score. Often better than standard NB for text imbalanced data.',
    ],
    checkQuestions: [
      { q: 'In Multinomial NB for spam detection, a test email contains the word "win" which appears in 0% of spam emails in training. Without Laplace smoothing, what happens?', a: 'P("win"|spam) = 0. Since NB multiplies all feature probabilities, the entire product P(x|spam) = 0, regardless of all other words in the email. The email can never be classified as spam. This is the zero-frequency problem. Laplace smoothing adds α=1 to all word counts: P("win"|spam) = (0+1)/(N_spam + V) where V is vocabulary size — small but non-zero, preserving the contribution of other features.' },
    ],
  },
  {
    id: 'calibration',
    title: 'Model Calibration',
    subtitle: 'Reliability diagrams, ECE, Platt scaling, isotonic regression',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['calibration', 'ECE', 'Platt scaling', 'reliability'],
    summary: `A model is well-calibrated if its predicted probability ŷ matches the empirical frequency of the positive class: P(y=1|ŷ=p) ≈ p. Uncalibrated models can be overconfident (probabilities near 0 and 1) or underconfident (clustered near 0.5). Calibration matters when model outputs are used as probabilities in downstream decisions — risk scoring, stacking, cost-sensitive threshold tuning.`,
    keyPoints: [
      'Reliability diagram (calibration curve): bin predictions by confidence level; plot mean predicted probability vs observed frequency per bin. Perfect calibration = diagonal line.',
      'Expected Calibration Error (ECE): weighted average of |accuracy_b − confidence_b| across bins, weighted by bin size. Scalar summary of calibration error.',
      'Overconfidence: tree-based models and neural networks without regularisation tend to output extreme probabilities. Underconfidence: SVMs with Platt scaling, some NB models.',
      'Platt scaling: fit logistic regression on validation set: P(y=1|f) = σ(af+b) where f is model output. Simple, fast, works well when miscalibration is monotone.',
      'Isotonic regression: non-parametric monotone calibration. Fits a non-decreasing step function to (score, label) pairs. More flexible than Platt but needs more validation data to avoid overfitting.',
      'Temperature scaling: for neural networks, divide logits by scalar T before softmax. T>1 makes distribution smoother (lower confidence), T<1 sharper. Single parameter, fast to fit, works well in practice.',
      'When calibration matters: (1) Stacking — miscalibrated base models mislead the meta-learner. (2) Decision thresholding — need calibrated P to set optimal threshold. (3) Medical/risk applications where probabilities are interpreted by humans.',
    ],
    checkQuestions: [
      { q: 'A Random Forest predicts P(y=1) = 0.9 for many samples, but only 60% of those samples are actually positive. What calibration technique would you apply?', a: 'The model is overconfident — predictions near 0.9 correspond to actual frequency of 0.6. Apply Platt scaling or isotonic regression on a held-out calibration set. Isotonic regression is preferred here if enough calibration data is available (~1000+ samples) because the miscalibration may be non-monotone. After calibration, a reliability diagram should show points near the diagonal. Validate calibration on a separate test set — never on the calibration set used to fit the calibrator.' },
    ],
  },
  {
    id: 'class_imbalance',
    title: 'Class Imbalance',
    subtitle: 'SMOTE, threshold tuning, class weights, precision@K',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['imbalance', 'SMOTE', 'precision@K', 'threshold'],
    summary: `Class imbalance occurs when one class is far more frequent than others (e.g., 99:1 fraud vs. non-fraud). Standard classifiers trained to minimise accuracy will predict the majority class almost always. Solutions fall into three categories: resampling the training data (SMOTE, undersampling), adjusting the decision threshold, or using cost-sensitive learning with class weights.`,
    keyPoints: [
      'Never use accuracy as the metric for imbalanced problems. 99% accuracy on 1% positive rate = always predicting negative. Use precision, recall, F1, AUC-ROC, PR-AUC, or business metrics.',
      'Class weights: set class_weight proportional to inverse frequency. sklearn\'s class_weight="balanced" computes w_k = n/(K·nₖ). Effectively reweights the loss function.',
      'SMOTE: Synthetic Minority Over-sampling Technique. For each minority sample, generate synthetic samples along line segments to k nearest minority neighbours. Avoids pure duplication.',
      'Undersampling: randomly remove majority class samples. Loses information but speeds training. Tomek links: remove majority samples that are nearest neighbours of minority samples — cleans decision boundary.',
      'Threshold tuning: model outputs P(y=1|x). Default threshold=0.5 assumes balanced costs. Lower threshold → higher recall, lower precision. Set threshold to optimise business metric (e.g., F_β with β>1 weights recall).',
      'Precision@K: among K highest-confidence positive predictions, what fraction are correct? Relevant for ranking use cases (fraud alerts, medical prioritisation) where you act on a fixed budget of alerts.',
      'In practice: class weights are the first thing to try (no data modification, free). SMOTE for tree-based models often hurts (trees handle imbalance natively with class weights). Threshold tuning is always needed at deployment.',
    ],
    checkQuestions: [
      { q: 'You build a fraud detector with 0.1% fraud rate. Your model achieves 99.5% AUC-ROC but the ops team says too many false positives. What do you change?', a: 'High AUC-ROC but too many false positives = the threshold is too low for the cost structure. Action: (1) Optimise threshold on Precision-Recall curve — find the threshold that achieves acceptable precision at sufficient recall. (2) Switch metric to Precision@K — if you can only review K alerts/day, you want the top-K predictions to be as precise as possible. (3) Check if PR-AUC is also high — PR-AUC is more informative than ROC-AUC for very imbalanced data and directly captures the precision-recall tradeoff.' },
    ],
  },
  {
    id: 'feature_selection',
    title: 'Feature Selection & Dimensionality',
    subtitle: 'Filter/wrapper/embedded, mutual information, RFE, SHAP',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['feature selection', 'mutual information', 'SHAP', 'RFE'],
    summary: `Feature selection reduces dimensionality by removing irrelevant or redundant features, improving generalisation, reducing training time, and aiding interpretability. Methods fall into three families: filter (rank features independently of the model), wrapper (search feature subsets using model performance), and embedded (selection as part of model training). With modern gradient boosting and neural nets, embedded methods (SHAP, L1) are most practical.`,
    keyPoints: [
      'Filter methods: evaluate features independently using statistical tests. Mutual information I(X;y) is most general (captures non-linear dependencies). ANOVA F-test and chi-squared for categorical targets.',
      'Correlation-based filter: remove features with |corr(X,y)| below threshold. Fast but misses non-linear relationships. Also remove features with high inter-feature correlation (multicollinearity).',
      'Wrapper methods: Recursive Feature Elimination (RFE) trains model, removes lowest-importance feature, repeats. Captures feature interactions but O(d) model fits — expensive.',
      'Embedded methods: L1 regularisation drives irrelevant feature weights to zero during training. Decision tree Gini importance. SHAP feature importance post-hoc.',
      'SHAP-based selection: train model once, compute mean |SHAP value| per feature, select top-k. Captures interaction effects correctly (unlike marginal importance). Most reliable in practice.',
      'Variance threshold: remove features with near-zero variance — they cannot be predictive. Constant features (var=0) are always removed first.',
      'Forward selection: start with empty set, add feature with best CV improvement. Backward elimination: start with all features, remove worst. Stepwise combines both. Computationally expensive but considers interactions.',
    ],
    checkQuestions: [
      { q: 'You have 500 features and want to reduce to ~50 before tuning a Random Forest. What is your approach?', a: 'Step 1: remove zero/near-zero variance features and any with >50% missing. Step 2: remove highly correlated pairs (|corr|>0.95) — keep one. Step 3: train an initial Random Forest with all remaining features; compute permutation importance or SHAP values. Select top-50 features by importance. Step 4: verify via CV that the 50-feature model achieves similar performance to the full model. Avoid pure filter methods for RF as they miss interactions; embedded SHAP/permutation importance is preferred.' },
    ],
  },
]

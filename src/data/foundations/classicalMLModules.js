export const CLASSICAL_ML_MODULES = [
  {
    id: 'linear_regression',
    title: 'Linear Regression from First Principles',
    subtitle: 'OLS, normal equation, geometric interpretation, assumptions',
    difficulty: 'foundational',
    estimatedMin: 28,
    tags: ['regression', 'OLS', 'linear models'],
    summary: `We need to predict a continuous output — price, revenue, risk — from input features, and the simplest useful assumption is that the relationship is linear: ŷ = θᵀx. Finding θ means minimising prediction error, and MSE is the natural choice — it is differentiable everywhere and its gradient sets up a clean closed-form solution: θ̂ = (XᵀX)⁻¹Xᵀy. That formula is exact and fast for small problems, but it has two failure modes. First, scale: inverting XᵀX costs O(d³), which breaks past roughly 10,000 features — gradient descent or iterative solvers are the practical alternatives at scale. Second, collinearity: when features are correlated, XᵀX becomes near-singular, small data changes flip coefficient signs, and individual weights lose all meaning even when predictions stay accurate. Ridge regularisation fixes collinearity by adding λI to the diagonal before inversion, guaranteeing a stable solution for any λ > 0. Before trusting any coefficient or p-value, plot the residuals — if there is a pattern there, the Gaussian noise assumption that underlies every standard error is violated.`,
    keyPoints: [
      `OLS minimises ‖y−Xθ‖² by setting the gradient to zero, yielding θ̂ = (XᵀX)⁻¹Xᵀy. The cost is O(nd² + d³), so direct inversion breaks past roughly d=10,000 features. At that scale, gradient descent or conjugate gradient solve the same problem iteratively without ever forming XᵀX explicitly.`,
      `Geometrically, ŷ = Xθ̂ is the orthogonal projection of y onto the column space of X — the closest point in that subspace to y. The residuals satisfy Xᵀe = 0, meaning they are perpendicular to every feature column. This is a structural consequence of the solution, not an assumption you verify after the fact.`,
      `The Gauss-Markov theorem guarantees OLS is BLUE — Best Linear Unbiased Estimator — when errors are zero-mean, uncorrelated, and homoscedastic. Normality is not required for the coefficient estimates; it only matters for the exact distribution of test statistics on small samples. With large n, the CLT gives asymptotically valid confidence intervals regardless.`,
      `Multicollinearity makes XᵀX near-singular: a tiny perturbation in a single training point produces massive swings in coefficient estimates — the model has high variance in its parameter space even when prediction accuracy looks fine. Predictions can remain accurate because the ill-conditioned directions average out, but individual coefficients become meaningless and unstable. Ridge adds λI to the diagonal, which directly stabilises the inversion at the same time as it shrinks the estimates.`,
      `R² = 1 − SS_res/SS_tot always increases when you add a feature, even a column of pure noise, because any predictor can explain at least a small fraction of the variance by chance. Adjusted R² penalises feature count, but cross-validation on a holdout is more reliable than both for deciding whether a new feature actually helps.`,
      `Heteroscedasticity — non-constant error variance — leaves OLS coefficients unbiased but makes their standard errors wrong, invalidating every t-test and confidence interval. The model still predicts, but you cannot do inference. White's robust standard errors correct the standard errors without changing the coefficients; weighted least squares down-weights high-variance observations during estimation.`,
      `Minimising squared error is identical to maximum likelihood under Gaussian errors — the two framings reach exactly the same θ̂. That Gaussian assumption is what squares residuals rather than taking absolute values. When residuals are heavy-tailed, L1 loss (least absolute deviations) targets the conditional median instead and is more robust to outliers, at the cost of losing the closed-form solution.`,
      `Residual diagnostics must happen before interpreting any output: plot residuals versus fitted values to check linearity and homoscedasticity, use a QQ-plot to check whether the Gaussian assumption is plausible, and plot residuals versus each predictor to catch non-linearities the model missed. A systematic pattern in any of these means your inference is wrong even if your predictions look fine.`,
    ],
    checkQuestions: [
      {
        q: `Why is it problematic to invert XᵀX numerically, and what is the preferred computational approach?`,
        a: `Forming XᵀX squares the condition number: if cond(X)=κ, then cond(XᵀX)=κ². For κ=10⁶ (not unusual in real data), cond(XᵀX)=10¹² — near the limit of double precision (~10¹⁶), meaning 4 significant digits are lost. The preferred approach: QR decomposition X=QR gives θ̂=R⁻¹Qᵀy, working with κ(X) directly. For very large systems: SVD-based pseudoinverse (sklearn default) or iterative solvers (conjugate gradient). sklearn's LinearRegression uses SVD and never fails due to exact multicollinearity — it computes the minimum-norm solution automatically.`
      },
      {
        q: `Your linear regression model has R²=0.95 but the residuals show a clear U-shape when plotted against fitted values. What does this mean?`,
        a: `A U-shaped residual pattern signals systematic non-linearity — the model is consistently underpredicting at low and high fitted values, and overpredicting in the middle. This violates the linearity assumption, meaning the model is misspecified. High R² does not validate the model — R² measures how much variance is explained but not whether the functional form is correct. Remedies: (1) Add polynomial terms (x², x³) or spline basis. (2) Apply a non-linear transformation to the response (log(y) for count/skewed data). (3) Use a non-parametric model (GAM, tree-based). Recheck residuals after each modification.`
      },
      {
        q: `You have two features with correlation 0.99. What happens to the linear regression coefficients, and how do Ridge and Lasso behave differently on this input?`,
        a: `With correlation 0.99, XᵀX is near-singular. OLS coefficients are extremely unstable — a small change in any training point causes large swings in both coefficients. Ridge regression (XᵀX + λI)⁻¹Xᵀy: adds λ to all eigenvalues, stabilising the inversion; Ridge splits the coefficient weight roughly equally between the two correlated features — both get non-zero, similar-magnitude coefficients. Lasso: arbitrarily picks one of the two correlated features (whichever has slightly larger correlation with the residual) and drives the other to exactly zero — the selected feature can change dramatically across different training sets. Ridge is safer when both correlated features genuinely matter.`
      },
      {
        q: `Gauss-Markov says OLS is BLUE. What exactly does "Best Linear Unbiased" mean, and what does it NOT guarantee?`,
        a: `"Unbiased" means E[θ̂] = θ (correct on average across all possible datasets). "Linear" means the estimator is a linear function of y: θ̂ = Cy for some matrix C. "Best" means minimum variance among all linear unbiased estimators. What GM does NOT guarantee: (1) OLS is not best among non-linear or biased estimators — Ridge (biased) can have lower MSE = Bias² + Variance. (2) OLS does not give minimum prediction error on new data — overfitting can make test MSE much higher. (3) BLUE requires the GM assumptions (no endogeneity, homoscedasticity) — violating them makes OLS non-optimal.`
      },
    ],
    takeaway: `OLS gives you the best linear unbiased estimates when errors are well-behaved, but two things kill it silently: collinear features make coefficients meaningless while predictions stay accurate, and non-constant error variance makes every standard error wrong while coefficients stay unbiased. Plot the residuals before you trust anything.`,
    interactiveId: 'linear_regression_viz',
  },
  {
    id: 'logistic_regression',
    title: 'Logistic Regression',
    subtitle: 'Sigmoid, cross-entropy loss, decision boundary, calibration',
    difficulty: 'foundational',
    estimatedMin: 28,
    tags: ['classification', 'logistic regression', 'calibration'],
    summary: `Linear regression predicts continuous outputs, but when the target is binary — fraud or not, click or not — we need probabilities bounded between 0 and 1. Applying linear regression directly produces outputs outside that range and has no natural stopping point. The fix is to model the log-odds linearly: log[P(y=1)/P(y=0)] = wᵀx + b, then invert through the sigmoid to get P(y=1|x) = σ(wᵀx + b). Training maximises the Bernoulli log-likelihood, which is equivalent to minimising cross-entropy. The key insight in the gradient is that sigmoid and cross-entropy combine to cancel the saturation term — the gradient at each step is simply (ŷ − y), exactly the same clean structure as linear regression, making optimisation straightforward. One new failure mode appears: perfectly separable data causes weights to diverge to infinity because the MLE keeps improving as weights grow. L2 regularisation prevents this by creating a finite optimal point for any λ > 0.`,
    keyPoints: [
      `The sigmoid σ(z) = 1/(1+e^{-z}) squashes any real-valued logit into (0,1), making it a probability. Its derivative σ'(z) = σ(z)(1−σ(z)) approaches zero near the extremes — gradients vanish when the model is highly confident, whether it is right or wrong. This is the saturation problem that cross-entropy is designed to solve.`,
      `Logistic regression models log-odds linearly: wᵀx + b = log[P(y=1)/P(y=0)]. Each weight wⱼ is the change in log-odds per unit increase in feature j — not the change in probability. A weight of 2.0 means the odds ratio multiplies by e², not that probability doubles. Interpreting coefficients on the probability scale requires exponentiating and accounting for the sigmoid's non-linearity.`,
      `Using MSE with sigmoid would produce gradient (ŷ−y)·σ'(z) — the σ'(z) term near-vanishes when the model is confidently wrong, causing learning to stall. Cross-entropy eliminates this: its gradient with respect to the logit z is exactly ŷ−y. The chain-rule cancellation between the 1/σ(z) in the log-likelihood and the σ'(z) in the chain rule produces the clean form. This is why cross-entropy is the only correct loss for sigmoid outputs.`,
      `The decision boundary is the hyperplane where P(y=1|x) = 0.5, i.e. wᵀx + b = 0. Logistic regression is a linear classifier — every decision surface it can produce is flat. Non-linear boundaries require explicit feature engineering (polynomial features, interactions) or a non-linear model.`,
      `When the model is correctly specified, logistic regression produces well-calibrated probabilities: a prediction of 0.7 corresponds to approximately 70% empirical positive rate in that bin. Random forests and SVMs lose this property without post-hoc calibration because they do not optimise a proper scoring rule during training.`,
      `Perfectly linearly separable data has no finite MLE solution. For any hyperplane that correctly classifies all training points, scaling up ‖w‖ always increases the log-likelihood — the supremum is at infinity. Gradient descent will diverge. L2 regularisation introduces a competing penalty λ‖w‖² that makes the optimal weight magnitude finite, ensuring a unique solution for any λ > 0.`,
      `Softmax extends the binary case to K classes: P(y=k|x) = exp(wₖᵀx)/Σⱼ exp(wⱼᵀx). Binary logistic regression is exactly the K=2 special case. One class is treated as the reference and the other K−1 sets of weights are learned relative to it.`,
      `In sklearn, C = 1/λ controls L2 regularisation strength — larger C means weaker regularisation, allowing larger weights. The default C=1 is a reasonable starting point, but when features outnumber samples (p >> n) strong regularisation (small C, e.g. 0.01) is needed. Always tune C via cross-validation rather than feel, because the right value is data-dependent.`,
    ],
    checkQuestions: [
      {
        q: `Why does logistic regression fail when classes are perfectly linearly separable, and how does L2 regularisation fix it?`,
        a: `When data is linearly separable, there exists a hyperplane wᵀx + b = 0 that correctly classifies all training points. The MLE maximises Σ log σ(yᵢ(wᵀxᵢ+b)). For correctly classified points, yᵢ(wᵀxᵢ+b) > 0 and σ → 1 as the product grows. The log-likelihood increases indefinitely as ‖w‖→∞ — there is no maximum, just a supremum at infinity. Gradient descent will diverge. L2 regularisation adds λ‖w‖² to the loss, creating a finite optimal ‖w‖*: the benefit of further increasing ‖w‖ is outweighed by the L2 penalty. The solution exists and is unique for any λ > 0.`
      },
      {
        q: `Your logistic regression model predicts P(y=1) = 0.8 for a sample. A reliability diagram shows that samples with predicted probability 0.8 are actually positive only 55% of the time. What is wrong and how do you fix it?`,
        a: `The model is overconfident — it outputs 0.8 but the empirical frequency is only 0.55. This can happen when the model is regularised too strongly (logits are shrunk below their optimal values), when the training data is imbalanced, or when the model is misspecified. Fix: apply Platt scaling — fit a logistic regression on a separate calibration set: P(y=1|f) = σ(af+b) where f is the original model's logit output. Or use isotonic regression for more flexible non-parametric recalibration. Both require a separate calibration set — never the training set. Validate on a held-out test set using ECE after calibration.`
      },
      {
        q: `How does the gradient of binary cross-entropy with sigmoid differ from MSE with linear output? What is the significance of this difference?`,
        a: `MSE + linear: ∂L/∂z = (ŷ−y) where ŷ = z (no saturation). Cross-entropy + sigmoid: ∂L/∂z = σ(z)−y = ŷ−y (same form). The significance: if we used MSE with sigmoid, we would get ∂L/∂z = (σ(z)−y)·σ(z)(1−σ(z)) — the σ(z)(1−σ(z)) term nearly vanishes when the model is confidently wrong (e.g., σ(z)≈0 when y=1), causing vanishing gradients. Cross-entropy eliminates this: the σ'(z) in the chain rule cancels with the 1/σ(z) in the log-likelihood gradient, giving the clean prediction-error gradient. This is why cross-entropy, not MSE, is the correct loss for classification with sigmoid/softmax outputs.`
      },
      {
        q: `You train logistic regression with C=1 (sklearn default, where C=1/λ). Your model underfits. What does C control and what value would you try next?`,
        a: `In sklearn, C = 1/λ is the inverse of regularisation strength. C=1 means λ=1 — moderate regularisation. Underfitting (high training error, high test error) means the model is too regularised: the L2 penalty is too strong and is pulling weights toward zero, preventing the model from fitting the training data. To reduce regularisation: increase C (e.g., try C=10, C=100). This allows larger weight magnitudes and a more complex decision boundary. Also check: are the features well-scaled? Logistic regression with L2 is scale-sensitive — standardise features first. Is the problem actually non-linear? If so, add polynomial features or switch to a non-linear model.`
      },
    ],
    takeaway: `Sigmoid and cross-entropy were designed together: cross-entropy's gradient cancels the saturation from sigmoid's derivative, giving a clean (ŷ−y) update that never stalls. Use MSE instead and your gradients vanish whenever the model is confidently wrong — exactly when you most need learning to continue.`,
    interactiveId: 'logistic_regression_viz',
  },
  {
    id: 'regularization',
    title: 'Regularisation Geometry',
    subtitle: 'L1 vs L2 geometry, Lasso sparsity, Ridge shrinkage, elastic net',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['regularisation', 'L1', 'L2', 'Lasso', 'Ridge'],
    summary: `OLS overfits when features outnumber samples or when features are correlated — in both cases the model uses spurious patterns in the training data that do not generalise. The simplest fix is to constrain how large the weights can grow. Adding a penalty on weight magnitude to the loss function does exactly this: the optimiser now trades off fitting the data against keeping weights small, using the same gradient descent that was causing overfitting to now balance both objectives. L2 (Ridge) penalises squared weight magnitude — it shrinks all weights smoothly toward zero and has a closed-form solution. L1 (Lasso) penalises absolute weight magnitude — it drives some weights to exactly zero, performing automatic feature selection. The geometry explains why Lasso sparsifies while Ridge does not: in the constrained formulation, L1 has a diamond-shaped boundary with corners on the coordinate axes, so expanding loss ellipses almost always hit a corner first and zero out the corresponding weight. L2 has a smooth sphere with no corners, so the contact point almost never lies on an axis.`,
    keyPoints: [
      `Ridge adds λI to the diagonal of XᵀX before inversion: θ̂_ridge = (XᵀX + λI)⁻¹Xᵀy. Near-singular XᵀX was the problem — multicollinear features made the smallest eigenvalues near zero, causing instability. Adding λ pushes all eigenvalues above λ, making inversion numerically stable for any λ > 0, even with perfectly correlated features.`,
      `In the eigenbasis of XᵀX, Ridge scales the k-th component by σₖ²/(σₖ²+λ). Directions with small singular values — the low-signal, high-noise directions — are shrunk most aggressively. Ridge is performing a soft rank truncation: it down-weights the directions where the data provides the weakest evidence, which are exactly the directions where overfitting is worst.`,
      `Lasso has no closed-form solution because the L1 norm is not differentiable at zero — the usual gradient trick fails. Coordinate descent solves it by cycling through one weight at a time and applying a soft-thresholding operator that sets the weight to exactly zero when the gradient is too small to overcome the L1 penalty. This is why Lasso can zero weights and Ridge cannot: the soft-threshold has a dead zone at the origin; the Ridge gradient always points away from it.`,
      `The geometry of sparsity: expand loss ellipses outward from the OLS solution until they touch the constraint boundary. The L1 diamond has corners on the coordinate axes — an ellipse hits a corner almost always, zeroing out the corresponding weight. The L2 sphere is smooth — the contact point almost never falls on an axis, so Ridge almost never zeros a weight exactly.`,
      `Lasso is the right choice when the true signal is sparse — a few features genuinely matter, most do not, and you want an interpretable result. Its known failure mode is correlated features: Lasso arbitrarily picks one from a correlated group and zeros the rest, and which one it picks can change across training runs. Elastic net adds an L2 term that stabilises correlated groups while preserving sparsity.`,
      `Ridge is the right choice when many features each contribute a small effect — dense signal — or when correlated features should be kept together because they each carry partially independent information. Ridge never zeros a weight exactly, so it does not perform feature selection; if you need both stability and sparsity, elastic net is the answer.`,
      `Elastic net: αλ‖w‖₁ + (1−α)λ‖w‖₂² runs L1 and L2 simultaneously. The L2 term introduces a grouping effect — correlated features receive similar non-zero coefficients rather than one being arbitrarily selected — while the L1 term still drives irrelevant features to zero. This is why elastic net is standard in genomics and high-dimensional text: correlated predictors should stay together, but the total number of non-zero predictors should still be small.`,
      `The regularisation path traces how each coefficient changes as λ varies from 0 (OLS) to ∞ (all zeros). For Lasso the path is piecewise linear — LARS traces it exactly in O(dp) time. For Ridge it is smooth and can be computed analytically. The path reveals which features are most robustly predictive (they enter first as λ decreases from ∞) and guides λ selection beyond what cross-validation alone shows.`,
    ],
    checkQuestions: [
      {
        q: `You have 200 samples and 500 features. Many features are likely irrelevant. Which regulariser do you choose and why?`,
        a: `L1 (Lasso) is preferred: it performs automatic feature selection by driving irrelevant feature weights to exactly zero, giving a sparse interpretable model. With n=200 << p=500, the problem is underdetermined (infinitely many OLS solutions). Sparsity is a reasonable assumption and helps generalisation. If features are highly correlated, elastic net is better than pure Lasso — Lasso arbitrarily picks among correlated features, while elastic net applies the grouping effect. Start with Lasso (sklearn's LogisticRegression with penalty='l1'), then try elastic net if Lasso's selected features are unstable across cross-validation folds.`
      },
      {
        q: `Explain geometrically why Lasso produces sparse solutions but Ridge does not, using the constrained optimisation formulation.`,
        a: `Constrained form: minimise loss(w) subject to ‖w‖_p ≤ r. The loss L(w) has elliptical level curves centred at θ̂_OLS. We want the point on the constraint boundary closest to θ̂_OLS. L1 constraint: ‖w‖₁ ≤ r is a diamond (in 2D) with corners at (r,0), (0,r), (−r,0), (0,−r). As we expand the ellipse from θ̂_OLS outward, it first hits a corner of the diamond where one coordinate is exactly zero. L2 constraint: ‖w‖₂ ≤ r is a circle (sphere in high-D) with no corners — the ellipse hits the sphere at a smooth point, almost never on an axis, so almost no coefficient is exactly zero.`
      },
      {
        q: `A Lasso model selects 10 features, but when you rerun with a different random seed (for data splitting), it selects a completely different 10 features. What does this mean and what should you do?`,
        a: `This instability signals that many non-selected features are nearly equally predictive and correlated with the 10 selected ones. Lasso's feature selection is unstable when multiple features share similar predictive power — it arbitrarily picks one from each correlated group. Remedies: (1) Switch to elastic net (grouping effect stabilises correlated feature selection). (2) Use stability selection: run Lasso on many bootstrap subsamples, keep only features selected in > 80% of runs — these are the truly stable predictors. (3) Use permutation importance or SHAP on a Random Forest to identify the genuinely important features without assuming sparsity.`
      },
    ],
    takeaway: `Ridge and Lasso both fight overfitting by penalising weight magnitude, but they differ in what they do to zero: Ridge shrinks toward it but stops; Lasso crosses it exactly. The geometry explains why — a diamond has corners, a sphere does not. When you need automatic feature selection, that corner is the whole point.`,
    interactiveId: 'regularization_viz',
  },
  {
    id: 'generalization',
    title: 'Generalisation Theory',
    subtitle: 'Bias-variance, VC dimension, PAC learning, double descent',
    difficulty: 'advanced',
    estimatedMin: 32,
    tags: ['bias-variance', 'VC dimension', 'overfitting', 'double descent'],
    summary: `Training accuracy is trivially achievable — a model with enough capacity will memorise every training point and reach 100% accuracy while being useless on new data. The real question is why some models generalise and others do not. The bias-variance decomposition gives the answer: expected test error decomposes into irreducible noise (a floor you cannot model away), systematic error from wrong assumptions (bias), and sensitivity to which training examples you happened to see (variance). These two trade off: simpler models underfit but are stable; complex models fit tightly but are brittle. The cure for one makes the other worse. Classical theory says test error follows a U-curve as complexity grows — and that is real, but incomplete. Overparameterised models exhibit double descent: test error rises at the interpolation threshold (where the model first memorises all training data), then falls again as the model grows massively larger. Gradient descent finds the minimum-norm interpolating solution among infinitely many that achieve zero training error, and those happen to be the smoother functions that generalise better.`,
    keyPoints: [
      `Bias-variance decomposition: E[(y−ŷ)²] = σ² + Bias²(ŷ) + Var(ŷ). The σ² term is irreducible — it is noise in the data itself that no model can remove. Bias is systematic error: the model consistently misses in the same direction because its assumptions are wrong. Variance is instability: change the training set slightly and the model changes substantially, because it learned noise rather than signal.`,
      `Every regularisation decision is a move along the bias-variance curve. Increasing λ in Ridge regression shrinks weights toward zero — that introduces bias (the model can no longer fit the true pattern exactly) but reduces variance (coefficient estimates become stable across different training sets). Cross-validation over λ finds the specific complexity level where this tradeoff is best for your dataset size.`,
      `VC dimension quantifies how expressive a hypothesis class is: it is the maximum number of points the class can label arbitrarily. Linear classifiers in ℝᵈ have VC-dim = d+1 — every new feature you add expands the class's expressiveness and increases overfitting risk. The PAC bound formalises this: test error ≤ train error + √(VC-dim·log(n)/n), so more features require proportionally more data to keep the generalisation gap small.`,
      `Inductive bias is what an architecture assumes before seeing any data. CNNs assume translation invariance — the same feature detector should work anywhere in the image. Transformers assume any two tokens can be directly related regardless of distance. RNNs assume local temporal structure matters most. Matching inductive bias to the actual structure in the data often matters more than model scale — a CNN trained on text learns something much harder than a Transformer would.`,
      `Double descent breaks the classical U-curve: test error rises near the interpolation threshold — where the model first has exactly enough capacity to fit all training data — then falls again in the massively overparameterised regime. Near the threshold, the model must fit the data with minimum slack, producing high-variance solutions. With far more parameters, many interpolating solutions exist and gradient descent finds a specific one.`,
      `Gradient descent has an implicit regularisation effect: among all the infinitely many zero-training-error solutions in an overparameterised model, SGD converges toward the minimum-norm one. Minimum-norm interpolating solutions correspond to smoother functions in the RKHS sense — they use the smallest effective model complexity needed to fit the data, which is why they generalise better than the same parameters trained by an explicit maximum-margin objective.`,
      `Test set hygiene is non-negotiable: any decision informed by test set performance — choosing an architecture, tuning a hyperparameter, selecting a feature — turns the test set into a validation set and you lose your unbiased generalisation estimate. The test set should be touched exactly once, after all decisions are final. Every other estimate of generalisation is a validation estimate, not a test estimate.`,
    ],
    checkQuestions: [
      {
        q: `A model achieves 99% training accuracy and 75% test accuracy. What does this tell you about bias and variance, and what would you do?`,
        a: `High training accuracy + much lower test accuracy = low bias (the model fits training data well) + high variance (does not generalise — sensitive to the specific training set). The model is overfitting. Remedies in priority order: (1) More training data — reduces variance most directly. (2) Stronger regularisation (L2, dropout, weight decay). (3) Simpler architecture (fewer parameters, shallower network). (4) Ensemble methods (bagging averages out variance). Increasing model capacity would worsen the problem. The 24% gap is large — start with data augmentation or getting more labels before architectural changes.`
      },
      {
        q: `Explain the double descent phenomenon. A neural network's test error is worse at 1000 parameters than at 100 parameters, but better at 1,000,000 parameters than at 100. How is this possible?`,
        a: `The 1000-parameter model is near the interpolation threshold — it has just enough capacity to memorise the training data, but the solution it finds has high variance (sensitive to the specific training set). The 100-parameter model is underparameterised and underfits — higher bias. The 1,000,000-parameter model is highly overparameterised: gradient descent finds the minimum-norm interpolating solution among infinitely many zero-training-error solutions. Minimum-norm solutions correspond to smoother, more generalising functions. The "implicit regularisation" of gradient descent prefers these smooth solutions. Classical bias-variance theory predicts the U-curve (100 → 1000); double descent adds the second descent (1000 → 1,000,000). Early stopping at 100 would have been a worse choice than using the massively overparameterised model.`
      },
      {
        q: `You increase a linear classifier's feature count from d=100 to d=500 (adding new features). Training accuracy improves but test accuracy degrades. Explain in terms of VC dimension and what you should do.`,
        a: `Linear classifiers in ℝᵈ have VC-dim = d+1. Increasing d from 100 to 500 increases VC-dim from 101 to 501 — the hypothesis class can now shatter more points, meaning it can fit more arbitrary patterns including noise in the training set. The generalisation bound increases: test error ≤ train error + √(500·log(n)/n) > √(100·log(n)/n). With the same n, the model now needs 5× more data to maintain the same generalisation gap. Fixes: (1) Regularise more strongly (larger λ for Ridge/Lasso to reduce effective complexity). (2) Apply feature selection — keep only features with genuine predictive value. (3) Collect more data proportional to the increased VC dimension.`
      },
    ],
    takeaway: `A model can fail two opposite ways and the cure for one makes the other worse. Bias is wrong assumptions — the model misses even on training data. Variance is memorised noise — the model collapses on new data. Every architecture choice, regularisation decision, and dataset size question is a move along this curve. The irreducible noise term is the floor: real data has noise you cannot model away, no matter how complex the model.`,
    interactiveId: 'bias_variance_viz',
  },
  {
    id: 'trees',
    title: 'Decision Trees',
    subtitle: 'Information gain, Gini, pruning, depth-accuracy tradeoff',
    difficulty: 'foundational',
    estimatedMin: 28,
    tags: ['decision trees', 'Gini', 'information gain'],
    summary: `Linear models fail when the true decision boundary is non-linear and cannot be expressed as a weighted sum of features. The obvious fix is to allow the model to carve the feature space into regions, each with its own prediction. Decision trees do this by making recursive axis-aligned splits: at each node, search every feature and every threshold for the one that most reduces class impurity in the resulting children. No feature scaling required, mixed feature types handled naturally, interactions captured automatically. The algorithm is greedy — globally optimal trees are NP-hard to find — and that greediness produces a structural problem: small changes in training data can change the top split, which cascades through the entire tree structure and produces a completely different model. That instability — high variance — is a genuine weakness for a single tree. It is also, paradoxically, the property that makes trees ideal building blocks for ensemble methods: average many unstable models and the errors cancel out, but the low bias from expressiveness stays.`,
    keyPoints: [
      `At each node, the algorithm exhaustively searches every feature and every split threshold to find the combination that minimises weighted child impurity. Gini: G = 1−Σpₖ²; Entropy: H = −Σpₖlog pₖ. Both measure the same thing — how mixed the labels are — and produce nearly identical splits in practice. Gini is faster (no logarithm); entropy is slightly preferable for heavily imbalanced classes where the log penalises extreme distributions more.`,
      `Greedy splitting is locally optimal at each node but not globally — the best split at node 2 may have required a suboptimal split at node 1. Finding the globally optimal tree is NP-hard, so every practical implementation accepts greedy. This means the tree can miss patterns that require multiple jointly-chosen splits, but in practice the gap between greedy and optimal is small because impurity decreases nearly monotonically.`,
      `A fully grown tree assigns one leaf per training sample and achieves 100% training accuracy. This is zero bias and maximum variance — the tree memorised every noise pattern in the training data. Each leaf has so few samples that prediction is essentially arbitrary. Max depth and min samples per leaf are the regularisation dials that trade variance for bias.`,
      `Cost-complexity pruning addresses this correctly: grow the full tree first, then prune pairs of leaves that add the most complexity relative to their impurity reduction benefit. The penalty is total error + α × (number of leaves). Cross-validation selects α — this is more principled than setting max_depth directly because it adapts to the actual structure of the data rather than a fixed capacity limit.`,
      `Gini importance accumulates total impurity reduction across every split that uses a feature. High-cardinality and continuous features get more split opportunities simply by having more possible threshold values, inflating their apparent importance regardless of actual predictive value. Permutation importance — shuffle the feature, measure the accuracy drop — removes this bias because it evaluates the feature's contribution to prediction quality, not to splitting frequency.`,
      `Tree instability cascades: a different top split changes which samples reach every node below it, so a slightly different training set can produce a tree that looks completely different at every level. This is structural, not a tuning problem — it is a consequence of greedy search over a high-variance function space. The instability is why a single tree should never be deployed alone, and why it becomes so powerful as the base learner for ensembles.`,
      `Trees capture feature interactions automatically: splitting on A and then B is equivalent to learning the interaction A×B without any explicit feature engineering. This is why trees handle tabular data well when the interaction structure is unknown — they discover it from the data. Linear models must have interactions written in explicitly, which requires domain knowledge the modeller may not have.`,
      `Trees cannot extrapolate beyond the training data range. A test point outside the range of training feature values gets the prediction of the nearest leaf — a constant, never a trend. For regression tasks with distribution shift at the tails, this is a hard failure. Linear models extrapolate (often badly, but at least they do it); trees refuse to, which can be better or worse depending on whether the true relationship outside the training range is actually linear.`,
    ],
    checkQuestions: [
      {
        q: `Why do decision trees have high variance, and how does this motivate random forests?`,
        a: `Trees are unstable: small changes in training data (even swapping a few samples) can produce very different top splits, which propagate down to completely different tree structures. This is high variance. A single tree can perfectly fit training data but fails on test data because it memorised noise. Random forests reduce variance by: (1) bagging — training each tree on a bootstrap sample (different data reduces correlation between trees), (2) random feature subsampling — considering only m features at each split (further decorrelates trees). The ensemble average has much lower variance while preserving low bias. Ensemble variance = ρσ² + (1−ρ)σ²/T where ρ is pairwise tree correlation — decorrelation (reducing ρ) is as important as having many trees.`
      },
      {
        q: `Gini impurity and entropy give almost identical splits in practice. When would you choose one over the other, and is there any principled reason?`,
        a: `Information gain (entropy reduction) has a principled derivation from information theory: it maximises the mutual information between the split and the class label. Gini impurity has a different interpretation: it is the probability of misclassifying a randomly drawn sample if classified according to the class distribution. In practice: (1) Gini is faster to compute (no logarithm). (2) Entropy tends to produce more balanced splits (log penalises extreme imbalance more). (3) For highly imbalanced classes, entropy is sometimes slightly better. In most benchmarks, the difference in final model quality is < 0.1%. Choose entropy when you care about the information-theoretic interpretation; choose Gini for speed on large datasets.`
      },
      {
        q: `A decision tree with max_depth=None achieves 100% training accuracy and 62% test accuracy. You reduce max_depth to 5 and get 85% training and 80% test accuracy. Explain this in terms of bias-variance, and how would you find the optimal depth?`,
        a: `max_depth=None: zero bias (perfectly fits training), enormous variance (memorises noise). Huge generalisation gap (100% → 62%). max_depth=5: increased bias (cannot fit every training pattern), much lower variance (simpler structure). Better generalisation gap (85% → 80%). The optimal depth is found via cross-validation: for depth in [1, 2, 3, ..., 20], compute CV test accuracy; pick the depth where CV accuracy is highest. sklearn's cost_complexity_pruning_path() returns a range of ccp_alpha values — sweep these with CV; this is more principled than limiting depth because it prunes in a data-adaptive way.`
      },
    ],
    takeaway: `A single tree has zero bias when fully grown and enormous variance — it memorises every training noise pattern. Pruning trades that variance for bias, but you cannot get both low-bias and low-variance from one tree. That is not a failure; that instability is the specific property that makes trees excellent ensemble building blocks: average many of them and the variance cancels, but the expressiveness stays.`,
    interactiveId: 'decision_tree_viz',
  },
  {
    id: 'random_forest',
    title: 'Random Forests',
    subtitle: 'Bagging, OOB error, feature importance, hyperparameter sensitivity',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['random forest', 'bagging', 'ensemble'],
    summary: `A single decision tree has high variance: small changes in the training set cascade through every split and produce a completely different model. If that instability is the problem, the solution is to embrace it at scale — train hundreds of different trees and average their predictions. The variance cancels out; the bias does not, because each tree is still deep and expressive. This is the core of random forests. But averaging trees trained on the same data would give highly correlated predictions — if all trees make the same mistake, averaging does nothing. Bootstrap sampling addresses this partially by training each tree on a different subset, but the deeper fix is random feature subsampling: at each split, only consider a random subset of features. This forces different trees to discover different structure, reducing inter-tree correlation ρ. The variance formula makes the stakes precise: ensemble variance = ρσ² + (1−ρ)σ²/T. As T grows, variance converges to ρσ² — the floor is set by correlation, not count. More trees past ~200 barely help; reducing max_features continues to help even with many trees.`,
    keyPoints: [
      `Bootstrap sampling draws n samples with replacement for each tree, so each tree sees roughly 63.2% unique training points and leaves ~36.8% out-of-bag. The OOB samples are held-out data that each tree never trained on — they provide a free validation estimate that does not cost a separate split or extra computation.`,
      `Random feature subsampling at each split is the key decorrelation mechanism. Bagging alone leaves trees correlated because all trees encounter the same important features. Forcing each split to consider only m randomly selected features makes different trees discover different patterns, driving inter-tree correlation ρ down. This is why max_features has more leverage than n_estimators.`,
      `Ensemble variance = ρσ² + (1−ρ)σ²/T. As T→∞ this converges to ρσ² — the irreducible floor from inter-tree correlation. Past ~200 trees the (1−ρ)σ²/T term is negligible and adding more trees is wasted compute. Reducing ρ by decreasing max_features continues to reduce the variance floor, which is why tuning max_features first is the right order of operations.`,
      `OOB error is a reliable free estimate: for each training sample, average predictions only from trees that did not include it in their bootstrap sample. Enable with oob_score=True in sklearn. OOB error should match cross-validation accuracy closely when train and test come from the same distribution — a large gap signals distribution shift or leakage.`,
      `Permutation importance works by shuffling a feature's values in the OOB set and measuring how much accuracy drops. Shuffling breaks the real relationship between that feature and the target — if accuracy drops a lot, the feature genuinely mattered. This is unbiased with respect to feature cardinality and works correctly in the presence of correlated features.`,
      `Gini importance inflates importance for high-cardinality and continuous features because they offer more possible split thresholds, getting more split opportunities per tree by chance alone. This is a structural bias in the statistic, not fixable by tuning. For any decision that relies on feature ranking — selection, communication, ablation studies — use permutation importance instead.`,
      `Practical hyperparameter order: (1) max_features — the highest-leverage dial for accuracy; (2) min_samples_leaf — controls minimum leaf size, reduces variance without hard depth limits; (3) n_estimators — more is better with diminishing returns past ~200, mostly a compute budget question; (4) max_depth — rarely needed if min_samples_leaf is set. Always set n_jobs=-1 first — random forests are embarrassingly parallel and the speedup is near-linear on multi-core machines.`,
    ],
    checkQuestions: [
      {
        q: `A random forest with 500 trees takes too long to train. What do you tune first, and what accuracy tradeoff do you expect?`,
        a: `First: check n_jobs=-1 (parallelise across cores) — this is free and often gives 4-16× speedup with no accuracy loss. Second: reduce n_estimators to 100 — the OOB error curve is typically flat past ~100-200 trees; accuracy loss is usually <0.5%. Third: reduce max_features (fewer features evaluated per split — faster splits). Fourth: increase min_samples_leaf (stops growing leaves with few samples — shallower trees, faster). For very large datasets (n > 1M): set max_samples < 1.0 to subsample without replacement per tree. Always verify with the OOB error that the reduced model is within 1% of the full model.`
      },
      {
        q: `Your random forest's OOB error is 10% but test error is 25%. What does this mean?`,
        a: `Large OOB-test gap indicates distribution shift or data leakage. OOB error estimates performance on training distribution (same X distribution as training data). A 15% gap means the test data comes from a different distribution than training data — or there was data leakage that made OOB unrealistically optimistic. Investigate: (1) Compare feature distributions between train and test (histogram, KS-test). (2) Check for temporal leakage: if test data is future data and training data includes features computed from the future. (3) Check for target leakage: features that encode the label. OOB error is a reliable estimate only when train and test are iid draws from the same distribution.`
      },
      {
        q: `Two features both have high Gini importance in a random forest, but when you remove either one individually, model performance barely changes. Explain and fix.`,
        a: `The two features are highly correlated — they carry the same information. When both are present, the random forest splits on each when the other is unavailable (due to max_features subsampling), giving both high total impurity reduction. But each feature alone is sufficient. Gini importance double-counts correlated features. Fix: (1) Use permutation importance — shuffle each feature while keeping the other, measure accuracy drop. A redundant feature's permutation importance will be near zero when its correlated partner is present. (2) Use SHAP values, which correctly attribute importance in the presence of correlated features. (3) Remove one of the pair: measure OOB error with and without each; keep the one whose removal hurts more.`
      },
    ],
    takeaway: `Random forests work because averaging high-variance predictions reduces variance — but only if the trees make different errors. The variance floor is ρσ², set entirely by inter-tree correlation. More trees past ~200 barely move this; tuning max_features downward reduces ρ and actually lowers the floor. The most important hyperparameter is the one that controls diversity, not quantity.`,
    interactiveId: 'random_forest_viz',
  },
  {
    id: 'gradient_boosting',
    interactiveId: 'gradient_boosting_viz',
    title: 'Gradient Boosting & XGBoost',
    subtitle: 'Residual fitting, shrinkage, XGBoost regularisation, early stopping',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['gradient boosting', 'XGBoost', 'LightGBM', 'ensemble'],
    summary: `Random forests reduce variance by averaging independent trees, but they cannot reduce bias — each tree is trained on the full target signal with the same algorithm, so the systematic errors persist. Boosting attacks bias directly: instead of training trees independently, build them sequentially where each new tree fits the errors of all previous trees. The current ensemble has residuals — predictions it gets wrong — and the next tree is trained specifically on those residuals, forcing the ensemble to correct its own mistakes. This is gradient descent in function space: the negative gradient of the loss at each training point tells you how much the prediction there needs to change, and each tree approximates that gradient function. The learning rate η scales each tree's step — smaller η with more trees finds a better minimum but requires early stopping to avoid overfitting. XGBoost makes this more precise by using a second-order Taylor expansion of the loss, which gives an analytically optimal leaf weight and a principled split criterion that prunes splits yielding insufficient improvement.`,
    keyPoints: [
      `The algorithm initialises F₀ to the best constant prediction, then for each round: compute pseudo-residuals rᵢ = −∂L/∂F(xᵢ) at current predictions, fit a shallow tree to those pseudo-residuals, update F_t = F_{t-1} + η·hₜ. Each tree is a gradient step — it moves every prediction slightly in the direction that reduces loss, as specified by the gradient of the loss function at the current prediction.`,
      `For MSE loss, the gradient of ½(y−F(x))² with respect to F(x) is exactly (F(x)−y), so pseudo-residuals are the actual residuals yᵢ−F_{t-1}(xᵢ). For cross-entropy loss, pseudo-residuals are (yᵢ−σ(F(xᵢ))) — the same prediction-error form. Any differentiable loss produces well-defined pseudo-residuals, which is why gradient boosting generalises to quantile regression, ranking losses, and custom business objectives without changing the algorithm.`,
      `Shrinkage: scaling each tree's contribution by η < 1 forces the model to take smaller steps toward the minimum. Halving η roughly doubles the optimal number of trees, but smaller steps reach a better-regularised solution — no single tree dominates, and the ensemble averages out more noise. The cost is longer training, which early stopping handles by monitoring validation loss rather than the step count.`,
      `Boosting trees must be shallow — typically depth 3–8. Each tree fits pseudo-residuals, not the original target, so the signal being modelled has already been partially removed by previous trees. Deep trees overfit the residuals at each step, causing the ensemble to chase noise. Random forests use full-depth trees because averaging handles their variance; boosting uses shallow trees because each is a weak corrector, not a standalone model.`,
      `XGBoost adds explicit regularisation directly into the split criterion: objective = Σ L(yᵢ,ŷᵢ) + Σₜ[γT + (λ/2)Σⱼ wⱼ²], where T is leaves and wⱼ are leaf weights. The γ term penalises adding extra leaves; the λ term penalises large leaf weights. A split only happens when it improves the regularised objective by at least γ — unprofitable splits are rejected during construction, not pruned afterward.`,
      `XGBoost uses a second-order Taylor expansion of the loss to derive the analytically optimal leaf weight: wⱼ* = −Gⱼ/(Hⱼ+λ), where Gⱼ is the sum of first-order gradients and Hⱼ is the sum of second-order gradients (Hessians) in leaf j. Using the Hessian provides curvature information — it scales the gradient step by how rapidly the loss is changing, giving more accurate updates especially near the minimum.`,
      `Early stopping is the correct way to set n_estimators — not grid search. Monitor validation loss after each tree and stop after early_stopping_rounds consecutive rounds with no improvement (50 is a common default). The model at the best validation round is the final model. This automatically finds the tree count where bias reduction stops outpacing overfitting, without manual tuning.`,
      `LightGBM grows trees leaf-wise — always expanding the single leaf with the highest gain — rather than level-wise. This makes training faster on large datasets and often achieves lower loss for the same number of leaves, but leaf-wise growth produces deeper, more unbalanced trees that are more prone to overfitting on small data. Use LightGBM for n > 100K and XGBoost for smaller datasets where overfitting risk is higher.`,
    ],
    checkQuestions: [
      {
        q: `XGBoost is overfitting the training set. List 5 hyperparameters you would tune and the direction of each change.`,
        a: `(1) Lower learning_rate (0.3→0.05): more regularisation, slower learning — increase n_estimators to compensate and use early stopping. (2) Reduce max_depth (6→3): shallower trees have less capacity to overfit residuals. (3) Increase min_child_weight (1→10): prevents splits on small groups — the minimum sum of instance weight (Hessian) in a leaf. (4) Add subsample < 1.0 (0.8): stochastic gradient boosting — each tree uses only 80% of training samples, adding noise that regularises. (5) Add colsample_bytree < 1.0 (0.8): feature subsampling like random forests, decorrelates trees. Also: increase lambda (L2 on leaf weights) and gamma (minimum gain for a split).`
      },
      {
        q: `Explain why gradient boosting is "gradient descent in function space." What is the function being optimised, and what does each tree compute?`,
        a: `In parameter-space gradient descent, we update θ in the direction −∇_θ L(θ). In function-space boosting, the "parameter" is the prediction function F, and we update F in the direction that reduces loss most: F_t = F_{t-1} − η·∇_F L(F). The negative gradient ∂L/∂F(xᵢ) at each training point is the pseudo-residual — it tells us how much to change the prediction at xᵢ to reduce loss. Each tree hₜ approximates this gradient function by fitting a shallow tree to the pseudo-residuals. Adding η·hₜ to F is a gradient step in function space. This view explains why boosting works for any differentiable loss: the gradient is always a well-defined direction of improvement, regardless of whether the loss is MSE, log-loss, quantile, or custom.`
      },
      {
        q: `You train XGBoost with 1000 trees and learning_rate=0.1. The validation loss flattens at tree 200 and then starts increasing. What does this tell you and what should you do?`,
        a: `The model is overfitting after tree 200. The optimal n_estimators is ~200, not 1000. The additional 800 trees are memorising training noise. Action: (1) Set early_stopping_rounds=50 and retrain — XGBoost will stop automatically when validation loss does not improve for 50 consecutive rounds, giving the optimal tree count. (2) The optimal model has ~200 trees at learning_rate=0.1. To potentially improve further: lower learning_rate to 0.01 and increase n_estimators to 2000 (with early stopping), as lower LR often finds a better final solution. (3) Also check if overfitting starts earlier with different max_depth or subsample settings.`
      },
      {
        q: `For a regression problem, gradient boosting with MSE loss fits actual residuals at each step. For binary classification with log-loss, what does it fit, and why is the pseudo-residual form the same?`,
        a: `For log-loss: L = −[y log ŷ + (1−y) log(1−ŷ)] where ŷ = σ(F(x)). The pseudo-residual is −∂L/∂F = y − σ(F(x)) = y − ŷ. This is identical in form to the MSE case (y − ŷ) and to the logistic regression gradient. The reason: gradient boosting with log-loss fits the negative gradient of the log-likelihood at the current predictions — which is exactly the residual in the probability scale (y − P̂(y=1|x)). The form (y − ŷ) is universal because all losses from the exponential family have gradients in this form. Gradient boosting with log-loss is equivalent to running many weak logistic-regression-gradient steps, each implemented as a shallow tree.`
      },
    ],
    takeaway: `Gradient boosting is gradient descent in function space — each tree is a step downhill on the loss surface, η sets the step size, and early stopping is your convergence criterion. That framing explains every tuning decision: smaller η means smaller steps and more trees before you converge, but a better final minimum; early stopping finds that minimum without guessing the tree count.`,
  },
  {
    id: 'ensembles',
    title: 'Ensemble Methods',
    subtitle: 'Bagging vs boosting vs stacking, diversity principle',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['ensembles', 'stacking', 'bagging', 'boosting'],
    summary: `Every model makes systematic errors — patterns in the data it consistently gets wrong. If you train a second model with a different structure or different data, it tends to make different systematic errors. Average the two predictions and the errors partially cancel, but the correct predictions reinforce. This is the entire mechanism behind ensemble methods. The diversity principle makes it precise: ensemble error ≈ ε(1+(T-1)ρ)/T where ρ is pairwise error correlation. When ρ=1 — both models fail on the same examples — averaging does nothing. When ρ=0 — errors are uncorrelated — the ensemble error shrinks as 1/T. In practice ρ is never zero, but combining a gradient boosted tree with a neural network achieves lower ρ than running five random forests with different seeds. Bagging, boosting, and stacking are three different strategies for building diverse models; the diversity principle explains why some combinations work and others do not.`,
    keyPoints: [
      `Bagging trains T models on T bootstrap samples and averages their predictions. It reduces variance without changing bias — it only helps when the base learner is high-variance (deep trees, large neural nets). Averaging low-variance models like linear regression gives minimal gain because there is little variance to cancel. The bootstrap samples ensure each model trains on a slightly different data distribution, introducing the diversity needed for averaging to help.`,
      `Boosting builds models sequentially, each correcting the errors of the previous ensemble. Unlike bagging, boosting targets bias — it makes each new model focus on the examples the current ensemble gets wrong. The tradeoff is overfitting risk as more models are added: each model focuses increasingly on hard or noisy examples that are inherently difficult to learn, eventually memorising noise.`,
      `Stacking (stacked generalisation) trains diverse base models on k-fold cross-validation, uses their out-of-fold predictions as features for a meta-learner, and trains the meta-learner to optimally combine the base models. The meta-learner discovers which base models are reliable in which regions of the input space. The OOF step is what prevents leakage: base model predictions on their own training data would be overfit, giving the meta-learner an unrealistically optimistic signal.`,
      `The diversity principle: ensemble error ≈ ε(1+(T-1)ρ)/T where ε is individual model error and ρ is pairwise error correlation. With T=10 models each at 20% error: if ρ=0.9 (highly correlated), ensemble error ≈ 18.2%; if ρ=0.1 (diverse), ensemble error ≈ 3.8%. The same 10 models, the same individual accuracy, radically different ensemble performance. Diversity is the whole game.`,
      `OOF (out-of-fold) predictions are the key mechanism in stacking that prevents leakage: for fold k, the base model is trained on folds 1..k-1,k+1..K and predicts on fold k — it has never seen fold k's labels during training. These held-out predictions are the meta-features. Using training-set predictions instead would give the meta-learner overfit base model outputs as features, causing it to trust the base models more than warranted.`,
      `Simple averaging is almost always within 0.1% of full stacking and has zero risk of meta-learner overfitting. Always benchmark against simple averaging before investing in a meta-learner. Stacking earns its complexity only when the meta-learner can meaningfully learn which base model to trust in which region — which requires that the base models have genuinely complementary strengths, not just different random seeds.`,
      `Production cost of ensembles is N× everything: N models to train, maintain, version, monitor, and serve. Inference latency is N× a single model (or parallelised if compute allows). Competition ensembles routinely combine 20-50 models; production systems typically cap at 2-5. Before committing to an ensemble in production, measure the accuracy gain versus the engineering cost and latency impact.`,
      `Snapshot ensembles capture diversity from a single training run by saving model checkpoints at different points in the learning rate schedule. Models at earlier checkpoints represent simpler, higher-bias solutions; later checkpoints represent more complex, lower-bias solutions. Averaging them combines multiple points on the bias-variance curve at no extra training cost — only the cost of storing multiple checkpoints and averaging at inference time.`,
    ],
    checkQuestions: [
      {
        q: `Your stacking ensemble overfits: high training accuracy but low CV accuracy on the meta-learner. What is the likely cause and fix?`,
        a: `Data leakage in meta-features. If base models were trained on the full training set and their training-set predictions were used as meta-features, the meta-learner sees optimistically good predictions — base models have memorised the training labels, so their training-set outputs are near-perfect even for hard examples. The meta-learner learns to trust these over-optimistic signals and fails on validation data. Fix: generate meta-features using out-of-fold (OOF) predictions — each base model is trained on k-1 folds and predicts on the held-out fold k. These OOF predictions represent what the base models can genuinely generalise to, not what they memorised.`
      },
      {
        q: `You have three diverse models: a random forest (AUC=0.82), gradient boosting (AUC=0.85), and logistic regression (AUC=0.78). How would you build an ensemble, and what AUC would you expect?`,
        a: `Start with simple averaging of probability outputs: ensemble_score = (0.33×RF_prob + 0.33×GBM_prob + 0.33×LR_prob). Evaluate on a held-out set — simple averaging often achieves AUC 0.86-0.87, slightly above the best single model. Next try weighted averaging: weights proportional to individual AUCs, optimised on a validation set (e.g., 0.25×RF + 0.50×GBM + 0.25×LR). If that helps, try stacking with OOF predictions as meta-features and a ridge-regularised logistic regression as meta-learner. The gain comes from diversity: RF and GBM make different errors (RF uses random feature subsets, GBM does sequential residual fitting), so their errors partially cancel.`
      },
      {
        q: `Two models make identical errors on the test set: wherever model A is wrong, model B is wrong on the same examples. What is the ensemble's test accuracy, and what does this illustrate about diversity?`,
        a: `Ensemble accuracy = individual model accuracy — no improvement at all. The diversity principle: if models make identical errors (correlation ρ=1), averaging does nothing because the errors do not cancel. The ensemble error formula: ε_ensemble ≈ ε(1+(T-1)ρ)/T. With ρ=1 and T=2: ε_ensemble ≈ ε(1+1)/2 = ε. This is why using the same algorithm with different random seeds gives minimal gain (high ρ), while combining a neural network and a gradient boosted tree gives more gain (different inductive biases lead to different error patterns, lower ρ).`
      },
    ],
    takeaway: `Ensemble gains come from error diversity, not model count. The formula is unambiguous: ensemble error ≈ ε(1+(T-1)ρ)/T. When ρ is near 1, adding models does almost nothing. The only lever that actually moves the floor is reducing ρ — meaning genuinely different algorithms, different features, or different inductive biases, not different random seeds on the same model.`,
    interactiveId: 'ensemble_viz',
  },
  {
    id: 'svm',
    title: 'Support Vector Machines',
    subtitle: 'Maximum-margin hyperplane, kernel trick, soft margin',
    difficulty: 'advanced',
    estimatedMin: 30,
    tags: ['SVM', 'kernel', 'margin', 'dual'],
    summary: `Logistic regression finds a decision boundary that correctly classifies training data, but there are infinitely many such boundaries — it returns whichever one minimises the loss, with no additional preference. SVMs argue that the specific boundary that matters is the one furthest from all training points: the maximum-margin hyperplane. Maximising the margin 2/‖w‖ is equivalent to minimising ‖w‖² subject to correctly classifying every training point by a margin of at least 1. The solution has a structural property: only the points sitting on the margin edges determine the boundary — move any other training point and nothing changes. These are the support vectors. Real data is not linearly separable, so the soft-margin extension allows some points to violate the margin, controlled by C. The kernel trick is the key generalisation: the dual form of the SVM only requires dot products between training points, and any kernel function k(xᵢ,xⱼ) that satisfies Mercer's condition can replace those dot products — mapping data to a rich feature space implicitly, without ever computing it.`,
    keyPoints: [
      `Hard-margin SVM: minimise ½‖w‖² subject to yᵢ(wᵀxᵢ+b) ≥ 1 for all i. This is only feasible when data is linearly separable with a non-zero gap. Maximising margin 2/‖w‖ is equivalent to this minimisation — wider margin means smaller weight norm. The dual reformulation rewrites the objective using dot products αᵢαⱼyᵢyⱼxᵢᵀxⱼ, and the kernel trick works by replacing exactly these dot products with a kernel function.`,
      `Soft-margin SVM introduces slack variables ξᵢ ≥ 0: minimise ½‖w‖² + C·Σξᵢ. Large C means heavy penalty for any margin violation — the model tries to correctly classify all training points at the cost of a narrower margin, risking overfitting. Small C allows more violations to achieve a wider margin, which regularises the solution and improves generalisation. C is the bias-variance tradeoff dial — analogous to 1/λ in Ridge regression.`,
      `Support vectors are the training points with αᵢ > 0 in the dual — the points on or inside the margin. The decision boundary depends only on these points, not on any training point safely outside the margin. Moving a non-support-vector does not change the boundary at all. This sparsity in training data is an important property: SVMs are robust to non-boundary examples in a way that logistic regression is not.`,
      `The kernel trick replaces dot products xᵢᵀxⱼ in the dual with kernel function k(xᵢ,xⱼ) = φ(xᵢ)ᵀφ(xⱼ) for some feature mapping φ. The RBF kernel k(x,x') = exp(−γ‖x−x'‖²) corresponds to an infinite-dimensional feature space — it is a universal approximator that can learn any decision boundary given sufficient data. The insight is that you only ever need k(xᵢ,xⱼ), not φ(xᵢ) itself, even when φ maps to infinite dimensions.`,
      `The RBF kernel's γ parameter controls how quickly the kernel value decays with distance. High γ means the kernel drops to near zero for nearby points — each support vector influences only a tiny local region and the boundary is complex and potentially overfit. Low γ means each support vector influences a wide region and the boundary is smooth. γ and C must be jointly tuned on a log scale: C=10,γ=0.01 produces a very different boundary than C=10,γ=10.`,
      `Hinge loss: max(0, 1−yᵢf(xᵢ)) is zero when a point is correctly classified with margin ≥ 1 and grows linearly for misclassified or margin-violating points. The soft-margin SVM minimises hinge loss plus L2 regularisation — the same structure as logistic regression with cross-entropy plus L2. In practice they often give nearly identical accuracy; the choice between them is usually about probability outputs (logistic regression gives calibrated probabilities; SVM does not) and scaling (both scale similarly with n for linear kernels).`,
      `SVMs do not scale to large datasets. The kernel matrix K where Kᵢⱼ = k(xᵢ,xⱼ) requires O(n²) memory — for n=100K that is 80GB of float64 storage before any computation. Training time is O(n²) to O(n³) depending on the solver. For n > 100K: use linear SVM (sklearn's LinearSVC via liblinear, O(nd) time) or SGD with hinge loss (SGDClassifier, O(nd) time). Kernel SVM is only viable for n < 50K.`,
      `Feature scaling is critical for SVM: the RBF kernel uses Euclidean distance ‖x−x'‖², so a feature with range [0, 1000] dominates a feature with range [0, 1] regardless of actual predictive value — the kernel is measuring units, not signal. Always StandardScaler before SVM. Unscaled features can make the kernel computation so dominated by one feature that the model effectively ignores all others.`,
    ],
    checkQuestions: [
      {
        q: `Your SVM with RBF kernel underfits the training data. What do you adjust, and what is the risk of each change?`,
        a: `Underfitting means the margin is too wide and the decision boundary is too smooth. Adjustments: (1) Increase C (allow fewer margin violations — tighter fit to training data). Risk: overfitting — the boundary becomes wiggly and does not generalise. (2) Increase γ (tighter, more localised RBF kernel — each support vector influences a smaller region). Risk: overfitting — at high γ, the model memorises training data with tiny "bubbles" around each point. Grid search over (C, γ) on log scale: C ∈ {0.01, 0.1, 1, 10, 100}, γ ∈ {0.001, 0.01, 0.1, 1, 10}. In practice: try the default C=1, gamma='scale' first — often competitive with tuned values.`
      },
      {
        q: `Explain the kernel trick. Why does it work, and what mathematical condition must a function k(x,x') satisfy to be a valid kernel?`,
        a: `The kernel trick works because the SVM dual objective only requires pairwise dot products xᵢᵀxⱼ — it never explicitly uses the feature vectors. By Mercer's theorem, any function k(x,x') = φ(x)ᵀφ(x') for some (possibly infinite-dimensional) φ can be substituted for these dot products. The condition: k must be a symmetric positive semi-definite function (the Gram matrix K where Kᵢⱼ = k(xᵢ,xⱼ) must be PSD for all datasets). This guarantees k corresponds to a valid dot product in some feature space. The magic: we get the computational benefit of working in a high-dimensional space (rich features) without the computational cost of computing φ(x) (which may be infinite-dimensional). The kernel matrix K is n×n regardless of the dimensionality of φ.`
      },
      {
        q: `SVMs and logistic regression both find a linear separator. In what situations would you prefer one over the other?`,
        a: `Prefer SVM when: (1) The dataset is small (n < 10K) and high-dimensional — SVMs have better theoretical guarantees (margin maximisation) in this regime. (2) The classes are well-separated — hard-margin or small-slack SVM exploits this. (3) You need a kernel for non-linear boundaries and cannot engineer features. Prefer logistic regression when: (1) You need calibrated probability outputs — SVM outputs distances, not probabilities (Platt scaling fixes this but adds complexity). (2) Large datasets (n > 100K) — LR scales linearly in n, SVM does not. (3) Interpretability matters — LR coefficients are log-odds ratios; SVM weights have less direct interpretation. (4) You want a fast baseline — LR trains in seconds.`
      },
    ],
    takeaway: `SVMs find the maximum-margin boundary — and that maximum-margin criterion has a key consequence: most training points become irrelevant once training is done. Only the support vectors matter. The kernel trick is how you get non-linear boundaries from a linear optimiser: substitute dot products with kernel evaluations, and the SVM learns in a potentially infinite-dimensional feature space without ever computing it. Both properties are elegant and practically limited: SVMs stall at n > 100K and gradient boosting wins at scale.`,
    interactiveId: 'svm_viz',
  },
  {
    id: 'knn',
    title: 'K-Nearest Neighbours',
    subtitle: 'Distance metrics, curse of dimensionality, ANN indexes',
    difficulty: 'foundational',
    estimatedMin: 22,
    tags: ['KNN', 'distance metrics', 'ANN'],
    summary: `Parametric models like logistic regression and decision trees learn a fixed functional form during training. When the true decision boundary is irregular — islands of one class surrounded by another, complex non-linear interactions — those functional forms become constraints that hurt. KNN avoids this entirely: it makes no assumption about the functional form, stores all training data, and at prediction time finds the k nearest training points and votes among their labels. The boundary adapts to whatever the data looks like. The cost is that all computation is deferred to inference time — O(nd) per query for brute force. The deeper problem is dimensionality. KNN works because nearby points in feature space have similar labels. In high dimensions, distance stops being a reliable measure of similarity: as d grows, the ratio of nearest-to-farthest distance converges to 1 and every point becomes approximately equidistant from every other. The neighbourhood concept breaks down, and KNN degenerates to near-random voting. The only fix is a better feature space.`,
    keyPoints: [
      `Distance metric choice is a prior about what "similar" means for your data. Euclidean ‖x−x'‖₂ treats all dimensions symmetrically and is sensitive to scale — one feature with large variance can dominate the distance calculation. Manhattan ‖x−x'‖₁ is more robust to outliers and sometimes preferable in high dimensions. Cosine similarity ignores magnitude and measures direction — it is the right choice for text and embedding spaces where length of the feature vector is meaningless.`,
      `k controls the bias-variance tradeoff in KNN. k=1 gives zero training error — every point is its own nearest neighbour — but maximum variance: the boundary is jagged and a single noisy training point creates a false island. Large k smooths the boundary and introduces bias by averaging over a broader neighbourhood. Tune k via cross-validation; weighted KNN (weights = 1/distance) reduces sensitivity to the choice of k and is almost always preferable to uniform voting.`,
      `Curse of dimensionality: in d dimensions, the ratio (distance to nearest neighbour)/(distance to farthest neighbour) converges to 1 as d→∞. All points become approximately equidistant. The fraction of data volume within distance ε of a query point shrinks as εᵈ — to keep the same fraction of the training data in the neighbourhood, ε must grow exponentially with d. With fixed n, KNN neighbourhoods span the entire space in high dimensions and the "local" vote becomes a global one.`,
      `The curse is a geometric fact, not a data sparsity problem. Even with unlimited training data, distances in high-dimensional spaces concentrate and the nearest neighbour is not meaningfully "near." KNN works reasonably well up to about d=20, then degrades rapidly. Dimensionality reduction (PCA, UMAP) or a learned metric are the only real solutions — they create a lower-dimensional space where distance is meaningful again.`,
      `Weighted KNN votes by 1/distance rather than uniform weight — closer points contribute more than distant ones. This makes the prediction more local and less sensitive to the choice of k, because the influence of borderline neighbours is automatically downweighted. Weighted KNN consistently outperforms uniform voting and should be the default unless there is a specific reason not to.`,
      `ANN (approximate nearest neighbour) indexes make KNN practical at scale. FAISS (GPU-accelerated, used in production search at Meta), HNSW (hierarchical graph index, excellent recall-speed tradeoff), and ScaNN (Google) all reduce query time from O(nd) to sublinear, trading at most 1-5% recall for 100-1000× speedup. For n > 1M, ANN is the only viable approach — brute-force KNN at that scale would require seconds per query.`,
      `KNN on raw features often misleads because raw features rarely reflect the true similarity structure. KNN on learned embeddings from contrastive learning or fine-tuned transformers is powerful: the embedding space is trained so that semantic or class similarity corresponds to geometric proximity. In these spaces, KNN becomes a highly effective classifier for few-shot problems and semantic search — the feature space does the heavy lifting, and k is just a smoothing parameter.`,
      `KNN imputation replaces a missing value with the weighted average of the same feature across the k nearest complete neighbours. It is more accurate than mean/median imputation for features with local structure, and faster to implement than model-based imputation (MICE). The weakness: if the missing feature is itself important for distance calculation, KNN imputation cannot use it to find relevant neighbours — impute iteratively or use a model that handles missingness natively.`,
    ],
    checkQuestions: [
      {
        q: `Why does KNN fail in 1000 dimensions even with millions of training points?`,
        a: `Curse of dimensionality: in d=1000 dimensions, the distance from a query point to its nearest neighbour converges to nearly the same value as the distance to its farthest neighbour. The distribution of pairwise distances becomes concentrated — the coefficient of variation of distances shrinks to 0 as d grows. The k nearest neighbours are no longer geometrically "local" to the query point — they are nearly as far as random points. The local averaging that makes KNN work (nearby points have similar labels) breaks down when "nearby" means "within 1000 dimensions of noise." Fix: dimensionality reduction (PCA, UMAP) or learned metric/embedding before applying KNN.`
      },
      {
        q: `A production recommendation system uses KNN with n=50M items and d=256-dimensional embeddings. Brute-force KNN is too slow. What is your architecture?`,
        a: `Use an ANN index. Process: (1) Offline: build an HNSW index or FAISS IVF index on the 50M item embeddings. HNSW is preferred for high-recall requirements; FAISS IVF is preferred for memory-constrained systems. (2) Online: for each user query embedding, query the index for the top-k approximate nearest neighbours (e.g., top-100 candidates). (3) Re-rank: apply a more expensive scoring function (dot product, learned ranker) to the top-100 candidates. This is the retrieve-and-rerank pattern. Performance: HNSW achieves >95% recall@10 with queries in ~1ms for 50M items — vs. ~10s for brute force. Tradeoff: index takes O(n·d) memory plus graph structure overhead.`
      },
      {
        q: `When would you choose KNN over a trained classifier like logistic regression or a decision tree?`,
        a: `Choose KNN when: (1) The decision boundary is highly non-linear and irregular — KNN adapts to any boundary shape without feature engineering. (2) The training set is very small and the feature space is low-dimensional (d < 20) — KNN with k=5 often outperforms logistic regression that has too few samples to estimate coefficients reliably. (3) You need online learning — adding a new training point to KNN requires no retraining, just appending to the index. (4) Instance-based explanations matter — "your prediction is X because your nearest neighbours are A, B, C" is intuitive. Prefer logistic regression or trees when: large n (KNN inference is O(nd)); high d; need probability outputs; training time is not a constraint.`
      },
    ],
    takeaway: `KNN makes one bet: nearby points in feature space have similar labels. The entire algorithm rests on that bet, which means the distance metric and the feature space are the model — k is just a smoothing parameter. Get the space wrong and no value of k rescues you. Get it right — via learned embeddings or a domain-appropriate metric — and KNN is competitive with any classifier.`,
    interactiveId: 'knn_viz',
  },
  {
    id: 'naive_bayes',
    title: 'Naïve Bayes',
    subtitle: 'Independence assumption, Gaussian NB, Laplace smoothing',
    difficulty: 'foundational',
    estimatedMin: 22,
    tags: ['Naive Bayes', 'independence', 'text classification'],
    summary: `Text classification puts a model in a difficult position: the feature space has tens of thousands of words, most of which appear rarely, and training data is often scarce. Logistic regression needs enough samples to estimate a reliable weight for every word — with 10,000 vocabulary items and 100 training documents, it severely overfits even with strong regularisation. Naïve Bayes sidesteps this by directly modelling P(word|class) from counts, which is stable even with very few samples. The price is the conditional independence assumption: P(x|y) = Π P(xⱼ|y), treating every word as independent given the class. This is obviously wrong — words co-occur — but the classifier does not need accurate joint probabilities. It only needs to rank P(y=1|x) above P(y=0|x) correctly, and the log-odds from independent features often achieves this even when the model of the joint is badly wrong. One failure mode is deterministic: an unseen word at test time gives P(word|class) = 0, which zeros out the entire posterior. Laplace smoothing — adding 1 to all counts — prevents this by ensuring no probability is ever exactly zero.`,
    keyPoints: [
      `Prediction: ŷ = argmax_k P(y=k) Πⱼ P(xⱼ|y=k). In practice, always use log-probabilities: argmax_k [log P(y=k) + Σⱼ log P(xⱼ|y=k)]. Multiplying thousands of small probabilities — each P(word|class) might be 0.001 — causes floating-point underflow to exactly zero before you finish the product. Log-space turns the product into a sum, eliminating underflow entirely.`,
      `Gaussian NB models P(xⱼ|y=k) = N(μⱼₖ, σⱼₖ²) — one mean and variance per feature per class, estimated from training samples. Training is a single pass to compute class-conditional means and variances, and prediction is a single pass through the Gaussian PDF for each feature. This only applies to continuous features; it is the wrong variant for text.`,
      `Multinomial NB models P(xⱼ|y=k) proportional to the count of feature j in class k — the correct variant for text where features are word counts or TF-IDF frequencies. The Multinomial model assumes each document is a bag of word draws from a class-conditional multinomial distribution. Gaussian NB applied to text would model word count as a continuous Gaussian, which ignores the discrete, sparse nature of document features.`,
      `Laplace smoothing adds α to all word counts before computing probabilities: P(word|class) = (count + α)/(N_class + α|V|). Without it, any word appearing in test documents but not in training documents for a given class gives P(word|class) = 0, which multiplies out to zero the entire posterior for that class. Laplace smoothing is equivalent to placing a symmetric Dirichlet(α) prior on word probabilities and taking the MAP estimate — it regularises the model and is essential for any real vocabulary.`,
      `Naïve Bayes works despite violated independence because classification only requires correct ranking of P(y=k|x), not accurate probability values. Even when features are highly correlated — which makes the modelled joint probability P(x|y) very different from the true joint — the log-odds sum Σⱼ log P(xⱼ|y=k) often ranks classes correctly. The wrong model of the distribution can still produce the right ordering of the posteriors.`,
      `NB probability outputs are poorly calibrated: the independence assumption causes the model to over-count correlated evidence, pushing posteriors toward 0 and 1 much faster than the true probability. An NB output of 0.99 should not be read as "99% confidence." If calibrated probabilities are needed, apply Platt scaling — fit logistic regression on the NB log-odds using a calibration set — to recover calibrated outputs.`,
      `NB advantages make it the right baseline for text: O(nd) training (just counting), O(d) prediction (just summing logs), naturally handles the p >> n regime where logistic regression overfits, and is interpretable — inspect P(word|class) directly to understand what words drive each class. Missing features are handled naturally by simply omitting their term from the sum, unlike logistic regression which requires imputation.`,
      `Complement NB models P(x|y≠k) — the complement class — and predicts the class with the minimum complement score. This corrects a systematic problem with standard Multinomial NB: when classes are imbalanced, the majority class has more training data and its word probabilities are more reliably estimated. Complement NB removes this asymmetry and often outperforms standard Multinomial NB on imbalanced text datasets.`,
    ],
    checkQuestions: [
      {
        q: `In Multinomial NB for spam detection, a test email contains the word "win" which appears in 0% of spam emails in training. Without Laplace smoothing, what happens?`,
        a: `P("win"|spam) = 0. Since NB multiplies all feature probabilities, the entire product P(x|spam) = 0, regardless of all other words in the email. The spam posterior P(spam|x) = 0. The email will never be classified as spam, even if every other word strongly indicates spam. This is the zero-frequency problem. Laplace smoothing adds α=1 to all word counts: P("win"|spam) = (0+1)/(N_spam + |V|) where |V| is vocabulary size — small but non-zero, preserving the contribution of all other features and allowing the model to classify correctly based on the other words.`
      },
      {
        q: `Why does Naïve Bayes often outperform logistic regression on text classification when training data is small?`,
        a: `NB estimates P(x|y) from per-class word frequencies — a generative model with very few parameters (one mean per word per class). This estimation is stable even with small n because it is just counting. Logistic regression estimates P(y|x) discriminatively — it needs to learn a weight for every word, requiring much more data to avoid overfitting in a high-dimensional space. With n=100 training documents and |V|=10,000 vocabulary, LR has 10,000 parameters and will overfit without strong regularisation, while NB has well-estimated word probabilities from the counts. As n grows, LR catches up and eventually surpasses NB because its discriminative objective is more directly aligned with the classification goal.`
      },
      {
        q: `Your Naïve Bayes classifier outputs P(spam)=0.99 for an email. How confident should you be, and what would you do if calibrated probabilities are required?`,
        a: `Very cautious: NB posteriors are poorly calibrated — the independence assumption makes P(y|x) converge toward 0 and 1 faster than the true probabilities. A NB output of 0.99 does not mean 99% of similarly-scored emails are spam. To get calibrated probabilities: (1) Apply Platt scaling — fit a logistic regression a·f+b on the NB log-odds f = log(P(spam)/P(ham)) using a held-out calibration set. (2) Apply isotonic regression if you have enough calibration data (>1000 samples) and suspect non-monotone miscalibration. After calibration, validate on a separate test set using a reliability diagram and ECE.`
      },
    ],
    takeaway: `Naïve Bayes needs correct ranking of class posteriors, not accurate probabilities — and that is a much weaker requirement. The independence assumption is always violated, but it violates symmetrically enough that the ranking stays right. That makes NB the correct first baseline for text and sparse data: it is fast, interpretable, and stable with very little data. Never trust the raw probability values without calibration — they are systematically pushed toward 0 and 1.`,
  },
  {
    id: 'calibration',
    title: 'Model Calibration',
    subtitle: 'Reliability diagrams, ECE, Platt scaling, isotonic regression',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['calibration', 'ECE', 'Platt scaling', 'reliability'],
    summary: `A model with high AUC ranks examples correctly — it scores true positives above false positives. But a ranking and a probability are different things. A model that outputs 0.9 for a group of patients where the true disease rate is 0.6 is confidently wrong: the ranking may be correct (the 0.9 patients are higher risk than the 0.5 patients) but the probability is useless for any downstream decision that asks "how likely is this specific patient to have the disease?" Miscalibration is the rule, not the exception: random forests push predictions toward 0 and 1 because averaging many trees concentrates the vote; SVMs output distances, not probabilities; naive Bayes over-counts correlated evidence and saturates. Calibration matters whenever the probability itself is used — risk scoring, insurance pricing, medical decisions, or as input to a meta-learner in stacking. The fix is post-hoc: hold out a calibration set, fit a mapping from raw model scores to empirical probabilities, and validate the fix on the test set.`,
    keyPoints: [
      `A reliability diagram reveals miscalibration: bin predictions into M buckets, and for each bin plot the mean predicted probability against the actual fraction of positives. Perfect calibration is the diagonal y=x. A curve below the diagonal means the model is overconfident — it predicts 0.8 for events that occur 0.6 of the time. A curve above the diagonal means underconfidence. The shape (S-curve, monotone shift) determines which calibration method to apply.`,
      `ECE (Expected Calibration Error) quantifies miscalibration as the weighted average of |confidence_b − accuracy_b| across bins, weighted by bin size. It collapses a reliability diagram to a single number — lower is better, 0 is perfect calibration. MCE (Maximum Calibration Error) measures the worst-bin deviation and matters when a single miscalibrated region has high stakes. Use ECE for overall summary and MCE when tail miscalibration is critical.`,
      `Tree-based models are systematically overconfident because the prediction at each leaf is the training fraction in that leaf. With small leaves, those fractions are near 0 or 1 regardless of the true class probability. Random forests average many leaves, but the average of many near-extreme values is still extreme. This is not a bug — it is a consequence of how trees split. Platt scaling or isotonic regression are the standard post-hoc fixes.`,
      `Platt scaling fits logistic regression σ(af+b) on a held-out calibration set, where f is the model's raw score or logit. Two parameters, fast to fit, appropriate when miscalibration is monotone — the reliability curve is sigmoid-shaped relative to the diagonal. It is the first calibration method to try. The risk is that logistic calibration cannot fix non-monotone miscalibration, where the model is overconfident in some score ranges and underconfident in others.`,
      `Isotonic regression fits a non-decreasing step function mapping raw scores to calibrated probabilities, with no parametric form assumed. It can correct any shape of miscalibration, including non-monotone patterns. The cost is that it requires at least 1000 calibration samples to avoid overfitting — with fewer samples, isotonic regression just memorises the calibration set. Use Platt when data is scarce; isotonic when you have sufficient calibration data and the miscalibration is clearly non-monotone.`,
      `Temperature scaling for neural networks divides all logits by a scalar T before the softmax: P(y=k|x) = softmax(z/T). T > 1 flattens the output distribution — reducing overconfidence without changing the ranking (AUC is preserved). T < 1 sharpens it. Only one parameter is fit on the validation set, making overfitting impossible even with a small calibration set. Temperature scaling is remarkably effective because neural network miscalibration is predominantly a uniform scaling issue, not a non-monotone one.`,
      `Calibration and discrimination are orthogonal. A model can have excellent AUC (perfect ranking) but terrible calibration (the probabilities are wrong), or be perfectly calibrated (each probability is empirically correct) but poor AUC (the probabilities barely separate classes). A credit model might correctly rank 10,000 applicants by default risk while systematically underestimating the absolute probability — great for ranking decisions, useless for setting loan loss reserves. You need both metrics to fully evaluate a probabilistic model.`,
      `The calibration set must be separate from both training and test sets. Using the training set to calibrate produces a degenerate calibrator that memorises training outputs — the calibrated model appears perfect on training data and fails on test data. Using the test set to calibrate contaminates your generalisation estimate — you are fitting to test data, and the resulting ECE on the test set is optimistically biased. The correct setup: split data into train / calibration / test, calibrate on the calibration set, evaluate calibration quality on the test set.`,
    ],
    checkQuestions: [
      {
        q: `A Random Forest predicts P(y=1) = 0.9 for many samples, but only 60% of those samples are actually positive. What calibration technique would you apply and how?`,
        a: `The model is overconfident — predictions near 0.9 correspond to actual frequency of 0.6. Apply isotonic regression or Platt scaling on a held-out calibration set (not the training set). Isotonic regression is preferred here if the calibration set has > 1000 samples, because the miscalibration may be non-monotone across the full probability range. Steps: (1) Hold out a calibration set during training. (2) Obtain raw model scores on the calibration set. (3) Fit isotonic regression mapping scores to labels. (4) At test time, pass raw scores through the isotonic regressor to get calibrated probabilities. Validate on a separate test set — never on the calibration set used to fit the calibrator.`
      },
      {
        q: `Your neural network is overconfident (ECE = 0.15). You apply temperature scaling and get ECE = 0.03. The temperature T chosen is 2.5. What does T=2.5 mean mechanically?`,
        a: `Temperature scaling divides the logit vector z by T before softmax: P(y=k|x) = softmax(z/T)_k. T=2.5 > 1 means the logit vector is scaled down by a factor of 2.5 — the differences between classes are reduced, making the softmax distribution flatter (less confident). Before scaling: if the top logit is 5.0 and second is 2.0 (difference 3.0), the network is very confident. After scaling by 2.5: the logits become 2.0 and 0.8 (difference 1.2), softmax gives a less extreme distribution. Mechanically, T scales the confidence of every prediction uniformly — it is a single parameter that can correct systematic overconfidence without changing the model's ranking (AUC is preserved).`
      },
      {
        q: `You have a stacking ensemble where a miscalibrated base model is one of the inputs to the meta-learner. Why is calibration critical here, and what happens if you do not calibrate?`,
        a: `The meta-learner uses base model probability outputs as features. If a base model is overconfident (outputs 0.95 when the true probability is 0.65), the meta-learner receives a feature value of 0.95 for what is actually a moderate-confidence example. The meta-learner tries to learn the relationship between these features and the true label — but miscalibrated features have a different scale than calibrated ones, making the meta-learner's job harder. In the worst case: the meta-learner trusts the overconfident model too much (the feature 0.95 looks like a strong signal) and under-weights the other base models. After calibration, all base models' outputs are on the same probability scale, and the meta-learner can correctly learn their relative reliability.`
      },
    ],
    takeaway: `AUC measures ranking; calibration measures whether the probabilities themselves are right. A model with AUC=0.95 and ECE=0.20 ranks examples correctly but its probability outputs are wrong — it is confidently wrong, not confidently right. Any time the downstream decision uses the probability — not just the rank — plot the reliability diagram before trusting the model.`,
  },
  {
    id: 'class_imbalance',
    title: 'Class Imbalance',
    subtitle: 'SMOTE, threshold tuning, class weights, precision@K',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['imbalance', 'SMOTE', 'precision@K', 'threshold'],
    summary: `A classifier on a 99:1 dataset can achieve 99% accuracy by predicting the majority class for every single example. It never makes a prediction about the rare class and it is completely useless — yet standard training procedures optimise exactly this metric. The root problem is that accuracy assumes false positives and false negatives cost the same; in fraud detection, cancer screening, and equipment failure prediction, they do not. The imbalance is not the problem: unequal class costs are the problem, and the imbalance is just what exposes them. Fix it at the right level. Class weights reweight the loss function so that misclassifying a minority example costs more, using the same gradient descent that was ignoring the minority class to now focus on it — no data changes, no new samples, just a different loss. Threshold tuning at deployment is always necessary regardless of training procedure, because the cost ratio of false positives to false negatives is problem-specific and the default 0.5 threshold almost never matches it.`,
    keyPoints: [
      `Accuracy is the wrong metric for imbalanced problems. On a 99:1 dataset, a model predicting majority class always achieves 99% accuracy while capturing 0% of the minority class. Precision, recall, F1, PR-AUC, and business cost metrics expose what accuracy hides. The choice of evaluation metric is not less important than the choice of model — a bad metric will lead you to select the wrong model every time.`,
      `Class weights reweight the loss so minority class errors are penalised more heavily: wₖ = n/(K·nₖ), which sklearn computes automatically with class_weight="balanced". Each minority example contributes more to the gradient update than a majority example. This shifts the decision boundary toward better minority class recall without modifying the training data or generating synthetic points. It is the cheapest first intervention and should always be tried before any data-level method.`,
      `SMOTE generates synthetic minority samples by interpolating between a minority point and one of its k nearest minority neighbours — creating new points in the minority class region rather than duplicating existing ones. The failure mode is placing synthetic points in regions where the minority and majority classes overlap, creating confusing training examples. SMOTE works best when the minority class has a compact, well-separated region; it hurts when minority and majority regions interleave.`,
      `Undersampling removes majority class samples to restore balance. Random undersampling is fast but discards information. Tomek links removal is smarter: it removes majority samples that are the nearest neighbour of a minority sample — cleaning the class boundary without random information loss. Combining SMOTE (oversample minority) with Tomek link removal (clean boundary) is often better than either alone because you add minority-class signal and simultaneously remove boundary ambiguity.`,
      `Threshold tuning is always required at deployment because the default 0.5 threshold assumes equal costs for false positives and false negatives. In fraud detection, missing a fraud (false negative) costs far more than flagging a legitimate transaction (false positive). The optimal threshold minimises expected business cost: cost = FP_count × cost_FP + FN_count × cost_FN. Plot the PR curve, identify the threshold achieving the required precision or recall, and validate on a held-out set.`,
      `Precision@K is the right metric when you act on a fixed budget: among the top-K highest-confidence positive predictions, what fraction are true positives? If your fraud team reviews K=50 alerts per day, Precision@50 directly measures how many reviews produce actual fraud cases. AUC-ROC measures the model across all thresholds; Precision@K measures the model at the specific operating point you actually use. They often diverge dramatically for very imbalanced problems.`,
      `PR-AUC is more informative than ROC-AUC for severely imbalanced data. ROC-AUC inflates because the true negative rate (specificity) is easy to maintain when negatives dominate — even a bad classifier gets credit for correctly classifying the 99% majority. PR-AUC focuses on precision and recall — both of which are sensitive to the minority class performance. On a 0.1% positive rate, ROC-AUC of 0.99 can coexist with Precision@1000 of 5% — the ROC number hides the practical failure.`,
      `SMOTE with tree-based models frequently hurts performance. Trees use class weights directly in their impurity calculations — the split criterion already down-weights majority class errors. SMOTE adds synthetic minority samples in regions the tree would have already learned to split correctly, introducing noise without adding signal. Class weights are the correct intervention for trees; SMOTE is primarily useful for distance-based models (KNN, SVM) and neural networks where the data distribution directly shapes the learned representation.`,
    ],
    checkQuestions: [
      {
        q: `You build a fraud detector with 0.1% fraud rate. Your model achieves 99.5% AUC-ROC but the ops team says too many false positives. What do you change?`,
        a: `High AUC-ROC but too many false positives = threshold is too low for the cost structure. The model's ranking is good (AUC 0.99+) but the default threshold of 0.5 is not calibrated to the 1000:1 cost ratio. Actions: (1) Compute PR curve — find threshold achieving the precision the ops team needs (e.g., 50% precision means 1 in 2 alerts is real fraud). (2) Frame as Precision@K — if ops reviews K=100 alerts/day, tune threshold to maximise precision of top-100 predictions. (3) Check PR-AUC — if it is also high, your model is genuinely good and you just need threshold calibration. If PR-AUC is low, the model needs improvement in the high-precision region.`
      },
      {
        q: `Your dataset has 1000 positive and 100,000 negative samples. You apply SMOTE to balance it to 50,000:100,000 and train a logistic regression. The test PR-AUC is lower than with class weights. Why?`,
        a: `SMOTE with logistic regression often hurts for several reasons: (1) SMOTE generates synthetic minority samples by interpolating between existing minority samples in feature space. For logistic regression (a linear model), the minority region may not be linearly separable from the majority — synthetic points in this region reinforce an incorrect linear boundary. (2) Class weights achieve the same reweighting effect as SMOTE for logistic regression but without adding noise. (3) SMOTE's interpolation can place synthetic points in the majority region, creating noisy examples that confuse the model. Class weights are the correct intervention for logistic regression on imbalanced data.`
      },
      {
        q: `You have a binary classifier for cancer screening. False negatives (missing cancer) are 10× more costly than false positives (unnecessary follow-up). How do you incorporate this into training and evaluation?`,
        a: `Training: set class_weight = {0: 1, 1: 10} — the loss for misclassifying a positive (cancer) sample is 10× the loss for misclassifying a negative. This shifts the decision boundary toward higher recall. Threshold: at deployment, tune the threshold on a calibration set to achieve the cost-optimal tradeoff: expected cost = FP_rate × 1 + FN_rate × 10. Find threshold minimising this. Evaluation: use F_β with β=√10 ≈ 3.16 (F_β weighs recall β times more than precision); also report Precision, Recall, and the cost metric directly. Never report only accuracy — it hides the critical false-negative rate.`
      },
    ],
    takeaway: `Class imbalance is a cost asymmetry problem masquerading as a data problem. The real issue is that standard training treats false positives and false negatives as equally bad, and they never are in high-stakes applications. Class weights fix the training in seconds. The threshold at deployment fixes the operating point. What you cannot skip is measuring with the right metric — accuracy on an imbalanced problem is not a metric, it is a way to miss the whole problem.`,
    interactiveId: 'class_imbalance_viz',
  },
  {
    id: 'feature_selection',
    title: 'Feature Selection & Dimensionality',
    subtitle: 'Filter/wrapper/embedded, mutual information, RFE, SHAP',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['feature selection', 'mutual information', 'SHAP', 'RFE'],
    summary: `Adding more features seems like it should always help — more information about the target means better predictions. In practice, irrelevant and redundant features add noise, inflate the effective dimensionality the model must search, increase training time, and make the model harder to interpret. The question is not "should I include this feature" but "does this feature reduce test error, or does it only reduce training error?" Selecting features based on test set performance is not an answer — it inflates apparent accuracy because the selected features were chosen to match the test labels. Feature selection must happen inside cross-validation. Among selection methods, filter methods evaluate features independently and miss interactions; wrapper methods search subsets using the model directly and are too slow for large feature sets; embedded methods like L1 regularisation and SHAP importance do selection as part of training and account for interactions. In practice, the most reliable pipeline is: train one gradient boosted model on all features, rank by SHAP importance, select the top-k — one training run, interactions accounted for, selection bias avoided.`,
    keyPoints: [
      `Filter methods score each feature independently of the model and of other features. Mutual information I(X;y) captures non-linear dependencies and is the most general criterion. ANOVA F-test handles linear continuous-target relationships. Chi-squared handles categorical features and targets. Filter methods are fast — O(nd) — but they miss multivariate interactions: two features that are each individually uncorrelated with the target can jointly be perfectly predictive, and filter methods will eliminate both.`,
      `Variance thresholding removes features with near-zero variance — they carry no information regardless of any model. A constant feature (var=0) contributes nothing to any split criterion or gradient. This is the cheapest possible first pass and should always run before any other selection method. In real datasets, near-constant features often appear from dummy variables for rare categories, features that were computed but never triggered, or data pipeline errors.`,
      `Mutual information estimation from finite samples is noisy — binning-based estimators introduce bias that depends on bin width. Sklearn's mutual_info_classif uses a k-nearest-neighbour estimator that avoids this bias and gives more accurate MI estimates with fewer samples. The tradeoff is that MI captures each feature's individual relationship with the target — two features with high MI might be redundant (they carry the same information), and MI does not reveal this.`,
      `RFE (Recursive Feature Elimination) trains the model, removes the lowest-importance feature, and repeats. It captures interactions because the model sees all remaining features at each step — the importance of feature A changes when feature B is removed if they were correlated. The cost is O(d) model fits, making it practical for small feature sets (d < 100) and prohibitive for large ones (d = 10,000 would require 10,000 training runs).`,
      `Embedded methods do selection as part of training. L1 regularisation drives irrelevant feature weights to exactly zero during gradient descent — the same optimiser that was causing overfitting now simultaneously selects features. Tree Gini importance and SHAP values rank features post-hoc from a trained model that already saw all features together, so interactions are reflected in the scores. Embedded methods are both faster than wrapper methods and more accurate than filter methods for correlated feature spaces.`,
      `Feature selection bias is a common source of inflated published results: compute feature importance on the full dataset (including test examples), select the top-k features, then evaluate on the test set. The test set labels were used to select features, making the selected features appear more predictive than they really are. Correct procedure: feature selection must be nested inside cross-validation — compute importance on training folds only, evaluate on the held-out fold. This adds complexity but is the only way to get an unbiased estimate.`,
      `SHAP-based selection trains one gradient boosted model on all features and computes mean |SHAP value| per feature. SHAP values decompose each prediction into per-feature contributions that correctly account for feature interactions — unlike Gini importance, which treats each split independently. A feature with high SHAP importance genuinely moves predictions, not just impurity. Train once, get reliable interaction-aware importance, select top-k: this is the most reliable selection pipeline for tabular data.`,
      `Correlated features inflate apparent feature count without adding independent information. If two features have |corr| > 0.95, they are nearly redundant — a model that uses both is not learning twice as much signal, it is learning the same signal with noise. Keep the one with higher mutual information with the target and remove the other. This also stabilises model fitting: correlated features compete for coefficient estimates in linear models and split credit in tree models, causing importance estimates to be unreliable for either feature individually.`,
    ],
    checkQuestions: [
      {
        q: `You have 500 features and want to reduce to ~50 before tuning a Random Forest. What is your step-by-step approach?`,
        a: `Step 1: remove constant and near-zero variance features (VarianceThreshold in sklearn — fast, no label needed). Step 2: remove highly correlated pairs (|corr|>0.95) — keep the one with higher MI with target. Step 3: train an initial Random Forest with all remaining features, compute permutation importance (or SHAP) on a validation fold. Select top-50 by importance. Step 4: verify that the 50-feature model achieves similar OOB or CV performance to the full-feature model (within 0.5%). Step 5: if performance drops, iterate with top-75 or top-100. Avoid pure filter methods (they miss interactions); embedded methods (permutation importance, SHAP) are preferred for RF because they account for feature interactions.`
      },
      {
        q: `A colleague selects the top-20 features using mutual information with the target label, then performs 5-fold CV to evaluate the model. What is wrong with this procedure?`,
        a: `Feature selection bias: mutual information was computed on the full dataset (including what becomes the test fold). The selected features were chosen because they happen to correlate with the label in all 5 folds — including the test fold. This inflates apparent performance because the features were selected using test-set label information. Correct procedure: feature selection must be nested inside the CV loop. For each fold: (1) train MI on the k-1 training folds only; (2) select top-20 features using that MI; (3) train model on training folds with those 20 features; (4) evaluate on the held-out test fold. This gives an unbiased estimate of both the selection procedure and the model.`
      },
      {
        q: `You select 20 features with Gini importance from a Random Forest and find that removing any single selected feature barely changes test performance. What does this suggest?`,
        a: `Two likely explanations: (1) Many of the 20 features are correlated — the information they collectively provide is redundant, and any individual feature can be replaced by a correlated substitute. Gini importance distributes credit among correlated features, making each look moderately important. True importance only appears when all correlated partners are removed simultaneously. (2) The model is robust to feature removal and individual features contribute marginally. Fix: (1) Use permutation importance or SHAP instead of Gini. (2) Try sequential ablation: remove subsets of correlated features together and measure the group's collective importance. (3) Check feature correlations among the 20 selected features — groups with |corr|>0.8 are likely providing redundant information.`
      },
    ],
    takeaway: `Feature selection on the full dataset before splitting is data leakage — the selected features were chosen partly because they happened to correlate with the test labels, and the apparent performance gain is artefactual. Selection must be nested inside cross-validation. In practice: train one gradient boosting model, rank by SHAP, take the top-k. One run, no leakage, interactions accounted for. That is the reliable pipeline.`,
  },
]

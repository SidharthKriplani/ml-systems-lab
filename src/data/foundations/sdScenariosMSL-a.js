export const SD_MSL_A = [
  {
    id: 'credit-default-scoring',
    title: 'Credit-Default Risk Scoring',
    prompt: 'Design an ML system to score credit-default risk for loan applications.',
    context: `A consumer lender approves roughly 40k loan applications per day, each scored synchronously at application time with a p99 budget of about 300ms (a human is waiting on the decision page). "Default" is defined as 90+ days past due, so a label only becomes trustworthy 6-18 months after origination. The base default rate is 3-5%, decisions are regulated (adverse-action reasons are legally required), and the score feeds both an approve/decline gate and a risk-based pricing tier.`,
    tags: ['credit risk', 'calibration', 'label delay', 'regulated ML', 'imbalance'],
    stages: [
      {
        id: 'requirements',
        title: 'Clarify requirements & constraints',
        ask: 'Pin down what the score is actually used for, when the label matures, and which regulatory constraints bound the design before proposing any model.',
        considerations: [
          'Exact label definition: 90+ DPD by month 12? month 18? Charge-off vs delinquency changes the target and the maturity window.',
          'The 6-18 month label-maturity delay means the freshest applications have no known outcome yet, so recent-vintage data is unlabeled at train time.',
          'Whether the score gates approve/decline only, or also drives risk-based pricing, which forces calibrated probabilities rather than a rank.',
          'Regulatory regime: ECOA/Reg B adverse-action reasons and (in some markets) SR 11-7 model-risk governance, which constrain model class and require explainability.',
          'Base rate (3-5%) and the cost asymmetry: a funded default is a large realized loss, a wrongly declined good applicant is lost lifetime margin plus fairness exposure.',
          'Selective-labels / reject inference: you only observe repayment for applicants you approved, so the training set is censored by past policy.',
          'Latency (p99 ~300ms synchronous) and throughput (40k/day, spiky) bound feature-fetch and model complexity.',
          'Point-in-time correctness: every feature must reflect only what was knowable at application time, no post-application bureau pulls or repayment behavior.',
        ],
        strong: [
          'States the label precisely (e.g. 90+ DPD observed within 12 months of origination) and immediately flags the 6-18 month maturity delay as the binding data constraint, not an afterthought.',
          'Separates the two use cases: a gate can tolerate a well-ordered score, but risk-based pricing needs calibrated probabilities, so calibration is a hard requirement here.',
          'Raises selective labels / reject inference up front: the model is trained only on approved applicants, so naive training bakes in the old approval policy.',
          'Names the regulatory constraints concretely: adverse-action reasons per Reg B, model governance under SR 11-7, and a strong preference for monotonic/explainable models.',
          'Quantifies the cost asymmetry rather than hand-waving: a default might cost 40-60% of principal, a false decline costs expected lifetime margin, so the operating threshold is an economic choice.',
          'Confirms the point-in-time rule explicitly: features are snapshotted as of application time, no leakage from future bureau data or repayment.',
        ],
        traps: [
          'Treating it as a generic binary classifier and skipping the label-maturity delay entirely.',
          'Assuming a random train/test split, which leaks future vintages and ignores that recent applications are unlabeled.',
          'Optimizing only AUC and never mentioning calibration, then proposing risk-based pricing on top of an uncalibrated score.',
          'Ignoring that the training data is censored by past approval decisions (reject inference).',
        ],
        probes: [
          'Your freshest 6 months of applications have no mature label. Do you train on them, and if so how?',
          'Why is a ranked score insufficient the moment the business wants to price risk in tiers rather than just approve or decline?',
          'How does training only on previously approved applicants bias the model, and what would you do about it?',
        ],
      },
      {
        id: 'architecture',
        title: 'High-level architecture',
        ask: 'Lay out the end-to-end flow from application event to decision, including the feature store, the point-in-time-correct training pipeline, and the champion/challenger serving path.',
        considerations: [
          'Where features come from: application form, internal history, and bureau pulls, each with its own freshness and availability SLA.',
          'A feature store with point-in-time joins so training features are reconstructed as-of application time, matching what serving will see.',
          'Train/serve skew: the exact same feature transformations must run offline and online, ideally shared code, to avoid the classic drift between pipelines.',
          'Vintage-based (temporal) train/validation/test splits rather than random splits, so evaluation respects the maturity delay.',
          'A champion/challenger harness so a new model can be shadow-scored against the live one before it ever gates a real decision.',
          'Handling missing bureau data at serve time (thin-file applicants) without silently defaulting features to zero.',
          'An explainability service that emits adverse-action reason codes alongside every declined score.',
          'Batch retraining cadence gated on label maturity, not a fixed weekly schedule, because labels arrive slowly.',
        ],
        strong: [
          'Draws the flow explicitly: application event -> feature store (point-in-time join) -> model -> calibrated probability -> threshold/pricing -> decision + reason codes, with the label-collection loop closing 12+ months later.',
          'Insists that offline and online feature transforms share one implementation to eliminate train/serve skew, and names the failure if they diverge.',
          'Uses vintage-based splits: train on originations old enough to have mature labels, validate on the next vintage, so evaluation never sees the future.',
          'Adds a champion/challenger (shadow) path so challengers accumulate predictions on live traffic without affecting decisions until they clear.',
          'Designs the feature store to reconstruct as-of features for training (avoiding leakage) and to serve the identical features online within the latency budget.',
          'Plans an explainability component (e.g. SHAP or a monotonic-GBM reason-code mapping) wired into the decline path to satisfy adverse-action requirements.',
        ],
        traps: [
          'A feature store that serves latest values for training instead of point-in-time values, silently leaking the future.',
          'Separate offline and online feature code, guaranteeing train/serve skew over time.',
          'No shadow/challenger path, so the only way to test a new model is to put it in front of real applicants.',
          'Bolting explainability on at the end as a nice-to-have rather than a required output of every decision.',
        ],
        probes: [
          'Walk me through exactly how one feature, say utilization over the last 3 months, is computed identically at train time and serve time.',
          'How do you validate a challenger model before it is allowed to decline a real applicant?',
          'A thin-file applicant has no bureau history. What does your pipeline feed the model, and how do you avoid a spurious low-risk score?',
        ],
      },
      {
        id: 'deep-dive',
        title: 'Deep-dive: the hard part',
        ask: 'Go deep on the two things that make credit scoring hard: producing calibrated probabilities under class imbalance, and preventing point-in-time leakage across the long label-maturity window.',
        considerations: [
          'Calibration is a first-class requirement: the model must output true probabilities, so pricing at score=0.08 really means ~8% default.',
          'Imbalance handling (3-5% positives) and its interaction with calibration: resampling or class weighting distorts base rates and must be corrected afterward.',
          'Post-hoc calibration methods (Platt scaling, isotonic regression) fit on a held-out, label-mature slice, and how to measure calibration (reliability diagram, ECE, Brier score).',
          'Point-in-time leakage vectors: bureau attributes that update, repayment behavior, or any feature refreshed after application, all must be frozen as-of application time.',
          'Model class tradeoff: monotonic gradient-boosted trees give strong accuracy plus regulator-friendly monotonic constraints and reason codes, versus a fully unconstrained model.',
          'Reject inference approaches (e.g. reweighting, augmentation, or a policy-aware objective) to counter selective labels.',
          'Threshold selection as an economic decision using the FP/FN cost asymmetry, not a default 0.5.',
          'Feature stability over the 12-18 month horizon so a feature is not predictive only because it leaked outcome information.',
        ],
        strong: [
          'Explains why ranking is not enough: pricing and expected-loss provisioning need calibrated probabilities, so calibration quality (ECE, reliability curve) is a headline metric, not AUC alone.',
          'Handles imbalance without breaking calibration: if class weighting or resampling is used to help the model learn, recalibrate afterward (isotonic/Platt on a mature holdout) so the base rate is restored.',
          'Enumerates concrete leakage traps: a bureau score refreshed post-application, a repayment-derived feature, or a most-recent-value join, and explains how the point-in-time store blocks each.',
          'Chooses monotonic GBMs deliberately: they satisfy monotonicity expectations (higher utilization should not lower risk), stay accurate, and support clean adverse-action reason codes.',
          'Addresses reject inference directly, e.g. reweighting approved applicants toward the applied-for population or a policy-aware training scheme, rather than pretending the sample is unbiased.',
          'Sets the operating threshold from the cost asymmetry and target approval rate, and can show how moving it trades expected loss against approval volume.',
          'Measures calibration per risk band and over time, since a model can be calibrated on average but badly miscalibrated for high-risk applicants.',
        ],
        traps: [
          'Claiming AUC/KS is enough while proposing risk-based pricing that structurally depends on calibrated probabilities.',
          'Oversampling the minority class and then reading probabilities straight off the model without recalibrating.',
          'Missing a subtle leakage source (a feature that quietly encodes the outcome) and mistaking the resulting inflated AUC for real skill.',
          'Picking an unconstrained black-box model with no path to monotonicity or reason codes in a regulated setting.',
        ],
        probes: [
          'You oversampled positives 10x to help the model learn. Your probabilities now average ~30% but the true base rate is 4%. What went wrong and how do you fix it?',
          'Show me a feature you would be suspicious of as a leakage source, and how the point-in-time store rules it out.',
          'How would you satisfy an adverse-action requirement while still using a gradient-boosted model?',
        ],
      },
      {
        id: 'evaluation',
        title: 'Evaluation & monitoring',
        ask: 'Define offline metrics that respect the maturity delay, the online metrics you can watch immediately, and the monitoring that catches population drift long before defaults show up.',
        considerations: [
          'Offline ranking/separation metrics (AUC, KS) plus calibration metrics (reliability diagram, ECE, Brier) on a label-mature, out-of-time vintage.',
          'The lag problem: the true business metric (realized default rate by score band) is only observable months later, so leading indicators are essential.',
          'Online proxies available immediately: approval rate, score distribution, applicant-mix shift, and early-payment-delinquency (first missed payment) as an early default signal.',
          'Population Stability Index (PSI) and feature drift monitoring to detect that the applicant population has shifted away from training.',
          'Vintage analysis / cohort curves: tracking default rate by origination month against predicted rate to spot degradation early.',
          'Champion/challenger comparison on live shadow scores before promotion.',
          'Fairness monitoring across protected-class proxies for disparate impact, tracked over time.',
          'Alerting on score-distribution shifts and calibration drift, since a miscalibrated model breaks pricing silently.',
        ],
        strong: [
          'Splits evaluation into offline (AUC/KS plus calibration/ECE/Brier on an out-of-time mature vintage) and online (approval rate, score distribution, early-payment delinquency as a leading loss indicator).',
          'Explicitly confronts the label lag: since realized default is months out, monitors PSI on inputs, prediction-distribution shift, and first-payment delinquency as fast proxies.',
          'Uses vintage/cohort curves comparing predicted vs actual default by origination month, so degradation surfaces as soon as a cohort seasons rather than after a full year.',
          'Monitors calibration in production, not just at training: tracks whether score=0.08 bands actually default near 8%, because pricing depends on it.',
          'Runs champion/challenger with real shadow predictions and promotes only on both discrimination and calibration improvement, with a rollback plan.',
          'Adds fairness/disparate-impact monitoring over time, treating a drift in approval rates across groups as an incident.',
        ],
        traps: [
          'Reporting only a single offline AUC and declaring victory, with no calibration or out-of-time evaluation.',
          'Having no leading indicator and waiting for realized defaults, so drift is caught a year too late.',
          'Monitoring model accuracy but not input/population drift (PSI), missing a silent covariate shift.',
          'Never re-checking calibration in production, so pricing quietly drifts off.',
        ],
        probes: [
          'Your realized default label is a year away. What do you watch tomorrow to know the model is degrading?',
          'PSI on income has jumped to 0.3 while AUC on your last mature vintage still looks fine. What is your read?',
          'How do you detect that the model is still well-ranked but no longer well-calibrated?',
        ],
      },
      {
        id: 'tradeoffs',
        title: 'Tradeoffs, failure modes & scaling',
        ask: 'Discuss the core tradeoffs (accuracy vs explainability, approval volume vs loss, retraining cadence vs label maturity) and the failure modes you would guard against.',
        considerations: [
          'Accuracy vs explainability: an unconstrained model may score marginally better but cannot cleanly produce adverse-action reasons or pass governance.',
          'Approval rate vs expected loss: the threshold is a business dial, and moving it trades volume against realized default.',
          'Retraining cadence vs label maturity: retrain too fast and you train on immature labels, too slow and you drift with the population.',
          'Feedback loop / policy censoring: today model shapes who gets approved, which shapes tomorrow training data, tightening the selective-labels problem over time.',
          'Macro shifts (rate hikes, recession) that break the stationarity assumption faster than labels mature.',
          'Failure modes: silent calibration drift breaking pricing, a leaked feature inflating offline metrics, and thin-file default-imputation producing systematically wrong scores.',
          'Serving reliability: a sensible fallback (e.g. a simpler scorecard) if the feature store or bureau pull times out within the 300ms budget.',
          'Cost: 40k scores/day is cheap to serve, so the expensive part is data pipelines, governance, and retraining, not inference.',
        ],
        strong: [
          'Frames the accuracy/explainability tradeoff honestly: chooses a monotonic GBM that is nearly as accurate as an unconstrained model but survives governance and yields reason codes.',
          'Treats the threshold as an economic lever and can articulate, using the cost asymmetry, how a 1-point approval-rate change moves expected loss.',
          'Ties retraining cadence to label maturity: retrains on newly-matured vintages and uses drift monitors, rather than a fixed weekly job that would train on unlabeled data.',
          'Recognizes the reject-inference feedback loop compounding over time and proposes deliberate exploration or a policy-aware objective to keep the sample honest.',
          'Names macroeconomic non-stationarity as a distinct risk and plans for it (macro features, faster drift response, stress scenarios), since defaults can spike before labels catch up.',
          'Specifies a serving fallback (cached score or a simple scorecard) when a bureau pull or feature fetch breaches the latency budget, so the decision page never hangs.',
        ],
        traps: [
          'Picking the highest-AUC model with no answer for adverse-action reasons or governance.',
          'Retraining on a fixed schedule that scoops up immature (still-unlabeled) applications.',
          'Ignoring the self-reinforcing censoring: the model only ever sees applicants it chose to approve.',
          'No degraded-mode plan when the feature store or bureau call is slow or down under the 300ms SLA.',
        ],
        probes: [
          'A recession hits and defaults climb, but your labels are still a year out. How does your system react before the labels arrive?',
          'The business wants to raise approval rate by 3 points. What do you need to see before agreeing?',
          'Your bureau provider is timing out on 5% of calls at peak. What does the system do for those applicants?',
        ],
      },
    ],
    rubric: [
      {
        dim: 'Requirements & scoping',
        strong: 'Nails the label definition and the 6-18 month maturity delay, separates gate vs pricing (so calibration is required), and surfaces selective labels and regulatory constraints in the first few minutes.',
        weak: 'Treats it as a generic binary classifier, assumes a random split, and never mentions label lag, calibration, or reject inference.',
      },
      {
        dim: 'Architecture & data flow',
        strong: 'Draws an end-to-end flow with a point-in-time feature store, shared offline/online transforms, vintage-based splits, a champion/challenger shadow path, and a reason-code service.',
        weak: 'Serves latest-value features for training, keeps offline and online code separate, and has no shadow path or explainability output.',
      },
      {
        dim: 'Core ML depth (modeling & features)',
        strong: 'Delivers calibrated probabilities under imbalance (recalibrate after resampling), chooses monotonic GBMs for accuracy plus reason codes, and blocks concrete point-in-time leakage vectors.',
        weak: 'Optimizes AUC only, reads probabilities straight off an oversampled model, and misses subtle leakage that inflates offline metrics.',
      },
      {
        dim: 'Evaluation (offline & online)',
        strong: 'Reports AUC/KS plus calibration (ECE/Brier) on an out-of-time mature vintage, and pairs it with immediate online proxies (early-payment delinquency, score distribution) given the label lag.',
        weak: 'Reports a single offline AUC, has no leading indicator, and waits for realized defaults a year later.',
      },
      {
        dim: 'Reliability, monitoring & failure modes',
        strong: 'Monitors PSI/population drift and production calibration, runs vintage cohort curves, tracks fairness over time, and plans a serving fallback when bureau/feature calls fail.',
        weak: 'Monitors only accuracy, never re-checks calibration in production, and has no degraded-mode plan.',
      },
      {
        dim: 'Scaling, latency & cost',
        strong: 'Meets the 300ms synchronous budget with a fallback scorecard, correctly notes inference is cheap while data pipelines and governance are the real cost, and ties retraining cadence to label maturity.',
        weak: 'Ignores the latency budget and failure paths, and proposes fixed-schedule retraining that trains on immature labels.',
      },
      {
        dim: 'Communication & structure',
        strong: 'Moves through the framework in order, states assumptions, quantifies the cost asymmetry, and defends each tradeoff (accuracy vs explainability, volume vs loss) crisply.',
        weak: 'Jumps to a model, gives vague answers on tradeoffs, and cannot connect design choices back to the business or regulatory constraints.',
      },
    ],
  },
  {
    id: 'feed-ranking-recsys',
    title: 'Personalized Feed Ranking',
    prompt: 'Design a personalized feed / recommendation ranking system.',
    context: `A social app serves a personalized home feed to ~50M daily active users, with a catalog of tens of millions of eligible items (posts, videos) refreshed constantly. Each feed request must return a ranked page in under ~200ms end-to-end, and the system handles peaks of ~300k requests per second. Labels are implicit engagement (clicks, watch time, likes, follows) that arrive within seconds to minutes, and the primary business metric is long-term engagement measured via online A/B tests.`,
    tags: ['recsys', 'retrieval', 'ranking', 'position bias', 'A/B testing'],
    stages: [
      {
        id: 'requirements',
        title: 'Clarify requirements & constraints',
        ask: 'Establish the objective (what engagement means and over what horizon), the scale, the latency budget, and the funnel structure before proposing any single model.',
        considerations: [
          'What "good" means: clicks vs watch time vs long-term retention, and whether short-term engagement is a proxy for a longer-term goal.',
          'Scale forces a funnel: you cannot run a heavy ranker over tens of millions of items in 200ms, so candidate generation must precede ranking.',
          'The latency budget (~200ms) has to be split across retrieval, ranking, and re-ranking, each with its own slice.',
          'Freshness: catalog and user interests change constantly, so embeddings and candidates must stay fresh, not stale from last week.',
          'Implicit-feedback labels are biased (position bias, presentation bias) and form a feedback loop: the model shapes what users see, which shapes the next batch of labels.',
          'Cold start on both sides: new users with no history and new items with no engagement.',
          'Business rules and diversity/integrity constraints that live in a re-ranking stage, not in the core relevance model.',
          'Offline vs online: offline metrics (NDCG, recall@k) only approximate the real objective, which is measured in A/B tests.',
        ],
        strong: [
          'Defines the objective concretely (e.g. optimize watch time and follows as a proxy for retention) and flags that short-term engagement can diverge from long-term value.',
          'Immediately reasons from scale to a funnel: tens of millions of items in 200ms rules out a single heavy model, so retrieval -> ranking -> re-ranking is forced, not a stylistic choice.',
          'Allocates the latency budget across stages up front (e.g. ~20ms retrieval, ~50-80ms ranking, ~10ms re-ranking) rather than treating 200ms as one bucket.',
          'Calls out that implicit labels are biased by position and by what the current system chose to show, creating a feedback loop that must be corrected.',
          'Names cold start for both users and items as a first-class requirement, not an edge case.',
          'Separates relevance (learned) from business/integrity/diversity rules (a re-ranking layer), so the objective stays clean.',
        ],
        traps: [
          'Proposing one big model to score the entire catalog, ignoring that scale makes that impossible in the latency budget.',
          'Optimizing raw CTR with no awareness of position bias or the short-vs-long-term engagement gap.',
          'Treating implicit feedback as unbiased ground truth.',
          'Forgetting cold start entirely.',
        ],
        probes: [
          'Why can you not just train one great ranking model and score every item?',
          'You optimize CTR and it goes up, but 30-day retention drops. What might be happening?',
          'How is a click on the top slot different, as a training label, from a click on the tenth slot?',
        ],
      },
      {
        id: 'architecture',
        title: 'High-level architecture',
        ask: 'Lay out the retrieval -> ranking -> re-ranking funnel, the feature store, the ANN index for retrieval, and where embeddings are computed and refreshed.',
        considerations: [
          'Candidate generation via two-tower retrieval: a user tower and an item tower producing embeddings, with items indexed in an ANN structure (e.g. HNSW/IVF).',
          'Multiple candidate sources unioned (two-tower, recent/trending, follow-graph, exploration) to feed the ranker a few thousand candidates.',
          'A heavier ranking model (e.g. a multi-task deep model) scoring the few thousand survivors with rich user/item/context features.',
          'A re-ranking stage applying diversity, freshness, integrity, and business rules on the top-scored items.',
          'A feature store serving user, item, and context features online within the latency budget, with matching offline features for training.',
          'Embedding freshness: item embeddings recomputed as content is created, user embeddings updated from recent activity (near-real-time), so the ANN index is not stale.',
          'Precomputation: item-tower embeddings computed offline/streaming and pushed to the index; the expensive user tower runs at request time or is cached.',
          'A logging pipeline that records impressions, positions, and engagement for training and for A/B analysis.',
        ],
        strong: [
          'Describes a two-tower retrieval model: user and item towers trained so dot-product/cosine approximates relevance, item embeddings served from an ANN index (HNSW or IVF-PQ) for sub-linear lookup.',
          'Unions several candidate sources (two-tower, trending/fresh, social-graph, an exploration bucket) so retrieval recall is not hostage to one model.',
          'Puts a heavier multi-task ranker (predicting several engagement heads: click, watch time, like) over the few thousand candidates, using user/item/context features.',
          'Adds a distinct re-ranking layer for diversity, freshness, and integrity rules, keeping those out of the learned relevance score.',
          'Designs a feature store with online serving that meets the latency budget and offline features that match it for training (no train/serve skew).',
          'Handles embedding freshness explicitly: item embeddings pushed to the index as content is published, user embeddings refreshed near-real-time so the funnel reflects current interests.',
        ],
        traps: [
          'A single retrieval source, so recall depends entirely on one model and misses fresh or long-tail items.',
          'Recomputing item embeddings only in a nightly batch, leaving the index hours or days stale.',
          'Folding diversity and business rules into the relevance model, muddying the objective and making it hard to tune.',
          'Ignoring the feature store, so serving features silently diverge from training features.',
        ],
        probes: [
          'How does an item published two minutes ago ever get retrieved for a user?',
          'Your NDCG is great offline but the feed feels repetitive. Which stage do you look at?',
          'Where exactly does a two-tower model spend its compute at serving time, and what can you precompute?',
        ],
      },
      {
        id: 'deep-dive',
        title: 'Deep-dive: the hard part',
        ask: 'Go deep on training-data generation under position bias and feedback loops, and on how retrieval recall sets a ceiling the ranker can never raise.',
        considerations: [
          'Retrieval recall is a hard ceiling: any item retrieval drops is unrankable, so a great ranker cannot fix poor retrieval recall.',
          'Position bias: an item shown at the top gets more clicks regardless of relevance, so raw click labels conflate relevance with position.',
          'Debiasing techniques: inverse-propensity weighting, position as a feature at train time zeroed at inference, or explicitly modeling examination.',
          'Feedback loops / popularity bias: the model recommends what it already favors, users engage, and that reinforces the same items, starving the tail.',
          'Exploration to break the loop: epsilon-style exploration or bandit-driven candidates so new/tail items accumulate signal.',
          'Negative sampling for two-tower training: in-batch negatives, hard negatives, and correcting for sampling bias.',
          'Multi-task ranking so the model balances several objectives rather than overfitting one biased signal like clicks.',
          'Delayed/partial labels for watch time and downstream actions, and how they enter training.',
        ],
        strong: [
          'States plainly that retrieval recall@k caps final quality: if recall@1000 is 0.6, 40% of the items the user would love are gone before ranking, and no ranker recovers them, so retrieval recall is audited first when quality is off.',
          'Handles position bias concretely: trains with position as a feature (set to a fixed value at inference) or uses inverse-propensity weighting, so the model learns relevance rather than position.',
          'Names the feedback loop and popularity bias as self-reinforcing, and adds deliberate exploration (bandit or epsilon candidates) so new and tail items get impressions and labels.',
          'Explains two-tower training details: in-batch negatives plus sampled hard negatives, with a correction (e.g. logQ / sampled-softmax correction) for the negative-sampling bias.',
          'Uses a multi-task ranker (click, watch time, like, follow heads) so the system does not chase a single click signal that is the most position-biased.',
          'Discusses handling watch-time and downstream labels that arrive later, and how logged impressions with positions feed both training and debiasing.',
        ],
        traps: [
          'Blaming a weak ranker for poor results without ever checking retrieval recall.',
          'Training directly on raw clicks with no position debiasing, so the model just learns to predict position.',
          'No exploration, so the feedback loop entrenches popular items and the tail dies.',
          'Two-tower training with random negatives only and no sampling-bias correction, giving weak retrieval.',
        ],
        probes: [
          'Your ranker is 0.95 AUC offline but users say obvious interests are missing. Retrieval recall@500 is 0.6. Where is the bug?',
          'How do you stop the model from simply learning that top-of-feed items are relevant?',
          'A great new creator gets no impressions because the model has never seen them engaged with. How do you break that cycle?',
        ],
      },
      {
        id: 'evaluation',
        title: 'Evaluation & monitoring',
        ask: 'Define offline ranking metrics, the online A/B metrics that are the real objective, and the monitoring that catches staleness, bias, and funnel regressions.',
        considerations: [
          'Offline metrics: recall@k for retrieval, NDCG/MAP for ranking, plus calibration if scores feed downstream logic.',
          'Offline metrics only approximate the goal because of feedback bias, so online A/B testing is the real arbiter.',
          'Online metrics: CTR, watch time, sessions, and guardrail metrics (diversity, integrity, long-term retention) in a controlled experiment.',
          'Counterfactual / off-policy evaluation to estimate a new policy before shipping it to live traffic.',
          'Monitoring retrieval recall and candidate-source health, since retrieval regressions silently cap quality.',
          'Embedding-freshness and index-staleness monitoring so fresh items are actually reachable.',
          'Feature drift and train/serve skew detection across the funnel.',
          'Guardrails against short-term-engagement optimization that hurts long-term retention.',
        ],
        strong: [
          'Separates offline (recall@k for retrieval, NDCG for ranking) from the online truth (A/B tests on watch time/sessions/retention with diversity and integrity guardrails).',
          'Explains that offline metrics are biased by the logging policy, so counterfactual/off-policy estimation is used to pre-screen candidates before an expensive A/B test.',
          'Monitors the funnel stage by stage: retrieval recall and per-source contribution, ranker calibration, and re-ranking effects, so a regression is localized quickly.',
          'Tracks embedding/index freshness explicitly, alerting when fresh items are not being retrieved (a staleness regression that offline metrics can miss).',
          'Adds guardrails so a CTR win that costs long-term retention does not ship, treating retention as the north star and short-term engagement as a proxy.',
          'Watches for train/serve skew and feature drift across the funnel, since implicit-feedback systems degrade quietly.',
        ],
        traps: [
          'Trusting offline NDCG as the final word and shipping without an A/B test.',
          'Only measuring CTR and ignoring diversity, integrity, and long-term retention guardrails.',
          'No per-stage monitoring, so a retrieval regression looks like a mysterious quality drop.',
          'Never checking index freshness, so fresh items silently stop appearing.',
        ],
        probes: [
          'Offline NDCG went up but the A/B test is flat. What are the likely explanations?',
          'How would you evaluate a new ranking policy before exposing real users to it?',
          'CTR is up 3% in the experiment but 30-day retention is down. Do you ship it?',
        ],
      },
      {
        id: 'tradeoffs',
        title: 'Tradeoffs, failure modes & scaling',
        ask: 'Discuss the funnel tradeoffs (recall vs latency vs cost), short-term vs long-term objectives, cold start, and the failure modes at 300k QPS.',
        considerations: [
          'Retrieval breadth vs latency/cost: more candidates raises recall but costs ANN latency and ranker compute.',
          'Ranker complexity vs the latency budget: a heavier model per item may blow the ~200ms budget at 300k QPS.',
          'Short-term engagement vs long-term retention: optimizing clicks can degrade the product even as metrics rise.',
          'Cold start: content-based features and exploration for new users/items until collaborative signal accrues.',
          'Feedback-loop / filter-bubble risk and the exploration cost needed to counter it.',
          'Serving reliability: ANN index or feature-store degradation, and fallbacks (popular/fresh feed) so the feed never fails.',
          'Cost at scale: caching user embeddings, precomputing item embeddings, and quantizing the ANN index to control spend.',
          'Staleness vs freshness of embeddings as a continuous tradeoff.',
        ],
        strong: [
          'Frames retrieval breadth as a recall/latency/cost knob: widening candidates raises recall but adds ANN and ranking cost, and picks an operating point against the 200ms budget.',
          'Keeps the ranker within budget at 300k QPS via feature caching, model distillation/quantization, and only scoring the few thousand retrieved candidates, not the catalog.',
          'Treats short-term vs long-term explicitly: uses long-term retention as the guardrail so a click-optimized model that erodes the product is caught.',
          'Solves cold start with content features and exploration: new users get context/trending, new items get exploration impressions until they earn collaborative signal.',
          'Plans graceful degradation: if the ANN index or feature store is slow, fall back to a cached or trending feed so users always get something.',
          'Controls cost by precomputing item embeddings, caching user embeddings, and using PQ/quantized ANN, noting that at this scale infra cost is a first-order design constraint.',
        ],
        traps: [
          'Maximizing recall by retrieving huge candidate sets with no regard for latency or cost.',
          'Shipping a heavier ranker without checking it fits the latency budget at peak QPS.',
          'Optimizing short-term clicks with no long-term guardrail.',
          'No fallback when retrieval or the feature store degrades, so the feed can go blank.',
        ],
        probes: [
          'You want to double retrieval candidates to lift recall. What breaks, and how do you pay for it?',
          'The ANN index is degraded during a spike. What does the user see?',
          'How do you keep a new creator from being permanently invisible while also not tanking feed quality with random content?',
        ],
      },
    ],
    rubric: [
      {
        dim: 'Requirements & scoping',
        strong: 'Defines the engagement objective (and its long-term proxy), reasons from scale to a mandatory funnel, splits the latency budget across stages, and flags position bias and cold start up front.',
        weak: 'Proposes one model to score the whole catalog, optimizes raw CTR, and ignores bias, cold start, and the latency split.',
      },
      {
        dim: 'Architecture & data flow',
        strong: 'Lays out two-tower retrieval with an ANN index, unioned candidate sources, a multi-task ranker, a re-ranking layer for rules/diversity, and a feature store with matched offline/online features and fresh embeddings.',
        weak: 'Single retrieval source, nightly-stale embeddings, business rules baked into the relevance model, and no feature store.',
      },
      {
        dim: 'Core ML depth (modeling & features)',
        strong: 'Explains retrieval recall as a hard ceiling, debiases position (IPW or position feature), corrects two-tower negative sampling, and adds exploration to break feedback loops.',
        weak: 'Trains on raw clicks with no debiasing, blames the ranker without checking recall, and uses random negatives with no correction.',
      },
      {
        dim: 'Evaluation (offline & online)',
        strong: 'Uses recall@k and NDCG offline, treats A/B tests as the real objective, applies off-policy evaluation to pre-screen, and enforces long-term retention guardrails.',
        weak: 'Trusts offline NDCG as final, measures only CTR, and ships without an experiment or guardrails.',
      },
      {
        dim: 'Reliability, monitoring & failure modes',
        strong: 'Monitors per-stage funnel health, embedding/index freshness, and train/serve skew, and provides a trending/cached fallback when retrieval or features degrade.',
        weak: 'No per-stage monitoring, no freshness checks, and no fallback, so regressions look like unexplained quality drops.',
      },
      {
        dim: 'Scaling, latency & cost',
        strong: 'Fits the ranker in the ~200ms budget at 300k QPS via caching, distillation, and candidate limiting, and controls cost with precomputed/quantized embeddings.',
        weak: 'Ignores the latency budget at peak, retrieves oversized candidate sets, and never addresses serving cost.',
      },
      {
        dim: 'Communication & structure',
        strong: 'Walks the funnel in order, justifies each stage from scale and latency, and defends tradeoffs (recall vs cost, short vs long term) with specifics.',
        weak: 'Jumps to a model, cannot explain why the funnel exists, and gives vague answers on bias and tradeoffs.',
      },
    ],
  },
  {
    id: 'fraud-detection',
    title: 'Real-Time Transaction Fraud Detection',
    prompt: 'Design a real-time fraud detection system for transactions.',
    context: `A payments platform authorizes ~5k transactions per second at peak and must return an approve/decline/review decision synchronously within ~50ms (the payment network is waiting). Fraud is well under 0.5% of transactions, but the cost asymmetry is severe: a missed fraud is a direct chargeback loss, while a wrongly blocked legitimate transaction is lost revenue and a furious customer. Ground-truth labels are delayed and noisy: chargebacks and confirmed-fraud reports arrive days to weeks later, and fraudsters actively adapt to the model.`,
    tags: ['fraud', 'imbalance', 'streaming features', 'concept drift', 'cost asymmetry'],
    stages: [
      {
        id: 'requirements',
        title: 'Clarify requirements & constraints',
        ask: 'Nail the decision (approve/decline/review), the sub-50ms synchronous budget, the extreme imbalance with severe cost asymmetry, and the delayed noisy labels before proposing a model.',
        considerations: [
          'The output is not binary: approve, decline, or route to a review queue, and each has a different cost.',
          'Latency is a hard ~50ms synchronous budget because the payment network blocks on the response.',
          'Extreme imbalance (fraud well under 0.5%) makes accuracy meaningless and PR-based metrics essential.',
          'Cost asymmetry: a false negative is a chargeback loss, a false positive is lost revenue plus customer harm, and the two are not equal.',
          'Labels are delayed (chargebacks arrive weeks later) and noisy (some fraud is never reported, some disputes are not fraud).',
          'Adversarial drift: fraudsters adapt, so the distribution shifts deliberately and continuously.',
          'Streaming/online features (velocity, aggregations over recent activity) must be computed within the latency budget.',
          'Temporal integrity: because of adaptation, evaluation must respect time order, never a random split.',
        ],
        strong: [
          'Defines the action space as approve/decline/review, and ties the review queue to the cost asymmetry: borderline cases go to humans rather than a hard decline.',
          'Treats the ~50ms synchronous budget as a hard constraint that bounds model complexity and feature computation, since the network is waiting.',
          'Frames the metric around extreme imbalance from the start: PR-AUC and precision@k, not accuracy or plain ROC-AUC.',
          'Quantifies the cost asymmetry and makes the operating threshold a cost-driven decision, not 0.5, with review as a middle option.',
          'Flags that labels are both delayed (weeks) and noisy (unreported fraud, non-fraud disputes), so the training signal is imperfect and lagging.',
          'Calls out adversarial concept drift explicitly and insists on temporal validation because of it.',
        ],
        traps: [
          'Framing it as plain binary classification and optimizing accuracy on a <0.5% positive class.',
          'Assuming labels are immediate and clean when chargebacks lag weeks and are noisy.',
          'Ignoring the FP/FN cost gap and picking a default 0.5 threshold.',
          'Using a random train/test split despite deliberate adversarial drift.',
        ],
        probes: [
          'Why is 99.6% accuracy a meaningless claim here?',
          'Chargebacks arrive three weeks after the transaction. What does that do to your training loop?',
          'Why is a random train/test split actively misleading for this problem?',
        ],
      },
      {
        id: 'architecture',
        title: 'High-level architecture',
        ask: 'Lay out the synchronous scoring path, the streaming feature pipeline for velocity/aggregation features, the review-queue routing, and the label-collection loop.',
        considerations: [
          'A synchronous scoring service in the authorization path that returns within ~50ms.',
          'A streaming feature pipeline (e.g. Flink/Kafka) computing velocity and aggregation features (transactions per card per hour, amount vs recent average) with an online store for low-latency reads.',
          'A feature store split into precomputed aggregates (updated by the stream) and request-time features (from the transaction itself).',
          'Consistency between the streaming features used online and the features reconstructed for training (point-in-time correctness to avoid train/serve skew).',
          'A decision layer mapping the score to approve/decline/review using cost-based thresholds.',
          'A review queue and case-management system that feeds back high-quality labels faster than chargebacks.',
          'A label-collection loop combining fast analyst labels with slow chargeback labels.',
          'A rules layer for known patterns and hard blocks alongside the ML model.',
        ],
        strong: [
          'Places a lightweight synchronous scorer in the auth path and computes velocity/aggregation features from a streaming pipeline with an online store read in single-digit ms.',
          'Distinguishes precomputed streaming aggregates (card velocity, device history, updated continuously) from request-time features, and reads both within the 50ms budget.',
          'Ensures the online feature values are reconstructable point-in-time for training, so the model trains on what it will actually see at serve time (no skew).',
          'Adds a decision layer that routes borderline scores to a human review queue rather than forcing approve/decline, exploiting the cost asymmetry.',
          'Designs the label loop to use analyst decisions and early fraud signals as faster labels, reconciled later with slow chargeback ground truth.',
          'Keeps a rules engine alongside the model for hard blocks and known attack patterns, so new attacks can be shut off immediately without retraining.',
        ],
        traps: [
          'A batch feature pipeline that cannot deliver up-to-the-second velocity features in the auth path.',
          'Streaming features online but recomputing them differently offline, guaranteeing train/serve skew.',
          'Only approve/decline with no review queue, forcing every borderline case into a costly error.',
          'Relying solely on ML with no rules layer to block a brand-new attack immediately.',
        ],
        probes: [
          'How do you compute transactions-per-card-in-the-last-hour inside a 50ms authorization?',
          'How do you make sure the velocity feature at training time equals the one at serving time?',
          'A new attack pattern appears at 2am. How do you block it before the next retrain?',
        ],
      },
      {
        id: 'deep-dive',
        title: 'Deep-dive: the hard part',
        ask: 'Go deep on the cost-asymmetric operating point under extreme imbalance, and on training under delayed, noisy labels with adversarial drift.',
        considerations: [
          'Extreme imbalance handling: class weighting, focal loss, or careful resampling, always paired with PR-based evaluation.',
          'The operating point is an expected-cost minimization: threshold(s) chosen from the FN (chargeback) vs FP (lost customer) costs, with a review band in between.',
          'Delayed labels: at any moment recent transactions are unlabeled, so training must account for label maturity and censoring.',
          'Noisy labels: unreported fraud (false negatives in the label) and disputed-but-legitimate transactions (false positives in the label) both corrupt the signal.',
          'Adversarial drift: fraud patterns shift deliberately, so a model trained on last quarter degrades fast; frequent retraining and drift detection are required.',
          'Velocity/graph features that capture coordinated fraud (shared devices, cards, IPs) that single-transaction features miss.',
          'Temporal validation: split by time and evaluate on the future, never randomly, to get an honest read under drift.',
          'A review queue as an active-learning source: analyst-labeled borderline cases are the highest-value training data.',
        ],
        strong: [
          'Chooses a cost-sensitive setup (class weights or focal loss) and evaluates with PR-AUC and precision/recall at the operating point, explicitly rejecting accuracy and plain ROC-AUC under <0.5% prevalence.',
          'Derives the threshold from expected cost: sets decline and review bands so expected chargeback loss is traded against expected lost-customer cost, not a default 0.5.',
          'Confronts delayed labels head-on: recent transactions are unlabeled, so training uses a matured window and treats the newest data as censored, with fast analyst labels bridging the gap.',
          'Names label noise concretely (unreported fraud, friendly-fraud disputes) and its effect on both training and measured precision/recall.',
          'Treats drift as adversarial: retrains frequently, monitors score and feature drift, and expects performance to decay between retrains as fraudsters adapt.',
          'Builds velocity and entity-graph features (shared card/device/IP) to catch coordinated rings that per-transaction features miss.',
          'Validates strictly temporally (train on past, test on future) and uses the review queue as an active-learning loop to label the hardest cases.',
        ],
        traps: [
          'Balancing the classes by resampling and then reading probabilities/precision as if the base rate were 50%.',
          'Choosing a 0.5 threshold and ignoring the FN-vs-FP cost gap entirely.',
          'Training on all recent data as if labels were mature, when the newest weeks are unlabeled or wrong.',
          'Evaluating with a random split, hiding the drift-driven degradation the model will suffer live.',
        ],
        probes: [
          'How do you set the decline threshold, and where does the review band come from?',
          'Some real fraud is never reported and some disputes are not fraud. How does that bias your measured precision and recall?',
          'Your model was great last month and is worse this month with no code change. What is the most likely cause and how do you confirm it?',
        ],
      },
      {
        id: 'evaluation',
        title: 'Evaluation & monitoring',
        ask: 'Define imbalance-aware offline metrics, temporal validation, the online metrics you can watch quickly, and drift monitoring for a deliberately adversarial environment.',
        considerations: [
          'Offline metrics for imbalance: PR-AUC, precision@k, recall at a fixed precision, and cost-weighted metrics, not accuracy.',
          'Temporal (out-of-time) validation so evaluation reflects performance under drift, never a random split.',
          'The label-lag problem: realized fraud/chargeback rates are weeks out, so leading indicators are needed.',
          'Online/leading metrics: decline rate, review-queue volume, approval-rate shifts, and early fraud signals available before chargebacks settle.',
          'Concept-drift monitoring on features and scores (PSI, distribution shift) as the primary early warning.',
          'False-positive impact monitoring: blocked-good-customer complaints and downstream churn, since FPs are expensive.',
          'Review-queue precision as a fast proxy for model quality on borderline cases.',
          'Alerting when score distribution or decline rate moves sharply, which signals either an attack or a broken feature.',
        ],
        strong: [
          'Uses PR-AUC and precision/recall at the operating point, plus cost-weighted metrics, and explicitly discards accuracy and plain ROC-AUC given the <0.5% base rate.',
          'Validates out-of-time (train on past weeks, test on the next), because a random split would hide the adversarial degradation the model faces live.',
          'Handles the label lag with leading indicators: decline rate, review volume, approval-rate shifts, and analyst-confirmed fraud, watched daily while chargebacks settle over weeks.',
          'Makes drift detection (PSI/score-distribution monitoring) the primary early-warning system, since fraud shifts deliberately and fast.',
          'Monitors false-positive impact directly (blocked-good-customer complaints, churn), treating a spike in FPs as an incident because of the cost asymmetry.',
          'Uses review-queue precision as a fast quality proxy and alerts on sharp decline-rate or score-distribution moves that indicate an attack or a broken feature pipeline.',
        ],
        traps: [
          'Reporting accuracy or plain ROC-AUC on a <0.5% positive class and calling it strong.',
          'Random-split evaluation that masks drift.',
          'No leading indicator, so degradation is only visible weeks later when chargebacks land.',
          'Monitoring fraud catch only, ignoring the false-positive cost side entirely.',
        ],
        probes: [
          'Chargeback labels are weeks out. What do you watch tomorrow to know the model is slipping?',
          'Your decline rate jumped from 1.2% to 3% overnight. Attack, drift, or a broken feature, and how do you tell?',
          'Why is a random 80/20 split a trap for evaluating this model?',
        ],
      },
      {
        id: 'tradeoffs',
        title: 'Tradeoffs, failure modes & scaling',
        ask: 'Discuss the core tradeoffs (fraud caught vs customers blocked, model complexity vs 50ms budget, retrain cadence vs adversarial drift) and the failure modes at 5k TPS.',
        considerations: [
          'Fraud caught vs legitimate customers blocked: the central operating-point tradeoff, mediated by the review queue.',
          'Model complexity vs the ~50ms budget: a heavier model may catch more fraud but blow the latency SLA at peak.',
          'Retrain cadence vs adversarial drift: retrain too slowly and fraudsters win, too fast and you chase noise or overfit recent attacks.',
          'Feedback loop / label bias: blocked transactions never generate a chargeback label, censoring the very cases the model acts on.',
          'Adversarial probing: fraudsters test small transactions to find the boundary, so the model must be robust to reconnaissance.',
          'Serving reliability: if the feature store or model is slow/down, a fallback (rules engine, conservative decline, or degraded score) within budget.',
          'Cost: at 5k TPS inference must be cheap and predictable; graph features and streaming state are the expensive parts.',
          'Human-in-the-loop capacity: the review queue has finite throughput, so review-band width must match analyst capacity.',
        ],
        strong: [
          'Frames the operating point as the fraud-caught vs customers-blocked dial and uses the review queue to soften it, sizing the review band to analyst capacity.',
          'Keeps the model within the 50ms budget (bounded-depth GBM or a small model plus rules) and notes that a heavier model that misses the SLA is disqualified at 5k TPS.',
          'Ties retrain cadence to adversarial drift: frequent retrains with drift-triggered refreshes, balancing responsiveness against overfitting to the latest attack.',
          'Recognizes the blocked-label feedback loop (declined transactions never yield chargeback labels) and proposes measures like a small allow-through sample or analyst review to keep learning on the boundary.',
          'Plans graceful degradation: on feature-store or model failure, fall back to the rules engine and conservative defaults within budget so the network still gets a timely decision.',
          'Addresses adversarial probing and cost: robustness to boundary-testing, and cheap predictable inference with the streaming/graph state as the real cost center.',
        ],
        traps: [
          'Maximizing fraud catch with no regard for the legitimate customers blocked or review-queue capacity.',
          'Proposing a heavy model that cannot meet the 50ms budget at 5k TPS.',
          'Fixed slow retrain cadence against an actively adapting adversary.',
          'No fallback when the feature pipeline or model degrades, stalling authorizations.',
        ],
        probes: [
          'You can catch 5% more fraud by declining 1% more transactions. How do you decide whether that is worth it?',
          'Because you block suspected fraud, those transactions never get a chargeback label. How does that bias your future model, and what do you do?',
          'The streaming feature store is degraded during a spike. What decision does the system return within 50ms?',
        ],
      },
    ],
    rubric: [
      {
        dim: 'Requirements & scoping',
        strong: 'Defines approve/decline/review, treats 50ms as hard, frames metrics around extreme imbalance and cost asymmetry, and flags delayed noisy labels and adversarial drift up front.',
        weak: 'Frames it as plain binary classification, optimizes accuracy on a <0.5% class, and assumes immediate clean labels with a random split.',
      },
      {
        dim: 'Architecture & data flow',
        strong: 'Lays out a synchronous scorer, a streaming velocity/aggregation feature pipeline with an online store, point-in-time-consistent training features, a review-queue label loop, and a rules layer.',
        weak: 'Uses batch features that cannot serve real-time velocity, has train/serve skew, offers only approve/decline, and relies solely on ML with no rules.',
      },
      {
        dim: 'Core ML depth (modeling & features)',
        strong: 'Uses cost-sensitive learning with PR-based evaluation, derives thresholds from FN/FP costs with a review band, handles delayed/noisy labels and drift, and builds entity-graph velocity features.',
        weak: 'Resamples then reads probabilities as if balanced, uses a 0.5 threshold, trains on immature labels, and misses coordinated-fraud features.',
      },
      {
        dim: 'Evaluation (offline & online)',
        strong: 'Reports PR-AUC and precision at the operating point, validates out-of-time, and pairs it with leading indicators (decline rate, review precision) given the weeks-long label lag.',
        weak: 'Reports accuracy/ROC-AUC on a rare class, uses a random split, and waits for chargebacks with no leading indicator.',
      },
      {
        dim: 'Reliability, monitoring & failure modes',
        strong: 'Makes drift/PSI monitoring the primary early warning, monitors false-positive impact and review-queue precision, and plans a rules/conservative fallback when features or the model degrade.',
        weak: 'Monitors only fraud catch, has no drift detection, and has no degraded-mode plan under the 50ms SLA.',
      },
      {
        dim: 'Scaling, latency & cost',
        strong: 'Fits the model in 50ms at 5k TPS (bounded model plus rules), ties retrain cadence to adversarial drift, sizes the review band to analyst capacity, and identifies streaming/graph state as the cost center.',
        weak: 'Proposes a model that misses the latency budget, uses a slow fixed retrain cadence, and ignores review-queue capacity and serving cost.',
      },
      {
        dim: 'Communication & structure',
        strong: 'Moves through the framework in order, keeps the cost asymmetry central, and defends each tradeoff (catch vs block, complexity vs latency, retrain cadence vs drift) with specifics.',
        weak: 'Jumps to a model, ignores the cost asymmetry, and gives vague answers on drift, labels, and tradeoffs.',
      },
    ],
  },
];

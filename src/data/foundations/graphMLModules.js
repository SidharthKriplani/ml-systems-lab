export const GRAPH_ML_MODULES = [
  {
    id: 'graph_representations',
    title: 'Graphs as ML Data Structures',
    subtitle: 'Adjacency formats, task types, permutation invariance, homophily, inductive vs transductive',
    difficulty: 'foundational',
    estimatedMin: 35,
    tags: ['graph', 'adjacency', 'CSR', 'homophily', 'permutation invariance', 'transductive'],
    summary: `The fundamental challenge graphs pose to neural networks is that there is no canonical ordering of nodes — the same graph can be represented by N! different adjacency matrices, so any valid graph neural network must be invariant to node permutations. Standard MLPs and CNNs assume a fixed, ordered input space (pixels, tokens), making them ill-suited for irregular, variable-size graphs. Graph signal processing offers an alternative intuition: convolution on a graph is a smoothing operation that diffuses features across edges, analogous to low-pass filtering on a grid. The choice between adjacency matrix and CSR (Compressed Sparse Row) format matters enormously in practice — most real-world graphs are sparse (|E| << |V|²) and CSR avoids quadratic memory while enabling efficient sparse matrix-vector products.`,
    keyPoints: [
      'Adjacency matrix A ∈ {0,1}^{NxN}: stores all N² entries regardless of edge count. Dense — O(N²) memory. Efficient for dense graphs (|E| ≈ N²). Prohibitively expensive for social graphs (N=10M nodes → 10^{14} entries). GCN on full adjacency requires O(N²) memory even at inference.',
      'CSR (Compressed Sparse Row): stores only nonzero entries. Three arrays: values (edge weights), col_indices (column of each entry), row_ptr (start of each row). Memory O(|E|+|V|). SpMM (sparse × dense) is the core GNN op — PyTorch Geometric and DGL both build on this. Message passing on CSR is naturally parallel across edges.',
      'Node-level tasks: classify or regress individual nodes (user fraud detection, protein function prediction). Edge-level tasks: predict existence or weight of edges (link prediction in recommender systems, knowledge graph completion). Graph-level tasks: classify entire graphs (molecular property prediction, circuit quality). The pooling mechanism required differs for each — node embeddings, edge embeddings, or readout function.',
      'Why standard NNs fail on graphs: (1) No fixed input size — graphs vary in |V| and |E|. (2) No canonical node ordering — the same node can appear at index 3 in one representation and index 87 in another. Permutation invariance requires f(PAP^T, Px) = f(A, x) for any permutation matrix P. MLPs applied to flattened adjacency matrices are not permutation invariant.',
      'Graph signal processing intuition: treat node features as signals on the graph. The graph Laplacian L = D - A captures the discrete gradient structure. Multiplying by L computes the difference between each node\'s feature and its neighbors\' mean — this is a high-pass filter. The GCN aggregation (adding A + I before normalising) is a low-pass filter that smooths features across edges. Over-smoothing: stacking too many layers makes all node embeddings converge to the same vector.',
      'Homophily vs heterophily: in homophilic graphs, connected nodes share the same label (social networks — friends have similar interests). Standard GNNs that average neighbor features work well here. In heterophilic graphs, connected nodes have different labels (fraud ring members connect to victims, bipartite recommendation graphs). Mean aggregation destroys signal in heterophilic settings — you need models that preserve the aggregated neighbor features separately from the ego node (H2GCN, CPGNN).',
      'Inductive vs transductive setting: transductive GNNs (spectral methods like vanilla GCN) train and test on the same fixed graph — they cannot generalise to unseen nodes or new graphs because the learned filters are specific to the trained graph\'s eigendecomposition. Inductive setting: model must generalise to new nodes/graphs at test time — required for production deployment where new users/items appear continuously. GraphSAGE, GAT, and most spatial methods are inductive by design.',
      'Heterogeneous graphs: multiple node types (user, item, category) and edge types (clicks, buys, viewed-together). Homogeneous GNNs ignore type information — relation-specific aggregation is critical for knowledge graphs and e-commerce graphs. Most real production graphs are heterogeneous.',
      'Dynamic graphs: most production graphs change over time (new users, new edges, item popularity shifts). Static GNNs retrained on snapshots miss temporal patterns. Temporal GNNs (TGAT, TGN) incorporate timestamps but add significant engineering complexity. Cold-start for new nodes with no edges is a hard open problem.',
    ],
    checkQuestions: [
      { q: 'You have a social network with 50M users and 5B edges. Explain concretely why you cannot use a standard dense adjacency matrix, and what data structure you would use instead.', a: 'Dense adjacency matrix: 50M × 50M = 2.5 × 10^{15} entries. Even as 1-bit booleans: 2.5 × 10^{15} / 8 ≈ 312 TB. Impossible. Use CSR: store only the 5B edges. Each entry is a (src, dst) pair plus optional weight — ~5B × 8 bytes ≈ 40 GB, feasible on a cluster. In PyTorch Geometric, the graph is stored as an edge_index tensor of shape [2, |E|] (COO format) which maps naturally to CSR for SpMM ops. At this scale you also need graph partitioning (METIS, random partitioning) to distribute across machines.' },
      { q: 'Explain what permutation invariance means for a graph neural network, and show why a 2-layer MLP applied to the flattened adjacency matrix is not permutation invariant.', a: 'Permutation invariance: f(A, X) = f(PAP^T, PX) for any permutation matrix P. The output is unchanged regardless of how nodes are numbered. MLP on flattened A: two different orderings of the same graph produce different 1D vectors after flattening — same graph, different inputs, different outputs. A valid GNN must be invariant to this reordering. Message passing GNNs achieve this by aggregating neighbor features with a permutation-invariant function (sum, mean, max) so the ordering of neighbors does not affect the output.' },
      { q: 'Your GNN for citation network node classification achieves 85% accuracy with 2 layers, but drops to 60% with 8 layers. What is happening and how do you fix it?', a: 'Over-smoothing: each GCN layer applies a low-pass filter. After 8 layers, each node\'s embedding is a weighted average of almost all nodes in its 8-hop neighborhood — all node embeddings converge to nearly the same vector, losing discriminative power. Fixes: (1) Residual connections — add the input to the output of each GNN layer (similar to ResNets). (2) JK-Net (Jumping Knowledge): concatenate embeddings from all layers, letting each node use the most informative aggregation depth. (3) Deeper architectures with DropEdge (randomly drop edges during training). (4) Limit depth to 2-4 layers in practice — most real graphs have small diameter so 2-3 hops capture most structural information.' },
      { q: 'Why do standard GNNs fail on heterophilic graphs, and name a method that handles them?', a: 'Standard GNNs aggregate neighbor features via mean/sum — if neighbors have different labels (heterophily), their features average out and the ego node loses discriminative signal. Example: in a fraud detection graph, a fraudster connects to many legitimate accounts — averaging their features makes the fraudster look legitimate. H2GCN addresses this by: (1) keeping ego-embedding separate from neighbor-embedding, (2) aggregating from 1-hop and 2-hop neighbors separately, (3) concatenating all rather than replacing the ego. CPGNN and FAGCN also handle heterophily. GPRGNN learns signed aggregation weights (can amplify rather than smooth) and works for both homo and heterophilic graphs.' },
    ],
    takeaway: `The fundamental constraint that makes GNNs different from every other neural network is permutation invariance — the same graph can be represented by N! different adjacency matrices, so any valid GNN must aggregate neighbor features with a permutation-invariant function (sum, mean, max). Everything else in GNN design follows from this constraint. In production, the choice between dense adjacency and CSR format is not a detail — for a 50M-node social graph, dense adjacency requires 312 TB of memory while CSR requires ~40 GB; the entire training and serving infrastructure depends on getting this right.`,
  },
  {
    id: 'spectral_gcn',
    title: 'Spectral Graph Convolution',
    subtitle: 'Graph Laplacian, ChebNet, Kipf & Welling GCN, renormalization trick, transductive limitation',
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['spectral', 'Laplacian', 'ChebNet', 'GCN', 'eigendecomposition', 'graph signal processing'],
    summary: `Spectral GNNs ground graph convolution in signal processing: the graph Laplacian plays the role of the frequency domain, and spectral filtering is defined by multiplying in eigenspace. The GCN by Kipf & Welling (2017) is a first-order approximation of Chebyshev spectral filters that avoids expensive eigendecomposition while retaining the smoothing behaviour — this approximation is the key insight that made GCNs practical. However, the renormalization trick (A + I) that stabilises training introduces a subtle coupling between the spectral filter and the graph topology, and spectral methods fundamentally cannot transfer to graphs not seen during training because their filters are functions of the trained graph\'s eigenstructure.`,
    keyPoints: [
      'Graph Laplacian: L = D - A where D is the diagonal degree matrix (D_{ii} = Σ_j A_{ij}). Normalized: L̃ = D^{-1/2} L D^{-1/2} = I - D^{-1/2} A D^{-1/2}. Eigendecomposition: L = UΛU^T where columns of U are eigenvectors and Λ is diagonal with eigenvalues λ_i ∈ [0, 2]. The eigenvectors form the graph Fourier basis.',
      'Graph Fourier transform: x̂ = U^T x (project signal onto eigenvectors). Inverse: x = Ux̂. Spectral filtering: ŷ = ĝ(Λ) ⊙ x̂, i.e., y = U ĝ(Λ) U^T x. The filter function ĝ(Λ) is applied pointwise to eigenvalues — analogous to multiplying in the frequency domain.',
      'Spectral filtering cost: eigendecomposition of L is O(N³). For N=1M nodes, completely infeasible. Also requires U explicitly — O(N²) memory. Solution: polynomial approximation. If ĝ(λ) = Σ_k θ_k λ^k, then U ĝ(Λ) U^T = Σ_k θ_k L^k — no need to compute eigenvectors. Convolution reduces to sparse matrix multiplications.',
      'ChebNet (Defferrard et al., 2016): approximate spectral filters with Chebyshev polynomials T_k(λ̃) where λ̃ = 2λ/λ_max - 1 ∈ [-1,1]. Filter: ĝ = Σ_{k=0}^{K} θ_k T_k(L̃). K-th order ChebNet considers K-hop neighborhoods. Chebyshev polynomials are chosen for numerical stability (min-max optimal approximation) and efficient recurrence: T_k(x) = 2xT_{k-1}(x) - T_{k-2}(x).',
      'Kipf & Welling GCN (2017): simplify ChebNet to K=1 (first-order), approximate λ_max ≈ 2. Filter becomes: θ₀(I) + θ₁(L̃) = θ₀I - θ₁D^{-1/2}AD^{-1/2}. Further simplify: θ₀ = -θ₁ = θ → single parameter per feature. Propagation rule: H^{(l+1)} = σ(D^{-1/2} Ã D^{-1/2} H^{(l)} W^{(l)}) where Ã = A + I.',
      'Renormalization trick: adding self-loops (A → A + I = Ã) before degree normalisation prevents numerical instability (nodes with degree 0) and ensures each node\'s own features contribute to its update. Without self-loops, a node with no edges would receive a zero vector. The trick also shifts the spectral range of the normalized adjacency from [-1,1] to approximately [0,1], improving gradient flow.',
      'Why spectral methods don\'t transfer: the eigenvectors U are specific to the trained graph. A new graph has different eigenvectors — you cannot apply the learned filter (which is a function in eigenspace) to a new graph without recomputing the full eigendecomposition. This makes spectral GCNs transductive — trained and tested on the exact same graph. PinSage (production at Pinterest) uses spatial methods for this reason.',
      'Spectral insight that survives: low eigenvalues correspond to smooth signals (slowly varying across the graph), high eigenvalues to rapidly oscillating signals. GCN applies a low-pass filter — it smooths features across edges. This is exactly the right prior for homophilic graphs where neighbors share labels. For heterophilic graphs, a high-pass filter would be better, but standard GCN always smooths.',
      'Scalability issue for spectral GCNs: even the first-order approximation requires the full adjacency matrix for Ã D^{-1/2} H at each layer. For large graphs, the matrix-vector product ÃH is O(|E| × d) per layer. Mini-batching requires careful handling of multi-hop neighborhoods to avoid neighbor explosion — not naturally handled by spectral formulations.',
    ],
    checkQuestions: [
      { q: 'Derive the GCN propagation rule from spectral filtering. Why does Kipf & Welling set K=1 and approximate λ_max=2?', a: 'Start from ChebNet K=2: ĝ ≈ θ₀T₀(L̃) + θ₁T₁(L̃) = θ₀I + θ₁L̃. With λ_max=2: L̃ = 2L/λ_max - I = L - I = D^{-1/2}LD^{-1/2} - I. So ĝ = θ₀I + θ₁(D^{-1/2}LD^{-1/2} - I) = (θ₀-θ₁)I - θ₁D^{-1/2}AD^{-1/2}. Setting θ = θ₀ = -θ₁: ĝ = θ(I + D^{-1/2}AD^{-1/2}). Add self-loops: Ã = A+I, D̃ = degree matrix of Ã. Final: H^{l+1} = σ(D̃^{-1/2}ÃD̃^{-1/2} H^l W^l). K=1 limits to 1-hop aggregation, reduces parameters, avoids overfitting. λ_max≈2 is an approximation — true value is ≤ 2 for normalized L, exact value varies per graph. Approximating prevents λ_max computation (expensive for large graphs).' },
      { q: 'A teammate proposes to train a spectral GCN on a protein-protein interaction network and then apply it to a new PPI network with different proteins. What is the fundamental problem and how do you fix it?', a: 'The trained GCN\'s learned filters are defined in terms of the eigenspace of the training graph\'s Laplacian. The new graph has a different Laplacian with different eigenvectors — there\'s no mapping between the two eigenspaces. The filter literally cannot be applied. Fix: switch to a spatial/message-passing GNN (GraphSAGE, GAT). These methods define aggregation functions over neighborhoods, not in eigenspace — they generalize naturally to unseen nodes and new graphs. Alternatively, for spectral methods: learn filters as polynomials of the Laplacian (ChebNet K-hop), since polynomial functions of L^k can be applied to any graph using sparse SpMM — the θ_k coefficients transfer across graphs.' },
      { q: 'What is over-smoothing in GCNs, and what is its formal spectral interpretation?', a: 'Over-smoothing: after many GCN layers, all node embeddings converge to the same vector, making nodes indistinguishable. Spectral interpretation: each GCN layer applies the low-pass filter D̃^{-1/2}ÃD̃^{-1/2}. Its eigenvalues are in [0,1] (normalized adjacency). With L layers, the filter is applied L times — eigenvalues are raised to the L-th power. High-frequency components (λ close to 0) vanish exponentially fast; only the dominant eigenvector (λ=1, corresponding to the stationary distribution of the random walk) survives. After many layers, H ≈ projection onto the dominant eigenvector — a constant times the degree sequence. All nodes look alike. Fix: residual connections, JK-Net, or limit depth to 2-4.' },
    ],
    takeaway: `The Kipf & Welling GCN is a K=1 first-order approximation of Chebyshev spectral filters that avoids eigendecomposition — this is what made GCNs practical. But the approximation comes with a hard limitation: spectral filters are defined in the eigenspace of a specific graph's Laplacian, so they cannot transfer to graphs not seen during training. This is why all production GNN systems (PinSage, fraud detection, drug discovery) use spatial/message-passing methods rather than spectral ones — inductive generalization to new nodes and graphs is a hard requirement.`,
  },
  {
    id: 'spatial_gcn',
    title: 'Spatial & Message-Passing GCNs',
    subtitle: 'GraphSAGE, neighbor sampling, aggregators, inductive learning, scalability',
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['GraphSAGE', 'message passing', 'neighbor sampling', 'inductive', 'aggregation', 'scalability'],
    summary: `GraphSAGE (Hamilton et al., 2017) reframes graph convolution as neighborhood sampling and aggregation rather than spectral filtering — each node aggregates a fixed-size sample of its neighbors using a learned function. The critical insight is that the aggregation function (not the weights) must be permutation invariant, and the resulting model is inductive: trained once on a known graph, it can generate embeddings for previously unseen nodes by running the same neighborhood aggregation procedure. This shifts GNNs from a matrix-factorization paradigm (transductive, requires the full graph) to a function-learning paradigm (inductive, generalizes to new data), which is the foundation of all production-scale GNN systems.`,
    keyPoints: [
      'GraphSAGE algorithm: for each node v, (1) sample a fixed-size neighborhood N(v) ⊆ neighbors(v), (2) aggregate neighbor features: h_N(v) = AGGREGATE({h_u : u ∈ N(v)}), (3) concatenate ego + aggregated: h_v = σ(W · CONCAT(h_v, h_N(v))). Repeat for K layers. Final embedding uses K-hop neighborhood information.',
      'Neighbor sampling for scalability: without sampling, K-layer GNN on a node requires its full K-hop neighborhood — exponential fan-out. A node with 100 neighbors needs 100 1-hop, 10,000 2-hop, 1M 3-hop neighbors at each forward pass. GraphSAGE samples a fixed |S₁| neighbors at hop 1, |S₂| at hop 2 — bounded computation per node (e.g., |S₁|=25, |S₂|=10 → 250 nodes maximum for 2-layer embedding).',
      'Mean aggregator: h_N(v) = σ(W · MEAN({h_u : u ∈ N(v) ∪ {v}})). Equivalent to GCN\'s normalized aggregation without self-loops. Simple, effective, but treats all neighbors equally. The original paper shows that CONCAT(ego, MEAN(neighbors)) outperforms pure MEAN by preserving the central node\'s identity.',
      'LSTM aggregator: apply an LSTM to a random permutation of neighbor features. Captures sequential dependencies but: (1) not permutation invariant (different random orderings give different results at inference), (2) more parameters, slower. Hamilton et al. find it empirically better than mean for some tasks despite the theoretical shortcoming — the LSTM may be learning a useful implicit ordering.',
      'Max-pooling aggregator: h_N(v) = max({σ(W_pool · h_u + b) : u ∈ N(v)}). Applies an elementwise max after a learned transformation. Captures the most activated feature across neighbors — useful when some neighbors are highly informative and others are noise. Often best for node classification on heterophilic graphs.',
      'Inductive vs transductive (Hamilton\'s key insight): GCN (Kipf) is trained with the full graph adjacency as input — weights are tied to the specific graph structure via spectral filter in graph eigenspace. GraphSAGE learns aggregation functions that map neighborhood features to embeddings — the same function applies to any neighborhood. New nodes: sample their neighbors, run the same K-layer aggregation, get an embedding without retraining. Critical for dynamic graphs where new users/items arrive continuously.',
      'Mini-batch training: for a batch of target nodes, compute the required computation graph by expanding neighborhoods layer by layer. For K=2 with sample sizes [10, 25]: a batch of 512 nodes requires ~512×25 = 12,800 1-hop nodes and ~12,800×10 = 128,000 2-hop nodes. This is the GraphSAGE mini-batch algorithm — nodes are fetched from a feature store and the neighborhood structure from a graph database.',
      'Production engineering: at Pinterest (PinSage), random-walk-based neighborhood sampling is used instead of uniform sampling — nodes visited more frequently in random walks from v are more important neighbors. Feature store (Redis/RocksDB for node features) and graph store (adjacency lists) are separate systems. Offline embedding computation via MapReduce for the full graph; online serving via approximate nearest neighbor (FAISS/ScaNN).',
      'Comparison with spectral GCN: GraphSAGE has more hyperparameters (aggregator type, sample sizes, layers) but is strictly more general. For transductive tasks on small graphs, GCN (Kipf) often achieves similar or better accuracy. For inductive tasks, large graphs, or dynamic graphs, GraphSAGE (and its variants) is the practical choice.',
    ],
    checkQuestions: [
      { q: 'A 3-layer GraphSAGE with neighbor sample sizes [15, 10, 5] is used to embed a batch of 256 target nodes. How many total nodes might be loaded from the feature store in the worst case?', a: 'Each target node samples 5 neighbors at depth 3. Each of those samples 10 at depth 2. Each of those samples 15 at depth 1. Worst case (no overlap): 256 × 5 = 1,280 at depth-3, × 10 = 12,800 at depth-2, × 15 = 192,000 at depth-1. Total: 256 + 1,280 + 12,800 + 192,000 = 206,336 nodes. With neighborhood overlap (common in dense graphs), this is much less in practice. This is why sample sizes are kept small (5-25) and why 2-3 layers are typical — deeper models require orders of magnitude more feature lookups.' },
      { q: 'Your GraphSAGE model is trained on a social network. A new user signs up with 3 connections to existing users. How do you compute their embedding without retraining?', a: 'This is exactly the inductive capability of GraphSAGE. Run forward pass for the new node: (1) fetch features of the 3 connected users (1-hop), (2) sample their neighbors (2-hop), (3) run the K-layer aggregation using trained weights. No retraining required. Contrast with transductive methods (spectral GCN, DeepWalk): these require adding the new node to the adjacency matrix and rerunning the optimization from scratch, or at minimum rerunning eigenvector computation. Cold-start caveat: if the new user has 0 connections, GraphSAGE falls back to the ego-only embedding (just W·x_v) — in practice, inject content-based features (profile, age, etc.) as node features to handle this.' },
      { q: 'Why is the LSTM aggregator in GraphSAGE theoretically flawed for graph learning, and under what conditions might it still be empirically useful?', a: 'LSTM is a sequential model — its output depends on the order of inputs. Neighbor sets have no canonical ordering (permutation invariance requirement). Different orderings of the same neighbor set yield different aggregated embeddings at inference time. This is theoretically incorrect for graph learning. Empirically it may still work because: (1) with random orderings during training, the model learns to be robust to ordering variations (acts as a form of augmentation), (2) in practice, node IDs impose a consistent ordering that the LSTM can exploit as a useful but spurious signal, (3) LSTMs have more parameters than mean/max aggregators and higher model capacity. Use max-pooling instead if you want both good empirical performance and theoretical soundness.' },
    ],
    takeaway: `GraphSAGE's critical insight is framing GNN as function learning rather than matrix factorization — the aggregation function maps any neighborhood to an embedding, so it naturally extends to unseen nodes and new graphs at inference time. This inductivity is non-negotiable in production: new users, new items, and new nodes arrive continuously, and a method that requires full graph retraining is not deployable. Neighbor sampling (fixed fan-out per hop) controls the exponential neighborhood explosion that makes full-batch K-layer GNNs intractable on large graphs.`,
  },
  {
    id: 'graph_attention',
    title: 'Graph Attention Networks',
    subtitle: 'Attention coefficients, multi-head GAT, GATv2 dynamic attention, edge features, when attention wins',
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['GAT', 'GATv2', 'attention', 'edge features', 'heterophily', 'multi-head'],
    summary: `Graph Attention Networks (Veličković et al., 2018) replace fixed neighborhood aggregation with learned, data-dependent edge weights — each neighbor's contribution is weighted by an attention coefficient computed from both node feature vectors. The key advantage over mean aggregation is that the model can learn to ignore noisy or irrelevant neighbors and focus on informative ones. However, the original GAT has a subtle flaw: its attention is computed from a linear function of concatenated features before any nonlinearity, making it a static ranking that cannot distinguish nodes that differ only in how their features interact — GATv2 (Brody et al., 2022) fixes this by applying the nonlinearity before the final linear projection.`,
    keyPoints: [
      'GAT attention coefficient: e_{ij} = LeakyReLU(a^T · CONCAT(W·h_i, W·h_j)) where a is a learnable attention vector and W is a shared linear transformation. Softmax over neighbors: α_{ij} = softmax_j(e_{ij}) = exp(e_{ij}) / Σ_{k∈N(i)} exp(e_{ik}). Aggregation: h\'_i = σ(Σ_j α_{ij} W h_j).',
      'GATv2 fix (Brody et al., 2022): original GAT computes e_{ij} = a^T · LeakyReLU(W₁h_i + W₂h_j). This is a linear function of a nonlinear function of a linear combination — critically, the ranking of neighbors is the same regardless of i. GATv2: e_{ij} = a^T · LeakyReLU(W · CONCAT(h_i, h_j)). The nonlinearity is applied inside the concatenation-linear product — different source nodes i can produce different neighbor rankings. GATv2 is a strictly more expressive (dynamic) attention.',
      'Multi-head attention: run K independent attention heads with separate (W^k, a^k) parameters. For hidden layers: concatenate K heads → h\'_i = ‖_{k=1}^K σ(Σ_j α^k_{ij} W^k h_j). For the final layer: average the K heads → h\'_i = σ((1/K) Σ_k Σ_j α^k_{ij} W^k h_j). K=8 heads is typical. Each head attends to a different aspect of the graph structure.',
      'Edge features in attention: standard GAT ignores edge features. Extension: e_{ij} = a^T · σ(W₁h_i + W₂h_j + W_e · e_{ij}^{feat}) where e_{ij}^{feat} is the edge feature vector (e.g., relationship type, edge weight, time delta). This is critical in knowledge graphs where edge type determines the semantic relationship. RGAT (Relational GAT) uses relation-specific attention parameters.',
      'Computational complexity: same O(|V|·d² + |E|·d) as GraphSAGE. The attention coefficient computation is O(|E|) — one coefficient per edge. Multi-head with K heads is K× more expensive. In practice, GAT is ~2-5× slower than GraphSAGE at the same hidden dimension due to coefficient computation and softmax over variable-size neighbor sets.',
      'When attention outperforms mean aggregation: (1) heterophilic graphs — attention can learn to downweight neighbors with different labels; (2) graphs with noisy edges (spurious connections, bots in social networks) — attention suppresses uninformative neighbors; (3) graphs with highly variable degree — attention naturally normalizes while mean is sensitive to node degree; (4) tasks where local structure varies significantly across the graph.',
      'Attention visualization: α_{ij} is interpretable — high weight means node j is important to node i. In practice, attention weights in GNNs are often surprisingly uniform (the model spreads attention across many neighbors rather than focusing sharply), especially in homophilic graphs where all neighbors are equally informative. Do not assume GAT attention is always semantically meaningful.',
      'Failure mode — attention collapse: in deep GAT networks with many layers, attention weights can concentrate on the same few neighbors (hubs) across all layers, causing information bottleneck and over-smoothing. Fix: dropout on attention weights during training (DropEdge or attention dropout). The original GAT paper uses dropout on attention coefficients α_{ij} — this regularizes the attention and prevents collapse.',
      'GAT for heterogeneous graphs: different edge types should have different attention mechanisms. HAN (Heterogeneous Attention Network) uses meta-path-based attention: first, node-level attention within each meta-path; second, semantic-level attention across meta-paths. This two-level hierarchy handles complex heterogeneous graph structure.',
    ],
    checkQuestions: [
      { q: 'What is the static attention problem in the original GAT, and how does GATv2 solve it?', a: 'In GAT, e_{ij} = a^T · LeakyReLU(W₁h_i + W₂h_j). The attention function can be written as a^T · LeakyReLU(f(i) + g(j)) where f(i) = W₁h_i is a function of i alone and g(j) = W₂h_j is a function of j alone. This means the ranking of neighbors j is the same for all source nodes i — it is "static" attention. Formally, if e_{ij} > e_{ik} for some source node i, then e_{i\'j} > e_{i\'k} for all other source nodes i\'. GATv2: e_{ij} = a^T · LeakyReLU(W · [h_i || h_j]). The concatenation is followed by a single linear layer W, then nonlinearity, then projection a^T. Now the interaction between h_i and h_j is captured before the nonlinearity — different source nodes i produce different neighbor rankings. This is called "dynamic" attention.' },
      { q: 'You are building a fraud detection GNN. The graph has legitimate users with many connections (hubs) and fraudsters with few connections. Why might mean aggregation fail, and how would GAT help?', a: 'Mean aggregation fails for two reasons: (1) Heterophily — legitimate users connect to both legitimate and fraudulent accounts; mean averaging dilutes the fraud signal. (2) Degree imbalance — a legitimate user hub with 1000 neighbors gets an embedding that is an average of all 1000, dominated by the majority class. GAT can learn to assign high attention weights to the few suspicious neighbors (unusual transaction amounts, rare merchant categories) and downweight the majority of normal neighbors. The attention mechanism effectively performs learned feature selection at the neighborhood level. In practice, also add edge features (transaction amount, time of day, merchant type) to the attention computation to make it transaction-aware.' },
      { q: 'Explain why the softmax normalization in GAT can be problematic in graphs with very high-degree nodes, and what you would do about it.', a: 'High-degree nodes (hubs): softmax normalizes over |N(i)| neighbors, so each attention weight α_{ij} = exp(e_{ij}) / Σ_k exp(e_{ik}). For a hub with 10,000 neighbors, even if one neighbor is highly relevant, its maximum possible attention weight is 1/1 in the extreme case, but the softmax denominator grows with degree. The hub\'s aggregated embedding is dominated by the large denominator — effectively a weighted average with similar weights for all neighbors. Alternatives: (1) top-K attention (keep only the K highest attention edges, renormalize); (2) sigmoid attention (no normalization, each neighbor independently gates); (3) sample neighbors first (GraphSAGE-style), then apply attention over the sample — bounds the effective denominator; (4) degree normalization: divide e_{ij} by √deg(i) before softmax.' },
    ],
    takeaway: `GATv2 fixes a subtle but important flaw in the original GAT: the original attention is "static" — the ranking of neighbors is the same regardless of which source node is doing the attending, because the nonlinearity is applied to linearly separable source and target terms. GATv2 applies the nonlinearity after concatenating source and target features, producing "dynamic" attention where different source nodes can rank their neighbors differently. This matters whenever the relevance of a neighbor genuinely depends on the identity of the querying node, which is the typical case in heterophilic or heterogeneous graphs.`,
  },
  {
    id: 'message_passing_framework',
    title: 'Message Passing Neural Networks (MPNN)',
    subtitle: 'Unified MPNN view, 1-WL test, expressiveness limits, higher-order GNNs, graph Transformers',
    difficulty: 'advanced',
    estimatedMin: 60,
    tags: ['MPNN', 'Weisfeiler-Leman', 'expressiveness', 'higher-order', 'graph Transformer', 'isomorphism'],
    summary: `The MPNN framework (Gilmer et al., 2017) provides a unified view of GCN, GAT, GraphSAGE, and most other GNN variants as instances of the same message-passing paradigm: compute messages from edges, aggregate them at nodes, and update node states. This unification reveals a fundamental expressiveness ceiling: any MPNN is at most as powerful as the 1-dimensional Weisfeiler-Leman (1-WL) graph isomorphism test, which means MPNNs cannot distinguish certain non-isomorphic graphs. This is not a theoretical curiosity — it has direct practical implications for molecular property prediction, where common substructures are structurally indistinguishable to 1-WL-equivalent GNNs.`,
    keyPoints: [
      'MPNN framework (Gilmer et al., 2017): three phases per layer. (1) Message: m_{vw}^t = M_t(h_v^t, h_w^t, e_{vw}) for each edge (v,w). (2) Aggregate: a_v^t = Σ_{w∈N(v)} m_{vw}^t (or max, or any permutation-invariant function). (3) Update: h_v^{t+1} = U_t(h_v^t, a_v^t). GCN: M=WH_w, aggregate=sum with normalization, U=σ. GAT: M=αWh_w, aggregate=weighted sum. GraphSAGE: M=Wh_w, aggregate=mean/max.',
      '1-Weisfeiler-Leman (1-WL) test: algorithm for graph isomorphism. At each step, each node gets a new color = hash(current color, multiset of neighbor colors). Repeat until stable. Two graphs are distinguished if they produce different final color histograms. The test is necessary but not sufficient for isomorphism — it can fail to distinguish non-isomorphic graphs.',
      'GIN (Graph Isomorphism Network): MPNNs with a sum aggregator and MLP update function are maximally expressive among MPNNs — exactly as powerful as 1-WL. Key theorem (Xu et al., 2019): if the aggregation function is injective over multisets (distinguishes different multisets), the MPNN is 1-WL equivalent. GIN: h_v^{l+1} = MLP((1+ε)h_v^l + Σ_{u∈N(v)} h_u^l). The (1+ε) term ensures injectivity over the self+neighbor combination.',
      '1-WL failure cases: regular graphs — two different k-regular graphs with the same number of nodes have identical 1-WL coloring (all nodes look the same). Cycles — 1-WL cannot distinguish a 3-cycle + 3-cycle from a 6-cycle. Rook graph vs Shrikhande graph. In chemistry, 1-WL cannot distinguish certain symmetric molecules, limiting GNN accuracy for property prediction.',
      'Higher-order WL and GNNs: k-WL tests work on k-tuples of nodes — exponentially more powerful but exponentially more expensive. k-GNN: computes messages between k-tuples. DS-GNN, PPGN (Provably Powerful Graph Networks) approximate higher-order expressiveness with better scalability. Practical insight: for most real-world tasks, 1-WL is sufficient — the graphs that fool 1-WL rarely appear in practice.',
      'Structural features that help: degree, triangle count, clustering coefficient, eigenvector centrality — these can be precomputed and added as node features. Random features (random positional encodings) can also break symmetry and improve expressiveness empirically. Relative positional encodings (shortest-path distance matrix, resistance distance) are used in graph Transformers.',
      'Graph Transformers: apply Transformer self-attention to all pairs of nodes (not just edges). Every node attends to every other node — O(|V|²) complexity. Positional encodings encode graph structure (Laplacian eigenvectors as PE in Graphormer, random walk PE in GPS). Expressive beyond 1-WL because attention is over all node pairs, not just edges. Graphormer (Microsoft) achieves state-of-the-art on molecular benchmarks. GPS (General, Powerful, Scalable) combines local MPNN with global Transformer attention.',
      'Practical expressiveness gap: GCN and GraphSAGE (mean aggregator) are strictly less powerful than 1-WL because mean cannot distinguish different multisets with the same mean (e.g., {1,2,3} and {1,1,4} have the same mean = 2). Sum aggregator (GIN) is 1-WL equivalent. In practice, GCN/GraphSAGE often match GIN on benchmark tasks — the theoretical gap matters most for tasks sensitive to exact substructure counts (molecular chemistry, combinatorial optimization).',
      'Over-squashing: information from distant nodes must be compressed into fixed-size embeddings through narrow graph bottlenecks. In a tree-like graph, the single bridge node between two subtrees must pass all information — this causes the gradient to vanish through the bridge. Symptom: GNNs fail on tasks requiring long-range dependencies. Fix: graph rewiring (add edges between distant relevant nodes), virtual nodes (one global node connected to all), or graph Transformers.',
    ],
    checkQuestions: [
      { q: 'Show that a mean-aggregation GNN (GCN) is strictly less expressive than 1-WL. Give a concrete example of two different neighborhood structures that GCN cannot distinguish.', a: 'Mean aggregation is not injective over multisets. Consider two nodes: node A has neighbors with features {1, 2, 3} (mean=2), node B has neighbors with features {1, 1, 4} (mean=2). The mean aggregated value is identical — GCN produces the same embedding for A and B. They are treated as equivalent despite having different local structures. 1-WL uses a hash of the multiset, which distinguishes {1,2,3} from {1,1,4}. Concrete graph example: two nodes in a graph, one with 2 neighbors of feature [1,3] (mean=2) and one with 2 neighbors of feature [2,2] (mean=2) — GCN cannot tell them apart. GIN uses sum instead of mean: Σ{1,3}=4 ≠ Σ{2,2}=4 wait — also fails for this case. Use MLP(sum) with sufficient capacity: hash({1,3}) ≠ hash({2,2}) even with the same sum, if the MLP is expressive enough.' },
      { q: 'What is over-squashing in GNNs, and how would you diagnose and fix it in a production model?', a: 'Over-squashing: information from distant nodes in a K-layer GNN must be squeezed through narrow graph topological bottlenecks — bridges, articulation points, sparse paths. The Jacobian of a node v\'s embedding with respect to a distant node u\'s features decays exponentially with the number of bottleneck nodes on the path. Symptom: model accuracy degrades on tasks requiring reasoning over long-range node pairs; removing distant node features doesn\'t change predictions. Diagnosis: compute the sensitivity ∂h_v/∂x_u for relevant (v,u) pairs — low sensitivity for distant but important pairs indicates over-squashing. Fixes: (1) Graph rewiring — add shortcuts (k-nearest-neighbor in embedding space, DiffWire). (2) Virtual node — add one node connected to all others; it acts as a highway for global information. (3) Graph Transformer — Graphormer or GPS; global attention bypasses topological bottlenecks. (4) Longer paths via positional encodings (RWPE encodes random walk statistics that capture graph-level structure).' },
      { q: 'GIN achieves maximal 1-WL expressiveness. Why does it still fail to distinguish some pairs of non-isomorphic graphs, and what class of graphs is this?', a: 'GIN is 1-WL equivalent — no more, no less. 1-WL itself fails on certain graph pairs. The clearest examples: (1) Any two non-isomorphic k-regular graphs of the same size (e.g., two different 3-regular graphs with 6 nodes). All nodes in a k-regular graph have the same degree and identical 1-hop neighborhoods (all of degree k) — 1-WL produces a single color for all nodes in both graphs. GIN cannot distinguish them. (2) The Rook graph and the Shrikhande graph — both are 6-regular on 16 nodes, 1-WL equivalent. (3) Decalin and bicyclo[2.2.2]octane in chemistry — similar issue. The fix is higher-order GNNs (k-WL, k>1), node positional encodings that break symmetry (random or deterministic), or structural features like triangle count that 1-WL cannot encode.' },
      { q: 'Explain why graph Transformers are more expressive than MPNNs and what practical tradeoff this introduces at scale.', a: 'Standard MPNNs pass messages only along existing edges — a node can only aggregate information from nodes within K hops after K layers. Graph Transformers apply attention between ALL pairs of nodes (or a sampled/approximate subset): node i can directly attend to node j regardless of whether there is an edge. This: (1) eliminates over-squashing — distant nodes communicate directly; (2) increases expressiveness beyond 1-WL because the model sees all pairwise relationships simultaneously. Tradeoff: O(|V|²) attention complexity. For a graph with 10,000 nodes: 10^8 attention pairs per head, 8 heads → 8×10^8 operations per layer. Infeasible for large graphs. Solutions: (1) Limit to top-K nearest neighbors in feature space (approximates sparse attention). (2) GPS framework: combine local MPNN (O(|E|)) with global attention on sampled nodes. (3) Big Bird / Longformer-style sparse attention patterns. (4) Cluster-based attention: attend within clusters only. In practice, graph Transformers are used for small molecular graphs (≤100 atoms) where O(|V|²) is tractable.' },
    ],
    takeaway: `Every MPNN is bounded in expressiveness by the 1-Weisfeiler-Leman test — and mean-aggregation GNNs (GCN, GraphSAGE) are strictly below that ceiling because mean cannot distinguish multisets with the same average. GIN with sum aggregation reaches the 1-WL ceiling. The practical interview point is that this expressiveness limit matters most for molecular chemistry and combinatorial tasks (where exact substructure counts matter), but for most node classification and link prediction tasks on real-world graphs, the theoretical gap between GCN and GIN closes empirically. Know when the gap matters.`,
  },
  {
    id: 'link_prediction',
    title: 'Link Prediction',
    subtitle: 'Heuristics, embedding decoders, knowledge graph completion, negative sampling, evaluation pitfalls',
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['link prediction', 'knowledge graph', 'TransE', 'negative sampling', 'MRR', 'Hits@K', 'data leakage'],
    summary: `Link prediction — inferring missing or future edges in a graph — is one of the most commercially impactful graph ML tasks, underlying friend recommendations, knowledge graph completion, and drug-target interaction prediction. The key tension is between simple heuristics (common neighbors achieves near-SOTA on some citation networks) and learned representations: heuristics are fast and interpretable but cannot incorporate node features, while embedding methods scale to large knowledge graphs and generalize across relation types. Evaluation methodology is treacherous — naive train/test splitting leaks structural information through shared neighbors, inflating apparent accuracy.`,
    keyPoints: [
      'Structural heuristics: Common Neighbors (CN) score(u,v) = |N(u) ∩ N(v)|. Adamic-Adar: Σ_{w∈N(u)∩N(v)} 1/log(|N(w)|) — downweights high-degree common neighbors. Katz index: Σ_{l=1}^∞ β^l |paths_{uv}^l| where β<1 decays long paths. Resource Allocation (RA): Σ_{w∈N(u)∩N(v)} 1/|N(w)|. These require no training, run in O(|E|) time, and often outperform learned methods on homophilic networks where triangle closure is the dominant link formation mechanism.',
      'Dot product decoder: given node embeddings z_u, z_v ∈ ℝ^d, predict P(edge) = σ(z_u · z_v). Requires embeddings to be in the same space. Equivalent to inner product similarity — works when proximity in embedding space correlates with link existence. Used in GraphSAGE and most node2vec-based systems.',
      'Bilinear decoder: P(edge) = σ(z_u^T R z_v) where R ∈ ℝ^{d×d} is a learned relation matrix. More expressive than dot product — R captures asymmetric relationships. For heterogeneous graphs with multiple relation types, use relation-specific R_r (DistMult, RESCAL models).',
      'Knowledge graph completion: KGs like Freebase, Wikidata store (head, relation, tail) triples — many are incomplete. Goal: predict missing tails given (head, relation, ?) or missing heads. TransE: h + r ≈ t, score = -‖h + r - t‖. Captures simple relational patterns but fails for symmetric relations (r(a,b) ↔ r(b,a) requires r=0). RotatE: models relations as rotations in complex space — handles symmetric, antisymmetric, inverse, composition patterns. ComplEx: complex-valued embeddings handle asymmetric relations. TransR, TransH handle one-to-many relations.',
      'Negative sampling: link prediction requires negative edges (non-existing edges) for training. Naive strategy: sample random (u,v) pairs. Problem: most random pairs are true negatives, making the task too easy and gradients uninformative. Better strategies: (1) corrupt the head or tail of a positive triple randomly (KG completion standard), (2) corrupt within the same type (for heterogeneous graphs), (3) hard negatives — sample near-positives in embedding space (more informative but risk false negatives). Number of negatives per positive (k) is a critical hyperparameter — k=5-20 typical.',
      'Evaluation metrics: AUC-ROC: probability that a positive edge is ranked above a random negative — standard for binary link prediction. MRR (Mean Reciprocal Rank): 1/rank for the correct entity in KG completion — rewards getting the right answer in top-1. Hits@K: fraction of correct entities ranked in top K (K=1,3,10). MRR and Hits@K are standard for KG benchmarks (FB15k-237, WN18RR). Filtered setting: when computing rank, remove other known positives from the ranking list — otherwise the model is penalized for correctly ranking other true triples above the target.',
      'Data leakage in link prediction: naive splitting randomly assigns edges to train/test. Problem: if edge (u,v) is in the test set but (u,w) and (w,v) are in the train set, the model can infer (u,v) via the common neighbor w — the structural information has leaked. Correct approach: (1) for transductive tasks, remove test edges before training (but keep nodes); the GNN is trained on the graph without test edges; (2) negative samples for evaluation should not be in the training graph; (3) for inductive tasks, use fresh subgraphs with no overlap in nodes. SEAL (Zhang & Chen, 2018) specifically addresses this by using local enclosing subgraphs and achieving state-of-the-art partly because it avoids leakage.',
      'SEAL framework: instead of using global node embeddings, SEAL extracts the local enclosing subgraph around each candidate link (the K-hop neighborhood of the pair), assigns structural labels (shortest-path distance to each endpoint), and trains a graph-level GNN classifier. This directly captures the structural pattern around the link — whether common neighbors form a triangle, etc. — rather than relying on node proximity in a global embedding space. Achieves best results on citation and social network benchmarks.',
    ],
    checkQuestions: [
      { q: 'You\'re building a friend recommendation system. Should you use Adamic-Adar or a GNN-based approach? What factors decide this?', a: 'Use Adamic-Adar if: (1) the network is highly homophilic (friends of friends become friends — triangle closure dominates), (2) you have no node features, (3) you need low latency and interpretability, (4) the graph is relatively static. Use a GNN if: (1) you have rich node features (user demographics, interests, activity) that should inform recommendations, (2) the graph is heterogeneous (user-item-category), (3) you need to handle new users (inductive), (4) the link formation mechanism is complex (content preferences, not just structural proximity). In practice, start with Adamic-Adar as a baseline — it\'s O(|E|), interpretable, and often achieves 80-90% of a GNN\'s AUC on social networks. Use GNNs when you need the remaining 10-20% lift and can afford the engineering cost.' },
      { q: 'Explain why TransE fails for symmetric relations in knowledge graphs and what model you would use instead.', a: 'TransE models relations as translations: h + r = t. For a symmetric relation r (friend_of, married_to): r(a,b) and r(b,a) both hold. TransE requires: a + r = b and b + r = a. Adding: a + b + 2r = a + b → r = 0. But if r=0, all entities collapse to the same point (h=t for all triples). TransE cannot model any non-trivial symmetric relation. RotatE: models r as element-wise rotation in complex space: t = h ∘ r where |r_i| = 1. For symmetric r: h ∘ r = t and t ∘ r = h → h ∘ r ∘ r = h → r² = 1 → r_i = ±1. This is satisfied by setting r_i = ±1 — RotatE handles symmetric relations. RotatE also handles inverse relations (r_inv = r̄, complex conjugate) and composition. ComplEx (complex-valued DistMult) also handles asymmetry. DistMult cannot model antisymmetric relations (r(a,b) → ¬r(b,a)).' },
      { q: 'Describe exactly how data leakage happens in link prediction and design a correct train/validation/test split for a citation network.', a: 'Leakage mechanism: if edge (A,B) is your test edge, but edges (A,C) and (C,B) are in your training graph, then the GNN trained on the training graph encodes C in both A\'s and B\'s embeddings (via message passing). At test time, the dot product z_A·z_B is high because both incorporate C\'s information — the model appears to predict (A,B) correctly, but only because it learned the triangle closure pattern through the leaked structural information. Correct split: (1) remove all test and validation edges from the adjacency matrix before training; (2) ensure test negative edges are not positive edges in the training graph; (3) use a fixed random seed and split by edge ID (not by node). For citation networks specifically: random edge splits are standard. For temporal graphs (e.g., new papers citing old ones): always split by time — train on edges before T, test on edges after T. This prevents looking into the future.' },
    ],
    takeaway: `Evaluation methodology is the most dangerous part of link prediction — naive random edge splits leak structural information through shared neighbors, inflating apparent accuracy significantly. The correct procedure removes test edges from the training graph entirely so the GNN never sees paths through those edges. For temporal graphs, a time-based split is mandatory: models trained with future knowledge and evaluated on "past" links are evaluating recall of a known graph, not prediction of an unknown one, which has no production relevance.`,
  },
  {
    id: 'node_classification_at_scale',
    title: 'Scalable GNNs for Node Classification',
    subtitle: 'Neighbor explosion, Cluster-GCN, GraphSAINT, SIGN, cold-start, class imbalance',
    difficulty: 'advanced',
    estimatedMin: 60,
    tags: ['scalable GNN', 'Cluster-GCN', 'GraphSAINT', 'SIGN', 'cold-start', 'mini-batch', 'imbalance'],
    summary: `Scaling GNNs to graphs with hundreds of millions of nodes exposes the neighbor explosion problem: a K-layer full-batch GCN requires loading the entire K-hop neighborhood of every node, which grows exponentially with K. The production solutions (Cluster-GCN, GraphSAINT, SIGN) take fundamentally different approaches — partition the graph, sample random subgraphs, or precompute aggregations offline — each with distinct tradeoffs in approximation quality, training stability, and engineering complexity. Cold-start for new nodes and class imbalance in fraud/spam detection graphs are additional production challenges that are often more important than choosing between GNN architectures.`,
    keyPoints: [
      'Neighbor explosion: full-batch GCN on N nodes requires O(N) memory for each layer — feasible for N≤100K but infeasible for N=100M. Mini-batching naively: for a K-layer GNN and a target batch of B nodes, the required neighborhood at hop k is |S_k|×|S_{k-1}|×...×|S_1| — exponential fan-out. With B=512, K=3, fan-out=[15,10,5]: need ~512×5×10×15 = 384,000 nodes per batch. Sampling reduces this but introduces approximation error.',
      'Cluster-GCN (Chiang et al., 2019): partition the graph into clusters using METIS. In each mini-batch, sample a cluster and compute GNN only within that cluster — no cross-cluster edges used. Avoids neighbor explosion entirely (no cross-cluster lookups). The approximation error comes from ignoring cross-cluster edges. Mitigation: sample multiple clusters per batch and use their union as the subgraph, including edges between them. Memory: only the subgraph fits in GPU memory. Bias: METIS creates dense clusters — within-cluster edges are over-represented. Sampling multiple clusters reduces this bias.',
      'GraphSAINT (Zeng et al., 2020): sample random node-induced or edge-induced subgraphs. For each mini-batch, independently sample a subgraph, train full GNN on it (no neighbor explosion within the subgraph). Key insight: normalize by sampling probability to get unbiased gradient estimates. Three samplers: node sampler (sample V\' ⊆ V randomly), edge sampler (sample E\' ⊆ E and take induced nodes), random walk sampler (sample random walks, take unique nodes — captures more diverse neighborhoods). Normalization coefficients are precomputed offline.',
      'SIGN (Scalable Inception Graph Neural Networks, Frasca et al., 2020): precompute diffusion features offline. For each order k ∈ {0,1,...,K}: X^k = (D̃^{-1/2} Ã D̃^{-1/2})^k X. These are the GCN-style aggregations at each depth. Store all {X^k} on disk. At training time: load X^0,...,X^K for a mini-batch, concatenate, pass through MLP. No graph structure needed at training time — pure offline preprocessing. Training is as fast as a standard MLP. Limitation: cannot handle new nodes with new edges (features must be precomputed); less flexible than online message passing.',
      'Graph distribution shift: the training subgraph (cluster, sampled subgraph) has different structural statistics than the full graph. Degree distribution changes, local clustering coefficients change — the model trained on the mini-batch distribution may not generalize to the full graph. GraphSAINT\'s normalization mitigates this; Cluster-GCN\'s multi-cluster batches help. In practice, always evaluate with full-graph inference (even if training uses mini-batches) to detect this gap.',
      'Cold-start for new nodes: a new node with no historical edges has no neighborhood for message passing — GNN produces only the ego-node embedding (content features only). Production solutions: (1) MLP baseline for new nodes (no graph structure), (2) content-based nearest-neighbor: find similar existing nodes in embedding space and use their average as the new node\'s embedding, (3) inductive GraphSAGE with metadata features: even with 1-2 initial edges, the model aggregates available neighbors, (4) delayed graph incorporation: batch-process new edges periodically (every hour/day), update embeddings for affected nodes.',
      'Class imbalance in node classification: fraud (0.1% positive), spam (1%), rare disease nodes. Standard cross-entropy with balanced sampling: over-sample minority or under-sample majority. For GNNs specifically: (1) focal loss (down-weights easy negatives), (2) class-weighted loss (weight positive by 1/positive_rate), (3) BalancedSampler: sample equal positive/negative batches. Structural imbalance: fraudsters often have fewer connections — their neighborhoods are sparser and message passing aggregates less information. Add structural features (degree, clustering coefficient) as node features to compensate.',
      'LABOR sampling (Liu et al., 2023): unbiased importance-based neighbor sampling. Instead of uniform sampling, sample neighbors with probability proportional to their importance (approximated by the product of edge weight and neighbor degree). Reduces variance in gradient estimates compared to uniform sampling — converges faster to the same solution as full-batch. Supported in PyTorch Geometric as of 2023.',
      'Scalability comparison: Cluster-GCN — best for graphs that partition well (dense communities), deterministic, low memory. GraphSAINT — better for graphs with poor community structure, unbiased, requires precomputing normalization. SIGN — fastest training (no graph at train time), best for static graphs where features change rarely. GraphSAGE mini-batch — most flexible, standard choice for dynamic graphs. Full-batch GCN — only for small graphs (N<500K on 80GB A100).',
    ],
    checkQuestions: [
      { q: 'Your GNN is trained with Cluster-GCN but accuracy on the full graph is 5% lower than on the training clusters. Diagnose the problem and propose fixes.', a: 'This is graph distribution shift. Cluster-GCN ignores cross-cluster edges during training — the model learns to classify nodes using only within-cluster neighbors. The full graph has many cross-cluster edges (≈ fraction of inter-community edges in real graphs). Node embeddings in the full graph incorporate these additional neighbors, shifting the input distribution the classifier head receives. Also, METIS clusters are denser than average — training on dense subgraphs biases the model toward high-degree node patterns. Fixes: (1) Multi-cluster mini-batching: sample 5-10 clusters per batch, include inter-cluster edges between them — reduces distribution gap. (2) Evaluate on a held-out partition during training (not just the training clusters) — detect the gap early. (3) Switch to GraphSAINT with edge-based sampler — more representative of the full graph topology. (4) Full-graph inference: always use the full graph for evaluation, even if it requires processing in node-partitioned chunks.' },
      { q: 'Design the architecture for GNN-based fraud detection at a payments company with 100M users, 1B transactions per day, and 0.1% fraud rate. Focus on scalability and handling cold-start.', a: 'Graph construction: bipartite user-merchant graph + user-user co-occurrence edges. Edge features: transaction amount, time of day, merchant category, device fingerprint. Node features: user account age, verification status, historical spending patterns. Scalability: SIGN for daily batch re-embeddings (precompute offline, serves low-latency). GraphSAGE mini-batch for online near-realtime (triggered on new high-risk transactions). Class imbalance: class-weighted focal loss (weight=1000× for fraud), hard negative mining (sample from confused near-negative transactions). Cold-start: new users → MLP-only classification using registration metadata + device fingerprint (no graph features until 5+ transactions). After 5 edges: include 1-hop GraphSAGE aggregation. Evaluation: time-based split (never random), track precision@top-K (because fraud analysts review top-K alerts), AUC for overall ranking. Explainability: log top-5 neighbors by attention weight for analyst review (GAT instead of GraphSAGE for interpretability).' },
      { q: 'Compare GraphSAINT and Cluster-GCN on a large Reddit graph (230K nodes, 11M edges, highly connected). Which would you choose and why?', a: 'Reddit has no strong community structure — it\'s relatively homogeneous with high inter-community connectivity. Cluster-GCN would create partitions that cut through many important cross-community edges, causing high distribution shift between training and full-graph inference. The inter-community edges are often the most informative for classification (cross-subreddit connections indicate topic diversity). GraphSAINT with random walk sampling is better here: random walks naturally traverse the graph without respecting community boundaries, sampling subgraphs that are representative of the overall topology. The normalization coefficients correct for sampling bias. GraphSAINT is the better choice. Empirically, on the Reddit benchmark, GraphSAINT achieves higher test F1 than Cluster-GCN (93.0% vs 90.4% in original papers) precisely because Reddit lacks strong community structure. For OGB-Products (e-commerce graph with category communities), Cluster-GCN would be more competitive.' },
    ],
    takeaway: `Scaling GNNs to billion-node graphs requires solving the neighbor explosion problem — a K-layer GNN's required neighborhood grows exponentially with K, making full-batch training impossible. Cluster-GCN, GraphSAINT, and SIGN each solve this differently (partition/sample/precompute), and the right choice depends on whether the graph has community structure, whether embeddings need to be dynamic, and whether training speed or inference latency is the bottleneck. The often-overlooked issue is that training on subgraphs introduces distribution shift from the full graph at inference time, and the 5% accuracy gap in the Cluster-GCN example is the canonical symptom of this.`,
  },
  {
    id: 'heterogeneous_graphs',
    title: 'Heterogeneous Graph Neural Networks',
    subtitle: 'Node/edge types, HAN, HGT, meta-paths, knowledge graphs, when to model heterogeneity',
    difficulty: 'advanced',
    estimatedMin: 60,
    tags: ['heterogeneous graph', 'HAN', 'HGT', 'meta-path', 'knowledge graph', 'RGCN', 'relational'],
    summary: `Real production graphs are almost always heterogeneous — Pinterest's graph has Pin, Board, User, and Image nodes connected by Save, Click, Follow, and Similarity edges. Treating all nodes and edges as the same type (homogenizing) discards the relational semantics that distinguish, say, a user clicking on a pin from a user saving it. However, heterogeneous GNNs introduce significant complexity: relation-specific parameters increase model size quadratically with the number of relation types, meta-path selection requires domain knowledge, and the training signal for rare relation types can be insufficient. The decision of when to model heterogeneity explicitly versus when to encode type information as node/edge features is one of the most consequential architecture decisions in production graph ML.`,
    keyPoints: [
      'Heterogeneous graph definition: G = (V, E, τ, φ) where τ: V → A (node type mapping) and φ: E → R (edge type mapping). |A| > 1 or |R| > 1. E-commerce example: A = {User, Item, Category, Brand}, R = {views, purchases, belongs_to, makes}. Each (src_type, edge_type, dst_type) triplet is a "canonical edge type". PyTorch Geometric HeteroData class represents each canonical type separately.',
      'RGCN (Relational GCN, Schlichtkrull et al., 2018): separate weight matrix W_r for each relation type r. Message from neighbor u via relation r: m_{u→v}^r = W_r h_u. Aggregation: h_v = σ(Σ_r Σ_{u∈N^r(v)} (1/c_{v,r}) W_r h_u). Problem: W_r ∈ ℝ^{d_in × d_out} for each of |R| relations → |R|×d² parameters. For KGs with 200+ relation types: massive overparameterization, risk of overfitting. Regularization: basis decomposition W_r = Σ_b a_{r,b} V_b (share B basis matrices across relations) or block decomposition.',
      'Meta-paths: sequences of node types and edge types that define a specific semantic path. Example in academic graph: Author→Paper→Author (APA, co-authorship), Author→Paper→Venue→Paper→Author (APVPA, same venue). Meta-path-based neighbors: nodes reachable via a specific meta-path. Key: different meta-paths capture different semantic relationships; the same pair of nodes may be connected via multiple meta-paths with different semantic meaning.',
      'HAN (Heterogeneous Attention Network, Wang et al., 2019): two-level attention. (1) Node-level attention: for each meta-path, compute attention between node and its meta-path neighbors (standard GAT attention). (2) Semantic-level attention: for each target node, compute a soft weighting of the different meta-path-based embeddings (which meta-path is more informative for this node?). Final embedding = weighted sum of meta-path-specific embeddings. Limitation: meta-paths must be defined manually by domain experts — not end-to-end learnable.',
      'HGT (Heterogeneous Graph Transformer, Hu et al., 2020): relation-specific attention without meta-paths. Message computation: h_{(s→t)}^l = MSG_ℜ(e)(h_s^{l-1}) where ℜ(e) = (τ(s), φ(e), τ(t)). Attention: α_{(s,e,t)} = softmax(ATT_ℜ(e)(h_s^{l-1}, h_t^{l-1})). The attention mechanism has type-specific key/query projections. This is effectively a Transformer with relation-specific parameters — no meta-path definition needed. Scales better to many relation types than RGCN (shared across types via additive decomposition).',
      'Knowledge graphs as heterogeneous graphs: KGs (Freebase, Wikidata, ConceptNet) are directed heterogeneous graphs where edges are named relations. Each triple (head, relation, tail) is an edge of type "relation". GNN-based KG completion: RGCN aggregates neighbor entities via relation-specific messages to produce entity embeddings, then uses TransE/DistMult decoder for triple scoring. R-GCN + DistMult outperforms pure embedding methods (TransE, RotatE) when entity connectivity is high but underperforms when KG is sparse.',
      'When to model heterogeneity vs homogenize: Model explicitly when: (1) edge type fundamentally changes the semantic meaning (click vs purchase encode different user intent), (2) you have enough data per relation type for separate parameters, (3) downstream task requires distinguishing between relation types (e.g., predicting "will user purchase after viewing?"). Homogenize when: (1) you have many rare relation types with few examples (<1000 edges), (2) the type information can be encoded in edge features (edge type embedding), (3) you need a simple baseline fast. Middle ground: encode node type and edge type as features (one-hot or learned embedding), use a standard GraphSAGE/GAT — captures type information without quadratic parameter growth.',
      'Cold-start for rare node types: a new node type added to a heterogeneous graph has no trained type-specific parameters. Solutions: (1) transfer learning — initialize new type parameters from the most similar existing type; (2) feature-based approach — rely on content features rather than structural parameters for rare types; (3) continual learning — fine-tune type-specific parameters on new type data while freezing shared parameters.',
      'R-GAT (Relational Graph Attention) for KGs: combine relation-specific message passing with attention. Attention coefficient between entity h and entity t via relation r: α_{h,r,t} = softmax over all (h,r\',t\') in KG. Important for one-to-many relations (one entity connected via many different relations to many entities) — attention learns which relations and which neighbors are most relevant for each target entity at each query.',
    ],
    checkQuestions: [
      { q: 'You have a heterogeneous e-commerce graph with 10 node types and 25 edge types. RGCN would require 25 separate weight matrices. What problems does this cause and how does HGT address them?', a: 'Problems with 25 W_r matrices: (1) Overparameterization — 25×d² parameters for the relational transformation alone; for d=256 and 25 types: 25×65,536 = 1.6M parameters just for relation weights, high overfitting risk for rare relations. (2) Rare relations — edge types like "refund_item" may have only 1,000 examples, providing insufficient gradient for a full d×d matrix. (3) Memory — storing 25 full weight matrices and their gradients on GPU. HGT addresses this via type-specific low-rank projections: node-type-specific linear projections (K matrices for K node types, not R relation matrices), and relation-specific attention biases (scalar or low-rank) rather than full matrices. This reduces parameters from O(|R|×d²) to O(|A|×d²) for the main weight matrices, with small O(|R|×d) relation-specific modifiers. Additionally, HGT uses multi-head attention across types — the attention mechanism provides soft relation weighting rather than hard separate parameters.' },
      { q: 'Meta-paths in HAN are defined manually. What is wrong with this, and how would you make meta-path selection data-driven?', a: 'Problems with manual meta-paths: (1) Requires domain expertise — an ML engineer may not know which semantic connections matter most in a new domain. (2) Combinatorial explosion — for 5 node types and 10 edge types, the number of length-3 meta-paths is ~10³ = 1000. (3) Fixed at deployment — if the graph evolves (new node types, edge types), meta-paths need manual updates. (4) May miss important non-obvious paths. Data-driven alternatives: (1) HGT — no meta-paths; relation-specific attention weights learn which (src_type, edge_type, dst_type) combinations matter. (2) HeCo / DMGI — contrastive learning on meta-path-based views without predefined semantic labels. (3) Automatic meta-path discovery — learn weights over a fixed-length set of all possible meta-paths up to length K, let the model learn to zero out unimportant ones (sparsity-inducing regularization). (4) R-HGNN — learns relation-triplet representations end-to-end without meta-path specification.' },
      { q: 'In a knowledge graph with 1M entities and 500 relation types, how would you handle the scalability and rare-relation problems simultaneously?', a: 'Scalability: (1) RGCN with basis decomposition: W_r = Σ_b a_{rb} V_b where B << |R|. Learn B=40 basis matrices shared across all 500 relation types; each relation has 40 scalar coefficients. Parameters: 40×d² (basis) + 500×40 (coefficients) vs 500×d² without decomposition — ~12× reduction at d=256. (2) Mini-batch training with relation-stratified sampling: ensure each batch has at least K edges per relation type. (3) GraphSAINT random walk sampling — naturally samples relations proportional to their frequency. Rare relations: (1) Frequency-based regularization: scale the learning rate for W_r inversely with the log frequency of relation r — rare relations get stronger regularization, not smaller gradients. (2) Relation grouping: cluster semantically similar relations (hierarchical clustering in pre-trained embedding space from TransE), share parameters within clusters. (3) Pad rare relations with synthetic data (rule-based KG completion: if (A, parent_of, B) and (B, parent_of, C) then (A, grandparent_of, C)).' },
    ],
    takeaway: `Heterogeneous graphs are the default in production, not the exception — Pinterest has Pin/Board/User/Image node types, e-commerce graphs have User/Item/Category/Brand, knowledge graphs have hundreds of entity types. The key architecture decision is whether to use relation-specific weight matrices (RGCN — quadratic parameter growth with |R|) or relation-specific attention with shared weights (HGT — linear growth). Choosing RGCN with 500 relation types and d=256 means 500 full weight matrices; basis decomposition reduces this by ~12× and is non-optional at scale.`,
  },
  {
    id: 'gnn_applications',
    title: 'GNNs in Production at Scale',
    subtitle: 'PinSage, fraud detection, drug discovery, dynamic graphs, feature engineering, real-time inference',
    difficulty: 'advanced',
    estimatedMin: 65,
    tags: ['PinSage', 'Pinterest', 'fraud detection', 'drug discovery', 'dynamic graphs', 'production', 'real-time GNN'],
    summary: `Production GNN systems face a fundamentally different set of problems than academic benchmarks: graphs with billions of nodes and edges, latency requirements measured in milliseconds, continuous graph updates that invalidate cached embeddings, and the need for interpretable predictions in high-stakes settings like fraud detection. PinSage (Ying et al., 2018) remains the canonical case study for how to bridge the gap between a 2-layer GraphSAGE on a 100-node test graph and a system serving 2 billion users — it introduced random walk-based importance sampling, MapReduce-based offline embedding computation, and ANN-based serving that collectively made billion-scale GNN recsys feasible.`,
    keyPoints: [
      'PinSage (Pinterest, 2018): 2B pins, 18B edges on pin-board bipartite graph. Challenge: full-batch GCN impossible (18B edges, 100GB+). Solution: random walk-based neighborhood sampling — for each pin, run 100 random walks of length 5; use the top-K most frequently visited pins as its "neighborhood" rather than all direct neighbors. This captures second-order proximity and is more robust than uniform neighbor sampling in the presence of hub pins.',
      'PinSage scalability stack: (1) Offline embedding: MapReduce pipeline processes all pins in hourly batches. GPU machines compute mini-batch embeddings; results written to key-value store (RocksDB). (2) Online serving: ANN (approximate nearest neighbor) lookup via FAISS on precomputed embeddings. Latency: <10ms for top-1000 similar pins. (3) Curriculum training: start with easy negatives (random pins), progressively use harder negatives (semantically similar but non-relevant pins from query expansion) — standard practice in production recsys GNNs.',
      'Fraud detection on transaction networks: nodes = users + merchants, edges = transactions with features (amount, time, merchant category, device ID). Task: binary node classification (fraud/not fraud) or edge classification (fraudulent transaction). Challenges: (1) Temporal causality — training data must use only past-time features; a transaction at T can use features from T-Δt only. (2) Adversarial adaptation — fraudsters study detection patterns and change behavior, causing distribution shift. (3) Graph structure as fraud signal — fraudsters create ring networks (A→B→C→A) or fan patterns (one account → many accounts); structural features (cycle count, betweenness centrality) are strong signals beyond node features.',
      'Drug discovery (molecular GNNs): molecular graphs — atoms as nodes (features: atomic number, charge, hybridization), bonds as edges (features: bond type, aromaticity). Tasks: molecular property prediction (HOMO-LUMO gap, solubility, toxicity), drug-target binding affinity, reaction yield prediction. Benchmark: OGB-molhiv, OGB-molpcba. Key challenge: small datasets (1K-100K molecules vs millions in NLP). Solutions: pretraining on large unlabeled molecular databases (ZINC, ChEMBL) then fine-tuning — analogous to BERT pretraining. Message passing is especially well-suited because molecular properties emerge from local bonding patterns (bond angles, functional groups).',
      'Social network link prediction at FAANG: Facebook friendship recommendation, LinkedIn "People You May Know", Twitter follow suggestions. Graph: billions of user nodes, tens of billions of social edges. Architecture: GraphSAGE or PinSage-style for offline embeddings, then ANN for retrieval. Key signals: mutual friends (common neighbors — first-order), school/employer overlap (heterogeneous graph features), content similarity (content embeddings via separate model). Serving: offline daily batch for most users, near-real-time update for highly active users.',
      'Dynamic graphs: edges and nodes appear and disappear over time. Approaches: (1) Snapshot-based — train on graph snapshots at regular intervals; static GNN on each snapshot. Simple but misses inter-snapshot temporal patterns. (2) TGAT (Temporal Graph Attention): node embeddings are functions of both node features and temporal encoding of event times — uses a Transformer-style time encoding. Handles continuous-time dynamics. (3) TGN (Temporal Graph Network, Rossi et al., 2020): nodes have memory states (RNN-based) that update with each new interaction. Memory persists across batches — captures long-term user history. Used for real-time recommendations where recency matters.',
      'Real-time GNN inference challenges: (1) Latency — a 2-layer GNN requires 2-hop neighborhood lookup; for a user with 1,000 friends, each with 1,000 friends: 1M feature lookups per inference request. Solution: precompute and cache 1-hop aggregations; update cache on new edges. (2) Graph updates — new edges (purchases, likes) invalidate cached embeddings for affected nodes and their neighbors. Event-driven cache invalidation: add new-edge event to a queue; worker recomputes embeddings for affected nodes. (3) Feature freshness — node features change (user balance, item inventory) more frequently than graph topology. Decouple feature store update from embedding recomputation.',
      'Feature engineering for graphs: beyond node content features, structural features are often the highest-ROI additions. (1) Degree (in/out separately for directed graphs). (2) Clustering coefficient: fraction of a node\'s neighbors that are also connected to each other. (3) PageRank or personalized PageRank — importance of node in the global graph. (4) Node2Vec embeddings from the graph topology alone. (5) Temporal features: average edge age, edge creation rate in last 7/30 days. These can be precomputed offline and added as node features before the GNN, effectively giving the GNN access to higher-order structural information without increasing GNN depth.',
      'Serving infrastructure: standard pattern for billion-scale GNN recsys. Feature store: Redis or RocksDB for sub-millisecond node feature lookup. Graph store: adjacency lists in distributed key-value store (per-node: sorted list of neighbor IDs). Embedding store: FAISS index for ANN lookup of cached node embeddings. Batch recompute pipeline: Spark/MapReduce + GPU workers for daily/hourly full-graph embedding refresh. Event stream: Kafka for real-time edge additions; consumer updates feature store and triggers embedding refresh for high-priority nodes. The GNN model itself is often a fixed artifact deployed for months — the embedding quality depends more on fresh graph data than model updates.',
    ],
    checkQuestions: [
      { q: 'You are the ML lead for friend recommendations at a social network with 500M users. Design a GNN system end-to-end, from data pipeline to serving. What are the top 3 engineering challenges?', a: 'Architecture: GraphSAGE-style 2-layer GNN. Node features: user demographics, interest embeddings (from content model), activity statistics (DAU, session length). Edge features: timestamp, mutual friend count at edge creation. Graph: user-user edge if followed or messaged within 90 days (avoid noisy implicit connections). Training: mini-batch GraphSAGE with 25/10 neighbor sampling, daily batch on 7-day edge window, negative sampling from users in same country (harder negatives than random). Offline pipeline: daily Spark job computes {X^0, X^1, X^2} (SIGN-style precomputed aggregations), trains MLP on top, generates 128-dim embeddings for all 500M users, uploads to FAISS index partitioned by user region. Serving: user requests → lookup user embedding from key-value store (Redis) → FAISS ANN for top-500 candidate users → re-rank with cross-encoder (pair features: mutual friends, geographic proximity). Top 3 challenges: (1) Embedding staleness — daily batch means new users/edges are invisible for up to 24h; add feature-based fallback for new users. (2) Distribution shift — users who request recommendations are more active than average; training distribution ≠ serving distribution. (3) Feedback loop — recommendations affect which edges form, which changes the training graph, which biases future recommendations (filter bubble).' },
      { q: 'PinSage uses random walks to define "neighborhoods" rather than direct graph neighbors. Why? What problem does this solve?', a: 'Pinterest\'s pin-board graph is bipartite: pins connect to boards, boards connect to pins. Direct 2-hop neighbors of a pin are "other pins in the same boards" — but a very popular board may have 100,000 pins. Uniform 1-hop → 2-hop neighborhood would either (a) be impossibly large, or (b) if uniformly sampled, over-weight popular boards and under-weight niche but relevant boards. Random walk solution: run T random walks of length L from pin p. Count visit frequency of each node across all walks. Top-K most frequently visited pins are the "neighborhood". This implicitly weights neighbors by: (1) how many paths connect them to p (multiple shared boards → higher frequency), (2) whether those paths go through specialized/rare boards (short random walks stay in the niche, increasing frequency for niche co-pins). Random walk neighborhoods are also more robust to graph noise (spurious edges affect single direct edges but not random walk statistics) and produce automatically size-bounded neighborhoods (top-K selection).' },
      { q: 'A fraud detection GNN achieves 99% AUC in offline evaluation but only 70% precision at 10% recall in production. Diagnose at least 3 possible causes.', a: '(1) Temporal leakage in training: offline evaluation splits randomly by transaction ID — fraud rings involve coordinated accounts, so the same ring members appear in both train and test, inflating AUC. Production sees entirely new fraud rings with different structural signatures. Fix: time-based split — train on T < cutoff, evaluate on T > cutoff. (2) Adversarial adaptation: fraudsters change behavior after detection policy changes (email alerts, account suspension). The model was trained on historical fraud patterns that no longer apply. Fix: monitor concept drift (track feature distribution of detected vs undetected fraud weekly), retrain monthly with recent data. (3) Graph feature leakage: node degree, clustering coefficient, or PageRank computed on the full graph (including future edges). Proper feature computation uses only graph state at transaction time T. Fix: temporal graph construction — for each transaction at time T, use only edges with timestamp < T. (4) Label delay: fraud is often identified weeks later (chargebacks take 30-60 days). Training labels for recent transactions are incomplete — positives labeled as negatives. Fix: use only transactions >60 days old as training data; use older confirmed labels.' },
    ],
    takeaway: `PinSage is the definitive case study for production GNNs at scale: 2B nodes, 18B edges, millisecond latency. Its innovations — random walk-based neighborhood importance sampling, MapReduce offline embedding computation, and ANN serving — collectively solve the three hard production problems of scalable GNNs: neighborhood explosion, embedding freshness, and low-latency retrieval. The practical lesson is that a GNN in production is not one model but a pipeline: feature store, graph store, offline batch embedding computation, ANN index, and event-driven cache invalidation are all load-bearing components, and the GNN model itself is often the least complex part.`,
  },
]

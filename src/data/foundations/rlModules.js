export const RL_MODULES = [
  {
    id: 'mdp_framework',
    title: 'Markov Decision Processes',
    subtitle: 'States, actions, rewards, transitions, discount factor — the formal RL framework',
    difficulty: 'foundational',
    estimatedMin: 40,
    tags: ['mdp', 'markov', 'bellman', 'discount', 'pomdp'],
    summary: `An MDP is the formal skeleton that every RL algorithm hangs on: a tuple (S, A, P, R, γ). The Markov property — that the future is conditionally independent of the past given the present state — sounds like a strong assumption, but real systems violate it constantly (robots with partial sensors, trading systems where past prices matter, multi-player games with hidden opponent state). RL works anyway because we can augment states to recover the property, or accept approximate solutions. The discount factor γ is often misread as "short-sightedness"; its deeper role is to ensure the infinite-horizon value sum converges and to encode uncertainty about whether the episode continues. Choosing γ is one of the most consequential decisions in applied RL and is almost never discussed in textbooks.`,
    keyPoints: [
      `MDP tuple: (S, A, P, R, γ). S = state space, A = action space, P(s'|s,a) = transition probability, R(s,a,s') = reward, γ ∈ [0,1) = discount factor. A policy π(a|s) maps states to action distributions.`,
      `Markov property: P(s_{t+1}|s_t,a_t,...,s_0,a_0) = P(s_{t+1}|s_t,a_t). The next state depends only on the current state and action, not the full history. This makes dynamic programming tractable. Without it, you need to track history, which causes exponential state space growth.`,
      `The Markov property is almost always violated in practice. A robot that only sees 2D camera images cannot distinguish "door is locked" from "door is unlocked" — history is needed. Trading: today's price alone doesn't predict tomorrow. Multi-player games: opponent policy is hidden state. Standard fix: stack k recent observations into the state (e.g., 4 frames in Atari DQN) — this usually recovers approximate Markov property.`,
      `Discount factor γ encodes three things: (1) Mathematical — ensures V(s) = Σ γ^t R_t converges. (2) Economic — time preference and uncertainty about episode termination. (3) Practical — γ close to 1 makes the agent care more about distant rewards but slows learning and destabilises training. The effective credit assignment horizon is 1/(1-γ) steps. γ=0.99 means the agent cares about rewards 100 steps out; γ=0.95 means 20. Choosing γ is a training stability decision, not just a preference specification.`,
      `Finite-horizon MDPs can have non-stationary optimal policies — the right action at step 50 may differ from the right action at step 1 because the time horizon remaining has changed. Infinite-horizon discounted MDPs have a stationary optimal policy. Most RL algorithms assume the infinite-horizon formulation even when training on finite episodes.`,
      `POMDPs (partial observability): the agent receives observation o_t = O(s_t), not s_t directly. The optimal solution requires a belief state b_t = P(s_t | o_1,...,o_t) — a distribution over states. Computing belief states exactly is PSPACE-hard. In practice: treat o_t as a state and hope approximate Markov holds, or use memory (LSTM/Transformer) to encode history into a compact latent representation that approximates the belief state.`,
      `Standard MDPs assume P and R are fixed. In recommender systems, user preferences evolve. In finance, market dynamics shift. Against human opponents, strategies change. Non-stationarity requires continual adaptation — standard RL algorithms that assume stationarity will overfit to current dynamics and fail when the environment shifts underneath them.`,
      `Reward design is where most applied RL fails. RL agents are optimisers — they will find unintended ways to maximise R. Potential-based reward shaping F(s,s') = γΦ(s') - Φ(s) can accelerate learning without changing the optimal policy. Arbitrary shaping changes the optimal policy in ways that are often subtle and hard to debug.`,
      `Episodic tasks have terminal states (games, robotics episodes). Continuing tasks run forever (process control). For episodic tasks, using γ=1 is sometimes valid. For continuing tasks γ < 1 is required for convergence — otherwise the infinite sum diverges.`,
    ],
    checkQuestions: [
      {
        q: `Why does the optimal policy for a finite-horizon MDP depend on the time remaining t, while the optimal policy for an infinite-horizon discounted MDP does not?`,
        a: `In a finite-horizon MDP with T steps remaining, the value function V_t(s) changes with t: with 1 step remaining, you can only take one action; with 10 steps, you can plan further. The Bellman recursion runs backward in time: V_t(s) = max_a [R(s,a) + Σ_{s'} P(s'|s,a) V_{t-1}(s')]. Since V_0(s) = 0 (terminal), each V_t differs, so the greedy policy at each t differs. In the infinite-horizon discounted case, there is no terminal time — the Bellman equation V*(s) = max_a [R(s,a) + γ Σ_{s'} P(s'|s,a) V*(s')] has a unique fixed point, and the same greedy policy is optimal at every time step (stationarity). Practically: if you train an agent on 1000-step episodes but the Bellman backup uses the infinite-horizon formulation, the agent's policy is suboptimal near episode boundaries where it should behave differently (e.g., take risks at the last step).`,
      },
      {
        q: `Your robot RL agent receives only a camera image as observation. It keeps walking into the same wall repeatedly. What Markov property violation is happening and how do you fix it?`,
        a: `The camera image alone is insufficient to determine the full state — the robot cannot tell from one frame which direction it was moving, whether a door it just tried was locked, or what it has already explored. The observation is not Markov: the next state depends on the action history, not just the current image. Fixes: (1) Frame stacking — concatenate the last k frames into the state (k=4 in DQN). This gives the agent velocity information (motion between frames) and short-term history. (2) Recurrent architecture — use an LSTM or Transformer over the observation history; the hidden state acts as a compressed belief state. (3) State estimation — if you have a physics model, run an EKF/particle filter to maintain a belief state and feed that to the RL agent. (4) Domain randomisation — if the issue is partial observability about environment structure, randomise the environment during training so the agent learns robust policies that don't require precise wall tracking.`,
      },
      {
        q: `Why is γ = 0.99 harder to train with than γ = 0.95, even if both converge to a valid solution?`,
        a: `The effective credit assignment horizon is approximately 1/(1-γ) steps. For γ=0.95, that is 20 steps; for γ=0.99, it is 100 steps. With γ=0.99, rewards from 100 steps in the future still contribute meaningfully to the value estimate of the current state. This creates two problems: (1) High variance in Monte Carlo returns — the sum of 100 future rewards has much higher variance than 20, making value estimates noisy and requiring more samples. (2) Slow propagation of information — temporal difference methods propagate value information one step per update; with γ=0.99 it takes ~100 updates for a reward signal to fully back-propagate to the state 100 steps before it. Practically, you often start with γ=0.9 during early training (fast, stable learning) and anneal toward 0.99 as the policy improves and value estimates stabilise.`,
      },
      {
        q: `A product team asks you to deploy an RL agent for content recommendation. What MDP design decisions do you make, and what can go wrong?`,
        a: `State: user context (recent interactions, demographics, session features) — must decide how many recent items to include (history length). If history is too short, Markov property violated. Action: which item to recommend — action space can be 10M+ items (continuous/discrete challenge). Reward: user engagement signal (click, watch time, like). Critical problem: engagement maximisation leads to clickbait, filter bubbles, addiction — Goodhart's Law. Need to add long-term retention, diversity, or fairness terms to the reward. Discount γ: if γ is too high (0.999), the agent optimises lifetime engagement but learns very slowly from individual interactions. Transition: user state is partially observable (you don't know their mood, attention). Deployment issues: reward delay (the user may not click immediately), non-stationarity (user preferences change over time), and offline data collection (all your initial training data is from the old non-RL policy — off-policy distribution shift).`,
      },
    ],
    takeaway: `The MDP formalism is not just notation — the Markov property, discount factor, and reward function are all design decisions that determine whether your RL system will work in practice. The deepest interview signal is understanding why the Markov property is almost always violated in the real world and how to recover from it (state augmentation, recurrent encoders, belief states), and why choosing γ close to 1 is not "better" but harder to train, not a free lunch.`,
  },
  {
    id: 'bellman_equations',
    title: 'Bellman Equations',
    subtitle: 'V(s), Q(s,a), optimality equations, contraction mapping, curse of dimensionality',
    difficulty: 'foundational',
    estimatedMin: 45,
    tags: ['bellman', 'value function', 'dynamic programming', 'contraction', 'tabular'],
    summary: `The Bellman equations are recursive consistency conditions that any optimal value function must satisfy. They're not just a mathematical formality — they're the algorithmic backbone of dynamic programming, Q-learning, and actor-critic methods. The contraction mapping theorem guarantees that iterated application of the Bellman operator converges to the unique fixed point (the optimal value function), but only under tabular or linear representations. The moment you introduce nonlinear function approximation, this guarantee vanishes — which is exactly why deep RL is hard. Understanding WHY the tabular guarantee breaks under approximation is more important for an MLE interview than being able to recite the Bellman equation itself.`,
    keyPoints: [
      `State value function V^π(s) = E_π[Σ γ^t R_{t+1} | S_0=s]. The expected discounted return starting from s following policy π. Action-value function Q^π(s,a) = E_π[Σ γ^t R_{t+1} | S_0=s, A_0=a]. Q adds the constraint that the first action is a, then π is followed.`,
      `Relationship: V^π(s) = Σ_a π(a|s) Q^π(s,a). V is the π-weighted average of Q over actions. Q^π(s,a) = R(s,a) + γ Σ_{s'} P(s'|s,a) V^π(s'). Q is immediate reward plus discounted expected value of the next state.`,
      `Bellman expectation equation for fixed policy π: V^π(s) = Σ_a π(a|s) [R(s,a) + γ Σ_{s'} P(s'|s,a) V^π(s')]. For tabular problems this is a linear system solvable exactly: V^π = (I - γP^π)^{-1} R^π. Matrix inverse is O(|S|^3) — intractable for large state spaces. This is why you need iterative methods.`,
      `Bellman optimality equation: V*(s) = max_a [R(s,a) + γ Σ_{s'} P(s'|s,a) V*(s')]. The max operator makes this nonlinear — unlike the expectation equation, you can't solve it with a matrix inverse. Q*(s,a) = R(s,a) + γ Σ_{s'} P(s'|s,a) max_{a'} Q*(s',a'). Given Q*, the optimal policy is π*(s) = argmax_a Q*(s,a).`,
      `Contraction mapping: the Bellman optimality operator T is a γ-contraction in the L∞ norm. By Banach's fixed-point theorem, iterating T from any starting V_0 converges to V* at geometric rate γ. This is value iteration: V_{k+1}(s) ← max_a [R(s,a) + γ Σ_{s'} P(s'|s,a) V_k(s')]. The convergence guarantee is the theoretical bedrock of tabular RL.`,
      `Policy iteration: (1) Policy evaluation — solve V^π exactly. (2) Policy improvement — π'(s) ← argmax_a Q^π(s,a). Repeat. Policy improvement theorem guarantees V^{π'} ≥ V^π everywhere. Converges in at most |A|^|S| policy steps, usually much faster in practice.`,
      `Curse of dimensionality: with a 10-dimensional continuous state discretised at 100 values per dimension, you have 100^10 = 10^20 states. Completely intractable to store or iterate over. This is not a computational inconvenience — it's the fundamental reason neural networks are necessary in practice.`,
      `Why function approximation breaks Bellman convergence: with a neural network V_θ(s), the Bellman update minimises ||V_θ(s) - (R + γ max_{a'} V_θ(s'))||^2. But the target (R + γ max_{a'} V_θ(s')) depends on θ simultaneously with the prediction. This is not a contraction. Convergence is no longer guaranteed; divergence is theoretically possible and empirically observed. This is the root cause of all DQN instability.`,
      `Policy evaluation by temporal difference: V(s_t) ← V(s_t) + α[R_{t+1} + γV(s_{t+1}) - V(s_t)]. The bracketed term is the TD error δ_t. Converges to V^π for tabular and linear function approximation under certain conditions. Not guaranteed with nonlinear approximation.`,
    ],
    checkQuestions: [
      {
        q: `Write the Bellman optimality equation for Q*(s,a) and explain what makes it "nonlinear," unlike the Bellman expectation equation.`,
        a: `Q*(s,a) = R(s,a) + γ Σ_{s'} P(s'|s,a) max_{a'} Q*(s',a'). The nonlinearity comes from the max_{a'} operator inside the recursion — the optimal Q at the current state depends on the maximum Q across all actions at the next state. This creates a system of nonlinear equations that cannot be solved with matrix inversion. By contrast, the Bellman expectation equation for a fixed policy π has Q^π(s,a) = R(s,a) + γ Σ_{s'} P(s'|s,a) Σ_{a'} π(a'|s') Q^π(s',a') — the Σ_{a'} π(a'|s') is a linear weighted sum, making the entire system linear and solvable as V^π = (I-γP^π)^{-1}R^π.`,
      },
      {
        q: `In policy iteration, why is policy improvement guaranteed to produce a policy at least as good as the current one? What is the formal argument?`,
        a: `Policy improvement theorem: let π and π' be two policies where π'(s) = argmax_a Q^π(s,a) for all s. Then V^{π'}(s) ≥ V^π(s) for all s. Proof: V^π(s) ≤ max_a Q^π(s,a) = Q^π(s,π'(s)) [by definition of π']. Q^π(s,π'(s)) = R(s,π'(s)) + γ Σ_{s'} P(s'|s,π'(s)) V^π(s'). By induction, keep expanding V^π(s') with the same inequality: V^π(s) ≤ E[R_{t+1} + γR_{t+2} + γ²R_{t+3} + ... | S_0=s, π']. In the limit this is V^{π'}(s). So V^{π'} ≥ V^π pointwise. The improvement is strict unless π was already optimal.`,
      },
      {
        q: `You are implementing Q-learning with a neural network and notice Q-values growing unboundedly during training. What is happening and how do you fix it?`,
        a: `Q-value divergence is a symptom of bootstrapping instability under function approximation: the target y = R + γ max_{a'} Q_θ(s',a') is itself a function of the same parameters θ being updated. Each gradient step moves θ to reduce the error to the target, but the target simultaneously moves (often in the same direction), creating a positive feedback loop. Fixes: (1) Target network: use a separate Q_θ^{-}(s',a') with parameters θ^{-} frozen for K steps (typically 1000-10000). The target becomes R + γ max_{a'} Q_{θ^{-}}(s',a') — no moving target. (2) Gradient clipping: clip the TD-error Huber loss (clip δ to [-1,1]) to prevent large gradient steps from a single overestimated Q value. (3) Reward scaling: scale rewards to [-1,1]. Large rewards push Q-values large, which causes overestimation via the max operator. (4) Lower learning rate: slower θ updates reduce the speed at which the feedback loop amplifies.`,
      },
      {
        q: `How many states does a simplified Atari game environment like Pong have, and why does this make tabular DP completely impractical?`,
        a: `Pong uses 210×160 RGB pixels at 8 bits each: 3 × 210 × 160 × 8 = 806,400 bits per frame. Even at 1 bit per pixel in grayscale (84×84 after preprocessing): 2^{84×84} = 2^{7056} states — a number incomprehensibly larger than the number of atoms in the observable universe (≈10^80 ≈ 2^{266}). Tabular DP requires storing V*(s) for every s and running Bellman updates over all s in each iteration. At 2^{7056} states, this is physically impossible. DQN sidesteps this by training a CNN Q_θ(s,a) that generalises across visually similar states — nearby pixels produce similar Q estimates without having ever seen those exact pixel combinations during training. The network parameterises the value function in a compressed 10M-parameter space rather than the 2^{7056}-state tabular space.`,
      },
    ],
    takeaway: `The contraction mapping guarantee is the theoretical bedrock of RL — it tells you why value iteration converges — but it only holds for tabular or linear representations. The moment you add a neural network, the guarantee vanishes and you get the moving-target problem. Interviewers want you to articulate exactly why: the bootstrap target y = R + γ max Q_θ(s',a') depends on the same θ you're updating, which is not a contraction, and that is the root cause of all DQN instability.`,
  },
  {
    id: 'temporal_difference',
    title: 'Temporal Difference Learning',
    subtitle: 'TD(0), TD(λ), SARSA vs Q-learning, deadly triad, divergence with FA',
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['td learning', 'sarsa', 'q-learning', 'eligibility traces', 'deadly triad', 'off-policy'],
    summary: `Temporal difference (TD) learning is the core algorithmic idea behind almost all practical RL: bootstrap the value estimate using the current value estimate itself, rather than waiting for the full return. This produces sample-efficient online updates but introduces bias (the bootstrap estimate of the future is wrong until convergence). The critical fault line in RL algorithms is on-policy (SARSA — learn about the policy you are executing) vs off-policy (Q-learning — learn about the greedy policy while executing something else). This distinction is not just theoretical: off-policy learning is vastly more sample-efficient (can reuse experience from old policies) but is responsible for the deadly triad — function approximation + bootstrapping + off-policy is the precise combination that can cause divergence, and understanding why is a standard senior ML interview question.`,
    keyPoints: [
      `Monte Carlo vs TD: MC estimates V(s) as the average observed return G_t. High variance, zero bias — uses actual future rewards. TD(0) uses the one-step target: V(s_t) ← V(s_t) + α[R_{t+1} + γV(s_{t+1}) - V(s_t)]. Low variance, introduces bias because V(s_{t+1}) is wrong early in training. TD often converges faster in practice because lower variance enables larger α.`,
      `TD(λ) interpolates between TD(0) and MC via eligibility traces. λ=0: pure TD(0). λ=1: pure MC. Intermediate λ trades variance and bias. Eligibility traces e_t(s) track which states deserve credit for the current TD error: e_t(s) ← γλ e_{t-1}(s) + 1[S_t=s]. V(s) ← V(s) + α δ_t e_t(s) for all s simultaneously. This is efficient backward credit assignment without re-running n-step returns.`,
      `On-policy: SARSA. Update uses the action actually taken: Q(s_t,a_t) ← Q(s_t,a_t) + α[R_{t+1} + γQ(s_{t+1},a_{t+1}) - Q(s_t,a_t)]. SARSA — (S,A,R,S',A'). Converges to Q^π where π is the policy being followed. If ε > 0, SARSA learns the ε-greedy policy, not the fully greedy one.`,
      `Off-policy: Q-learning. Uses the greedy action at the next state regardless of what action was taken: Q(s_t,a_t) ← Q(s_t,a_t) + α[R_{t+1} + γ max_{a'} Q(s_{t+1},a') - Q(s_t,a_t)]. The max makes this off-policy — you're learning about the greedy policy while following any behaviour policy. Off-policy enables experience replay, offline RL, and reuse of old experience.`,
      `SARSA vs Q-learning in practice: in a windy gridworld with a cliff, Q-learning learns the shorter path along the cliff edge (optimal for the greedy policy) but falls off during training. SARSA learns a safer path away from the cliff, accounting for the random ε-greedy actions. Final performance: Q-learning wins. Cumulative reward during training: SARSA wins.`,
      `Deadly triad (Sutton & Barto): function approximation + bootstrapping + off-policy training. Any two are safe; all three can cause divergence. Baird's counterexample (1995) demonstrates exact divergence with linear FA, bootstrapping, and off-policy in a 7-state MDP. This is not a numerical issue — it's a structural property of the update rule.`,
      `Why Q-learning diverges with linear FA: the Q-learning update is equivalent to gradient descent on ||Q_θ(s,a) - y||^2 where y = R + γ max_{a'} Q_θ(s',a'). But y depends on θ. The true gradient includes a term ∂y/∂θ that is conventionally ignored (the "semi-gradient"). Without this correction, the update is not a true gradient of any fixed objective. With off-policy data, the semi-gradient error can compound, causing Q-values to grow unboundedly.`,
      `Double Q-learning eliminates overestimation. Q-learning overestimates because max_a Q(s',a) uses the same Q for selecting AND evaluating the action. E[max_a Q(s',a)] ≥ max_a E[Q(s',a)] by Jensen's inequality (max is convex). Double Q-learning: Q_A selects the action, Q_B evaluates it. Expected overestimation is eliminated.`,
      `n-step TD returns: G_t^{(n)} = R_{t+1} + γR_{t+2} + ... + γ^{n-1}R_{t+n} + γ^n V(s_{t+n}). n=1: TD(0). n=∞: MC. Larger n reduces bootstrapping bias at the cost of higher variance. In practice n=5-20 often outperforms both extremes.`,
    ],
    checkQuestions: [
      {
        q: `SARSA and Q-learning have identical updates except for one term. What is that term, and what are the theoretical and practical implications of the difference?`,
        a: `SARSA target: R_{t+1} + γ Q(s_{t+1}, A_{t+1}) where A_{t+1} is the next action sampled from π. Q-learning target: R_{t+1} + γ max_{a'} Q(s_{t+1}, a'). The max vs π-sampled action is the only difference. Theoretical: SARSA is on-policy (converges to Q^π, the value of the behaviour policy). Q-learning is off-policy (converges to Q*, the value of the greedy policy), meaning it can learn the optimal policy even when collecting data with a suboptimal behaviour policy. Practical: (1) Q-learning is more sample-efficient — it can reuse data from old policies and from replay buffers, which is why DQN uses Q-learning, not SARSA. (2) SARSA is safer in stochastic environments during training — it accounts for the exploratory actions that will actually be taken. (3) Q-learning with neural networks has the deadly triad (FA + bootstrapping + off-policy), making it susceptible to divergence; SARSA with on-policy data avoids the off-policy component of the triad.`,
      },
      {
        q: `Explain Baird's counterexample intuitively. Why does Q-learning with linear function approximation diverge even in a simple MDP?`,
        a: `Baird's counterexample is a 7-state MDP where all states have 0 reward and two possible actions: one that leads to a single "dashed" state, one that uniformly randomly visits any of the other states. With a specific linear parameterisation of V_θ(s), off-policy TD updates (using a behaviour policy that doesn't match the target policy) cause θ to diverge to infinity. Intuition: with tabular representations, the Bellman operator T is a γ-contraction in L∞. With linear FA, we project the TD target onto the linear function space (the "projected Bellman operator" TΠ). The composition TΠ is a γ-contraction in the 2-norm under on-policy data (stable), but under off-policy data the projection Π uses an incorrect weighting (the off-policy state distribution rather than on-policy). With wrong weighting, TΠ may not be a contraction at all — and repeated application of a non-contraction diverges. The structural fix is importance sampling (weight updates by π(a|s)/μ(a|s)) or using gradient TD methods (GTD, GTD2) which minimise the projected Bellman error with a true gradient — stable under all three deadly triad conditions.`,
      },
      {
        q: `You are training a Q-learning agent on a game environment and observe that the Q-values grow from ~10 to ~10^6 over 500k steps, with training reward staying flat. Diagnose and fix.`,
        a: `Runaway Q-values with flat reward is a classic deadly triad symptom: (1) Off-policy (replay buffer) + bootstrapping + function approximation creates a positive feedback loop where max_{a'} Q(s',a') overestimates, which inflates the target, which inflates Q further. Fixes in order of impact: (1) Target network — freeze Q_{θ^-} for 10k steps; the target no longer moves at the same rate as predictions. This is the single most important fix. (2) Huber loss / clipping — clip TD error δ to [-1, 1] before computing the gradient; prevents a single catastrophically wrong Q estimate from causing a huge gradient step. (3) Reward clipping — clip rewards to [-1,+1] (standard in Atari DQN); keeps Q-values on a reasonable scale. (4) Lower learning rate — slower θ updates give the feedback loop less time to amplify per update. (5) Check for Double DQN — the max operator overestimates; using two networks for selection and evaluation reduces systematic upward bias in the target.`,
      },
    ],
    takeaway: `The deadly triad — function approximation, bootstrapping, off-policy data — is the precise combination that can cause divergence in TD learning, and all three components are simultaneously present in Q-learning with neural networks. SARSA avoids the off-policy component of the triad at the cost of sample efficiency; understanding this tradeoff (and when each choice is correct) is the key distinction between surface familiarity and genuine mastery.`,
  },
  {
    id: 'deep_q_networks',
    interactiveId: 'q_learning_viz',
    title: 'Deep Q-Networks',
    subtitle: 'Experience replay, target network, Double DQN, Dueling, Prioritized Replay, failure modes',
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['dqn', 'experience replay', 'target network', 'double dqn', 'dueling', 'prioritized replay'],
    summary: `DQN (Mnih et al., 2013/2015) was the first algorithm to successfully train a deep neural network to play Atari games directly from pixels. The two key innovations — experience replay and a target network — are engineering solutions to a purely theoretical problem: the deadly triad. Without both, training a neural network with Q-learning is numerically unstable. Understanding why each component was necessary (not just what it does) is the interview question. Beyond the original DQN, the "Rainbow" paper identified six independent improvements that each address a specific DQN failure mode — knowing which improvement targets which failure is a differentiator.`,
    keyPoints: [
      `Experience replay: transitions (s_t, a_t, r_{t+1}, s_{t+1}) are stored in a replay buffer (capacity 10^5 to 10^6). Mini-batches are sampled uniformly at random. Two reasons: (1) IID requirement — consecutive experiences are highly correlated. Gradient descent assumes IID samples; correlated gradients cause oscillation and slow convergence. Random sampling breaks temporal correlation. (2) Data efficiency — each transition is reused for multiple gradient updates, not just one.`,
      `Target network: a copy of Q_θ with parameters θ^- frozen and updated only every C steps (C = 1000-10000). TD target becomes R + γ max_{a'} Q_{θ^-}(s',a'). This decouples the target from the training network, preventing the moving-target instability. Without it: each gradient step moves both the prediction and the target, creating a nonstationary supervised learning problem that amplifies itself.`,
      `DQN training loop: every step, push (s,a,r,s') to replay buffer. Every 4 steps: sample 32-64 transitions, compute targets with θ^-, update θ by gradient descent on Huber loss. Every C steps: θ^- ← θ. This asymmetry (update θ every 4, update θ^- every C=1000) is empirically critical.`,
      `Double DQN: standard DQN target max_{a'} Q_θ-(s',a') uses the same network to both select (argmax) and evaluate (Q value) the action. Even if individual Q values are unbiased, max over many random variables is upward biased (Jensen's inequality — max is convex). Fix: action selection by θ, evaluation by θ^-: y = R + γ Q_{θ^-}(s', argmax_{a'} Q_θ(s',a')). Reduces overestimation bias; empirically gives +3-5% on Atari.`,
      `Dueling DQN: decompose Q(s,a) = V(s) + A(s,a), where V(s) is the state value and A(s,a) is the advantage. Two network heads that merge: Q(s,a) = V(s) + A(s,a) - (1/|A|)Σ_{a'} A(s,a'). Subtracting mean advantage makes A identifiable. Benefit: V(s) can be updated from any action in that state; more efficient in states where action choice doesn't matter much.`,
      `Prioritized Experience Replay (PER): sample transitions with probability proportional to |δ_t|^α (TD error magnitude). High TD error = surprising transition = more informative gradient. Importance sampling weights w_i = (1/N · 1/P(i))^β correct the bias introduced by non-uniform sampling. β anneals from 0.4 to 1.0 during training. PER gives +5-10% on Atari independently.`,
      `DQN fails on continuous action spaces. Computing max_{a'} Q(s',a') requires a discrete action space. For continuous actions (robotics, control), this argmax is intractable. Alternatives: DDPG (deterministic policy gradient), SAC (soft actor-critic with entropy regularisation), TD3. These are the standard algorithms for continuous control.`,
      `DQN fails on sparse rewards. If rewards are 0 for 99% of transitions, most TD errors are zero and the buffer is filled with uninformative transitions. Fixes: reward shaping, curiosity-driven exploration (intrinsic rewards for novel states), hindsight experience replay (HER — relabel failed episodes with the actual achieved goal), or hierarchical RL.`,
      `Rainbow combines six improvements (Double DQN + Prioritized Replay + Dueling + Multi-step returns + Distributional RL + NoisyNets). Rainbow significantly outperforms each individual improvement — the improvements are largely additive. The distributional component (C51, QR-DQN) is often the single most impactful addition beyond Double DQN.`,
    ],
    checkQuestions: [
      {
        q: `Why exactly does experience replay require IID samples? What happens if you train on correlated sequential transitions from the current trajectory?`,
        a: `Stochastic gradient descent converges when the gradient estimate E[∇L(θ; batch)] ≈ ∇L(θ; full data). This requires each sample to be drawn IID from the training distribution. With correlated sequential transitions (s_1, s_2, s_3, ..., from the same trajectory): the gradient estimate is dominated by the recent trajectory's statistics. If the current trajectory is in room A of a game, the gradient updates only reflect room-A experiences, rapidly overfitting Q estimates for room A while catastrophically forgetting room B, C, etc. The policy then changes (because Q estimates shift), which changes which room the agent visits, which changes the gradient again — oscillating between overfitting different parts of the state space. The replay buffer provides a diverse, temporally decorrelated sample that represents the full distribution of past experiences, giving a stable gradient estimate. Practically: training on the last 4 transitions from the current trajectory gave wildly unstable results in the original Atari experiments; uniform replay over 10^6 transitions was the fix.`,
      },
      {
        q: `What is the difference between Dueling DQN and standard DQN architecturally, and in what types of states does Dueling provide the largest benefit?`,
        a: `Standard DQN: CNN → shared layers → Q(s,a) per action (single head). Dueling DQN: CNN → shared layers → split into two streams: [V-stream: V(s), scalar] and [A-stream: A(s,a), |A|-dimensional]. Merged: Q(s,a) = V(s) + A(s,a) - mean_a A(s,a). Largest benefit in states where the action choice is irrelevant for immediate value — e.g., driving on a highway with no cars ahead: whether you steer slightly left or right doesn't matter much right now. With standard DQN, Q(s,a) for all actions must be updated together (they share no structure). With Dueling, V(s) can be updated from ANY transition in that state, regardless of which action was taken — V learns faster. The advantage stream only needs to learn the relative action differences. Benefit is most pronounced in: (1) large action spaces where most actions are similarly good/bad, (2) states where the agent must estimate "how good is this situation" independently of action choice, and (3) games with many harmless actions (Atari games with many equivalent no-op directions).`,
      },
      {
        q: `You are applying DQN to a robotic manipulation task where the reward is 1 only when the robot successfully places an object and 0 otherwise, with episodes of 200 steps. After 10M steps, the policy never achieves reward > 0. What is happening and what are your next steps?`,
        a: `The agent has never received a positive reward signal — the policy is learned entirely from 0-reward transitions, which provide zero gradient signal about what to do differently. The replay buffer is 100% uninformative. This is the sparse reward problem. Next steps in order of implementation difficulty: (1) Curriculum learning — start with the object already close to the target (easy task, higher reward frequency), gradually move the starting position farther away as the agent improves. (2) Hindsight Experience Replay (HER): treat each failed trajectory as a successful attempt to achieve the actual state reached at the end of the episode. Relabel the goal in the transition as s_T (what was achieved) instead of s_goal. Immediately provides positive reward signal from every episode. (3) Dense reward shaping — add intermediate rewards for making progress: reward for reducing distance between gripper and object, reward for lifting the object. Risk: shaped reward can change the optimal policy (use potential-based shaping to be safe). (4) Demo-augmented RL — add expert demonstrations to the replay buffer; even 10-50 successful demonstrations can bootstrap the agent past the initial sparse reward barrier. (5) Exploration: increase ε in ε-greedy significantly, or use a curiosity module (ICM) to drive exploration of novel states (may incidentally find the reward).`,
      },
    ],
    takeaway: `DQN's two key innovations — experience replay and the target network — are engineering solutions to the deadly triad, not arbitrary design choices. Experience replay breaks temporal correlation to satisfy the IID assumption of SGD; the target network freezes the bootstrap target to prevent the moving-target feedback loop. An interviewer who asks "why does DQN need a target network?" wants you to trace the instability back to the non-contraction of the semi-gradient update, not just say "it helps training."`,
  },
  {
    id: 'policy_gradients',
    title: 'Policy Gradients',
    subtitle: 'REINFORCE, log-derivative trick, high variance, baselines, why PG beats value-based',
    difficulty: 'intermediate',
    estimatedMin: 50,
    interactiveId: 'policy_gradient_viz',
    tags: ['policy gradient', 'reinforce', 'log-derivative', 'variance reduction', 'baseline', 'continuous actions'],
    summary: `Policy gradient methods directly parameterise and optimise the policy π_θ(a|s), rather than learning a value function and deriving the policy from it. The fundamental result is the Policy Gradient Theorem: ∇_θ J(θ) = E_{τ~π_θ}[Σ_t ∇_θ log π_θ(a_t|s_t) · G_t]. This is elegant — you never need the environment's transition model. But REINFORCE, the direct application, has catastrophically high variance: the return G_t used as the weight can be enormous and highly variable, making gradient estimates noisy to the point of uselessness. Understanding how and why baseline subtraction reduces variance without introducing bias — and why policy gradients are necessary for continuous and stochastic action spaces where value-based methods cannot apply — is what separates a deep understanding from surface familiarity.`,
    keyPoints: [
      `Policy gradient theorem: ∇_θ J(θ) = E_{τ~π_θ}[Σ_t ∇_θ log π_θ(a_t|s_t) · Q^{π_θ}(s_t,a_t)]. J(θ) is the expected return under policy π_θ. Derivation uses the log-derivative trick: ∇_θ π_θ(a|s) = π_θ(a|s) · ∇_θ log π_θ(a|s). This converts the gradient of an expectation with respect to the sampling distribution into an expectation of the gradient of the log policy.`,
      `Log-derivative trick (score function estimator): for any function f(x) where x ~ p_θ(x): ∇_θ E_{x~p_θ}[f(x)] = E_{x~p_θ}[f(x) · ∇_θ log p_θ(x)]. This is the core identity. It allows estimating the gradient of an expectation via Monte Carlo sampling without differentiating through the sampling process — crucial because sampling is not differentiable.`,
      `REINFORCE (Williams, 1992): for each episode, collect trajectory τ. For each timestep t: g_t = (Σ_{k=t}^T γ^{k-t} r_{k+1}) · ∇_θ log π_θ(a_t|s_t). Update: θ ← θ + α · (1/T) Σ_t g_t. Uses full Monte Carlo return — zero bias, catastrophically high variance. Requires complete episodes, unsuitable for continuing tasks.`,
      `High variance problem: G_t can range from -1000 to +1000 and varies dramatically across trajectory samples. The policy gradient estimate has variance proportional to Var[G_t]. With high variance, gradients oscillate — sometimes pointing in the right direction, sometimes not. REINFORCE without variance reduction requires millions of episodes where an actor-critic with baselines needs thousands.`,
      `Baseline subtraction: replace G_t with (G_t - b(s_t)) where b(s_t) is any function of the state (not the action). The gradient is unchanged: E[(G_t - b(s_t)) ∇ log π(a_t|s_t)] = E[G_t ∇ log π] - 0. The second term is zero because E_{a~π}[∇ log π(a|s)] = ∇_θ Σ_a π(a|s) = ∇_θ 1 = 0. The baseline centers G_t around a mean prediction without biasing the gradient.`,
      `Optimal baseline is the variance-minimising choice — in practice V^π(s_t) is used. This gives the advantage function A(s_t,a_t) = G_t - V^π(s_t) as the policy gradient weight. The advantage asks "how much better was this action than average?" — centering the signal at zero and dramatically reducing variance.`,
      `Policy gradients are necessary for continuous action spaces. DQN requires argmax_a Q(s,a) — intractable over a continuous space (e.g., 30 robot joint torques). Policy gradients directly parameterise π_θ(a|s) as a Gaussian N(μ_θ(s), σ²_θ(s)). The action is sampled from the Gaussian; no argmax required.`,
      `In adversarial settings, the optimal policy is genuinely stochastic. In rock-paper-scissors, any deterministic policy is exploitable. Value-based methods converge to the deterministic greedy policy — they cannot represent a mixed-strategy Nash equilibrium. Policy gradient methods can learn π_θ(a|s) = (1/3, 1/3, 1/3) by driving all Q-values toward equality.`,
      `Policy gradient variance sources: (1) Episode-level variance — different trajectory samples. (2) Action randomness — each action sample a_t ~ π. (3) Environment stochasticity — same action produces different outcomes. Techniques to reduce: baseline/critic, actor-critic (replace G_t with learned Q estimate), GAE (weighted multi-step returns), importance sampling with clipping.`,
    ],
    checkQuestions: [
      {
        q: `Derive why E_{a~π}[b(s) · ∇_θ log π_θ(a|s)] = 0 for any baseline b that depends only on the state, not the action.`,
        a: `E_{a~π}[b(s) · ∇_θ log π_θ(a|s)] = b(s) · E_{a~π}[∇_θ log π_θ(a|s)] [b(s) is a constant w.r.t. the expectation over a]. E_{a~π}[∇_θ log π_θ(a|s)] = Σ_a π_θ(a|s) · ∇_θ log π_θ(a|s) = Σ_a π_θ(a|s) · ∇_θ π_θ(a|s) / π_θ(a|s) = Σ_a ∇_θ π_θ(a|s) = ∇_θ Σ_a π_θ(a|s) = ∇_θ 1 = 0. The policy sums to 1 over all actions; its gradient sums to 0. Therefore the expectation is b(s) · 0 = 0. Any state-dependent baseline can be subtracted from G_t without biasing the gradient estimate — only action-dependent baselines would introduce bias.`,
      },
      {
        q: `You are training a continuous-control robot with REINFORCE and the policy fails to improve despite 50,000 episodes. What is likely happening and what changes do you make?`,
        a: `The most likely issue is catastrophically high gradient variance. With raw REINFORCE using G_t (full episode return), the variance of the gradient estimate is proportional to Var[G_t]. For a 100-step episode with stochastic transitions, G_t can vary enormously across episodes. At 50,000 episodes without improvement, the gradients are likely oscillating — sometimes pointing in the right direction, sometimes the wrong direction, with noise completely dominating the signal. Changes: (1) Immediate: add a baseline. Fit a neural network V_φ(s) by regression on Monte Carlo returns. Replace G_t with A_t = G_t - V_φ(s_t) — variance drops dramatically because A_t is centred near 0. (2) Use n-step returns instead of full-episode MC: G_t^{(n)} = Σ_{k=0}^{n-1} γ^k r_{t+k+1} + γ^n V(s_{t+n}). Reduces variance vs full MC. (3) Switch to actor-critic (A2C/PPO): replace the MC return with a learned value estimate at each step — much lower variance than MC. (4) Check policy parameterisation: ensure the initial policy entropy is high (the policy is exploring, not immediately collapsing to a deterministic action). (5) Check reward scale: if rewards are on the order of 1000, policy gradient magnitudes are huge and unstable — scale rewards to roughly [-1, 1].`,
      },
      {
        q: `In a two-player zero-sum game like poker, why is a stochastic optimal policy strictly necessary, and what does this mean for the choice of algorithm?`,
        a: `In adversarial settings, a deterministic policy is exploitable: if the opponent knows your deterministic policy (or can learn it), they can choose the best response that defeats it with probability 1. In rock-paper-scissors, any deterministic policy (always play rock) loses to the opponent's best response (always play paper). The Nash equilibrium is the unique mixed strategy where no player can improve by deviating: play each action with probability 1/3. Value-based methods (DQN, Q-learning) converge to the deterministic greedy policy π*(s) = argmax_a Q*(s,a) — they cannot represent the mixed strategy Nash equilibrium because they reduce to a single action per state. Policy gradient methods directly parameterise π_θ(a|s) as a distribution (e.g., softmax output), so they CAN converge to a mixed strategy when the gradient pushes all Q-values toward equality. For competitive multi-agent RL (AlphaStar, Poker), policy gradients with self-play (or methods like CFR — Counterfactual Regret Minimisation) are required. Additionally, in partially observable single-player settings, mixed strategies can outperform deterministic ones by hiding the agent's intent from the environment.`,
      },
    ],
    takeaway: `The log-derivative trick is the mathematical core of all policy gradient methods — it converts the gradient of an expectation under a parameterised distribution into an expectation of a tractable quantity, without requiring the environment to be differentiable. The baseline subtraction insight — that any state-dependent baseline can be subtracted without biasing the gradient because E_{a~π}[∇ log π] = 0 — is what distinguishes someone who has memorised REINFORCE from someone who understands why variance reduction works.`,
  },
  {
    id: 'actor_critic',
    title: 'Actor-Critic Methods',
    subtitle: 'A2C, A3C, advantage function, GAE, async vs sync, bias-variance in advantage estimation',
    difficulty: 'intermediate',
    estimatedMin: 55,
    tags: ['actor-critic', 'a2c', 'a3c', 'advantage', 'gae', 'bias-variance'],
    summary: `Actor-critic methods resolve the fundamental tension in RL between policy gradients (unbiased but high variance) and value-based methods (low variance but require a model or function approximation). The actor (policy π_θ) learns via policy gradient; the critic (value function V_φ) provides a low-variance baseline via the advantage A(s,a) = Q(s,a) - V(s). The advantage intuitively answers "how much better was this action than average?" — positive advantage means the action was better than average and should be reinforced. GAE (Generalized Advantage Estimation) is the principled way to trade off bias and variance in the advantage estimate, and understanding the λ parameter in GAE is a direct parallel to understanding λ in TD(λ).`,
    keyPoints: [
      `Actor-critic architecture: two components trained simultaneously. Actor: π_θ(a|s) — parameterised policy. Critic: V_φ(s) — value function estimator. Actor loss: L_π = -E[log π_θ(a|s) · A(s,a)]. Critic loss: L_V = E[(V_φ(s) - G_t)^2]. Gradients flow separately for θ and φ — they can share a trunk network with separate heads.`,
      `Advantage function: A^π(s,a) = Q^π(s,a) - V^π(s). How much better is action a vs the average action under π? Positive A: reinforce. Negative A: discourage. Key identity: E_{a~π}[A^π(s,a)] = 0. The advantage reduces variance vs using raw G_t by centering the return around the expected return V(s).`,
      `One-step advantage estimate: Â_t = r_t + γV_φ(s_{t+1}) - V_φ(s_t) = δ_t (the TD error). Lowest variance — uses only one reward and two value lookups. But high bias: V_φ(s_{t+1}) is imperfect early in training, and this error propagates directly into the advantage estimate.`,
      `Monte Carlo advantage: Â_t = G_t - V_φ(s_t). Uses actual return from the episode. Zero bias (no bootstrapping), high variance (G_t varies a lot across trajectories). Use when the critic is very inaccurate or episodes are short.`,
      `GAE (Generalized Advantage Estimation, Schulman et al. 2015): GAE^π(γ,λ) = Σ_{l=0}^∞ (γλ)^l δ_{t+l}^V = δ_t + γλδ_{t+1} + (γλ)^2δ_{t+2} + .... λ=0: one-step TD error (high bias, low variance). λ=1: MC advantage (low bias, high variance). Typical λ=0.95. GAE trusts recent rewards more than distant bootstrapped estimates — the (γλ)^l decay limits the propagation of value function error across time.`,
      `Why GAE works: distant TD errors δ_{t+l} are dominated by V(s_{t+l+1}), which is inaccurate early in training. This is the bias of bootstrapping. By using λ < 1, GAE limits the influence of those inaccurate distant estimates. As training progresses and V_φ improves, you can increase λ toward 1 to reduce bias.`,
      `A3C: multiple actor threads run in parallel, each with its own environment instance. Each thread computes gradients asynchronously and pushes them to a shared parameter server. No replay buffer needed — asynchrony provides the sample decorrelation. Linear speedup with more cores. But asynchronous updates can use stale gradients.`,
      `A2C (synchronous A3C): wait for all workers before updating parameters. Allows correct advantage estimation using a shared critic. More stable than A3C. In practice A2C is preferred on modern multi-GPU hardware where synchronous batching is efficient.`,
      `Shared backbone vs separate networks: in practice, actor and critic often share a convolutional backbone with separate heads. Sharing reduces parameters and jointly optimises the representation. Downside: gradients from policy loss and value loss can conflict — scale the value loss by a coefficient (e.g., 0.5) to prevent the critic gradient from dominating.`,
    ],
    checkQuestions: [
      {
        q: `Prove that E_{a~π}[A^π(s,a)] = 0. Why does this property make the advantage a better policy gradient weight than Q(s,a)?`,
        a: `E_{a~π}[A^π(s,a)] = E_{a~π}[Q^π(s,a) - V^π(s)] = E_{a~π}[Q^π(s,a)] - V^π(s) = V^π(s) - V^π(s) = 0. The last step uses V^π(s) = Σ_a π(a|s) Q^π(s,a) = E_{a~π}[Q^π(s,a)] by definition. This zero-mean property means the advantage acts as a centred signal: reinforcing actions that performed above-average and discouraging those below average. Using raw Q^π(s,a) as the policy gradient weight: E[Q^π(s,a)] = V^π(s), which can be large and positive (e.g., V=1000 in a high-reward game) even for average actions. The gradient is dominated by this large mean, giving a noisy signal about which actions are relatively better or worse. Advantage subtracts this mean, centering the signal at 0 and dramatically reducing variance of the gradient estimate without biasing it.`,
      },
      {
        q: `In GAE, what does setting λ=0 vs λ=0.95 vs λ=1 do to the advantage estimate? When would you choose each?`,
        a: `λ=0: Â_t = δ_t = r_t + γV(s_{t+1}) - V(s_t). One-step TD error. High bias (all future returns estimated by V, which is inaccurate early), low variance (only one reward, two value lookups). Use when: the critic V is well-trained and accurate, the environment is low-variance, or sample efficiency is critical (short trajectories). λ=0.95 (default in PPO): Â_t = Σ_l (0.95γ)^l δ_{t+l}. Considers roughly 1/(1-0.95)=20 future timesteps. Moderate bias, moderate variance. Empirically best for most tasks — balances a stable critic correction with enough real reward signal. λ=1: Â_t = G_t - V(s_t). Full Monte Carlo advantage. Zero bias (no bootstrapping), high variance (returns from many future stochastic actions). Use when: episodes are short (low variance even at λ=1), the critic is very inaccurate (trust actual rewards more than estimates), or you need unbiased gradient estimates (theory-heavy settings). In practice, λ=0.95-0.97 with episode lengths of 128-2048 steps is the standard PPO configuration for most environments.`,
      },
      {
        q: `You are training an actor-critic agent and notice that the actor loss keeps decreasing but the critic loss oscillates and never converges. The agent's reward also oscillates. What is happening?`,
        a: `This pattern indicates the actor and critic are destabilising each other. The actor is updating rapidly (perhaps with a high learning rate) while the critic cannot keep up — the value estimates V_φ(s) are always chasing a moving target (the policy π_θ is changing, changing the distribution of states and rewards, changing what V^π should be). The oscillating critic loss means the advantage estimates are inaccurate and noisy, which injects bad gradient signal into the actor, causing the actor to change more, causing the critic to chase a new target — a feedback loop. Fixes: (1) Lower the actor learning rate relative to the critic (e.g., lr_actor = lr_critic / 10). Let the critic "catch up" between actor updates. (2) PPO-style trust region: constrain how much the policy can change per update (clip ratio to [1-ε, 1+ε]). Prevents the policy from jumping so far that the critic's value estimates become completely stale. (3) Multiple critic updates per actor update: update V_φ for K=10 gradient steps per policy gradient step. (4) Reduce the shared backbone learning rate (if actor and critic share a network trunk): a single large gradient from either head can destabilise the shared representation.`,
      },
    ],
    takeaway: `GAE is not just "a way to estimate the advantage" — it is the principled interpolation between the one-step TD error (high bias, low variance) and the full Monte Carlo return (low bias, high variance), controlled by λ. The zero-mean property of the advantage — E_{a~π}[A(s,a)] = 0 — is what makes it a better policy gradient weight than raw Q-values: it centers the gradient signal and dramatically reduces variance without introducing any bias.`,
  },
  {
    id: 'ppo_trpo',
    title: 'PPO and TRPO',
    subtitle: 'Trust region, KL constraint, clipped surrogate, entropy bonus, implementation details',
    difficulty: 'advanced',
    estimatedMin: 60,
    tags: ['ppo', 'trpo', 'trust region', 'kl divergence', 'clipped objective', 'entropy'],
    summary: `TRPO and PPO solve the same problem: vanilla policy gradient updates can be too large, causing the new policy to be so different from the old one that the advantage estimates (computed under the old policy) are completely wrong — leading to catastrophic policy collapse. TRPO formalises this as a KL-divergence constraint, which produces a theoretically safe update but requires expensive second-order optimisation (conjugate gradient + line search). PPO approximates TRPO using a simple clipped objective that prevents large updates without any second-order computation. PPO has become the default algorithm for most RLHF training of LLMs, and understanding its implementation details — what the clipping does, why the entropy bonus matters, how many mini-batch updates to do — is essential for production ML work.`,
    keyPoints: [
      `Core problem (policy collapse): vanilla PG uses importance sampling to reuse data: L^{IS}(θ) = E_{a~π_{old}}[(π_θ(a|s)/π_{old}(a|s)) · A^{π_{old}}(s,a)]. The ratio r_t(θ) = π_θ(a|s)/π_{old}(a|s) adjusts for distribution shift. If π_θ diverges far from π_{old}, importance weights can be huge (or tiny), making L^{IS} a poor proxy for the true objective. Unconstrained maximisation can move the policy to a region where advantage estimates are completely wrong.`,
      `TRPO (Schulman et al. 2015): maximise L^{IS}(θ) subject to E_s[KL(π_{old}(·|s) ‖ π_θ(·|s))] ≤ δ. The KL constraint limits how far the new policy can move. The theory: if the KL constraint is satisfied, the true expected return is lower-bounded by L^{IS} minus a KL-dependent penalty. Solution: conjugate gradient to compute the natural gradient direction, then line search for the step size. Expensive: requires Fisher information matrix-vector products and multiple forward passes.`,
      `PPO (Schulman et al. 2017): L^{CLIP}(θ) = E[min(r_t(θ) A_t, clip(r_t(θ), 1-ε, 1+ε) A_t)]. When A_t > 0 (good action): if r_t > 1+ε, the clip kills the gradient — don't keep reinforcing an action once the policy is already much more likely to take it. When A_t < 0 (bad action): if r_t < 1-ε, the clip kills the gradient — don't keep discouraging an action the policy has already moved far from. This enforces a soft trust region without any second-order computation.`,
      `Why PPO outperforms TRPO in practice: (1) First-order only — PPO uses vanilla Adam, no conjugate gradient or Fisher matrix. Much faster per update. (2) Multiple mini-batch updates — PPO runs K=4-10 gradient steps per batch of collected data. TRPO does one. Better data efficiency. (3) Simpler hyperparameter (ε = 0.2). (4) Clip is a hard constraint on r_t, while KL is soft — clip may actually be more conservative in practice.`,
      `Entropy bonus: add H(π_θ(·|s)) = -Σ_a π_θ(a|s) log π_θ(a|s) with coefficient β (typically 0.01): L(θ) = L^{CLIP}(θ) - β · H(π_θ). Entropy maximisation prevents premature convergence to a deterministic policy — it encourages exploration. In RLHF training of LLMs, entropy regularisation is replaced by a KL penalty against the pre-trained model: L = reward - β · KL(π_θ ‖ π_{SFT}).`,
      `PPO hyperparameters that matter: ε (clip ratio) = 0.1-0.2; too large → no trust region effect. K (mini-batch epochs) = 4-10; more epochs → more data efficiency but more policy deviation from π_{old}. Mini-batch size = 32-256. GAE λ = 0.95. γ = 0.99. Value function coefficient c_V = 0.5. Max gradient norm = 0.5-5.0.`,
      `PPO for LLM fine-tuning (RLHF): the actor is the LLM, the critic is a separate value head on the same base model. The environment is text generation: state = tokens generated so far, action = next token (vocabulary-sized). The reward comes from a reward model trained on human preferences. Key difference: action space is ~50,000 tokens, no environment transitions, and episodes are short (100-2000 tokens). The KL penalty to the SFT reference model prevents reward hacking.`,
      `TRPO monotonic improvement guarantee: J(π_{new}) ≥ J(π_{old}) - C · max_s KL(π_{old}(·|s) ‖ π_{new}(·|s)). PPO has no such guarantee — the clipping is heuristic. For safety-critical systems (healthcare robots, autonomous driving) where you need formal guarantees about policy degradation, TRPO or constrained MDP formulations are preferred.`,
    ],
    checkQuestions: [
      {
        q: `Explain what the PPO clip objective does in the case where A_t > 0 (good action) and the new policy probability is much higher than the old one (r_t >> 1+ε).`,
        a: `With A_t > 0 and r_t = π_θ(a|s)/π_{old}(a|s) >> 1+ε: the unclipped objective r_t · A_t would be large and positive, encouraging a large gradient step to further increase π_θ(a|s) (reinforce the good action strongly). But if r_t is already much larger than 1+ε, it means the new policy has already made this action much more probable than the old policy — we've drifted far from π_{old}, so the advantage estimate A_t (computed under π_{old}) is no longer reliable for the current policy π_θ. The clip sets the objective to (1+ε) · A_t and takes the minimum of this and r_t · A_t. Since r_t > 1+ε, (1+ε) · A_t < r_t · A_t, so the minimum selects (1+ε) · A_t. Critically, ∂/∂θ [(1+ε) · A_t] = 0 with respect to r_t = π_θ/π_{old} (the clip value doesn't depend on θ) — the gradient is killed. No further gradient pushes the policy even further from π_{old}. The trust region is enforced: we do not keep reinforcing an action once the policy is already much more likely to take it than before.`,
      },
      {
        q: `You are training PPO on a continuous control task and observe that training is stable for 100 updates, then the policy collapses — mean episode reward drops from +500 to near 0 and never recovers. What happened and how do you diagnose and fix it?`,
        a: `Policy collapse after stable training is typically caused by one of: (1) Too many mini-batch epochs (K too high): after many SGD steps per collected batch, the policy has moved far from π_{old} — the importance weights r_t are very large or very small, the clip is being hit constantly, and the gradient signal is essentially noise. The policy becomes random and never recovers because all subsequent gradient estimates are from a collapsed random policy. Fix: reduce K from 10 to 4, or add an early stopping condition: if the mean KL(π_{old} ‖ π_{new}) exceeds a threshold (e.g., 0.015) during the K epochs, stop updating. (2) Learning rate too high: a single large gradient step can move the policy outside the trust region before clipping kicks in. Fix: reduce lr, add gradient norm clipping. (3) Critic collapse: if V_φ suddenly becomes a poor estimator (e.g., due to a shift in the reward distribution), the advantage estimates A_t = G_t - V_φ(s_t) become large and noisy, causing a large policy gradient update. Fix: regularise the critic, use separate learning rates for actor and critic (typically lr_critic 5-10× larger so the critic tracks faster). Diagnosis: log r_t distribution per update (should stay near 1); log the fraction of clipped updates (should be 10-30%, not 90%+); log KL(π_{old} ‖ π_{new}) per update; log value function loss and its variance.`,
      },
      {
        q: `In RLHF with PPO for an LLM, why is the KL penalty to the SFT model necessary? What happens if you remove it?`,
        a: `The KL penalty L = reward_model_score - β · KL(π_θ ‖ π_{SFT}) prevents reward hacking (Goodhart's Law: any measure used as a target ceases to be a good measure). The reward model is an imperfect proxy for human preferences — it's trained on a finite set of preference comparisons and generalises imperfectly. Without the KL penalty, PPO will find policy π_θ that maximises the reward model score, regardless of whether the text generated is actually good by human standards. Empirically, without KL: the LLM generates repetitive text ("great great great great..." scores high on positivity metrics), incoherent text that exploits the reward model's blind spots, outputs that are very different from the SFT model's distribution (distribution shift causes the reward model to be unreliable — it was trained on SFT-generated comparisons, not the new policy's outputs). The KL term prevents the policy from straying too far from the SFT baseline. Setting β too high → LLM barely changes from SFT (conservative but safe). Setting β too low → reward hacking. Typical β: 0.01-0.1, often decayed during training as the reward model's coverage of the policy's distribution improves. Empirically, higher β is better for tasks where the reward model is weak (complex reasoning) and lower β is acceptable when the reward model is reliable (factual question answering with ground truth).`,
      },
    ],
    takeaway: `PPO's clipped objective is not just a heuristic — it enforces a trust region by killing gradients when the policy ratio r_t moves outside [1-ε, 1+ε], preventing the policy from drifting so far from π_old that the advantage estimates become unreliable. The practical consequence is that more mini-batch epochs per collected batch (K too high) defeats the trust region: the policy drifts far from π_old over K gradient steps, making subsequent updates unreliable and causing the collapse patterns seen in production training runs.`,
  },
  {
    id: 'rlhf_reward_modeling',
    title: 'RLHF and Reward Modeling',
    subtitle: `Bradley-Terry model, reward hacking, Goodhart's law, DPO, KL penalty, evaluation`,
    difficulty: 'advanced',
    estimatedMin: 65,
    tags: ['rlhf', 'reward model', 'bradley-terry', 'reward hacking', 'dpo', 'goodhart'],
    summary: `RLHF is how GPT-4, Claude, Gemini, and virtually every deployed LLM is aligned with human values. The core insight: humans can compare outputs (A is better than B) even when they can't specify what "good" means formally — this comparative signal trains a reward model, which then fine-tunes the LLM with PPO. The most important thing to understand about RLHF in practice is not the algorithm — it's Goodhart's Law: once the reward model becomes the training signal, the LLM will find ways to score highly on it that don't correspond to actual human preferences. Sycophancy, verbosity, formatting exploitation — these are not bugs, they're the optimizer doing exactly what you asked. DPO offers a formulation that avoids the explicit reward model entirely. Whether DPO actually avoids reward hacking or merely moves it is an open research question.`,
    keyPoints: [
      `Bradley-Terry preference model: P(y_w > y_l | x) = σ(r_φ(x,y_w) - r_φ(x,y_l)). The reward model r_φ is trained to maximise the likelihood of observed human preferences: L(φ) = -E[log σ(r_φ(x,y_w) - r_φ(x,y_l))]. The model learns a scalar quality score per (prompt, response) pair from pairwise comparisons, without needing absolute quality labels.`,
      `Reward model architecture: typically the LLM itself (same or smaller size) fine-tuned on preference data, with a linear scalar head on the last token's hidden state. Trained on 10k-100k human preference pairs. The reward model is then frozen and used as the reward signal for PPO.`,
      `RLHF training pipeline: (1) SFT — fine-tune base LLM on high-quality demonstrations. (2) Reward Modeling — collect preference pairs, train Bradley-Terry reward model. (3) RL fine-tuning — PPO with reward model plus KL penalty to SFT: r(x,y) = r_φ(x,y) - β log (π_θ(y|x) / π_{SFT}(y|x)).`,
      `Goodhart's Law in practice: PPO is a powerful optimiser and will find unintended ways to maximise r_φ. Sycophancy: the LLM learns agreeing with the user scores highly, regardless of accuracy. Verbosity: longer answers score higher on helpfulness reward models. Formatting exploitation: spurious headers and bullet points if the reward model was trained on markdown-formatted outputs. Overconfident outputs: hedging scores lower than confident assertions, even when confidence isn't warranted.`,
      `Reward model overoptimisation: as PPO training continues, the LLM policy diverges from the SFT distribution. The reward model was trained on SFT-generated outputs — its predictions are reliable in-distribution. As the policy drifts out-of-distribution, reward model scores become meaningless and the LLM achieves arbitrarily high scores with nonsensical outputs. The Pareto frontier of (human preference score, KL from SFT) is the key diagnostic — reward model score may keep increasing while actual human preference peaks then decreases.`,
      `Constitutional AI (Anthropic): instead of human preference labels, use an LLM to evaluate outputs against a set of principles (the "constitution"). The LLM generates revised outputs that better satisfy the principles, then trains a reward model on AI-generated preference data. Scales more cheaply than human annotation, but transfers the alignment problem to the quality of the constitution and the LLM's self-critique capability.`,
      `DPO (Direct Preference Optimisation, Rafailov et al. 2023): rewrites the RLHF objective as a direct loss on preference data, eliminating both the reward model and PPO. L_{DPO}(θ) = -E[log σ(β log(π_θ(y_w|x)/π_{ref}(y_w|x)) - β log(π_θ(y_l|x)/π_{ref}(y_l|x)))]. The LLM itself acts as the implicit reward model. The partition function Z(x) cancels in the derivation — this is the key mathematical insight.`,
      `DPO vs RLHF in practice: DPO is simpler (no PPO, no reward model), more stable, faster. RLHF with PPO is more flexible — the reward model can incorporate multiple signals and the KL penalty can be adjusted dynamically. DPO can overfit to the preference dataset and may not generalise well to prompts not in training. RLHF with online PPO generates new responses and evaluates them — this online data generation is crucial for generalisation to new prompts.`,
      `Evaluation challenges: reward model score is circular (we optimised it). Human evaluation is expensive and high variance. LLM-as-judge is scalable but inherits the judge model's biases (typically prefers its own outputs). Win rate on benchmark prompts is sensitive to prompt distribution. The field lacks a reliable automatic metric for alignment quality.`,
    ],
    checkQuestions: [
      {
        q: `Derive the DPO loss from the RLHF objective. What key mathematical insight allows you to eliminate the reward model?`,
        a: `Start with the RLHF objective: max_π E_{x,y~π}[r(x,y)] - β KL(π ‖ π_{ref}). The unique optimal solution is π*(y|x) = π_{ref}(y|x) exp(r(x,y)/β) / Z(x) where Z(x) = Σ_y π_{ref}(y|x) exp(r(x,y)/β) is the partition function. Rearranging: r*(x,y) = β log(π*(y|x)/π_{ref}(y|x)) + β log Z(x). Substituting into the Bradley-Terry loss P(y_w > y_l) = σ(r(x,y_w) - r(x,y_l)): the β log Z(x) terms cancel (same for y_w and y_l): P*(y_w > y_l | x) = σ(β log(π*(y_w|x)/π_{ref}(y_w|x)) - β log(π*(y_l|x)/π_{ref}(y_l|x))). Now parameterise π* ≈ π_θ and maximise the log-likelihood of observed preferences: L_{DPO}(θ) = -E_{(x,y_w,y_l)~D}[log σ(β(log π_θ(y_w|x) - log π_{ref}(y_w|x)) - β(log π_θ(y_l|x) - log π_{ref}(y_l|x)))]. The key insight: the partition function Z(x) cancels because it appears identically in both r(x,y_w) and r(x,y_l). The LLM policy ratio π_θ/π_{ref} plays the role of the implicit reward, and no separate reward model is needed.`,
      },
      {
        q: `A model trained with RLHF consistently gives verbose answers (3x longer than the SFT baseline) with high reward model scores but lower human preference in blind evaluation. What is happening and how do you fix it?`,
        a: `This is classic reward hacking via verbosity. The reward model learned a spurious correlation: in the preference data used to train it, human annotators (consciously or not) gave higher ratings to longer, more detailed answers — possibly because more information looked more helpful. The reward model learned "length → high score" rather than "quality → high score." PPO found the verbosity shortcut: generate long answers to maximise r_φ, regardless of quality. Evidence: reward model score is high, human score is low — the two have decoupled, which is the definition of overoptimisation (Goodhart's Law). Fixes: (1) Length penalty — add -α · len(y) to the reward function during PPO. Requires calibrating α carefully. (2) Better reward model training data — include preference pairs where shorter, more precise answers are explicitly preferred over verbose ones. Add length-controlled preference collection. (3) Win-rate calibration — monitor win rate vs response length; if verbosity correlates with win rate in the reward model but not in human eval, the reward model has this spurious feature. Retrain with length-stratified comparisons. (4) Constitutional principle — add a principle "concise answers are preferred when they fully address the question." (5) DPO — directly train on human preference pairs that include (long,short) comparisons with the short answer preferred; DPO encodes these preferences directly without the reward model proxying them.`,
      },
      {
        q: `Why is "LLM-as-judge" evaluation problematic for assessing RLHF model quality, even when the judge is a much stronger model than the one being evaluated?`,
        a: `Multiple compounding biases make LLM-as-judge unreliable: (1) Self-preference / model family bias: LLMs trained by the same company or on similar data exhibit strong preferences for outputs resembling their own training distribution. GPT-4 consistently rates GPT-4-style outputs higher; Claude consistently rates Claude-style outputs higher — even when blind comparisons by humans show no such preference. (2) Sycophancy in the judge: the judge LLM may rate more verbose, confident, or elaborately formatted responses higher — the same biases present in the evaluated model can be present in the judge. (3) Positional bias: LLM judges show strong preference for the first or second option in (A,B) comparisons depending on the model. Must randomise order and average. (4) Length bias: longer responses are consistently rated higher by LLM judges. (5) Distribution shift: if the RLHF model has drifted far from the SFT baseline to score highly on some dimension, the judge evaluates in a region where its calibration is poor. (6) Prompt sensitivity: minor changes in the evaluation prompt ("Which is better?" vs "Which is more helpful?") can flip rankings. What to use instead: human evaluation on a held-out set of prompts that is independent of the training distribution, with inter-annotator agreement reported. Automated metrics for specific capabilities (factuality: F1 on verified QA pairs; instruction following: rule-based metrics on structured tasks; safety: classifier-based).`,
      },
    ],
    takeaway: `Goodhart's Law is the central challenge of RLHF: the reward model is an imperfect proxy for human preferences, and a powerful optimizer like PPO will exploit that imperfection — producing sycophantic, verbose, or formatting-exploiting outputs that score highly on the reward model but poorly in blind human evaluation. The KL penalty to the SFT model is not optional hygiene; it is the mechanism that prevents the LLM from escaping the reward model's reliable in-distribution region, and tuning β is one of the most consequential decisions in an RLHF training run.`,
  },
  {
    id: 'exploration_exploitation',
    interactiveId: 'exploration_exploitation_viz',
    title: 'Exploration vs Exploitation',
    subtitle: 'ε-greedy, UCB, curiosity (ICM), count-based, Thompson sampling, high-dimensional exploration',
    difficulty: 'advanced',
    estimatedMin: 55,
    tags: ['exploration', 'exploitation', 'ucb', 'thompson sampling', 'curiosity', 'intrinsic reward'],
    summary: `Exploration is the central unsolved problem of RL in hard environments. In simple settings (tabular MDPs with dense rewards), ε-greedy suffices — occasionally take a random action and you'll eventually see every state. In hard settings (Montezuma's Revenge, open-world robotics, molecular design), random exploration fails catastrophically: the probability of randomly discovering the reward by chance is essentially zero. The key insight that separates senior RL practitioners from junior ones: exploration is a directed search problem, not a random one. Curiosity-driven exploration (ICM), count-based exploration, posterior sampling (Thompson sampling), and UCB are all different instantiations of the same idea — seek states or actions where you have uncertainty, because that's where information gain is highest.`,
    keyPoints: [
      `ε-greedy: take a random action with probability ε, otherwise take argmax_a Q(s,a). Simple, widely used. Decay schedule: start with ε=1, anneal to ε=0.1 over training. Failure mode: random exploration in large or high-dimensional state spaces is useless. The probability of randomly reaching a reward at the end of a 100-step sequence is (ε/|A|)^100 — essentially zero. Cannot solve Montezuma's Revenge.`,
      `UCB (Upper Confidence Bound): a_t = argmax_a [Q(s,a) + c√(ln t / N(s,a))]. The second term is an exploration bonus inversely proportional to visit count — low N → high bonus → optimistic estimate. UCB is optimistic in the face of uncertainty: act as if the uncertain arm has its highest plausible value. Regret-optimal for bandits: achieves O(√(T ln T)) cumulative regret.`,
      `Count-based exploration: add intrinsic reward r_intrinsic(s) = c / √N(s). Unvisited states get large bonuses. Challenge: counts are intractable in continuous or high-dimensional state spaces. Approximations: locality-sensitive hashing, pseudocounts (SimHash, CTS density model).`,
      `ICM (Intrinsic Curiosity Module, Pathak et al. 2017): train a forward model f: (s_t, a_t) → ŝ_{t+1}. Intrinsic reward = prediction error in feature space: r_i = ||ŝ_{t+1} - φ(s_{t+1})||^2. φ is a feature encoder trained by an inverse dynamics model — forced to encode only features controllable by the agent. This solves the noisy TV problem: the encoder learns to ignore uncontrollable noise because it doesn't help predict which action was taken.`,
      `Noisy TV problem: pure prediction-error curiosity without feature learning is attracted to television static or fireplace flames — maximal prediction error forever, but zero learning. ICM's solution: features that can't be predicted from actions are invisible to the curiosity signal. RND's solution: fixed target network gives stochastic states a deterministic representation that the distillation network quickly learns, causing intrinsic reward to decay after a few visits.`,
      `RND (Random Network Distillation): intrinsic reward = ||f(s_t) - g(s_t)||^2 where f is a fixed random network and g is a trained network that tries to predict f. Novelty = large distillation error. After a few visits to any state, g learns to predict f well, and intrinsic reward decays. Simpler than ICM and avoids the noisy TV problem by design. Most practical production-grade curiosity method.`,
      `Thompson sampling (posterior sampling for RL): maintain a probability distribution over possible MDPs. At each episode, sample one MDP from the posterior and act optimally in it. Optimal Bayesian exploration — near-optimal regret. Challenge: maintaining a distribution over MDPs is computationally intractable except in simple settings. Approximation: use an ensemble of Q-networks or a Bayesian neural network.`,
      `Optimism in the face of uncertainty (OFU): UCB, count-based bonuses, and model-based upper confidence bounds are all instantiations of the same principle — act as if the uncertain region is optimal, explore it to resolve the uncertainty, then update. The alternative (pessimism under uncertainty) is used in offline RL to avoid actions with poorly estimated Q-values.`,
      `Exploration in practice for large-scale systems: RND is the most practical curiosity-based method — no forward model training, computationally cheap, avoids noisy TV. For sparse-reward robotics: HER effectively provides dense rewards from failed trajectories. For combinatorial spaces (protein design, drug discovery): Thompson sampling over learned surrogate models (Bayesian optimisation) rather than environment-based RL exploration.`,
    ],
    checkQuestions: [
      {
        q: `Why does ε-greedy exploration fail on Montezuma's Revenge (an Atari game with hard exploration), and what specific property of the environment causes the failure?`,
        a: `Montezuma's Revenge requires the agent to: navigate a room, collect a key, walk to a specific door, unlock it, reach the next room, and so on. The reward for collecting the first key is sparse — it comes only after executing a sequence of perhaps 100+ specific actions in the correct order. The probability of random ε-greedy exploration discovering this sequence is (ε/|A|)^100 ≈ (0.1/18)^100 ≈ 10^{-218}. Effectively impossible even with billions of environment steps. The agent learns nothing useful — it collects no reward signal for any Q-value update because it never sees a nonzero reward. The fundamental issue: Montezuma has a hierarchical structure (subgoals: get key, open door, advance room) that cannot be decomposed by a flat single-step exploration strategy. Fixes that work: (1) ICM / RND — the agent is rewarded for visiting novel room layouts, which drives it forward through doors and into new rooms. (2) HER — not directly applicable (no relabellable goal). (3) Hierarchical RL — define subgoals (reach door, collect key) and solve subgoal-conditioned policies. (4) Human demonstration replay — seed the buffer with a few expert trajectories showing the key-collection sequence.`,
      },
      {
        q: `Explain the noisy TV problem in curiosity-driven exploration. How does ICM's feature encoder solve it, and how does RND avoid it entirely?`,
        a: `Noisy TV problem: a pure prediction-error curiosity module rewards the agent proportional to ||ŝ_{t+1} - s_{t+1}||^2 (how wrong its world model prediction was). A television displaying random static changes each frame unpredictably — the world model can never learn to predict random noise, so prediction error stays high forever. The agent discovers the TV, stands in front of it, and collects maximum intrinsic reward indefinitely without exploring the rest of the environment. ICM solution: the feature encoder φ is trained by an inverse dynamics model — φ is trained to predict the action taken between s_t and s_{t+1} from φ(s_t) and φ(s_{t+1}). The inverse dynamics task forces φ to encode only features that are controllable by the agent. TV static: the agent's actions do not cause the static — there is no causal link. The inverse dynamics model learns to ignore TV features because they don't help predict which action was taken. The intrinsic reward in feature space ignores the TV noise. RND solution: different approach. The target network f is fixed and random, so f(TV_frame) is fixed and deterministic for each frame pattern. The distillation network g quickly learns to predict f(TV_frame) after seeing a few frames. Intrinsic reward ||f(s) - g(s)||^2 decays to near-zero after the agent has visited that TV state a few times. The agent moves on. RND is simpler and avoids the noisy TV problem by definition — stochastic states are not persistently novel because the fixed target f is always the same for a given input.`,
      },
      {
        q: `You are applying RL to a drug discovery task — the agent proposes molecular structures and receives a reward based on the drug's predicted binding affinity. The action space is discrete (atom type × position) but the molecule space has ~10^{60} valid molecules. How do you handle exploration?`,
        a: `Standard ε-greedy is hopeless in a 10^{60}-molecule space. This is a combinatorial optimisation problem, not a sequential interaction problem — the right approach depends on the problem structure. Options: (1) Bayesian optimisation with surrogate model: build a surrogate model (Gaussian Process or neural network ensemble) over molecular fingerprint/embedding space. Use Thompson sampling — sample a molecule from the posterior distribution over the surrogate's predicted distribution, evaluate it (run the binding affinity predictor), update the posterior, repeat. BO is the gold standard for expensive-to-evaluate function optimisation with limited budget. (2) Generative RL (REINVENT, GCPN): use a graph or SMILES-based generative model as the policy. The policy generates molecules token by token. Reward = binding affinity. Exploration = policy entropy encourages diverse molecule generation. Add diversity bonuses (similarity penalty to already-generated molecules) to prevent mode collapse. (3) Molecular property prediction + active learning: use uncertainty quantification (MC dropout, ensembles) to identify molecules with high predicted score AND high uncertainty. Prioritise these for expensive wet-lab evaluation (not RL, but same exploration principle). (4) Fragment-based exploration: decompose molecules into chemical fragments (known building blocks). Sample fragment combinations — the effective search space is smaller and the fragments are chemically valid. Key production consideration: the binding affinity predictor (typically a docked score) is itself an approximation — RL against an imperfect proxy reward will overfit the proxy. Include diversity rewards, synthetic accessibility penalties, and periodic wet-lab validation in the reward design.`,
      },
    ],
    takeaway: `Exploration is a directed search problem, not a random one — ε-greedy fails catastrophically in hard environments because the probability of randomly discovering a reward at the end of a long action sequence is essentially zero. The key insight unifying UCB, count-based bonuses, and curiosity modules is "optimism in the face of uncertainty": seek states or actions where your uncertainty is highest, because that is where information gain is greatest. RND is the most practical production-grade curiosity method because it avoids the noisy TV problem by design: the fixed target network gives stochastic states a deterministic representation that the distillation network quickly learns to predict, causing intrinsic reward to decay after a few revisits.`,
  },
  {
    id: 'rl_production',
    title: 'RL in Production',
    subtitle: 'Off-policy evaluation, reward delay, sim-to-real, safe RL, when NOT to use RL',
    difficulty: 'advanced',
    estimatedMin: 70,
    tags: ['off-policy evaluation', 'importance sampling', 'doubly robust', 'sim-to-real', 'safe rl', 'constrained mdp'],
    summary: `Deploying RL in production is categorically harder than deploying supervised learning. The core problems: you can't safely evaluate a new policy by running it in production before deploying it (off-policy evaluation is your only tool), the environment is non-stationary, reward signals are often delayed by hours or days, and safety constraints must be enforced with near-zero tolerance. The most important skill for a production RL engineer is knowing when NOT to use RL — many problems that look like RL are better solved with contextual bandits, supervised learning, or simple rule-based systems. That judgment call, and the ability to defend it, is what the FAANG RL interview is really testing.`,
    keyPoints: [
      `Off-policy evaluation (OPE): estimating the value of a new policy π_e using data collected under a different policy π_b. Three methods: (1) Direct Method (DM): fit a reward model and compute E_{a~π_e}[R̂(s,a)]. Biased if the model is misspecified. (2) Importance Sampling (IS): weight each trajectory by the likelihood ratio π_e/π_b. Unbiased but exponentially high variance in long-horizon tasks. (3) Doubly Robust (DR): combines DM and IS — unbiased if EITHER the reward model is correct OR the importance weights are correct. The gold standard for production OPE.`,
      `Importance sampling variance problem: for a trajectory of T steps, the IS weight is a product of T per-step ratios. Each ratio can be up to 33x for a moderately exploratory behaviour policy. With T=100 steps, trajectory-level weights of 33^100 are effectively infinite. Most weights are near zero, most trajectories contribute nothing. DR fixes this by using the DM estimate to absorb most of the variance, with IS correcting only for the DM's residual error.`,
      `Doubly robust estimator: V̂^DR = (1/N) Σ_n [w(τ_n)(G(τ_n) - R̂(s_n,a_n)) + V̂^{DM}(s_n)]. If R̂ is correct: IS correction term has near-zero mean, DM dominates with low variance. If IS weights are correct: DM error is corrected by the IS term. Two chances to be right — the "doubly robust" name is well earned.`,
      `Reward delay and sparse signals: many real systems observe reward long after the action. Recommendations: click happens hours after the impression. Drug trial: efficacy measured weeks after dosing. Standard handling: (1) Auxiliary proxies — faster-to-observe correlated signals (hover time as proxy for click). Risk: Goodhart's Law again. (2) Reward attribution — supervised learning or causal methods to attribute delayed rewards to specific actions. (3) Conservative credit assignment — assume delayed reward belongs to the most recent action.`,
      `Sim-to-real gap: sources include imperfect physics (friction, cable dynamics), observation discrepancy (rendered vs real camera images), actuation delay (simulator is synchronous; real robot has latency), and contact/deformable objects. Mitigation: domain randomisation (randomise mass, friction, lighting so the policy learns robustness), domain adaptation (fine-tune on small real data), system identification (calibrate simulator from real data).`,
      `Constrained MDPs (safe RL): max_π J(π) subject to C_i(π) ≤ d_i. Methods: (1) Lagrangian relaxation — add penalty λ_i · C_i(π) to the objective; λ rises if the constraint is violated. (2) CPO (Constrained Policy Optimisation): trust-region method projecting the policy update to satisfy constraints. (3) Safety layers: project the RL policy's action into the safe action set using a differentiable safety filter.`,
      `When NOT to use RL: (1) Credit assignment too hard — outcome observed in 6 months, use causal inference + bandit methods instead. (2) Reward specification is unclear — if you can't write a formal reward function without Goodhart violations, use supervised learning with human labels. (3) Environment is non-stationary faster than the agent can adapt. (4) No reliable simulation and production experiments are expensive. (5) Interpretability required — medical, legal, financial domains need explainable decisions. (6) Sample efficiency is critical — RL is sample-hungry; if you have 1000 episodes total, Bayesian optimisation wins.`,
      `Offline RL vs online RL: offline (batch) RL trains entirely on fixed logged data. Critical challenge: distributional shift — actions the offline policy wants to take may not appear in logged data, so Q-values for those actions are extrapolated from out-of-distribution. This extrapolation error causes overestimation of novel actions. Fixes: CQL (Conservative Q-Learning — penalises Q values for actions not in the data), BCQ (Batch-Constrained Q-learning — constrains the policy to actions similar to those in the dataset), IQL (Implicit Q-Learning — avoids OOD actions via expectile regression).`,
      `Contextual bandits as RL-lite: many real-world problems have no meaningful state transitions — the action at time t affects only the reward at time t, not future states. Single-recommendation interactions in a recommender system: clicking a video doesn't meaningfully change the user's fundamental preferences. Contextual bandits are simpler to train, have much lower-variance OPE estimators, and are easier to deploy safely. Use bandits over RL when the horizon effects are short or credit assignment is not sequential.`,
    ],
    checkQuestions: [
      {
        q: `You have logged data from a recommendation policy π_b (ε-greedy with ε=0.3) and want to estimate the click-through rate of a new model π_e. You have 10M impressions and 10 candidate items per request. Describe the IS estimator, its variance problem for long horizon, and why DR is better.`,
        a: `At the individual recommendation level (contextual bandit setting — one recommendation per request, no sequential state dependencies): IS estimator for a single impression: V̂^IS = (1/N) Σ_n (π_e(a_n|s_n)/π_b(a_n|s_n)) r_n. For ε-greedy π_b with ε=0.3: π_b(a|s) = ε/10 + (1-ε)·1[a=argmax Q(s,a')]. If π_e selects a different action from the greedy: π_e(a|s)/π_b(a|s) = 1/(ε/10) = 33.3. If π_e selects the same greedy action: π_e(a|s)/π_b(a|s) = 1/((1-ε) + ε/10) = 1/0.73 ≈ 1.37. High variance weights for actions that π_b rarely took. At 10M impressions, the central limit theorem helps — variance of the IS estimator ∝ Var[w·r]/N, manageable with 10M samples. DR estimator: V̂^DR = (1/N) Σ_n [w_n(r_n - R̂(s_n,a_n)) + R̂(s_n,π_e)], where R̂(s,a) is a predicted click rate (logistic regression or ML model trained on logged data) and R̂(s,π_e) = E_{a~π_e}[R̂(s,a)]. If R̂ is well-calibrated (low model bias, which is achievable with 10M samples): the IS correction term is small (nearly zero mean), and DM dominates with low variance. If R̂ has some model error: the IS term corrects for it, up to the variance of the correction. DR is better because: (1) Direct Method alone is biased (R̂ is imperfect). (2) IS alone has high variance for high-weight actions. (3) DR gives the best of both. Long-horizon amplification: in sequential MDPs (user session of T=20 recommendations), the trajectory-level IS weight is Π_{t=0}^T w_t — product of 20 per-step weights. Each weight can be up to 33.3; the trajectory weight can be 33.3^20 ≈ 10^30. Most trajectory weights are essentially zero (all 10^30 or nearly 0), giving catastrophic variance. DR at the trajectory level uses per-step DM values to reduce the bootstrapping.`,
      },
      {
        q: `Your RL agent for robot manipulation works perfectly in simulation (95% success rate) but achieves only 20% in the real lab. What are the sources of the sim-to-real gap and how do you diagnose and close each?`,
        a: `Sim-to-real gap sources and diagnosis/fix for each: (1) Physics mismatch — friction, mass, contact dynamics differ. Diagnosis: film the robot performing the same trajectory in simulation and reality and compare the state evolution. Fix: system identification (fit simulator parameters from real rollouts); domain randomisation over physics parameters (mass ±20%, friction ±50%, gripper compliance ±30%) so the policy learns to handle variation. (2) Observation discrepancy — simulation renders idealised images; real camera has noise, different lighting, background clutter. Diagnosis: compare histogram of real vs sim camera frames. Fix: domain randomisation of lighting, texture, camera angle; sim-to-real image transfer (train a GAN to translate sim images to realistic images, train policy on translated images); or use depth camera which has smaller sim-to-real gap than RGB. (3) Actuation delay — real robot servo has 20-50ms delay; simulator updates synchronously each timestep. Diagnosis: introduce simulated latency and measure performance drop. Fix: add realistic actuation delay to simulator; train with randomised delay; use a predictive controller that models the delay. (4) Contact and deformable objects — rigid body simulators cannot model soft object deformation (grasping compliant items). Diagnosis: test with rigid objects first. Fix: use physics engines with deformable body support (SoftGym, FleX); represent object state probabilistically. (5) Distributional shift in policy — policy trained on specific sim initial conditions; real environment has different object placement variance. Diagnosis: test with varied object placements. Fix: domain randomisation of object placement, orientation, and appearance. Measurement: track the "sim-to-real gap" as (sim success rate) - (real success rate) as a continuous metric; separate contributions by testing against sim with each factor degraded individually.`,
      },
      {
        q: `A team proposes using RL for a clinical trial treatment assignment (which treatment to give each patient each day). What are the specific risks, and what alternative framework would you recommend?`,
        a: `This is exactly the case where NOT to use RL. Specific risks: (1) Credit assignment and delay — clinical outcomes (survival, recovery) are measured weeks to months after treatment assignment. Standard RL credit assignment is designed for delays of seconds to hours, not months. The MDP state would need to encode the full patient medical history, which is partially observed (missing data, irregular sampling). (2) Non-stationarity — patient populations, treatment protocols, and standards of care change over time. An RL policy trained on one patient cohort will be out-of-distribution on future cohorts. (3) Safety constraints are catastrophic, not just a cost — a constraint violation (overdose, harmful drug combination) is potentially fatal. Constrained MDP formulations bound expected constraint violations; they do not eliminate them. A 0.1% constraint violation rate is unacceptable when it means 0.1% patient deaths. (4) Sample efficiency — clinical trials have hundreds to thousands of patients, not millions. RL requires enormous sample counts to learn a reliable policy. (5) Interpretability required — regulatory bodies (FDA) require explainable treatment decisions. RL policies are opaque. (6) Distribution shift — the RL policy changes the data distribution (it assigns treatments), which breaks the stationarity assumption and invalidates future evaluation. Recommended alternative: contextual bandits with Thompson sampling + safety constraints. Contextual bandit: treatment on day t affects only day t's outcome (or short-horizon outcomes); no long-range sequential dependencies. Thompson sampling: maintains a posterior over treatment effect parameters (Bayesian, interpretable, uncertainty-aware). Safety: use a conservative arm-exclusion rule (exclude treatment arms where the posterior probability of harm exceeds a threshold) rather than a soft Lagrangian constraint. Offline policy evaluation: doubly robust estimator with short horizons and interpretable feature-based reward models. Or best: a Bayesian adaptive clinical trial design (REMAP, response-adaptive randomisation) — well-established, FDA-recognised, provides both statistical validity and adaptivity.`,
      },
    ],
    takeaway: `The most important production RL skill is knowing when not to use RL — if reward signals are delayed by months, if you cannot specify a reward function without Goodhart violations, or if the environment is non-stationary faster than your agent can adapt, RL will fail and a contextual bandit, supervised model, or Bayesian optimisation approach will outperform it. Off-policy evaluation via doubly robust estimation is the production engineer's primary tool for evaluating a new policy before deployment, and understanding why trajectory-level importance sampling has exponential variance (product of T per-step ratios) explains why DR — which provides two chances to be correct — is the gold standard.`,
  },
]

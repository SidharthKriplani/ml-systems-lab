export const PROBABILISTIC_ML_MODULES = [
  {
    id: 'bayesian_inference',
    title: 'Bayesian Inference',
    subtitle: 'Likelihood, prior, posterior, conjugate priors, predictive distribution, sequential updating',
    difficulty: 'foundational',
    estimatedMin: 50,
    tags: ['bayes', 'posterior', 'prior', 'likelihood', 'conjugate priors', 'credible interval'],
    summary: `Most ML systems give you a point estimate and call it a day. Bayesian inference gives you a distribution over what the parameter could be — and that distinction matters every time data is scarce, you're updating beliefs in real time, or the cost of being confidently wrong is high. The update rule is simple: posterior ∝ likelihood × prior. Your prior encodes what you knew before seeing data. The likelihood scores how well each parameter value explains what you observed. Multiply them, normalise, and you have the posterior. The catch is that normalisation — integrating the likelihood over the entire parameter space — is analytically intractable for most real models, which is what drives all the approximation machinery (MCMC, VI, conjugate shortcuts).`,
    keyPoints: [
      `Bayes' theorem: p(θ|X) = p(X|θ)p(θ) / p(X). The normaliser p(X) = ∫ p(X|θ)p(θ)dθ is the marginal likelihood — a constant with respect to θ, so posterior ∝ likelihood × prior. This integral is what makes most real-world posteriors intractable.`,
      `Likelihood p(X|θ) is a function of θ given fixed data X. It is NOT a probability distribution over θ — it doesn't integrate to 1 over θ. The likelihood doesn't represent uncertainty about θ; it scores how well θ explains the observed data. Conflating likelihood with probability over parameters is one of the most common Bayesian confusions.`,
      `Conjugate priors exist so that the posterior stays in the same distributional family as the prior, giving closed-form updates. Beta(α,β) + Binomial data → Beta(α + successes, β + failures). Gaussian prior on μ + Gaussian likelihood → Gaussian posterior. Dirichlet + categorical → Dirichlet posterior. Conjugacy is a computational convenience, not a truth about the world — it's an approximation to avoid integration.`,
      `The right way to predict a new point x* is to integrate over the posterior: p(x*|X) = ∫ p(x*|θ)p(θ|X)dθ. Plugging in the MAP estimate θ̂ instead — the common shortcut — underestimates predictive uncertainty. In low-data regimes, that underestimate is large enough to matter.`,
      `Bayesian A/B testing with Beta-Binomial: after observing conversions, both p_A and p_B have Beta posteriors. P(A beats B) = ∫∫ 1[p_A > p_B] p(p_A|data) p(p_B|data) dp_A dp_B — computable analytically or by Monte Carlo. No p-value cutoff needed; you stop when P(A > B) clears your decision threshold (e.g., 95%).`,
      `Credible intervals and confidence intervals are not the same thing. A 95% Bayesian credible interval [L, U] means P(θ ∈ [L,U] | data) = 0.95 — a direct probability statement about the parameter. A 95% frequentist confidence interval means that if you repeated the experiment many times, 95% of the resulting intervals would contain the true θ. For any single interval you've computed, the true θ either is or isn't in it — there's no probability involved. Credible intervals are far easier to explain to stakeholders.`,
      `Sequential Bayesian updating is exactly what it sounds like: the posterior from today becomes the prior for tomorrow. With Beta-Binomial, after day 1 you have Beta(α₁, β₁). Feed it new data on day 2 and you get Beta(α₁ + new_successes, β₁ + new_failures). No reprocessing of historical data needed. This is the natural model for streaming systems and early-stopping A/B tests.`,
      `In low-data regimes, the prior dominates — your conclusions are largely determined by what you assumed before seeing any data. In high-data regimes, the likelihood takes over and the prior gets washed out. Always sanity-check prior sensitivity: re-run with a more diffuse prior. If the posterior shifts substantially, you don't yet have enough data to draw firm conclusions.`,
      `MAP (Maximum A Posteriori) estimate: θ_MAP = argmax_θ [log p(X|θ) + log p(θ)]. This is regularised MLE — L2 regularisation is a Gaussian prior, L1 is a Laplace prior. MAP collapses the posterior to a point estimate and throws away all information about posterior shape and spread.`,
      `Watch out for priors that look uninformative but aren't. A uniform prior over θ ∈ [0,1] seems neutral. A uniform prior over log(θ) implies something very different about derived quantities. Always ask what your prior implies about the quantities you actually care about, not just the parameterisation you happened to write down.`,
    ],
    checkQuestions: [
      {
        q: 'You run a Bayesian A/B test. After 500 conversions each, the posterior P(p_A > p_B | data) = 0.94. Your decision threshold is 0.95. Your boss says "just call it — it\'s clearly A". What do you do and why?',
        a: 'Do not call it early without examining the expected loss. P(A>B)=0.94 is close but below threshold — the 6% probability that B is actually better is not negligible, especially if the magnitude of the loss matters. Compute the expected loss from a wrong decision: if A is rolled out but B is actually better, what is the expected revenue difference? If the loss is small (e.g., 0.01% conversion rate difference), maybe proceed. If the loss is large, wait for more data or lower the threshold explicitly and document the risk. Simply calling it because it "looks like A" violates the decision rule you set prospectively — it introduces optional stopping bias even in Bayesian tests when the threshold is used loosely.',
      },
      {
        q: 'What is the marginal likelihood p(X) in Bayes\' theorem and why is it hard to compute?',
        a: 'p(X) = ∫ p(X|θ)p(θ)dθ is the normalising constant that makes the posterior a valid probability distribution. It requires integrating the likelihood over the entire parameter space under the prior. For models with continuous, high-dimensional θ, this integral is analytically intractable — there is no closed form. It requires approximations: MCMC (samples from the posterior avoiding explicit normalisation via Metropolis-Hastings ratio), variational inference (lower bound on log p(X) via the ELBO), or conjugate priors (where the integral has a known closed form). The marginal likelihood is also the quantity used for Bayesian model selection — the model with higher p(X) is preferred, balancing fit and complexity via automatic Occam\'s razor.',
      },
      {
        q: 'You have Beta(2,2) prior on a coin\'s bias θ. You flip it 3 times and observe HHH. What is the posterior and MAP estimate? How does this differ from the MLE?',
        a: 'Posterior: Beta(2+3, 2+0) = Beta(5, 2). MAP = (α-1)/(α+β-2) = 4/5 = 0.8. MLE = 3/3 = 1.0. The prior pulls the estimate away from the degenerate MLE of 1.0. With only 3 observations, the Beta(2,2) prior (equivalent to 2 pseudo-successes and 2 pseudo-failures) materially regularises the estimate. The posterior is also a distribution — not just 0.8, but with spread reflecting uncertainty. E[θ|data] = 5/7 ≈ 0.71 (posterior mean is more conservative than MAP). As n → ∞, all three converge.',
      },
      {
        q: 'When would you NOT use Bayesian inference in production and use frequentist methods instead?',
        a: 'When computational cost is prohibitive: computing the posterior requires MCMC or VI — much slower than fitting a point estimate. Most production ranking/recommendation systems cannot afford this per inference call. When the prior is hard to specify and the choice matters: in high-stakes settings with small data, an incorrect prior leads to incorrect conclusions. Frequentist methods make no prior assumptions. When interpretability and regulatory compliance require frequentist framing: clinical trials, financial risk models, and legal standards often mandate confidence intervals and p-values. When the model is so large (e.g., billion-parameter neural networks) that Bayesian inference over all weights is practically impossible — deep ensembles or conformal prediction are used instead.',
      },
    ],
    takeaway: `The fundamental Bayesian insight is that posterior ∝ likelihood × prior — uncertainty about parameters is tracked as a full distribution, not collapsed to a point estimate. The practical consequence is that MAP plugged into the predictive distribution underestimates uncertainty in low-data regimes; the correct predictive distribution integrates over the full posterior. In an interview context, the sharpest demonstration of understanding is explaining why credible intervals (direct probability statements about parameters) are not the same as confidence intervals (long-run coverage guarantees) and why that difference matters for business decisions.`,
  },
  {
    id: 'gaussian_processes',
    title: 'Gaussian Processes',
    subtitle: 'Distribution over functions, kernel selection, GP regression, sparse GPs, Bayesian optimisation',
    difficulty: 'advanced',
    estimatedMin: 65,
    tags: ['GP', 'gaussian process', 'kernel', 'Bayesian optimisation', 'inducing points', 'SVGP'],
    summary: `Most models give you a prediction at a point. A Gaussian Process gives you a prediction and a calibrated uncertainty estimate at every single input — not just at training points, anywhere in the input space. That's the thing that makes GPs uniquely valuable: closed-form posterior uncertainty. You define a GP through a mean function and a kernel (covariance function), observe some noisy data, and in one matrix inversion you get the full posterior over functions — posterior mean as your prediction, posterior variance as your uncertainty. The price is O(n³) in compute and O(n²) in memory for that matrix inversion, which becomes the hard wall around n ≈ 10,000. Everything in the sparse GP literature is about getting around that wall.`,
    keyPoints: [
      `A GP is fully specified by a mean function m(x) and a kernel k(x, x'). Any finite collection f(x₁), ..., f(xₙ) follows a multivariate Gaussian: f ~ N(m, K) where Kᵢⱼ = k(xᵢ, xⱼ). The kernel encodes your structural beliefs about the function — its smoothness, length-scale, and periodicity. Choosing the kernel is choosing your prior over functions.`,
      `GP regression with noisy observations y = f(X) + ε, ε ~ N(0, σ²I): the posterior at new points X* is Gaussian. Posterior mean: μ* = m(X*) + K(X*,X)[K(X,X)+σ²I]⁻¹(y-m(X)). Posterior variance: Σ* = K(X*,X*) - K(X*,X)[K(X,X)+σ²I]⁻¹K(X,X*). Uncertainty collapses near observed data, balloons in unexplored regions — exactly what you want from a model that knows what it doesn't know.`,
      `Kernel choice matters more than most practitioners admit. RBF (squared exponential): k(x,x') = σ²exp(-‖x-x'‖²/2ℓ²). Infinitely differentiable — implausibly smooth for most real functions. Matérn kernels control differentiability via ν: Matérn 3/2 (once differentiable) and 5/2 (twice differentiable) are almost always better defaults. Add a periodic kernel for recurring patterns. Compose kernels additively or multiplicatively to encode structure. The kernel is the most important modeling decision you'll make with a GP.`,
      `Hyperparameters (length-scale ℓ, signal variance σ², noise σ²_n) are learned by maximising the log marginal likelihood: log p(y|X,θ) = -½yᵀ(K+σ²I)⁻¹y - ½log|K+σ²I| - n/2 log(2π). The first term rewards fit; the log-determinant penalises complexity. This Occam's razor is automatic: the model with the simplest kernel that explains the data wins.`,
      `O(n³) scaling is the central engineering fact about GPs. Inverting the n×n kernel matrix costs O(n³) compute and O(n²) memory. For n > 10,000, this is simply not feasible. The entire sparse GP literature exists to approximate this inversion.`,
      `Sparse GPs get around O(n³) by introducing m << n inducing points Z that summarise the data. FITC approximates the full GP with low-rank + diagonal structure at O(nm²) per step. SVGP (Stochastic Variational GP) places a variational distribution q(u) over inducing outputs u=f(Z) and optimises the ELBO with minibatch SGD — O(m³) per step regardless of n. With m=500 and minibatches, you can train on millions of points.`,
      `Bayesian optimisation is where GPs earn their keep in production. Use a GP as a surrogate for an expensive black-box function (hyperparameter search, drug discovery, materials science). Acquisition functions decide where to query next: Expected Improvement (EI): E[max(0, f(x) - f(x+))]. UCB: μ(x) + κσ(x) — κ tunes explore/exploit tradeoff. Thompson Sampling: sample a function from the posterior, maximise it. BO with a GP is orders of magnitude more sample-efficient than grid or random search for expensive evaluations.`,
      `GPs belong in your toolkit for: small datasets (n < 10,000) where uncertainty quantification is the point; Bayesian optimisation for hyperparameter search (Vizier, Ax/BoTorch); time series with structured temporal kernels; scientific regression where domain knowledge can be encoded in the kernel. They don't belong in: high-dimensional unstructured inputs (image pixels without a meaningful kernel), or n > 100,000 without significant approximation infrastructure.`,
      `The most common GP mistake is defaulting to RBF. Real-world functions — sensor readings, financial returns, experimental measurements — are not infinitely differentiable. Matérn 5/2 or 3/2 is the right starting point. Also: always normalise outputs to zero mean and unit variance before fitting. GPs are sensitive to output scale in ways that will quietly break your model if you skip this step.`,
    ],
    checkQuestions: [
      {
        q: 'You have 50,000 training points and want to use a GP. What are your options and what do you trade away with each?',
        a: 'Option 1: Exact GP with Cholesky decomposition — O(n³) = infeasible for n=50k. Option 2: SVGP with m=500 inducing points. Train with minibatch SGD, O(m³) per step. Trade-off: the inducing point approximation introduces bias — the posterior is not the true GP posterior, it is a variational approximation. Predictive uncertainty may be underestimated between inducing points. Option 3: SKI (Structured Kernel Interpolation) — places inducing points on a regular grid, uses Toeplitz structure for O(n log n) matrix-vector products. Requires inputs to be low-dimensional and works best on 1-3D grids. Option 4: Use a deep kernel (DKN) — map inputs through a neural network, then apply a GP in the latent space. Retains O(m³) for the GP but gains expressivity of neural feature learning. For n=50k, SVGP or DKN with good inducing point selection (k-means initialisation) is the standard recommendation.',
      },
      {
        q: 'Your GP Bayesian optimisation run converges prematurely — the acquisition function keeps suggesting the same region. What went wrong and how do you fix it?',
        a: 'Premature convergence usually indicates insufficient exploration: the acquisition function is exploiting the current best region rather than exploring uncertain areas. Possible causes: (1) UCB with too-small κ — the confidence bound rewards exploitation too heavily. Increase κ to force exploration. (2) The GP posterior variance has collapsed at a bad optimum because the noise model is wrong — if σ²_n is too large, the GP thinks all observations are noise and uncertainty never decreases. Check the learned hyperparameters. (3) The kernel length-scale is too large — the GP believes the function is smooth everywhere, so nearby unexplored points seem well-characterised. Fix: use Matérn with shorter length-scale, or add an ARD (Automatic Relevance Determination) kernel. (4) Use Thompson Sampling instead of EI/UCB — it naturally provides diversity in suggestions by sampling different function realisations.',
      },
      {
        q: 'How does the GP marginal likelihood objective perform automatic model selection, and what is its failure mode?',
        a: 'The log marginal likelihood log p(y|X,θ) = -½yᵀ(K+σ²I)⁻¹y - ½log|K+σ²I| - n/2 log(2π) has two terms in tension: the first term (data fit) rewards kernels that explain the data well. The second term (log-determinant complexity penalty) penalises kernels that are too expressive — an overly flexible kernel can fit any data, but the log|K| term is large, penalising it. This Occam\'s razor is automatic: the MML prefers the simplest model consistent with the data. Failure mode: with n << hyperparameter count, the MML surface has multiple local optima. Random restarts help but do not guarantee the global optimum. In very low-data regimes (n < 20), the MML can be dominated by the prior and the learned hyperparameters may not reflect the true function structure — use informative priors on hyperparameters (fully Bayesian treatment of θ) or simply validate out-of-sample.',
      },
    ],
    takeaway: `GPs are the correct tool when you need calibrated uncertainty at every prediction point, not just a point estimate — this is what makes them the standard for Bayesian optimisation and small-data scientific regression. The O(n³) wall is the central engineering constraint, and SVGP with inducing points is the standard workaround for n > 10,000. In interviews, demonstrating that kernel choice encodes structural assumptions (Matérn 5/2 for realistic roughness, periodic kernel for cyclic patterns, RBF only when you truly believe infinite differentiability) shows you understand that a GP is only as good as the prior you encode in its kernel.`,
  },
  {
    id: 'variational_inference',
    title: 'Variational Inference',
    subtitle: 'ELBO, KL divergence, mean field VI, CAVI, stochastic VI — when to use over MCMC',
    difficulty: 'advanced',
    estimatedMin: 60,
    tags: ['variational inference', 'ELBO', 'KL divergence', 'mean field', 'CAVI', 'stochastic VI'],
    summary: `The posterior p(z|x) is almost always intractable — the normalising integral doesn't have a closed form. MCMC solves this by sampling; it's asymptotically exact but slow. Variational inference (VI) solves it by turning the inference problem into an optimisation problem. Pick a tractable family Q of distributions, then find the member q*(z) ∈ Q that's closest to p(z|x) in KL divergence. That's it. The payoff is that you get mature gradient-based optimisation machinery and can scale to large datasets via minibatch gradients. The cost is bias: VI systematically underestimates posterior uncertainty because the KL it minimises is mode-seeking, not mass-covering.`,
    keyPoints: [
      `The ELBO (Evidence Lower Bound): log p(x) = ELBO(q) + KL[q(z) ‖ p(z|x)]. Since KL ≥ 0, the ELBO is always a lower bound on log p(x). Maximising the ELBO over q is exactly minimising KL[q(z) ‖ p(z|x)]. Written out: ELBO = E_q[log p(x|z)] + E_q[log p(z)] - E_q[log q(z)] — reconstruction quality plus how close q is to the prior.`,
      `Mean field VI assumes q(z) = ∏ᵢ qᵢ(zᵢ) — each latent variable is independent in the approximate posterior. This is the central approximation, and it's a strong one: real posteriors almost always have correlations between latent variables. Mean field ignores all of them. Each marginal can be well-matched while the joint approximation is terrible.`,
      `CAVI (Coordinate Ascent Variational Inference) updates each qᵢ(zᵢ) one at a time, holding all others fixed. The optimal update is qᵢ*(zᵢ) ∝ exp(E_{-i}[log p(z,x)]). With conjugate priors, these updates are closed-form and look a lot like EM. Without conjugacy, you need gradient-based VI (BBVI via reparameterisation).`,
      `Why VI underestimates uncertainty: VI minimises KL[q ‖ p] = E_q[log q - log p]. This penalises q for putting mass where p is small, but never penalises q for failing to cover regions where p is large. The result: q concentrates on one mode of a multimodal posterior and ignores the rest. The reverse KL[p ‖ q] would spread q across all modes (mass-covering) — that's what Expectation Propagation uses — but it's computationally harder. Mode-seeking KL is the cheaper choice; overconfident posteriors are the price.`,
      `Stochastic VI scales CAVI to large datasets. Standard CAVI sweeps all n points per update — O(n) per iteration. Stochastic VI uses minibatch ELBO gradient estimates with SGD/Adam, reducing cost to O(batch_size) per update. This is how you run VI on millions of examples — used in LDA at scale, SVGP, and most deep generative models.`,
      `VI vs MCMC in one sentence: MCMC is asymptotically exact but slow; VI is biased but fast. Use VI when n is large and approximate uncertainty is acceptable. Use MCMC when exact uncertainty is the actual deliverable — scientific inference, high-stakes medical decisions — and you can afford the runtime.`,
      `Amortised VI trains an inference network to predict q(z|x) directly from x, rather than running iterative CAVI per datapoint. VAEs use this: the encoder outputs μ(x), σ(x) defining q(z|x) = N(μ(x), σ(x)²). At test time, inference is one forward pass — O(1) instead of CAVI's iterative convergence. The cost is that the encoder is a single learned function, not a per-datapoint optimisation, so it can be less accurate.`,
      `The most common VI failure: the ELBO converges but predictions are poor because Q is too restrictive for the true posterior. A diagonal Gaussian mean field can't represent correlated latent variables. Richer families help: normalising flows chain invertible transformations to express complex q, hierarchical VI adds auxiliary latents. If the ELBO at convergence is much lower than the log evidence from MCMC, the variational gap is large and you need a richer family.`,
    ],
    checkQuestions: [
      {
        q: 'Explain why the ELBO is a lower bound on log p(x) and why maximising it is equivalent to minimising KL[q ‖ p(z|x)].',
        a: 'log p(x) = log ∫ p(x,z)dz = log E_q[p(x,z)/q(z)] ≥ E_q[log p(x,z)/q(z)] by Jensen\'s inequality (log is concave). The RHS is the ELBO = E_q[log p(x,z)] - E_q[log q(z)]. The gap between log p(x) and the ELBO equals KL[q(z) ‖ p(z|x)]: log p(x) - ELBO = E_q[log q(z) - log p(z|x)] = KL[q ‖ p(z|x)] ≥ 0. So ELBO = log p(x) - KL[q ‖ p(z|x)]. Since log p(x) is fixed wrt q, maximising ELBO over q is exactly minimising KL[q ‖ p(z|x)].',
      },
      {
        q: 'Your mean field VI model gives very tight posteriors (narrow q distributions) but makes poor predictions. What is likely happening and how do you diagnose it?',
        a: 'Likely cause: mean field VI has converged to a single mode of a multimodal posterior, and the narrow q is overconfident in that mode. The true posterior may be multimodal (e.g., the model is non-identifiable — swapping two cluster centres gives the same likelihood). Diagnosis: (1) Compare ELBO at convergence from multiple random initialisations — if they differ substantially, the landscape is multimodal. (2) Run short MCMC chains (HMC) and compare marginal posteriors — if MCMC posteriors are much wider or multimodal while VI posteriors are narrow, the variational family is inadequate. (3) Check posterior predictive samples — draw θ ~ q(θ) and generate x* ~ p(x*|θ). If samples are much less diverse than held-out data, the posterior is underestimated. Fix: use normalising flows or mixture-of-Gaussians as the variational family, or switch to MCMC if uncertainty is critical.',
      },
      {
        q: 'What is the difference between CAVI and black-box variational inference (BBVI), and when does each apply?',
        a: 'CAVI (Coordinate Ascent VI) requires conjugate prior-likelihood pairs so that the optimal qᵢ update is analytically closed form. It is fast and exact within the mean field family but only works for models designed with conjugate structure (e.g., LDA with Dirichlet-Multinomial). BBVI (Black-Box VI) estimates the ELBO gradient using Monte Carlo: ∇_λ ELBO = E_q[∇_λ log q(z|λ) · (log p(x,z) - log q(z|λ))]. This works for any differentiable model but the gradient estimator has high variance — requires variance reduction tricks (control variates, reparameterisation). Reparameterisation trick (used in VAEs): if q(z|λ) = N(μ,σ²), write z = μ + σε where ε ~ N(0,1). Then ∇_λ ELBO = E_ε[∇_λ log p(x, μ+σε) - ∇_λ log q(μ+σε|λ)]. The gradient now passes through the sampled z, giving low-variance estimates. BBVI with reparameterisation is the default for neural latent variable models.',
      },
    ],
    takeaway: `VI is biased by construction — it minimises KL[q ‖ p], which is mode-seeking, causing it to concentrate on one posterior mode and systematically underestimate uncertainty. The practical implication is that tight VI posteriors do not mean the posterior is actually tight; they may mean VI found a local optimum and ignored other modes. The critical interview distinction is VI vs MCMC: VI is the correct choice when scalability matters and approximate uncertainty is acceptable (large datasets, amortised inference in VAEs), MCMC when exact uncertainty is the actual deliverable.`,
  },
  {
    id: 'vae_foundations',
    title: 'Variational Autoencoders',
    subtitle: 'Generative model, ELBO, reparameterisation trick, posterior collapse, β-VAE, representation learning',
    difficulty: 'advanced',
    estimatedMin: 65,
    tags: ['VAE', 'ELBO', 'reparameterisation', 'posterior collapse', 'beta-VAE', 'latent space'],
    summary: `VAEs are most commonly introduced as image generators, but that's not where they shine today — diffusion models have taken that crown. What VAEs actually offer is a principled framework for learning structured latent representations: a decoder p(x|z) that generates data from latent codes, and an encoder q(z|x) that inverts this, approximating the posterior over latent codes given data. Training maximises the ELBO — reconstruction quality minus the KL cost of deviating from the prior — end-to-end. The reparameterisation trick is the key insight that makes gradient flow through the sampling step possible. The central failure mode to know cold is posterior collapse: the decoder becomes so powerful it ignores z entirely, the encoder collapses to the prior, and you've trained an expensive unconditional generator.`,
    keyPoints: [
      `Generative model: p_θ(x,z) = p_θ(x|z)p(z). The prior p(z) = N(0,I). The decoder p_θ(x|z) is a neural network mapping latent code z to a distribution over x — Gaussian for continuous x, Bernoulli for binary. To generate: sample z ~ N(0,I), then sample x ~ p_θ(x|z).`,
      `The encoder q_φ(z|x) = N(μ_φ(x), diag(σ_φ(x)²)) is a neural network mapping input x to the mean and diagonal variance of an approximate posterior over z. This is amortised VI — one encoder handles all inputs rather than running a separate optimisation per x.`,
      `VAE ELBO: L(θ,φ;x) = E_{q_φ(z|x)}[log p_θ(x|z)] - KL[q_φ(z|x) ‖ p(z)]. The reconstruction term rewards the decoder for explaining the data given latent codes sampled from the encoder. The KL term penalises the encoder for drifting far from the prior N(0,I). For diagonal Gaussian q, the KL is closed form: -½ Σⱼ(1 + log σⱼ² - μⱼ² - σⱼ²).`,
      `The reparameterisation trick: you can't backpropagate through a sampling operation z ~ N(μ,σ²) because the sampling step has no gradient. Fix: write z = μ_φ(x) + σ_φ(x)⊙ε where ε ~ N(0,I). Now z is a deterministic function of the encoder parameters and a fixed noise draw. Gradients flow through μ and σ to φ. This is the single innovation that makes end-to-end VAE training tractable.`,
      `Posterior collapse is the pathological training failure to watch for. The decoder becomes so expressive it models p(x) without using z at all — the encoder learns to output the prior (q(z|x) ≈ N(0,I)), KL collapses to near zero, but reconstruction still looks fine because the decoder ignores z. Symptom: KL term near zero after training, latent codes carry no information about x. Most commonly caused by autoregressive decoders (PixelCNN, Transformer) that are powerful enough to model the data marginal alone.`,
      `Fixes for posterior collapse: KL annealing — start training with β=0 (pure reconstruction), linearly ramp β to 1 over the first 30% of training, forcing the model to learn to use z before regularisation kicks in. Free bits — floor the KL cost at δ per latent dimension, preventing the optimizer from collapsing dimensions that aren't yet informative. β < 1 during early training is another lever.`,
      `β-VAE: L = E_{q_φ}[log p_θ(x|z)] - β·KL[q_φ(z|x) ‖ p(z)]. β > 1 over-penalises the KL, forcing the encoder into fewer, more independent latent dimensions — each dimension learns to control one factor of variation (position, colour, size). This is disentanglement. β < 1 combats posterior collapse. β = 1 is the standard VAE. Higher β means better disentanglement at the cost of reconstruction quality — you're trading off representation structure against generative fidelity.`,
      `Interpolation works in VAE latent space in a way it doesn't in standard autoencoders. Because the encoder maps to a regularised N(0,I) space, spherical interpolation between z₁ and z₂ produces semantically coherent intermediate samples. Standard autoencoders have irregular latent spaces — interpolations land in low-density holes and produce garbage. The KL regularisation is what forces latent space continuity.`,
      `Production uses for VAEs: anomaly detection (low ELBO = poor reconstruction or high KL → flag as anomalous), data imputation (infer z from observed dimensions, decode to fill missing values), molecule generation and drug discovery (VAE latent spaces over molecular graphs enable gradient-based property optimisation), representation learning (encoder embeddings carry uncertainty information that discriminative models discard). VAE outputs are blurrier than GAN or diffusion outputs — that's not a bug, it's a direct consequence of optimising expected log-likelihood under an approximate posterior, which averages over plausible reconstructions.`,
    ],
    checkQuestions: [
      {
        q: 'Explain why the reparameterisation trick is necessary, and describe a case where it cannot be applied.',
        a: 'Without reparameterisation: the ELBO gradient wrt φ is ∇_φ E_{z~q_φ}[f(z)] = ∇_φ ∫ f(z)q_φ(z)dz. The sampling distribution depends on φ, so you cannot push ∇_φ inside the expectation directly. The REINFORCE (score function) estimator does ∇_φ E[f(z)] = E[f(z)∇_φ log q_φ(z)] — unbiased but extremely high variance. Reparameterisation: write z = g_φ(ε) where ε ~ p(ε) does not depend on φ. Then ∇_φ E_{ε~p}[f(g_φ(ε))] = E_ε[∇_φ f(g_φ(ε))] — push gradient inside, low variance. Inapplicable when z is discrete (Bernoulli, Categorical): there is no differentiable reparameterisation. Workarounds: Gumbel-Softmax trick (continuous relaxation with temperature τ → 0 approaches discrete), REINFORCE with baseline for discrete VAEs. Discrete VAEs are used in VQ-VAE (vector quantisation) and DALL-E v1.',
      },
      {
        q: 'Your VAE\'s KL loss is near zero after 10 epochs. What is happening and how do you fix it?',
        a: 'Near-zero KL means q(z|x) ≈ p(z) = N(0,I) — the encoder is outputting the prior regardless of x. This is posterior collapse: the decoder has learned to reconstruct x without using z. Diagnosis: check if reconstruction loss is still decreasing (if yes, the decoder is powerful enough to model p(x) autoregressively without z). Fixes: (1) KL annealing: start training with β=0 (pure reconstruction), linearly increase β to 1 over the first 30% of training. This forces the decoder to first learn to use z before the KL regularisation becomes active. (2) Free bits: set KL cost to max(KL, λ) per latent dimension with λ ≈ 0.5 — no penalty when KL is small, preventing collapse. (3) Reduce decoder capacity: use a simpler decoder (e.g., MLP instead of PixelCNN). The most expressive decoders collapse the most easily because they can ignore z and still achieve low reconstruction loss.',
      },
      {
        q: 'Why do VAE-generated images look blurry compared to GAN outputs, and is this fixable within the VAE framework?',
        a: 'VAEs maximise E_{q(z|x)}[log p_θ(x|z)] — expected log-likelihood averaged over the posterior distribution of z given x. When p_θ(x|z) is a Gaussian, this is equivalent to minimising mean squared reconstruction error averaged over z samples. MSE averages over possible reconstructions — if x* could plausibly be decoded from z with slight spatial variation, the MSE-optimal output is the average of those possibilities, which is blurry. GANs instead use an adversarial loss that scores sharpness directly, producing individual samples rather than averages. Within the VAE framework: (1) Use a perceptual loss (VGG feature-space MSE) instead of pixel-MSE — reduces blurriness but changes the generative model semantics. (2) Use VQ-VAE with a separate autoregressive prior — quantises z, enabling discrete latent codes that support sharper decodings. (3) Add a GAN discriminator term to the ELBO (as in VAE-GAN hybrids). True VAE blurriness under Gaussian decoder is not fully fixable without changing the loss function.',
      },
    ],
    takeaway: `VAE blurriness is not a bug — it is a direct mathematical consequence of optimising expected MSE over the posterior, which produces the average of all plausible reconstructions. Posterior collapse (KL → 0, encoder ignoring the input) is the central training failure and is caused by expressive decoders that can model p(x) without using z; KL annealing is the standard fix. The reparameterisation trick — writing z = μ + σ·ε so gradients flow through the sampling step — is what makes end-to-end VAE training tractable and breaks down precisely when z is discrete.`,
  },
  {
    id: 'approximate_inference',
    title: 'Approximate Inference Methods',
    subtitle: 'Laplace approximation, importance sampling, MCMC, HMC, diagnostics — when to use which',
    difficulty: 'advanced',
    estimatedMin: 70,
    tags: ['MCMC', 'HMC', 'Metropolis-Hastings', 'Laplace approximation', 'importance sampling', 'R-hat', 'ESS'],
    summary: `Outside of conjugate models, the posterior p(θ|X) is intractable — you can't compute it, only approximate it. The methods for doing this range from fast-but-wrong (MAP, Laplace) to slow-but-right (MCMC). MCMC is the gold standard for scientific inference: given infinite time, it converges to the true posterior. In production ML, that runtime cost is usually prohibitive, so most practitioners use MAP estimates with frequentist standard errors and reserve MCMC for settings where exact uncertainty is the product you're delivering — clinical decision support, scientific simulation, hierarchical models where you actually need the full posterior. Knowing when each method is appropriate, and how to tell whether MCMC has actually converged, is what separates someone who's read about Bayesian inference from someone who's done it.`,
    keyPoints: [
      `Laplace approximation: find the MAP, compute the Hessian H = -∇²log p(θ|X) at that point, approximate the posterior as N(θ_MAP, H⁻¹). No sampling required — just one optimisation and one Hessian computation. Works well when the posterior is genuinely unimodal and approximately Gaussian, which the Bernstein-von Mises theorem guarantees asymptotically. Fails badly for multimodal posteriors, heavy-tailed posteriors, or when the MAP is not a representative posterior summary.`,
      `Importance sampling estimates E_p[f(θ)] using a proposal q: E_p[f(θ)] ≈ Σᵢ wᵢ f(θᵢ) where wᵢ ∝ p(θᵢ|X)/q(θᵢ). The effective sample size ESS ≈ (Σwᵢ)²/Σwᵢ² tells you how many i.i.d. samples the weighted set is worth. If ESS << N, a handful of high-weight samples are carrying the entire estimate. In high dimensions, IS collapses catastrophically — the typical sets of p and q have negligible overlap, almost all weights are near zero, and you'd need exponentially many samples to get a reliable estimate.`,
      `Metropolis-Hastings: propose θ* ~ q(θ*|θ_current), accept with probability α = min(1, p(θ*|X)q(θ_current|θ*) / p(θ_current|X)q(θ*|θ_current)). The critical insight: only the ratio p(θ*|X)/p(θ_current|X) = p(X|θ*)p(θ*) / p(X|θ)p(θ) is needed — the intractable normaliser p(X) cancels in the ratio. This is why MCMC can sample from unnormalised posteriors.`,
      `HMC augments state with momentum, uses gradient information to make large correlated proposals that are accepted at high rates. Hamiltonian dynamics: H(θ,r) = -log p(θ|X) + rᵀr/2. Leapfrog integrator approximates dynamics, followed by a Metropolis correction. No random walk behaviour — HMC explores the posterior far more efficiently than random-walk MH. NUTS (No-U-Turn Sampler, default in Stan and PyMC) adapts step size and trajectory length automatically.`,
      `MCMC diagnostics are non-negotiable. R-hat (Gelman-Rubin): run K independent chains. R-hat = √(total variance / within-chain variance). R-hat < 1.01 → converged. R-hat > 1.1 → the chains are exploring different regions, you don't have the posterior yet. Effective Sample Size (ESS): accounts for within-chain autocorrelation. ESS = N / (1 + 2Σₖ ρₖ). ESS < 100 → high Monte Carlo error, need more samples. Trace plots: should look like a hairy caterpillar — no trends, no sticking, all chains overlapping.`,
      `Divergences in HMC are a red flag, not a warning. A divergence means the leapfrog integrator hit extreme posterior curvature and went numerically unstable. Any divergences mean the posterior geometry is problematic and your estimates are biased. Non-centred reparameterisation fixes the most common cause. Never report results from a sampler with divergences.`,
      `Non-centred parameterisation is the most important practical HMC trick for hierarchical models. Centred: μ_i ~ N(μ, σ²) directly — creates a funnel geometry that's pathological for HMC when σ is small. Non-centred: write μ_i = μ + σ·z_i where z_i ~ N(0,1), sample z_i instead. The geometry is well-conditioned and the funnel disappears. If you're seeing divergences in a hierarchical model, this is the first thing to try.`,
      `Rough guide for method selection: MAP + frequentist CIs for most production systems — fast, scalable, interpretable, underestimates uncertainty but usually fine. Laplace for last-layer BNNs and fast post-MAP uncertainty estimates. Light MCMC (small data, few parameters) for scientific models, clinical risk scoring, hierarchical A/B test analysis where you need the full posterior. NUTS/HMC in Stan or PyMC for serious Bayesian analysis with well under a million parameters. VI (CAVI, BBVI) for large-scale latent variable models where sampling is too slow. Deep ensembles + conformal prediction for large neural networks where full Bayes is infeasible.`,
    ],
    checkQuestions: [
      {
        q: 'You run 4 MCMC chains with NUTS. After 2000 samples per chain, R-hat = 1.35 for a key parameter. What do you do?',
        a: 'R-hat = 1.35 >> 1.01 — the chains have not converged. The chains are exploring different regions of the posterior. Steps: (1) Examine trace plots for all 4 chains — look for systematic differences (e.g., two chains stuck at high values, two at low values), trends indicating the chain has not finished burning in, or slow mixing. (2) Check for divergences in HMC — if there are many divergences, the posterior has a funnel or extreme curvature. Non-centred reparameterisation likely needed. (3) Run chains much longer — 2000 samples is often insufficient for hierarchical models. Try 10,000 samples with adequate warmup. (4) Check the model specification: is the posterior actually multimodal? Unidentifiable models (e.g., label-switching in mixture models) cause persistent R-hat > 1 regardless of chain length. Fix the model with ordering constraints or other identifiability constraints. Never report results with R-hat > 1.01.',
      },
      {
        q: 'Why does importance sampling fail in high dimensions, and what is the effective sample size telling you?',
        a: 'In d dimensions, the typical set of p (where the posterior mass concentrates) and the typical set of q have exponentially shrinking overlap as d increases. Even if q and p are both Gaussians with similar means, if p has slightly smaller variance, then q will draw samples from the tails of p where p(θ)/q(θ) is exponentially small — almost all weights will be near zero except for a few lucky samples near the mode. ESS captures this: if ESS = 10 out of N = 10,000 samples, then effectively only 10 samples are contributing to the estimate — the remaining 9,990 have negligible weight. The estimate is dominated by a handful of samples and has extremely high variance. In practice, IS becomes unusable for d > ~20 without careful proposal design (SMC, annealed IS). The log-weight variance ≈ d × (variance of log p - log q per dimension) — exponential in d.',
      },
      {
        q: 'What is the Bernstein-von Mises theorem and when does it break down?',
        a: 'The Bernstein-von Mises (BvM) theorem: under regularity conditions, as n → ∞, the posterior p(θ|X) converges to N(θ_true, I(θ_true)⁻¹/n) — a Gaussian centred at the MLE with covariance equal to the inverse Fisher information. This means: (1) In large samples, the posterior is approximately Gaussian. (2) The Laplace approximation becomes exact. (3) Bayesian credible intervals coincide with frequentist confidence intervals. Breakdown conditions: (a) Non-regular models: non-differentiable likelihoods, boundaries, or non-identifiable models (mixture models with k unknown). (b) High-dimensional settings where d grows with n: if d/n → c > 0, BvM fails — the posterior does not concentrate at the truth. (c) Misspecified models: if the true data-generating process is not in the model class, BvM can fail or converge to the wrong distribution. (d) Semiparametric/nonparametric models: infinite-dimensional posteriors do not satisfy BvM in general. Practical implication: do not trust the Laplace approximation for models with fewer than ~50 observations per parameter.',
      },
    ],
    takeaway: `R-hat is the non-negotiable convergence diagnostic for MCMC — never report results with R-hat > 1.01, because chains exploring different posterior regions means the samples are not from the target distribution. The most practically important HMC trick is non-centred reparameterisation for hierarchical models: the centred parameterisation creates a funnel geometry that causes divergences and slow mixing, while writing μ_i = μ + σ·z_i moves the geometry to well-conditioned territory. Understanding when Metropolis-Hastings cancels the intractable normaliser p(X) is the key insight that explains why MCMC works at all for unnormalised posteriors.`,
  },
  {
    id: 'bayesian_neural_networks',
    title: 'Bayesian Neural Networks & Uncertainty Quantification',
    subtitle: 'Weight distributions, aleatoric vs epistemic uncertainty, dropout VI, last-layer Laplace, deep ensembles, conformal prediction',
    difficulty: 'advanced',
    estimatedMin: 70,
    tags: ['BNN', 'uncertainty', 'epistemic', 'aleatoric', 'deep ensembles', 'conformal prediction', 'dropout VI'],
    summary: `The standard neural network gives you a point prediction with no honest signal about how confident it should be. Bayesian Neural Networks address this by placing distributions over weights rather than point estimates — the resulting predictive uncertainty splits into two distinct types: irreducible noise in the data (aleatoric) and uncertainty from not having seen enough of the right data (epistemic). In practice, exact BNNs are infeasible at any useful scale, so practitioners use approximations. The empirical punchline is that deep ensembles — just train several models from different random seeds — consistently beat most principled Bayesian approximations on calibration benchmarks. The reason isn't Bayesian coverage; it's that ensembles sample different loss basins and get function diversity that MC Dropout and most VI methods miss. Conformal prediction takes a completely different route: rigorous coverage guarantees without any Bayesian machinery.`,
    keyPoints: [
      `Aleatoric uncertainty is irreducible noise in the data. Even infinite training data won't remove it. A blurry medical image has inherent label ambiguity regardless of how big your training set is. Model it explicitly by predicting σ_θ(x) alongside μ_θ(x): p(y|x,θ) = N(μ_θ(x), σ_θ(x)²), where the network outputs both the prediction and its heteroscedastic noise level.`,
      `Epistemic uncertainty comes from not having enough data in a region — it's reducible. A well-calibrated BNN should be uncertain on out-of-distribution inputs and confident on in-distribution inputs. In a BNN, epistemic uncertainty = variance of E[y|x,θ] over the posterior p(θ|data). The key operational use: high epistemic uncertainty in a specific region tells you exactly what data to collect next.`,
      `MC Dropout (Gal & Ghahramani, 2016) runs T forward passes at test time with random dropout masks, treating their variance as predictive uncertainty. Formally equivalent to approximate VI in a deep GP under specific conditions. In practice: it's the most widely deployed BNN approximation because it requires zero architecture changes — just leave dropout on at test time. The VI interpretation requires specific dropout rates that practitioners rarely honour, but it works well enough to be useful.`,
      `Last-layer Laplace approximation: train to MAP, then fit a Gaussian posterior only on the last-layer weights W_last ~ N(W̃, H_last⁻¹). The Hessian is only d_last × d_last — feasible even for large networks. Everything else stays as a point estimate. This is a strong practical baseline: reuses pretrained weights, zero training changes, adds only a post-hoc Hessian computation, and calibrates well for classification tasks.`,
      `Deep ensembles: train M independent networks from different random seeds (and optionally different data shuffles). Predictive distribution is a mixture of their M softmax outputs. Uncertainty is the disagreement among ensemble members. Empirically dominates MC Dropout, last-layer Laplace, and most VI methods on calibration benchmarks. The cost is M × training time and M × inference cost — that's the honest tradeoff.`,
      `Why ensembles outperform most Bayesian approximations: deep network loss landscapes are highly multimodal. Different random seeds converge to different basins, each corresponding to a different functional solution. MC Dropout and VI both approximate a distribution around one basin. Ensembles explicitly sample multiple basins and get genuine function-space diversity. The "Bayesian" framing is post-hoc rationalization — their advantage is loss-basin diversity, not posterior coverage.`,
      `Temperature scaling: after training, find T on a held-out validation set minimising NLL with scaled logits: σ(f(x)/T). T > 1 softens predictions, T < 1 sharpens. Accuracy doesn't change — argmax is invariant to scaling. The single most effective simple recalibration technique. Apply it to any neural network used for probabilistic prediction. Always.`,
      `Conformal prediction provides a formal coverage guarantee with no distributional assumptions on the model. Compute nonconformity scores s(xᵢ,yᵢ) on a calibration set. Prediction set for a new x*: C(x*) = {y : s(x*,y) ≤ quantile_{1-α}(calibration scores)}. Guarantee: P(y* ∈ C(x*)) ≥ 1-α under exchangeability of the calibration and test data. The prediction set is large when the model is uncertain, small when confident. This is the only uncertainty method with a formal coverage guarantee.`,
      `OOD detection quality separates good uncertainty methods from bad ones. Neural networks are notoriously overconfident on OOD inputs — high softmax probability for inputs that look nothing like the training distribution. Common methods: maximum softmax probability (simple baseline, bad), ODIN (temperature scaling + input perturbations), Mahalanobis distance in feature space, energy-based scores. Deep ensembles produce better OOD uncertainty than single-model methods. Conformal prediction is the only method with a formal guarantee — everything else is heuristic.`,
    ],
    checkQuestions: [
      {
        q: 'A model predicts 90% probability that a tumour is benign. Is this aleatoric or epistemic uncertainty, and how would you tell the difference?',
        a: 'The 10% probability of malignancy could reflect either type. To distinguish: (1) Collect more data from similar cases. If the 10% uncertainty persists even with thousands of similar images, it is aleatoric — the images themselves are ambiguous (low contrast, poor resolution, borderline morphology). If the uncertainty decreases with more similar training data, it was epistemic — the model was uncertain because it had seen few similar cases. (2) Use a BNN or ensemble: epistemic uncertainty appears as disagreement between ensemble members or high posterior variance over weights. Aleatoric uncertainty appears as high variance in the likelihood term p(y|x,θ) for any given θ. (3) Practically: epistemic is higher in sparse data regions (rare tumour type, unusual scan protocol). Aleatoric is higher for inherently ambiguous images regardless of training set size. For clinical deployment: aleatoric uncertainty means "get a second opinion or better scan"; epistemic means "this patient type is underrepresented in our training data — flag for clinical review and log for future retraining."',
      },
      {
        q: 'You need uncertainty estimates for a 100M-parameter production model. MC Dropout adds 100ms per call (20 forward passes). What alternatives exist?',
        a: 'The latency budget rules out MC Dropout for synchronous production calls. Options: (1) Last-layer Laplace: compute the Hessian of the last layer only (e.g., softmax weights, d=1024×C). One forward pass + one matrix-vector product with H_last⁻¹. Latency: ~same as a single forward pass + O(C²) overhead. Good calibration for classification tasks. (2) Single-pass deterministic uncertainty: SNGP (Spectral Normalised GP output layer) adds a GP head to the last layer, computes posterior variance in one forward pass. DUQ (Deterministic Uncertainty Quantification) uses RBF kernel distances in feature space. (3) Conformal prediction post-hoc: calibrate prediction sets offline on a held-out set, then at inference use the model\'s softmax output + precomputed thresholds. Zero additional latency at inference. (4) Distill the ensemble into a single model with uncertainty outputs (knowledge distillation from ensemble to BNN). (5) Asynchronous uncertainty: serve the point prediction synchronously, compute ensemble/MC-Dropout uncertainty asynchronously and log it for monitoring without blocking the user. The right choice depends on whether uncertainty gates a decision (synchronous needed) or is used for monitoring (asynchronous acceptable).',
      },
      {
        q: 'What guarantee does conformal prediction provide, and what assumption can violate it?',
        a: 'Conformal prediction guarantees: P(y* ∈ C(x*)) ≥ 1-α, where α is the target miscoverage rate (e.g., 0.1 for 90% coverage). This is a marginal coverage guarantee — averaged over the draw of x*. It does not guarantee conditional coverage P(y* ∈ C(x*)|x*) ≥ 1-α for every specific x*. The assumption required is exchangeability: the calibration set and the test point (x*,y*) are jointly exchangeable (a generalisation of i.i.d.). Violations: (1) Distribution shift — if the test distribution differs from calibration, exchangeability fails and coverage is not guaranteed. A model calibrated on hospital A may not achieve 90% coverage on hospital B. (2) Temporal non-stationarity — if the data-generating process changes over time, past calibration data is not exchangeable with future test data. (3) Selective prediction (only predict on confident examples) — selecting test examples based on the model score breaks exchangeability unless handled carefully (Mondrian conformal prediction conditions on additional covariates). Practical implication: recompute conformal quantiles on fresh calibration data from the same distribution as current test data, and monitor coverage on production data.',
      },
    ],
    takeaway: `Deep ensembles consistently outperform MC Dropout and most VI-based BNN approximations in empirical calibration benchmarks, and their advantage is not Bayesian coverage — it is function space diversity from different loss basins. The aleatoric/epistemic distinction has a concrete operational meaning: aleatoric uncertainty cannot be reduced by collecting more data (the image itself is ambiguous), while epistemic uncertainty directs you to gather more data of that type. Conformal prediction is the only method with a formal coverage guarantee under exchangeability, making it the right choice when coverage guarantees are a system requirement rather than a best-effort goal.`,
  },
  {
    id: 'calibration',
    title: 'Probabilistic Calibration',
    subtitle: 'Reliability diagrams, ECE, overconfidence in NNs, temperature scaling, Platt scaling, isotonic regression',
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['calibration', 'ECE', 'temperature scaling', 'Platt scaling', 'reliability diagram', 'overconfidence'],
    summary: `Calibration answers a simple question: when your model says 70% confident, does it actually get it right 70% of the time? A model can be accurate but badly miscalibrated — and in any application where the probability itself drives a decision (medical risk scores, fraud thresholds, credit pricing), a miscalibrated model is making systematically wrong decisions even when it's picking the right class. Modern neural networks are well-documented to be overconfident out of the box (Guo et al., 2017). Raw softmax outputs should never be trusted as probabilities for production use without calibration. The good news: temperature scaling fixes most of it in five lines of code and doesn't touch accuracy.`,
    keyPoints: [
      `Calibration definition: p(Y=1 | f(X)=p) = p for all p ∈ [0,1]. Among all predictions where your model outputs probability p, exactly fraction p of the true labels should be 1. Perfect calibration means the model's confidence is an accurate statement of its accuracy at that confidence level.`,
      `The reliability diagram is your first diagnostic. Bin predictions into 10 equal-width intervals. For each bin, plot mean predicted confidence vs. observed accuracy. Perfect calibration is the diagonal y=x. Overconfident models fall below the diagonal — they claim higher confidence than their accuracy warrants. Underconfident fall above. The shape of the deviation tells you whether to use temperature scaling (uniform shift) or something more flexible.`,
      `ECE = Σₘ |Bₘ|/n · |acc(Bₘ) - conf(Bₘ)|: weighted average of |accuracy - confidence| across bins. Well-tuned temperature-scaled networks hit ECE ≈ 1-3%. Uncalibrated neural networks often sit at 5-15%. Watch the binning strategy: equal-width bins can be dominated by nearly empty extreme bins. Adaptive bins (equal samples per bin) are more reliable. Report MCE (Maximum Calibration Error) alongside ECE to capture worst-case gaps.`,
      `Neural networks are systematically overconfident because cross-entropy training is minimised when the network outputs a near-one-hot distribution — pushing logits larger and larger. Larger and deeper models are more overconfident, not less. Guo et al. (2017) documented this across ResNets and DenseNets. It is a well-replicated finding. Assume your network is overconfident until proven otherwise.`,
      `Temperature scaling: fit a scalar T on a held-out validation set minimising NLL of softmax(f(x)/T). T > 1 softens predictions, T < 1 sharpens. Key properties: accuracy is unchanged (argmax of softmax(f/T) = argmax of f), one parameter so there's no overfitting risk, applies post-hoc with no retraining. This is the default step before deploying any neural network classifier for probabilistic use.`,
      `Platt scaling: fit sigmoid(af(x) + b) on a held-out calibration set. Two parameters instead of one — can fix both over/underconfidence and bias. More flexible than temperature scaling but can overfit if the calibration set is small. Originally developed for SVMs. Use Platt scaling when temperature scaling leaves a residual pattern in the reliability diagram.`,
      `Isotonic regression: fit a non-parametric monotone function from predicted probabilities to observed frequencies. Most flexible calibration method — can handle any shape of systematic miscalibration. Overfits aggressively on small calibration sets. Reserve for when you have > 10,000 calibration samples and temperature scaling isn't getting the job done.`,
      `Why calibration matters in production: a miscalibrated fraud score of 0.8 that actually corresponds to 60% risk leads to systematic under-blocking. A model saying 90% sepsis probability when the true rate is 60% changes treatment decisions. If the output probability feeds downstream as a feature, miscalibration propagates. Any threshold-based decision (alert if P(fraud) > 0.7) requires accurate probabilities at that specific threshold — ECE averaged across bins won't tell you whether you're miscalibrated exactly at your decision boundary.`,
      `Calibration and discrimination are separate axes. A model can be perfectly calibrated by just predicting the base rate everywhere — but that's useless for decision-making. Calibration is necessary but not sufficient. The Brier score = (1/n)Σ(pᵢ - yᵢ)² decomposes into calibration + refinement, so it jointly penalises both. Use AUC/AP for discrimination, ECE for calibration, Brier score as a joint summary.`,
    ],
    checkQuestions: [
      {
        q: 'Your model has 92% accuracy but ECE = 12%. What does this mean, and what would you do?',
        a: 'High accuracy but high ECE means the model makes correct predictions but with wrong confidence levels — likely overconfident. On average, in each calibration bin, the predicted confidence exceeds the observed accuracy by ~12 percentage points. For example, when the model says 95% confidence, the true accuracy in that bin is ~83%. This matters for any downstream use of the probability. Steps: (1) Inspect the reliability diagram to see where miscalibration is worst. (2) Apply temperature scaling: fit T on a held-out validation set by minimising NLL. Check if ECE drops to acceptable levels (< 3%). (3) If temperature scaling is insufficient (asymmetric miscalibration), try Platt scaling. (4) Verify calibration on the test set (not the calibration set used to fit T). (5) Report both pre- and post-calibration ECE. Note: calibration should be measured on the same distribution the model will be deployed on — if distribution shift is expected, recalibrate on recent in-distribution data periodically.',
      },
      {
        q: 'You need to compare two models for a medical triage application. Model A has AUC=0.88, ECE=0.03. Model B has AUC=0.91, ECE=0.11. Which do you deploy?',
        a: 'For medical triage, the decision depends on how predictions are used. If the model output triggers a threshold-based action (e.g., "high risk if score > 0.7 → immediate escalation"), calibration is critical — Model B\'s ECE=0.11 means its stated 0.7 probability may correspond to actual risk of 0.59, leading to systematic under-triage. Model A\'s better calibration means its threshold is more reliable. If the model output is used only for relative ranking (e.g., prioritise the top 100 patients by risk score), AUC matters more and Model B is better. For triage that uses probability thresholds to determine care pathway: prefer Model A or recalibrate Model B. Post-hoc calibration of Model B (temperature scaling) should be attempted first — if Model B\'s ECE can be brought to < 0.03 without losing much AUC, it wins. Request both pre- and post-calibration ECE for any model comparison in probabilistic prediction contexts.',
      },
      {
        q: 'Why does standard cross-entropy training produce overconfident neural networks, and does label smoothing fix it?',
        a: 'Cross-entropy loss is minimised when the model outputs a one-hot probability vector — infinite logit for the correct class, -∞ for all others. With finite-capacity networks, training pushes logits to increasingly large values to approach this ideal, resulting in softmax outputs concentrated near 1.0 on the argmax class. This is the overconfidence mechanism. Label smoothing (replace hard targets y_i ∈ {0,1} with soft targets (1-ε)y_i + ε/K) penalises overconfident outputs by assigning small probability ε/K to all non-target classes. It was shown by Müller et al. (2019) that label smoothing improves calibration, sometimes dramatically — but also that it distorts the penultimate-layer representations, reducing their usefulness as features for downstream tasks. Label smoothing is not a substitute for post-hoc calibration: it changes the training objective and may not achieve the same ECE reduction as temperature scaling. The current recommendation: use label smoothing (ε ≈ 0.1) during training for regularisation benefits, then always apply temperature scaling post-training for deployment.',
      },
    ],
    takeaway: `Temperature scaling is the single most important post-training step for any neural network classifier used for probabilistic predictions: it cannot hurt accuracy (argmax is unchanged), requires only one parameter fit on a validation set, and dramatically reduces ECE for the systematic overconfidence that cross-entropy training induces. The AUC vs ECE tradeoff question (AUC measures ranking quality, ECE measures probability accuracy at the threshold you will actually use for decisions) is the calibration insight interviewers most want to hear — a high-AUC model with poor calibration systematically misprices risk at every decision threshold.`,
  },
  {
    id: 'information_geometry',
    title: 'Information Geometry & Natural Gradient',
    subtitle: 'Fisher information matrix, statistical manifold, natural gradient descent, K-FAC, connection to Adam',
    difficulty: 'advanced',
    estimatedMin: 75,
    tags: ['Fisher information', 'natural gradient', 'information geometry', 'K-FAC', 'Riemannian metric', 'Adam'],
    summary: `Standard gradient descent measures progress in parameter space. But a step of fixed Euclidean length in parameter space can correspond to a tiny nudge or a massive shift in what the model actually computes — depending on the local geometry of the distribution. Information geometry formalises this: the space of probability distributions is a Riemannian manifold where distances are measured by the Fisher information matrix, not by Euclidean norms. Natural gradient descent (Amari, 1998) premultiplies the gradient by the inverse FIM, taking steps that are equal-sized in distribution space. The result: faster convergence per step, because you're respecting the actual geometry. The catch: computing the full FIM inverse is O(p²) in storage and O(p³) in compute — prohibitive for any model with more than a few thousand parameters. Adam's diagonal learning rate scaling is a crude approximation to this, which explains both why Adam works and why it fails on highly correlated parameters.`,
    keyPoints: [
      `The Fisher information matrix: F = E_{x~p_θ}[∇_θ log p(x|θ) ∇_θ log p(x|θ)ᵀ] = -E_{x~p_θ}[∇²_θ log p(x|θ)]. The FIM quantifies how much the distribution p(x|θ) changes when θ is perturbed. Locally: KL[p_θ ‖ p_{θ+δ}] ≈ ½ δᵀFδ. So the FIM is the curvature of the KL divergence landscape — the metric tensor for distribution space.`,
      `The statistical manifold: the set of all distributions {p(x|θ): θ ∈ Θ} forms a Riemannian manifold. The FIM is the metric tensor at each point. Two distributions can be far apart in parameter space (large ‖Δθ‖₂) but close in distribution space (small KL), or vice versa. Standard gradient descent ignores this — it treats all parameter directions as equally meaningful, which they aren't.`,
      `Natural gradient: θ_{t+1} = θ_t - η F(θ_t)⁻¹ ∇_θ L. The F⁻¹∇L term transforms the gradient from parameter space into distribution space. The key property is covariance under reparameterisation: if you apply any bijective transformation φ = h(θ), the natural gradient in φ-space produces the same update as in θ-space (after accounting for the Jacobian). Euclidean gradient is not covariant — reparameterising your model changes which optimum gradient descent finds.`,
      `Natural gradient converges faster in loss landscapes with high curvature variation. In a ravine (high curvature one direction, low another), standard SGD zigzags. Natural gradient rescales step sizes by local curvature — large steps in low-curvature directions, small steps in high-curvature directions. Per distribution-space step, you get more ELBO reduction than any gradient step of the same parameter-space norm.`,
      `FIM = Hessian of KL: KL[p_θ ‖ p_{θ+δ}] ≈ ½δᵀFδ. This is the operational definition that matters. Flat directions in F (zero eigenvalues) correspond to parameter directions that don't change the model's output distribution — overparameterisation, symmetries, dead neurons. These are the directions where gradient descent wastes effort.`,
      `K-FAC (Kronecker-Factored Approximate Curvature): for a layer with weights W, the FIM admits a Kronecker product approximation F ≈ A ⊗ G where A = E[aₜaₜᵀ] (input activations covariance) and G = E[gₜgₜᵀ] (output gradient covariance). Storage drops from O(p²) to O(d_in² + d_out²) per layer. Inversion: (A⊗G)⁻¹ = A⁻¹⊗G⁻¹. K-FAC makes approximate natural gradient tractable for neural networks and is used in second-order optimisers at DeepMind.`,
      `Adam as diagonal FIM approximation: the second moment v_t ≈ diag(F) is a diagonal estimate of the Fisher. Dividing gradient by √v_t + ε approximates rescaling by the diagonal Fisher. AdaGrad is the exact diagonal Fisher accumulation for i.i.d. data. RMSProp is an exponentially windowed version. Better FIM approximation → faster convergence per step, higher compute cost per step. Adam is the sweet spot for most deep learning tasks; K-FAC wins when data is small and per-step cost is affordable.`,
      `Overparameterised networks and the FIM: with more parameters than data, the FIM is rank-deficient — many parameter directions don't change the output distribution. Standard gradient descent finds the minimum parameter-space norm solution. Natural gradient finds the minimum distribution-space norm update. This distinction matters for understanding implicit regularisation in deep networks and why gradient descent generalises despite fitting training data exactly.`,
      `TRPO/PPO connection: Trust Region Policy Optimisation explicitly constrains KL[π_old ‖ π_new] ≤ δ at each update — that's exactly a natural gradient step with a trust region in distribution space. PPO approximates this with a clipped surrogate. The KL constraint is what makes RL training stable: Euclidean constraints on weight updates don't prevent large changes in the policy distribution, but KL constraints do.`,
    ],
    checkQuestions: [
      {
        q: 'Why is the natural gradient invariant to reparameterisation of the model parameters, and why does this matter?',
        a: 'Standard gradient: if φ = h(θ) is a reparameterisation, ∇_φ L = J⁻ᵀ ∇_θ L where J = ∂φ/∂θ. The gradient direction changes under reparameterisation — the step depends on how you chose to parameterise the model. Natural gradient: F_φ = J⁻ᵀ F_θ J⁻¹ (FIM transforms as a covariant tensor). F_φ⁻¹ ∇_φ L = J F_θ⁻¹ J^{-T} J^{-T} ∇_θ L — this correctly maps back to the same update in θ-space (up to Jacobian). The natural gradient step corresponds to the same distribution-space step regardless of parameterisation. Why it matters: for neural networks, different parameterisations (e.g., using batch norm, or reparameterising weights vs. activations) should not change which optimum is found. Standard gradient descent is sensitive to parameterisation — you get different solutions depending on how you implement the same model (e.g., batch norm vs. weight normalisation produces different implicit regularisation). Natural gradient is theoretically parameterisation-free.',
      },
      {
        q: 'Adam\'s second moment estimate v_t ≈ diag(F). What does this mean for Adam\'s performance on highly correlated parameters?',
        a: 'Adam uses a diagonal approximation to the FIM — it rescales each parameter\'s gradient independently based on that parameter\'s gradient variance, but ignores correlations between parameters (off-diagonal elements of F). For highly correlated parameters (e.g., features in a linear layer with collinear inputs, or parameters in two adjacent residual blocks with strong shared gradient signals), the diagonal F misrepresents the true curvature. Consequence: Adam takes steps that are suboptimal in correlated directions — it may overstep in directions where the true curvature (including correlations) is high, or understep in directions where the diagonal approximation underestimates the step size. This manifests as slower convergence on problems with highly correlated parameters (certain NLP tasks with attention across similar tokens, or dense layers with correlated input features). K-FAC handles this by modelling input-output correlations within each layer via the Kronecker product, which explains why K-FAC converges in fewer steps than Adam on some tasks — at higher per-step computational cost.',
      },
      {
        q: 'What is the connection between the Fisher information matrix and confidence intervals in maximum likelihood estimation?',
        a: 'By the Cramér-Rao bound, the variance of any unbiased estimator θ̂ satisfies Var(θ̂) ≥ F(θ)⁻¹. The MLE achieves this bound asymptotically: √n(θ̂_MLE - θ_true) → N(0, F(θ_true)⁻¹) in distribution. The asymptotic covariance of the MLE is the inverse FIM — this is where frequentist confidence intervals for MLEs come from. In practice: compute the observed FIM (Hessian of log-likelihood at θ̂) as a consistent estimator of the expected FIM. The ij-th element of F⁻¹ gives the asymptotic covariance between θ̂ᵢ and θ̂ⱼ. For deep neural networks: the FIM is p×p (p = millions of parameters) — direct inversion is impossible. Diagonal approximations give per-parameter confidence intervals, which are used in continual learning (Elastic Weight Consolidation — EWC penalises changes to parameters with high Fisher information, preserving important knowledge from previous tasks).',
      },
    ],
    takeaway: `The central insight of information geometry is that parameter space is the wrong space in which to measure gradient steps — what matters is how much the model's output distribution changes, which is measured by the Fisher information matrix. Adam's per-parameter learning rate rescaling (dividing by √v_t) is a diagonal FIM approximation, which is why it fails on highly correlated parameters where off-diagonal FIM terms matter. The connection to TRPO/PPO is the most concrete production use case: constraining updates by KL[π_old ‖ π_new] rather than by Euclidean weight change is what makes RL training stable.`,
  },
  {
    id: 'probabilistic_graphical_models',
    title: 'Probabilistic Graphical Models',
    subtitle: 'Bayesian networks, MRFs, d-separation, factor graphs, belief propagation, HMMs',
    difficulty: 'advanced',
    estimatedMin: 70,
    tags: ['PGM', 'Bayesian network', 'MRF', 'd-separation', 'belief propagation', 'HMM', 'factor graph'],
    summary: `PGMs give you a vocabulary for encoding what's independent of what in a high-dimensional distribution, using graphs. Bayesian networks use directed graphs to encode generative causal stories. Markov Random Fields use undirected graphs to encode symmetric correlations. Both make complex joints tractable by factoring them into local potentials — inference becomes a local operation over the graph rather than a global operation over the full joint. PGMs largely ceded perception tasks (images, speech, text) to deep learning after 2012. They remain the right tool when conditional independence structure needs to be explicitly represented, audited, and explained — medical diagnosis networks, causal models, structured prediction with hard output constraints.`,
    keyPoints: [
      `Bayesian network: a DAG where each node Xᵢ has parents Pa(Xᵢ). The joint factorises as p(X₁,...,Xₙ) = Πᵢ p(Xᵢ | Pa(Xᵢ)). No edges = full independence. Fully connected = full joint, no independence exploited. The structure of the DAG is the entire modelling decision — it encodes your assumptions about the causal generating process.`,
      `Markov Random Field (MRF): undirected graph, joint factorises over cliques: p(X) = (1/Z) Πc ψc(Xc). The partition function Z = Σ_X Πc ψc(Xc) is typically intractable — it sums over all configurations. MRFs are used for symmetric statistical correlations: image segmentation (neighbouring pixels tend to share labels), spatial statistics, Ising models. The intractable Z is the central computational problem.`,
      `d-separation is the fundamental tool for reading conditional independence off a Bayesian network. X and Y are d-separated given Z if Z blocks all paths between them. Blocking rules: a chain (X → m → Y) or fork (X ← m → Y) is blocked when m ∈ Z. A collider (X → m ← Y) is blocked when m ∉ Z AND no descendant of m is in Z. d-separation implies conditional independence in all distributions consistent with the graph. Learn the collider rule — it's the one that trips people up.`,
      `Factor graphs make the factorisation structure explicit: bipartite graph with variable nodes (circles) and factor nodes (squares). Each factor ψ_a connects to a subset of variables. The joint = Π_a ψ_a(Xa). Factor graphs with loops require approximate inference; trees allow exact inference via belief propagation.`,
      `Belief propagation (sum-product): messages pass between variable and factor nodes in both directions. Variable-to-factor: μ_{x→f}(x) = Πg∈ne(x)\\f μ_{g→x}(x). Factor-to-variable: μ_{f→x}(x) = Σ_{~x} ψ_f(Xf) Πy∈ne(f)\\x μ_{y→f}(y). After 2|E| message passes on a tree, marginals are exact. On loopy graphs, loopy BP is an approximation — may not converge, but works well in practice for error-correcting codes and image segmentation.`,
      `HMM is a Bayesian network over a sequence. Hidden Markov chain s₁,...,sT with transition p(sₜ|sₜ₋₁). Observations xₜ ~ p(xₜ|sₜ). Forward-backward algorithm (BP on the chain): O(T·K²) to compute all marginals. Viterbi (max-product): most likely state sequence. Baum-Welch (EM for HMMs): parameter learning. These three algorithms are the core HMM toolkit.`,
      `Exact inference in general BNs is #P-hard. The junction tree algorithm handles tractable cases: moralise → triangulate → build junction tree → run BP on the tree. Complexity is exponential in treewidth. Treewidth ≤ ~20 is usually feasible. Real-world networks often have higher treewidth, making exact inference impossible and forcing approximations (loopy BP, VI, MCMC).`,
      `Why PGMs lost to deep learning for perception: feature engineering burden (you had to hand-specify variables and structure), inference costs (exact inference is exponential in treewidth), and poor scalability compared to GPU-friendly neural networks. Why PGMs are still used: auditable conditional independence structure, structured prediction (linear-chain CRFs for NER are still competitive in low-resource settings), domain knowledge encoding, and causal analysis where the graph topology has scientific meaning.`,
      `CRFs (Conditional Random Fields) are the discriminative version of MRFs for structured prediction: p(Y|X) = (1/Z(X)) Πc ψc(Yc,X). The normaliser Z(X) conditions on input X, making it tractable for linear-chain CRFs. CRFs outperform generative HMMs for sequence labelling when input features are complex (text features in NER). Largely replaced by BERT fine-tuning for most NLP tasks, but remain relevant in low-resource settings and when structured output guarantees are required.`,
    ],
    checkQuestions: [
      {
        q: 'In a Bayesian network X → Z ← Y, are X and Y marginally independent? Are they independent given Z?',
        a: 'X → Z ← Y is a v-structure (collider at Z). Marginally: X and Y are independent (no active path between X and Y when Z is not observed — the collider Z blocks the path). P(X,Y) = P(X)P(Y). Conditionally given Z: conditioning on the collider Z opens the path X → Z ← Y — X and Y become dependent given Z. This is Berkson\'s paradox (collider bias, selection bias). Example: Talent (X) and Beauty (Y) are independent in the population. Among movie stars (Z = "hired by Hollywood", which requires either high talent or high beauty), talent and beauty become negatively correlated — knowing a movie star has low talent increases our belief they must be highly beautiful to explain their career. Practical implication: in causal analysis, never condition on a collider unless you explicitly account for the induced dependence.',
      },
      {
        q: 'What is the treewidth of a graph and why does it determine inference complexity in PGMs?',
        a: 'Treewidth is a graph property that measures "how close to a tree" a graph is. A tree has treewidth 1. The junction tree algorithm converts a graph into a tree of cliques (junction tree) where the largest clique has size (treewidth + 1). Inference on the junction tree is exact via belief propagation, running in time O(K^{tw+1} × nodes) where K is the state space size and tw is the treewidth. For tw = 1 (trees): inference is O(K² × n) — tractable. For tw = 20, K = 3 (binary-ish states): O(3^{21}) per node — intractable. Many real-world graphical models (social networks, medical diagnosis networks) have high treewidth, making exact inference #P-hard. This is the fundamental limitation of PGMs: the expressiveness of rich conditional independence structure comes at the cost of exponential inference complexity in general graphs. Modern approaches: loopy belief propagation (approximate, fast, widely used in error-correcting codes and computer vision), MCMC on the graph, variational inference with factorised approximations.',
      },
      {
        q: 'You want to use an HMM for anomaly detection in a time series of server metrics. What are the failure modes and how would you address them?',
        a: 'Core HMM assumptions and their failure modes: (1) Markov assumption: p(sₜ|s₁,...,sₜ₋₁) = p(sₜ|sₜ₋₁). Real server metrics often have long-range dependencies (daily seasonality over 1440 minutes, weekly patterns) that a first-order Markov chain cannot capture. Fix: higher-order HMM (k-th order: condition on sₜ₋₁,...,sₜ₋ₖ) or add time-of-day as a feature in the emission distribution. (2) Stationarity: the transition matrix A and emission parameters are fixed across time. Server behaviour changes (deployments, traffic spikes, configuration changes). Fix: sliding window re-estimation of parameters; online EM for HMMs. (3) Emission distribution mismatch: standard HMMs use Gaussian or discrete emissions. If metrics are multivariate and have non-Gaussian tails (e.g., latency distributions are heavy-tailed log-normal), the emission model is misspecified. Use mixture-of-Gaussians emissions or Student-t. (4) Number of hidden states K must be specified: if K is too small, the model cannot distinguish anomaly from unusual-but-normal states. Too large: overfit. Use model selection (BIC, held-out log-likelihood) to choose K. (5) Anomaly definition ambiguity: HMMs assign probabilities to sequences, not events. Anomaly = low p(x₁,...,xT) under the model, but the model may not have learned what constitutes a real anomaly vs. a novel-but-normal pattern if training data is contaminated.',
      },
    ],
    takeaway: `The collider (v-structure) is the most interview-critical concept in PGMs: conditioning on a collider Z opens the path between its parents X and Y, creating a dependency that did not exist marginally — Berkson's paradox. This is the mechanism behind selection bias in observational studies (conditioning on "accepted to the study" induces spurious correlations). Treewidth determines inference complexity: exact inference is tractable only for low-treewidth graphs (trees, polytrees), and the exponential cost in treewidth is why PGMs lost to neural networks for perception tasks — but remain the right tool when conditional independence structure must be explicitly represented and auditable.`,
  },
]

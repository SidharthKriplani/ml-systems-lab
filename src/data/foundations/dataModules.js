export const DATA_MODULES = [
  {
    id: 'data_quality_audit',
    title: 'Data Quality Audit',
    subtitle: `Understand what makes data "dirty" and why auditing before modeling is non-negotiable.`,
    difficulty: 'foundational',
    estimatedMin: 35,
    tags: ['data quality', 'profiling', 'missing values', 'outliers'],
    summary: `The problem with dirty data is not that errors exist — it is that powerful models learn from them confidently. A gradient-boosted tree trained on a dataset where age = -3 for some rows will learn that negative ages predict something, training metrics will look fine, and the model will be deployed before anyone notices.

A single malformed row can silently flip a column from int64 to object, causing the entire column to be dropped. A referential integrity failure after a table join causes rows to silently disappear — the pipeline reports success while 120,000 training examples vanish. Data profiling — per-column statistics, null rates, referential integrity checks, anomaly flags — exposes these problems before training. Without it, you are debugging confident wrong predictions in production rather than catching data errors in development, where the cost is a fraction of the incident response.`,
    keyPoints: [
      `**A null in an "income" field either means the person declined to answer (informative missingness — the null itself is signal) or the field was never collected for that cohort (structural gap — the null carries no information).** Treating both the same destroys one of these signals. Determine the mechanism before choosing the treatment.`,
      `**Duplicates in a dataset where the same row lands in both train and test inflate test-set accuracy directly.** The model memorizes the duplicate training rows, then "predicts" them correctly in the test set. Development metrics look great; production performance falls immediately because real production data has no training duplicates.`,
      `**Before clipping or removing an outlier, determine what process generated it.** A $50,000 transaction in a fraud dataset is an outlier by any statistical measure. Clipping it to the 99th percentile destroys the signal the model most needs — the rare high-value fraud pattern that distinguishes this problem from noise.`,
      `**A column stored as strings that contains mostly numeric values will be silently dropped or error-out in most ML libraries.** A single row containing "N/A" in an otherwise numeric column flips the entire column type at load time. This produces no visible error — the column simply disappears from the feature matrix.`,
      `**Negative ages, future timestamps, and 7-digit zip codes are impossible values — they violate hard domain constraints regardless of statistical context.** They should be nullified and routed through the missing-value pipeline, never imputed. Imputing a -3 age with the column mean propagates the error as if it were real information.`,
      `**Data profiling is not a one-time setup step.** Distributions shift as new cohorts are onboarded, upstream systems change, and business events occur. A column with 2% nulls in January that has 18% nulls in June signals a structural change in data collection — the model trained in January will degrade on that feature when deployed against June data.`,
      `**"Garbage in, garbage out" understates the danger.** With a powerful model, the correct framing is "garbage in, confident garbage out." The model assigns high predicted probabilities to wrong outcomes rather than flagging uncertainty. The error is invisible until ground-truth labels arrive.`,
      `**Referential integrity failures — foreign key values in one table that have no match in the joined table — cause silent row loss after joins. 120,000 rows can disappear and the pipeline reports no error.** Those missing rows are almost never a random sample: they are typically a specific cohort (older customers, a different region) that becomes unrepresented in training.`,
      `**A null rate above ~40% in a single feature means the model learns from that feature on less than 60% of rows.** Interaction terms involving that feature are estimated from an incomplete subset. Whether those 40% missing values share a common cause is the question to answer before deciding whether to include the feature at all.`,
      `**Profile the test set separately from the train set, then compare distributions.** A column with 2% nulls in train and 18% nulls in test signals a structural change between data collection periods. The deployed model trained on the low-null version will encounter the high-null version and behave differently from what evaluation predicted.`,
    ],
    takeaway: `Models are confident even when trained on garbage — training metrics cannot distinguish signal from well-learned noise. Data quality failures surface as production incidents, not training errors. Catch them in profiling before training, not in debugging after deployment.`,
    checkQuestions: [
      {
        q: `A colleague argues that outlier rows should simply be removed before training to keep the model clean. What breaks if you follow this advice blindly on a fraud detection dataset?`,
        a: `Fraud transactions are inherently outliers — they are rare, high-value, and anomalous by definition. Removing outliers before training a fraud model would systematically eliminate the positive class, leaving you with a model trained almost entirely on legitimate transactions. The model would learn to classify everything as non-fraud and achieve 99%+ accuracy on a dataset where fraud is 1% — while catching zero fraud cases. Outlier removal requires knowing why a value is extreme, not just that it is extreme.`,
      },
      {
        q: `You join two tables on a customer ID and your training set shrinks from 500,000 rows to 380,000 without any error. What likely happened and why does it matter for your model?`,
        a: `A left join or inner join dropped 120,000 rows because those customer IDs had no match in the second table. This is a referential integrity failure — the IDs exist in one system but not the other, likely because of different data collection pipelines or time windows. This matters enormously: the 120,000 dropped rows are almost certainly not a random sample. They might be older customers, customers from a different region, or customers who churned — exactly the population where your model's predictions matter most. The model is trained on a biased subset and will generalize poorly to the full population.`,
      },
      {
        q: `What is the difference between an outlier and an impossible value, and why should they be handled differently?`,
        a: `An outlier is a value that is statistically extreme but potentially valid — a transaction of $50,000 is unusual but can happen. An impossible value is one that violates a hard constraint regardless of context — a person cannot be -3 years old, a percentage cannot be 150. Outliers require domain judgment: they may be signal (rare fraud) or noise (entry errors). Impossible values are always errors and should be nullified before any other processing. Treating them differently matters because imputing an impossible value (e.g., replacing -3 age with the mean) propagates the error into the training data as if it were real information.`,
      },
      {
        q: `You run a data profile on your training set in January and the model performs well. You retrain in June without re-profiling and performance drops. What data quality issue is most likely responsible?`,
        a: `Distribution shift between the January and June data — a data quality issue that profiling would have caught. Common causes: a new data source was added (changing the null rate or value range of a feature), a upstream system changed its encoding (e.g., a categorical field now uses different labels), or a business event changed the real-world distribution (a pricing change that shifted transaction amounts). Without profiling at retraining time, the pipeline reports no error but the model is now trained on a different distribution than the one it was originally validated on.`,
      },
      {
        q: `A column in your dataset has 55% null values. A colleague says to impute with the median. What should you do before accepting that advice?`,
        a: `First, determine the mechanism of missingness: why are 55% of values missing? If the nulls are MNAR (Missing Not At Random) — e.g., patients with the most severe condition are the ones who didn't get the lab test recorded — then imputing with the median fills in values that are systematically wrong for the highest-risk group, introducing bias. Second, consider whether the column is worth including at all: a feature observed for only 45% of rows will create interaction effects that are poorly estimated. Third, check whether the missing rate in the test set matches training (55%) — if it doesn't, there is a structural data collection difference. Imputing with the median is only appropriate after these questions are answered.`,
      },
    ],
  },
  {
    id: 'missing_value_handling',
    title: 'Missing Value Handling',
    subtitle: `The mechanism of missingness — MCAR, MAR, MNAR — determines the right treatment, not the null rate alone.`,
    difficulty: 'foundational',
    estimatedMin: 40,
    tags: ['missing data', 'imputation', 'MCAR', 'MAR', 'MNAR', 'MICE'],
    summary: `A dataset with missing values has a problem before any model runs: applying the wrong treatment trains the model on fiction. The right treatment depends entirely on WHY values are missing, not just that they are. MCAR (Missing Completely At Random) means missingness is independent of everything — a sensor randomly dropping packets. You can drop rows or impute with minimal bias. MAR (Missing At Random) means missingness depends on observed variables — a patient's income is more likely missing if they are uninsured, but income itself is not the cause. Model-based imputation (MICE, KNN) recovers accurate estimates. MNAR (Missing Not At Random) means the missing value itself predicts its own absence — severely depressed patients skip the depression survey, patients with the worst lab results are the ones whose results are not recorded. Any imputation fills in systematically wrong values for the highest-risk group, and no statistical method fixes this without modeling the missingness mechanism directly. Diagnosing the type precedes choosing the treatment.`,
    keyPoints: [
      `**MCAR is the least common mechanism in real data.** Most production missingness is MAR or MNAR. Mean imputation on MAR data is biased because it ignores predictive structure in other columns. Mean imputation on MNAR data is worse — it fills in an optimistic value for the highest-risk cases, which is exactly the group where wrong predictions cause the most harm.`,
      `**MNAR failure mode in practice: if patients with the worst outcomes are most likely to have a lab value missing, imputing with the column mean fills in a near-average value for the highest-risk group.** The model trains on data where high-risk patients look average, learns low-risk predictions for them, and systematically misses the people who most need intervention.`,
      `**Add a binary missingness indicator alongside any imputation.** The indicator captures the signal that "this field was absent" — which is itself predictive information when the missingness is informative (MNAR or MAR). A model that sees both the imputed value and the indicator can learn from the pattern of absence. A model with only the imputed value loses that signal entirely.`,
      `**MICE trains a regression model for each column with missing values using all other columns as predictors, then iterates until imputed values converge.** This approach is significantly more accurate than mean imputation for MAR data because it exploits the correlation structure. The tradeoff: MICE requires managing a set of fitted models as pipeline state — every serving-time imputation must use the same fitted models from training.`,
      `**KNN imputation finds the k most similar rows by Euclidean distance on non-missing features and averages their values.** It respects local distribution structure rather than global means, which is useful when the relationship between features is non-linear. The constraint: KNN is O(n²) and requires feature scaling before distance computation, making it expensive for large datasets.`,
      `**Dropping rows with missing values is only valid if those rows are MCAR and the fraction dropped is small (under ~5%).** Drop 20% of rows and you have almost certainly removed a non-random subset — patients who skipped a test, customers who declined a survey question, users on a specific platform. The training set is now biased against exactly the production population where the model matters most.`,
      `**Fit all imputers on the training set only, then apply the fitted parameters to train, val, and test.** Fitting on the full dataset lets test-set statistics (the column mean, the KNN neighbor structure) contaminate the imputer's parameters — the training representation now reflects test-set values, producing optimistic metrics that collapse in production.`,
      `**Above ~50% missingness, reconsider whether imputation makes sense at all.** A model learning from 50% of rows on one feature and 100% on another produces poorly estimated interaction terms for that feature. Sometimes dropping the feature and creating a single "feature was missing" binary predictor captures the signal more honestly than imputing half the values.`,
      `**Mode imputation for categoricals inflates the most common category.** If one category represents 70% of observed values, mode imputation assigns that value to every null — artificially boosting its frequency in the training distribution and suppressing signal from the actual distribution of the missing cases.`,
      `**Detect likely MNAR by regressing the missingness indicator on all observed features.** If no observed feature predicts missingness, MNAR is the leading suspect — something unobserved is driving both the missing value and the outcome. Confirm with domain knowledge: does it make sense that the value would be absent precisely when it is most extreme?`,
    ],
    takeaway: `The mechanism of missingness — not the missing rate — determines the right treatment. Imputing MNAR data systematically fills in the wrong values for the cases where your model is most consequential. Choosing a method before diagnosing the mechanism is a modeling error, not a statistical one.`,
    checkQuestions: [
      {
        q: `You are building a model to predict hospital readmission. A lab test result is missing for 30% of patients. A colleague imputes with the median. What is wrong with this approach and what would you do instead?`,
        a: `Lab tests are typically ordered when a clinician suspects a problem — meaning the test is more likely to be missing when the patient appears healthy, and present when the clinician is concerned. This is MNAR: the probability of the value being missing depends on the value itself (patients with extreme lab values get tested; patients near normal do not). Imputing with the median assigns an "average" lab value to patients who were never tested, which may be systematically wrong for the sickest patients. The right approach is to add a binary "was this test ordered" indicator feature, which captures the clinical signal of missingness. Model-based imputation from other covariates can also be used, but only after acknowledging the MNAR risk.`,
      },
      {
        q: `Why is it data leakage to fit a mean imputer on the full dataset (train + test) before splitting?`,
        a: `When you compute the column mean on the full dataset, that mean is influenced by the test-set values. When you then impute missing values in the training set using that mean, the training set's imputed values contain information derived from the test set. This means the model is indirectly "seeing" test-set statistics during training, which inflates cross-validation and test-set accuracy. In production, you will never have access to future data when computing your imputer — so fitting on the full dataset creates an unrealistic and optimistic evaluation. The rule is: fit all preprocessing transformers (scalers, imputers, encoders) on the training fold only.`,
      },
      {
        q: `What is the difference between mean imputation and MICE, and when does the difference matter most?`,
        a: `Mean imputation replaces all missing values in a column with that column's mean — it is fast and simple but ignores all relationships between columns. MICE treats imputation as a prediction problem: for each column with missing values, it trains a regression model using all other columns as features, imputes the missing values with those predictions, then iterates until convergence. The difference matters most when features are correlated. If missing income values are predictable from education level, employment status, and age, MICE will produce accurate imputations while mean imputation will produce the overall average regardless of these predictors. For MCAR data with low missingness rates, the difference is small. For MAR data with correlated features and moderate missingness (10-30%), MICE can dramatically improve imputation accuracy.`,
      },
      {
        q: `A model trained with complete-case analysis (dropping all rows with any null) achieves 92% accuracy. When you deploy it, accuracy drops to 84%. What is the most likely explanation?`,
        a: `The dropped rows were not MCAR — they were systematically different from the complete cases. The model was trained on a biased subset (rows where all features happened to be observed) and tuned its decision boundaries to that subset. In production, it encounters the full population including the types of rows that were dropped during training. The 8-point accuracy gap reflects the distribution mismatch between the training population (complete cases only) and the production population (everyone). This is the core danger of complete-case analysis: it is only valid when missing data is MCAR, which is rarely true in practice.`,
      },
    ],
  },
  {
    id: 'feature_engineering',
    title: 'Feature Engineering',
    subtitle: `Transform raw columns into representations that expose the signal a model can actually learn from.`,
    difficulty: 'foundational',
    estimatedMin: 45,
    tags: ['feature engineering', 'transformations', 'cyclical encoding', 'interaction terms', 'log transform'],
    summary: `Raw features are almost never the right inputs, and presenting them as-is forces the model to either rediscover structure that domain knowledge already encodes or fail entirely. A raw hour-of-day integer places hour 23 and hour 0 at maximum distance (23 apart), when in reality they are one hour apart on a circular clock. A linear model trained on right-skewed income data devotes its coefficient's explanatory power to distinguishing billionaires while ignoring variation among typical earners. Neither the model nor more training data solves these representation problems — the wrong geometry prevents the model from finding the relationship at all. Feature engineering creates the geometric structure the model needs: log transformations compress skew, sin/cos encoding wraps cyclical features into adjacent coordinates, interaction terms expose joint effects that neither feature reveals alone.

The tradeoff is that engineered features encode assumptions. A domain-correct assumption can be worth millions of training rows. A wrong one teaches the model a relationship that does not exist.`,
    keyPoints: [
      `**Log transformation compresses right-skewed distributions, making linear relationships valid in regions the raw scale obscures.** It works only for strictly positive values — log(0) is undefined. The standard workaround is log(x + 1), which maps 0 → 0 and preserves the compression. For linear models and neural networks, this transformation is often the difference between convergence and failure to learn the relationship at all.`,
      `**Sin/cos encoding for cyclical features solves a geometric problem.** Hour 23 and hour 0 are one hour apart on the clock, but raw integer encoding places them 23 units apart — a distance that prevents any linear model from learning that they are adjacent. Encoding as sin(2π·hour/24) and cos(2π·hour/24) maps each hour to a point on the unit circle. Hour 23 and hour 0 end up geometrically close. The same principle applies to day-of-week (period 7), month (period 12), and compass degrees (period 360).`,
      `**Interaction terms expose joint effects that neither feature reveals alone.** High-value international transactions are a fraud signal; high-value domestic transactions and low-value international transactions may not be. The feature "transaction_amount × is_international" captures exactly the combination. Tree-based models can discover this split on their own; linear models cannot unless the interaction is made explicit. The danger: a 100-feature dataset has ~5,000 possible pairwise interactions, most of which are noise. Creating all of them dramatically increases overfitting risk.`,
      `**Tree-based models are largely immune to the representational problems that motivate feature engineering.** A random forest finds the same optimal split threshold whether income is in raw dollars or log-dollars — the relative ordering is preserved. Log transforms, scaling, and most single-feature transformations provide no lift for trees. They matter significantly for linear models, SVMs, and neural networks with linear layers.`,
      `**Ratio features normalize for scale differences between entities without requiring the model to learn the normalization.** Click-through rate (clicks / impressions) captures user engagement independent of volume. Revenue per customer captures value independent of customer count. The model would need to infer these ratios from correlated raw features — an unnecessary burden that domain knowledge can eliminate directly.`,
      `**Temporal lag features — yesterday's value, a 7-day rolling mean — are essential for time series but carry a severe leakage risk.** The feature must be computed using only values observable at prediction time. "7-day rolling mean" computed using the label day's value includes future information. Constructing temporal features correctly requires a strict temporal join: the feature value at each training row must reflect only data available before the label timestamp.`,
      `**Domain features encode what a model would need enormous data to discover on its own. "Days since last purchase" in a churn model captures the recency signal a sequence model would require millions of rows to extract from raw event logs.** A domain expert can create this feature in an hour; learning it from raw timestamps requires a deep architecture and a very large dataset — and may still fail on sparse users.`,
      `**Engineered features have diminishing returns.** The first few high-signal features provide most of the lift. Additional features increase training cost, memory, and overfitting risk without proportional accuracy gain. The test: add a feature, measure permutation importance. If it does not move, remove it — complexity without benefit is always a liability.`,
      `**Binning continuous features captures non-monotone relationships (U-shaped age-risk curves, for example), but at the cost of information loss — all values within a bin are treated identically.** For tree models, this is almost never the right choice: the tree finds its own optimal splits and will outperform any manual binning. For linear models, binning is a reasonable alternative when the relationship is clearly non-linear.`,
      `**The most valuable feature engineering happens before data collection, not after.** Designing a system to record time-between-events rather than just event counts creates a feature worth more than any post-hoc transformation. Retrospectively engineering features from raw logs has a ceiling determined by what was recorded. What was not captured cannot be recovered.`,
    ],
    takeaway: `Models learn from the geometry of the feature space you give them. The same information in the wrong representation can be entirely invisible to the model — not learned slowly, but never learned at all. Encoding choices are structural decisions, not preprocessing details.`,
    checkQuestions: [
      {
        q: `You have a "time_of_day" feature encoded as an integer from 0 to 23. Your model performs poorly on predictions for late-night events. What is the encoding problem and how do you fix it?`,
        a: `The raw integer encoding treats hour 23 and hour 0 as maximally different (23 apart), when in reality they are 1 hour apart on the circular clock. A model using this encoding cannot learn that midnight behavior resembles 11pm behavior. The fix is sin/cos encoding: create two new features, sin(2π·hour/24) and cos(2π·hour/24). These two features together encode each hour as a point on the unit circle, so that hour 23 and hour 0 are geometrically close. This is essential for any cyclical feature: day-of-week (period 7), month-of-year (period 12), and degrees (period 360) all need the same treatment.`,
      },
      {
        q: `Why does log-transforming an income feature help a linear regression model but not a random forest?`,
        a: `Linear regression assumes a linear relationship between each feature and the target. When income is right-skewed (a few extremely high earners), the linear model devotes most of its coefficient's explanatory power to distinguishing the extreme values, effectively ignoring variation among typical earners. Log-transforming compresses the upper tail and spreads the distribution more evenly, making the linear relationship more valid. A random forest, by contrast, splits on thresholds: it can learn "if income > $50,000 go left, else go right" regardless of the scale or distribution. Monotone transformations like log do not change which threshold is the optimal split — the forest finds equivalent splits either way. Log transform matters for models that assume distributional properties (linear models, SVMs, neural nets with linear layers) and is largely irrelevant for tree-based models.`,
      },
      {
        q: `You are building a fraud detection model and add an interaction term: transaction_amount × is_international. What does this feature capture that neither original feature captures alone?`,
        a: `The interaction term captures the combined effect of high-value international transactions — the pattern that high amounts are suspicious specifically when the transaction is international, not when it is domestic. Individually, transaction_amount tells the model about scale and is_international tells the model about geography. Neither alone tells the model that their combination is the key fraud signal. The interaction term is the product of these two features, which will be large (high amount × international = 1) only when both conditions hold. A linear model cannot discover this relationship from the original features; it can only learn additive effects. For tree models, the interaction is learnable without an explicit feature (the tree can split on amount within the international subset), but the explicit feature can still speed up training.`,
      },
      {
        q: `A data scientist creates 200 interaction features from a 20-feature dataset and reports improved validation accuracy. What risk does this improvement mask and how would you verify it?`,
        a: `With 200 additional features (most of which are noise), the model has dramatically more capacity to overfit the training and validation sets. If the validation set was used to tune any hyperparameters, or if features were selected based on validation performance, the reported improvement is optimistic. To verify, you need a held-out test set that was never touched during feature construction or selection. You should also check training vs. validation accuracy gap — a much larger gap after adding interaction features signals overfitting. The principled approach is to select interaction features using domain knowledge or statistical tests on the training fold only, before any validation evaluation.`,
      },
    ],
  },
  {
    id: 'categorical_encoding',
    title: 'Categorical Encoding',
    subtitle: `Convert categories into numbers without lying to your model about distance, order, or information from the target.`,
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['encoding', 'one-hot', 'target encoding', 'ordinal', 'cardinality', 'embeddings'],
    summary: `The problem with categorical encoding is that every choice makes an implicit claim about category structure, and the wrong claim actively misleads the model. Ordinal encoding (red=0, blue=1, green=2) asserts that blue is twice as different from green as it is from red — a claim with no basis for unordered categories. A linear model trained on ordinal-encoded cities learns a spurious linear trend across an arbitrary integer ordering. One-hot encoding avoids false distance but explodes to hundreds of columns at high cardinality, producing features so sparse that each column has fewer than 10 positive values in a 10,000-row dataset — a regime that causes overfitting and training instability. Target encoding is compact but creates data leakage if computed before the split: the encoded value for each training row reflects its own label, giving the model the answer during training. The right encoding is determined by the cardinality of the feature, the model type consuming it, and whether a genuine distance or ordering relationship exists.`,
    keyPoints: [
      `**Ordinal encoding on unordered categories introduces spurious distance that the model cannot ignore.** A linear regression learns a single coefficient for the city feature, scaled by the integer. Cities with adjacent integers are treated as similar; cities with large integers receive disproportionate weight. The model fits a meaningless trend across an arbitrary ordering. Trees are less affected because they split on thresholds rather than linear relationships, but the spurious encoding still limits what the tree can learn.`,
      `**One-hot encoding at high cardinality creates a sparsity problem. 1,000 cities produce 999 binary columns.** In a 10,000-row dataset, each column has approximately 10 positive values — the model sees a signal for each city on only 10 training examples. Trees overfit to these sparse patterns; linear models assign unstable coefficients. The threshold for switching strategies is around 50 unique values — above that, target encoding, frequency encoding, or embeddings are better choices.`,
      `**Target encoding leakage works through the training row's own label.** When you compute the per-category mean target across the full dataset, each row's encoded value is influenced by its own outcome. For a category with one row, the encoded value is exactly the target — the model receives a direct copy of what it is predicting. Fix: leave-one-out encoding computes the category mean excluding the current row. Cross-validation-based encoding computes the mean from the training folds only, applying it to the validation fold.`,
      `**Frequency encoding replaces each category with its rate of appearance in the training set.** It handles high cardinality without leakage risk — no target values are used. Its structural weakness: two categories that happen to appear at the same rate receive identical encodings even when they are semantically unrelated. The model cannot distinguish them.`,
      `**Unseen categories at serving time expose the fragility of each encoding.** One-hot encoding silently sets all indicator columns to zero — producing an all-zero row the model may never have trained on. Target encoders have no stored mean for the new category and typically fall back to the global mean, losing per-category signal. Hash encoding always handles new categories by mapping them to a bucket — the most naturally robust approach, at the cost of collisions between semantically unrelated categories.`,
      `**Embedding layers learn dense vector representations from data rather than relying on any hand-crafted distance assumption.** Two merchant IDs that behave identically in the data will end up geometrically close in embedding space, regardless of how different their raw IDs are. This is the only encoding that can capture semantic similarity for high-cardinality features like product IDs or user IDs. The tradeoff: categories seen rarely will have poorly trained embeddings — the few gradient updates they receive may not converge.`,
      `**The dummy variable trap: k one-hot columns sum exactly to 1 for every row — perfect multicollinearity.** A linear model with an intercept has an exact linear dependency between the intercept and the k columns. The result is that coefficients are unidentifiable; remove one column (drop_first=True in pandas) to break the dependency. Tree models are unaffected because they do not estimate coefficients.`,
      `**Binary encoding uses the binary representation of each category's integer index — log2(k) columns instead of k.** For 1,000 cities: 10 columns instead of 999. This dramatically reduces dimensionality. The tradeoff is that the bit-string representation imposes arbitrary relationships between categories — city 5 (binary: 00101) and city 4 (binary: 00100) differ by one bit, but that bit has no semantic meaning.`,
      `**Hash encoding maps each category to a bucket via a hash function.** The vocabulary is fixed at encoding time (the number of buckets), it handles unseen categories automatically, and it requires no stored mapping. The failure mode is hash collisions: two different categories land in the same bucket and the model conflates them. With enough buckets relative to categories, collisions are rare enough to be acceptable.`,
      `**Model type and encoding interact strongly.** Trees find their own optimal split thresholds on any encoding, so ordinal encoding harms them less — the tree learns "if code > 25, go left" without caring that the ordering is arbitrary. Linear models treat the integer as a quantitative predictor and multiply it by a coefficient, making ordinal encoding on unordered categories a direct source of bias. Neural networks benefit most from embedding layers for high-cardinality features because one-hot input to dense layers is equivalent to an embedding lookup with redundant computation.`,
    ],
    takeaway: `Every encoding encodes an assumption about category structure. Ordinal encoding asserts that distance between categories is real. One-hot asserts equal distance but creates sparsity at scale. Target encoding is compact but leaks labels without cross-validation. The wrong encoding for the wrong feature is not a preprocessing detail — it is a false claim that the model will learn as if it were true.`,
    checkQuestions: [
      {
        q: `You apply ordinal encoding to a "city" feature with 50 unique values and train a linear regression. What exactly goes wrong?`,
        a: `Ordinal encoding assigns integers 0–49 to the 50 cities in an arbitrary order. Linear regression learns a single coefficient for the city feature, which it multiplies by the integer to get a contribution to the prediction. This means the model is forced to assume that "city 49" has exactly 49 times the effect of "city 1" — a constraint with no basis in reality. Cities that happen to receive adjacent integers are treated as similar; cities with high integers are given disproportionate weight. The model fits a meaningless linear trend across an arbitrary ordering. One-hot encoding would instead give the model an independent coefficient for each city, removing the false linearity assumption. Tree models are immune to this problem because they learn threshold splits (city_code > 25?) rather than linear relationships.`,
      },
      {
        q: `Walk through exactly why target encoding without cross-validation causes data leakage.`,
        a: `When you compute target encoding by taking the mean target for all rows with category X, you include the row you are about to use as a training example. That row's own target value contributes to the encoded feature value for that row. In the extreme case of a category with only one row, the encoded value is exactly the target value — the model receives a feature that is a direct copy of what it is trying to predict, making training accuracy artificially perfect. Even with many rows per category, each row's encoded value reflects its own outcome slightly, inflating apparent in-sample performance. The fix is leave-one-out or fold-based encoding: when computing the mean target for a training row, exclude that row's own target from the mean. During inference, use the mean over all training rows with that category.`,
      },
      {
        q: `At inference time, your model receives a city it has never seen in training. How does each encoding strategy handle this, and which is most robust?`,
        a: `One-hot encoding sets all city indicator columns to 0 — equivalent to a new implicit "other" category. This is handled silently but may produce unpredictable behavior if the model never learned to act on all-zero city columns. Ordinal encoding has no valid integer for an unseen city — you must either assign it the "unknown" integer or raise an error. Target encoding has no mean target for an unseen city — the typical fallback is the global mean target, which is reasonable but loses the per-category signal. Hash encoding maps the new city to a bucket automatically — the hash function always produces a valid bucket index, making it the most naturally robust to new categories. Embedding layers require a dedicated "unknown" token or a fallback to the zero vector. In production systems, maintaining an explicit "other" or "unknown" category in the vocabulary and training on it is the most robust general approach.`,
      },
      {
        q: `A feature has 5,000 unique merchant IDs and you are training a neural network. Why is one-hot encoding a bad choice here, and what should you use instead?`,
        a: `One-hot encoding 5,000 merchant IDs produces 4,999 binary columns (dropping one for the intercept). For a dataset with, say, 100,000 rows, each column has on average 20 non-zero values — an extremely sparse input. Neural networks learn poorly from sparse inputs because gradient flow through nearly-all-zero feature vectors is weak, weight updates are small, and most neurons learn nothing from most examples. The resulting embedding matrix (first layer weights × 5,000 input columns) has 5,000 × d parameters where d is the hidden dimension — orders of magnitude more than needed. An embedding layer is the right choice: it directly looks up a learned dense vector for each merchant ID without constructing a 5,000-dimensional sparse input, uses only 5,000 × d parameters (same count), and can capture semantic similarity between merchants that one-hot encoding cannot represent.`,
      },
    ],
  },
  {
    id: 'feature_scaling',
    title: 'Feature Scaling',
    subtitle: `Ensure features are on comparable scales so that gradient descent, distance metrics, and regularization work correctly.`,
    difficulty: 'foundational',
    estimatedMin: 35,
    tags: ['scaling', 'standardization', 'normalization', 'robust scaling', 'data leakage'],
    summary: `The problem with unscaled features is that most ML algorithms are sensitive to magnitude in ways that have nothing to do with predictive value.

Gradient descent steps are proportional to gradient magnitude — a feature on [0, 100,000] produces gradients 100,000 times larger than a feature on [0, 1], causing the optimizer to devote nearly all of its updates to the large-scale feature and ignore the small-scale one. K-Means and KNN compute Euclidean distance, so annual income in dollars is 1,000 times more influential than age in years — the clustering ignores age entirely without scaling. L1 and L2 regularization penalize coefficient magnitude, but when features live on different scales, the coefficients differ by a factor of that scale even for features with equal effect size, producing a biased penalty that shrinks small-scale features more aggressively than large-scale ones. Tree models are the exception: they split on thresholds, and the relative ordering of values is unchanged by scaling, so trees gain nothing from it. The scaling choice (standardization, min-max, robust) depends on whether outliers are present and meaningful — min-max is destroyed by a single extreme value.`,
    keyPoints: [
      `**Fit the scaler on the training set only, then apply the fitted parameters to train, val, and test.** Fitting on the full dataset computes statistics that include test-set values. The training transformation then reflects the test distribution — a form of data leakage that produces optimistic metrics which collapse when the model encounters truly unseen data.`,
      `**Min-max normalization is the wrong choice when outliers are present.** A column [1, 2, 3, 4, 10000] normalizes to approximately [0, 0, 0, 0, 1.0]. The outlier owns the entire range and every other value collapses to near zero. The model loses the ability to distinguish between values 1 through 4 — they are all mapped to the same region of feature space.`,
      `**Robust scaling (median + IQR) is designed for datasets where extreme values are real data, not errors.** It maps the median to 0 and the interquartile range to a fixed interval, making it far less sensitive to outliers than standardization. The tradeoff is that it does not guarantee a specific output range — values outside the IQR can extend well beyond [-1, 1].`,
      `**Neural networks are particularly sensitive to input scale because gradient magnitudes propagate back through all layers multiplicatively.** A large-scale input feature produces large activations in the first layer, large gradients flowing backward, and large weight updates — while small-scale features receive near-zero gradient updates. Batch normalization mitigates this at intermediate layers but does not replace proper input scaling.`,
      `**K-Means is entirely determined by Euclidean distance.** A customer dataset with age (range 60) and annual income (range 180,000) assigns income a 3,000-times larger contribution to every pairwise distance calculation. Without scaling, K-Means clusters entirely by income and treats age as invisible. After standardization, each feature contributes equally to distance — both features matter.`,
      `**Regularization penalizes coefficient magnitude.** Feature A on [0, 1] and feature B on [0, 1000] have coefficients that differ by a factor of 1000 for the same effect size. L2 penalty applies to both equally — it shrinks A's coefficient far more aggressively relative to its actual importance. Without scaling, regularization punishes small-scale features and leaves large-scale features undertrained.`,
      `**Tree models split on threshold values: "income > 50000" and "income_scaled > 0.5" identify the identical training partition.** The optimal threshold value shifts with scaling but the optimal split does not. Scaling tree inputs wastes computation and provides zero accuracy benefit.`,
      `**In cross-validation, fit the scaler inside each fold on that fold's training data only, then apply to the validation fold using the training-fold parameters.** Fitting the scaler on the full training set before the CV loop leaks fold-level statistics — the validation fold's distribution influences the scaling parameters used to transform it.`,
      `**When adding new features to an existing deployed model, refit the scaler on the updated training set that includes the new features.** Stale scaling parameters from the original training run no longer reflect the distribution of the new features and will silently distort their values at serving time.`,
    ],
    takeaway: `Gradient descent, Euclidean distance, and regularization all implicitly weight large-scale features over small-scale ones — through optimization dynamics, not through any signal about importance. Scale the inputs to remove this artifact. Fit the scaler on training data only, and refit it inside the cross-validation loop, not outside it.`,
    checkQuestions: [
      {
        q: `You fit a StandardScaler on your entire dataset (train + test combined) before splitting. What exactly is wrong with this?`,
        a: `When you compute the mean and standard deviation over the full dataset, those statistics are influenced by the test-set values. The mean used to center each feature and the standard deviation used to scale it encode information about the test set distribution. When you then transform the training data using these statistics, the training representation reflects the test set. This is data leakage: the model's training environment contains information that would not be available at the time of training in a real deployment. The effect is that your cross-validation and test-set accuracy are optimistically biased — the model has seen, through the scaler, some statistical properties of examples it is supposedly being evaluated on. The correct approach: fit the scaler on the training fold only, then apply the same fitted scaler to both the training fold and the validation/test set.`,
      },
      {
        q: `A K-Means clustering of customer data with features [age (range 20-80), annual_income (range 20,000-200,000)] produces clusters that are entirely separated by income and ignore age. Why does this happen and how do you fix it?`,
        a: `K-Means computes Euclidean distance between data points. The income feature has a range of 180,000 while the age feature has a range of 60. A difference of 1 year in age contributes 1 unit to the Euclidean distance, while a difference of $1,000 in income contributes 1,000 units — and income ranges are in the tens of thousands. Income dominates the distance computation so completely that age is effectively invisible: two customers of the same income but 30 years apart in age look nearly identical to K-Means. The fix is to standardize both features before clustering: after standardization, each feature has std=1, so a 1-std difference in age and a 1-std difference in income contribute equally to the distance calculation. The clusters will then reflect variation in both features.`,
      },
      {
        q: `You are doing 5-fold cross-validation and you fit a MinMaxScaler on the full training set before the CV loop. What is the consequence?`,
        a: `The MinMaxScaler computes the minimum and maximum of each feature across all 5 folds combined. When the CV loop evaluates fold 1 (folds 2-5 as training, fold 1 as validation), the scaler has already been fit using fold 1 values — so fold 1's min/max values contributed to the scaling parameters. The validation fold transformation therefore reflects statistics derived from the validation examples, which constitutes leakage. In practice, this usually has a small effect (the min/max of one fold does not change dramatically from the full dataset), but the correct approach is to fit the scaler inside the CV loop: fit on the training folds, then transform the validation fold using only the training-fold statistics.`,
      },
      {
        q: `Why does applying StandardScaler to the inputs of a random forest not improve performance, while applying it to a logistic regression typically does?`,
        a: `A random forest learns by finding optimal binary splits on individual features: "is feature X > threshold T?" The answer to this question is identical regardless of whether X is in its original scale or has been standardized — the relative ordering of values is preserved, so the same threshold exists (just at a different numeric value). Scaling does not change which split is optimal, only the numeric value of the threshold. A logistic regression, by contrast, learns a weighted sum of features: the prediction is a function of w1·x1 + w2·x2 + ... + wk·xk. If x1 ranges from 0 to 1 and x2 ranges from 0 to 100,000, gradient descent will update w2 much more slowly (large-scale features have smaller gradients relative to their contribution), and regularization will penalize w1 much more aggressively than w2 for the same effect size. Standardization removes this asymmetry, allowing the optimizer to converge faster and regularization to penalize fairly.`,
      },
    ],
  },
  {
    id: 'class_imbalance',
    title: 'Class Imbalance',
    subtitle: `When 99% of examples are one class, accuracy is a lie — learn the techniques that actually work.`,
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['imbalance', 'SMOTE', 'class weights', 'oversampling', 'undersampling', 'threshold moving'],
    summary: `On a dataset where 1% of transactions are fraudulent, a model that predicts "not fraud" for every single input achieves 99% accuracy. Class imbalance is not a statistical curiosity — it is the normal condition for fraud detection, medical diagnosis, and anomaly detection. The first failure is metric-based: reporting accuracy on a skewed dataset tells you nothing about model performance on the minority class. The second failure is architectural: a standard cross-entropy loss assigns equal importance to each example, so the model is optimized 99 times harder on the majority class than the minority class. Fixing imbalance starts by switching to a cost-sensitive metric (precision, recall, F1, AUC-PR) and then choosing the right intervention. Class weighting adjusts the loss, threshold moving adjusts the decision boundary, SMOTE generates synthetic minority examples. Each operates at a different point in the pipeline and has different failure modes — notably, SMOTE applied before the train-test split contaminates the test set with synthetic data and invalidates evaluation.`,
    keyPoints: [
      `**A model predicting the majority class for every example achieves 99% accuracy on a 99:1 dataset.** This is the baseline — the model must beat it on metrics that measure minority class performance, not aggregate accuracy. Report precision, recall, and F1 at minimum. For severe imbalance, report AUC-PR: the precision-recall curve is determined entirely by minority class performance and does not inflate with the majority class count.`,
      `**SMOTE interpolates between existing minority samples in feature space to create synthetic new ones, rather than duplicating rows.** Duplicates provide no new geometric information — the model still sees only the original minority region boundary. Interpolated synthetic samples build a denser minority region, giving the model more decision boundary information to learn from.`,
      `**SMOTE fails when minority and majority classes heavily overlap in feature space.** Interpolating between two minority samples that are surrounded by majority samples generates synthetic points that land in majority territory. The model trains on these as minority examples but at inference time, those feature regions produce majority-class predictions — the synthetic examples are misleading. SMOTE-ENN and SMOTE-Tomek add a cleaning step that removes synthetic samples misclassified by k-NN neighbors.`,
      `**Class weighting is the cleanest approach for most tabular problems. class_weight="balanced" in sklearn scales each class's contribution to the loss by inverse frequency — a class 10 times rarer gets 10 times the loss penalty per misclassification.** No data modification is required, no resampling-before-splitting risk, and it integrates cleanly with cross-validation because no data is added or removed.`,
      `**Threshold moving is a post-training intervention: lower the classification threshold from 0.5 to 0.1 or 0.2 to increase recall at the cost of precision.** It changes the decision rule without touching the trained model or its probability estimates. The right threshold depends on the cost ratio of false negatives to false positives — missing a $5,000 fraud case is not the same as triggering a $50 manual review.`,
      `**Undersampling removes majority class examples to restore balance.** It discards real data, which destroys useful distributional information about the majority class. The cases where it works: the majority class has many near-duplicates and the dataset is large enough that removing majority examples does not push the model into underfitting. Never undersample the test set.`,
      `**Apply SMOTE inside the training fold only, never to the full dataset before splitting.** Synthetic samples generated from the full dataset can be geometrically near real test-set minority examples — the test set is then contaminated with data that is structurally similar to training points. Evaluation on such a test set produces inflated metrics that collapse when the model encounters truly unseen data.`,
      `**Most gradient boosting implementations support sample weights natively.** XGBoost's scale_pos_weight, LightGBM's is_unbalance, and sklearn's sample_weight achieve the same effect as class weighting without any data modification. For tabular data, these are cleaner than SMOTE — no resampling pipeline complexity, no risk of synthetic data leakage.`,
      `**AUC-PR (area under the precision-recall curve) is more informative than AUC-ROC under severe imbalance.** AUC-ROC can reach 0.95 or higher even when the model catches almost no minority examples, because the enormous true negative count dominates the ROC denominator. AUC-PR directly measures the tradeoff between precision and recall on the minority class and is not inflated by the majority class size.`,
      `**Imbalance handling must be driven by the business cost function, not a statistical preference for balanced classes.** The optimal decision threshold is not 0.5 — it is the threshold that minimizes expected cost. Compute expected cost at each operating point and pick the minimum. A fraud model where each missed fraud costs $10,000 and each false positive costs $50 should operate at a very different threshold than one with equal costs.`,
    ],
    takeaway: `The model is not the problem — the evaluation metric and the loss function are. Reporting accuracy on imbalanced data is a measurement failure. The first fix is switching to precision, recall, and AUC-PR. The second is adjusting the loss via class weights or threshold — not necessarily resampling.`,
    checkQuestions: [
      {
        q: `Your fraud model achieves 99.2% accuracy and your colleague is satisfied. What would you check to determine if this model is actually useful?`,
        a: `First, check the baseline: if 99% of transactions are legitimate, predicting "not fraud" for every transaction yields 99% accuracy. The model needs to beat this trivial baseline on metrics that actually capture fraud detection. Check recall (sensitivity): what fraction of actual fraud cases does the model correctly flag? A model with 99.2% accuracy and 0% recall catches no fraud at all. Check precision: of the cases flagged as fraud, what fraction are actually fraudulent? And compute the AUC-PR (area under the precision-recall curve) which directly measures performance on the minority class. If the model has high accuracy but low recall and low AUC-PR, it is effectively a sophisticated version of "always predict not fraud" and is useless for the actual business purpose.`,
      },
      {
        q: `Why is applying SMOTE to the full dataset before splitting into train and test sets invalid?`,
        a: `SMOTE generates synthetic minority samples by interpolating between real minority samples. If you apply it to the full dataset, some of the synthetic samples will be near real test-set minority examples, because the interpolation uses test-set neighbors. The test set then contains synthetic examples (not real data) and is no longer a valid holdout for estimating real-world performance — you are evaluating the model on made-up data points that are structurally similar to what it trained on. The correct procedure: split first into train and test using real data, then apply SMOTE only to the training set. The test set must always contain only real, unmodified examples so that it represents actual production distribution.`,
      },
      {
        q: `Compare class weighting and SMOTE: when would you choose one over the other for a 50:1 imbalanced tabular dataset?`,
        a: `Class weighting is almost always the first choice for tabular data: it requires no data modification, works natively in most model implementations, interacts cleanly with cross-validation (no risk of resampling-before-splitting leakage), and scales to any imbalance ratio without generating data. SMOTE is preferred when the minority class is genuinely underrepresented in feature space — when there are too few minority examples for the model to learn a good decision boundary, not just that the loss function is unbalanced. For a 50:1 ratio with thousands of minority examples, class weighting is sufficient. For a 50:1 ratio with only 50 minority examples total, SMOTE can help by creating a denser minority region for the model to learn from. For deep learning, class weighting is almost always preferred because SMOTE on high-dimensional or mixed-type tabular data is harder to validate.`,
      },
      {
        q: `You lower the classification threshold from 0.5 to 0.2 and your recall increases from 0.6 to 0.9 but precision drops from 0.8 to 0.3. Is this an improvement?`,
        a: `Whether this is an improvement depends entirely on the relative cost of false negatives versus false positives in the deployment context. A higher recall (0.9 vs 0.6) means catching 50% more actual fraud cases — if each fraud case costs $5,000 uncaught, this is highly valuable. The lower precision (0.3 vs 0.8) means more false positives — if each false positive triggers a $50 manual review, the increase in false positives needs to be justified by the cases caught. The decision cannot be made from these numbers alone without knowing the business cost ratio. In fraud detection, where the cost of missing a fraud is typically much higher than the cost of a false alarm, a threshold of 0.2 may well be the right operating point. The correct approach is to compute the expected cost at each threshold and choose the minimum-cost threshold.`,
      },
      {
        q: `What is the SMOTE failure mode when minority and majority classes heavily overlap in feature space?`,
        a: `SMOTE generates synthetic minority samples by interpolating between existing minority samples along the line connecting a sample to one of its k nearest minority neighbors. When minority and majority classes overlap significantly, some minority samples are surrounded by majority samples, and the synthetic points generated by interpolating between minority samples will fall inside the majority-class region. The model trains on these synthetic minority samples as if they are real minority examples, but at inference time, those feature-space regions produce majority-class predictions — the synthetic samples are misleading. This is why SMOTE-ENN and SMOTE-Tomek variants exist: they apply an undersampling step after SMOTE to remove synthetic samples that are misclassified by k-NN, cleaning the decision boundary and removing the cross-class synthetic contamination.`,
      },
    ],
  },
  {
    id: 'data_splits_and_leakage',
    title: 'Data Splits and Leakage',
    subtitle: `Understand why models that look great in development fail in production — and the exact mistakes that cause it.`,
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['train-test split', 'cross-validation', 'data leakage', 'temporal leakage', 'overfitting'],
    summary: `Data leakage is the most common reason a model looks spectacular in development and fails the first week in production. Leakage is any mechanism by which information from the evaluation set reaches the training process — and it fires no error.

The model trains successfully, metrics are excellent, the model ships, and then performance collapses when real data arrives. The failure pattern is always the same: the development environment was too easy because the model had indirect access to information it will never have at deployment time. Common sources: scalers or imputers fitted on the full dataset before splitting (their statistics reflect test-set values, coloring the training transformation); temporal leakage in time-series (a random split trains on next week's data to predict last week's, but in production you never have future information); target-derived features that encode the label directly; group leakage where the same patient's records appear in both train and validation. Each produces an optimistic illusion of generalization that evaporates the moment real data arrives.`,
    keyPoints: [
      `**The test set is a one-time-use instrument.** Every time you look at test-set performance and adjust something — hyperparameters, features, architecture — you have implicitly tuned to it. It no longer measures generalization; it measures how well you optimized to that specific sample. Use a held-out test set strictly once, at the very end, after all decisions are finalized.`,
      `**Temporal leakage in time-series problems: random shuffling trains on next week's data to predict last week's.** In production you never have access to future data at prediction time. A model that uses future information to predict the past achieves accuracy that is impossible to replicate in deployment. The correct split is always temporal: train on earlier data, validate on later data.`,
      `**Preprocessing leakage: fitting a StandardScaler or SimpleImputer on the full dataset before splitting computes statistics (mean, std, column medians) that reflect the test-set distribution.** The training transformation now contains test-set information. The model learns on a representation colored by data it will supposedly be evaluated on. Fit all transformers on the training fold only, then apply the fitted parameters to validation and test.`,
      `**Target encoding leakage: computing per-category mean target across the full dataset means each training row's encoded feature is influenced by its own outcome.** For a category with one row, the encoded value is exactly the target. Leave-one-out encoding computes the category mean excluding the current row. Cross-validation-based encoding computes category means from training folds only, never from the row being encoded.`,
      `**Group leakage occurs when related examples are split across train and validation.** A medical imaging dataset with 20 images per patient — random splitting puts 16 images in train and 4 in validation for the same patient. The model learns patient-specific patterns (scanner calibration, anatomy) during training and "validates" on the same patient. Grouped k-fold keeps all of a given patient's records in the same fold, ensuring validation tests generalization to new patients.`,
      `**Label leakage through derived features: including "claim_paid" as a feature when predicting "will_claim" gives the model the answer during training.** At prediction time — before the claim outcome is known — this feature is unavailable. Anything derived from the outcome variable after it is known must be excluded from the feature set. Apply strict temporal cutoffs: only features observable before the prediction date are valid inputs.`,
      `**Stratified k-fold ensures each fold has the same class distribution as the full dataset.** Without stratification, a random fold in a highly imbalanced dataset may contain zero minority examples — the model trains with no positive examples in some folds and the evaluation is meaningless.`,
      `**Time-series cross-validation with an expanding window: fold 1 trains on months 1–6 and validates on month 7; fold 2 trains on months 1–7 and validates on month 8.** Training data is always temporally earlier than validation data. This correctly simulates the production constraint that prediction always uses only past information.`,
      `**A sudden jump of 10–20 percentage points in accuracy after adding a single feature is a leakage red flag.** Genuine predictive signals rarely produce gains of that magnitude. Before crediting the new feature, verify it cannot be derived from the label — check correlation with the target, check whether it is available at prediction time, and check whether the jump disappears when the feature is added after a proper train-test split.`,
      `**Correct cross-validation order: split into folds, then for each fold, fit all preprocessing on the training portion only, apply fitted transformers to both training and validation, train the model, evaluate.** Preprocessing must execute inside the fold loop, not before it. Running it outside the loop leaks fold-level statistics into every validation step.`,
    ],
    takeaway: `Any information that flows from evaluation examples into training — through preprocessing statistics, feature engineering, or incorrect splits — creates an optimistic illusion of generalization that evaporates when real data arrives. No error fires during training. The model reports excellent metrics and fails in production. The only protection is structural: enforce the split before fitting any transformer, and validate on data the model has never touched.`,
    checkQuestions: [
      {
        q: `You build a model to predict customer churn. You include "support_tickets_after_churn_date" as a feature. The model achieves 98% accuracy. What is the problem?`,
        a: `This is label leakage through a target-derived feature. "Support tickets after churn date" can only be known after the customer has already churned — you are using future information (post-churn behavior) as a feature to predict whether the customer churns. At prediction time (before churn happens), this feature would be unknown. The model achieves 98% accuracy because it has essentially been given the answer. In production, when you try to predict churn for currently active customers, this feature is unavailable (the customer has not yet churned, so there are no post-churn tickets), and the model's accuracy will collapse. The fix is strict temporal cutoffs: only use information available before the prediction date as features.`,
      },
      {
        q: `Why does random train-test splitting fail for time-series forecasting, and what is the correct splitting strategy?`,
        a: `In a time-series problem, the future depends on the past — a model trained to predict sales tomorrow should only use data from today and before. Random splitting shuffles examples across time, creating a situation where a model might be trained on data from March to predict January. In production, January comes before March, so March data would never be available when predicting January. The model learns temporal correlations backward, producing an accuracy estimate that is impossible to achieve in deployment. The correct strategy is a temporal split: train on data from time 0 to T, validate on data from T to T+k. For cross-validation, use an expanding or sliding window: each fold trains on earlier data and evaluates on immediately subsequent data, always maintaining the constraint that training data is older than validation data.`,
      },
      {
        q: `You run 5-fold cross-validation on a medical imaging dataset with 500 patients and 20 images per patient. You get CV accuracy of 94%. You deploy and get 71%. What happened?`,
        a: `Group leakage: a random 5-fold split put different images from the same patient into different folds. A patient might have 16 images in the training fold and 4 in the validation fold. The model learned the specific patterns of each patient's imaging (lighting, scanner calibration, patient-specific anatomy) during training and then "validated" on different images of the same patients it already knew. The 94% CV accuracy measures how well the model generalizes to new images of patients it has already seen — which is much easier than generalizing to new patients entirely. The 71% production accuracy is the true generalization score (to unseen patients). The fix is grouped k-fold splitting: all images of a given patient must stay in the same fold, ensuring validation always uses patients the model has never seen.`,
      },
      {
        q: `List three preprocessing operations that can cause leakage when applied before the train-test split.`,
        a: `First, StandardScaler and MinMaxScaler: fitting on the full dataset computes mean, std, min, max across train and test together, so the training data's normalization reflects test-set statistics. Second, SimpleImputer or KNNImputer: imputing missing values using statistics computed from the full dataset (including test examples) means test-set values influence the fill values used in training. Third, target encoding: computing per-category mean target across the full dataset means each row's encoded feature value reflects all other rows' labels including those in the test set, creating a direct channel for label information to flow into training features. All three must be fit on training data only and then applied (using the training-fit parameters) to the validation and test sets.`,
      },
      {
        q: `What is the correct order of operations for preprocessing inside a k-fold cross-validation loop?`,
        a: `For each fold: (1) Split the training indices from the validation indices according to the fold definition. (2) Using only the training indices, fit all preprocessing transformers — scaler, imputer, encoder. (3) Transform the training data using the fitted transformers. (4) Transform the validation data using the same fitted transformers (fitted on training only — never refit on validation). (5) Train the model on the transformed training data. (6) Evaluate the model on the transformed validation data. The key invariant is that the validation data must always be transformed using parameters derived exclusively from training data. This mimics the production scenario where your deployed model uses a scaler fitted during training to process new incoming data.`,
      },
    ],
  },
  {
    id: 'feature_selection',
    title: 'Feature Selection',
    subtitle: `Reduce dimensionality to fight overfitting, cut training cost, and build models that generalize.`,
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['feature selection', 'curse of dimensionality', 'LASSO', 'RFE', 'mutual information', 'multicollinearity'],
    summary: `Adding features without adding data is one of the most reliable ways to degrade model generalization. Every new feature adds a dimension to the feature space, and in high-dimensional spaces data points become equidistant — the geometric structure that learning algorithms depend on breaks down. A regression model with 200 features and 500 training rows has more degrees of freedom than examples to constrain them; it will memorize the training noise rather than learning signal. The rule of thumb is roughly 10 training examples per feature for regression to generalize reliably. Feature selection is not about finding which features are "good" — it is about finding the right set that trades accuracy for generalization. Filter methods are fast but blind to interactions. Wrapper methods (RFE) are expensive but model-aware. Embedded methods (LASSO, tree importance) perform selection during training. VIF detects multicollinearity, where two near-collinear features cause each other's coefficients to become unstable and their signs to flip across training runs.`,
    keyPoints: [
      `**~10 training examples per feature is a rule of thumb for regression to generalize reliably.** A 200-feature dataset with 500 rows is severely underdetermined — the model has 200 free parameters to fit 500 examples, and most of those 200 features will absorb noise. Adding features without proportional data growth pushes the model deeper into the overfitting regime.`,
      `**Filter methods evaluate features in isolation using correlation, mutual information, or chi-squared statistics.** A feature with zero marginal correlation to the target might be essential in combination with another feature — it carries no individual signal but captures a critical interaction. Filter methods discard it. This is their structural limitation: they cannot discover features that matter only jointly.`,
      `**Mutual information captures non-linear statistical dependence between a feature and the target.** A feature with a U-shaped or V-shaped relationship to the target has zero Pearson correlation — linear correlation is blind to it. Pearson-based filtering discards it; mutual information retains it. Use mutual information when the feature-target relationship may be non-monotone.`,
      `**RFE trains a model, removes the lowest-importance feature, and repeats until the desired number of features remains.** For p features, this requires p model fits — expensive but effective because each elimination decision is made by a model that sees the interaction structure. The resulting subset is aware of which features are redundant when others are present, which filter methods cannot assess.`,
      `**LASSO (L1 regularization) drives some coefficients to exactly zero by the geometry of the L1 constraint region — the optimization landscape intersects the L1 diamond at corners where coordinates are zero.** Ridge (L2) pushes coefficients toward zero but almost never to exactly zero. Increasing LASSO regularization strength increases sparsity. This is feature selection embedded in the training objective, which avoids the separate selection-then-fitting pipeline.`,
      `**VIF (Variance Inflation Factor) above 10 indicates severe multicollinearity: one feature is a near-linear combination of others.** The consequence is that the feature's coefficient becomes unstable — small changes in training data cause large swings in the estimated coefficient, and its sign can flip. The model cannot attribute credit correctly between the correlated features because both carry nearly the same information.`,
      `**Tree feature importance (total impurity reduction across all splits) is biased toward high-cardinality features.** A feature with many unique values offers more potential split points and therefore produces higher impurity reduction scores even if the feature has no predictive value. Permutation importance is a better diagnostic: shuffle the feature and measure the drop in validation performance. A genuine feature causes a large drop when shuffled; a spurious high-cardinality feature does not.`,
      `**Selecting features based on test-set performance is leakage.** The test set is no longer a valid generalization estimate once it has influenced any modeling decision, including which features to include. Feature selection must happen using training data only, ideally inside each cross-validation fold so that the feature subset is itself not overfit to a particular train-val partition.`,
      `**When regularization is available — LASSO, Ridge, Elastic Net, tree depth limits — explicit feature selection is often unnecessary.** Regularization manages the bias-variance tradeoff automatically during training. Explicit feature selection adds value when the goal is interpretability (a regulator needs to understand which features are used), inference cost reduction (fewer features reduce serving latency), or an auditable fixed feature set that cannot change between retraining runs.`,
      `**Stepwise selection (forward or backward) greedily adds or removes features one at a time.** Each step is optimal given the current set, but greedy choices are irreversible — a feature removed in early backward selection cannot re-enter the set even if its value becomes apparent in combination with later-selected features. Stepwise is computationally tractable but can miss the globally optimal subset.`,
    ],
    takeaway: `More features always increase model capacity and often increase overfitting. Feature selection is a bias-variance tradeoff: too few features and the model misses signal; too many and it memorizes noise. Performing selection on the test set is leakage that invalidates the evaluation — it must happen inside the training process, ideally inside each cross-validation fold.`,
    checkQuestions: [
      {
        q: `You filter features by Pearson correlation with the target and retain only the top 20. Your model performs worse than with all 100 features. What most likely went wrong?`,
        a: `Pearson correlation measures linear dependence between a single feature and the target, evaluated in isolation. Features with weak marginal correlation may still be essential when combined with other features — they capture interaction effects that neither feature reveals alone. For example, "time_of_day" might have near-zero correlation with purchase probability overall, but when combined with "day_of_week", it powerfully predicts conversion. Filter methods cannot detect this because they evaluate features one at a time. Additionally, features with non-linear relationships to the target will be underranked by Pearson correlation: a quadratic or U-shaped relationship has near-zero Pearson correlation but strong predictive value. The fix is to use mutual information (which captures non-linear dependence) or wrapper methods (which evaluate feature subsets via model performance, capturing interactions).`,
      },
      {
        q: `Why does LASSO shrink some coefficients to exactly zero while Ridge (L2) rarely does, even though both add a penalty?`,
        a: `The geometry of the constraint region differs between L1 and L2. L2 regularization adds a penalty proportional to the sum of squared coefficients, which is a smooth sphere in coefficient space — the minimum of the penalized objective almost never hits a coordinate axis, so coefficients are pushed toward (but not to) zero. L1 regularization adds a penalty proportional to the sum of absolute values, which is a diamond (or hypercube) in coefficient space. The corners of this diamond lie on the coordinate axes (where exactly one coefficient is non-zero and the rest are zero). The optimization landscape of a loss function intersects the L1 constraint region at these corners, producing exact zeros. This is why L1 effectively performs feature selection while L2 produces small but non-zero coefficients. Elastic Net (a mixture of L1 and L2) provides sparsity (from L1) while stabilizing the selection among correlated features (from L2's grouping effect).`,
      },
      {
        q: `You compute feature importance from a gradient boosted tree and find that "customer_id" is the second most important feature. What has happened?`,
        a: `Tree feature importance (measured by total impurity reduction across all splits) is biased toward high-cardinality features — features with many unique values offer more potential split points. "Customer_id" is a unique identifier with cardinality equal to the number of rows. The tree can use it to memorize the training set: split on customer_id = 12345 to perfectly isolate a single customer's rows. This produces large impurity reductions at training time but generalizes to zero new customers at test time. Customer_id is essentially an index key with no predictive signal for unseen data. The fix: remove identifier columns before training, or use permutation importance, which measures the actual performance degradation from shuffling a feature rather than the training-time impurity reduction.`,
      },
      {
        q: `A linear model has two features that are 0.97 correlated (e.g., "height_cm" and "height_inches"). What specific failure mode does this cause and how do you fix it?`,
        a: `When two features are highly correlated, they both carry nearly the same information. The model can achieve the same prediction by assigning a large positive weight to one and a large negative weight to the other, or any linear combination of the two — there are infinitely many coefficient combinations that produce identical predictions. The normal equations for linear regression become numerically near-singular: small changes in the training data cause large swings in the estimated coefficients. This is multicollinearity. Symptoms: high VIF (likely >50 for 0.97 correlation), unstable coefficients across bootstrap samples, inability to interpret which feature is "important." The fix: (1) drop one of the correlated features (height_cm and height_inches carry identical information); (2) apply Ridge regression (L2), which stabilizes coefficients under collinearity by penalizing their magnitude; or (3) create a single combined feature (their average or principal component).`,
      },
    ],
  },
  {
    id: 'distribution_shift',
    title: 'Distribution Shift',
    subtitle: `The core assumption of supervised learning — train and deploy distributions match — is almost always violated in production.`,
    difficulty: 'advanced',
    estimatedMin: 55,
    tags: ['distribution shift', 'covariate shift', 'concept drift', 'label shift', 'model monitoring', 'KS test'],
    summary: `Every supervised ML model is trained under one assumption: the distribution of data at prediction time matches the distribution during training. This assumption is almost always violated in real deployments. The problem is not that this violates some statistical theorem — it is that the model continues to output confident predictions regardless.

A model making predictions on shifted data still outputs high confidence scores. The API call succeeds. No error fires. You find out weeks later when ground-truth labels arrive and you can finally measure what the predictions were worth. Covariate shift (P(X) changes but P(Y|X) is stable) can often be corrected by importance weighting without new labels. Concept drift (P(Y|X) changes) cannot — the old labels are wrong under the new relationship and retraining with fresh data is the only fix. Label shift (P(Y) changes but P(X|Y) is stable) can be corrected by adjusting the predicted probability distribution to the new class prior. Each type requires a different response, which means diagnosing the type comes before choosing the intervention.`,
    keyPoints: [
      `**Covariate shift is when the feature distribution at serving time diverges from training time while the conditional P(Y|X) stays the same.** The model's learned boundaries were calibrated to the training distribution — in the new region it is extrapolating, not interpolating. PSI > 0.2 on a key feature is a reliable early signal that the model's assumptions are breaking down before ground-truth labels confirm it.`,
      `**Concept drift is when P(Y|X) changes — the same feature values now predict different outcomes.** Fraudsters adopt a technique that makes fraudulent transactions look like legitimate ones. The feature distribution may be stable (same transaction types, same demographics), but the labels that should be assigned have changed. Importance weighting old training data does not help because the old labels are wrong under the new relationship. New labeled data and retraining are the only fix.`,
      `**Silent degradation is the most dangerous property of shift.** Model confidence is a function of learned weights applied to input features — it is not a measure of how well the input resembles the training distribution. The model continues outputting 95% confidence on inputs it has never encountered. The API call succeeds. The business consequence is that you cannot detect the problem from model behavior alone — only external ground truth labels, which arrive with a time lag, confirm the degradation.`,
      `**PSI (Population Stability Index) buckets a feature's distribution into deciles and computes weighted divergence between training and production distributions.** PSI < 0.1: stable. 0.1–0.2: moderate shift, investigate. > 0.2: severe shift, treat as a trigger for investigation and likely retraining. Track PSI per feature, not as an aggregate — aggregate metrics hide per-feature problems.`,
      `**Minimum viable feature drift monitoring: track mean, std, null rate, and percentile distribution for each feature in production.** Alert when the KS test p-value drops below 0.05 on a rolling window, indicating statistically significant distributional change. This infrastructure is not optional for any production ML system — without it, you have no early warning before ground truth labels confirm what went wrong.`,
      `**Label shift can be corrected without retraining when P(X|Y) is stable.** BBSE (Black Box Shift Estimation) estimates the change in P(Y) by comparing the model's output score distribution at deployment time to the training-time output distribution, then reweights predicted probabilities to account for the new class prior. This works when the patterns that distinguish classes are unchanged — only the rates have shifted.`,
      `**Concept drift has two subtypes requiring different detection approaches.** Sudden drift: a discrete event (a policy change, a new fraud technique, a product launch) produces a sharp performance drop visible in a narrow comparison window. Gradual drift: slow continuous change over months where no single day looks alarming but the cumulative effect is large. Gradual drift requires long baseline windows for comparison and is frequently missed by monitoring that only looks at short rolling windows.`,
      `**Retraining schedules (monthly, weekly) are a blunt instrument.** They retrain unnecessarily when distribution is stable and may be too slow when shift is rapid. Trigger-based retraining — fire when PSI or KS exceeds a threshold, or when model performance on a labeled validation window drops — is more efficient and more responsive. It requires calibrating the trigger threshold to avoid spurious alerts while catching meaningful shift.`,
      `**In high-velocity domains — fraud, social media feeds, financial markets — distribution shifts can happen in days or hours.** Fraudsters respond to model signals within weeks of a model deployment. Training data from two years ago encodes patterns that adversaries have already adapted around. Assume the training data is wrong until current monitoring says otherwise.`,
      `**Importance weighting for covariate shift correction: train a logistic regression to classify "is this example from the training set or from production?" Use the predicted probability as an inverse weight in the training loss.** Training examples that resemble production receive high weight; training examples unlike anything in production receive low weight. The model effectively learns from a reweighted distribution that resembles deployment, without requiring new labels.`,
    ],
    takeaway: `Model accuracy is measured against the training distribution, not the production distribution. A model can be silently wrong from day one if the distributions diverge — and the model's confidence scores will not tell you. Diagnosing the type of shift (covariate, concept, or label) determines whether you can fix it without new labels. Only monitoring catches it before the business does.`,
    checkQuestions: [
      {
        q: `Your fraud model was trained in 2022. In 2024, fraudsters adopt a new technique that makes fraudulent transactions look like legitimate ones. What type of shift is this and can it be fixed without new labels?`,
        a: `This is concept drift: the conditional relationship P(Y|X) has changed. In 2022, the feature patterns that predicted fraud (large transactions to new merchants, unusual geographies) were valid. In 2024, the new fraud technique mimics legitimate transaction patterns, so the same feature values now have a different label. This cannot be corrected by reweighting old training data — the 2022 labels are simply wrong for describing the 2024 relationship between features and fraud. The only fix is fresh labeled data: you need examples of the new fraud pattern, labeled by human reviewers or rule-based systems, and a retraining or fine-tuning step. Covariate shift (P(X) changes) can sometimes be corrected by importance weighting without new labels, but concept drift cannot.`,
      },
      {
        q: `Describe what happens to a model's confidence scores under distribution shift, and why this makes shift especially dangerous.`,
        a: `A model's confidence score is determined by its learned parameters applied to the input features — it is a function of the model's internal state, not of whether the input is representative of the training distribution. Under distribution shift, the model receives inputs it has never encountered before, but its sigmoid or softmax output will still produce a number between 0 and 1. Commonly, shifted inputs land in regions of feature space where the model produces high-confidence predictions (because they resemble the boundaries of known classes or because the model generalizes confidently outside its training manifold). The result is that the model continues to output confident predictions even as its accuracy collapses. Unlike a database query that errors on invalid input, the model silently returns a wrong answer with 95% confidence. This makes shift dangerous: there is no automatic error signal. Monitoring requires external ground truth labels (with their associated time lag) or sophisticated distribution-level tests.`,
      },
      {
        q: `What is the difference between covariate shift and concept drift, and why does the distinction determine whether you can avoid retraining?`,
        a: `Covariate shift: P(X) changes (the input feature distribution shifts) but P(Y|X) stays the same (given the same inputs, the correct label is unchanged). Example: your training data was 80% urban users, but your production users are 60% rural — but rural users with the same feature profile still convert at the same rate. Because P(Y|X) is unchanged, your old labeled training data is still correct — it just underrepresents the current input distribution. This can be corrected by importance weighting: up-weight training examples that resemble production, down-weight those that do not, without collecting new labels. Concept drift: P(Y|X) changes — the same feature values now predict different outcomes. Old training labels are wrong under the new relationship. Importance weighting on wrong labels produces a wrong model. New labels are required.`,
      },
      {
        q: `You run a KS test comparing the training and production distributions of your top 5 features and find p < 0.01 for one feature. What does this mean and what should you do?`,
        a: `A p-value < 0.01 on the KS test means you can reject the null hypothesis (that training and production distributions for this feature are drawn from the same distribution) with 99% confidence. The feature's distribution has statistically significantly shifted between training time and production. The next steps: (1) investigate the magnitude of shift, not just statistical significance — with large samples, even trivial shifts become statistically significant. Use PSI to measure practical magnitude: PSI > 0.1 warrants concern, PSI > 0.2 requires action. (2) Investigate the cause: is this a data collection change (a bug introduced a null imputation that changed the distribution?), a real-world change (a new user cohort?), or seasonal variation? (3) Check whether downstream model performance has degraded. If performance is still on target, the shift may not be impacting predictions. If performance has degraded, trigger retraining or rollback.`,
      },
      {
        q: `A model deployed in January shows 89% AUC. By June, AUC has drifted to 78%. Feature distribution monitoring shows stable PSI across all features. What type of shift does this suggest?`,
        a: `Stable feature distributions (low PSI) but degraded model performance (AUC drop) is the signature of concept drift: P(X) has not changed (hence stable PSI on features) but P(Y|X) has changed (hence degraded accuracy). The model's learned mapping from features to labels was calibrated to the January relationship — that relationship has since changed. Common causes: seasonal shifts in user behavior (January vs. June shopping patterns differ), competitive changes (a competitor launched a product that changed which users are motivated to convert), policy changes (a new pricing model changed what features predict churn), or external events (a regulatory change modified what fraud looks like). The fix is new labeled data from the post-shift period and retraining. Feature monitoring alone (PSI) is insufficient — you also need to monitor model output distribution and track prediction accuracy with ground-truth labels.`,
      },
    ],
  },
  {
    id: 'data_augmentation',
    title: 'Data Augmentation',
    subtitle: `Artificially expand your training distribution by adding realistic variations — but only ones that preserve the label.`,
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['augmentation', 'SMOTE', 'image augmentation', 'mixup', 'text augmentation', 'back-translation'],
    summary: `The problem augmentation solves is overfitting from insufficient training data — the model memorizes the training examples instead of learning general invariances. A photo of a cat flipped horizontally is still a cat. A sentence with a synonym substituted still has the same sentiment. By training on modified versions of existing examples, the model learns that these transformations do not change the label, and stops memorizing specific pixel arrangements. The hard constraint that defines the entire approach: every augmentation must preserve the label, and only a domain expert can verify this for each class. Rotating a "6" by 180 degrees produces a "9" — a label-violating transformation that corrupts the training signal.

This means augmentation strategies are domain-specific, cannot be automated without human validation, and do not help when the model is underfitting — adding harder variations to data the model cannot yet learn makes the problem worse, not better.`,
    keyPoints: [
      `**Label preservation is the non-negotiable constraint.** A "6" rotated 180 degrees looks like a "9." A "1" flipped horizontally is still a "1" — but the domain expert must verify this for every class before applying any transformation at scale. The model cannot distinguish label-preserving augmentations from label-violating ones. Applying label-violating augmentations creates conflicting supervision: the same visual pattern receives different labels depending on which rotation was applied, and training diverges or learns the wrong invariances.`,
      `**Mixup creates new training examples by linearly interpolating between two existing examples: new_x = λ·x1 + (1-λ)·x2, new_y = λ·y1 + (1-λ)·y2.** The model trains on blended pixels with blended labels. This forces linear behavior between training examples — the model cannot make arbitrary predictions in the regions between training points. The result is stronger regularization and better calibration. Mixup is particularly effective for image classification and tabular data with continuous labels.`,
      `**Back-translation for text augmentation: translate a sentence to French, then back to English using a different model.** The result is a grammatically natural paraphrase with the same meaning but different vocabulary and phrasing. This is effective precisely because it produces the kind of surface variation that synonym replacement rules cannot replicate — the paraphrase is a real human-like sentence, not a mechanically constructed one.`,
      `**Apply augmentations on-the-fly during training, not as a preprocessing step.** Pre-computed augmentations mean the model sees the same rotated version of an image at every epoch — it memorizes the specific augmented pixel arrangement just as easily as the original. On-the-fly randomization applies a different random transformation at each training step, so across many epochs the model effectively trains on a near-infinite variety of augmented versions and is forced to learn the underlying invariance.`,
      `**Augmentation does not help underfitting models.** If training accuracy is low (the model has not captured the signal in the original data), adding more varied examples makes the problem harder without fixing the capacity or representation issue. The diagnostic: compare training and validation accuracy. High train, low validation means the model is overfitting — augmentation helps. Low train, low validation means the model is underfitting — fix the architecture or features first.`,
      `**Test-time augmentation (TTA) generates multiple augmented versions of each test example, runs each through the model, and averages the predictions.** Because predictions on slightly different versions of the same input vary due to model stochasticity, averaging reduces prediction variance and typically improves accuracy by 1–3 percentage points with no additional training cost.`,
      `**Tabular augmentation has weak empirical support compared to images and text.** There is no obvious label-preserving invariance in tabular data — adding Gaussian noise to a customer's age or income does not produce a "different view of the same customer" in the way horizontal flipping produces a different view of the same cat. Gaussian noise injection helps when the dataset is very small. SMOTE addresses class imbalance specifically, not general regularization.`,
      `**Augmentation strength is a hyperparameter.** Extreme rotations (45+ degrees for digits) or radical color shifts create samples that no longer plausibly represent the class — the augmentation adds noise rather than teaching invariance. Tune augmentation magnitude with the same rigor as learning rate: measure validation accuracy at different augmentation strengths rather than choosing a default.`,
      `**Augmentation in domain adaptation: when you have labeled source domain data and unlabeled target domain data, augmenting source examples with transformations that bring them closer to the target domain distribution is a form of unsupervised domain adaptation.** This is distinct from regularization-focused augmentation — the goal is reducing the distribution gap between domains, not building invariance to label-preserving transformations.`,
    ],
    takeaway: `Augmentation encodes invariances — transformations that should not change the model's prediction. Those invariances must be validated by a domain expert for every class. The model cannot distinguish label-preserving transformations from label-violating ones, and augmenting with the wrong transformations creates conflicting training supervision that degrades rather than improves performance.`,
    checkQuestions: [
      {
        q: `You are training a digit recognition model and augment by rotating all training images up to 45 degrees. Performance degrades. What went wrong?`,
        a: `Many handwritten digits are not rotationally invariant to large angles. A "6" rotated 180 degrees looks like a "9." A "9" rotated 90 degrees looks ambiguous. A "1" rotated 45 degrees is still a "1" but an unusual one. By augmenting with 45-degree rotations, you introduced label-violating samples: images labeled "6" that visually look like "9" to any human. The model is trained with conflicting supervision — the same visual pattern receives different labels depending on whether it is the original or an augmented version. The rule is that augmentations must be label-safe for every class, not just for some. Modest rotations (up to 10-15 degrees) are typically label-safe for digit recognition; 45-degree rotations are not.`,
      },
      {
        q: `What is Mixup augmentation and why does it act as a regularizer?`,
        a: `Mixup constructs new training examples by linearly interpolating between two randomly selected training examples: new_x = λ·x1 + (1-λ)·x2 and new_y = λ·y1 + (1-λ)·y2, where λ is drawn from a Beta distribution. The training example is literally a weighted average of two images (pixels blended) and the label is a weighted mixture of the two label vectors. It acts as a regularizer because it forces the model to predict a smooth interpolation of labels in the regions between training examples. Without Mixup, the model is only supervised at discrete training points and can behave arbitrarily in between. Mixup constrains the model to be approximately linear between pairs of training examples, which reduces overfitting and improves calibration (the model's confidence is better aligned with its actual accuracy).`,
      },
      {
        q: `When does augmentation help and when does it not? Give a scenario for each.`,
        a: `Augmentation helps when the model overfits: a small dataset where the model achieves near-perfect training accuracy but poor validation accuracy is exactly the regime where augmentation — by creating more diverse training examples — reduces variance. Example: a medical imaging classifier with 500 training images per class; random crops, flips, and color jitter dramatically expand the effective training set. Augmentation does not help when the model underfits: if training accuracy is 60% (the model has not yet captured the signal in the original data), adding more variations of already-unlearnable data makes the problem harder without fixing the capacity or training problem. Example: using a linear model on image pixel features — adding augmented images will not fix the fundamental insufficiency of the representation. The diagnostic is to compare training and validation accuracy: high train, low validation → augmentation helps; low train, low validation → fix the model first.`,
      },
      {
        q: `Why should augmentation transformations be applied on-the-fly during training rather than pre-computed and saved to disk?`,
        a: `If augmentations are pre-computed (applied once before training), the model sees the same augmented versions of each image at every epoch. In epoch 1, it sees "cat_image_1_rotated_23deg." In epoch 5, it sees exactly the same "cat_image_1_rotated_23deg" again. After enough epochs, the model memorizes these specific augmented versions just as easily as it would memorize the originals — no regularization benefit is achieved. On-the-fly augmentation applies a random transformation at each training step: epoch 1 might use rotation=23deg; epoch 5 might use rotation=7deg for the same image. Each time the model sees an image, it sees a freshly randomized variant. Over thousands of epochs, the model is effectively trained on a near-infinite variety of augmented versions, which forces it to learn the underlying invariances rather than memorizing specific pixel arrangements.`,
      },
    ],
  },
  {
    id: 'data_versioning_and_pipelines',
    title: 'Data Versioning and Pipelines',
    subtitle: `Models are only reproducible if both code and data are versioned — and production ML breaks when training and serving compute features differently.`,
    difficulty: 'advanced',
    estimatedMin: 40,
    tags: ['DVC', 'feature store', 'data versioning', 'training-serving skew', 'pipeline orchestration', 'reproducibility'],
    summary: `A model trained on unknown data with features computed by unknown logic is not reproducible and not debuggable. When a production model degrades, answering "what changed?" requires knowing exactly what training data was used, what feature computation code ran against it, and what preprocessing parameters were applied. Without explicit tooling, this information almost never exists. DVC solves the data versioning half: it stores a content hash pointer alongside the training code in Git, so reverting to any past commit recovers both the code and the exact dataset used. Training-serving skew is the other major failure mode: feature computation is implemented twice — once in Python for training, once in SQL or Java for serving — and the two implementations accumulate subtle differences through timezone handling, null treatment, and rounding.

The model receives inputs it was never trained on, accuracy collapses, and no error fires. Feature stores solve this by centralizing feature computation into a single canonical implementation shared by both pipelines. Beyond these two, data pipelines fail for reasons code pipelines do not: upstream schemas change silently, nullable columns appear in previously non-nullable fields, and tables arrive late. Making pipelines observable — with row-count assertions, schema validation, null rate monitoring — converts silent corruption into loud failures.`,
    keyPoints: [
      `**DVC stores a .dvc pointer file (a content hash of the dataset) committed alongside the training code in Git.** Reverting to any past commit recovers both the training code and the exact dataset state at that commit — full reproducibility. Without this, the "same" code run against "the same" table six months later may produce a different model because the underlying data changed silently.`,
      `**Training-serving skew is responsible for a disproportionate share of production ML incidents.** A model trained on batch SQL features and served with real-time Python logic will behave differently in production even if both codepaths are "correct." Different timezone handling, different null treatment, different aggregation windows — the model was trained on one distribution and is evaluated on another. The fix is not better testing; it is a single feature computation function shared by both the training pipeline and the serving layer. Without this structural fix, the gap silently widens every time either codepath is modified.`,
      `**Feature stores centralize feature computation into one canonical implementation with two storage backends: an offline store (data warehouse or Parquet lake) for point-in-time training data generation, and an online store (Redis, DynamoDB) for low-latency serving.** Both are written by the same computation logic — drift between training and serving becomes structurally impossible rather than aspirationally prevented.`,
      `**Point-in-time correctness is the requirement that a training example for a prediction made at time T uses feature values as they existed at time T, not as they exist when the pipeline runs.** Backfilling a "30-day purchase count" using today's data includes purchases that happened after the label date — future information leaked into training features. This inflates offline metrics and produces a model that systematically outperforms in development and underperforms in production.`,
      `**Minimum viable pipeline observability: row count assertions (1M rows yesterday, 100K rows today → alert), schema validation that fails loudly if a column is renamed or its type changes, null rate monitoring (2% nulls in training, 25% in production → alert), and staleness checks (pipeline has not run in 24 hours → alert).** These are the signals that distinguish a pipeline that ran from a pipeline that ran correctly.`,
      `**Schema drift is a silent failure mode.** An upstream team renames a column from "transaction_value" to "txn_amount." The pipeline reads nulls where it expected values. No error is raised. The model retrains on a null-imputed version of the feature, silently degrades, and the incident trace points to "mysterious performance degradation" six weeks later. A data contract that asserts column names and types at pipeline start catches this on day one.`,
      `**Experiment tracking (MLflow, Weights & Biases) records hyperparameters, metrics, dataset versions, and model artifacts for every training run.** Combined with data versioning, every past run is fully recoverable: given a run ID, you can recover the exact code, data, parameters, and evaluation results. Without this, debugging a production regression requires reconstructing conditions from memory — which nobody can do reliably six weeks later.`,
      `**Data contracts are formal, machine-checkable specifications of upstream source guarantees: column names, types, null rates, value ranges, update frequency.** When the contract is violated, the pipeline fails at the validation step — loudly and on day one — rather than silently propagating the violation through feature computation and into the trained model. The contract is the specification; the validation step is the enforcement.`,
      `**Backfilling historical features requires point-in-time discipline at the feature store layer.** Computing "average purchase value in the last 30 days" across all historical dates using today's data includes purchases that occurred after each historical date — temporal leakage baked into the feature values themselves. A feature store with time-travel support queries feature values as of a specified timestamp, enforcing this automatically.`,
    ],
    takeaway: `A model is a function of code and data together. Versioning only the code leaves half the reproducibility problem unsolved. Centralizing feature computation eliminates training-serving skew by making divergence structurally impossible rather than hoping discipline holds. Loud pipeline failures are better than silent ones — a pipeline that fails on day one is always better than one that silently corrupts a model and surfaces six weeks later.`,
    checkQuestions: [
      {
        q: `A model trained and evaluated in your offline pipeline shows 91% AUC. After deployment, production AUC is 77%. No distribution shift is detected in features monitored at prediction time. What is the most likely cause?`,
        a: `The most likely cause is training-serving skew: the features used during serving are computed differently from the features used during training. Monitoring features at prediction time confirms their distribution is stable (ruling out distribution shift), but if the feature values themselves are wrong — computed with different logic, different null handling, different time windows — the model receives inputs it was never trained on. Common examples: the training pipeline computed "days since last purchase" using a batch SQL snapshot with a specific cutoff time, but the serving system computes it from a live database that includes transactions from the current day. Or timezone handling differs (UTC in training, local time in serving). Or a feature is computed from a different table version in production. The fix is to audit the feature computation code in both pipelines and compare output values for the same raw input on a held-out sample.`,
      },
      {
        q: `You need to reproduce a model trained 8 months ago to debug a regression. You have the training code at the exact commit, but you cannot reproduce the results. What is missing from your MLOps setup?`,
        a: `Data versioning is missing. The training code alone is insufficient to reproduce a model — you also need the exact dataset used to train it. Eight months ago, the training set had a specific set of rows, feature values, and label assignments. Since then, the upstream data has likely changed: new rows have been added, old rows may have been updated or deleted, feature engineering pipelines may have been modified. Without DVC (or equivalent data versioning), you cannot recover the exact dataset state that existed 8 months ago. The fix for the future: use DVC to commit a .dvc pointer file alongside each model training run, so the dataset version is permanently associated with the code version. With experiment tracking (MLflow), also record the dataset version as a run parameter.`,
      },
      {
        q: `What is point-in-time correctness in a feature store and what goes wrong without it?`,
        a: `Point-in-time correctness ensures that when you assemble a training example for a model predicting on date T, you use the feature values as they existed at time T — not as they exist when you assemble the training data (which could be months later). Without it, you commit temporal leakage: for a customer churn model, if you use today's "30-day purchase count" to label a training example from 6 months ago, you include purchases from the 5 months after the label date. The feature value is contaminated by future information. A feature store with point-in-time support maintains the full history of feature values with timestamps, and the training data assembly query specifies "give me the feature value for customer X as of date T" — not "give me the current value." Without this, every historical training example is contaminated by future data, producing inflated accuracy that collapses in production.`,
      },
      {
        q: `An upstream team renames a column from "transaction_value" to "txn_amount" without notifying your team. Your retraining pipeline runs successfully with no errors. Six weeks later, you notice model performance has dropped. What happened and how would a data contract have prevented it?`,
        a: `The pipeline read the renamed column as null (the old column name no longer exists in the table, and many SQL engines return nulls for missing columns rather than erroring). The imputer filled in the mean of a now-null column — likely 0 or the column's historical mean from the imputer fitted at the previous training run. The model was retrained on a version of the transaction value feature that was entirely imputed noise, producing silently degraded performance six weeks before it was noticed. A data contract would have prevented this: it specifies that the upstream table must contain a column named "transaction_value" of type FLOAT with a null rate below 1%. The data validation step runs this contract check before any feature computation, and when the column is absent, the pipeline fails immediately with a descriptive error — blocking the corrupted retraining run and alerting the team the day it happened, not six weeks later.`,
      },
    ],
  },
]

export const DATA_MODULES = [
  {
    id: 'data_quality_audit',
    title: 'Data Quality Audit',
    subtitle: `Understand what makes data "dirty" and why auditing before modeling is non-negotiable.`,
    difficulty: 'foundational',
    estimatedMin: 35,
    tags: ['data quality', 'profiling', 'missing values', 'outliers'],
    summary: `Data quality is the degree to which a dataset accurately represents the phenomenon you want to model — and most real-world datasets fall short in ways that are easy to miss. "Dirty" data comes in several forms: missing values (fields left blank or recorded as nulls), duplicates (the same row recorded twice, often from system merges), outliers (values that are numerically extreme — either genuinely rare events or data entry errors), inconsistent types (a numeric column that contains the string "N/A"), and impossible values (a person with age -3 or a transaction timestamped in the future). The danger is not just that these errors exist but that models are extremely good at fitting noise — a gradient-boosted tree will happily learn that "age = -3" predicts some target, and it will look fine in training. Data profiling is the practice of systematically examining a dataset before modeling: computing per-column statistics (min, max, mean, null rate, unique value count, value distribution), checking referential integrity between tables, and flagging anomalies. Tools like pandas-profiling, Great Expectations, and ydata-profiling automate much of this. The cost of skipping this step is high: models trained on dirty data produce confident wrong predictions, and debugging them downstream is far more expensive than catching problems at the source.`,
    keyPoints: [
      `Missing values are not all the same problem — a null in an "income" field might mean the person declined to answer (informative) or that the field wasn't collected for that cohort (structural). Treating all nulls identically is a data quality error in itself.`,
      `Duplicates are especially dangerous in train/test splits: if the same row appears in both train and test, your test-set accuracy is inflated and your model will underperform in production where it sees each example once.`,
      `Outlier treatment requires domain judgment. A transaction of $10,000 in a fraud dataset might be a legitimate high-value purchase or a fraudulent one — clipping it to the 99th percentile destroys the signal. Always ask what generated the outlier before removing it.`,
      `Inconsistent types are a silent killer. A column stored as strings that "looks" numeric will cause most ML libraries to throw an error or silently drop the column. Pandas will infer dtype at load time, but a single malformed row can flip the entire column from int64 to object.`,
      `Impossible values — negative ages, timestamps in the future, zip codes with 7 digits — are always errors, never signal. They should be nullified and then handled by the missing-value pipeline, not imputed with the column mean.`,
      `Data profiling is not a one-time step: distributions shift as new data arrives. Building profiling into your training pipeline (as automated assertions) catches drift before it silently corrupts a retraining run.`,
      `The "garbage in, garbage out" principle understates the risk: with a powerful model, it is "garbage in, confident garbage out" — the model will assign high probabilities to wrong predictions rather than flagging uncertainty.`,
      `Referential integrity failures (foreign keys in one table that have no match in another) are common after database joins and produce silent data loss — rows get dropped in the join and the pipeline reports no error.`,
      `Null rates above ~40% in a feature are a strong signal to reconsider whether the feature should be used at all. A model that learns from 40% of rows on one feature and 100% on another will have interaction terms that are poorly estimated.`,
      `Always profile the test set separately from the train set and compare distributions. A column with 2% nulls in train and 18% nulls in test is a warning that something changed between data collection periods — that model will degrade.`,
    ],
    takeaway: `The key insight of data quality auditing is that models are confident even when trained on garbage, which means quality failures are invisible until production — so you must catch them before training, not after.`,
    checkQuestions: [
      {
        q: `A colleague argues that outlier rows should simply be removed before training to keep the model clean. What breaks if you follow this advice blindly on a fraud detection dataset?`,
        a: `Fraud transactions are inherently outliers — they are rare, high-value, and anomalous by definition. Removing outliers before training a fraud model would systematically eliminate the positive class, leaving you with a model trained almost entirely on legitimate transactions. The model would learn to classify everything as non-fraud and achieve 99%+ accuracy on a dataset where fraud is 1% — while catching zero fraud cases. Outlier removal requires knowing why a value is extreme, not just that it is extreme.`,
      },
      {
        q: `You join two tables on a customer ID and your training set shrinks from 500,000 rows to 380,000 without any error. What likely happened and why does it matter for your model?`,
        a: `A left join or inner join dropped 120,000 rows because those customer IDs had no match in the second table. This is a referential integrity failure — the IDs exist in one system but not the other, likely because of different data collection pipelines or time windows. This matters enormously: the 120,000 dropped rows are almost certainly not a random sample. They might be older customers, customers from a different region, or customers who churned — exactly the population where your model's predictions matter most. The model is trained on a biased subset and will generalize poorly to the full population.`,
      },
      {
        q: `What is the difference between an outlier and an impossible value, and why should they be handled differently?`,
        a: `An outlier is a value that is statistically extreme but potentially valid — a transaction of $50,000 is unusual but can happen. An impossible value is one that violates a hard constraint regardless of context — a person cannot be -3 years old, a percentage cannot be 150. Outliers require domain judgment: they may be signal (rare fraud) or noise (entry errors). Impossible values are always errors and should be nullified before any other processing. Treating them differently matters because imputing an impossible value (e.g., replacing -3 age with the mean) propagates the error into the training data as if it were real information.`,
      },
      {
        q: `You run a data profile on your training set in January and the model performs well. You retrain in June without re-profiling and performance drops. What data quality issue is most likely responsible?`,
        a: `Distribution shift between the January and June data — a data quality issue that profiling would have caught. Common causes: a new data source was added (changing the null rate or value range of a feature), a upstream system changed its encoding (e.g., a categorical field now uses different labels), or a business event changed the real-world distribution (a pricing change that shifted transaction amounts). Without profiling at retraining time, the pipeline reports no error but the model is now trained on a different distribution than the one it was originally validated on.`,
      },
      {
        q: `A column in your dataset has 55% null values. A colleague says to impute with the median. What should you do before accepting that advice?`,
        a: `First, determine the mechanism of missingness: why are 55% of values missing? If the nulls are MNAR (Missing Not At Random) — e.g., patients with the most severe condition are the ones who didn't get the lab test recorded — then imputing with the median fills in values that are systematically wrong for the highest-risk group, introducing bias. Second, consider whether the column is worth including at all: a feature observed for only 45% of rows will create interaction effects that are poorly estimated. Third, check whether the missing rate in the test set matches training (55%) — if it doesn't, there is a structural data collection difference. Imputing with the median is only appropriate after these questions are answered.`,
      },
    ],
  },
  {
    id: 'missing_value_handling',
    title: 'Missing Value Handling',
    subtitle: `The mechanism of missingness — MCAR, MAR, MNAR — determines the right treatment, not the null rate alone.`,
    difficulty: 'foundational',
    estimatedMin: 40,
    tags: ['missing data', 'imputation', 'MCAR', 'MAR', 'MNAR', 'MICE'],
    summary: `Missing data is not a monolithic problem — the reason a value is missing completely changes what you should do about it. Statisticians categorize missingness into three mechanisms. Missing Completely At Random (MCAR) means the probability of a value being missing is independent of everything — like a sensor randomly dropping packets. Missing At Random (MAR) means the probability of missingness depends on other observed variables — a patient's income is more likely to be missing if they are uninsured, but income itself is not the cause. Missing Not At Random (MNAR) means the probability of missingness depends on the missing value itself — severely depressed patients are more likely to skip a depression survey. The mechanism matters because simple imputation (replacing nulls with mean, median, or mode) is only appropriate for MCAR data. For MAR data, model-based imputation that uses other columns as predictors — like MICE (Multiple Imputation by Chained Equations) or KNN imputation — can recover accurate estimates. For MNAR data, any imputation is dangerous because it fills in values that are systematically wrong for exactly the group where the feature is most informative. A frequently overlooked technique is adding a binary indicator feature ("was this field missing: yes/no") alongside any imputation — this preserves the information that missingness itself contains, which a model can learn to use as a predictive signal.`,
    keyPoints: [
      `MCAR is the least common mechanism in real data but the only one where mean/median imputation is statistically safe. Most real missingness is MAR or MNAR, which means simple imputation introduces bias by default.`,
      `The MNAR trap in production: if patients with the worst outcomes are most likely to have a lab value missing, imputing with the column mean fills in an optimistic value for the highest-risk group. Your model will predict low risk for the people who need intervention most.`,
      `Adding a binary missingness indicator feature is often more powerful than imputation alone. The indicator tells the model "this field was absent" — which is itself a signal. A model that sees both the imputed value and the indicator can learn the pattern; a model with only the imputed value cannot.`,
      `MICE (Multiple Imputation by Chained Equations) works by training a regression model for each column with missing values, using all other columns as features, and iterating until the imputed values converge. It is significantly more accurate than mean imputation for MAR data but adds training complexity and pipeline state.`,
      `KNN imputation finds the k most similar rows (by Euclidean distance on non-missing features) and averages their values for the missing column. It respects local structure but is expensive at scale: O(n²) comparisons for n rows. It also requires scaling features before computing distances.`,
      `Dropping rows with missing values is only valid if the rows are MCAR and the dropped fraction is small (under ~5%). Dropping 20% of rows because they have nulls almost certainly removes a non-random subset — creating a biased training set that performs worse on the full production population.`,
      `The critical production rule: fit your imputer on the training set only, then use it to transform train, val, and test. If you fit on the full dataset, information about test-set values leaks into the imputer's statistics and inflates your reported accuracy — this is data leakage.`,
      `When a feature has more than 50% missing values, consider whether imputation makes sense at all. A model learning from 40% of rows on one feature will have poorly estimated interaction effects. Sometimes the right answer is to drop the feature and treat "feature missing" as its own separate binary predictor.`,
      `Mode imputation for categorical features has a specific failure mode: in a column where one category is 70% of observed values, mode imputation converts all nulls to that category, artificially inflating its representation and suppressing the signal from the actual distribution.`,
      `MNAR is detectable (weakly) by checking whether the null pattern in a column correlates with observed features. If missingness in "income" correlates with "insurance status", you have evidence of MAR. If no observed feature predicts missingness, it could be MNAR — and you need domain knowledge to confirm.`,
    ],
    takeaway: `The key insight of missing value handling is that the reason a value is missing is more important than the missing rate — imputing MNAR data systematically fills in wrong values for exactly the cases where your model most needs to get it right.`,
    checkQuestions: [
      {
        q: `You are building a model to predict hospital readmission. A lab test result is missing for 30% of patients. A colleague imputes with the median. What is wrong with this approach and what would you do instead?`,
        a: `Lab tests are typically ordered when a clinician suspects a problem — meaning the test is more likely to be missing when the patient appears healthy, and present when the clinician is concerned. This is MNAR: the probability of the value being missing depends on the value itself (patients with extreme lab values get tested; patients near normal do not). Imputing with the median assigns an "average" lab value to patients who were never tested, which may be systematically wrong for the sickest patients. The right approach is to add a binary "was this test ordered" indicator feature, which captures the clinical signal of missingness. Model-based imputation from other covariates can also be used, but only after acknowledging the MNAR risk.`,
      },
      {
        q: `Why is it data leakage to fit a mean imputer on the full dataset (train + test) before splitting?`,
        a: `When you compute the column mean on the full dataset, that mean is influenced by the test-set values. When you then impute missing values in the training set using that mean, the training set's imputed values contain information derived from the test set. This means the model is indirectly "seeing" test-set statistics during training, which inflates cross-validation and test-set accuracy. In production, you will never have access to future data when computing your imputer — so fitting on the full dataset creates an unrealistic and optimistic evaluation. The rule is: fit all preprocessing transformers (scalers, imputers, encoders) on the training fold only.`,
      },
      {
        q: `What is the difference between mean imputation and MICE, and when does the difference matter most?`,
        a: `Mean imputation replaces all missing values in a column with that column's mean — it is fast and simple but ignores all relationships between columns. MICE treats imputation as a prediction problem: for each column with missing values, it trains a regression model using all other columns as features, imputes the missing values with those predictions, then iterates until convergence. The difference matters most when features are correlated. If missing income values are predictable from education level, employment status, and age, MICE will produce accurate imputations while mean imputation will produce the overall average regardless of these predictors. For MCAR data with low missingness rates, the difference is small. For MAR data with correlated features and moderate missingness (10-30%), MICE can dramatically improve imputation accuracy.`,
      },
      {
        q: `A model trained with complete-case analysis (dropping all rows with any null) achieves 92% accuracy. When you deploy it, accuracy drops to 84%. What is the most likely explanation?`,
        a: `The dropped rows were not MCAR — they were systematically different from the complete cases. The model was trained on a biased subset (rows where all features happened to be observed) and tuned its decision boundaries to that subset. In production, it encounters the full population including the types of rows that were dropped during training. The 8-point accuracy gap reflects the distribution mismatch between the training population (complete cases only) and the production population (everyone). This is the core danger of complete-case analysis: it is only valid when missing data is MCAR, which is rarely true in practice.`,
      },
    ],
  },
  {
    id: 'feature_engineering',
    title: 'Feature Engineering',
    subtitle: `Transform raw columns into representations that expose the signal a model can actually learn from.`,
    difficulty: 'foundational',
    estimatedMin: 45,
    tags: ['feature engineering', 'transformations', 'cyclical encoding', 'interaction terms', 'log transform'],
    summary: `Feature engineering is the process of transforming raw data columns into new representations that better expose the underlying patterns a model needs to learn. Raw features are rarely optimal: a column containing raw timestamps does not directly tell a model that Sundays differ from Mondays, or that midnight differs from noon. A column with right-skewed income values (a few billionaires, many middle-income earners) will cause linear models to over-weight extreme values. The goal of feature engineering is to do the representational work that models — especially linear and tree models — cannot easily do themselves. Numeric transformations are the simplest category: log transformation compresses right-skewed distributions so that the difference between $10,000 and $100,000 is treated more similarly to the difference between $1,000 and $10,000. Polynomial features and interaction terms create new columns from products of existing ones (e.g., height × weight → BMI proxy), capturing non-linear relationships without changing the model architecture. Date and time features are especially valuable: extracting hour-of-day, day-of-week, month, and quarter from a timestamp turns a single opaque integer into a set of interpretable signals. Cyclical features like hour-of-day require sin/cos encoding (hour 23 is close to hour 0, but raw numeric encoding makes them appear maximally different). Domain features — features built from knowledge of the problem rather than generic transformations — are often the highest-value because they encode information that a model would need millions of rows to discover on its own.`,
    keyPoints: [
      `Log transformation is appropriate for right-skewed distributions where extreme values compress the useful range. It only works for strictly positive values — log(0) is undefined. Common workaround: log(x + 1), which maps 0 → 0 and shifts all other values slightly.`,
      `Sin/cos encoding for cyclical features: hour 23 and hour 0 are one hour apart, but if you encode hour as a raw integer (0–23), a model sees them as 23 apart. Encoding as sin(2π·hour/24) and cos(2π·hour/24) places them next to each other in 2D space, which is the correct geometric relationship.`,
      `Interaction terms (feature A × feature B) are powerful but dangerous: with p features, there are p(p-1)/2 possible pairwise interactions. Creating all of them for a 100-feature dataset produces ~5,000 new columns, most of which are noise — this dramatically increases overfitting risk. Use domain knowledge to select which interactions matter.`,
      `Tree-based models (decision trees, random forests, gradient boosting) can implicitly learn monotone transformations and some interaction effects without explicit engineering. Log transforms matter less for trees but matter significantly for linear models, SVMs, and neural networks with linear layers.`,
      `Ratio features are among the most predictive in financial and behavioral domains: clicks/impressions (CTR), revenue/customers (ARPU), transactions/sessions (conversion rate). They normalize for scale differences between entities without requiring the model to learn the ratio itself.`,
      `Temporal lag features — yesterday's value, last week's value, a 7-day rolling mean — are essential for time-series tasks. They must be constructed carefully to avoid look-ahead leakage: you can only use values that would have been observable at prediction time.`,
      `Feature engineering for deep learning: modern deep networks can learn many transformations from raw data given enough data. However, domain features (e.g., days-since-last-purchase in a churn model) still outperform raw timestamps even with deep models, because they encode a relationship that is hard to learn from behavioral sequences alone.`,
      `The diminishing returns problem: after the first few high-value features, additional engineered features provide less and less lift while increasing training cost and overfitting risk. Measure feature importance before and after adding new features to track whether they are helping.`,
      `Binning continuous features (e.g., grouping ages into 18-24, 25-34, 35-44) can help when the relationship between feature and target is non-monotone (U-shaped or step-function). But binning loses information and is generally inferior to letting tree models find the splits themselves.`,
      `The most valuable feature engineering happens before data collection, not after: designing your system to record the right signals (e.g., time-between-actions rather than just action counts) is worth more than any post-hoc transformation.`,
    ],
    takeaway: `The key insight of feature engineering is that models learn from the geometry of your feature space, so the same information in a different representation can be the difference between a model that learns nothing and one that achieves state-of-the-art performance.`,
    checkQuestions: [
      {
        q: `You have a "time_of_day" feature encoded as an integer from 0 to 23. Your model performs poorly on predictions for late-night events. What is the encoding problem and how do you fix it?`,
        a: `The raw integer encoding treats hour 23 and hour 0 as maximally different (23 apart), when in reality they are 1 hour apart on the circular clock. A model using this encoding cannot learn that midnight behavior resembles 11pm behavior. The fix is sin/cos encoding: create two new features, sin(2π·hour/24) and cos(2π·hour/24). These two features together encode each hour as a point on the unit circle, so that hour 23 and hour 0 are geometrically close. This is essential for any cyclical feature: day-of-week (period 7), month-of-year (period 12), and degrees (period 360) all need the same treatment.`,
      },
      {
        q: `Why does log-transforming an income feature help a linear regression model but not a random forest?`,
        a: `Linear regression assumes a linear relationship between each feature and the target. When income is right-skewed (a few extremely high earners), the linear model devotes most of its coefficient's explanatory power to distinguishing the extreme values, effectively ignoring variation among typical earners. Log-transforming compresses the upper tail and spreads the distribution more evenly, making the linear relationship more valid. A random forest, by contrast, splits on thresholds: it can learn "if income > $50,000 go left, else go right" regardless of the scale or distribution. Monotone transformations like log do not change which threshold is the optimal split — the forest finds equivalent splits either way. Log transform matters for models that assume distributional properties (linear models, SVMs, neural nets with linear layers) and is largely irrelevant for tree-based models.`,
      },
      {
        q: `You are building a fraud detection model and add an interaction term: transaction_amount × is_international. What does this feature capture that neither original feature captures alone?`,
        a: `The interaction term captures the combined effect of high-value international transactions — the pattern that high amounts are suspicious specifically when the transaction is international, not when it is domestic. Individually, transaction_amount tells the model about scale and is_international tells the model about geography. Neither alone tells the model that their combination is the key fraud signal. The interaction term is the product of these two features, which will be large (high amount × international = 1) only when both conditions hold. A linear model cannot discover this relationship from the original features; it can only learn additive effects. For tree models, the interaction is learnable without an explicit feature (the tree can split on amount within the international subset), but the explicit feature can still speed up training.`,
      },
      {
        q: `A data scientist creates 200 interaction features from a 20-feature dataset and reports improved validation accuracy. What risk does this improvement mask and how would you verify it?`,
        a: `With 200 additional features (most of which are noise), the model has dramatically more capacity to overfit the training and validation sets. If the validation set was used to tune any hyperparameters, or if features were selected based on validation performance, the reported improvement is optimistic. To verify, you need a held-out test set that was never touched during feature construction or selection. You should also check training vs. validation accuracy gap — a much larger gap after adding interaction features signals overfitting. The principled approach is to select interaction features using domain knowledge or statistical tests on the training fold only, before any validation evaluation.`,
      },
    ],
  },
  {
    id: 'categorical_encoding',
    title: 'Categorical Encoding',
    subtitle: `Convert categories into numbers without lying to your model about distance, order, or information from the target.`,
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['encoding', 'one-hot', 'target encoding', 'ordinal', 'cardinality', 'embeddings'],
    summary: `Machine learning models operate on numbers. Categorical variables — features whose values are names rather than quantities — must be converted into numeric representations before training. The choice of encoding is consequential: the wrong encoding tells the model false things about the structure of the data. One-hot encoding (OHE) creates a binary column for each unique category value: "color = red" becomes [1, 0, 0], "color = blue" becomes [0, 1, 0]. This makes no assumptions about ordering and works well for low-cardinality features (under ~20 unique values). Ordinal encoding assigns an integer to each category — red=0, blue=1, green=2 — which is appropriate only when the category has a meaningful order (e.g., "low", "medium", "high"). Applying ordinal encoding to unordered categories tells a linear model that green is twice as large as blue, which is false. Target encoding replaces each category with the mean target value for rows in that category — if 40% of transactions labeled "merchant_A" are fraudulent, merchant_A gets encoded as 0.4. It is compact and powerful but introduces data leakage if computed before the train/test split or outside cross-validation folds. Embedding layers in neural networks learn a dense vector representation for each category during training — this is the only encoding that can represent high-cardinality categories (millions of unique IDs) efficiently and capture semantic similarity between categories.`,
    keyPoints: [
      `The dummy variable trap: with k categories, one-hot encoding produces k binary columns that sum to exactly 1. This creates perfect multicollinearity — any one column is perfectly predictable from the others. Linear models with an intercept term must drop one column (use k-1 columns); tree models are unaffected.`,
      `Cardinality explosion: a feature with 1,000 unique cities one-hot encoded adds 999 binary columns. For a dataset with 10,000 rows, each column has on average only 10 positive values — extremely sparse. Sparse high-cardinality features cause overfitting and training instability. Switch to target encoding, frequency encoding, or embeddings once cardinality exceeds ~50.`,
      `Target encoding leakage: if you compute the per-category mean target across the entire dataset and then use it as a feature in training, the training rows' own target values contaminate their feature values. The fix: compute target encoding only from other folds during cross-validation, or use leave-one-out encoding. Never compute target encodings on the full dataset before splitting.`,
      `Frequency encoding replaces each category with its frequency in the training set (e.g., merchant_A appears in 3% of rows → 0.03). It has no leakage risk (it uses only the feature, not the target) and handles high cardinality, but it conflates categories that happen to appear the same number of times with no other reason to be similar.`,
      `Ordinal encoding is correct for genuinely ordered categories and produces a single compact column. Applied to unordered categories, it introduces spurious distance relationships: a model will learn that "red" (0) and "blue" (1) are more similar than "red" (0) and "green" (2), which is false.`,
      `Rare categories are a production problem: a category seen once in training may not appear in training at all but appears at inference time. One-hot encoding handles this silently (all indicator columns are 0), but target encoders with no fallback will produce a null. Always handle unseen categories explicitly — by mapping to a global mean, a dedicated "unknown" bucket, or the mode.`,
      `Binary encoding is a compromise for high-cardinality: represent the integer index of each category in binary, producing log2(k) columns instead of k. For 1,000 cities, it uses 10 columns instead of 999. It implicitly assigns a bit-string relationship between categories (no semantic meaning), which is less harmful than ordinal encoding's distance relationship but still imposes arbitrary structure.`,
      `Embedding layers learn the optimal dense representation during training. Unlike all hand-crafted encodings, they can learn that two superficially different categories behave identically (two cities in the same economic zone), and they do it from data. The tradeoff: they require enough data per category to learn a useful embedding — a category seen 3 times will have a poorly initialized embedding.`,
      `One underused technique: hash encoding applies a hash function to map each category to one of h buckets, producing h binary columns. It handles new categories at inference time naturally (the hash always produces a valid bucket), avoids storing a vocabulary, but introduces hash collisions — two categories may be mapped to the same bucket.`,
      `The interaction between encoding and model type matters: tree models can use ordinal encoding for any categorical (they find their own splits anyway), while linear models are more sensitive. Neural networks benefit most from embeddings for high-cardinality features.`,
    ],
    takeaway: `The key insight of categorical encoding is that the encoding is a claim about the structure of the category — ordinal encoding claims order exists, one-hot claims equal distance — so using the wrong encoding for a given category type actively misleads your model.`,
    checkQuestions: [
      {
        q: `You apply ordinal encoding to a "city" feature with 50 unique values and train a linear regression. What exactly goes wrong?`,
        a: `Ordinal encoding assigns integers 0–49 to the 50 cities in an arbitrary order. Linear regression learns a single coefficient for the city feature, which it multiplies by the integer to get a contribution to the prediction. This means the model is forced to assume that "city 49" has exactly 49 times the effect of "city 1" — a constraint with no basis in reality. Cities that happen to receive adjacent integers are treated as similar; cities with high integers are given disproportionate weight. The model fits a meaningless linear trend across an arbitrary ordering. One-hot encoding would instead give the model an independent coefficient for each city, removing the false linearity assumption. Tree models are immune to this problem because they learn threshold splits (city_code > 25?) rather than linear relationships.`,
      },
      {
        q: `Walk through exactly why target encoding without cross-validation causes data leakage.`,
        a: `When you compute target encoding by taking the mean target for all rows with category X, you include the row you are about to use as a training example. That row's own target value contributes to the encoded feature value for that row. In the extreme case of a category with only one row, the encoded value is exactly the target value — the model receives a feature that is a direct copy of what it is trying to predict, making training accuracy artificially perfect. Even with many rows per category, each row's encoded value reflects its own outcome slightly, inflating apparent in-sample performance. The fix is leave-one-out or fold-based encoding: when computing the mean target for a training row, exclude that row's own target from the mean. During inference, use the mean over all training rows with that category.`,
      },
      {
        q: `At inference time, your model receives a city it has never seen in training. How does each encoding strategy handle this, and which is most robust?`,
        a: `One-hot encoding sets all city indicator columns to 0 — equivalent to a new implicit "other" category. This is handled silently but may produce unpredictable behavior if the model never learned to act on all-zero city columns. Ordinal encoding has no valid integer for an unseen city — you must either assign it the "unknown" integer or raise an error. Target encoding has no mean target for an unseen city — the typical fallback is the global mean target, which is reasonable but loses the per-category signal. Hash encoding maps the new city to a bucket automatically — the hash function always produces a valid bucket index, making it the most naturally robust to new categories. Embedding layers require a dedicated "unknown" token or a fallback to the zero vector. In production systems, maintaining an explicit "other" or "unknown" category in the vocabulary and training on it is the most robust general approach.`,
      },
      {
        q: `A feature has 5,000 unique merchant IDs and you are training a neural network. Why is one-hot encoding a bad choice here, and what should you use instead?`,
        a: `One-hot encoding 5,000 merchant IDs produces 4,999 binary columns (dropping one for the intercept). For a dataset with, say, 100,000 rows, each column has on average 20 non-zero values — an extremely sparse input. Neural networks learn poorly from sparse inputs because gradient flow through nearly-all-zero feature vectors is weak, weight updates are small, and most neurons learn nothing from most examples. The resulting embedding matrix (first layer weights × 5,000 input columns) has 5,000 × d parameters where d is the hidden dimension — orders of magnitude more than needed. An embedding layer is the right choice: it directly looks up a learned dense vector for each merchant ID without constructing a 5,000-dimensional sparse input, uses only 5,000 × d parameters (same count), and can capture semantic similarity between merchants that one-hot encoding cannot represent.`,
      },
    ],
  },
  {
    id: 'feature_scaling',
    title: 'Feature Scaling',
    subtitle: `Ensure features are on comparable scales so that gradient descent, distance metrics, and regularization work correctly.`,
    difficulty: 'foundational',
    estimatedMin: 35,
    tags: ['scaling', 'standardization', 'normalization', 'robust scaling', 'data leakage'],
    summary: `Feature scaling is the process of transforming numeric features so that they are on comparable numerical scales. This matters for a surprisingly large fraction of machine learning algorithms. Gradient descent, the optimization algorithm that trains linear models and neural networks, takes steps proportional to the gradient. If one feature has values in the range [0, 1] and another has values in [0, 100,000], the gradient with respect to the large-scale feature will dominate each update step, causing slow convergence and instability. Distance-based algorithms — K-Nearest Neighbors, Support Vector Machines, and K-Means clustering — compute distances between data points as Euclidean distances or dot products. A feature with large values will dominate the distance calculation, effectively making all other features invisible. Regularization (L1 and L2 penalties) penalizes large coefficient values — but if one feature is on a much larger scale, its coefficient will naturally be small even without regularization, making the penalty inconsistent across features. Standardization (z-score scaling) transforms each feature to have mean 0 and standard deviation 1: subtract the mean and divide by the standard deviation. Min-max normalization rescales each feature to the range [0, 1] by subtracting the minimum and dividing by the range — but it is highly sensitive to outliers, which become the new extremes. Robust scaling uses the median and interquartile range (IQR) instead of mean and standard deviation, making it resistant to outliers. Tree-based models (decision trees, random forests, gradient boosting) are scale-invariant because they make binary splits on individual features; the absolute scale of a feature does not affect where the optimal split threshold is.`,
    keyPoints: [
      `The critical production rule: always fit the scaler on the training set only, then use that fitted scaler to transform train, validation, and test sets. Fitting on the full dataset lets test-set statistics (mean, std) influence the training transformation — a subtle form of data leakage.`,
      `Min-max normalization is the wrong choice when your data has outliers. A column with values [1, 2, 3, 4, 10,000] gets normalized to [~0, ~0, ~0, ~0, 1.0] — the outlier dominates the range and all other values collapse near zero.`,
      `Robust scaling (using median and IQR) is preferred when outliers are present and meaningful (you do not want to clip them). It maps the median to 0 and the IQR to the range [-0.5, 0.5] by default, making it less sensitive to extreme values than standardization.`,
      `Neural networks are particularly sensitive to feature scale because gradient magnitudes flow back through all layers. Poorly scaled inputs cause some weights to receive enormous gradient updates and others near-zero, slowing or preventing convergence. Batch normalization mitigates this at intermediate layers but does not replace input scaling.`,
      `K-Means clustering is fully determined by Euclidean distances. A feature with std=1000 contributes 1,000× more to the distance than a feature with std=1. Without scaling, K-Means effectively clusters on whichever feature has the largest variance, ignoring all others.`,
      `Regularization (LASSO, Ridge) penalizes the magnitude of coefficients. If feature A is on scale [0, 1] and feature B is on scale [0, 1000], the coefficient for A will naturally be 1000× larger to have the same effect size. L2 regularization will shrink A's coefficient much more aggressively, introducing a biased penalty that has nothing to do with feature importance.`,
      `Tree models are genuinely scale-invariant: a decision tree splitting on "income > 50000" is identical to one splitting on "income_scaled > 0.5" — the tree finds the equivalent threshold either way. Applying scaling to tree inputs does not hurt but provides no benefit.`,
      `When you add new features to a deployed model, refit the scaler on the updated training set. The old scaler's mean and std are invalid for the new distribution, and using stale scaling parameters in production is a silent source of degraded performance.`,
      `One practical trap: when doing cross-validation, fit the scaler inside each fold (on the training fold of that fold), not on the full training set before splitting. Fitting on the full training set before CV leaks fold-level statistics into the validation fold.`,
    ],
    takeaway: `The key insight of feature scaling is that algorithms sensitive to magnitude — gradient descent, distance metrics, regularization — will implicitly weight large-scale features over small-scale ones unless you actively remove the scale difference, which means fitting your scaler on training data only.`,
    checkQuestions: [
      {
        q: `You fit a StandardScaler on your entire dataset (train + test combined) before splitting. What exactly is wrong with this?`,
        a: `When you compute the mean and standard deviation over the full dataset, those statistics are influenced by the test-set values. The mean used to center each feature and the standard deviation used to scale it encode information about the test set distribution. When you then transform the training data using these statistics, the training representation reflects the test set. This is data leakage: the model's training environment contains information that would not be available at the time of training in a real deployment. The effect is that your cross-validation and test-set accuracy are optimistically biased — the model has seen, through the scaler, some statistical properties of examples it is supposedly being evaluated on. The correct approach: fit the scaler on the training fold only, then apply the same fitted scaler to both the training fold and the validation/test set.`,
      },
      {
        q: `A K-Means clustering of customer data with features [age (range 20-80), annual_income (range 20,000-200,000)] produces clusters that are entirely separated by income and ignore age. Why does this happen and how do you fix it?`,
        a: `K-Means computes Euclidean distance between data points. The income feature has a range of 180,000 while the age feature has a range of 60. A difference of 1 year in age contributes 1 unit to the Euclidean distance, while a difference of $1,000 in income contributes 1,000 units — and income ranges are in the tens of thousands. Income dominates the distance computation so completely that age is effectively invisible: two customers of the same income but 30 years apart in age look nearly identical to K-Means. The fix is to standardize both features before clustering: after standardization, each feature has std=1, so a 1-std difference in age and a 1-std difference in income contribute equally to the distance calculation. The clusters will then reflect variation in both features.`,
      },
      {
        q: `You are doing 5-fold cross-validation and you fit a MinMaxScaler on the full training set before the CV loop. What is the consequence?`,
        a: `The MinMaxScaler computes the minimum and maximum of each feature across all 5 folds combined. When the CV loop evaluates fold 1 (folds 2-5 as training, fold 1 as validation), the scaler has already been fit using fold 1 values — so fold 1's min/max values contributed to the scaling parameters. The validation fold transformation therefore reflects statistics derived from the validation examples, which constitutes leakage. In practice, this usually has a small effect (the min/max of one fold does not change dramatically from the full dataset), but the correct approach is to fit the scaler inside the CV loop: fit on the training folds, then transform the validation fold using only the training-fold statistics.`,
      },
      {
        q: `Why does applying StandardScaler to the inputs of a random forest not improve performance, while applying it to a logistic regression typically does?`,
        a: `A random forest learns by finding optimal binary splits on individual features: "is feature X > threshold T?" The answer to this question is identical regardless of whether X is in its original scale or has been standardized — the relative ordering of values is preserved, so the same threshold exists (just at a different numeric value). Scaling does not change which split is optimal, only the numeric value of the threshold. A logistic regression, by contrast, learns a weighted sum of features: the prediction is a function of w1·x1 + w2·x2 + ... + wk·xk. If x1 ranges from 0 to 1 and x2 ranges from 0 to 100,000, gradient descent will update w2 much more slowly (large-scale features have smaller gradients relative to their contribution), and regularization will penalize w1 much more aggressively than w2 for the same effect size. Standardization removes this asymmetry, allowing the optimizer to converge faster and regularization to penalize fairly.`,
      },
    ],
  },
  {
    id: 'class_imbalance',
    title: 'Class Imbalance',
    subtitle: `When 99% of examples are one class, accuracy is a lie — learn the techniques that actually work.`,
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['imbalance', 'SMOTE', 'class weights', 'oversampling', 'undersampling', 'threshold moving'],
    summary: `Class imbalance occurs when the distribution of target class labels in a dataset is highly skewed — for example, 99% of transactions are legitimate and 1% are fraudulent. This is the normal condition for fraud detection, medical diagnosis, anomaly detection, and any rare-event prediction task. The core problem is not statistical but metric-based: a model that predicts "not fraud" for every single transaction achieves 99% accuracy, while being completely useless. Accuracy is a misleading metric when classes are imbalanced; practitioners instead use precision, recall, F1-score, AUC-ROC, and AUC-PR (area under the precision-recall curve, which is especially informative for severe imbalance). Addressing imbalance happens at multiple levels. Oversampling the minority class increases the number of minority examples — either by exact duplication (random oversampling) or by synthetic generation. SMOTE (Synthetic Minority Over-sampling Technique) generates synthetic examples by interpolating between existing minority samples in feature space: pick a minority sample, find its k nearest minority neighbors, and create a new point at a random position along the line between them. This is better than exact duplication because it creates variation rather than repetition. Undersampling the majority class removes majority examples. Random undersampling discards majority examples uniformly; informed undersampling methods like Tomek links remove majority samples that are too close to the minority boundary, cleaning the decision boundary. Class weighting is often the cleanest solution: instead of resampling, directly penalize misclassifying minority samples more heavily by multiplying their gradient contribution by a weight proportional to inverse class frequency. Threshold moving adjusts the decision threshold after training — instead of predicting positive when probability > 0.5, you might use 0.1 to catch more fraud at the cost of more false positives.`,
    keyPoints: [
      `Accuracy is meaningless for imbalanced classes. A model predicting the majority class for every example achieves 99% accuracy on a 99:1 dataset. Always report precision, recall, and F1 (or AUC-PR for severe imbalance) when classes are skewed.`,
      `SMOTE generates synthetic minority samples by interpolating in feature space, not by duplicating rows. The distinction matters: duplicates create no new information and cause overfitting to the exact minority points; synthetic interpolations create a denser minority distribution that is harder to overfit.`,
      `SMOTE has a known failure mode: it generates samples by interpolating between minority points, which can create synthetic samples that cross into majority-class territory when classes overlap. SMOTE-ENN and SMOTE-Tomek variants clean generated samples by removing those that are misclassified by KNN.`,
      `Class weights (the cleanest technique for most cases): setting class_weight="balanced" in sklearn scales each class's contribution to the loss function by the inverse of its frequency. A class that is 10× rarer gets 10× the loss penalty per example. This requires no data modification and interacts cleanly with cross-validation.`,
      `Threshold moving is a post-training technique: after training, instead of classifying positive when predicted probability > 0.5, you lower the threshold to increase recall at the cost of precision. It does not change the model's learned probabilities — only the decision rule applied to them. The optimal threshold depends on the business cost ratio of false positives vs. false negatives.`,
      `Undersampling throws away data, which is expensive when the majority class contains useful distributional information. It works best when the majority class has many near-duplicate examples (redundant data) and the dataset is large enough that discarding majority samples does not cause the model to underfit.`,
      `The resampling-before-splitting trap: if you apply SMOTE to the full dataset before splitting into train/test, synthetic minority samples may appear in both train and test sets. The test set then contains synthetic (non-real) examples and your evaluation is invalid. Always apply resampling inside the training fold only.`,
      `For gradient boosting specifically: most implementations support sample weights natively, which achieves the same effect as class weighting without any resampling. XGBoost's scale_pos_weight parameter and sklearn's sample_weight parameter are cleaner than SMOTE for tabular data.`,
      `The AUC-PR (precision-recall curve area) is more informative than AUC-ROC under severe imbalance. AUC-ROC can be misleadingly high (0.95+) even when a model catches few minority examples, because the ROC curve is dominated by the large number of true negatives.`,
      `Imbalance handling should reflect the business cost function. If missing one fraud case costs $10,000 and a false positive costs $10 in manual review, the optimal decision threshold is not 0.5 — it is the threshold where the expected cost is minimized. Always ask what the real costs of each error type are before choosing a technique.`,
    ],
    takeaway: `The key insight of class imbalance is that the model is not the problem — the evaluation metric and decision threshold are, which means the first fix is almost always switching from accuracy to a cost-sensitive metric and only then deciding whether to resample.`,
    checkQuestions: [
      {
        q: `Your fraud model achieves 99.2% accuracy and your colleague is satisfied. What would you check to determine if this model is actually useful?`,
        a: `First, check the baseline: if 99% of transactions are legitimate, predicting "not fraud" for every transaction yields 99% accuracy. The model needs to beat this trivial baseline on metrics that actually capture fraud detection. Check recall (sensitivity): what fraction of actual fraud cases does the model correctly flag? A model with 99.2% accuracy and 0% recall catches no fraud at all. Check precision: of the cases flagged as fraud, what fraction are actually fraudulent? And compute the AUC-PR (area under the precision-recall curve) which directly measures performance on the minority class. If the model has high accuracy but low recall and low AUC-PR, it is effectively a sophisticated version of "always predict not fraud" and is useless for the actual business purpose.`,
      },
      {
        q: `Why is applying SMOTE to the full dataset before splitting into train and test sets invalid?`,
        a: `SMOTE generates synthetic minority samples by interpolating between real minority samples. If you apply it to the full dataset, some of the synthetic samples will be near real test-set minority examples, because the interpolation uses test-set neighbors. The test set then contains synthetic examples (not real data) and is no longer a valid holdout for estimating real-world performance — you are evaluating the model on made-up data points that are structurally similar to what it trained on. The correct procedure: split first into train and test using real data, then apply SMOTE only to the training set. The test set must always contain only real, unmodified examples so that it represents actual production distribution.`,
      },
      {
        q: `Compare class weighting and SMOTE: when would you choose one over the other for a 50:1 imbalanced tabular dataset?`,
        a: `Class weighting is almost always the first choice for tabular data: it requires no data modification, works natively in most model implementations, interacts cleanly with cross-validation (no risk of resampling-before-splitting leakage), and scales to any imbalance ratio without generating data. SMOTE is preferred when the minority class is genuinely underrepresented in feature space — when there are too few minority examples for the model to learn a good decision boundary, not just that the loss function is unbalanced. For a 50:1 ratio with thousands of minority examples, class weighting is sufficient. For a 50:1 ratio with only 50 minority examples total, SMOTE can help by creating a denser minority region for the model to learn from. For deep learning, class weighting is almost always preferred because SMOTE on high-dimensional or mixed-type tabular data is harder to validate.`,
      },
      {
        q: `You lower the classification threshold from 0.5 to 0.2 and your recall increases from 0.6 to 0.9 but precision drops from 0.8 to 0.3. Is this an improvement?`,
        a: `Whether this is an improvement depends entirely on the relative cost of false negatives versus false positives in the deployment context. A higher recall (0.9 vs 0.6) means catching 50% more actual fraud cases — if each fraud case costs $5,000 uncaught, this is highly valuable. The lower precision (0.3 vs 0.8) means more false positives — if each false positive triggers a $50 manual review, the increase in false positives needs to be justified by the cases caught. The decision cannot be made from these numbers alone without knowing the business cost ratio. In fraud detection, where the cost of missing a fraud is typically much higher than the cost of a false alarm, a threshold of 0.2 may well be the right operating point. The correct approach is to compute the expected cost at each threshold and choose the minimum-cost threshold.`,
      },
      {
        q: `What is the SMOTE failure mode when minority and majority classes heavily overlap in feature space?`,
        a: `SMOTE generates synthetic minority samples by interpolating between existing minority samples along the line connecting a sample to one of its k nearest minority neighbors. When minority and majority classes overlap significantly, some minority samples are surrounded by majority samples, and the synthetic points generated by interpolating between minority samples will fall inside the majority-class region. The model trains on these synthetic minority samples as if they are real minority examples, but at inference time, those feature-space regions produce majority-class predictions — the synthetic samples are misleading. This is why SMOTE-ENN and SMOTE-Tomek variants exist: they apply an undersampling step after SMOTE to remove synthetic samples that are misclassified by k-NN, cleaning the decision boundary and removing the cross-class synthetic contamination.`,
      },
    ],
  },
  {
    id: 'data_splits_and_leakage',
    title: 'Data Splits and Leakage',
    subtitle: `Understand why models that look great in development fail in production — and the exact mistakes that cause it.`,
    difficulty: 'intermediate',
    estimatedMin: 50,
    tags: ['train-test split', 'cross-validation', 'data leakage', 'temporal leakage', 'overfitting'],
    summary: `The fundamental goal of machine learning evaluation is to estimate how well a model will generalize to data it has never seen. This requires a strict separation between the data used to train the model and the data used to evaluate it. The standard pipeline uses three sets: a training set (the model fits its parameters to this), a validation set (hyperparameters are tuned using performance on this), and a test set (a final, held-out estimate of generalization that is touched exactly once). Data leakage is any mechanism by which information from the evaluation set contaminates the training process, causing the model to appear better than it will actually perform in production. Leakage is insidious because it produces models that look spectacular in development and fail in deployment — often silently, without error messages. The most common leakage sources are: preprocessing transformations fitted on the full dataset (scaling, imputation, encoding) that incorporate test-set statistics; temporal leakage in time-series problems, where future information contaminates the model's view of the past; target-derived features that encode the label; and cross-validation splits that do not account for grouping structure (the same patient's data appearing in both train and validation folds). Cross-validation improves over a single train/val split by averaging performance across multiple folds, reducing variance in the estimate. Stratified k-fold maintains the class distribution across folds. Time-series cross-validation (expanding window or sliding window) ensures that the model is always trained on past data and evaluated on future data — a requirement that random splits violate.`,
    keyPoints: [
      `The test set must be touched exactly once, at the very end. Every time you look at test-set performance and adjust anything — hyperparameters, features, architecture — you have implicitly tuned to the test set and it is no longer a valid estimate of generalization.`,
      `Temporal leakage: in a time-series problem, randomly shuffling and splitting creates a situation where the model is trained on data from next week to predict data from last week. In production, you never have future information. Random splits on time-series produce wildly optimistic accuracy estimates.`,
      `The most common preprocessing leakage mistake: fit StandardScaler, MinMaxScaler, or SimpleImputer on X_full before train_test_split. The scaler's mean/std now reflects the test distribution, and the model's training data is colored by test-set statistics.`,
      `Target encoding leakage: computing per-category mean target across the full dataset before splitting means each training row's encoded feature reflects its own label (even slightly). This is circular: the feature contains information about the label it is trying to predict.`,
      `Group leakage: if your dataset has 10,000 rows from 100 patients (100 rows per patient), a random split may put 80 rows from patient 42 in train and 20 rows in validation. The model learns patient 42's patterns in train, then validates on patient 42's patterns — overestimating generalization to new patients.`,
      `Label leakage through derived features: including features that encode the outcome directly (e.g., including "claim_paid" as a feature when predicting "will_claim") or that are derived from the outcome (total revenue as a feature when predicting whether a sale happened) will produce 100% train accuracy and 0% generalization.`,
      `Stratified k-fold is important for classification: it ensures each fold has approximately the same class distribution as the full dataset, preventing a fold from having zero examples of the minority class.`,
      `Time-series cross-validation with an expanding window: fold 1 trains on months 1-6 and validates on month 7. Fold 2 trains on months 1-7 and validates on month 8. This mimics the real deployment scenario where all historical data is used to predict the next period.`,
      `A practical leakage detection heuristic: if your model achieves near-perfect accuracy or a massive jump in performance after adding a specific feature, suspect leakage from that feature. Real-world signals rarely produce 10-20 percentage point gains without explanation.`,
      `The correct cross-validation order: split into folds → for each fold, fit all preprocessing (scaler, imputer, encoder) on the training portion only → transform both train and validation using the train-fitted transformers → train model → evaluate on validation. Preprocessing must happen inside the fold loop, not outside it.`,
    ],
    takeaway: `The key insight of data splits and leakage is that any information that flows from evaluation examples into training — through preprocessing, feature engineering, or splits — creates an optimistic illusion of generalization that evaporates the moment the model hits production.`,
    checkQuestions: [
      {
        q: `You build a model to predict customer churn. You include "support_tickets_after_churn_date" as a feature. The model achieves 98% accuracy. What is the problem?`,
        a: `This is label leakage through a target-derived feature. "Support tickets after churn date" can only be known after the customer has already churned — you are using future information (post-churn behavior) as a feature to predict whether the customer churns. At prediction time (before churn happens), this feature would be unknown. The model achieves 98% accuracy because it has essentially been given the answer. In production, when you try to predict churn for currently active customers, this feature is unavailable (the customer has not yet churned, so there are no post-churn tickets), and the model's accuracy will collapse. The fix is strict temporal cutoffs: only use information available before the prediction date as features.`,
      },
      {
        q: `Why does random train-test splitting fail for time-series forecasting, and what is the correct splitting strategy?`,
        a: `In a time-series problem, the future depends on the past — a model trained to predict sales tomorrow should only use data from today and before. Random splitting shuffles examples across time, creating a situation where a model might be trained on data from March to predict January. In production, January comes before March, so March data would never be available when predicting January. The model learns temporal correlations backward, producing an accuracy estimate that is impossible to achieve in deployment. The correct strategy is a temporal split: train on data from time 0 to T, validate on data from T to T+k. For cross-validation, use an expanding or sliding window: each fold trains on earlier data and evaluates on immediately subsequent data, always maintaining the constraint that training data is older than validation data.`,
      },
      {
        q: `You run 5-fold cross-validation on a medical imaging dataset with 500 patients and 20 images per patient. You get CV accuracy of 94%. You deploy and get 71%. What happened?`,
        a: `Group leakage: a random 5-fold split put different images from the same patient into different folds. A patient might have 16 images in the training fold and 4 in the validation fold. The model learned the specific patterns of each patient's imaging (lighting, scanner calibration, patient-specific anatomy) during training and then "validated" on different images of the same patients it already knew. The 94% CV accuracy measures how well the model generalizes to new images of patients it has already seen — which is much easier than generalizing to new patients entirely. The 71% production accuracy is the true generalization score (to unseen patients). The fix is grouped k-fold splitting: all images of a given patient must stay in the same fold, ensuring validation always uses patients the model has never seen.`,
      },
      {
        q: `List three preprocessing operations that can cause leakage when applied before the train-test split.`,
        a: `First, StandardScaler and MinMaxScaler: fitting on the full dataset computes mean, std, min, max across train and test together, so the training data's normalization reflects test-set statistics. Second, SimpleImputer or KNNImputer: imputing missing values using statistics computed from the full dataset (including test examples) means test-set values influence the fill values used in training. Third, target encoding: computing per-category mean target across the full dataset means each row's encoded feature value reflects all other rows' labels including those in the test set, creating a direct channel for label information to flow into training features. All three must be fit on training data only and then applied (using the training-fit parameters) to the validation and test sets.`,
      },
      {
        q: `What is the correct order of operations for preprocessing inside a k-fold cross-validation loop?`,
        a: `For each fold: (1) Split the training indices from the validation indices according to the fold definition. (2) Using only the training indices, fit all preprocessing transformers — scaler, imputer, encoder. (3) Transform the training data using the fitted transformers. (4) Transform the validation data using the same fitted transformers (fitted on training only — never refit on validation). (5) Train the model on the transformed training data. (6) Evaluate the model on the transformed validation data. The key invariant is that the validation data must always be transformed using parameters derived exclusively from training data. This mimics the production scenario where your deployed model uses a scaler fitted during training to process new incoming data.`,
      },
    ],
  },
  {
    id: 'feature_selection',
    title: 'Feature Selection',
    subtitle: `Reduce dimensionality to fight overfitting, cut training cost, and build models that generalize.`,
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['feature selection', 'curse of dimensionality', 'LASSO', 'RFE', 'mutual information', 'multicollinearity'],
    summary: `Feature selection is the process of identifying which features in a dataset are most relevant to the prediction task and discarding the rest. The motivation is rooted in the curse of dimensionality: as the number of features p grows relative to the number of training examples n, the feature space becomes exponentially sparse. Each new feature adds a dimension, and in high-dimensional spaces, data points become equidistant from each other — the geometric structure that learning algorithms rely on begins to break down. More concretely, a model with more parameters than strictly necessary for the task will overfit: it will learn spurious correlations in the training set that do not hold in the test set. Feature selection methods fall into three families. Filter methods evaluate each feature independently using statistical measures: correlation with the target, mutual information (measuring statistical dependence without assuming linearity), or chi-squared test for categorical features vs. categorical targets. They are fast but ignore interactions between features. Wrapper methods evaluate subsets of features by actually training and evaluating a model: Recursive Feature Elimination (RFE) starts with all features, trains a model, removes the lowest-importance feature, and repeats until the desired number remains. Wrappers are expensive but capture feature interactions. Embedded methods perform selection as part of model training: LASSO (L1 regularization) penalizes the sum of absolute coefficient values, which has the geometric property of driving some coefficients to exactly zero — effectively eliminating those features. Tree feature importance provides embedded selection in tree-based models. The Variance Inflation Factor (VIF) detects multicollinearity: when one feature is a near-linear combination of others, the model's coefficients become unstable.`,
    keyPoints: [
      `The curse of dimensionality in practical terms: to maintain statistical significance, you roughly need exponentially more samples as you add features. A rule of thumb for regression is ~10 examples per feature. A 200-feature dataset needs ~2,000 examples before it starts to generalize reliably.`,
      `Filter methods (correlation, mutual information) evaluate features in isolation. A feature with zero marginal correlation to the target might still be critical when combined with another feature — filter methods will incorrectly remove it. This is their fundamental limitation.`,
      `Mutual information is a more powerful filter metric than Pearson correlation because it captures non-linear statistical dependence. A feature with a U-shaped relationship to the target has zero Pearson correlation but high mutual information.`,
      `RFE (Recursive Feature Elimination) fits a model repeatedly, each time removing the least important feature. For a model with p features, this requires p model fits — expensive, but the resulting feature subset is aware of inter-feature interactions in a way that filter methods cannot achieve.`,
      `LASSO (L1 regularization) shrinks coefficients toward zero by penalizing their absolute values. Unlike L2 (Ridge), which pushes coefficients small but rarely to exactly zero, L1 creates sparse solutions with some coefficients exactly at zero. Increasing the regularization strength increases sparsity, effectively performing feature selection.`,
      `VIF (Variance Inflation Factor) for feature X measures how much of X's variance is explained by other features. A VIF > 10 indicates severe multicollinearity. The consequence: the coefficient for X becomes unstable (high variance across different training sets) and its sign may even flip — the model cannot distinguish which of the correlated features deserves the credit.`,
      `Tree feature importance has a known bias: features with more unique values (high cardinality) tend to receive higher importance scores because they offer more split points. Use permutation importance (randomly shuffle the feature and measure the performance drop) for a more reliable importance estimate.`,
      `Selecting features based on test-set performance is leakage. Feature selection must be performed using only training data (ideally inside each cross-validation fold) to avoid overfitting the feature set to the evaluation set.`,
      `When regularization is available (LASSO, Ridge, Elastic Net, or tree models with max_depth constraints), explicit feature selection is often unnecessary — the regularization handles the bias-variance tradeoff automatically. Explicit feature selection is most valuable when you need interpretability, reduced inference cost, or a stable feature set that can be audited.`,
      `Forward and backward stepwise selection are wrapper methods that start from empty or full feature sets and greedily add or remove one feature at a time. They are computationally lighter than exhaustive search but may miss the globally optimal subset because greedy choices cannot be undone.`,
    ],
    takeaway: `The key insight of feature selection is that adding features always increases model capacity and often increases overfitting risk, so selection is really a bias-variance tradeoff decision — and performing it inside the training process (not on the test set) is the only valid approach.`,
    checkQuestions: [
      {
        q: `You filter features by Pearson correlation with the target and retain only the top 20. Your model performs worse than with all 100 features. What most likely went wrong?`,
        a: `Pearson correlation measures linear dependence between a single feature and the target, evaluated in isolation. Features with weak marginal correlation may still be essential when combined with other features — they capture interaction effects that neither feature reveals alone. For example, "time_of_day" might have near-zero correlation with purchase probability overall, but when combined with "day_of_week", it powerfully predicts conversion. Filter methods cannot detect this because they evaluate features one at a time. Additionally, features with non-linear relationships to the target will be underranked by Pearson correlation: a quadratic or U-shaped relationship has near-zero Pearson correlation but strong predictive value. The fix is to use mutual information (which captures non-linear dependence) or wrapper methods (which evaluate feature subsets via model performance, capturing interactions).`,
      },
      {
        q: `Why does LASSO shrink some coefficients to exactly zero while Ridge (L2) rarely does, even though both add a penalty?`,
        a: `The geometry of the constraint region differs between L1 and L2. L2 regularization adds a penalty proportional to the sum of squared coefficients, which is a smooth sphere in coefficient space — the minimum of the penalized objective almost never hits a coordinate axis, so coefficients are pushed toward (but not to) zero. L1 regularization adds a penalty proportional to the sum of absolute values, which is a diamond (or hypercube) in coefficient space. The corners of this diamond lie on the coordinate axes (where exactly one coefficient is non-zero and the rest are zero). The optimization landscape of a loss function intersects the L1 constraint region at these corners, producing exact zeros. This is why L1 effectively performs feature selection while L2 produces small but non-zero coefficients. Elastic Net (a mixture of L1 and L2) provides sparsity (from L1) while stabilizing the selection among correlated features (from L2's grouping effect).`,
      },
      {
        q: `You compute feature importance from a gradient boosted tree and find that "customer_id" is the second most important feature. What has happened?`,
        a: `Tree feature importance (measured by total impurity reduction across all splits) is biased toward high-cardinality features — features with many unique values offer more potential split points. "Customer_id" is a unique identifier with cardinality equal to the number of rows. The tree can use it to memorize the training set: split on customer_id = 12345 to perfectly isolate a single customer's rows. This produces large impurity reductions at training time but generalizes to zero new customers at test time. Customer_id is essentially an index key with no predictive signal for unseen data. The fix: remove identifier columns before training, or use permutation importance, which measures the actual performance degradation from shuffling a feature rather than the training-time impurity reduction.`,
      },
      {
        q: `A linear model has two features that are 0.97 correlated (e.g., "height_cm" and "height_inches"). What specific failure mode does this cause and how do you fix it?`,
        a: `When two features are highly correlated, they both carry nearly the same information. The model can achieve the same prediction by assigning a large positive weight to one and a large negative weight to the other, or any linear combination of the two — there are infinitely many coefficient combinations that produce identical predictions. The normal equations for linear regression become numerically near-singular: small changes in the training data cause large swings in the estimated coefficients. This is multicollinearity. Symptoms: high VIF (likely >50 for 0.97 correlation), unstable coefficients across bootstrap samples, inability to interpret which feature is "important." The fix: (1) drop one of the correlated features (height_cm and height_inches carry identical information); (2) apply Ridge regression (L2), which stabilizes coefficients under collinearity by penalizing their magnitude; or (3) create a single combined feature (their average or principal component).`,
      },
    ],
  },
  {
    id: 'distribution_shift',
    title: 'Distribution Shift',
    subtitle: `The core assumption of supervised learning — train and deploy distributions match — is almost always violated in production.`,
    difficulty: 'advanced',
    estimatedMin: 55,
    tags: ['distribution shift', 'covariate shift', 'concept drift', 'label shift', 'model monitoring', 'KS test'],
    summary: `Every supervised machine learning model is trained under one fundamental assumption: that the distribution of data at prediction time matches the distribution of data during training. This assumption — called the i.i.d. assumption (independent and identically distributed) — is almost always violated to some degree in real deployments. Distribution shift is the general term for when this assumption fails. There are three distinct mechanisms. Covariate shift occurs when the distribution of input features P(X) changes between training and deployment, but the conditional distribution of the label given features P(Y|X) remains stable — user demographics shift, but the same demographics still predict the same outcomes. Concept drift occurs when P(Y|X) changes — the relationship between features and the target itself changes, as when a new fraud pattern emerges that looks superficially like legitimate transactions. Label shift (also called prior probability shift) occurs when P(Y) changes — fraud rate goes from 1% to 5% — but P(X|Y) stays constant. Each requires different detection and correction strategies. Detecting shift requires monitoring: comparing feature distributions between training time and production using statistical tests. The Kolmogorov-Smirnov (KS) test measures whether two univariate distributions are drawn from the same underlying distribution. Population Stability Index (PSI) measures the shift in a distribution across time using information-theoretic divergence. Models degrade silently under shift: they continue making predictions with high confidence even when the real-world accuracy has collapsed, because confidence is a function of the model's learned parameters, not the match between current data and training data.`,
    keyPoints: [
      `Covariate shift (P(X) changes, P(Y|X) stable) is the most common type and can often be corrected by importance weighting: up-weight training examples that resemble the production distribution and down-weight those that do not. The weights are estimated by training a classifier to distinguish training from production examples.`,
      `Concept drift (P(Y|X) changes) cannot be corrected by reweighting old training data — the old labels are simply wrong under the new relationship. The only fix is fresh labeled data from the current distribution and retraining, or using an adaptive model that continuously updates.`,
      `Silent degradation is the most dangerous aspect of distribution shift: a model making predictions on shifted data outputs high-confidence scores (because confidence is a function of learned weights, not data match), so the API call succeeds and no alarm fires. Degradation is only visible in performance metrics, which require ground-truth labels — which often arrive with a lag.`,
      `The PSI (Population Stability Index) buckets a feature's distribution into deciles and computes a weighted divergence between training and production distributions. PSI < 0.1 indicates stable distribution; 0.1-0.2 is moderate shift warranting investigation; > 0.2 is severe shift suggesting retraining.`,
      `Feature drift monitoring should be part of every production ML system. The minimum viable monitoring setup: track mean, standard deviation, null rate, and a histogram or percentile distribution for each feature, and alert when the KS test p-value drops below 0.05 on a rolling window.`,
      `Label shift can be corrected without retraining using the BBSE (Black Box Shift Estimation) method: estimate the change in P(Y) by looking at the model's output distribution, then reweight the model's predicted probabilities to account for the new class prior. This works when P(X|Y) is stable.`,
      `Concept drift has two subtypes: sudden drift (a discrete event changes the relationship, like a policy change) and gradual drift (slow continuous change over months). Detection methods differ: sudden drift shows up in a sharp performance drop; gradual drift requires comparing model performance over long windows.`,
      `Retraining schedules (retrain monthly, retrain weekly) are a blunt tool. A better approach is trigger-based retraining: monitor PSI or KS statistics on a rolling window and trigger retraining when shift exceeds a threshold. This avoids unnecessary retraining when the distribution is stable.`,
      `The data freshness trap: using training data from 2 years ago to predict on today's users assumes 2 years have not changed the user population or their behaviors. In high-velocity domains (fraud, social media, finance), distribution shifts can happen in days.`,
      `Importance weighting under covariate shift: train a density ratio estimator (logistic regression predicting "is this example from train or production?") and use the predicted probability as an inverse weight in training. High weight goes to training examples that look like production; low weight to training examples unlike anything in production.`,
    ],
    takeaway: `The key insight of distribution shift is that model accuracy is measured against the training distribution, not the production distribution, which means a high-accuracy model can be silently wrong in production from day one if the distributions do not match.`,
    checkQuestions: [
      {
        q: `Your fraud model was trained in 2022. In 2024, fraudsters adopt a new technique that makes fraudulent transactions look like legitimate ones. What type of shift is this and can it be fixed without new labels?`,
        a: `This is concept drift: the conditional relationship P(Y|X) has changed. In 2022, the feature patterns that predicted fraud (large transactions to new merchants, unusual geographies) were valid. In 2024, the new fraud technique mimics legitimate transaction patterns, so the same feature values now have a different label. This cannot be corrected by reweighting old training data — the 2022 labels are simply wrong for describing the 2024 relationship between features and fraud. The only fix is fresh labeled data: you need examples of the new fraud pattern, labeled by human reviewers or rule-based systems, and a retraining or fine-tuning step. Covariate shift (P(X) changes) can sometimes be corrected by importance weighting without new labels, but concept drift cannot.`,
      },
      {
        q: `Describe what happens to a model's confidence scores under distribution shift, and why this makes shift especially dangerous.`,
        a: `A model's confidence score is determined by its learned parameters applied to the input features — it is a function of the model's internal state, not of whether the input is representative of the training distribution. Under distribution shift, the model receives inputs it has never encountered before, but its sigmoid or softmax output will still produce a number between 0 and 1. Commonly, shifted inputs land in regions of feature space where the model produces high-confidence predictions (because they resemble the boundaries of known classes or because the model generalizes confidently outside its training manifold). The result is that the model continues to output confident predictions even as its accuracy collapses. Unlike a database query that errors on invalid input, the model silently returns a wrong answer with 95% confidence. This makes shift dangerous: there is no automatic error signal. Monitoring requires external ground truth labels (with their associated time lag) or sophisticated distribution-level tests.`,
      },
      {
        q: `What is the difference between covariate shift and concept drift, and why does the distinction determine whether you can avoid retraining?`,
        a: `Covariate shift: P(X) changes (the input feature distribution shifts) but P(Y|X) stays the same (given the same inputs, the correct label is unchanged). Example: your training data was 80% urban users, but your production users are 60% rural — but rural users with the same feature profile still convert at the same rate. Because P(Y|X) is unchanged, your old labeled training data is still correct — it just underrepresents the current input distribution. This can be corrected by importance weighting: up-weight training examples that resemble production, down-weight those that do not, without collecting new labels. Concept drift: P(Y|X) changes — the same feature values now predict different outcomes. Old training labels are wrong under the new relationship. Importance weighting on wrong labels produces a wrong model. New labels are required.`,
      },
      {
        q: `You run a KS test comparing the training and production distributions of your top 5 features and find p < 0.01 for one feature. What does this mean and what should you do?`,
        a: `A p-value < 0.01 on the KS test means you can reject the null hypothesis (that training and production distributions for this feature are drawn from the same distribution) with 99% confidence. The feature's distribution has statistically significantly shifted between training time and production. The next steps: (1) investigate the magnitude of shift, not just statistical significance — with large samples, even trivial shifts become statistically significant. Use PSI to measure practical magnitude: PSI > 0.1 warrants concern, PSI > 0.2 requires action. (2) Investigate the cause: is this a data collection change (a bug introduced a null imputation that changed the distribution?), a real-world change (a new user cohort?), or seasonal variation? (3) Check whether downstream model performance has degraded. If performance is still on target, the shift may not be impacting predictions. If performance has degraded, trigger retraining or rollback.`,
      },
      {
        q: `A model deployed in January shows 89% AUC. By June, AUC has drifted to 78%. Feature distribution monitoring shows stable PSI across all features. What type of shift does this suggest?`,
        a: `Stable feature distributions (low PSI) but degraded model performance (AUC drop) is the signature of concept drift: P(X) has not changed (hence stable PSI on features) but P(Y|X) has changed (hence degraded accuracy). The model's learned mapping from features to labels was calibrated to the January relationship — that relationship has since changed. Common causes: seasonal shifts in user behavior (January vs. June shopping patterns differ), competitive changes (a competitor launched a product that changed which users are motivated to convert), policy changes (a new pricing model changed what features predict churn), or external events (a regulatory change modified what fraud looks like). The fix is new labeled data from the post-shift period and retraining. Feature monitoring alone (PSI) is insufficient — you also need to monitor model output distribution and track prediction accuracy with ground-truth labels.`,
      },
    ],
  },
  {
    id: 'data_augmentation',
    title: 'Data Augmentation',
    subtitle: `Artificially expand your training distribution by adding realistic variations — but only ones that preserve the label.`,
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['augmentation', 'SMOTE', 'image augmentation', 'mixup', 'text augmentation', 'back-translation'],
    summary: `Data augmentation is the practice of artificially expanding the training dataset by creating modified versions of existing examples. The underlying principle is that a model should be invariant to certain transformations of the input — a photo of a cat is still a cat whether it is flipped horizontally, slightly cropped, or adjusted in brightness. By training on augmented versions of the data, the model is explicitly taught these invariances rather than having to learn them from a potentially insufficient volume of natural variation in the training set. The key constraint is that every augmentation must preserve the label: a horizontally flipped image of a cat is still a cat; a sentence with a synonym substituted still has the same sentiment. Augmentation strategies are domain-specific. For images: geometric transforms (flip, rotate, crop, zoom), color-space transforms (brightness, contrast, saturation), and noise injection (Gaussian noise, dropout of pixels). Advanced image techniques include Mixup (interpolating both images and their labels: the new image is a weighted average of two training images, and the label is the same weighted average of their one-hot labels) and CutMix (pasting a rectangular region from one image into another, with a label proportional to the pasted area). For text: synonym replacement, random insertion/deletion of words, back-translation (translate to French, then back to English — produces natural paraphrase variation). For tabular data: Gaussian noise injection adds small random perturbations to numeric features, and SMOTE generates synthetic minority samples. Augmentation is most effective when the model overfits due to insufficient data — it is less useful when the dataset is large and the bottleneck is model capacity.`,
    keyPoints: [
      `The label-preserving constraint is the non-negotiable rule of augmentation. Rotating an "8" by 180 degrees produces a valid "8" — still label-preserving. Rotating a "6" by 180 degrees produces a "9" — label-violating. The domain expert must verify which transformations are label-safe.`,
      `Mixup is a label-smoothing augmentation: the mixed image has label y = λ·y1 + (1-λ)·y2, where λ is the mixing ratio. The model trains to output a soft mixture of the two original labels, which acts as a strong regularizer and improves calibration. Mixup works because it forces the model to have linear behavior between training examples.`,
      `Back-translation for text augmentation: translate a sentence from English to French, then back to English with a different translation model. The result is a paraphrase with the same meaning and label but different word choice and phrasing. This is one of the most effective text augmentation strategies because it produces grammatically natural variations.`,
      `Augmentation during training, not preprocessing: augmentations (especially random crops, flips, and color jitter) should be applied on-the-fly during each training epoch. If applied once as a preprocessing step, the model sees the same augmented versions repeatedly and memorizes them instead of learning invariance.`,
      `Augmentation does not help when the model is underfitting. If training accuracy is already low, the model has not yet captured the signal in the original data — adding more variations makes the problem harder without fixing the underlying capacity issue. Augmentation helps with high variance (overfitting), not high bias (underfitting).`,
      `Test-time augmentation (TTA): at inference time, create multiple augmented versions of each test example, run them through the model, and average the predictions. This can improve accuracy by 1-3 percentage points by reducing the variance of a single prediction.`,
      `Tabular augmentation has limited evidence: Gaussian noise injection helps in small datasets, but unlike images, there is no obvious invariance to exploit in tabular data (there is no equivalent of "a table row is valid whether features are listed in any order"). SMOTE is the most widely used tabular augmentation but specifically targets class imbalance rather than general regularization.`,
      `Augmentation magnitude matters: too much augmentation (extreme rotations, radical color changes) can create samples that are no longer plausible instances of the class, effectively adding noise to the training set. The augmentation strength should be tuned like a hyperparameter.`,
      `Domain adaptation via augmentation: when you have labeled source data and unlabeled target data, augmenting the source data with transformations that bring it closer to the target domain is a form of unsupervised domain adaptation. This is related to but distinct from standard augmentation for regularization.`,
    ],
    takeaway: `The key insight of data augmentation is that it teaches a model the invariances relevant to your problem by example — but those invariances must be chosen by a human who understands the domain, because the model cannot distinguish between label-preserving and label-violating transformations on its own.`,
    checkQuestions: [
      {
        q: `You are training a digit recognition model and augment by rotating all training images up to 45 degrees. Performance degrades. What went wrong?`,
        a: `Many handwritten digits are not rotationally invariant to large angles. A "6" rotated 180 degrees looks like a "9." A "9" rotated 90 degrees looks ambiguous. A "1" rotated 45 degrees is still a "1" but an unusual one. By augmenting with 45-degree rotations, you introduced label-violating samples: images labeled "6" that visually look like "9" to any human. The model is trained with conflicting supervision — the same visual pattern receives different labels depending on whether it is the original or an augmented version. The rule is that augmentations must be label-safe for every class, not just for some. Modest rotations (up to 10-15 degrees) are typically label-safe for digit recognition; 45-degree rotations are not.`,
      },
      {
        q: `What is Mixup augmentation and why does it act as a regularizer?`,
        a: `Mixup constructs new training examples by linearly interpolating between two randomly selected training examples: new_x = λ·x1 + (1-λ)·x2 and new_y = λ·y1 + (1-λ)·y2, where λ is drawn from a Beta distribution. The training example is literally a weighted average of two images (pixels blended) and the label is a weighted mixture of the two label vectors. It acts as a regularizer because it forces the model to predict a smooth interpolation of labels in the regions between training examples. Without Mixup, the model is only supervised at discrete training points and can behave arbitrarily in between. Mixup constrains the model to be approximately linear between pairs of training examples, which reduces overfitting and improves calibration (the model's confidence is better aligned with its actual accuracy).`,
      },
      {
        q: `When does augmentation help and when does it not? Give a scenario for each.`,
        a: `Augmentation helps when the model overfits: a small dataset where the model achieves near-perfect training accuracy but poor validation accuracy is exactly the regime where augmentation — by creating more diverse training examples — reduces variance. Example: a medical imaging classifier with 500 training images per class; random crops, flips, and color jitter dramatically expand the effective training set. Augmentation does not help when the model underfits: if training accuracy is 60% (the model has not yet captured the signal in the original data), adding more variations of already-unlearnable data makes the problem harder without fixing the capacity or training problem. Example: using a linear model on image pixel features — adding augmented images will not fix the fundamental insufficiency of the representation. The diagnostic is to compare training and validation accuracy: high train, low validation → augmentation helps; low train, low validation → fix the model first.`,
      },
      {
        q: `Why should augmentation transformations be applied on-the-fly during training rather than pre-computed and saved to disk?`,
        a: `If augmentations are pre-computed (applied once before training), the model sees the same augmented versions of each image at every epoch. In epoch 1, it sees "cat_image_1_rotated_23deg." In epoch 5, it sees exactly the same "cat_image_1_rotated_23deg" again. After enough epochs, the model memorizes these specific augmented versions just as easily as it would memorize the originals — no regularization benefit is achieved. On-the-fly augmentation applies a random transformation at each training step: epoch 1 might use rotation=23deg; epoch 5 might use rotation=7deg for the same image. Each time the model sees an image, it sees a freshly randomized variant. Over thousands of epochs, the model is effectively trained on a near-infinite variety of augmented versions, which forces it to learn the underlying invariances rather than memorizing specific pixel arrangements.`,
      },
    ],
  },
  {
    id: 'data_versioning_and_pipelines',
    title: 'Data Versioning and Pipelines',
    subtitle: `Models are only reproducible if both code and data are versioned — and production ML breaks when training and serving compute features differently.`,
    difficulty: 'advanced',
    estimatedMin: 40,
    tags: ['DVC', 'feature store', 'data versioning', 'training-serving skew', 'pipeline orchestration', 'reproducibility'],
    summary: `A machine learning model is a function of both code and data — yet most software engineering version control practices track only code. Data Version Control (DVC) extends the Git workflow to datasets: it tracks pointers to datasets stored in remote storage (S3, GCS, local filesystem) and records which exact dataset version was used to train each model. Without data versioning, reproducing a past model or debugging a performance regression requires knowing not just what code was used but what data — information that is rarely preserved without explicit tooling. The training-serving skew problem is one of the most common and costly bugs in production ML. It occurs when the feature computation logic used at training time differs from the logic used at inference time. Example: the training pipeline computes "days since last login" using a batch SQL query that was run at a specific timestamp; the serving system computes the same feature in real time from a live database. Subtle differences — timezone handling, null treatment, rounding — produce different numeric values for the same raw data, and the model receives inputs it was never trained on. Feature stores are the architectural solution: they centralize feature computation into a single canonical implementation that is shared between the training pipeline and the inference pipeline. Both training and serving call the same feature computation code, guaranteeing that training and serving features are computed identically. Production data pipelines fail for reasons that code pipelines do not: upstream data sources change schemas without warning, new null values appear in previously non-nullable columns, record volumes change by orders of magnitude, and tables are populated late. Making pipelines observable — with data quality checks, row-count assertions, schema validation, and alerting — is the difference between a pipeline that fails silently (corrupting a model) and one that fails loudly (raising an alert and blocking the retraining run).`,
    keyPoints: [
      `DVC (Data Version Control) stores a lightweight .dvc file that records a pointer (content hash) to the actual dataset stored in remote storage. The .dvc file is committed to git, making each git commit implicitly version the dataset used at that time. Reverting to a past git commit gives you both the training code and the exact dataset used for that run.`,
      `Training-serving skew is responsible for a disproportionate share of production ML incidents. The root cause is almost always the same: feature computation is implemented twice (once in Python for training, once in SQL or Java for serving) and the two implementations drift apart. The fix is a feature store: implement features once, share the implementation.`,
      `Feature stores have two components: an offline store (a data warehouse or Parquet lake for training) and an online store (a low-latency key-value store like Redis or DynamoDB for serving). At training time, features are read from the offline store. At serving time, the same feature values are read from the online store. The computation that writes to both stores uses the same logic.`,
      `Point-in-time correctness is a feature store concept: when assembling training data for a model that predicts on a given date, you must use the feature values as they existed at that exact date — not the feature values as they exist today (which reflect subsequent events). A feature store with time-travel support enforces this automatically.`,
      `Pipeline observability minimum viable set: (1) row count checks (if yesterday had 1M rows and today has 100K, alert), (2) schema validation (fail loudly if a new nullable column appears or a column changes type), (3) null rate monitoring (if a feature's null rate jumps from 2% to 25%, alert), (4) staleness checks (if the pipeline has not run in 24 hours, alert).`,
      `Schema drift is a silent pipeline killer: an upstream team renames a column, and the downstream training pipeline now reads nulls where it expected values. The pipeline does not error (reading a null is valid); it silently produces a model trained on a null-imputed version of a feature that should not be null.`,
      `Experiment tracking (MLflow, Weights & Biases) complements data versioning by recording hyperparameters, metrics, and model artifacts for each training run. Together, data versioning + experiment tracking provides full reproducibility: given a past training run's ID, you can recover the exact code, data, parameters, and outputs.`,
      `Data contracts are a preventive tool: formal, machine-checkable specifications of what an upstream data source guarantees to deliver (column names, types, null rates, value ranges, update frequency). When an upstream source violates its contract, the downstream pipeline fails at a schema-validation step rather than silently producing corrupted features.`,
      `Backfilling historical features for a feature store requires careful attention to point-in-time correctness. If you backfill "average purchase value in the last 30 days" for all historical dates using today's data, you will include future purchases in the historical average — a form of temporal data leakage applied to the feature store.`,
    ],
    takeaway: `The key insight of data versioning and pipelines is that a model trained on unknown data with features computed by unknown logic is not reproducible and not debuggable — which means data and feature logic must be versioned and centralized before you can trust your production ML system.`,
    checkQuestions: [
      {
        q: `A model trained and evaluated in your offline pipeline shows 91% AUC. After deployment, production AUC is 77%. No distribution shift is detected in features monitored at prediction time. What is the most likely cause?`,
        a: `The most likely cause is training-serving skew: the features used during serving are computed differently from the features used during training. Monitoring features at prediction time confirms their distribution is stable (ruling out distribution shift), but if the feature values themselves are wrong — computed with different logic, different null handling, different time windows — the model receives inputs it was never trained on. Common examples: the training pipeline computed "days since last purchase" using a batch SQL snapshot with a specific cutoff time, but the serving system computes it from a live database that includes transactions from the current day. Or timezone handling differs (UTC in training, local time in serving). Or a feature is computed from a different table version in production. The fix is to audit the feature computation code in both pipelines and compare output values for the same raw input on a held-out sample.`,
      },
      {
        q: `You need to reproduce a model trained 8 months ago to debug a regression. You have the training code at the exact commit, but you cannot reproduce the results. What is missing from your MLOps setup?`,
        a: `Data versioning is missing. The training code alone is insufficient to reproduce a model — you also need the exact dataset used to train it. Eight months ago, the training set had a specific set of rows, feature values, and label assignments. Since then, the upstream data has likely changed: new rows have been added, old rows may have been updated or deleted, feature engineering pipelines may have been modified. Without DVC (or equivalent data versioning), you cannot recover the exact dataset state that existed 8 months ago. The fix for the future: use DVC to commit a .dvc pointer file alongside each model training run, so the dataset version is permanently associated with the code version. With experiment tracking (MLflow), also record the dataset version as a run parameter.`,
      },
      {
        q: `What is point-in-time correctness in a feature store and what goes wrong without it?`,
        a: `Point-in-time correctness ensures that when you assemble a training example for a model predicting on date T, you use the feature values as they existed at time T — not as they exist when you assemble the training data (which could be months later). Without it, you commit temporal leakage: for a customer churn model, if you use today's "30-day purchase count" to label a training example from 6 months ago, you include purchases from the 5 months after the label date. The feature value is contaminated by future information. A feature store with point-in-time support maintains the full history of feature values with timestamps, and the training data assembly query specifies "give me the feature value for customer X as of date T" — not "give me the current value." Without this, every historical training example is contaminated by future data, producing inflated accuracy that collapses in production.`,
      },
      {
        q: `An upstream team renames a column from "transaction_value" to "txn_amount" without notifying your team. Your retraining pipeline runs successfully with no errors. Six weeks later, you notice model performance has dropped. What happened and how would a data contract have prevented it?`,
        a: `The pipeline read the renamed column as null (the old column name no longer exists in the table, and many SQL engines return nulls for missing columns rather than erroring). The imputer filled in the mean of a now-null column — likely 0 or the column's historical mean from the imputer fitted at the previous training run. The model was retrained on a version of the transaction value feature that was entirely imputed noise, producing silently degraded performance six weeks before it was noticed. A data contract would have prevented this: it specifies that the upstream table must contain a column named "transaction_value" of type FLOAT with a null rate below 1%. The data validation step runs this contract check before any feature computation, and when the column is absent, the pipeline fails immediately with a descriptive error — blocking the corrupted retraining run and alerting the team the day it happened, not six weeks later.`,
      },
    ],
  },
]

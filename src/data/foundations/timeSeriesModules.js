export const TIME_SERIES_MODULES = [
  {
    id: 'stationarity',
    interactiveId: 'stationarity_viz',
    title: 'Stationarity & Differencing',
    subtitle: 'Unit root tests, spurious regression, integration order, cointegration',
    difficulty: 'foundational',
    estimatedMin: 35,
    tags: ['stationarity', 'unit-root', 'differencing', 'cointegration', 'ADF', 'KPSS'],
    summary: `Regress two independent random walks against each other and you'll get R² near 1 and t-statistics in double digits — not because they're related, but because both are trending. That's spurious regression, and it invalidates every downstream conclusion. It's the reason stationarity matters: non-stationary series have growing variance and shifting means, so the statistical tests that assume constant moments produce completely unreliable results. The ADF and KPSS tests tell you whether you have a unit root; differencing removes trends; seasonal differencing removes periodicity. The flip side of non-stationarity is cointegration — two non-stationary series can share a long-run equilibrium whose spread is stationary, and error correction models exploit that structure rather than discarding it.`,
    keyPoints: [
      `Two random walks regressed against each other will look strongly related — R² > 0.5, |t| > 2 — with zero true relationship. The tell is Durbin-Watson near 0: residuals are nearly perfectly autocorrelated, which is the signature of a spurious regression between two non-stationary series. OLS standard errors assume independent residuals; when they're serially correlated, standard errors are severely underestimated and all inference collapses. This is not a small-sample problem — it gets worse with more data.`,
      `Weak (covariance) stationarity requires three properties: constant mean E[Y_t] = μ, constant variance Var(Y_t) = σ², and autocovariance Cov(Y_t, Y_{t-k}) = γ(k) that depends only on lag k, not on t. A random walk Y_t = Y_{t-1} + ε_t violates all three — variance grows as tσ², so the series wanders without bound. One difference gives ΔY_t = ε_t, which is stationary. The I(d) notation means d differences are needed to achieve stationarity.`,
      `ADF test has H₀: unit root present, H₁: stationary. Includes lags of ΔY_t to purge residual autocorrelation. Critical values are non-standard (more negative than t-distribution). The test has low power in small samples and often fails to reject even when the series is trend-stationary. KPSS flips the null: H₀ is stationary, H₁ is unit root. Running both together resolves ambiguity: ADF rejects and KPSS doesn't reject → stationarity. ADF doesn't reject and KPSS rejects → unit root. Both rejecting → possible fractional integration or local non-stationarity.`,
      `Trend-stationary and difference-stationary series look similar in plots but require different treatment. A trend-stationary series (Y_t = α + βt + ε_t) should be detrended — regress out the linear trend and model residuals. A difference-stationary series needs differencing. The error matters: detrending a difference-stationary series leaves residuals that still have a unit root. Differencing a trend-stationary series removes real signal and induces unnecessary MA structure.`,
      `Cointegration is the productive flip side of non-stationarity. Two I(1) series X_t and Y_t are cointegrated when there exists β such that Y_t − βX_t is I(0). The spread is stationary even though the individual series are not. Economically, the series share a long-run equilibrium. The Engle-Granger test runs ADF on the residuals of a regression of Y on X. The Johansen trace/max-eigenvalue test handles multiple cointegrating vectors simultaneously.`,
      `Error Correction Model: ΔY_t = α(Y_{t-1} − βX_{t-1}) + short-run dynamics. The error-correction term α(Y_{t-1} − βX_{t-1}) pulls the system back toward equilibrium; α < 0 ensures mean reversion. If you difference cointegrated series without including the error-correction term, you throw away the long-run relationship. The short-run model (first differences) tells you about dynamics; the error-correction term tells you about the equilibrium.`,
      `Stationarity is not a one-time check at model training time. A series that was stationary from 2015-2019 can develop a unit root after a structural shift (new competitor, regulatory change, macroeconomic shock). Rolling ADF tests with a sliding window, CUSUM tests on residuals, or online variance monitoring catch this. A single ADF on historical data at training time is insufficient for a deployed model — stationarity must be monitored continuously in production.`,
    ],
    checkQuestions: [
      {
        q: 'You regress daily revenue on daily temperature for 3 years and get R² = 0.72 with t-stat = 18. The DW statistic is 0.12. What is wrong and how do you fix it?',
        a: 'DW ≈ 0.12 means residuals are nearly perfectly positively autocorrelated — a hallmark of spurious regression between two non-stationary series. Both revenue and temperature likely have trends or unit roots. OLS standard errors are severely underestimated (White noise assumption violated), making the t-stat meaningless. Fix: (1) Test both series for stationarity with ADF/KPSS. (2) If both I(1), test for cointegration. (3) If not cointegrated, work in first differences: regress ΔRevenue on ΔTemperature. (4) If cointegrated, use an ECM. The R² of 0.72 is an artifact of shared time trend absorption, not genuine predictive power.'
      },
      {
        q: 'ADF says your series is stationary but KPSS also rejects its null. What does this mean and what do you do?',
        a: 'This is a conflicting signals situation. ADF rejects H₀ (unit root) → evidence for stationarity. KPSS rejects H₀ (stationarity) → evidence for unit root. Both rejecting suggests the series may be: (1) fractionally integrated I(d) with 0 < d < 1 — neither purely stationary nor I(1), (2) locally stationary but with structural breaks that resemble non-stationarity, or (3) the sample is too short for reliable inference. Actions: test for structural breaks (Zivot-Andrews ADF allows one break), try ARFIMA (fractional differencing), or apply regime-change tests. Do not blindly difference — over-differencing introduces unit roots in the MA component.'
      },
      {
        q: 'You have two financial time series (stock price and its futures contract price) that are both I(1). How do you decide whether to model them separately in first differences or jointly?',
        a: 'Test for cointegration. Spot and futures prices of the same asset are theoretically cointegrated by no-arbitrage — their spread (basis) should be mean-reverting. Run the Johansen trace test: if you find one cointegrating vector, use a Vector Error Correction Model (VECM). The spread Y_t − βX_t is the error-correction term. Modelling each in first differences independently ignores the long-run equilibrium and loses information. In production: the ECM coefficient α quantifies speed of mean reversion — useful for pairs trading strategies. If α is near zero, cointegration is nominal and not exploitable.'
      },
      {
        q: 'Your revenue series passes ADF stationarity test. You fit an ARIMA(1,0,1) and the residuals look clean. Six months later the model performance degrades sharply. What likely happened and how do you detect it earlier?',
        a: 'The most likely cause is a structural break — the underlying data-generating process changed (new product launch, market expansion, seasonality shift). A one-time ADF test only confirms stationarity at the point of fitting; it does not guarantee future stationarity. Early detection strategies: (1) rolling ADF test with a sliding window of 90–180 days — alert when the p-value rises above 0.1. (2) Monitor rolling mean and variance of the series; statistically significant shifts (CUSUM or MOSUM tests) indicate non-stationarity. (3) Track model forecast errors with a CUSUM chart — systematic drift in errors precedes performance collapse by weeks.'
      },
    ],
    takeaway: `Stationarity is not a box to check once at model training time — spurious regression is the immediate consequence of skipping it, and structural breaks mean a series that was stationary at training time may not be stationary in production. The most important inference to demonstrate is knowing when two non-stationary series should be modelled jointly (cointegration + ECM preserves the long-run relationship) versus separately in first differences (when no cointegrating vector exists and the long-run relationship is meaningless).`,
  },

  {
    id: 'arima_family',
    interactiveId: 'arima_viz',
    title: 'ARIMA Family',
    subtitle: 'AR/MA intuition, ACF/PACF identification, SARIMA, Box-Jenkins, structural break failure',
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['ARIMA', 'SARIMA', 'ACF', 'PACF', 'Box-Jenkins', 'autoregression'],
    summary: `White noise has no predictable structure — every observation is independent of every other. Any predictable time series has autocorrelation: its current value depends on past values or past errors. AR(p) models this directly as a linear combination of p lagged values. But a series with a trend is non-stationary — AR can't model it. And a series with periodicity has seasonal autocorrelation that plain AR misses. ARIMA handles the trend problem by differencing d times before fitting AR and MA components. SARIMA adds seasonal AR, differencing, and MA to handle repeating periodic patterns. The practitioner's problem is that ACF/PACF identification — the traditional way to choose p, d, q — produces clean textbook cutoffs only on textbook data. Real series mix AR and MA, so the Box-Jenkins workflow is iterative: tentatively identify orders, fit, check residuals, and revise until the Ljung-Box test passes at all relevant lags including seasonal ones.`,
    keyPoints: [
      `AR(p) captures autocorrelation through the series' own past: Y_t = φ₁Y_{t-1} + ... + φ_pY_{t-p} + ε_t. The current value is a weighted sum of p lagged values plus noise. For stationarity, all roots of the characteristic polynomial must lie outside the unit circle. The sign of φ₁ determines the autocorrelation structure: positive φ₁ captures momentum (values persist), negative φ₁ captures oscillation (values alternate signs).`,
      `MA(q) captures dependence on past shocks rather than past values: Y_t = ε_t + θ₁ε_{t-1} + ... + θ_qε_{t-q}. An MA process is always stationary — it's a finite weighted sum of white noise shocks. Invertibility (roots of the MA polynomial outside the unit circle) is needed for unique parameter identification. The key difference from AR: MA shocks have a finite memory of exactly q periods; AR shocks propagate with exponentially decaying memory.`,
      `ACF/PACF are the diagnostic tools for identifying AR and MA orders. AR(p): ACF decays exponentially or sinusoidally (infinite memory), PACF cuts off sharply after lag p. MA(q): PACF decays, ACF cuts off after lag q. ARMA: both ACF and PACF decay. In practice these clean cutoffs appear only in simulated data — real series mix AR and MA contributions, making the cutoffs ambiguous. Treat ACF/PACF as a starting point and compare candidate models by AIC/BIC.`,
      `ARIMA(p,d,q) handles non-stationary series by differencing d times before fitting ARMA(p,q). Over-differencing is a common error — d=2 is rarely justified. If d=1 residuals still show a unit root, suspect a structural break or missing seasonality rather than adding d=2. Over-differencing introduces unnecessary MA structure and can cause the model to chase noise.`,
      `SARIMA(p,d,q)(P,D,Q)[s] adds a seasonal layer at period s (e.g., s=12 for monthly data with annual seasonality). Seasonal AR(P) captures autocorrelation at multiples of s. Seasonal differencing D at lag s removes repeating periodic trends. Seasonal MA(Q) captures dependence on past seasonal shocks. The interaction between non-seasonal and seasonal parameters makes order selection genuinely complex — a lag-12 spike in the ACF of residuals after fitting non-seasonal ARIMA is the diagnostic to add (P,D,Q)[12].`,
      `Box-Jenkins in practice: identify (stationarise, plot ACF/PACF, tentatively pick orders), estimate (MLE or conditional least squares), diagnose (Ljung-Box on residuals, ACF of residuals at all lags including seasonal, check heteroskedasticity in residuals), iterate. Most practitioners stop too early at the diagnose step — a Ljung-Box p-value of 0.42 overall can mask a specific lag-12 ACF spike that signals missing seasonality. Always check the seasonal lags explicitly.`,
      `ARIMA's core assumption is that p, d, q are constant over time. A structural break — regime change, crisis, product discontinuation — violates this. After a break, the model absorbs it as a long-lag autocorrelation effect, producing biased forecasts indefinitely. CUSUM test detects gradual drift; Chow test evaluates a known break date; Zivot-Andrews ADF tests for an unknown break date. The right response to a structural break is to re-identify ARIMA orders on post-break data, not to add more lags.`,
      `At scale with thousands of series, per-series ARIMA is impractical: ACF/PACF interpretation requires human-in-the-loop or expensive auto-ARIMA grid search, many series have intermittent demand or insufficient history, and cold-start is unsolvable. Global neural forecasters (N-BEATS, TFT) or gradient-boosted tree models trained across all series typically win in these settings. ARIMA's advantage is interpretability and reliability for individual, well-understood series with stable history.`,
    ],
    checkQuestions: [
      {
        q: 'Your ACF shows significant spikes at lags 1, 2, 3 that decay, and your PACF shows a single significant spike at lag 1 that cuts off. What model do you fit and why?',
        a: 'This pattern — PACF cuts off at lag 1, ACF decays — is the signature of an AR(1) process. For AR(p): PACF cuts off sharply after p, ACF decays. Fit ARIMA(1,0,0) if the series is stationary. Verify: after fitting AR(1), check residuals with Ljung-Box — if significant autocorrelation remains at lag 2+, try AR(2) or AR(1) with an MA component. The decaying ACF pattern (exponential or sinusoidal) vs. a cut-off at q is what distinguishes AR from MA identification.'
      },
      {
        q: 'You fit ARIMA(2,1,2) to a monthly sales series. The Ljung-Box test on residuals passes (p=0.42), but the ACF of residuals shows a significant spike at lag 12. What does this mean and what is the fix?',
        a: 'The Ljung-Box passing at overall level does not mean all lags are clean — it tests a weighted combination. The significant ACF spike at lag 12 is a seasonal pattern not captured by the non-seasonal ARIMA. The residuals are not white noise at the seasonal frequency. Fix: add a seasonal component — upgrade to SARIMA(2,1,2)(1,0,1)[12] or SARIMA(2,1,2)(0,1,1)[12]. Start with seasonal differencing D=1 (subtract last year\'s value): if seasonal pattern is roughly constant in variance, additive seasonality, D=1 should remove it. Then fit seasonal MA(1) for the remaining lag-12 spike. Recheck Ljung-Box specifically at seasonal lags (12, 24, 36).'
      },
      {
        q: 'Your e-commerce platform has 50,000 product SKUs. You need daily sales forecasts for each. Why is per-SKU ARIMA unrealistic and what do you use instead?',
        a: 'Per-SKU ARIMA is unrealistic for several reasons: (1) Order selection requires human-in-the-loop ACF/PACF interpretation or expensive auto-ARIMA search (grid search over AIC for each SKU). (2) Many SKUs have intermittent demand (many zeros) — ARIMA is undefined or collapses. (3) Cold start: new SKUs have <30 days of history, insufficient for stable parameter estimation. (4) SKU-level structural breaks (promotions, stockouts) are frequent and not handled automatically. Better approaches: (1) Global neural models (N-BEATS, LightGBM with lag features) trained across all SKUs — cross-series patterns improve sparse SKU forecasts. (2) Croston\'s method or ADIDA for intermittent demand. (3) Hierarchical forecasting with top-down or bottom-up reconciliation (MinT). At FAANG scale, gradient-boosted trees with hand-crafted lag, seasonal, and event features typically dominate per-SKU ARIMA.'
      },
      {
        q: 'You fit ARIMA(0,1,1) and ARIMA(1,1,0) to the same series. Both pass diagnostics. The MA model has lower AIC. A colleague argues the AR model is more interpretable for business stakeholders. How do you decide?',
        a: 'This exposes the ARIMA duality: ARIMA(0,1,1) with θ near 1 is mathematically equivalent to exponential smoothing (SES). ARIMA(1,1,0) with φ near 1 is close to a random walk with momentum. Both can fit similar patterns because AR(∞) = MA(1) under invertibility (Wold decomposition). Decision framework: (1) If forecasting accuracy is the only criterion, use the lower AIC model — ARIMA(0,1,1). (2) If stakeholder communication matters, AR is more naturally expressed as "next value = weighted average of recent values." (3) Check out-of-sample performance on a held-out window — AIC is in-sample; the better AIC model may overfit. (4) If both models produce nearly identical forecasts (check PI width and point forecasts), the distinction is irrelevant in production — deploy the simpler one.'
      },
    ],
    takeaway: `ACF/PACF identification is a starting point, not a recipe: real series mix AR and MA components, so textbook cutoffs rarely appear cleanly and you always iterate via residual diagnostics. The Ljung-Box test passing overall doesn't guarantee clean residuals — always check seasonal lags explicitly. The single most important ARIMA failure mode to diagnose is the structural break: when the data-generating process shifts, the model absorbs the break as a spurious long-lag effect and produces indefinitely biased forecasts until you re-identify orders on post-break data.`,
  },

  {
    id: 'seasonality_decomposition',
    interactiveId: 'time_series_decomp_viz',
    title: 'Seasonality & Decomposition',
    subtitle: 'STL, additive vs multiplicative, X-13-ARIMA-SEATS, Fourier terms, irregular seasonality',
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['seasonality', 'STL', 'decomposition', 'Fourier', 'X-13', 'additive', 'multiplicative'],
    summary: `A time series with both trend and seasonality is hard to model directly: the trend makes the series non-stationary, and the seasonal component repeats at a fixed period but with amplitude that may scale with the trend level. Modelling the combined series forces a single model to represent structure operating at two different timescales simultaneously. Decomposition separates these into three components — trend, seasonal, residual — so each can be modelled, forecast, and analysed independently. Choosing additive versus multiplicative decomposition is not aesthetic: it determines whether the seasonal component is a fixed absolute number or a fraction of the current level. Get it wrong and every downstream forecast inherits systematic bias. STL dominates classical decomposition for most ML work because it lets the seasonal component evolve and is robust to outliers. But STL's biggest limitation is also its most common production failure: it encodes seasonality at a fixed calendar period and cannot handle events that shift across the calendar, like Black Friday.`,
    keyPoints: [
      `Additive decomposition: Y_t = T_t + S_t + R_t. The seasonal component is a fixed absolute number regardless of the trend level — a store that always sells exactly 200 extra units in December. Multiplicative: Y_t = T_t × S_t × R_t. The seasonal component is a fraction of the current level — 20% higher December sales, so the absolute lift grows as the business grows. Diagnosing which applies: if seasonal amplitude stays constant as the trend grows, use additive; if it scales with the level, use multiplicative or log-transform first.`,
      `Classical decomposition extracts trend via centred moving average, computes seasonal indices as per-period averages, and residualises. Two fatal flaws: the seasonal component is forced identical across all years (it cannot evolve), and trend estimates are missing at the endpoints (the first and last s/2 observations). For business time series where seasonal patterns shift over years, classical decomposition produces a systematically biased seasonal estimate for recent data — the period you most care about.`,
      `STL (Seasonal and Trend decomposition using Loess): iterative smoother alternating inner seasonal loop and outer trend loop. Key parameters are period (s), trend window (t.window), and seasonal window (s.window). s.window="periodic" forces the seasonal component identical across all years — correct only when the seasonal driver is physically fixed (daylight hours). A finite s.window allows the seasonal component to evolve over time. The robust option downweights outliers in LOESS fitting so anomalies don't corrupt the seasonal estimate.`,
      `Multiple seasonalities require iterative STL (MSTL). Hourly data with both daily and weekly patterns: decompose at weekly period first, then decompose the remainder at the daily period. Single STL cannot handle two simultaneous seasonal frequencies. This is a common failure mode for high-frequency data (hourly electricity, real-time traffic) where practitioners apply a single STL and wonder why the residuals still have structure.`,
      `Fourier terms replace seasonal dummy variables with K pairs of sin/cos: sin(2πkt/s) and cos(2πkt/s) for k=1,...,K. The advantage: works for non-integer periods like annual seasonality in daily data (period = 365.25 days — no integer period exists for dummies) and very long periods where dummies are impractical. K controls seasonal shape complexity — K=s/2 is equivalent to dummies. Fourier terms are fitted as OLS regression coefficients, making them computationally cheap and easy to include in any regression model.`,
      `Calendar-shifting events are where STL breaks down completely. Black Friday falls on the fourth Thursday of November — it shifts by up to six days from year to year. In STL with a fixed 52-week period, that spike smears across 3-4 weeks in the seasonal component and the peak estimate is attenuated by a factor of ~3-6. Explicit holiday calendars (Prophet, X-13) or event features (days_to_black_friday, is_black_friday_week) are the correct tool. STL applied to series with calendar-shifting events will systematically underforecast the peak.`,
      `X-13-ARIMA-SEATS is what national statistics agencies use for official economic data. It runs ARIMA before decomposition to handle outliers, trading day effects (months have different numbers of weekdays each year), and holiday effects explicitly. More rigorous than STL; appropriate when you need auditable, reproducible decompositions on economic data. For most ML applications, STL with explicit holiday features is simpler and sufficient.`,
      `A common pipeline: decompose → model trend, seasonal, and residual components separately → recompose. The residual after removing trend and seasonality is close to stationary, making it much easier to model with ARIMA or a neural forecaster. Forecasting on the residual alone often outperforms forecasting on the raw series because the model no longer has to simultaneously capture multiple structure types at different timescales.`,
    ],
    checkQuestions: [
      {
        q: 'You apply STL decomposition to weekly website traffic and the seasonal component shows the amplitude doubling over 3 years while the trend also doubles. Is additive or multiplicative STL more appropriate and how does this change your downstream modelling?',
        a: 'The seasonal amplitude scaling with the trend level is the definition of multiplicative seasonality. Additive STL will extract an evolving seasonal component that appears to grow, but this is an artifact — it conflates trend and seasonal interaction. Fix: take log(Y_t) and apply STL to the log series (converting multiplicative to additive). The extracted log-seasonal component should then be constant (or slowly evolving) over time. For downstream modelling: forecast log(T_t) and log(S_t) separately, then exponentiate to recover the original scale. Prediction intervals must also be computed on the log scale and back-transformed (they will be asymmetric on the original scale — wider above the point forecast than below).'
      },
      {
        q: 'You are building a real-time anomaly detector for server error rates, which show strong daily and weekly patterns. STL fails because STL requires the series to be longer than two periods and the seasonal window must be odd. For 10-minute data with both daily (144 points/period) and weekly (1008 points/period) seasonality, what is your approach?',
        a: 'For dual-frequency high-frequency data: (1) MSTL: apply STL iteratively — first decompose at weekly period (1008), then decompose the residuals at daily period (144). This handles both seasonalities. (2) Alternatively, use Fourier terms directly in an online regression model: maintain a running regression of Y_t on K=5-10 Fourier pairs for daily and weekly periods, use the regression residual as the anomaly signal. Fourier regression is computationally cheap and updateable online. (3) For production anomaly detection specifically: use STL-decomposed baseline + 3-sigma band on the residuals, but update the decomposition on a rolling 4-week window rather than the full history to adapt to gradual shifts. Ljung-Box on the residuals confirms whether the seasonal removal is adequate.'
      },
      {
        q: 'A stakeholder asks why your Black Friday sales forecast is consistently off by 20%. Your model uses STL decomposition with a fixed 52-week seasonal pattern. What is the root cause?',
        a: 'STL with fixed period assumes seasonality is aligned to the same calendar week every year. Black Friday falls on the fourth Thursday of November — it shifts between November 22 and November 28 each year, a ±3 business day shift. In STL, this smears the Black Friday spike across 3-4 weeks in the seasonal component, attenuating the peak estimate. Fix: (1) Add an explicit holiday indicator feature covering the Black Friday window (Nov 22 – Dec 2) with effects that can vary by year — use a holiday-effect model (as in Prophet or X-13) rather than encoding it in the seasonal component. (2) Use TBATS (Trigonometric seasonality, Box-Cox, ARMA errors, Trend, Seasonal) which handles calendar effects. (3) At FAANG scale: encode Black Friday as a feature (days_to_black_friday, is_black_friday_week) in a gradient boosted model — more flexible than statistical decomposition.'
      },
      {
        q: 'What does it mean to set s.window="periodic" in STL, when is it correct to do so, and what is the risk of always using it?',
        a: 's.window="periodic" forces the seasonal component to be identical across all years — the seasonal pattern is extracted as a fixed template. This is appropriate when: the underlying seasonal driver is physically fixed (e.g., the number of daylight hours follows a fixed annual cycle). The risk of always using it: if seasonal patterns evolve — consumer behaviour shifts, population ages, product mix changes — a fixed seasonal template will misfit recent years, leaving systematic residuals that look like trend but are actually seasonality drift. Detection: plot the seasonal component extracted in different years; if it visibly changes, use a finite s.window (e.g., 7 or 11 for annual seasonality in weekly data). Smaller s.window = more flexible seasonal evolution but noisier estimates. The right value is usually the smallest window that does not visibly overfit noise in the seasonal component.'
      },
    ],
    takeaway: `The practical insight that separates strong candidates is knowing when STL fails and what to do instead. STL handles smoothly evolving seasonality at a fixed calendar period but is blind to calendar-shifting events (Black Friday, Ramadan, Easter) because it assumes the seasonal spike falls at the same calendar week each year. The diagnostic — "seasonal component smeared across 3-4 weeks, peak attenuated" — identifies STL failure, and the fix is explicit holiday features or event indicators rather than encoding the event in the seasonal component.`,
  },

  {
    id: 'prophet_framework',
    title: 'Prophet',
    subtitle: 'Piecewise growth, Fourier seasonality, changepoints, uncertainty, failure modes',
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['prophet', 'changepoints', 'Fourier', 'piecewise-linear', 'uncertainty', 'holiday'],
    summary: `Most time series forecasting tools require deep domain expertise to configure — choosing ARIMA orders, specifying seasonal structure, diagnosing residuals. Prophet was built to solve a specific operational problem at Meta: let analysts without time series expertise produce sensible forecasts for thousands of business KPIs without model-by-model tuning. It achieves this by encoding strong structural assumptions: piecewise linear growth with sparse changepoints, Fourier seasonality at weekly and annual periods, and an explicit holiday calendar. These assumptions work well for typical business metrics (daily active users, weekly revenue, annual seasonal sales). The mistake is treating Prophet as a general-purpose forecaster. Feed it a volatile financial series, a mean-reverting series, or anything where recent trend doesn't extrapolate linearly, and it will produce confidently wrong forecasts. Knowing the failure modes matters more than knowing the feature list.`,
    keyPoints: [
      `Prophet is a structural additive regression model: y(t) = g(t) + s(t) + h(t) + ε_t. g(t) is piecewise linear (or logistic) growth, s(t) is Fourier seasonality, h(t) is holiday effects. Each component is a separate, interpretable regression. This makes Prophet easy to inspect and debug — you can plot each component and check whether the trend extrapolation, seasonal pattern, and holiday effects make domain-knowledge sense.`,
      `Piecewise linear growth: rate changes δ_j at potential changepoints are regularised with a Laplace prior, so most changepoints have δ ≈ 0 — sparsity by design. Changepoints are placed automatically across the first 80% of training data. This means the last 20% of training data has few changepoints — recent trend changes go undetected. changepoint_prior_scale (default 0.05) controls how aggressively trend changes are allowed; it is the single most consequential hyperparameter. Too large → overfits recent trend at the forecast boundary; too small → sluggish response to genuine structural breaks.`,
      `Seasonality is modelled via Fourier series: S(t) = Σ [aₙ cos(2πnt/P) + bₙ sin(2πnt/P)] with N=10 harmonics for yearly (P=365.25) and N=3 for weekly (P=7). These coefficients are fitted by OLS as part of the additive regression. Not spectral estimation — just linear regression on engineered features. This is why Prophet works natively with non-integer periods like 365.25, where seasonal dummies are impractical.`,
      `changepoint_prior_scale is the most consequential hyperparameter because it controls trend extrapolation. Too large: the model fits every recent zigzag as a changepoint and extrapolates the last slope aggressively — produces trend explosions at the forecast boundary. Too small: the model ignores genuine structural breaks and forecasts with a stale trend. Don't leave it at the default without running cross-validation and checking whether the trend extrapolation at the forecast horizon makes domain-knowledge sense.`,
      `Uncertainty in Prophet comes in two forms. MAP estimation (the default) is fast but prediction intervals capture only observation noise and future changepoint sampling uncertainty — they do not propagate parameter uncertainty. MCMC (mcmc_samples > 0) gives full posterior uncertainty and properly calibrated intervals. For business forecasts where interval width drives decisions (inventory safety stock, budget reserves), MAP intervals are systematically too narrow. Always check empirical coverage via cross-validation before reporting intervals.`,
      `Prophet's failure modes are predictable from its structural assumptions. Mean-reverting series (stock spreads, some financial metrics): Prophet assumes piecewise linear trend; a mean-reverting series has no long-run trend and the piecewise linear model will produce upward-drifting forecasts. Multiplicative seasonality: supported via seasonality_mode="multiplicative" but only as a global mode — all seasons are multiplicative or none are. External shocks: add_regressor is too rigid for series dominated by unpredictable events. Less than 1-2 years of history: annual seasonality identification degrades significantly.`,
      `add_regressor adds exogenous features as linear terms in the regression. The critical production trap: the regressor must be available at forecast time. If you add observed weather as a regressor during training and backtesting, the model appears accurate — but in production, you'd need weather forecasts for the forecast horizon. Substituting forecast weather for actual weather introduces regressor error that inflates MAPE. Always backtest with the same information available at deployment time.`,
      `Prophet's cross-validation: prophet.diagnostics.cross_validation() with initial (training window), period (spacing between cutoffs), and horizon. This is rolling-origin evaluation, not random train-test split. performance_metrics() summarises MAPE/RMSE/MAE by forecast horizon — essential for understanding where accuracy degrades. Empirical interval coverage from this output tells you whether MAP or MCMC intervals are needed.`,
    ],
    checkQuestions: [
      {
        q: 'Your Prophet model produces a forecast for next quarter that shows a sharp trend acceleration starting exactly where your training data ends. What is the likely cause and how do you fix it?',
        a: 'This is a classic changepoint detection artifact. Prophet places potential changepoints in the first 80% of training data. Near the end of training, the model has fewer changepoints, so recent trend changes go undetected — but the Fourier seasonal terms and changepoint extrapolation create an illusion of acceleration at the forecast boundary. More commonly: if the last few months of training data had an unusual positive trend (holiday surge, marketing push), Prophet extrapolates this trend into the future. Fix: (1) Inspect the trend component — call plot_components() and check if the trend curve is realistic at the forecast horizon. (2) Reduce changepoint_prior_scale to smooth the trend. (3) Set changepoint_range=0.95 to allow changepoints closer to the end of training. (4) Add an explicit growth cap with logistic growth to prevent trend explosion. (5) Most importantly: domain-knowledge check — is this extrapolation physically plausible?'
      },
      {
        q: 'You add daily temperature as an external regressor to Prophet to forecast energy demand. During backtesting, MAPE is 3%. In production, MAPE is 22%. What happened?',
        a: 'The regressor value at forecast time is unknown. During backtesting, you used actual observed temperature (future-known values) for the forecast period — a lookahead bias. In production, you would need a weather forecast for the forecast horizon. If actual temperatures were substituted with temperature forecasts (which have their own errors), the propagated regressor error inflates forecast error. Fix: (1) In cross-validation, only use temperature forecasts (not actuals) for dates beyond each cutoff — simulate production conditions. (2) Alternatively, use temperature as a feature only for short-horizon forecasts (1–3 days ahead) where weather forecasts are accurate; beyond 7 days, model temperature uncertainty explicitly or remove the regressor. (3) Check regressor correlation over the holdout: if temperature dropped during a period not seen in training, the model may extrapolate outside its training distribution.'
      },
      {
        q: 'A manager wants a 90% prediction interval for monthly revenue 6 months out. You have a trained Prophet model. What are the steps to produce well-calibrated intervals and what are their limitations?',
        a: 'Steps: (1) Enable MCMC sampling: set mcmc_samples=300 (or higher) in the Prophet constructor — MAP estimation only models uncertainty from future trend changepoints and observation noise, not parameter uncertainty. MCMC gives full posterior over all parameters. (2) Set interval_width=0.9. (3) Evaluate calibration: run cross_validation() with horizon=6 months across multiple historical cutoffs; compute coverage_rate from performance_metrics() — a well-calibrated 90% PI should contain the actuals ~90% of the time. Limitations: (1) Prophet interval uncertainty comes from future changepoints (sampled from the historical changepoint prior) and observation noise — it does not model regime changes not present in history. (2) 6-month forecasts have compound uncertainty; Prophet intervals often undercover at long horizons. (3) If the series has heavy-tailed noise (e.g., viral spikes), Gaussian likelihood underestimates tail risk — consider Student-t likelihood via a custom Stan model.'
      },
    ],
    takeaway: `Prophet is a specific tool for a specific problem: business KPIs with trend + weekly + yearly seasonality, designed for analysts who need sensible forecasts without deep time series expertise. Its failure modes are predictable from its structural assumptions — trend explosion at the forecast boundary when changepoint_prior_scale is too high, silently using future regressor values during backtesting, and underconfident MAP intervals. changepoint_prior_scale is the single most consequential hyperparameter and must be validated via rolling-origin cross-validation rather than left at the default.`,
  },

  {
    id: 'exponential_smoothing',
    title: 'Exponential Smoothing & ETS',
    subtitle: 'SES, Holt, Holt-Winters, ETS state space, connection to ARIMA, MLE tuning',
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['exponential smoothing', 'ETS', 'Holt-Winters', 'state space', 'SES', 'MLE'],
    summary: `ARIMA requires ACF/PACF identification to choose p, d, q — a manual process that breaks at scale and fails for practitioners without time series expertise. Exponential smoothing methods sidestep this by imposing a simple structural assumption: past observations should be weighted by recency, with exponentially decaying weights. SES does this for level; Holt's method adds a trend component; Holt-Winters adds seasonality. The mistake is treating these as heuristic update rules. ETS (Error-Trend-Seasonality) is a proper statistical state space model — it has a likelihood function, parameters estimated via MLE, and AIC-based model selection across 30 component combinations. This means ETS implicitly performs ARIMA order selection without the ACF/PACF identification step, which is why ETS consistently outperforms ARIMA on large benchmark datasets like M3 and M4.`,
    keyPoints: [
      `ARIMA order selection requires ACF/PACF interpretation, which is manual, error-prone, and impossible at scale. ETS bypasses this entirely: it assumes exponentially decaying weights on past observations, estimates the decay parameter α via MLE, and selects among Error-Trend-Seasonality component combinations via AIC. The automation is the point — ETS delivers ARIMA-quality forecasts without per-series manual identification.`,
      `Simple Exponential Smoothing (SES): ŷ_{t+1} = αY_t + (1-α)ŷ_t = Σ α(1-α)^j Y_{t-j}. α ∈ (0,1) controls recency weighting — α=1 is a naïve forecast (only yesterday matters), α→0 weights all history equally. Optimal α is estimated by MLE. SES is the optimal forecast for a random walk with Gaussian noise — it is exactly equivalent to ARIMA(0,1,1) with θ = 1-α. A high fitted α (e.g., 0.92) means the series has near-random-walk dynamics: past values beyond one period ago carry almost no predictive information.`,
      `Holt's double exponential smoothing adds a trend component. Level: L_t = αY_t + (1-α)(L_{t-1} + b_{t-1}). Slope: b_t = β(L_t - L_{t-1}) + (1-β)b_{t-1}. Forecast: ŷ_{t+h} = L_t + hb_t. Linear trend extrapolation at long horizons is dangerous — a series that has been trending up for 2 years is not guaranteed to continue. The damped trend variant (damped-trend Holt) multiplies the slope by φ^h with φ ∈ (0.8, 0.98): the slope shrinks toward zero at long horizons. This is one of the most reliable improvements for medium-horizon forecasting.`,
      `Holt-Winters adds a seasonal component. Additive: S_t = γ(Y_t - L_{t-1} - b_{t-1}) + (1-γ)S_{t-s}. Multiplicative: S_t = γ(Y_t / (L_{t-1} + b_{t-1})) + (1-γ)S_{t-s}. All parameters (α, β, γ) are estimated jointly by MLE — not independently. Multiplicative seasonality handles the case where seasonal amplitude scales with level; log-transforming and using additive is an equivalent approach.`,
      `ETS(E, T, S) notation: E ∈ {A, M} for error type (additive or multiplicative), T ∈ {N, A, Ad, M, Md} for trend (none, additive, additive damped, multiplicative, multiplicative damped), S ∈ {N, A, M} for seasonality. Up to 30 valid combinations. AIC over all valid ETS variants selects the best automatically — this is the equivalent of Box-Jenkins order selection done via likelihood comparison rather than ACF/PACF reading.`,
      `ETS-ARIMA equivalence proves that exponential smoothing is not ad hoc. SES (ETS(A,N,N)) = ARIMA(0,1,1). Holt's method (ETS(A,A,N)) = ARIMA(0,2,2). Holt-Winters additive (ETS(A,A,A)) = ARIMA(0,1,m+1)(0,1,0)[m]. ETS implicitly selects ARIMA order via structural assumptions rather than ACF/PACF exploration. This equivalence explains why ETS and ARIMA perform similarly on benchmark datasets — they're fitting the same class of models via different parameterisations.`,
      `ETS outperforms ARIMA in practice for two reasons. Parsimony: ETS has fewer parameters with stronger regularisation via structural assumptions, reducing overfitting on short series. Multiplicative error models: ETS(M,...) handles heteroskedasticity (variance increasing with level) naturally by multiplying the noise term by the current level. ARIMA with Gaussian errors assumes constant variance, which misspecifies series with growing amplitude. Holt-Winters multiplicative error produces correct prediction intervals for heteroskedastic series where ARIMA does not.`,
      `Croston's method handles intermittent demand (series with many zeros) — separately exponentially smooths demand size and inter-demand interval. SES and Holt-Winters produce nonzero forecasts even when zeros dominate, which is wrong by construction for spare-parts or SKU-level intermittent series. For series with more than 30-50% zeros, Croston or ADIDA (Aggregated-Disaggregated Intermittent Demand Approach) are the correct starting points.`,
    ],
    checkQuestions: [
      {
        q: 'You fit SES to a daily sales series and find the optimal α = 0.92 via MLE. What does this imply about the data and what model class does SES correspond to at this parameter value?',
        a: 'α = 0.92 means almost all weight is on the most recent observation — the model behaves close to a naïve (random walk) forecast. This implies: (1) The series has very little autocorrelation at lags > 1 — past values beyond yesterday have almost no predictive power. (2) The series is highly volatile or mean-reverting quickly. (3) SES with α close to 1 corresponds to ARIMA(0,1,1) with θ = 1−α ≈ 0.08 — an ARIMA where the MA term is nearly zero, i.e., a near-random-walk. Implication for forecasting: point forecasts collapse to the last observation; the forecast is essentially "tomorrow = today." Consider whether a more informative model exists or whether this is simply a high-noise series with no exploitable structure.'
      },
      {
        q: 'Your ETS model is ETS(M,A,M) — multiplicative error, additive trend, multiplicative seasonality. What forecast distribution does this imply and why are prediction intervals different from ETS(A,A,A)?',
        a: 'ETS(M,A,M) uses multiplicative errors: the noise term multiplies the signal. This means variance grows with the level — heteroskedasticity is built in. The h-step-ahead forecast distribution is not Gaussian; it is log-normally distributed (approximately). Prediction intervals must be computed via simulation (bootstrap innovation errors forward) rather than the Gaussian formula ŷ ± z·σ. ETS(A,A,A) has additive errors — constant variance regardless of level — and produces Gaussian prediction intervals via the analytical formula. In production: for series where the seasonal amplitude scales with the level (revenue, traffic), ETS(M,A,M) PI from simulation are wider and better calibrated than ETS(A,A,A) Gaussian PI. The Hyndman forecast package computes these via 5000 simulation paths automatically when using ETS multiplicative components.'
      },
      {
        q: 'The M4 forecasting competition showed that ETS and ARIMA individually perform worse than simple combination methods. How does this affect how you should use ETS in production?',
        a: 'The M4 result (Makridakis et al., 2020) showed that simple combination forecasts — e.g., average of ARIMA, ETS, and theta method — outperformed any individual model by 5–10% MASE. The implication is that model uncertainty is large enough that combining reduces variance without proportionally increasing bias. Production implications: (1) Do not commit to a single model class; ensemble ARIMA + ETS + Prophet for any important series. (2) Use the theta method as a baseline — it is simple, fast, and extremely competitive. (3) Optimal combination weights can be estimated on a validation window (penalised regression on validation errors) or used as equal weights. (4) For 50,000+ series, the overhead of fitting 3 models per series is dominated by the latency of data IO — the computation itself is cheap. The cost-accuracy tradeoff strongly favours ensembling.'
      },
    ],
    takeaway: `ETS is not a heuristic — it is a proper state space model whose parameters are estimated by MLE and whose AIC-selected variant implicitly performs ARIMA order selection without the ACF/PACF identification step. The ETS-ARIMA equivalence (SES = ARIMA(0,1,1), Holt = ARIMA(0,2,2)) proves these are the same underlying model class in different parameterisations. The practical M4 conclusion: always ensemble ETS with ARIMA and a simple baseline, because model uncertainty across forecast horizons is large enough that combination consistently dominates any single method.`,
  },

  {
    id: 'neural_forecasting',
    title: 'Neural Forecasting',
    subtitle: 'N-BEATS, N-HiTS, TFT, PatchTST, when transformers lose to MLPs, foundation models',
    difficulty: 'advanced',
    estimatedMin: 60,
    tags: ['N-BEATS', 'N-HiTS', 'TFT', 'PatchTST', 'transformer', 'neural forecasting', 'TimeGPT', 'MOIRAI'],
    summary: `Transformers achieved state-of-the-art results in NLP and computer vision by learning long-range dependencies through self-attention. Applied to time series, Informer, Autoformer, and FEDformer all claimed benchmark wins before a 2023 paper (Zeng et al.) showed that a single linear layer outperforms all of them on the same benchmarks. The reason is structural: attention is permutation-equivariant by design — it computes scores from pairwise content similarity, not position, so shuffling the timestamps barely changes the output. A model that ignores temporal order cannot capture the sequential dependencies that define a time series. N-BEATS and N-HiTS sidestep this with MLP stacks and basis decomposition designed explicitly for temporal structure. TFT is competitive despite using attention because its LSTM encoder, gating, and variable selection preserve the temporal inductive bias that vanilla Transformers discard. The practical question is not "which neural model" but "when does any neural model beat a well-tuned statistical baseline."`,
    keyPoints: [
      `Transformers fail at time series because self-attention is permutation-equivariant: the attention score between tokens is computed from content similarity (dot product of embeddings), not from their temporal position. Positional encodings are added but don't fundamentally fix this — they just make position part of the content. Shuffle the timestamps in an ETT benchmark and Transformer performance barely changes. A model that is blind to temporal order cannot model autocorrelation, trend, or seasonality directly.`,
      `N-BEATS (Oreshkin et al. 2020): purely MLP-based, no recurrence, no attention. Each block produces a backcast (reconstruction of its input) and a forecast. Residual connections subtract the backcast before passing to the next block — subsequent blocks model what previous blocks couldn't explain. Each block focuses on progressively harder residuals, preventing any single block from having to model all temporal structure. Two modes: Generic (learned basis) and Interpretable (polynomial trend + Fourier seasonality basis).`,
      `N-HiTS (Challu et al. 2022): extends N-BEATS with hierarchical multi-rate sampling. Different stacks operate at different timescales — a coarse stack captures slow trends from downsampled series, a fine stack captures high-frequency variation. This multi-scale structure is the right inductive bias for long-horizon forecasting: trends need coarse temporal resolution, seasonal patterns need fine resolution. N-HiTS outperforms N-BEATS on horizons >96 steps.`,
      `Temporal Fusion Transformer (TFT, Lim et al. 2021) is competitive despite using attention because it retains explicit temporal structure. LSTM encoder/decoder processes the sequence, preserving temporal order. Variable Selection Networks gate which features matter at each timestep. Multi-head attention operates on LSTM outputs — not raw timestamps — capturing long-range dependencies in an already-temporally-encoded representation. TFT is the strongest neural model when rich covariates exist (static entity features, known future inputs, observed past features). Without covariates, N-HiTS typically wins.`,
      `PatchTST (Nie et al. 2023) fixes the Transformer's permutation equivariance problem for time series by treating non-overlapping patches (local windows) as the tokens, not individual timesteps. Attention between patches captures relationships between local temporal patterns rather than individual points. Channel-independent design processes each variate separately. This substantially outperforms vanilla Transformers and remains competitive with DLinear.`,
      `DLinear (Zeng et al. 2023) — a single linear layer applied to the flattened lookback window — outperforms Informer, Autoformer, and FEDformer on standard ETT benchmarks. This is the benchmark credibility test for neural forecasting papers: if your model doesn't beat DLinear, the architectural complexity is not justified. Always include DLinear and N-HiTS as baselines before claiming a neural model win.`,
      `Foundation models for time series (TimeGPT, MOIRAI): trained on massive time series corpora to enable zero-shot forecasting. Useful for cold start — when you have fewer than 30 observations per series, a fine-tuned statistical model has too little data. With 2+ years of in-domain history, local or global neural models trained on your data will dominate zero-shot foundation models. Foundation models are calibrated to their training distribution; pharmaceutical, industrial, or niche domain series that differ from the training corpus will show degraded zero-shot performance.`,
      `Global vs local is the key architectural decision. Local models (ARIMA, ETS, Prophet) fit one model per series — maximum in-sample fit but no cross-series learning. Global models (N-BEATS, TFT, TimeGPT) train across all series jointly — cross-series patterns improve forecasts for sparse or new series, but require series to be related enough for transfer to help. When series are heterogeneous or from a different distribution than the training corpus, local models win.`,
    ],
    checkQuestions: [
      {
        q: 'A colleague trains an Informer model on your ETT dataset and reports 15% lower MSE than ARIMA. You are sceptical. What do you do before trusting this result?',
        a: 'Compare against simple baselines first: (1) DLinear (a simple linear layer on the flattened lookback window) — Zeng et al. showed it outperforms Informer on ETT. If DLinear matches or beats Informer, the Informer gains are not from the Transformer architecture. (2) NLinear (normalised linear model) — even simpler, often competitive. (3) Check the lookback window and horizon match: Informer and ARIMA may use different lookback lengths — ensure fair comparison. (4) Check the evaluation split: if Informer was tuned on the test set (implicit or explicit), results are inflated. (5) Verify: is the ARIMA fitted per-step (rolling origin) or as a single fit? ARIMA with rolling origin is competitive; ARIMA fit once and extrapolated degrades. Conclusion: Informer outperforming ARIMA is plausible but the margin should be verified against at least DLinear and N-HiTS to confirm the gains are from temporal modelling, not from incidental lookback advantages.'
      },
      {
        q: 'You need to forecast hourly electricity demand for 1000 substations, 168 hours ahead (one week). You have 3 years of historical data per substation. TFT vs N-HiTS vs SARIMA — which do you use and why?',
        a: 'N-HiTS is the strongest starting point here. Reasoning: (1) 168-hour (long) horizon: N-HiTS hierarchical pooling handles long horizons better than N-BEATS and comparably to TFT but with lower compute. (2) 1000 series: a global N-HiTS trained across all substations is more efficient than fitting 1000 SARIMA models and likely better for substations with lower SNR. (3) SARIMA(p,d,q)(P,D,Q)[24](P,D,Q)[168]: two seasonal periods (daily + weekly) — SARIMA with two seasonalities is technically doable (BATS/TBATS) but complex; SARIMA does not natively handle double seasonality. (4) TFT is the best choice IF you have meaningful covariates — temperature, time-of-day, day-of-week, holidays, economic indicators — because TFT\'s variable selection and covariate handling outperform N-HiTS when regressors are informative. Without covariates, N-HiTS wins. (5) SARIMA baseline: always fit and evaluate; if N-HiTS does not beat SARIMA by >5% MASE, the simpler model is preferred. Practical pipeline: N-HiTS global model + SARIMA per-substation ensembled.'
      },
      {
        q: 'You train a TimeGPT zero-shot model on a new domain (pharmaceutical sales) without fine-tuning. It performs worse than Holt-Winters. When would you expect a foundation model to outperform statistical methods, and what limits them?',
        a: 'Foundation model advantages are concentrated in: (1) Cold start — fewer than 30 observations per series (new drug launch). Holt-Winters needs at least 2 seasonal cycles to fit; TimeGPT\'s pretrained seasonality representations can generalise. (2) Heterogeneous portfolio — when series have wildly different dynamics and per-series fitting is not feasible. (3) Zero-shot cross-domain when the target domain is well-represented in the pretraining corpus (retail, weather, finance). Limits: (1) Distribution shift — pharmaceutical sales patterns (approval spikes, generic entry cliffs, off-label use growth) differ from the pretraining corpus. The model has no mechanism to adapt to domain-specific patterns not in pretraining. (2) Foundation models are calibrated to their training distribution — PI widths and point forecasts may be systematically biased in new domains. (3) Interpretability — a brand manager cannot interrogate why TimeGPT predicts a spike in March. (4) Fine-tuning helps but requires infrastructure. Conclusion: with 3 years of data per series, you have enough history for reliable Holt-Winters fitting. Fine-tune TimeGPT on your historical data or use N-BEATS global model — both should outperform zero-shot here.'
      },
      {
        q: 'What is the doubly residual architecture in N-BEATS and why does it improve forecasting over a plain deep MLP?',
        a: 'N-BEATS doubly residual: each block (a stack of FC layers) produces two outputs: (1) backcast b̂_t — a reconstruction of the block\'s input x_t. (2) forecast f̂_{t+h} — a prediction for future values. The residual connection subtracts the backcast from the input before passing to the next block: x_{t+1} = x_t − b̂_t. The forecast from all blocks is summed: ŷ_{t+h} = Σ f̂^{(k)}_{t+h}. Why this works: (1) Each block focuses on what previous blocks could not explain — progressive residual fitting reduces the modelling complexity for each block. (2) The backcast acts as a self-supervised signal — the block must simultaneously reconstruct the past (easy) and forecast the future (hard), forcing it to learn meaningful representations rather than overfitting to noise. (3) Gradient flow: residual connections ensure gradients reach early blocks without vanishing. Compared to a plain deep MLP of equal depth, N-BEATS shows better generalisation because each block is constrained to a smaller hypothesis class (what remains after previous blocks\' explanation), reducing effective model complexity.'
      },
    ],
    takeaway: `The most important neural forecasting insight is that Transformers are not automatically better for time series: attention is permutation-equivariant by design and ignores temporal order, which is why a single linear layer (DLinear) outperforms Informer on standard benchmarks. N-HiTS and TFT are competitive because they retain explicit temporal inductive bias — hierarchical multi-scale pooling and LSTM encoding respectively. Foundation models (TimeGPT, MOIRAI) win only on cold start (fewer than 30 observations); with adequate history, local statistical or global neural models trained in-domain dominate zero-shot performance.`,
  },

  {
    id: 'forecast_evaluation',
    title: 'Forecast Evaluation',
    subtitle: 'MASE, pinball loss, Winkler score, Diebold-Mariano, rolling vs expanding window',
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['MASE', 'MAPE', 'pinball loss', 'Winkler score', 'Diebold-Mariano', 'backtesting', 'lookahead bias'],
    summary: `MAPE is the default metric most teams use, and it has three distinct failure modes that make it actively misleading. It's undefined when actuals are near zero (division by zero). It systematically biases selection toward models that underforedict, because over-forecasts generate larger percentage errors than under-forecasts of equal absolute magnitude. And it's not comparable across series with different scales. MASE solves all three by normalising against the in-sample naïve forecast — scale-free, symmetric, defined when Y_t = 0, and interpretable (MASE < 1 means you beat the naïve baseline). But metric choice is the second problem. The first is backtesting design: a rolling-origin evaluation that faithfully simulates production conditions is the only reliable proxy for live performance. Fitting normalisation scalers on the full dataset including the test period is ubiquitous and inflates reported performance by 5-15% — not from overfitting the model, but from the preprocessing pipeline implicitly seeing the future.`,
    keyPoints: [
      `MAPE fails in three distinct ways. Division by zero: any series with zero values on some days breaks it — daily new user counts, SKU-level sales with stockouts, any count series. Asymmetric penalty: over-forecasting by 50% produces a larger MAPE contribution than under-forecasting by 50% — MAPE is minimised by systematically underforecasting, biasing model selection toward conservative forecasters even when over-forecasting is equally costly. Scale dependence: MAPE on a series with mean 10 versus mean 10,000 are not comparable, making cross-series performance comparison meaningless.`,
      `MASE (Hyndman & Koehler 2006) solves all three MAPE failure modes. MASE = MAE / (MAE of in-sample naïve forecast). MASE < 1 means the model beats the naïve baseline on average. Scale-free: the normalisation makes MASE comparable across series with different scales. Symmetric: over- and under-forecasting of equal magnitude contribute equally. Defined when Y_t = 0: the denominator uses historical naïve errors which are well-defined even when actuals are zero. The M4 competition chose MASE as the primary metric — treat it as the default.`,
      `Probabilistic forecast evaluation captures what point metrics miss. A point forecast of 100 with ±5 uncertainty and ±500 uncertainty have identical point forecast errors but completely different decision implications for inventory or capacity planning. Pinball (quantile) loss: L_q(y, ŷ) = q(y - ŷ) if y ≥ ŷ, else (1-q)(ŷ - y). Averaging over all quantiles gives CRPS — the gold standard for distributional forecasts. Winkler score penalises interval width plus extra penalty for actuals outside the interval, directly measuring calibration and sharpness.`,
      `Diebold-Mariano test: statistically tests whether two forecasters have equal expected loss. Test statistic based on d_t = L(e₁_t) − L(e₂_t), using Newey-West standard errors for autocorrelated loss differentials. Both models must be evaluated on the same test set at the same horizon. For a portfolio of 1000 series: compute per-series MASE difference d_i, run a paired t-test or Wilcoxon signed-rank on {d_i}. Report effect size (median MASE difference) alongside p-value — a statistically significant difference of 0.001 MASE is operationally irrelevant.`,
      `Rolling vs expanding window: expanding window uses all available history (training window grows over time) — correct for stationary DGPs where more data always helps. Rolling window uses a fixed training size — simulates non-stationary environments where only recent data is relevant. Rolling gives more evaluation cutoffs per total dataset length. Start with expanding; switch to rolling if model performance degrades systematically on more recent cutoffs (a signal of non-stationarity in the DGP).`,
      `Lookahead bias is the most common way backtesting results fail to transfer to production. Four forms: (1) Using future covariate values at forecast time — the most obvious form. (2) Fitting normalisation (mean, std) on the full dataset including the test period — the scaler "knows" the future level. (3) Feature lag calculation errors without proper temporal shifts — feature at time t accidentally includes values from t+1. (4) Selecting the model with best test MAPE and reporting it as unbiased — the test set has been implicitly used for selection. The fix: strict temporal split, all preprocessing fitted on training data only, a validation set for model selection, and the test set touched exactly once for final unbiased evaluation.`,
      `Lookahead bias magnitude: fitting a Z-score normaliser on training + test combined inflates performance because test-period values are included in the mean and standard deviation. If the test period has a higher mean (trending series), the full-dataset normalisation partially de-trends the test values, making the model appear more accurate than it would be in production. Empirically, this inflates reported MASE by 5-15% for non-stationary series. Always fit preprocessing on training data only and apply to test using training-fitted parameters.`,
    ],
    checkQuestions: [
      {
        q: 'You have two models. Model A has MAPE = 8%, Model B has MAPE = 12%. Model A looks better. But 15% of your series have true values below 5. Which model should you trust and how do you decide?',
        a: 'MAPE is unreliable when true values are near zero. For the 15% of series with Y_t < 5, a small absolute error produces enormous percentage error — those series dominate the MAPE average and likely distort the comparison. Model A may have "better" MAPE simply by making smaller absolute errors on near-zero series, not because it is better overall. Fix: (1) Compute MASE (or MAE) which is robust to near-zero series. (2) Separately evaluate the near-zero series subset — plot distribution of errors by series mean level. (3) Compute MAPE excluding near-zero series (Y_t < 5) and compare. (4) If the business cost of errors is proportional to the absolute error (not the percentage error), use MAE or MASE as the primary metric. Do not report a single MAPE number when 15% of the series invalidate its computation.'
      },
      {
        q: 'You are comparing two forecasting models across 1,000 series. How do you statistically test which model is better, controlling for the multiple-series problem?',
        a: 'The Diebold-Mariano test applies to a single series. For a portfolio: (1) Compute per-series MASE for each model: MASE₁_i and MASE₂_i for series i = 1,...,1000. (2) Compute d_i = MASE₁_i − MASE₂_i. Run a paired t-test (or Wilcoxon signed-rank if non-normal) on {d_i}. H₀: E[d] = 0. This is the M4 competition approach. (3) For robustness: bootstrap the series — resample with replacement from the 1000 series, compute the mean d on each bootstrap sample, derive a 95% CI. (4) Effect size matters more than p-value: a difference of 0.002 MASE is statistically significant with 1000 series but operationally irrelevant. Report median MASE and the fraction of series where each model wins.'
      },
      {
        q: 'You fit your preprocessing pipeline (including Z-score normalisation) on the full training+test set combined, evaluate on the held-out test, and get MASE = 0.83. What is wrong and how large could the bias be?',
        a: 'This is lookahead bias in preprocessing. Z-score normalisation uses the mean and standard deviation of the full dataset including the test period. The model sees future information through the normalisation constants: the mean includes test-period values, so the transformation "knows" the future level and scale. The bias direction is toward underestimating forecast error — the normalised test values are artificially closer to the model\'s training distribution. Bias magnitude depends on how much the test period differs from training in mean/variance: if the test period had a trend shift (higher mean), the full-dataset normalisation partially corrects for this, making the model appear better than it would be in production. Fix: fit the scaler only on training data, then transform validation and test using training-fitted parameters. Re-run evaluation. In practice, this often changes MASE by 5–15% for non-stationary series.'
      },
      {
        q: 'A PM asks why your 95% prediction interval contains the actual value only 81% of the time in production. What are the likely causes and how do you diagnose them?',
        a: 'A 95% PI with 81% empirical coverage means the intervals are too narrow (overconfident). Likely causes: (1) Model misspecification: the error distribution assumed by the model (e.g., Gaussian for ARIMA/ETS) underestimates tail mass. Real demand is often right-skewed or heavy-tailed. (2) Parameter uncertainty not propagated: MAP estimation (most forecasters) treats parameters as fixed — correct PI requires MCMC or bootstrap to propagate parameter uncertainty, which grows with forecast horizon. (3) Structural breaks: the model was fitted before a distribution shift; the PI is calibrated to the old regime. (4) Feature lookahead removed at production time: PI was computed with perfect covariates during backtesting, but production uses forecasted covariates with their own errors. Diagnosis: (1) Compute empirical coverage at each horizon h separately — does coverage degrade further out? Degrading coverage = parameter uncertainty or structural break. (2) Compare residual distribution to the assumed distribution — heavy tails → use Student-t likelihood. (3) Pit histogram (probability integral transform): residuals/σ should be uniform(0,1) for well-calibrated Gaussian PI — deviations show misspecification.'
      },
    ],
    takeaway: `MAPE misleads in three ways simultaneously: it's undefined for zero-valued series, penalises over-forecasting more than under-forecasting of equal absolute magnitude, and can't be compared across series with different scales. MASE solves all three and should be the default metric. The more consequential failure mode is backtesting design: fitting preprocessing (normalisation, scaling) on the full dataset including test data is a ubiquitous form of lookahead bias that inflates reported MASE by 5-15% — not from overfitting the model, but from the scaler implicitly encoding future level information.`,
  },

  {
    id: 'ts_anomaly_detection',
    title: 'Time Series Anomaly Detection',
    subtitle: 'Point/contextual/collective anomalies, CUSUM, STL residuals, LSTM autoencoders, adaptive thresholds',
    difficulty: 'advanced',
    estimatedMin: 55,
    tags: ['anomaly detection', 'CUSUM', 'STL', 'LSTM autoencoder', 'isolation forest', 'contextual anomaly'],
    summary: `Time series anomaly detection fails in production for one specific reason more than any other: teams apply a threshold to the raw series instead of the residuals of a properly specified seasonal and trend model. An API error rate of 10,000 per minute is normal during peak traffic and anomalous at 3am — but a static threshold treats both identically. The raw series conflates seasonality, trend, and anomaly signal into a single number; a threshold on raw values fires whenever any component is high, including seasonality that's entirely expected. Decompose first, threshold on the residuals, and nearly all seasonality-driven false positives disappear. The quality ceiling for any anomaly detector is the quality of its baseline model — the residuals are only as clean as the decomposition.`,
    keyPoints: [
      `Applying a static threshold to a seasonal series produces false positives every time the seasonal component peaks — not because anything unusual happened, but because the underlying cycle reached its expected high. Decompose Y_t = T_t + S_t + R_t, then threshold R_t. Point anomaly: |R_t| > k·σ_R where σ_R is an IQR-based robust standard deviation and k=3-4. The decomposition removes expected variation; the threshold detects unexpected deviation. This single change eliminates the majority of seasonality-driven false positives in most production monitoring systems.`,
      `Three anomaly types require different detection strategies. Point anomalies: a single observation far from expected (latency spike, sensor error). Contextual anomalies: a value normal in one context but anomalous in another — 500 sales on a random Tuesday versus 500 sales on Black Friday (should be 50,000). Collective anomalies: a sequence of individually normal observations that are jointly anomalous — 5 days of conversion rate 1% below normal, each individually within tolerance but collectively a significant degradation. Each type requires a different detection approach.`,
      `CUSUM (Cumulative Sum Control Chart): C_t = max(0, C_{t-1} + (Y_t − μ₀ − k)). Alert when C_t > h. Directional — it accumulates evidence of sustained shift rather than reacting to single spikes. This makes CUSUM naturally suited for detecting collective anomalies (sustained shifts) and resistant to false positives from individual noisy observations. Requires specifying μ₀ and k — doesn't adapt to non-stationary baselines or seasonality without preprocessing (decompose first, then apply CUSUM to residuals).`,
      `Isolation Forest for time series requires careful feature engineering. Applied to raw values, it ignores temporal structure entirely — it doesn't know that observation 100 follows observation 99. The correct input is a feature matrix: lag values [Y_{t-1}, Y_{t-2}, ...], rolling mean, rolling std, time-of-day, day-of-week. Without lag features, Isolation Forest is a point anomaly detector with no temporal awareness. It also cannot detect collective anomalies without explicit sliding window features encoding the joint distribution of a sequence.`,
      `LSTM Autoencoders encode a time window and decode it back, using reconstruction error as the anomaly score. Trained on normal data only. High reconstruction error = the window is unusual relative to training distribution. The production problem is threshold calibration: the reconstruction error distribution shifts as the system drifts (concept drift), so a static threshold set at training time produces increasing false positives over time. Use a rolling calibration window — 99th percentile of reconstruction error over the past 7-14 days as the threshold, updated daily.`,
      `Adaptive thresholds are necessary when variance is non-constant, when the series has multiple operating modes, or when you need to directly control the alert rate. Static 3-sigma fails on all three counts. EWMA control charts adapt to recent variance. Quantile regression estimates conditional quantiles as functions of time features (capturing time-varying variance). Conformal prediction provides coverage-guaranteed anomaly scores without distributional assumptions — the alert fires when the score falls in the top α fraction of calibration set scores, guaranteeing at most α false positive rate by construction.`,
      `Root cause vs symptom: a single upstream failure propagates through a dependency graph and produces correlated anomalies across dozens of downstream metrics simultaneously, flooding the alert queue. A DB failure causes API latency, cache miss rate, and error rate to all spike within seconds. Alert flooding without root cause identification means incident responders chase symptoms while the root cause persists. Multi-variate anomaly detection finds correlated joint anomalies; causal graph traversal identifies whether the anomaly originated upstream or is local. Alert correlation groups alerts firing within the same time window into a single incident.`,
      `Evaluation when labels are scarce: PR-AUC (precision-recall) beats ROC-AUC for imbalanced anomaly detection — imbalance is extreme (1 anomaly per 1000 normal observations), so ROC-AUC is dominated by the large number of true negatives. NAB (Numenta Anomaly Benchmark) score rewards early detection within a window before impact — detection two hours late is still valuable, just less so than detection immediately. Precision@k (fraction of top-k flagged events that are true anomalies) is practical when operators review top-ranked alerts.`,
    ],
    checkQuestions: [
      {
        q: 'Your 3-sigma threshold on raw API error rate generates 200 false-positive alerts per day because of daily and weekly seasonality. What is the right fix?',
        a: 'The problem is applying a static threshold to a non-stationary (seasonal) series. The fix is to threshold the residuals of a seasonal model, not the raw series: (1) Decompose the series with STL (or similar) — extract the seasonal and trend components. (2) Compute residuals R_t = Y_t − T_t − S_t. (3) Fit a 3-sigma threshold on the residuals (use IQR-based σ̂ = IQR/1.35 for robustness to outliers in the residual). (4) Alert when |R_t| > 3σ̂. This immediately removes seasonality-driven false positives. Further: (5) Use EWMA control chart on residuals to adapt σ̂ to changing baseline variance (e.g., weekday vs weekend variance differ). (6) Set alert rate target (e.g., 5 alerts/day) and use empirical 99th percentile of residuals over a rolling 14-day window as the threshold — directly controls alert rate.'
      },
      {
        q: 'You use an LSTM autoencoder for multivariate anomaly detection on 50 metrics. The reconstruction error correctly identifies an outage on day 15 of deployment, but by day 90 the false positive rate has tripled. What happened and how do you fix it?',
        a: 'Concept drift: the "normal" behaviour of the 50 metrics has shifted over 90 days (traffic patterns, user mix, new features deployed), but the LSTM autoencoder was trained on day 0–14 data. The model now considers the new normal as anomalous because it deviates from its stale training distribution. The reconstruction error for normal recent data is now systematically higher, causing false positives. Fixes: (1) Continual learning / periodic retraining: retrain the autoencoder on a rolling 30-60 day window of recent data, treating it as normal. (2) Adaptive threshold: instead of a fixed reconstruction-error threshold, calibrate the threshold daily as the 99th percentile of reconstruction errors from the past 7-14 days. This separates threshold adaptation (easy) from model adaptation (expensive). (3) Change detection on the anomaly score itself: if the mean reconstruction error drifts upward steadily, trigger a model retrain. In practice: adaptive threshold is the first line of defence, periodic retraining is the second.'
      },
      {
        q: 'You detect a latency spike anomaly in your API service. Your colleague says it is a "real anomaly." You say it is a downstream symptom. How do you distinguish, and what are the implications for incident response?',
        a: 'The distinction is whether the latency spike is the root cause or a consequence of an upstream anomaly propagating through a dependency graph. Detection approach: (1) Check the temporal order: did the upstream service (database, cache, external API) anomaly precede the latency spike? If the database latency spiked 30 seconds before API latency, the API spike is a symptom. (2) Causal graph analysis: trace the call graph. If API → DB → Storage, and DB shows an anomaly simultaneously, the anomaly likely originated in DB or Storage. (3) Correlation analysis across metrics: a true root cause anomaly propagates to downstream metrics with a delay proportional to propagation latency. Cross-correlate anomaly times across metrics to find the earliest-onset metric in the dependency chain. Incident response implication: treating a symptom as root cause leads to incorrect mitigation (restart the API server when the DB is the issue). Building a causal dependency graph of services and running automated root cause analysis (e.g., Facebook\'s FBDC, Prometheus correlation tools) is the scalable solution for distributed systems.'
      },
      {
        q: 'You are designing an anomaly detection system for 10,000 IoT sensors. Labelled anomalies exist for only 50 sensors. How do you evaluate model performance across all 10,000?',
        a: 'With 50 labelled sensors and 9,950 unlabelled: (1) Supervised evaluation on labelled sensors: train a model, compute PR-AUC / F1 / precision@k on labelled sensors. Use this as a proxy for overall performance — valid if the 50 labelled sensors are representative of the full distribution. Check representativeness: do the 50 sensors span the range of behaviour types (failure modes, sensor types, traffic levels)? (2) Surrogate evaluation on unlabelled sensors: inject synthetic anomalies (point spikes, level shifts, seasonal distortions) into normal sensor data — evaluate detection rate on injected anomalies. This tests the detector\'s ability to find known anomaly types. (3) Alert rate monitoring: if a calibrated system should fire at 1% of timesteps, monitor alert rate across all 10,000 sensors. Systematic deviation (e.g., 5% alert rate on certain sensor clusters) indicates miscalibration for those clusters. (4) Human review sample: randomly sample 100 triggered alerts across all sensors, have a domain expert label them (true/false positive). Estimate overall precision from this sample. (5) Active learning: use the anomaly score to prioritise which unlabelled sensors to send for labelling — label the highest-scoring ambiguous cases to grow the labelled set efficiently.'
      },
    ],
    takeaway: `Nearly all production false-positive problems in time series anomaly detection trace back to applying a threshold to the raw series instead of the residuals of a properly specified seasonal+trend model. The decompose-first, threshold-on-residuals pattern eliminates seasonality-driven false positives immediately. The second most important insight is distinguishing root causes from downstream symptoms via causal graph traversal: a single upstream failure floods the alert queue with correlated alerts across dozens of metrics, and incident response fails when teams chase symptoms while the root cause persists.`,
  },

  {
    id: 'causal_ts',
    title: 'Causal Inference in Time Series',
    subtitle: 'Granger causality, ITS, synthetic control, CausalImpact, DiD, temporal autocorrelation',
    difficulty: 'advanced',
    estimatedMin: 65,
    tags: ['Granger causality', 'CausalImpact', 'synthetic control', 'interrupted time series', 'DiD', 'BSTS'],
    summary: `Granger causality is the most widely misused concept in applied time series work. It answers a predictive question — does X help predict Y beyond Y's own past? — not a causal one. A shared upstream cause Z that affects both X and Y with different lags produces Granger causality between X and Y with zero direct relationship. Knowing that search volume Granger-causes sales tells you nothing about whether investing in SEO will increase sales. The tools that actually support causal claims — synthetic control, CausalImpact, interrupted time series, difference-in-differences — all require a credible counterfactual: what would have happened to the treated unit absent the intervention. The harder problem in practice is staggered rollouts, where different units receive treatment at different times. Standard two-way fixed effects DiD is biased under treatment effect heterogeneity in this case — already-treated units contaminate the control group — and the fix (Callaway-Sant'Anna) is not widely known.`,
    keyPoints: [
      `Granger causality tests whether lagged X improves prediction of Y beyond lagged Y alone — it is a predictive test, not a causal test. A shared upstream cause Z that affects both X and Y with different lags produces Granger causality X → Y with no direct mechanism. TV advertising that simultaneously increases search volume and sales will produce Granger causality from search to sales even if search has no causal effect on sales. The correct interpretation: "X has predictive information about Y beyond Y's own past." The incorrect interpretation: "changing X will change Y."`,
      `Interrupted Time Series (ITS): OLS regression Y_t = β₀ + β₁T + β₂D_t + β₃(T − T*)D_t + ε_t, where D_t = 1{t ≥ T*}. β₂ is the immediate level change; β₃ is the slope change post-intervention. Critical assumptions: no other intervention coincides at T*, the pre-period trend extrapolates cleanly, and residuals are not autocorrelated (autocorrelated residuals underestimate SE — use Prais-Winsten or Newey-West correction). ITS works without any control unit but requires a credible counterfactual from the pre-period trend extrapolation.`,
      `Synthetic control (Abadie et al.): constructs a weighted combination of control units that matches the treated unit's pre-treatment outcomes as closely as possible. The post-treatment gap (treated − synthetic control) is the estimated treatment effect. The critical advantage over DiD: pre-treatment fit quality is directly observable — you can see whether the synthetic control matches the treated unit before claiming an effect. If pre-period fit is poor, the counterfactual is unreliable and the treatment effect estimate is invalid.`,
      `CausalImpact (Brodersen et al., Google 2015): Bayesian Structural Time Series fitted on the pre-intervention treated series using control series as regressors. Post-intervention: extrapolate the counterfactual; treatment effect = observed − counterfactual with full posterior credible intervals. Key assumption: the relationship between treated and control series is stable across the intervention. If control series were independently affected by the intervention (spillover), the counterfactual is polluted and the effect estimate is biased.`,
      `Difference-in-Differences: DiD = (Y_{treated,post} − Y_{treated,pre}) − (Y_{control,post} − Y_{control,pre}). The parallel trends assumption requires that the treated and control units would have moved identically absent the treatment. Temporal autocorrelation within units inflates t-statistics — cluster standard errors at the unit level. Always plot pre-trend event study coefficients to validate parallel trends before reporting a single DiD estimate.`,
      `Staggered DiD and the TWFE bias: when different units receive treatment at different times, two-way fixed effects (TWFE) regression uses already-treated units as controls for later-treated units. With heterogeneous treatment effects (e.g., the effect grows over time), TWFE produces estimates that can be negatively weighted — some comparisons literally subtract true treatment effects. Callaway-Sant'Anna computes cohort-specific ATTs (average treatment effects for each treatment cohort) using only clean controls (never-treated or not-yet-treated) and aggregates properly.`,
      `Temporal autocorrelation breaks standard causal methods in specific ways. IV exclusion restrictions become implausible when instrument and outcome share autocorrelated common trends. Regression discontinuity with time as the running variable is especially fragile — observations near the cutoff are highly correlated, conflating the treatment effect with local autocorrelation. Time-varying confounding requires marginal structural models (MSM) with inverse probability of treatment weighting (IPTW), not cross-sectional propensity score matching.`,
      `Event study (dynamic DiD): estimate effects at each relative time period with Y_{it} = αᵢ + λ_t + Σ_{k≠-1} δ_k 1{t − G_i = k} + ε_{it}. Pre-treatment δ_k coefficients (k < 0) test parallel trends — a joint F-test on pre-treatment periods is the validation. Post-treatment coefficients show dynamic effect trajectory. Always visualise as an event study plot before reporting a single DiD estimate — if pre-treatment effects are nonzero, the parallel trends assumption fails and the DiD estimate is biased.`,
    ],
    checkQuestions: [
      {
        q: 'You find that Google search volume for your brand Granger-causes weekly sales (F-test p < 0.001). A PM wants to invest in SEO to increase search volume and thereby increase sales. What is the problem with this reasoning?',
        a: 'Granger causality shows that lagged search volume predicts sales beyond sales\'s own history — it does not establish a causal mechanism where increasing search volume will increase sales. Two alternative explanations: (1) Common cause: TV advertising or PR events simultaneously increase search volume AND sales — neither causes the other. The advertising is the Granger cause of both, creating spurious Granger causality from search to sales. (2) Reverse causality with lag: sales growth leads to word-of-mouth, which increases search interest — the temporal lag creates the appearance of search leading sales. To distinguish: (a) Run an experiment — randomly vary search ads spend and see if sales respond. (b) Look for instrumental variable — a policy that affects search volume but not sales directly. (c) Control for advertising spend in the Granger VAR — if the Granger causality disappears when advertising is included, it was a common cause. Granger causality is a useful screening tool for hypotheses, not a justification for interventions.'
      },
      {
        q: 'You are measuring the impact of a new feature launched to users in Germany on January 15. You have daily active users (DAU) data for Germany (treated) and France (control) from January 2023 onwards. How do you use CausalImpact and what assumptions must hold?',
        a: 'Setup: pre-period = 2023-01-01 to 2025-01-14, post-period = 2025-01-15 onwards. Model: fit BSTS on Germany DAU using France DAU as regressor during pre-period. Post-period: extrapolate the counterfactual using the Germany-France relationship learned pre-launch; compare to observed Germany DAU. Assumptions: (1) Stable relationship: the Germany-France DAU relationship must be stable across the launch. If France had its own event (holiday, competitor launch) in the post-period, the counterfactual is polluted. (2) No spillover: France users are not affected by the Germany feature — violated if users can VPN or if the feature affects platform-wide dynamics. (3) Pre-period adequacy: enough pre-period data (at least 3x the post-period length) for the BSTS to learn a reliable relationship. (4) Control quality: France must behave similarly to Germany absent the treatment — check pre-period parallel trends. Output: posterior distribution of the causal effect with 95% credible intervals. Report cumulative effect and point estimate with uncertainty.'
      },
      {
        q: 'Your company rolls out a pricing change to different markets in different months over a 6-month window. You use TWFE DiD to estimate the effect. A colleague says your estimate is biased. Why and what do you do?',
        a: 'Staggered treatment timing invalidates TWFE in the presence of treatment effect heterogeneity across cohorts or time. TWFE uses "already treated" units as control units for "later treated" units. If the treatment effect is dynamic (e.g., the pricing change has larger effects 3 months after adoption than 1 month after), already-treated units contaminate the control group with their own treatment effects. The TWFE estimator δ is a weighted average of cohort-specific effects where the weights depend on group sizes and timing — they can be negative, meaning some comparisons subtract true treatment effects. Correct approach: (1) Use Callaway-Sant\'Anna (CS) estimator: compute 2x2 DiD for each (cohort, relative time) combination using "clean controls" (never-treated or not-yet-treated units only). Aggregate cohort-specific ATTs. (2) Alternatively, use Sun-Abraham: interaction-weighted estimator that is robust to treatment effect heterogeneity. (3) Plot event-study coefficients — the shape of dynamic effects tells you if heterogeneity is the issue (flat post-treatment effects → less bias from TWFE).'
      },
      {
        q: 'You want to estimate the causal effect of an algorithm change on user engagement, but the change was rolled out gradually to all users with no holdout group. CausalImpact, synthetic control, and ITS all require a control group or counterfactual. What do you do?',
        a: 'Without any control group, causal estimation is extremely difficult but not impossible. Options: (1) ITS with internal counterfactual: model the pre-change trend (including seasonal components) and extrapolate. The treatment effect = observed post-change − extrapolated counterfactual. Valid only if the pre-change model is well-specified and no other events coincide with the change. Use BSTS to incorporate prior knowledge about expected post-change dynamics. (2) Regression discontinuity in time: if the rollout happened sharply at a known date, compare users just before and just after the date. Works only if: (a) users are not anticipating the change, (b) the engagement metric is continuous at the boundary absent the treatment, (c) no concurrent events. Temporal autocorrelation near the boundary inflates t-statistics — use CCT bandwidth selection with robust SE. (3) Donor pool construction: find external series (competitor platforms, adjacent product lines) not affected by the change and use them as synthetic control donors. Requires strong parallel-trends assumption between internal and external series. (4) Honest answer: without a holdout group, causal identification is not possible without strong untestable assumptions. The right fix is prospective: design future launches with a holdout or phased rollout for causal evaluation.'
      },
    ],
    takeaway: `Granger causality is the most frequently misused concept in applied time series work: it measures predictive priority, not causation, and a common upstream cause produces Granger causality between two otherwise unrelated series. The second most important insight for applied causal time series is that TWFE DiD is biased under staggered rollouts with heterogeneous treatment effects — already-treated units contaminate the control group, and the fix is Callaway-Sant'Anna, not just clustering standard errors. Always run an event study plot before reporting any DiD estimate.`,
  },
]

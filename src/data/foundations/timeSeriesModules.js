export const TIME_SERIES_MODULES = [
  {
    id: 'stationarity',
    title: 'Stationarity & Differencing',
    subtitle: 'Unit root tests, spurious regression, integration order, cointegration',
    difficulty: 'foundational',
    estimatedMin: 35,
    tags: ['stationarity', 'unit-root', 'differencing', 'cointegration', 'ADF', 'KPSS'],
    summary: `Stationarity is the most commonly violated assumption in time series modelling, yet it is misunderstood as a property to "fix" rather than a diagnostic signal about the data-generating process. A process is strictly stationary if its joint distribution is time-invariant; in practice, weak (covariance) stationarity — constant mean, variance, and autocovariance depending only on lag — is what most models require. The critical non-obvious insight is that regressing one random walk on another produces spurious correlation close to 1 even when the series are independent, because both absorb the same shared time trend. Cointegration refines this: two I(1) series can have a stationary linear combination (their "spread"), and error correction models exploit this to model their joint dynamics.`,
    keyPoints: [
      'Weak (covariance) stationarity requires: E[Y_t] = μ (constant), Var(Y_t) = σ² (constant), Cov(Y_t, Y_{t-k}) = γ(k) (depends only on lag k, not t). Strict stationarity requires the full joint distribution to be time-invariant — strictly stronger, rarely tested directly.',
      'A random walk Y_t = Y_{t-1} + ε_t is I(1): its variance grows as tσ², mean is constant but meaningless. Differencing once yields ΔY_t = ε_t which is I(0). The "I(d)" notation means d differences are needed to achieve stationarity.',
      'Spurious regression: OLS of one independent random walk on another typically yields |t| > 2 and R² > 0.5 even with zero true relationship. The Durbin-Watson statistic near 0 is a red flag — residuals are extremely autocorrelated, meaning the standard OLS inference is completely invalid.',
      'ADF (Augmented Dickey-Fuller) test: tests H₀: unit root present (non-stationary) against H₁: stationary. Includes lags of ΔY_t to purge autocorrelation from residuals. Critical values are non-standard (more negative than t-distribution). Low power in small samples — often fails to reject the null even when the series is trend-stationary.',
      'KPSS test: flips the null — H₀: stationary, H₁: unit root. Complementary to ADF. Run both: if ADF rejects and KPSS fails to reject → stationary. If ADF fails to reject and KPSS rejects → unit root. If both reject → the series may be fractionally integrated or locally non-stationary. If both fail to reject → insufficient data to distinguish.',
      'Trend stationarity vs difference stationarity: a trend-stationary series has a deterministic trend (Y_t = α + βt + ε_t, stationary around the trend) and should be detrended, not differenced. A difference-stationary series (random walk with drift) needs differencing. Detrending a difference-stationary series does not achieve stationarity — residuals still have a unit root.',
      'Cointegration: two I(1) series X_t and Y_t are cointegrated if there exists β such that Y_t − βX_t is I(0). Economic interpretation: the series share a long-run equilibrium. Engle-Granger test: regress Y on X, test ADF on residuals. Johansen trace/max-eigenvalue test handles multiple cointegrating vectors.',
      'Error Correction Model (ECM): ΔY_t = α(Y_{t-1} − βX_{t-1}) + short-run dynamics. The error-correction term α pulls the system back toward equilibrium. α < 0 ensures mean reversion. Cointegrated series must be modelled as ECM — differencing alone loses long-run information.',
      'Production gotcha: stationarity can change over time. A series stationary during 2015–2019 may develop a unit root post-2020 due to structural shifts. Rolling ADF tests or online monitoring of variance are more reliable than one-time tests on historical data.',
    ],
    checkQuestions: [
      {
        q: 'You regress daily revenue on daily temperature for 3 years and get R² = 0.72 with t-stat = 18. The DW statistic is 0.12. What is wrong and how do you fix it?',
        a: 'DW ≈ 0.12 means residuals are nearly perfectly positively autocorrelated — a hallmark of spurious regression between two non-stationary series. Both revenue and temperature likely have trends or unit roots. OLS standard errors are severely underestimated (White noise assumption violated), making the t-stat meaningless. Fix: (1) Test both series for stationarity with ADF/KPSS. (2) If both I(1), test for cointegration. (3) If not cointegrated, work in first differences: regress ΔRevenue on ΔTemperature. (4) If cointegrated, use an ECM. The R² of 0.72 is an artifact of shared time trend absorption, not genuine predictive power.'
      },
      {
        q: 'ADF says your series is stationary but KPSS also rejects its null. What does this mean and what do you do?',
        a: 'This is a conflicting signals situation. ADF rejects H₀ (unit root) → evidence for stationarity. KPSS rejects H₀ (stationarity) → evidence for unit root. Both rejecting suggests the series may be: (1) fractionally integrated I(d) with 0 < d < 1 — neither purely stationary nor I(1), (2) locally stationary but with structural breaks that resemble non-stationarity, or (3) the sample is too short for reliable inference. Actions: test for structural breaks (Zivot-Andrews ADF allows one break), try ARFIMA (fractional differencing), or apply regime-change tests. Do not blindly difference — over-differencing introduces unit roots in the MA component.'
      },
      {
        q: 'You have two financial time series (stock price and its futures contract price) that are both I(1). How do you decide whether to model them separately in first differences or jointly?',
        a: 'Test for cointegration. Spot and futures prices of the same asset are theoretically cointegrated by no-arbitrage — their spread (basis) should be mean-reverting. Run the Johansen trace test: if you find one cointegrating vector, use a Vector Error Correction Model (VECM). The spread Y_t − βX_t is the error-correction term. Modelling each in first differences independently ignores the long-run equilibrium and loses information. In production: the ECM coefficient α quantifies speed of mean reversion — useful for pairs trading strategies. If α is near zero, cointegration is nominal and not exploitable.'
      },
      {
        q: 'Your revenue series passes ADF stationarity test. You fit an ARIMA(1,0,1) and the residuals look clean. Six months later the model performance degrades sharply. What likely happened and how do you detect it earlier?',
        a: 'The most likely cause is a structural break — the underlying data-generating process changed (new product launch, market expansion, seasonality shift). A one-time ADF test only confirms stationarity at the point of fitting; it does not guarantee future stationarity. Early detection strategies: (1) rolling ADF test with a sliding window of 90–180 days — alert when the p-value rises above 0.1. (2) Monitor rolling mean and variance of the series; statistically significant shifts (CUSUM or MOSUM tests) indicate non-stationarity. (3) Track model forecast errors with a CUSUM chart — systematic drift in errors precedes performance collapse by weeks.'
      },
    ],
  },

  {
    id: 'arima_family',
    title: 'ARIMA Family',
    subtitle: 'AR/MA intuition, ACF/PACF identification, SARIMA, Box-Jenkins, structural break failure',
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['ARIMA', 'SARIMA', 'ACF', 'PACF', 'Box-Jenkins', 'autoregression'],
    summary: `ARIMA models are best understood not as a forecasting algorithm but as a statistical characterisation of how past values and past errors propagate forward in time. The AR component models momentum (past values predict future values) while the MA component models shock absorption (past forecast errors propagate into future values). The key insight practitioners miss is that ACF/PACF interpretation is an art, not a rule: real-world series rarely show clean cut-offs because AR and MA components mix. Box-Jenkins methodology is iterative and domain-knowledge-heavy; software that auto-selects (p,d,q) via AIC is a useful heuristic but not a substitute for understanding what the parameters represent.`,
    keyPoints: [
      'AR(p) — autoregressive: Y_t = φ₁Y_{t-1} + ... + φ_pY_{t-p} + ε_t. Current value is a weighted sum of p lagged values plus noise. Stationarity requires all roots of the characteristic polynomial to lie outside the unit circle. AR captures momentum, mean reversion, and oscillations depending on the sign of φ.',
      'MA(q) — moving average: Y_t = ε_t + θ₁ε_{t-1} + ... + θ_qε_{t-q}. Current value is a weighted sum of q past shocks plus current shock. MA is always stationary (finite sum of white noise). Invertibility requires roots of MA polynomial outside unit circle — needed for unique parameter identification.',
      'ACF and PACF for identification: AR(p) → ACF decays exponentially/sinusoidally, PACF cuts off sharply after lag p. MA(q) → PACF decays, ACF cuts off sharply after lag q. ARMA(p,q) → both decay. In practice, the cut-offs are rarely clean — use ACF/PACF as a starting point, compare multiple models by AIC/BIC.',
      'ARIMA(p,d,q): differencing d times to achieve stationarity, then fitting ARMA(p,q) on the differenced series. Over-differencing is a common mistake: d=2 is rarely needed; if d=1 residuals still have unit root, check for structural break or seasonality rather than adding d=2.',
      'SARIMA(p,d,q)(P,D,Q)[s]: adds seasonal AR, seasonal differencing, and seasonal MA terms at lag multiples of s. For monthly data with annual seasonality s=12: seasonal difference at lag 12 handles multiplicative seasonality. Interaction between non-seasonal and seasonal components makes parameter interpretation difficult.',
      'Box-Jenkins methodology: (1) Identify: stationarise, plot ACF/PACF, tentatively identify orders. (2) Estimate: MLE or conditional least squares. (3) Diagnose: Ljung-Box test on residuals (H₀: residuals are white noise), ACF of residuals, check for heteroskedasticity. (4) Iterate. The iterate step is non-trivial — failed Ljung-Box with lag-12 spike suggests missing seasonal term.',
      'Why ARIMA fails on structural breaks: ARIMA assumes the (p,d,q) parameters are constant over time. A structural break — regime change, crisis, product discontinuation — violates this. The model absorbs the break as a long-lag effect, producing biased forecasts indefinitely after the break. Detection: CUSUM test, Chow test at known break date, or Zivot-Andrews test for unknown break.',
      'Information criteria: AIC = 2k − 2ln(L) penalises parameters mildly (favours larger models). BIC = k·ln(n) − 2ln(L) penalises more strongly (favours parsimony). BIC is preferred for prediction in large samples; AIC for small samples or when the true order is complex.',
      'Production reality: ARIMA works best for low-noise, single-series problems with 2–10 years of history and stable structure. It does not handle external regressors naturally (use ARIMAX), does not handle multiple seasonalities, and requires manual order selection at scale. At FAANG scale with thousands of time series, Prophet or automated neural forecasters often outperform ARIMA without per-series tuning.',
    ],
    checkQuestions: [
      {
        q: 'Your ACF shows significant spikes at lags 1, 2, 3 that decay, and your PACF shows a single significant spike at lag 1 that cuts off. What model do you fit and why?',
        a: 'This pattern — PACF cuts off at lag 1, ACF decays — is the signature of an AR(1) process. For AR(p): PACF cuts off sharply after p, ACF decays. Fit ARIMA(1,0,0) if the series is stationary. Verify: after fitting AR(1), check residuals with Ljung-Box — if significant autocorrelation remains at lag 2+, try AR(2) or AR(1) with an MA component. The decaying ACF pattern (exponential or sinusoidal) vs. a cut-off at q is what distinguishes AR from MA identification.'
      },
      {
        q: 'You fit ARIMA(2,1,2) to a monthly sales series. The Ljung-Box test on residuals passes (p=0.42), but the ACF of residuals shows a significant spike at lag 12. What does this mean and what is the fix?',
        a: 'The Ljung-Box passing at overall level does not mean all lags are clean — it tests a weighted combination. The significant ACF spike at lag 12 is a seasonal pattern not captured by the non-seasonal ARIMA. The residuals are not white noise at the seasonal frequency. Fix: add a seasonal component — upgrade to SARIMA(2,1,2)(1,0,1)[12] or SARIMA(2,1,2)(0,1,1)[12]. Start with seasonal differencing D=1 (subtract last year\'s value): if seasonal pattern is roughly constant in variance, additive seasonality, D=1 should remove it. Then fit seasonal MA(1) for the remaining lag-12 spike. Recheck Ljung-Box specifically at seasonal lags (12, 24, 36).'
      },
      {
        q: 'Your e-commerce platform has 50,000 product SKUs. You need daily sales forecasts for each. Why is per-SKU ARIMA unrealistic and what do you use instead?',
        a: 'Per-SKU ARIMA is unrealistic for several reasons: (1) Order selection requires human-in-the-loop ACF/PACF interpretation or expensive auto-ARIMA search (grid search over AIC for each SKU). (2) Many SKUs have intermittent demand (many zeros) — ARIMA is undefined or collapses. (3) Cold start: new SKUs have <30 days of history, insufficient for stable parameter estimation. (4) SKU-level structural breaks (promotions, stockouts) are frequent and not handled automatically. Better approaches: (1) Global neural models (N-BEATS, LightGBM with lag features) trained across all SKUs — cross-series patterns improve sparse SKU forecasts. (2) Croston\'s method or ADIDA for intermittent demand. (3) Hierarchical forecasting with top-down or bottom-up reconciliation (MinT). At FAANG scale, gradient-boosted trees with hand-crafted lag, seasonal, and event features typically dominate per-SKU ARIMA.'
      },
      {
        q: 'You fit ARIMA(0,1,1) and ARIMA(1,1,0) to the same series. Both pass diagnostics. The MA model has lower AIC. A colleague argues the AR model is more interpretable for business stakeholders. How do you decide?',
        a: 'This exposes the ARIMA duality: ARIMA(0,1,1) with θ near 1 is mathematically equivalent to exponential smoothing (SES). ARIMA(1,1,0) with φ near 1 is close to a random walk with momentum. Both can fit similar patterns because AR(∞) = MA(1) under invertibility (Wold decomposition). Decision framework: (1) If forecasting accuracy is the only criterion, use the lower AIC model — ARIMA(0,1,1). (2) If stakeholder communication matters, AR is more naturally expressed as "next value = weighted average of recent values." (3) Check out-of-sample performance on a held-out window — AIC is in-sample; the better AIC model may overfit. (4) If both models produce nearly identical forecasts (check PI width and point forecasts), the distinction is irrelevant in production — deploy the simpler one.'
      },
    ],
  },

  {
    id: 'seasonality_decomposition',
    title: 'Seasonality & Decomposition',
    subtitle: 'STL, additive vs multiplicative, X-13-ARIMA-SEATS, Fourier terms, irregular seasonality',
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['seasonality', 'STL', 'decomposition', 'Fourier', 'X-13', 'additive', 'multiplicative'],
    summary: `Seasonal decomposition is not just a visualisation aid — it is a modelling decision with downstream consequences. Additive vs multiplicative decomposition reflects whether seasonal effects are constant in absolute terms or scale with the trend level. The critical non-obvious point: STL (Cleveland et al., 1990) dominates classical decomposition and X-11 for most ML use cases because it handles multiple seasonalities, is robust to outliers via LOESS, and allows the seasonal component to evolve over time. The hardest seasonality problems in production are not fixed-period seasonal effects but irregular ones — social media virality, shopping events that shift by calendar year, or dual seasonalities (daily + weekly in real-time systems).`,
    keyPoints: [
      'Additive decomposition: Y_t = T_t + S_t + R_t. Appropriate when seasonal amplitude is constant regardless of the trend level (e.g., a store that always sells exactly 200 extra units in December). Multiplicative: Y_t = T_t × S_t × R_t. Appropriate when seasonal amplitude scales with the level (e.g., 20% higher sales in December — the absolute lift grows as the business grows). Log transform converts multiplicative to additive: log(Y_t) = log(T_t) + log(S_t) + log(R_t).',
      'Classical decomposition (moving average): extract trend via centred moving average of window size s, compute seasonal indices as averages of (Y_t − T_t) per season, then R_t = Y_t − T_t − S_t. Critical flaw: the seasonal component is forced to be identical across all years — cannot evolve. Also, trend cannot be estimated at the endpoints (first and last s/2 observations are missing).',
      'STL (Seasonal and Trend decomposition using Loess): iterative procedure alternating a seasonal smoother (inner loop) and trend smoother (outer loop). Key parameters: period (s), trend window (t.window), seasonal window (s.window). s.window="periodic" forces constant seasonality; smaller integer values allow more rapid seasonal evolution. Outlier robustness: if robust=True, downweights observations with large residuals in LOESS fitting — point anomalies do not corrupt seasonal estimates.',
      'STL advantage for multiple seasonalities: STL can be applied iteratively (MSTL) to extract multiple seasonal components at different frequencies — e.g., daily + weekly for hourly data. First decompose at weekly frequency, then decompose the remainder at daily frequency.',
      'X-11/X-12-ARIMA/X-13-ARIMA-SEATS: US Census Bureau methods used by national statistics agencies. X-13 runs ARIMA on the series before decomposition to handle outliers, trading day effects, and holiday effects explicitly. More rigorous than STL for official statistics; overkill for most ML applications. SEATS (Signal Extraction in ARIMA Time Series) is the model-based component within X-13.',
      'Fourier terms for seasonality in regression: instead of dummy variables (one per season = high dimensionality), use K pairs of Fourier terms: sin(2πkt/s) and cos(2πkt/s) for k=1,...,K. Larger K allows more complex seasonal shapes; K=s/2 is the saturated model (equivalent to dummies). Advantage: smooth seasonal curve with few parameters; works for non-integer or long periods (e.g., annual seasonality in daily data: period = 365.25, not feasible with dummies).',
      'Irregular / dynamic seasonality: the hardest production case. Social media engagement has no fixed period — viral events create spurious "seasonality." Retail shopping events (Black Friday, Ramadan, Diwali) shift by 1–4 weeks per year relative to the Gregorian calendar. Holiday-adjusted series (using a holiday calendar with effect windows) outperform pure Fourier/STL approaches here. Prophet\'s holiday component and X-13 trading-day adjustment handle specific known shifts; purely data-driven STL does not.',
      'Seasonal adjustment before forecasting: a common pipeline is decompose → forecast each component separately → recompose. But forecasting the residual directly (after removing trend and seasonality) with ARIMA often outperforms forecasting the raw series. The residual is closer to stationary and has no seasonal ARIMA complexity.',
      'Gotcha: seasonal adjustment can remove legitimate signal. For a product with demand driven by weather, the "weather seasonality" is not noise — it is the causal mechanism. Removing it then adding weather as an external regressor is the correct approach, not treating seasonality as purely nuisance variation.',
    ],
    checkQuestions: [
      {
        q: 'You apply STL decomposition to weekly website traffic and the seasonal component shows the amplitude doubling over 3 years while the trend also doubles. Is additive or multiplicative STL more appropriate and how does this change your downstream modelling?',
        a: 'The seasonal amplitude scaling with the trend level is the definition of multiplicative seasonality. Additive STL will extract an evolving seasonal component that appears to grow, but this is an artifact — it conflates trend and seasonal interaction. Fix: take log(Y_t) and apply STL to the log series (converting multiplicative to additive). The extracted log-seasonal component should then be constant (or slowly evolving) over time. For downstream modelling: forecast log(T_t) and log(S_t) separately, then exponentiate to recover the original scale. Prediction intervals must also be computed on the log scale and back-transformed (they will be asymmetric on the original scale — wider above the point forecast than below).'
      },
      {
        q: 'You are building a real-time anomaly detector for server error rates, which show strong daily and weekly patterns. STL fails because STL requires the series to be longer than two periods and the seasonal window must be odd. For 10-minute data with both daily (144 points/period) and weekly (1008 points/period) seasonality, what is your approach?',
        a: 'For dual-frequency high-frequency data: (1) MSTL: apply STL iteratively — first decompose at weekly period (1008), then decompose the residuals at daily period (144). This handles both seasonalities. (2) Alternatively, use Fourier terms directly in an online regression model: maintain a running regression of Y_t on K=5-10 Fourier pairs for daily and weekly periods, use the regression residual as the anomaly signal. Fourier regression is computationally cheap and updateable online. (3) For production anomaly detection specifically: use STL-decomposed baseline + 3-sigma band on the residuals, but update the decomposition on a rolling 4-week window rather than the full history to adapt to gradual shifts. Ljung-Box on the residuals confirms whether the seasonal removal is adequate.'
      },
      {
        q: 'A stakeholder asks why your Black Friday sales forecast is consistently off by 20%. Your model uses STL decomposition with a fixed 52-week seasonal pattern. What is the root cause?',
        a: 'STL with fixed period assumes seasonality is aligned to the same calendar week every year. Black Friday falls on the fourth Thursday of November — it shifts between November 22 and November 28 each year, a ±3 business day shift. In STL, this smears the Black Friday spike across 3-4 weeks in the seasonal component, attenuating the peak estimate. Fix: (1) Add an explicit holiday indicator feature covering the Black Friday window (Nov 22 – Dec 2) with effects that can vary by year — use a holiday-effect model (as in Prophet or X-13) rather than encoding it in the seasonal component. (2) Use TBATS (Trigonometric seasonality, Box-Cox, ARMA errors, Trend, Seasonal) which handles calendar effects. (3) At FAANG scale: encode Black Friday as a feature (days_to_black_friday, is_black_friday_week) in a gradient boosted model — more flexible than statistical decomposition.'
      },
      {
        q: 'What does it mean to set s.window="periodic" in STL, when is it correct to do so, and what is the risk of always using it?',
        a: 's.window="periodic" forces the seasonal component to be identical across all years — the seasonal pattern is extracted as a fixed template. This is appropriate when: the underlying seasonal driver is physically fixed (e.g., the number of daylight hours follows a fixed annual cycle). The risk of always using it: if seasonal patterns evolve — consumer behaviour shifts, population ages, product mix changes — a fixed seasonal template will misfit recent years, leaving systematic residuals that look like trend but are actually seasonality drift. Detection: plot the seasonal component extracted in different years; if it visibly changes, use a finite s.window (e.g., 7 or 11 for annual seasonality in weekly data). Smaller s.window = more flexible seasonal evolution but noisier estimates. The right value is usually the smallest window that does not visibly overfit noise in the seasonal component.'
      },
    ],
  },

  {
    id: 'prophet_framework',
    title: 'Prophet',
    subtitle: 'Piecewise growth, Fourier seasonality, changepoints, uncertainty, failure modes',
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['prophet', 'changepoints', 'Fourier', 'piecewise-linear', 'uncertainty', 'holiday'],
    summary: `Prophet (Taylor & Letham, 2018) solves a specific, real problem at Meta: producing interpretable, robust forecasts for thousands of analyst-facing business metrics at a company where analysts without time series expertise need to forecast and diagnose trends. Its piecewise linear growth model with automatic changepoint detection is best understood as a structural additive regression model, not a black box. The misunderstood part: Prophet is not a general-purpose forecaster — it is optimised for business KPIs with strong trend + weekly + yearly seasonality. When the data does not match this generative model (e.g., highly volatile financial series, series dominated by external shocks, multiplicative seasonality at scale), Prophet underperforms dramatically.`,
    keyPoints: [
      'Growth model: Prophet models trend as piecewise linear (or piecewise logistic for saturating growth). Piecewise linear: g(t) = (k + aᵀδ)t + (m + aᵀγ), where δ is a vector of rate changes at changepoints and a is an indicator vector. The model places changepoints automatically across the first 80% of the training data (configurable) and uses a Laplace prior on δ to enforce sparsity — most changepoints have δ ≈ 0.',
      'Seasonality via Fourier series: S(t) = Σ_{n=1}^{N} [aₙ cos(2πnt/P) + bₙ sin(2πnt/P)]. P = 365.25 for yearly, 7 for weekly. N controls flexibility: N=10 for yearly (20 parameters), N=3 for weekly (6 parameters). Fourier terms are fitted as OLS regression coefficients conditional on trend — not estimated via spectral methods.',
      'Holiday effects: additive indicator regression. For each holiday h and each day d in a window around h, Prophet adds an indicator feature. Each holiday gets its own coefficient. The key advantage over dummy variables: the window width and smoothing can be tuned; lower-country / narrower-scope holidays need separate calendars.',
      'Changepoint detection mechanics: Prophet does not detect changepoints post-hoc; it places changepoints at pre-specified potential locations and uses the Laplace prior to shrink most δ to zero. changepoint_prior_scale (default 0.05) controls how aggressively trend changes are allowed. Large value → more flexible trend → risk of overfitting recent trend. Small value → smooth trend → lags structural breaks. This is the most consequential hyperparameter in Prophet.',
      'Uncertainty quantification: Prophet offers two uncertainty modes. (1) MAP (Maximum A Posteriori, default): fast, but confidence intervals only reflect observation noise and future changepoint uncertainty, not parameter uncertainty. (2) MCMC sampling (mcmc_samples > 0): full posterior — slower but correct uncertainty. For business forecasts where interval coverage matters (inventory safety stock), MCMC intervals are meaningfully wider and better calibrated.',
      'Prophet failure modes: (1) Trend is not piecewise linear — mean-reverting series (prices), oscillating series, series with abrupt permanent level shifts (Prophet fits the shift slowly via changepoints). (2) Multiplicative seasonality at scale — Prophet supports multiplicative seasonality (seasonality_mode="multiplicative") but only as a global mode, not mixed additive/multiplicative. (3) Series driven by external shocks or regressors — Prophet with one external regressor (add_regressor) is too rigid. (4) Short series — Prophet needs ≥ 1–2 years to reliably identify annual seasonality; shorter series degrade gracefully but lose seasonal fit quality.',
      'Add_regressor: exogenous regressors can be added as additional linear features. Critical gotcha: the regressor must be available at forecast time — if you add daily weather as a regressor, you need weather forecasts for the horizon period. Many production failures stem from training with known regressor values but failing to provide forecasted regressor values during inference.',
      'Cross-validation in Prophet: use prophet.diagnostics.cross_validation() with initial (training window), period (spacing between cutoffs), and horizon. Returns forecasts at multiple historical cutoff dates — compute MAPE/RMSE/MAE across cutoffs. This is rolling-origin evaluation, not random train-test split. Use performance_metrics() to summarise by forecast horizon.',
    ],
    checkQuestions: [
      {
        q: 'Your Prophet model produces a forecast for next quarter that shows a sharp trend acceleration starting exactly where your training data ends. What is the likely cause and how do you fix it?',
        a: 'This is a classic changepoint detection artifact. Prophet places potential changepoints in the first 80% of training data. Near the end of training, the model has fewer changepoints, so recent trend changes go undetected — but the Fourier seasonal terms and changepoint extrapolation create an illusion of acceleration at the forecast boundary. More commonly: if the last few months of training data had an unusual positive trend (holiday surge, marketing push), Prophet extrapolates this trend into the future. Fix: (1) Inspect the trend component — call plot_components() and check if the trend curve is realistic at the forecast horizon. (2) Reduce changepoint_prior_scale to smooth the trend. (3) Set changepoint_range=0.95 to allow changepoints closer to the end of training. (4) Add an explicit growth cap with logistic growth to prevent trend explosion. (5) Most importantly: domain-knowledge check — is this extrapolation physically plausible?'
      },
      {
        q: 'You add daily temperature as an external regressor to Prophet to forecast energy demand. During backtesting, MAPE is 3%. In production, MAPE is 22%. What happened?',
        a: 'The regressor value at forecast time is unknown. During backtesting, you used actual observed temperature (future-known values) for the forecast period — a lookahead bias. In production, you would need a weather forecast for the forecast horizon. If actual temperatures were substituted with temperature forecasts (which have their own errors), the propagated regressor error inflates forecast error. Fix: (1) In cross-validation, only use temperature forecasts (not actuals) for dates beyond each cutoff — simulate production conditions. (2) Alternatively, use temperature as a feature only for short-horizon forecasts (1–3 days ahead) where weather forecasts are accurate; beyond 7 days, model temperature uncertainty explicitly or remove the regressor. (3) Check regressor correlation over the holdout: if temperature dropped during a period not seen in training, the model may extrapolate outside its training distribution.'
      },
      {
        q: 'A manager wants a 90% prediction interval for monthly revenue 6 months out. You have a trained Prophet model. What are the steps to produce well-calibrated intervals and what are their limitations?',
        a: 'Steps: (1) Enable MCMC sampling: set mcmc_samples=300 (or higher) in the Prophet constructor — MAP estimation only models uncertainty from future trend changepoints and observation noise, not parameter uncertainty. MCMC gives full posterior over all parameters. (2) Set interval_width=0.9. (3) Evaluate calibration: run cross_validation() with horizon=6 months across multiple historical cutoffs; compute coverage_rate from performance_metrics() — a well-calibrated 90% PI should contain the actuals ~90% of the time. Limitations: (1) Prophet interval uncertainty comes from future changepoints (sampled from the historical changepoint prior) and observation noise — it does not model regime changes not present in history. (2) 6-month forecasts have compound uncertainty; Prophet intervals often undercover at long horizons. (3) If the series has heavy-tailed noise (e.g., viral spikes), Gaussian likelihood underestimates tail risk — consider Student-t likelihood via a custom Stan model.'
      },
    ],
  },

  {
    id: 'exponential_smoothing',
    title: 'Exponential Smoothing & ETS',
    subtitle: 'SES, Holt, Holt-Winters, ETS state space, connection to ARIMA, MLE tuning',
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['exponential smoothing', 'ETS', 'Holt-Winters', 'state space', 'SES', 'MLE'],
    summary: `Exponential smoothing methods are among the most reliably accurate short-horizon forecasters in practice, consistently outperforming ARIMA on large benchmark datasets (M3, M4 competitions). The key insight is that ETS (Error-Trend-Seasonality) is not a heuristic update rule but a proper statistical model with a state space formulation, likelihood function, and optimally estimated parameters via MLE. The theoretical connection to ARIMA (certain ARIMA models are equivalent to certain ETS models) explains why ETS is competitive: it is selecting the right ARIMA order automatically via the structural assumption of exponential decay in the weights given to past observations.`,
    keyPoints: [
      'Simple Exponential Smoothing (SES): ŷ_{t+1} = αY_t + (1−α)ŷ_t = Σ_{j=0}^{t-1} α(1−α)^j Y_{t-j}. The smoothing parameter α ∈ (0,1) controls recency weighting. α=1: only last observation matters (naïve forecast). α→0: all observations weighted equally (global mean). Optimal α is estimated by MLE. SES is optimal for a random walk with Gaussian noise — i.e., ARIMA(0,1,1) with θ = 1−α.',
      'Holt\'s double exponential smoothing: extends SES with a trend component. Two update equations: level L_t = αY_t + (1−α)(L_{t-1} + b_{t-1}); slope b_t = β(L_t − L_{t-1}) + (1−β)b_{t-1}. Forecast: ŷ_{t+h} = L_t + hb_t. The slope extrapolates linearly into the future — problematic at long horizons. Damped trend variant: ŷ_{t+h} = L_t + (φ + φ² + ... + φʰ)b_t with 0 < φ < 1 damps the trend toward zero, improving long-horizon accuracy.',
      'Holt-Winters triple exponential smoothing: adds a seasonal component. Additive: S_t = γ(Y_t − L_{t-1} − b_{t-1}) + (1−γ)S_{t-s}; ŷ_{t+h} = L_t + hb_t + S_{t+h-s}. Multiplicative: S_t = γ(Y_t / (L_{t-1} + b_{t-1})) + (1−γ)S_{t-s}; ŷ_{t+h} = (L_t + hb_t)S_{t+h-s}. Three smoothing parameters (α, β, γ) optimised jointly by MLE.',
      'ETS framework (Hyndman et al.): unifies all exponential smoothing methods under state space notation. ETS(E, T, S): E ∈ {A, M} (additive or multiplicative error), T ∈ {N, A, Ad, M, Md} (None, Additive, Additive damped, Multiplicative, Multiplicative damped), S ∈ {N, A, M} (None, Additive, Multiplicative). 30 possible combinations (some non-identifiable). AIC over all well-defined models selects the best.',
      'ETS likelihood and AIC: ETS has a proper Gaussian (or gamma for multiplicative error) likelihood. Parameters (α, β, γ, φ, initial states) jointly estimated by numerical MLE. This is faster and more reliable than grid search. AIC = −2ln(L) + 2k selects among ETS model classes — completely automated, no ACF/PACF plotting required.',
      'ETS-ARIMA equivalence: SES (ETS(A,N,N)) = ARIMA(0,1,1). Holt\'s method (ETS(A,A,N)) = ARIMA(0,2,2). Holt-Winters additive (ETS(A,A,A)) = ARIMA(0,1,m+1)(0,1,0)[m]. The equivalence means ETS implicitly selects the ARIMA order via structural assumption — it is model selection by construction rather than by ACF/PACF exploration.',
      'Why ETS often beats ARIMA in practice: (1) Fewer parameters — ETS models are more parsimonious. (2) Multiplicative error models (ETS(M,...)) handle heteroskedasticity naturally; ARIMA with Gaussian errors does not. (3) ETS initial state estimation is numerically stable; ARIMA has more convergence issues in MLE. (4) Damped trend is extremely powerful for medium-horizon forecasts — it resists trend extrapolation overfit.',
      'Intermittent demand: Croston\'s method (not ETS) for series with many zeros. Separately exponentially smooths the demand size and inter-demand interval; combines to estimate the expected demand rate. ADIDA (Aggregate-Disaggregate Intermittent Demand Approach) and IMAPA are modern alternatives. SES and Holt-Winters are not appropriate for intermittent demand — they produce non-zero forecasts even when zeros dominate.',
    ],
    checkQuestions: [
      {
        q: 'You fit SES to a daily sales series and find the optimal α = 0.92 via MLE. What does this imply about the data and what model class does SES correspond to at this parameter value?',
        a: 'α = 0.92 means almost all weight is on the most recent observation — the model behaves close to a naïve (random walk) forecast. This implies: (1) The series has very little autocorrelation at lags > 1 — past values beyond yesterday have almost no predictive power. (2) The series is highly volatile or mean-reverting quickly. (3) SES with α close to 1 corresponds to ARIMA(0,1,1) with θ = 1−α ≈ 0.08 — an ARIMA where the MA term is nearly zero, i.e., a near-random-walk. Implication for forecasting: point forecasts collapse to the last observation; the forecast is essentially "tomorrow = today." Consider whether a more informative model exists or whether this is simply a high-noise series with no exploitable structure.'
      },
      {
        q: 'Your ETS model is ETS(M,A,M) — multiplicative error, additive trend, multiplicative seasonality. What forecast distribution does this imply and why are prediction intervals different from ETS(A,A,A)?',
        a: 'ETS(M,A,M) uses multiplicative errors: the noise term multiplies the signal. This means variance grows with the level — heteroskedasticity is built in. The h-step-ahead forecast distribution is not Gaussian; it is log-normally distributed (approximately). Prediction intervals must be computed via simulation (bootstrap innovation errors forward) rather than the Gaussian formula ŷ ± z·σ. ETS(A,A,A) has additive errors — constant variance regardless of level — and produces Gaussian prediction intervals via the analytical formula. In production: for series where the seasonal amplitude scales with the level (revenue, traffic), ETS(M,A,M) PI from simulation are wider and better calibrated than ETS(A,A,A) Gaussian PI. The Hyndman forecast package computes these via 5000 simulation paths automatically when using ETS multiplicative components.'
      },
      {
        q: 'The M4 forecasting competition showed that ETS and ARIMA individually perform worse than simple combination methods. How does this affect how you should use ETS in production?',
        a: 'The M4 result (Makridakis et al., 2020) showed that simple combination forecasts — e.g., average of ARIMA, ETS, and theta method — outperformed any individual model by 5–10% MASE. The implication is that model uncertainty is large enough that combining reduces variance without proportionally increasing bias. Production implications: (1) Do not commit to a single model class; ensemble ARIMA + ETS + Prophet for any important series. (2) Use the theta method as a baseline — it is simple, fast, and extremely competitive. (3) Optimal combination weights can be estimated on a validation window (penalised regression on validation errors) or used as equal weights. (4) For 50,000+ series, the overhead of fitting 3 models per series is dominated by the latency of data IO — the computation itself is cheap. The cost-accuracy tradeoff strongly favours ensembling.'
      },
    ],
  },

  {
    id: 'neural_forecasting',
    title: 'Neural Forecasting',
    subtitle: 'N-BEATS, N-HiTS, TFT, PatchTST, when transformers lose to MLPs, foundation models',
    difficulty: 'advanced',
    estimatedMin: 60,
    tags: ['N-BEATS', 'N-HiTS', 'TFT', 'PatchTST', 'transformer', 'neural forecasting', 'TimeGPT', 'MOIRAI'],
    summary: `The dominant finding in neural time series forecasting research since 2020 is counterintuitive: vanilla linear models and simple MLPs consistently outperform Transformer-based architectures on standard benchmarks (ETTh1, ETTm1, Weather, Exchange-Rate), as shown by Zeng et al. (2023). Transformers fail in time series for a structural reason — the attention mechanism treats the time dimension as a set (permutation-equivariant by design), discarding temporal order. N-BEATS and N-HiTS sidestep this by using residual stacks with doubly residual connections and basis decomposition. The Temporal Fusion Transformer (TFT) remains competitive because it is not a pure Transformer — its gating, variable selection networks, and LSTM encoder retain explicit temporal inductive biases.`,
    keyPoints: [
      'N-BEATS (Neural Basis Expansion Analysis for Time Series, Oreshkin et al. 2020): purely MLP-based, no recurrence, no convolutions. Architecture: backward/forward blocks with doubly residual connections. Each block predicts a "backcast" (reconstruction of input) and a "forecast." Residual connections subtract the backcast from the input — subsequent blocks model what the previous blocks could not explain. Two configurations: Generic (learned basis) and Interpretable (polynomial basis for trend + Fourier basis for seasonality — coefficients map directly to trend and seasonal components).',
      'N-HiTS (Neural Hierarchical Interpolation for Time Series, Challu et al. 2022): extends N-BEATS with hierarchical pooling. Different stacks operate at different time scales — coarse stack captures slow trends, fine stack captures high-frequency variation. Multi-rate sampling in each stack reduces computational cost by downsampling inputs before passing to blocks. Outperforms N-BEATS on long-horizon forecasting (horizon > 96 steps) by capturing multi-scale temporal patterns.',
      'Temporal Fusion Transformer (Lim et al. 2021): designed explicitly for multi-horizon forecasting with mixed inputs (static covariates, known future inputs, observed past inputs). Key components: (1) Variable Selection Network — gated residual network that learns which input variables are relevant at each timestep. (2) LSTM encoder/decoder — explicit sequential inductive bias that preserves temporal order. (3) Multi-head attention — applied on LSTM outputs to capture long-range dependencies. (4) Gating mechanisms — suppress irrelevant components at each layer. TFT handles different covariate types natively; most other models require manual feature engineering.',
      'Why attention fails in raw time series: self-attention is permutation-equivariant — attention scores are computed from pairwise content similarity, not position. Positional encodings are added but do not fundamentally change the permutation-equivariant nature. For time series where temporal order is the primary structure, this is a fundamental architectural mismatch. Shuffling timestamps in ETT benchmarks barely changes Transformer performance — confirming it is not using temporal order.',
      'PatchTST (Nie et al. 2023): addresses the permutation problem by treating time series as patches (non-overlapping subseries windows). Channel-independent: each variate is processed independently. Transformer attention operates on patches (not individual timesteps), effectively reducing the sequence length and making attention over semantically meaningful local windows rather than individual points. Outperforms vanilla Transformers significantly; competitive with DLinear (simple linear model).',
      'Informer (Zhou et al. 2021) and its limitations: proposed ProbSparse self-attention to reduce O(T²) attention complexity to O(T log T) for long sequences. Won AAAI Best Paper 2021. Subsequently shown by Are Transformers Effective for TS (Zeng 2023) that a single linear layer outperforms Informer, Autoformer, FEDformer on the same benchmarks. The benchmark results in the original Informer paper were not compared against strong baselines.',
      'Transfer learning for time series: unlike NLP/CV where pretraining on large corpora transfers broadly, time series pretraining is harder because different domains have incompatible temporal dynamics (stock prices vs electricity demand vs medical signals). TimeGPT (Garza 2023) is a Transformer pretrained on 100B time series points — shows zero-shot forecasting competitive with fitted statistical models on some benchmarks but sensitive to domain shift. MOIRAI (Salesforce) uses a universal patch-based architecture with multi-patch size training to handle variable frequencies. Both models are most useful for cold-start problems (insufficient in-domain data to fit a model) — for series with 2+ years of history, fine-tuned local models still dominate.',
      'Global vs local models: local models (ARIMA, ETS) fit independently per series. Global models (N-BEATS, TFT, TimeGPT) train on many series jointly, learning cross-series patterns. Global models win when: series are related (same domain, similar dynamics), cold-start data is scarce, or when the set of series is too large for per-series fitting. Local models win when: series are heterogeneous, the domain differs from the training distribution, or sufficient history exists for reliable local estimation.',
    ],
    checkQuestions: [
      {
        q: 'A colleague trains an Informer model on your ETT dataset and reports 15% lower MSE than ARIMA. You are sceptical. What do you do before trusting this result?',
        a: 'Compare against simple baselines first: (1) DLinear (a simple linear layer on the flattened lookback window) — Zeng et al. showed it outperforms Informer on ETT. If DLinear matches or beats Informer, the Informer gains are not from the Transformer architecture. (2) NLinear (normalised linear model) — even simpler, often competitive. (3) Check the lookback window and horizon match: Informer and ARIMA may use different lookback lengths — ensure fair comparison. (4) Check the evaluation split: if Informer was tuned on the test set (implicit or explicit), results are inflated. (5) Verify: is the ARIMA fitted per-step (rolling origin) or as a single fit? ARIMA with rolling origin is competitive; ARIMA fit once and extrapolated degrades. Conclusion: Informer outperforming ARIMA is plausible but the margin should be verified against at least DLinear and N-HiTS to confirm the gains are from temporal modelling, not from incidental lookback advantages.'
      },
      {
        q: 'You need to forecast hourly electricity demand for 1000 substations, 168 hours ahead (one week). You have 3 years of historical data per substation. TFT vs N-HiTS vs SARIMA — which do you use and why?',
        a: 'N-HiTS is the strongest starting point here. Reasoning: (1) 168-hour (long) horizon: N-HiTS hierarchical pooling handles long horizons better than N-BEATS and comparably to TFT but with lower compute. (2) 1000 series: a global N-HiTS trained across all substations is more efficient than fitting 1000 SARIMA models and likely better for substations with lower SNR. (3) SARIMA(p,d,q)(P,D,Q)[24](P,D,Q)[168]: two seasonal periods (daily + weekly) — SARIMA with two seasonalities is technically doable (BATS/TBATS) but complex; SARIMA does not natively handle double seasonality. (4) TFT is the best choice IF you have meaningful covariates — temperature, time-of-day, day-of-week, holidays, economic indicators — because TFT\'s variable selection and covariate handling outperform N-HiTS when regressors are informative. Without covariates, N-HiTS wins. (5) SARIMA baseline: always fit and evaluate; if N-HiTS does not beat SARIMA by >5% MASE, the simpler model is preferred. Practical pipeline: N-HiTS global model + SARIMA per-substation ensembled.'
      },
      {
        q: 'You train a TimeGPT zero-shot model on a new domain (pharmaceutical sales) without fine-tuning. It performs worse than Holt-Winters. When would you expect a foundation model to outperform statistical methods, and what limits them?',
        a: 'Foundation model advantages are concentrated in: (1) Cold start — fewer than 30 observations per series (new drug launch). Holt-Winters needs at least 2 seasonal cycles to fit; TimeGPT\'s pretrained seasonality representations can generalise. (2) Heterogeneous portfolio — when series have wildly different dynamics and per-series fitting is not feasible. (3) Zero-shot cross-domain when the target domain is well-represented in the pretraining corpus (retail, weather, finance). Limits: (1) Distribution shift — pharmaceutical sales patterns (approval spikes, generic entry cliffs, off-label use growth) differ from the pretraining corpus. The model has no mechanism to adapt to domain-specific patterns not in pretraining. (2) Foundation models are calibrated to their training distribution — PI widths and point forecasts may be systematically biased in new domains. (3) Interpretability — a brand manager cannot interrogate why TimeGPT predicts a spike in March. (4) Fine-tuning helps but requires infrastructure. Conclusion: with 3 years of data per series, you have enough history for reliable Holt-Winters fitting. Fine-tune TimeGPT on your historical data or use N-BEATS global model — both should outperform zero-shot here.'
      },
      {
        q: 'What is the doubly residual architecture in N-BEATS and why does it improve forecasting over a plain deep MLP?',
        a: 'N-BEATS doubly residual: each block (a stack of FC layers) produces two outputs: (1) backcast b̂_t — a reconstruction of the block\'s input x_t. (2) forecast f̂_{t+h} — a prediction for future values. The residual connection subtracts the backcast from the input before passing to the next block: x_{t+1} = x_t − b̂_t. The forecast from all blocks is summed: ŷ_{t+h} = Σ f̂^{(k)}_{t+h}. Why this works: (1) Each block focuses on what previous blocks could not explain — progressive residual fitting reduces the modelling complexity for each block. (2) The backcast acts as a self-supervised signal — the block must simultaneously reconstruct the past (easy) and forecast the future (hard), forcing it to learn meaningful representations rather than overfitting to noise. (3) Gradient flow: residual connections ensure gradients reach early blocks without vanishing. Compared to a plain deep MLP of equal depth, N-BEATS shows better generalisation because each block is constrained to a smaller hypothesis class (what remains after previous blocks\' explanation), reducing effective model complexity.'
      },
    ],
  },

  {
    id: 'forecast_evaluation',
    title: 'Forecast Evaluation',
    subtitle: 'MASE, pinball loss, Winkler score, Diebold-Mariano, rolling vs expanding window',
    difficulty: 'intermediate',
    estimatedMin: 45,
    tags: ['MASE', 'MAPE', 'pinball loss', 'Winkler score', 'Diebold-Mariano', 'backtesting', 'lookahead bias'],
    summary: `Choosing the wrong evaluation metric is one of the most common mistakes in production time series work, and it is consequential — models that rank first on MAPE may rank last on MASE, and neither may align with business cost. MAPE is scale-free and intuitive but numerically pathological when actuals are near zero; it also penalises positive errors more than negative errors of the same magnitude, systematically biasing model selection toward downward-biased forecasts. MASE (Mean Absolute Scaled Error) solves the scale problem by normalising against the in-sample naïve MAE — it is interpretable across series and is the M4 competition standard. The hardest evaluation problem is not metric choice but backtesting design: rolling-origin evaluation that correctly simulates production conditions (no lookahead in features, model refit cadence, covariate availability) is the only reliable proxy for live performance.`,
    keyPoints: [
      'MAE and RMSE: MAE = mean|e_t|, RMSE = sqrt(mean(e_t²)). RMSE penalises large errors more than MAE — useful when large errors are especially costly. Both are scale-dependent — incomparable across series with different scales. RMSE is sensitive to outliers; a single large error can dominate. Use MAE if you want robustness to occasional large errors; RMSE if large errors should be penalised more heavily.',
      'MAPE pitfalls: MAPE = mean(|e_t|/|Y_t|) × 100. Three failure modes: (1) Division by zero or near-zero when Y_t ≈ 0 — e.g., a new product with zero sales, or a daily series with zero counts on weekends. (2) Asymmetric penalisation: error of +10 on Y_t=100 → 10% error. Error of −10 on Y_t=100 → 10% error. But error of +10 on Y_t=10 → 100%. The asymmetry in scale means MAPE is minimised by forecasts that are systematically lower than the mean — MAPE-optimal point forecast is the median, not mean, in typical positively-skewed distributions. (3) Scale sensitivity: MAPE on a series with mean 100 vs mean 10,000 are not comparable.',
      'SMAPE (Symmetric MAPE): SMAPE = mean(|Y_t − Ŷ_t| / ((|Y_t| + |Ŷ_t|)/2)). Intended to fix MAPE asymmetry. Still undefined when both Y_t and Ŷ_t = 0. Still not truly symmetric — overforecasting and underforecasting by the same amount yield different SMAPE values due to the denominator. Used in M3/M4 competitions but not a principled fix.',
      'MASE (Mean Absolute Scaled Error, Hyndman & Koehler 2006): MASE = MAE / (MAE of in-sample naïve forecast). Naïve forecast: Ŷ_t = Y_{t-1} (or Y_{t-s} for seasonal). MASE < 1 means the model beats the naïve baseline. MASE is scale-free (can compare across series), symmetric, and defined when Y_t = 0. Recommended for M4 competition, forecast competitions generally, and production evaluation when comparing across heterogeneous series portfolios.',
      'Quantile / probabilistic forecasting evaluation: point forecasts discard uncertainty. Pinball loss (quantile loss): L_q(y, ŷ) = q(y − ŷ) if y ≥ ŷ, else (1−q)(ŷ − y). Averaging over all quantiles q ∈ (0,1) gives CRPS (Continuous Ranked Probability Score) — the gold standard for probabilistic forecasting. Winkler score for interval forecasts: penalises interval width plus a penalty when the actual falls outside the interval (proportional to how far outside). CRPS = MASE of distribution forecasts.',
      'Diebold-Mariano (DM) test: tests whether two forecasters have equal expected loss. H₀: E[L(e₁)] = E[L(e₂)]. Test statistic based on the series d_t = L(e₁_t) − L(e₂_t) — if d_t is autocorrelated (which it usually is), DM uses Newey-West standard errors. Modified Harvey-Leybourne-Newbold (HLN) correction for small samples. Practical gotcha: DM test assumes forecasts are computed on the same test set at the same horizon. Comparing two models with different backtesting windows is invalid.',
      'Rolling-window vs expanding-window backtesting: rolling window (fixed training size) simulates a production environment where only recent data is used — appropriate when the DGP is non-stationary. Expanding window (all available history) simulates a model that uses all past data — appropriate for stationary DGPs with long history. Rolling window gives more cutoffs for the same total dataset length. Expanding window produces better estimates in later periods (more training data). In practice: start with expanding window; switch to rolling if you detect distribution shift (model performance degrades over time).',
      'Lookahead bias in backtesting: (1) Using future values of covariates (weather, prices) as if they were known at forecast time. (2) Fitting preprocessing (normalisation, log transform, imputation) on the full dataset including the test period — mean and variance of the full series leak future information. (3) Feature lag calculation errors: a feature computed with df.shift(1) is safe; without shift, it uses the current observation. (4) Model selection on the test set — choosing the model with best test MAPE then reporting that MAPE as unbiased is double-dipping. Fix: strict temporal train/validation/test split; all preprocessing fitted only on training data; validation for model selection, test for final unbiased evaluation.',
    ],
    checkQuestions: [
      {
        q: 'You have two models. Model A has MAPE = 8%, Model B has MAPE = 12%. Model A looks better. But 15% of your series have true values below 5. Which model should you trust and how do you decide?',
        a: 'MAPE is unreliable when true values are near zero. For the 15% of series with Y_t < 5, a small absolute error produces enormous percentage error — those series dominate the MAPE average and likely distort the comparison. Model A may have "better" MAPE simply by making smaller absolute errors on near-zero series, not because it is better overall. Fix: (1) Compute MASE (or MAE) which is robust to near-zero series. (2) Separately evaluate the near-zero series subset — plot distribution of errors by series mean level. (3) Compute MAPE excluding near-zero series (Y_t < 5) and compare. (4) If the business cost of errors is proportional to the absolute error (not the percentage error), use MAE or MASE as the primary metric. Do not report a single MAPE number when 15% of the series invalidate its computation.'
      },
      {
        q: 'You are comparing two forecasting models across 1,000 series. How do you statistically test which model is better, controlling for the multiple-series problem?',
        a: 'The Diebold-Mariano test applies to a single series. For a portfolio: (1) Compute per-series MASE for each model: MASE₁_i and MASE₂_i for series i = 1,...,1000. (2) Compute d_i = MASE₁_i − MASE₂_i. Run a paired t-test (or Wilcoxon signed-rank if non-normal) on {d_i}. H₀: E[d] = 0. This is the M4 competition approach. (3) For robustness: bootstrap the series — resample with replacement from the 1000 series, compute the mean d on each bootstrap sample, derive a 95% CI. (4) Effect size matters more than p-value: a difference of 0.002 MASE is statistically significant with 1000 series but operationally irrelevant. Report median MASE and the fraction of series where each model wins.'
      },
      {
        q: 'You fit your preprocessing pipeline (including Z-score normalisation) on the full training+test set combined, evaluate on the held-out test, and get MASE = 0.83. What is wrong and how large could the bias be?',
        a: 'This is lookahead bias in preprocessing. Z-score normalisation uses the mean and standard deviation of the full dataset including the test period. The model sees future information through the normalisation constants: the mean includes test-period values, so the transformation "knows" the future level and scale. The bias direction is toward underestimating forecast error — the normalised test values are artificially closer to the model\'s training distribution. Bias magnitude depends on how much the test period differs from training in mean/variance: if the test period had a trend shift (higher mean), the full-dataset normalisation partially corrects for this, making the model appear better than it would be in production. Fix: fit the scaler only on training data, then transform validation and test using training-fitted parameters. Re-run evaluation. In practice, this often changes MASE by 5–15% for non-stationary series.'
      },
      {
        q: 'A PM asks why your 95% prediction interval contains the actual value only 81% of the time in production. What are the likely causes and how do you diagnose them?',
        a: 'A 95% PI with 81% empirical coverage means the intervals are too narrow (overconfident). Likely causes: (1) Model misspecification: the error distribution assumed by the model (e.g., Gaussian for ARIMA/ETS) underestimates tail mass. Real demand is often right-skewed or heavy-tailed. (2) Parameter uncertainty not propagated: MAP estimation (most forecasters) treats parameters as fixed — correct PI requires MCMC or bootstrap to propagate parameter uncertainty, which grows with forecast horizon. (3) Structural breaks: the model was fitted before a distribution shift; the PI is calibrated to the old regime. (4) Feature lookahead removed at production time: PI was computed with perfect covariates during backtesting, but production uses forecasted covariates with their own errors. Diagnosis: (1) Compute empirical coverage at each horizon h separately — does coverage degrade further out? Degrading coverage = parameter uncertainty or structural break. (2) Compare residual distribution to the assumed distribution — heavy tails → use Student-t likelihood. (3) Pit histogram (probability integral transform): residuals/σ should be uniform(0,1) for well-calibrated Gaussian PI — deviations show misspecification.'
      },
    ],
  },

  {
    id: 'ts_anomaly_detection',
    title: 'Time Series Anomaly Detection',
    subtitle: 'Point/contextual/collective anomalies, CUSUM, STL residuals, LSTM autoencoders, adaptive thresholds',
    difficulty: 'advanced',
    estimatedMin: 55,
    tags: ['anomaly detection', 'CUSUM', 'STL', 'LSTM autoencoder', 'isolation forest', 'contextual anomaly'],
    summary: `Time series anomaly detection is harder than tabular anomaly detection because what constitutes an anomaly is context-dependent in time — a value of 10,000 API errors per minute is normal during a traffic spike but anomalous at 3am on a Sunday. The fundamental difficulty is that "normal" is defined by a model of the expected behaviour, and the quality of anomaly detection is entirely bounded by the quality of that model. Most production systems fail not because of bad anomaly detectors but because of bad baseline models: seasonal patterns not removed, trend not accounted for, or variance not stabilised. The best anomaly detectors are therefore residual monitors — detect anomalies in the residuals of a well-specified forecasting model.`,
    keyPoints: [
      'Anomaly taxonomy: (1) Point anomalies — a single observation is far from the expected value (e.g., a spike in server latency). (2) Contextual anomalies — a value is anomalous in context but would be normal in another context (e.g., 500 sales on a Tuesday is anomalous, but 500 on Black Friday is expected). (3) Collective anomalies — a sequence of observations is anomalous together even if each individual point looks normal (e.g., a sustained 5% drop in conversion rate for 3 days).',
      'CUSUM (Cumulative Sum Control Chart): detects persistent shifts in the mean. Statistic: C_t = max(0, C_{t-1} + (Y_t − μ₀ − k)), where k is the allowance (half the detectable shift). Alert when C_t > h (control limit). Advantages: directional (detects upward/downward shifts separately), computationally trivial, interpretable. Limitation: requires specifying μ₀ (expected mean) and k — does not adapt to non-stationary baseline or seasonality without preprocessing.',
      'STL residual monitoring: decompose Y_t = T_t + S_t + R_t, monitor R_t. Point anomaly: |R_t| > k·σ_R where k = 3–4 and σ_R is the IQR-based robust standard deviation. Advantages: handles trend and seasonality; robust STL (parameter robust=True) prevents anomalies from corrupting the decomposition. Limitation: STL needs full window to decompose — for online/streaming data, maintain a rolling STL fit that updates periodically (e.g., every 24 hours for daily data).',
      'Isolation Forest for time series: isolates anomalies by randomly partitioning feature space — anomalies are isolated in fewer splits. For time series: compute features (lag values, rolling mean, rolling std, hour-of-day, day-of-week) and apply Isolation Forest on this feature matrix. Key: the feature engineering encodes temporal context — without lag features, Isolation Forest ignores temporal structure. Limitation: cannot detect collective (subsequence) anomalies without explicit sliding window features.',
      'LSTM Autoencoders: encode a time window into a fixed-length representation and decode back to the original window. Reconstruction error = anomaly score. High reconstruction error → the window is unusual relative to training patterns. Training: only on "normal" data (unsupervised, assuming anomalies are rare). Production gotcha: threshold calibration is the hard part — reconstruction error distribution is not stationary; it grows with the forecast horizon and changes with concept drift. Use a rolling calibration window to set the threshold at the 99th percentile of recent reconstruction errors.',
      'Adaptive thresholds: static 3-sigma thresholds fail when: (1) Variance is non-constant (heteroskedasticity), (2) The series has multiple operating modes, (3) Alert rates need to be controlled rather than false positive rates. Adaptive approaches: (a) Exponentially weighted moving std (EWMA control chart) — threshold adapts to recent variance. (b) Quantile regression: estimate the expected 1st and 99th percentile as functions of time features, alert when observation falls outside. (c) Conformal prediction: provides coverage-guaranteed anomaly detection without distribution assumptions — any model can generate conformal anomaly scores.',
      'Root cause vs symptom: a single root cause can produce correlated anomalies across dozens of downstream metrics simultaneously. Detecting each individually → alert flood. Better approach: (1) Multi-variate anomaly detection (Multivariate Gaussian, Sparse PCA of residuals, DeepMC) to detect joint anomalies. (2) Causal graph traversal — given a known dependency graph (service A calls B, B calls C), anomaly propagation analysis identifies whether the anomaly originated upstream or is a local issue. (3) Alert correlation: group alerts that fire within a time window — alert storms from a single event should be grouped into one incident.',
      'Evaluation of anomaly detectors: labelled anomalies are scarce. Precision@k (what fraction of the top-k flagged events are true anomalies) is practical when a human operator reviews the top alerts. ROC-AUC on imbalanced data is misleading — PR-AUC (precision-recall) is better. Time-series-aware F1: account for temporal proximity — a detection 2 hours late may still be useful. NAB (Numenta Anomaly Benchmark) score rewards early detection within a time window before the anomaly causes impact.',
    ],
    checkQuestions: [
      {
        q: 'Your 3-sigma threshold on raw API error rate generates 200 false-positive alerts per day because of daily and weekly seasonality. What is the right fix?',
        a: 'The problem is applying a static threshold to a non-stationary (seasonal) series. The fix is to threshold the residuals of a seasonal model, not the raw series: (1) Decompose the series with STL (or similar) — extract the seasonal and trend components. (2) Compute residuals R_t = Y_t − T_t − S_t. (3) Fit a 3-sigma threshold on the residuals (use IQR-based σ̂ = IQR/1.35 for robustness to outliers in the residual). (4) Alert when |R_t| > 3σ̂. This immediately removes seasonality-driven false positives. Further: (5) Use EWMA control chart on residuals to adapt σ̂ to changing baseline variance (e.g., weekday vs weekend variance differ). (6) Set alert rate target (e.g., 5 alerts/day) and use empirical 99th percentile of residuals over a rolling 14-day window as the threshold — directly controls alert rate.'
      },
      {
        q: 'You use an LSTM autoencoder for multivariate anomaly detection on 50 metrics. The reconstruction error correctly identifies an outage on day 15 of deployment, but by day 90 the false positive rate has tripled. What happened and how do you fix it?',
        a: 'Concept drift: the "normal" behaviour of the 50 metrics has shifted over 90 days (traffic patterns, user mix, new features deployed), but the LSTM autoencoder was trained on day 0–14 data. The model now considers the new normal as anomalous because it deviates from its stale training distribution. The reconstruction error for normal recent data is now systematically higher, causing false positives. Fixes: (1) Continual learning / periodic retraining: retrain the autoencoder on a rolling 30-60 day window of recent data, treating it as normal. (2) Adaptive threshold: instead of a fixed reconstruction-error threshold, calibrate the threshold daily as the 99th percentile of reconstruction errors from the past 7-14 days. This separates threshold adaptation (easy) from model adaptation (expensive). (3) Change detection on the anomaly score itself: if the mean reconstruction error drifts upward steadily, trigger a model retrain. In practice: adaptive threshold is the first line of defence, periodic retraining is the second.'
      },
      {
        q: 'You detect a latency spike anomaly in your API service. Your colleague says it is a "real anomaly." You say it is a downstream symptom. How do you distinguish, and what are the implications for incident response?',
        a: 'The distinction is whether the latency spike is the root cause or a consequence of an upstream anomaly propagating through a dependency graph. Detection approach: (1) Check the temporal order: did the upstream service (database, cache, external API) anomaly precede the latency spike? If the database latency spiked 30 seconds before API latency, the API spike is a symptom. (2) Causal graph analysis: trace the call graph. If API → DB → Storage, and DB shows an anomaly simultaneously, the anomaly likely originated in DB or Storage. (3) Correlation analysis across metrics: a true root cause anomaly propagates to downstream metrics with a delay proportional to propagation latency. Cross-correlate anomaly times across metrics to find the earliest-onset metric in the dependency chain. Incident response implication: treating a symptom as root cause leads to incorrect mitigation (restart the API server when the DB is the issue). Building a causal dependency graph of services and running automated root cause analysis (e.g., Facebook\'s FBDC, Prometheus correlation tools) is the scalable solution for distributed systems.'
      },
      {
        q: 'You are designing an anomaly detection system for 10,000 IoT sensors. Labelled anomalies exist for only 50 sensors. How do you evaluate model performance across all 10,000?',
        a: 'With 50 labelled sensors and 9,950 unlabelled: (1) Supervised evaluation on labelled sensors: train a model, compute PR-AUC / F1 / precision@k on labelled sensors. Use this as a proxy for overall performance — valid if the 50 labelled sensors are representative of the full distribution. Check representativeness: do the 50 sensors span the range of behaviour types (failure modes, sensor types, traffic levels)? (2) Surrogate evaluation on unlabelled sensors: inject synthetic anomalies (point spikes, level shifts, seasonal distortions) into normal sensor data — evaluate detection rate on injected anomalies. This tests the detector\'s ability to find known anomaly types. (3) Alert rate monitoring: if a calibrated system should fire at 1% of timesteps, monitor alert rate across all 10,000 sensors. Systematic deviation (e.g., 5% alert rate on certain sensor clusters) indicates miscalibration for those clusters. (4) Human review sample: randomly sample 100 triggered alerts across all sensors, have a domain expert label them (true/false positive). Estimate overall precision from this sample. (5) Active learning: use the anomaly score to prioritise which unlabelled sensors to send for labelling — label the highest-scoring ambiguous cases to grow the labelled set efficiently.'
      },
    ],
  },

  {
    id: 'causal_ts',
    title: 'Causal Inference in Time Series',
    subtitle: 'Granger causality, ITS, synthetic control, CausalImpact, DiD, temporal autocorrelation',
    difficulty: 'advanced',
    estimatedMin: 65,
    tags: ['Granger causality', 'CausalImpact', 'synthetic control', 'interrupted time series', 'DiD', 'BSTS'],
    summary: `Causal inference in time series is conceptually distinct from standard causal inference because temporal autocorrelation violates the independence assumption that underpins most causal methods, and because the treatment often cannot be randomised. Granger causality — the most widely misused concept in applied time series — answers the question "does X help predict Y beyond Y's own past?" which is a predictive, not causal, question. The gold standard for time series intervention evaluation is the counterfactual control: either a geographic or entity control (synthetic control) or a pre-period prediction (interrupted time series, CausalImpact) that serves as the what-would-have-happened-without-the-intervention baseline. Difference-in-differences extends naturally to panel time series but requires the parallel trends assumption, which temporal autocorrelation makes more fragile.`,
    keyPoints: [
      'Granger causality: X Granger-causes Y if including lagged X in a regression on Y significantly improves prediction over using lagged Y alone. Test: F-test on the added X lags in VAR. Common misinterpretation: Granger causality is not causality — it is predictive priority. A shared common cause (Z causes both X and Y with different lags) produces Granger causality between X and Y without any direct causal link. Always ask: "is there a common cause that could explain this?" before interpreting Granger causality.',
      'Interrupted Time Series (ITS): a quasi-experimental design where the intervention occurs at a known time point T*. OLS regression: Y_t = β₀ + β₁T + β₂D_t + β₃(T − T*)D_t + ε_t, where D_t = 1{t ≥ T*}. β₂ estimates the immediate level change; β₃ estimates the change in trend. Critical assumptions: (1) No other intervention at T*. (2) The counterfactual trend can be extrapolated from the pre-period. (3) Residuals are not strongly autocorrelated — if they are, SE is underestimated; use Prais-Winsten or Newey-West SE. ITS is single-series; adding a control group produces controlled ITS (CITS).',
      'Synthetic control method (Abadie et al.): constructs a weighted combination of control units that matches the treated unit\'s pre-treatment covariates and outcomes as closely as possible. Weights w_j solve: minimize ||X₁ − Xw||_V subject to Σw_j = 1, w_j ≥ 0. Post-treatment: the synthetic control predicts the counterfactual; treatment effect = treated − synthetic. Advantages: (1) No parametric assumption on the outcome model. (2) Pre-treatment fit quality is observable — poor fit reveals the counterfactual is unreliable. Limitation: requires many pre-treatment periods (typically 10+ years for annual data) and the treated unit must be within the convex hull of control units.',
      'CausalImpact (Brodersen et al., Google 2015): Bayesian Structural Time Series (BSTS) model. Pre-intervention: fit a state space model on the treated series with control series as regressors. Post-intervention: extrapolate the counterfactual using the pre-fitted model. Treatment effect = observed − counterfactual. BSTS components: local linear trend, seasonality (Fourier), regression on controls. Uncertainty: full posterior over counterfactual gives credible intervals for the causal effect. Key assumption: the relationship between treated and control series is stable across the intervention. CausalImpact is widely used at Google/FAANG for measuring the impact of launches, ranking changes, and policy interventions.',
      'Difference-in-Differences in panel time series: DiD = (Y_{treated,post} − Y_{treated,pre}) − (Y_{control,post} − Y_{control,pre}). With multiple time periods: TWFE (Two-Way Fixed Effects) regression Y_{it} = αᵢ + λ_t + δD_{it} + ε_{it}. δ is the DiD estimate. Parallel trends assumption: treated and control units would have had parallel trends absent treatment. Temporal autocorrelation inflates t-statistics — residuals ε_{it} are correlated within units over time; cluster standard errors at the unit level.',
      'Staggered DiD problem: when different units receive treatment at different times (common in A/B test rollouts), TWFE estimator δ is a weighted average of 2x2 DiD comparisons where the weights can be negative — "already treated" units serve as controls for "later treated" units, contaminating the estimate. Solution: Callaway-Sant\'Anna or Sun-Abraham estimators compute cohort-specific treatment effects and aggregate properly. In practice: always visualise event-study plots (coefficients on lags and leads of treatment) — pre-treatment coefficients near zero validate parallel trends; post-treatment coefficients show dynamic treatment effects.',
      'Why standard causal methods break with temporal autocorrelation: (1) IV exclusion restriction becomes implausible when the instrument and outcome share autocorrelated common trends. (2) Regression discontinuity in time (RD with running variable = time) is especially fragile — observations close to the cutoff date are highly correlated; the local regression conflates the treatment effect with the local autocorrelation structure. (3) Propensity score matching in longitudinal data fails because today\'s treatment assignment depends on yesterday\'s outcome (time-varying confounding). Fix: marginal structural models (MSM) with IPTW handle time-varying confounding.',
      'Event study analysis (dynamic DiD): instead of a single δ, estimate effects at each relative time period: Y_{it} = αᵢ + λ_t + Σ_{k≠-1} δ_k 1{t − G_i = k} + ε_{it}, where G_i is the treatment adoption date. δ_k for k < 0 test parallel pre-trends; δ_k for k ≥ 0 show the dynamic treatment effect trajectory. Pre-trend test: joint F-test on {δ_k, k < 0}. Visualised as an event study plot with confidence bands.',
    ],
    checkQuestions: [
      {
        q: 'You find that Google search volume for your brand Granger-causes weekly sales (F-test p < 0.001). A PM wants to invest in SEO to increase search volume and thereby increase sales. What is the problem with this reasoning?',
        a: 'Granger causality shows that lagged search volume predicts sales beyond sales\'s own history — it does not establish a causal mechanism where increasing search volume will increase sales. Two alternative explanations: (1) Common cause: TV advertising or PR events simultaneously increase search volume AND sales — neither causes the other. The advertising is the Granger cause of both, creating spurious Granger causality from search to sales. (2) Reverse causality with lag: sales growth leads to word-of-mouth, which increases search interest — the temporal lag creates the appearance of search leading sales. To distinguish: (a) Run an experiment — randomly vary search ads spend and see if sales respond. (b) Look for instrumental variable — a policy that affects search volume but not sales directly. (c) Control for advertising spend in the Granger VAR — if the Granger causality disappears when advertising is included, it was a common cause. Granger causality is a useful screening tool for hypotheses, not a justification for interventions.'
      },
      {
        q: 'You are measuring the impact of a new feature launched to users in Germany on January 15. You have daily active users (DAU) data for Germany (treated) and France (control) from January 2023 onwards. How do you use CausalImpact and what assumptions must hold?',
        a: 'Setup: pre-period = 2023-01-01 to 2025-01-14, post-period = 2025-01-15 onwards. Model: fit BSTS on Germany DAU using France DAU as regressor during pre-period. Post-period: extrapolate the counterfactual using the Germany-France relationship learned pre-launch; compare to observed Germany DAU. Assumptions: (1) Stable relationship: the Germany-France DAU relationship must be stable across the launch. If France had its own event (holiday, competitor launch) in the post-period, the counterfactual is polluted. (2) No spillover: France users are not affected by the Germany feature — violated if users can VPN or if the feature affects platform-wide dynamics. (3) Pre-period adequacy: enough pre-period data (at least 3x the post-period length) for the BSTS to learn a reliable relationship. (4) Control quality: France must behave similarly to Germany absent the treatment — check pre-period parallel trends. Output: posterior distribution of the causal effect with 95% credible intervals. Report cumulative effect and point estimate with uncertainty.'
      },
      {
        q: 'Your company rolls out a pricing change to different markets in different months over a 6-month window. You use TWFE DiD to estimate the effect. A colleague says your estimate is biased. Why and what do you do?',
        a: 'Staggered treatment timing invalidates TWFE in the presence of treatment effect heterogeneity across cohorts or time. TWFE uses "already treated" units as control units for "later treated" units. If the treatment effect is dynamic (e.g., the pricing change has larger effects 3 months after adoption than 1 month after), already-treated units contaminate the control group with their own treatment effects. The TWFE estimator δ is a weighted average of cohort-specific effects where the weights depend on group sizes and timing — they can be negative, meaning some comparisons subtract true treatment effects. Correct approach: (1) Use Callaway-Sant\'Anna (CS) estimator: compute 2x2 DiD for each (cohort, relative time) combination using "clean controls" (never-treated or not-yet-treated units only). Aggregate cohort-specific ATTs. (2) Alternatively, use Sun-Abraham: interaction-weighted estimator that is robust to treatment effect heterogeneity. (3) Plot event-study coefficients — the shape of dynamic effects tells you if heterogeneity is the issue (flat post-treatment effects → less bias from TWFE).'
      },
      {
        q: 'You want to estimate the causal effect of an algorithm change on user engagement, but the change was rolled out gradually to all users with no holdout group. CausalImpact, synthetic control, and ITS all require a control group or counterfactual. What do you do?',
        a: 'Without any control group, causal estimation is extremely difficult but not impossible. Options: (1) ITS with internal counterfactual: model the pre-change trend (including seasonal components) and extrapolate. The treatment effect = observed post-change − extrapolated counterfactual. Valid only if the pre-change model is well-specified and no other events coincide with the change. Use BSTS to incorporate prior knowledge about expected post-change dynamics. (2) Regression discontinuity in time: if the rollout happened sharply at a known date, compare users just before and just after the date. Works only if: (a) users are not anticipating the change, (b) the engagement metric is continuous at the boundary absent the treatment, (c) no concurrent events. Temporal autocorrelation near the boundary inflates t-statistics — use CCT bandwidth selection with robust SE. (3) Donor pool construction: find external series (competitor platforms, adjacent product lines) not affected by the change and use them as synthetic control donors. Requires strong parallel-trends assumption between internal and external series. (4) Honest answer: without a holdout group, causal identification is not possible without strong untestable assumptions. The right fix is prospective: design future launches with a holdout or phased rollout for causal evaluation.'
      },
    ],
  },
]

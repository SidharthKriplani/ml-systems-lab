export const SD_MSL_B = [
  {
    id: 'realtime-serving-feature-store',
    title: 'Real-Time Serving and Feature Platform',
    prompt: 'Design the real-time model serving and feature platform for online predictions.',
    context:
      'A payments company runs ~40 models (fraud, risk, personalization) across product teams, serving a combined ~80k predictions/second at peak with a strict 50ms P99 budget on the fraud path. Today each team hand-rolls its own feature computation and its own serving endpoint, and the same "user 30-day spend" feature is implemented three different ways. Leadership wants one platform so a model can be trained and served against a single feature definition without training-serving skew.',
    tags: ['feature store', 'serving', 'training-serving skew', 'model registry', 'latency'],
    stages: [
      {
        id: 'requirements',
        title: 'Clarify requirements & constraints',
        ask: 'Pin down the serving SLA, the freshness each feature actually needs, and what "one platform" has to guarantee across teams before you draw any boxes.',
        considerations: [
          'Latency SLA and QPS per path: the 50ms P99 fraud path is a different system from a 300ms personalization path, and one platform must serve both without forcing the slow budget on the fast one.',
          'Freshness tiers per feature: some features are fine at daily batch (lifetime totals), some need streaming updates within seconds (velocity/counts), some must be computed at request time from the live payload (amount, merchant).',
          'Training-serving parity is the core requirement: the same feature definition must produce the same value offline (training) and online (serving), or every model silently learns on data it will never see in production.',
          'Point-in-time correctness for training: a label at time t must only see feature values as of t, or aggregates leak future information and offline metrics become fiction.',
          'Read vs write scale asymmetry: 80k reads/second at serving vs. batch/stream writes; the online store is a low-latency read path, the offline store is a throughput-oriented training path.',
          'Consistency and availability tradeoff: is a slightly stale feature acceptable (yes, usually) versus a missing feature (needs a defined default), and what is the cost of each.',
          'Governance: who can register a feature, how are breaking changes to a definition versioned, and how is a feature deprecated when three teams depend on it.',
          'Cost envelope: an online store holding hot features for every entity at 80k QPS is a real infra bill; scope which entities and features actually need online materialization.',
        ],
        strong: [
          'Separates the fast fraud path (50ms) from slower paths explicitly and refuses to let one SLA dictate the whole platform, because the serving architecture is a function of the tightest budget.',
          'Names three concrete freshness tiers with examples: batch (daily lifetime aggregates), streaming (sub-second velocity counts via Flink/Kafka), and request-time (features derived from the incoming transaction payload).',
          'States training-serving skew as the primary failure the platform exists to kill, and ties it to a single feature definition consumed by both training and serving.',
          'Calls out point-in-time correctness as a training-side requirement, distinct from serving freshness, and explains that as-of joins prevent future-leakage aggregates.',
          'Quantifies the read path: 80k QPS at P99 50ms implies an in-memory online store (Redis/DynamoDB-class), not a warehouse query per request.',
          'Frames "one platform" as a coordination guarantee (one definition, one source of truth) rather than a modeling improvement.',
        ],
        traps: [
          'Treating all features as one freshness class and defaulting everything to real-time streaming, which is the most expensive tier and unnecessary for lifetime aggregates.',
          'Conflating serving freshness with point-in-time correctness; they are different problems (one is serving latency of fresh values, the other is training-time leakage).',
          'Assuming a single online store must serve every model, ignoring that a 50ms path and a 300ms path have different consistency and caching needs.',
          'Skipping the SLA/QPS elicitation and jumping straight to "we will use a feature store," which is the answer, not the reasoning.',
        ],
        probes: [
          'Which specific feature in a fraud model needs request-time computation rather than a precomputed value, and why can it not be materialized ahead of time?',
          'What breaks first if you serve every feature from a nightly batch: freshness or point-in-time correctness, and for which feature type?',
          'How do you decide whether a given feature earns a slot in the expensive online store at all?',
        ],
      },
      {
        id: 'architecture',
        title: 'High-level architecture',
        ask: 'Lay out the offline and online stores, the model server, and how a request flows from raw payload to a served prediction under budget.',
        considerations: [
          'Dual-store shape: an offline store (warehouse/lake, columnar, throughput-oriented) for training and backfills, and an online store (in-memory KV) for low-latency serving reads.',
          'A single feature-definition layer that compiles to both a batch job (writes offline + materializes online) and an online transform, so the same logic runs in both places.',
          'Streaming pipeline (Kafka -> Flink/Spark Streaming) that maintains real-time aggregates and upserts them into the online store continuously.',
          'Model server design: a dedicated inference service with dynamic request batching, model loaded in memory, GPU only where it pays off (deep models) versus CPU for GBMs.',
          'Caching layers: cache hot feature vectors and cache identical-input predictions where the input is stable within a short window.',
          'Async fan-out for feature fetch: issue all online-store reads in parallel and wait for the max, not the sum, each with a timeout to a default.',
          'A model registry sitting between training and serving that versions the model artifact together with the feature set and schema it was trained against.',
          'Request flow: payload -> request-time features + parallel online-store lookups -> assemble vector -> model server (batched) -> score -> log the exact served feature vector for later training.',
        ],
        strong: [
          'Draws the dual store explicitly: offline (columnar warehouse, point-in-time joins for training) and online (Redis/DynamoDB-class KV, single-digit-ms reads for serving), fed by one shared definition.',
          'Puts a request-batching inference server in the path and reasons about the batch-size vs latency tradeoff: larger batches raise throughput and P99 tail, so cap batch wait to a few ms under a 50ms budget.',
          'Uses async fan-out for the feature reads and states the numbers: four 5ms lookups in parallel cost 5ms, in serial 20ms, which alone can blow the budget before the model runs.',
          'Logs the exact feature vector that was served, so training data is reconstructed from what the model actually saw, closing the skew loop rather than re-deriving features later.',
          'Versions model and feature schema together in the registry so a rollback restores a consistent model-plus-features pair, not a model against a drifted feature definition.',
          'Distinguishes CPU serving for GBM fraud models from GPU serving for deep personalization, matching hardware to model class and budget.',
        ],
        traps: [
          'A single store for both training and serving, which forces either warehouse latency into the serving path or throws away point-in-time training correctness.',
          'Serial feature fetch, paying the sum of lookup latencies and blowing the budget before inference starts.',
          'Versioning the model artifact but not the feature definitions it was trained on, so a rollback pairs an old model with new features and reintroduces skew.',
          'No serving-time logging of the actual feature vector, forcing training to recompute features and inviting exactly the skew the platform was meant to remove.',
        ],
        probes: [
          'Where does dynamic batching help and where does it hurt on a 50ms P99 path, and how do you bound the batch wait?',
          'Why log the served feature vector at inference time instead of recomputing features for training from raw logs later?',
          'What has to be co-versioned with the model in the registry for a rollback to be safe?',
        ],
      },
      {
        id: 'deep-dive',
        title: 'Deep-dive: the hard part',
        ask: 'Go deep on training-serving parity and point-in-time correctness: how do you guarantee the offline and online values of a feature actually match, and how do you roll out a new model safely?',
        considerations: [
          'Skew sources: different code paths (Spark for batch vs. a Python transform online), different null/timezone handling, different aggregation windows, and staleness between the two stores.',
          'The single-definition remedy: define the feature once (e.g., a declarative transform) and generate both the batch materialization and the online computation from it, so divergence is structurally impossible.',
          'Point-in-time joins: for each training label at time t, join feature values as-of t using event timestamps, never the latest value, or a 7-day aggregate silently includes post-label days.',
          'Backfills: when a new feature is added, recompute its historical values over the training window with the same as-of logic so old training rows get correct history, not present-day values.',
          'Parity monitoring: continuously sample live serving values and recompute the offline value for the same entity/time and alert on divergence beyond a tolerance.',
          'Safe rollout ladder: shadow (new model scores live traffic, decisions ignored, outputs logged), canary (small live percentage), champion-challenger / A-B, then full ramp.',
          'Feature-plus-model versioning at rollout: a new model may depend on a new feature version; both must ship and roll back together.',
          'Freshness-vs-cost per feature: streaming upserts for volatile counts, periodic materialization for stable aggregates, and request-time compute where no precomputation is valid.',
        ],
        strong: [
          'Names the concrete skew sources (divergent code paths, null/timezone handling, window mismatches, store staleness) and argues the only durable fix is one definition compiled to both sides, not two hand-kept-in-sync implementations.',
          'Explains point-in-time joins with a leakage example: a "30-day spend as of label time" that accidentally uses the latest value inflates offline AUC and collapses in production.',
          'Treats backfill as a first-class step: adding a feature requires recomputing its history as-of past timestamps so training rows are not contaminated with present values.',
          'Describes a shadow-then-canary-then-champion-challenger rollout, with shadow scoring live traffic and logging so you compare distributions before any decision is affected.',
          'Adds an online-vs-offline parity monitor that samples served values and recomputes the offline equivalent, alerting on divergence, so skew is caught continuously not at the next incident.',
          'Ties freshness tiers back to cost: streaming only for volatile features, batch materialization for stable ones, request-time where precomputation is invalid.',
          'Rolls back model and feature version as a pair, refusing to leave a rolled-back model reading a newer feature definition.',
        ],
        traps: [
          'Maintaining two separate feature implementations (batch and online) and promising to keep them in sync by discipline, which is exactly how skew returns.',
          'Using the latest feature value in training joins instead of as-of values, leaking the future and inflating offline metrics.',
          'Rolling out a new model straight to full traffic with no shadow or canary, so a scoring or feature regression hits every user at once.',
          'Adding a feature without backfilling its history, so historical training rows carry present-day values and the model learns a distribution it will never serve.',
        ],
        probes: [
          'Walk me through a point-in-time join for a 30-day-spend feature on a label at time t: which rows are and are not allowed in the aggregate?',
          'How does shadow mode let you catch a skew or calibration problem before a single real decision changes?',
          'A new model needs a new version of an existing feature. What is the deploy and rollback unit?',
        ],
      },
      {
        id: 'evaluation',
        title: 'Evaluation & monitoring',
        ask: 'Define what you measure offline before shipping, what you watch online after, and the specific drift and latency signals that trigger action.',
        considerations: [
          'Offline: model quality on point-in-time-correct data (AUC/PR for fraud, calibration if scores feed thresholds), plus a replay that confirms offline and online feature values agree.',
          'Online: business metric (fraud caught vs. false-decline rate), served-prediction distribution, and per-stage latency (feature fetch, inference, total) at P50/P95/P99.',
          'Feature drift monitoring: track input distributions per feature (PSI/KL) so a shifted upstream source is caught before it degrades the model.',
          'Prediction drift: watch the served score distribution; a sudden shift often means an upstream feature broke, not that the world changed.',
          'Freshness/staleness monitoring: alert when the online store lags its source beyond the tier tolerance (e.g., streaming aggregate more than N seconds stale).',
          'Parity/skew alert: continuous online-vs-offline value comparison on a sample of entities.',
          'Latency budget monitoring end-to-end in production, not from component microbenchmarks, since feature fetch usually dominates.',
          'Calibration monitoring where the score feeds a threshold or an expected-value decision, since an uncalibrated drift silently moves the operating point.',
        ],
        strong: [
          'Separates offline gates (point-in-time AUC/PR, calibration, feature-parity replay) from online gates (fraud-caught vs. false-decline, latency P99, drift) and says which must pass before ramp.',
          'Instruments per-stage latency (feature fetch vs. inference vs. total) at P99 and monitors it in production, because the fraud SLA only exists when the stages are summed.',
          'Monitors input feature drift with a concrete statistic (PSI or KL per feature) and prediction drift on the served score, and distinguishes a real-world shift from a broken upstream feature.',
          'Keeps the parity monitor live as an evaluation signal, treating a growing online-vs-offline gap as an early warning of skew regression.',
          'Watches store freshness/staleness as its own signal, since a lagging streaming aggregate degrades fraud detection without any model change.',
          'Monitors calibration because the fraud score feeds a decision threshold, so drift in calibration moves the false-decline rate even when ranking looks fine.',
        ],
        traps: [
          'Trusting offline AUC computed on non-point-in-time data, which is inflated by leakage and will not hold online.',
          'Monitoring only aggregate latency, missing that feature fetch is the P99 driver while inference looks fine.',
          'Watching model accuracy but not feature drift or store staleness, so the first symptom is a fraud spike days after an upstream feed broke.',
          'Ignoring calibration drift on a thresholded score, letting the operating point move silently.',
        ],
        probes: [
          'A fraud model degrades but its architecture is unchanged. Which three monitors do you look at first and in what order?',
          'How do you tell a genuine distribution shift from a broken upstream feature using drift signals?',
          'Why is per-stage latency more actionable than a single end-to-end number on this path?',
        ],
      },
      {
        id: 'tradeoffs',
        title: 'Tradeoffs, failure modes & scaling',
        ask: 'Name the failure modes of the serving and feature path, the fallbacks, and how cost and freshness trade off as you scale to more models and higher QPS.',
        considerations: [
          'Online-store outage or slow read: define a fallback (last cached feature, a default value, or a rule-based score) behind a circuit breaker that trips on error-rate threshold.',
          'Model endpoint down or slow: fall back to a cheaper model or a rule-based decision rather than failing the transaction, with the fallback defined before the incident.',
          'Freshness-vs-cost: streaming every feature is the most accurate and the most expensive; scope streaming to volatile features and let stable ones ride batch materialization.',
          'Online-store cost at 80k QPS: hot storage for every entity/feature is a real bill; use TTLs, materialize only served features, and cache hot keys.',
          'Batching-vs-latency at scale: larger inference batches raise throughput and cut cost per prediction but lengthen the P99 tail; cap batch wait under budget.',
          'Skew regression as the silent scaling failure: more teams and features means more chances for a divergent definition, which the parity monitor and single-definition layer must contain.',
          'Backfill cost when adding features across a large history, and the compute it consumes on the offline store.',
          'Multi-tenancy: isolating one team\'s heavy feature or model so it cannot starve the shared online store or inference fleet.',
        ],
        strong: [
          'Defines concrete degraded responses per failure: online-store miss -> last cached value or default; model endpoint slow -> cheaper model or rule-based score; both behind a circuit breaker that trips automatically on error rate.',
          'Argues the freshness-vs-cost tradeoff quantitatively: reserve streaming for volatile counts, put lifetime aggregates on batch, and note streaming everything multiplies infra cost for little gain on stable features.',
          'Controls online-store cost with TTLs, materializing only served features, and caching hot keys, rather than storing every feature for every entity forever.',
          'Reasons the batching tail explicitly: bigger batches cut cost per prediction but push P99, so the batch wait is capped to protect the 50ms budget.',
          'Identifies skew regression as the defining scaling risk (more teams, more definitions) and leans on the single-definition layer plus parity monitor to contain it.',
          'Adds multi-tenant isolation so one team\'s expensive feature or model cannot degrade the shared serving fleet for the fraud path.',
        ],
        traps: [
          'No defined fallback, so an online-store or model outage fails live transactions and engineers improvise mid-incident.',
          'Streaming every feature for freshness and discovering the infra bill only after it ships.',
          'Unbounded online store (no TTL, every feature for every entity) that scales cost linearly with the catalog.',
          'Letting inference batch size grow for throughput without capping the wait, quietly breaking the P99 SLA.',
        ],
        probes: [
          'The online store starts timing out at peak. What does the fraud path return in the next 30 seconds and who decided that beforehand?',
          'Where would you spend to gain freshness and where would you refuse to, given the cost of streaming?',
          'As you add 20 more models, what is the single most likely way training-serving skew creeps back, and what contains it?',
        ],
      },
    ],
    rubric: [
      {
        dim: 'Requirements & scoping',
        strong:
          'Elicits per-path SLA and QPS, separates the 50ms fraud path from slower paths, defines freshness tiers per feature, and names both training-serving parity and point-in-time correctness as distinct requirements.',
        weak: 'Jumps to "use a feature store" without SLAs, treats all features as one freshness class, and conflates serving freshness with point-in-time correctness.',
      },
      {
        dim: 'Architecture & data flow',
        strong:
          'Draws a dual offline/online store fed by one feature definition, a request-batching inference server, async fan-out for feature reads, served-vector logging, and a registry that co-versions model and feature schema.',
        weak: 'Uses a single store for training and serving, fetches features serially, and versions the model without its feature definitions.',
      },
      {
        dim: 'Core ML depth (modeling & features)',
        strong:
          'Explains point-in-time joins with a leakage example, insists on one compiled definition to kill skew, treats backfills as first-class, and matches model class to hardware (GBM on CPU, deep on GPU).',
        weak: 'Keeps two hand-synced feature implementations, uses latest values in training joins, and ignores backfill of new features.',
      },
      {
        dim: 'Evaluation (offline & online)',
        strong:
          'Gates on point-in-time offline metrics plus a parity replay, then online business metric, drift (PSI/KL), calibration, and per-stage latency, with a shadow/canary ladder before ramp.',
        weak: 'Trusts non-point-in-time AUC, ships straight to full traffic, and monitors only aggregate accuracy and latency.',
      },
      {
        dim: 'Reliability, monitoring & failure modes',
        strong:
          'Defines fallbacks for online-store and model-endpoint failure behind a circuit breaker, keeps a live online-vs-offline parity monitor, and watches store staleness and prediction drift as early warnings.',
        weak: 'Has no predefined fallback, no parity monitor, and discovers upstream breakage only from a downstream fraud spike.',
      },
      {
        dim: 'Scaling, latency & cost',
        strong:
          'Reserves streaming for volatile features, bounds the online store with TTLs and served-only materialization, caps inference batch wait to protect P99, and isolates tenants on the shared fleet.',
        weak: 'Streams every feature, runs an unbounded online store, and lets batch size grow for throughput until the P99 SLA breaks.',
      },
      {
        dim: 'Communication & structure',
        strong:
          'Moves requirements -> architecture -> parity deep-dive -> evaluation -> tradeoffs, states numbers (QPS, ms budgets, store sizes), and names the skew failure the platform exists to prevent.',
        weak: 'Lists technologies without ordering, gives no numbers, and never articulates why the platform exists beyond "best practice."',
      },
    ],
  },
  {
    id: 'search-ranking-ltr',
    title: 'Search Ranking (Learning-to-Rank)',
    prompt: 'Design a search ranking system using learning-to-rank.',
    context:
      'An e-commerce marketplace runs product search over a ~50M-item catalog, ~5k queries/second at peak, with a 200ms end-to-end latency budget from query to rendered results. The current system is pure BM25 and loses on synonyms and intent ("running shoes" misses "trainers"), and business wants to rank on purchase likelihood, not just text match. Labels are implicit: clicks and purchases, which are heavily biased by the position and presentation the current ranker chose.',
    tags: ['learning-to-rank', 'retrieval', 'position bias', 'NDCG', 'two-stage'],
    stages: [
      {
        id: 'requirements',
        title: 'Clarify requirements & constraints',
        ask: 'Define the success metric, the latency budget across stages, the label source and its biases, and what "good ranking" means for this marketplace before choosing an LTR flavor.',
        considerations: [
          'Success definition: is the objective click-through, add-to-cart, purchase, or revenue, and how is it traded against relevance and seller diversity.',
          'Latency budget allocation across retrieval, reranking, and rendering within 200ms; the reranker only gets what the retrieval stage leaves it.',
          'Label source: clicks and purchases are implicit and abundant but biased; explicit human relevance judgments are unbiased but sparse and expensive.',
          'Position and presentation bias: the top result gets clicks regardless of relevance, and richer presentation (image, badge) skews clicks independent of true relevance.',
          'Query mix: head queries (frequent, well-covered) vs. tail queries (rare, sparse labels), which behave very differently under both retrieval and LTR.',
          'Index freshness: new items, price changes, and out-of-stock states must reach the index fast or the ranker surfaces wrong or unavailable products.',
          'Scale: 50M items cannot be scored by an expensive model per query, forcing a retrieve-then-rank split.',
          'Guardrails: out-of-stock suppression, adult/policy filtering, and query-intent constraints that must hold regardless of the learned score.',
        ],
        strong: [
          'Pins the primary metric to a business outcome (e.g., purchase likelihood or revenue-per-search) and names the guardrails (diversity, out-of-stock, policy) it must not sacrifice.',
          'Allocates the 200ms budget across stages up front and notes the reranker\'s budget is whatever retrieval and feature fetch leave, usually tens of ms.',
          'States plainly that implicit labels (clicks/purchases) are biased by position and presentation and that this is a correctness problem, not just noise.',
          'Distinguishes head from tail queries and flags that tail queries have sparse labels and weaker retrieval, so metrics must be reported per stratum.',
          'Recognizes 50M items forces a two-stage retrieve-then-rank design because no precise model can score the full catalog in budget.',
          'Separates the offline proxy metric (NDCG/MRR on graded relevance) from the online target (engagement/conversion), and says the online metric is the truth.',
        ],
        traps: [
          'Taking raw clicks as ground-truth relevance without acknowledging position and presentation bias, which bakes the current ranker\'s choices into the new one.',
          'Optimizing a single objective (CTR) and ignoring that clickbait-style high-CTR items can lower conversion and diversity.',
          'Ignoring the latency budget and assuming the expensive reranker can score thousands of items.',
          'Treating head and tail queries identically, so tail performance is invisible in a click-weighted average dominated by head traffic.',
        ],
        probes: [
          'Why is a top-ranked item\'s high click rate not evidence that your ranker is good?',
          'How would you split the 200ms budget across retrieval, feature fetch, reranking, and rendering?',
          'Which is the metric you ship on: offline NDCG or an online conversion A/B, and why can they disagree?',
        ],
      },
      {
        id: 'architecture',
        title: 'High-level architecture',
        ask: 'Lay out the two-stage design: candidate retrieval (lexical plus semantic) into an LTR reranker, and the data/feature flow that feeds it.',
        considerations: [
          'Stage 1 retrieval as a union of lexical (BM25 over an inverted index) and semantic (bi-encoder embeddings in an ANN index) to cover both exact-term and intent matches.',
          'Retrieval optimizes recall: it must not drop a relevant item, because the reranker can only reorder what it receives.',
          'Stage 2 reranker: an LTR model (LambdaMART-class GBM or a neural ranker) scoring a few hundred candidates on rich features.',
          'Feature families: query features (length, intent class, embedding), document features (price, rating, popularity, freshness), and query-document interaction features (BM25 score, embedding similarity, historical CTR for this query-item pair).',
          'Feature fetch from a feature store within the reranking budget, using async fan-out.',
          'Index pipeline: catalog updates (new items, price, stock) flowing to both the lexical index and the ANN index, with a freshness path for fast-changing fields.',
          'Logging: log the served slate, positions, and presentation so labels can be de-biased later.',
          'A re-ranking/business-rules layer on top for diversity, seller fairness, and out-of-stock suppression.',
        ],
        strong: [
          'Specifies a hybrid retrieval union: BM25 for exact/rare-term recall plus a bi-encoder ANN for semantic/intent recall, then explains fusion (score combination or reciprocal-rank fusion) before the reranker.',
          'States clearly that retrieval\'s job is recall and the reranker\'s job is precision, mirroring the recall-then-precision split, and that a missed item at retrieval is unrecoverable.',
          'Chooses an LTR reranker deliberately (LambdaMART for tabular features, or a neural ranker if embeddings/text dominate) and scores only the few hundred retrieved candidates.',
          'Enumerates the three feature families with concrete examples, especially the query-document interaction features (BM25 score, embedding cosine, historical query-item CTR) that carry most of the ranking signal.',
          'Fetches features via async fan-out within the reranking budget and logs the served slate/positions for later bias correction.',
          'Puts diversity and business rules in a distinct re-ranking layer after the learned score, so guardrails do not corrupt the learned relevance signal.',
        ],
        traps: [
          'A single-stage design that tries to score all 50M items with the LTR model, which cannot meet the latency budget.',
          'Semantic-only retrieval that loses exact-term and rare-token matches (SKUs, brand names) that BM25 handles well.',
          'Forgetting query-document interaction features, leaving the reranker with only independent query and doc signals and weak ranking power.',
          'Baking diversity and business rules into the learned objective, entangling guardrails with the relevance signal so neither can be tuned cleanly.',
        ],
        probes: [
          'Why keep BM25 at all once you have a strong semantic retriever?',
          'Which feature family usually carries the most ranking signal, and why?',
          'Why put diversity in a separate re-ranking layer rather than in the LTR loss?',
        ],
      },
      {
        id: 'deep-dive',
        title: 'Deep-dive: the hard part',
        ask: 'Go deep on labels and the LTR objective: how do you turn biased clicks into trustworthy training data, and how do you choose pointwise vs. pairwise vs. listwise?',
        considerations: [
          'Position bias: the top position collects clicks regardless of relevance, so training on raw clicks teaches the model to reproduce the previous ranker\'s order (a self-reinforcing loop).',
          'Inverse-propensity weighting (IPW): weight each click by 1/P(click | position) so top-position clicks count less and lower-position clicks count more, recovering an unbiased relevance estimate.',
          'Estimating the propensities: a position-bias model from result randomization (RandPair / intervention swaps) or from an eval model, since the propensities are not known a priori.',
          'Presentation bias beyond position: image quality, badges, and price can drive clicks independent of relevance and must be accounted for or held out.',
          'Pointwise vs. pairwise vs. listwise: pointwise regresses a score per item and misses that ranking is relative; pairwise learns "A above B"; listwise optimizes the whole list.',
          'LambdaMART as the practical choice: pairwise gradients weighted by the change in NDCG so a swap near the top gets a far larger push than one near the bottom, injecting rank-position awareness without a differentiable NDCG.',
          'Building graded labels from behavior: purchase > add-to-cart > click > impression, forming a relevance grade rather than a binary click.',
          'Occasional exploration/randomization in the served slate to gather unbiased data and to estimate propensities.',
        ],
        strong: [
          'Diagnoses the feedback loop precisely: raw-click training resurfaces whatever the previous ranker showed at the top, because position 1 gets clicks independent of relevance.',
          'Applies inverse-propensity weighting (weight each example by 1/P(click|position)) and, crucially, explains how the propensities are estimated via result randomization rather than assumed.',
          'Builds graded relevance from behavior (purchase > cart > click > skip) rather than a binary click, so the model learns the value ladder the business cares about.',
          'Explains why ranking is relative and rejects pointwise as the primary objective, choosing LambdaMART: pairwise gradients scaled by the NDCG change so top-of-list swaps dominate the gradient.',
          'Accounts for presentation bias (image/badge/price) as a second bias source beyond position, not just position alone.',
          'Adds a small randomization/exploration budget in the served slate both to de-bias future labels and to keep estimating propensities.',
        ],
        traps: [
          'Training on raw clicks and treating position-1 clicks as strong relevance, cementing the incumbent ranker\'s biases.',
          'Assuming fixed known propensities instead of estimating the position-bias curve from randomization.',
          'Choosing pointwise regression and sorting by it, ignoring that ranking is a relative problem where the top swaps matter most.',
          'Directly "optimizing NDCG" as if it were differentiable, instead of weighting pairwise gradients by the NDCG change.',
        ],
        probes: [
          'Where do the propensities in your IPW come from, and what happens if they are wrong?',
          'Why does LambdaMART weight gradients by the NDCG delta instead of optimizing NDCG directly?',
          'A classifier gets every absolute relevance score right but ranks worse than a pairwise model. How is that possible?',
        ],
      },
      {
        id: 'evaluation',
        title: 'Evaluation & monitoring',
        ask: 'Define offline ranking metrics, the online experiment, and the monitoring that keeps retrieval recall and ranking quality honest in production.',
        considerations: [
          'Offline: NDCG@k and MRR on graded, de-biased labels, reported per query stratum (head vs. tail) not just in aggregate.',
          'Retrieval recall audit: recall@k of the retrieval stage, since it caps final quality and a "great reranker, mediocre results" symptom usually traces here.',
          'Counterfactual/off-policy estimation from logs (IPW-based) to estimate a new ranker\'s online engagement before serving it, pruning bad candidates cheaply.',
          'Online: A/B test on the business metric (conversion, revenue-per-search, add-to-cart) with guardrails on diversity, latency, and zero-result rate.',
          'Offline-online gap: NDCG can rise while conversion falls, so the online metric is the decision metric and offline is a filter.',
          'Latency monitoring per stage (retrieval, feature fetch, rerank) at P99 within the 200ms budget.',
          'Index freshness monitoring: staleness of new items, price, and stock in both indexes.',
          'Drift: query-mix shift, feature drift, and score-distribution drift, plus null-result and out-of-stock-in-results rates as quality signals.',
        ],
        strong: [
          'Reports NDCG@k and MRR on de-biased graded labels and insists on per-stratum reporting so tail-query regressions are not hidden by head traffic.',
          'Audits retrieval recall@k explicitly as the ceiling on final quality and checks it first when the reranker looks good but results feel wrong.',
          'Uses counterfactual (IPW off-policy) evaluation on logs to estimate online lift before an A/B, and knows its risk is high variance when the new policy diverges from the logged one.',
          'Runs an online A/B on conversion/revenue-per-search with diversity, latency, and zero-result guardrails, and treats the online metric as the ship decision.',
          'Names the offline-online gap explicitly: NDCG up while conversion down is possible, so offline is a filter and online is the truth.',
          'Monitors per-stage P99 latency and index freshness, since a stale index or a slow stage degrades results without any model change.',
        ],
        traps: [
          'Shipping on offline NDCG alone and assuming it predicts online conversion.',
          'Reporting only aggregate NDCG, letting tail-query regressions hide inside head-heavy traffic.',
          'Never auditing retrieval recall, so a low-recall retriever caps quality no matter how good the reranker is.',
          'Ignoring zero-result and out-of-stock-in-results rates, which are direct quality failures users feel immediately.',
        ],
        probes: [
          'Your reranker\'s NDCG improved but conversion did not move. Where do you look first?',
          'How do you estimate a candidate ranker\'s online engagement from logs before an A/B, and what is the main risk?',
          'Why report NDCG per query stratum instead of one aggregate number?',
        ],
      },
      {
        id: 'tradeoffs',
        title: 'Tradeoffs, failure modes & scaling',
        ask: 'Name the failure modes across the two stages, the freshness and cost tradeoffs, and how the design scales with catalog size and QPS.',
        considerations: [
          'Retrieval recall as the ceiling: cutting retrieval candidates to save latency permanently caps final quality, an unrecoverable loss downstream.',
          'ANN index staleness on a fast-changing catalog (price, stock): stale embeddings retrieve wrong or unavailable items; needs delta re-embedding of changed items, not just nightly rebuilds.',
          'Feedback loop drift: the ranker keeps training on data it shaped, so without ongoing IPW and randomization it re-entrenches its own biases over time.',
          'Latency-vs-quality: a bigger reranker or more candidates raises quality and latency; the 200ms budget forces an operating point, possibly a pre-ranking stage.',
          'Cost: embedding 50M items, maintaining an ANN index in RAM, and running a reranker at 5k QPS is a real bill; quantization (IVF-PQ) trades a little recall for large RAM savings at retrieval.',
          'Head-vs-tail: head queries are cache-friendly and label-rich; tail queries need semantic retrieval and are where relevance quality is won or lost.',
          'Fallbacks: if the reranker or feature store is slow/down, fall back to BM25 ordering behind a circuit breaker rather than failing the search.',
          'Guardrails at scale: diversity and seller fairness must hold as catalog and traffic grow, without letting a few sellers dominate every slate.',
        ],
        strong: [
          'Treats retrieval recall as a hard ceiling and refuses to shrink the candidate set for latency without a pre-ranking stage to protect recall.',
          'Handles ANN staleness on a volatile catalog with delta re-embedding of changed items and continuous upserts, reserving full rebuilds for structural changes.',
          'Names the feedback loop as the durable failure: without ongoing IPW plus randomization, the ranker re-entrenches its own biases, so de-biasing is continuous, not one-time.',
          'Reasons cost concretely: quantize the retrieval index (IVF-PQ) to cut RAM with a small recall loss the reranker re-scores away, while keeping the reranker precise on hundreds of candidates.',
          'Defines a fallback to BM25 ordering behind a circuit breaker when the reranker or feature store degrades, so search never returns an error.',
          'Protects diversity and seller-fairness guardrails as traffic scales, in the post-ranking layer, so no cluster of sellers monopolizes slates.',
        ],
        traps: [
          'Cutting retrieval candidates to hit latency and silently capping final quality with no pre-ranking stage.',
          'Rebuilding the ANN index only nightly on a catalog whose prices and stock change every few minutes.',
          'One-time de-biasing that ignores the ongoing feedback loop, letting biases re-accumulate.',
          'No fallback, so a slow reranker or feature-store outage returns errors instead of degraded BM25 results.',
        ],
        probes: [
          'You need to shave 30ms off the reranking path. What do you cut, and how do you protect retrieval recall while doing it?',
          'Prices change every few minutes but your ANN index rebuilds nightly. What breaks and how do you fix it?',
          'Why is de-biasing a continuous obligation rather than a one-time training-data fix?',
        ],
      },
    ],
    rubric: [
      {
        dim: 'Requirements & scoping',
        strong:
          'Pins the metric to a business outcome, allocates the 200ms budget across stages, names position/presentation bias in the labels, and separates head from tail queries.',
        weak: 'Treats raw clicks as relevance, optimizes CTR alone, ignores the latency budget, and lumps head and tail queries together.',
      },
      {
        dim: 'Architecture & data flow',
        strong:
          'Designs hybrid BM25+semantic retrieval feeding an LTR reranker, states recall-then-precision, enumerates query/doc/interaction features, and separates diversity into a post-ranking layer.',
        weak: 'Tries to score 50M items in one stage or uses semantic-only retrieval, forgets interaction features, and bakes business rules into the loss.',
      },
      {
        dim: 'Core ML depth (modeling & features)',
        strong:
          'Applies IPW with propensities estimated from randomization, builds graded labels, and chooses LambdaMART with NDCG-weighted pairwise gradients, explaining why ranking is relative.',
        weak: 'Trains on raw clicks, assumes known propensities, and sorts a pointwise classifier while calling it learning-to-rank.',
      },
      {
        dim: 'Evaluation (offline & online)',
        strong:
          'Reports NDCG/MRR per stratum on de-biased labels, audits retrieval recall, uses off-policy IPW estimation before A/B, and ships on an online conversion metric with guardrails.',
        weak: 'Ships on aggregate offline NDCG, never audits retrieval recall, and assumes offline predicts online conversion.',
      },
      {
        dim: 'Reliability, monitoring & failure modes',
        strong:
          'Falls back to BM25 behind a circuit breaker, monitors index freshness and per-stage P99, and treats the feedback loop as a continuous de-biasing obligation.',
        weak: 'Has no fallback, rebuilds indexes too slowly for a volatile catalog, and de-biases once and forgets the feedback loop.',
      },
      {
        dim: 'Scaling, latency & cost',
        strong:
          'Protects retrieval recall as a ceiling, adds a pre-ranking stage when squeezed, quantizes the retrieval index (IVF-PQ) for RAM, and enforces diversity as traffic grows.',
        weak: 'Cuts candidates for latency with no pre-ranking, ignores index RAM cost, and lets a few sellers dominate slates at scale.',
      },
      {
        dim: 'Communication & structure',
        strong:
          'Moves requirements -> two-stage architecture -> labels/LTR deep-dive -> evaluation -> tradeoffs, quantifies budgets and scale, and names bias and recall as the load-bearing problems.',
        weak: 'Lists ranking techniques with no structure or numbers and never explains why bias correction or retrieval recall matter.',
      },
    ],
  },
  {
    id: 'demand-forecasting',
    title: 'Demand / Time-Series Forecasting System',
    prompt: 'Design a demand and time-series forecasting system, for example inventory or capacity planning.',
    context:
      'A retailer needs daily demand forecasts for ~200k SKU-store series across 2k stores, refreshed nightly, to drive replenishment and warehouse capacity. Forecasts feed automated purchase orders, so systematic bias is expensive in both directions: over-forecast wastes cash and shelf space, under-forecast causes stockouts and lost sales. Demand is spiky (promotions, holidays, weather), many series are short or intermittent, and new SKUs launch weekly with no history.',
    tags: ['forecasting', 'time series', 'hierarchical', 'backtesting', 'cold start'],
    stages: [
      {
        id: 'requirements',
        title: 'Clarify requirements & constraints',
        ask: 'Establish the forecast horizon, granularity, what the forecast drives, the accuracy metric with its bias asymmetry, and the operational constraints before choosing a model class.',
        considerations: [
          'Horizon and granularity: daily forecasts per SKU-store over a lead-time horizon (e.g., 1-4 weeks), which determines model and feature design.',
          'What it drives: replenishment and capacity, so the output must be actionable at the reorder decision, not just a point number.',
          'Metric and asymmetry: over- vs. under-forecast have different costs (holding vs. stockout), so the loss should reflect that (e.g., pinball loss at a service-level quantile), not symmetric RMSE alone.',
          'Prediction intervals, not just point forecasts: replenishment needs a quantile (safety stock at a target service level), so the system must output a distribution.',
          'Series heterogeneity: fast movers with long history vs. intermittent/slow movers vs. brand-new SKUs with no history.',
          'Scale: 200k series refreshed nightly within a batch window, which constrains per-series model complexity and training time.',
          'Data realities: promotions, price changes, holidays, weather, and stockouts (censored demand: zero sales may mean out-of-stock, not zero demand).',
          'Retraining cadence and the batch SLA: the nightly window bounds how much compute the training and scoring can consume.',
        ],
        strong: [
          'Defines horizon and granularity from the decision it drives (lead-time replenishment per SKU-store) rather than picking a horizon arbitrarily.',
          'Names the cost asymmetry and picks an asymmetric objective, e.g., pinball/quantile loss at the target service level, over symmetric RMSE, because over- and under-forecast cost differently.',
          'Requires prediction intervals/quantiles, not point forecasts, since safety stock is a quantile decision, and states which service-level quantile (e.g., P90) matters.',
          'Segments series into long-history, intermittent, and cold-start classes up front, because each needs a different modeling path.',
          'Flags censored demand: a stockout produces zero sales that is not zero demand, and using raw sales as the label biases forecasts downward.',
          'Bounds model complexity by the nightly batch window and the 200k-series scale, making per-series bespoke models a non-starter without a global approach.',
        ],
        traps: [
          'Optimizing symmetric RMSE and ignoring that stockouts and overstock have very different business costs.',
          'Producing point forecasts only, leaving replenishment with no basis for safety stock.',
          'Treating zero sales as zero demand and ignoring stockout censoring, which biases the model to under-forecast exactly the popular items.',
          'Assuming a bespoke ARIMA per series is feasible at 200k series in a nightly window.',
        ],
        probes: [
          'A stockout shows up as zero sales. Why is training on raw sales a problem, and for which SKUs is it worst?',
          'Why is a point forecast insufficient for a replenishment decision?',
          'How does the cost asymmetry between overstock and stockout change your loss function?',
        ],
      },
      {
        id: 'architecture',
        title: 'High-level architecture',
        ask: 'Lay out the data pipeline, the feature engineering, the model strategy across many series, and how forecasts flow to the replenishment system.',
        considerations: [
          'Ingestion of sales, price, promotion calendar, holidays, weather, and inventory/stockout flags into a training table keyed by series and date.',
          'Feature engineering: lag features (last day/week/year), rolling aggregates, calendar features (day-of-week, holiday, month), promotion and price features, and weather, all computed as-of the forecast origin to avoid leakage.',
          'Model strategy: a global model (one gradient-boosting or deep model trained across all series with series identifiers/embeddings) rather than 200k per-series models, so sparse series borrow strength from the population.',
          'A path for classical baselines (ETS/ARIMA, seasonal-naive) as a benchmark and a fallback for stable, long-history series.',
          'Hierarchical structure: SKU-store rolls up to SKU, category, store, region; forecasts should be coherent across the hierarchy (reconciliation).',
          'Cold-start path for new SKUs: forecast from attributes and analogs (similar products, category-level seasonality) until history accrues.',
          'Quantile outputs: produce multiple quantiles (e.g., P50/P90) so downstream safety stock is a lookup, not a re-run.',
          'Batch orchestration: nightly training/scoring job that lands forecasts in a store the replenishment system reads.',
        ],
        strong: [
          'Chooses a global model (gradient-boosted trees like LightGBM, or a global deep model such as DeepAR/TFT-class) trained across all series with series embeddings, and justifies it: sparse and intermittent series borrow strength from the population, which 200k independent models cannot.',
          'Keeps classical methods (ETS/ARIMA, seasonal-naive) as benchmarks and as robust fallbacks for stable long-history series, rather than dismissing them.',
          'Engineers leakage-safe features computed as-of the forecast origin: lags, rolling stats, calendar, promotion/price, and weather, and calls out that any as-of violation leaks the future.',
          'Adds hierarchical reconciliation so SKU-store, SKU, and category forecasts are coherent (bottom-up or MinT-style reconciliation), which matters when planners act at multiple levels.',
          'Designs the cold-start path via product attributes and analogs (category seasonality, similar SKUs) so a new SKU gets a sensible forecast before it has history.',
          'Outputs quantiles directly (P50/P90) so replenishment reads safety stock without re-running the model.',
        ],
        traps: [
          'Fitting an independent ARIMA/ETS per series at 200k scale, which is slow, brittle on short series, and cannot share signal.',
          'Computing features that peek past the forecast origin (e.g., a rolling mean that includes future days), leaking the target.',
          'Ignoring the hierarchy, producing SKU-store forecasts that do not sum to a sane category total planners trust.',
          'No cold-start path, so every new SKU launches with a zero or naive forecast and mis-orders inventory.',
        ],
        probes: [
          'Why train one global model across series instead of 200k per-series models?',
          'Give a concrete feature that would leak the future if computed carelessly, and how you prevent it.',
          'Why do planners care that SKU-store forecasts reconcile up to category level?',
        ],
      },
      {
        id: 'deep-dive',
        title: 'Deep-dive: the hard part',
        ask: 'Go deep on temporal validation and leakage: how do you backtest without cheating, and how do you validate a global model across heterogeneous series?',
        considerations: [
          'Random k-fold is invalid for time series: shuffling lets the model train on future dates and predict the past, inflating accuracy that collapses in production.',
          'Rolling-origin (walk-forward) backtesting: fix an origin, forecast the horizon, roll the origin forward, and average errors over many origins to estimate true out-of-sample accuracy.',
          'As-of feature computation: every feature at forecast origin t must use only data available by t, including lags, rolling stats, and any external join.',
          'Multiple backtest windows to cover seasonality and promotions, not a single cut that happens to miss the holiday peak.',
          'Per-segment evaluation: aggregate error hides that the model may be great on fast movers and terrible on intermittent or cold-start series.',
          'Intermittent-demand handling: many series are mostly zeros; standard RMSE is misleading, and specialized treatment (e.g., Croston-style or quantile targets) is needed.',
          'Prediction-interval validation: check calibration/coverage of the quantiles (does the P90 actually cover ~90%), not just point error.',
          'Backtest-to-production consistency: the backtest must simulate the exact nightly information set, or offline accuracy will not match online.',
        ],
        strong: [
          'Rejects random k-fold outright and uses rolling-origin (walk-forward) backtesting with multiple origins, explaining that any shuffle lets the model see the future and inflates offline accuracy.',
          'Enforces as-of feature computation across every feature, including external joins (weather, promotions), so the backtest replays exactly the information available at each origin.',
          'Evaluates per segment (fast/slow/intermittent/cold-start), because an aggregate error masks that the model may fail exactly on the hard series that drive stockouts.',
          'Validates prediction-interval calibration (does P90 cover ~90% of actuals) rather than only point error, since safety stock depends on the quantile being honest.',
          'Handles intermittent demand explicitly (quantile targets or Croston-style methods) and notes that RMSE alone is misleading on mostly-zero series.',
          'Runs several backtest windows spanning promotions and holidays so a single favorable cut cannot hide seasonal failure, and matches the backtest information set to the nightly production set.',
        ],
        traps: [
          'Using shuffled cross-validation on time series, the single most common and most damaging forecasting mistake.',
          'Reporting a single aggregate error that hides catastrophic performance on intermittent or cold-start series.',
          'Validating only the point forecast and never checking whether the P90 interval is calibrated.',
          'Backtesting on one window that misses the promotion/holiday peak, so offline looks fine and production fails at peak.',
        ],
        probes: [
          'Why is shuffled k-fold specifically wrong here, and what does rolling-origin do instead?',
          'How do you verify your P90 forecast interval is actually a P90 and not a P70?',
          'Your aggregate error looks good but stockouts persist. What did the aggregate hide?',
        ],
      },
      {
        id: 'evaluation',
        title: 'Evaluation & monitoring',
        ask: 'Define the offline backtest metrics, the online accuracy and business tracking, and the monitoring that catches regime change before it wrecks replenishment.',
        considerations: [
          'Offline: backtested error by segment and horizon (e.g., WAPE/MASE for point, pinball loss for quantiles), plus interval coverage, versus a seasonal-naive baseline.',
          'Baseline discipline: a model must beat seasonal-naive per segment, or it is adding complexity without value.',
          'Online accuracy tracking: compare realized demand to forecast as actuals arrive, per segment and horizon, and track forecast bias (systematic over/under).',
          'Business metrics: stockout rate, overstock/holding cost, and service level, since those are what the forecast actually drives.',
          'Bias monitoring specifically: a small persistent bias compounds into large inventory error across 200k series.',
          'Regime-change detection: monitor error and bias over time so a demand shift (new competitor, price change, macro shock) is caught as accuracy degrades.',
          'Data-quality monitors: missing feeds, stockout-censoring flags, and promotion-calendar gaps that silently corrupt inputs.',
          'Retraining trigger: retrain on a cadence and also on a monitored accuracy/bias breach, not only on a fixed schedule.',
        ],
        strong: [
          'Reports backtested WAPE/MASE and pinball loss by segment and horizon against a seasonal-naive baseline, and refuses to ship a model that does not beat naive per segment.',
          'Tracks online accuracy and, specifically, forecast bias as actuals land, because a small systematic bias compounds into large inventory error across 200k series.',
          'Ties evaluation to business outcomes (stockout rate, holding cost, service level), not just statistical error, since the forecast exists to drive replenishment.',
          'Monitors error and bias trends for regime change and wires the retraining trigger to a monitored breach, not only a fixed cadence.',
          'Validates interval coverage online too (are P90 forecasts covering ~90%), since replenishment safety stock relies on it.',
          'Adds data-quality monitors for missing feeds, censoring flags, and promotion-calendar gaps that quietly poison inputs.',
        ],
        traps: [
          'Reporting one aggregate accuracy number with no seasonal-naive baseline, so it is impossible to tell if the model adds value.',
          'Tracking only point accuracy and never forecast bias, missing the systematic drift that compounds into inventory error.',
          'Evaluating statistical error but never stockout/overstock outcomes, optimizing a proxy the business does not feel.',
          'Retraining only on a fixed schedule, so a regime shift degrades forecasts for weeks before the next scheduled retrain.',
        ],
        probes: [
          'Why insist a model beat seasonal-naive per segment before shipping it?',
          'Why is forecast bias more dangerous than variance at 200k-series scale?',
          'How would you detect a demand regime change before it shows up as stockouts?',
        ],
      },
      {
        id: 'tradeoffs',
        title: 'Tradeoffs, failure modes & scaling',
        ask: 'Name the failure modes, the classical-vs-global tradeoff, cold-start handling, and how the design scales and stays robust in the nightly batch.',
        considerations: [
          'Classical (ETS/ARIMA) vs. global ML: classical is interpretable and robust on stable long-history series but scales poorly and cannot share signal; global ML shares strength across series but is heavier and can overfit spiky promotions.',
          'Cold-start and intermittent series as the persistent failure: new SKUs and mostly-zero series are where naive designs mis-order inventory.',
          'Regime change: a global model trained on the past can lag a structural demand shift; monitoring plus retraining cadence bounds the lag.',
          'Promotion and holiday spikes: rare, high-magnitude events that a model can under- or over-react to; explicit promo features and possibly a separate uplift treatment.',
          'Batch-window and cost: 200k series nightly bounds training time; a global model amortizes far better than per-series fits, and horizontal scaling (partition by series group) helps.',
          'Fallbacks: if the global model fails or a series has too little data, fall back to seasonal-naive/ETS rather than emitting a bad or missing forecast.',
          'Reconciliation cost and coherence: hierarchical reconciliation adds compute but keeps multi-level plans consistent.',
          'Robustness to bad inputs: guard against a corrupted promotion calendar or a data-feed gap producing wild forecasts that flow into automated purchase orders.',
        ],
        strong: [
          'Frames classical-vs-global as a real tradeoff: ETS/ARIMA robust and cheap per stable series but non-scaling and signal-isolated, versus a global model that shares strength across 200k series at more compute and overfitting risk, and often blends the two.',
          'Treats cold-start and intermittent series as the durable failure mode and gives concrete handling (attribute/analog forecasts, quantile targets) plus a seasonal-naive fallback.',
          'Handles promotions/holidays explicitly with promo features and considers separate uplift modeling, rather than letting a global model average spikes away.',
          'Bounds regime-change lag with monitoring plus a breach-triggered retrain, acknowledging any historical model lags a structural shift.',
          'Reasons the nightly batch and cost concretely: a global model amortizes across series far better than per-series fits and partitions by series group to fit the window.',
          'Adds robustness guards so a corrupted promotion feed or data gap cannot push wild forecasts into automated purchase orders, with a seasonal-naive fallback behind the check.',
        ],
        traps: [
          'Picking global ML dogmatically and dismissing classical baselines that are more robust on stable long-history series.',
          'No fallback for cold-start or data-starved series, so they get a broken forecast that drives a bad purchase order.',
          'Letting a corrupted promotion calendar or missing feed flow straight into automated orders with no guardrail.',
          'Assuming a fixed retrain cadence handles regime change, ignoring the lag between shift and the next scheduled retrain.',
        ],
        probes: [
          'When would you still prefer classical ETS/ARIMA over a global ML model here?',
          'A data feed drops and the model emits wild forecasts that feed automated purchase orders. What stops the damage?',
          'How does a global model change your nightly-batch cost versus fitting per series?',
        ],
      },
    ],
    rubric: [
      {
        dim: 'Requirements & scoping',
        strong:
          'Derives horizon/granularity from the replenishment decision, picks an asymmetric quantile objective, requires prediction intervals, segments series, and flags stockout censoring.',
        weak: 'Optimizes symmetric RMSE, outputs point forecasts only, treats zero sales as zero demand, and ignores series heterogeneity.',
      },
      {
        dim: 'Architecture & data flow',
        strong:
          'Chooses a global model with series embeddings over 200k per-series fits, keeps classical baselines, engineers leakage-safe as-of features, adds hierarchical reconciliation, and outputs quantiles.',
        weak: 'Fits independent ARIMA per series, computes leaky features, ignores the hierarchy, and has no cold-start path.',
      },
      {
        dim: 'Core ML depth (modeling & features)',
        strong:
          'Uses rolling-origin backtesting with as-of features, handles intermittent demand and censoring, validates interval calibration, and evaluates per segment.',
        weak: 'Uses shuffled k-fold, reports one aggregate error, and never checks interval coverage or intermittent-series behavior.',
      },
      {
        dim: 'Evaluation (offline & online)',
        strong:
          'Backtests WAPE/MASE and pinball loss by segment/horizon versus seasonal-naive, tracks online bias and business outcomes (stockout/holding), and triggers retrain on breach.',
        weak: 'Reports one accuracy number with no baseline, tracks no bias, and never ties evaluation to stockout/overstock cost.',
      },
      {
        dim: 'Reliability, monitoring & failure modes',
        strong:
          'Monitors bias and error for regime change, guards against corrupted feeds reaching automated orders, and falls back to seasonal-naive/ETS for data-starved series.',
        weak: 'Has no bias or regime monitoring, lets bad inputs flow into purchase orders, and emits broken forecasts for cold-start series.',
      },
      {
        dim: 'Scaling, latency & cost',
        strong:
          'Justifies a global model for nightly-batch amortization across 200k series, partitions by series group, and blends classical where it is more robust and cheaper.',
        weak: 'Fits per-series models that blow the batch window and dismisses classical baselines dogmatically.',
      },
      {
        dim: 'Communication & structure',
        strong:
          'Moves requirements -> architecture -> temporal-validation deep-dive -> evaluation -> tradeoffs, quantifies scale and horizon, and names leakage and bias as the load-bearing risks.',
        weak: 'Lists forecasting methods with no structure or numbers and never explains why temporal validation or bias matter.',
      },
    ],
  },
];

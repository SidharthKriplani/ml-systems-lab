export const UNSUPERVISED_MODULES = [
  {
    id: 'clustering_overview',
    title: 'Clustering Taxonomy',
    subtitle: 'Partitional vs hierarchical vs density-based, no ground truth = no accuracy',
    difficulty: 'foundational',
    estimatedMin: 38,
    tags: ['clustering', 'taxonomy', 'unsupervised'],
    summary: `Clustering is one of those methods where the algorithm is easy to run and the results are easy to misinterpret. There are no labels to optimise against — so choosing a clustering algorithm is really choosing a definition of what "similar" means, and the only valid final evaluation is whether the resulting clusters are actionable for the downstream business problem. Partitional methods like k-means assign every point to exactly one cluster and require knowing k upfront. Hierarchical methods build a full tree of merges, revealing structure at every granularity simultaneously without committing to k. Density-based methods like DBSCAN define clusters as contiguous high-density regions, discovering arbitrary shapes and explicitly labelling sparse points as noise. Evaluation without labels must use intrinsic metrics — silhouette score, Davies-Bouldin index — that measure geometric properties of the clusters, but these metrics can disagree with each other and with domain expert judgement. The failure mode of clustering is over-trusting a metric score: a silhouette of 0.7 with meaningless clusters is worse than a silhouette of 0.5 with actionable customer segments. The right algorithm is chosen based on expected cluster shape, tolerance for noise, whether hierarchy is meaningful, and dataset scale.`,
    keyPoints: [
      `Partitional clustering (k-means, k-medoids) divides n points into exactly k non-overlapping clusters. You must specify k upfront even when the true number of clusters is unknown — choosing k incorrectly forces the algorithm to split natural clusters or merge unrelated ones. The failure mode is picking k=5 because it is round, not because the data has 5 natural groups.`,
      `Hierarchical clustering builds a complete dendrogram — a binary tree where every possible merge is recorded — without requiring k upfront. The analyst chooses the number of clusters post-hoc by cutting the tree at a height threshold. This is the key advantage: you can explore multiple granularities from one run. The cost is computational — hierarchical clustering is O(n² log n) memory and time, making it unsuitable for n > 10,000 without approximations.`,
      `Density-based clustering (DBSCAN, HDBSCAN) defines clusters as regions where points are sufficiently dense, connected by density-reachability chains. Points in sparse regions are explicitly labelled as noise, not forced into a cluster. This makes it the correct method when the data has outliers that would distort centroids (k-means) or when cluster shapes are non-convex. The failure mode: clusters with varying density confuse a single eps threshold — HDBSCAN solves this.`,
      `Intrinsic evaluation metrics — silhouette score, Davies-Bouldin index, Calinski-Harabasz index — measure geometric properties like compactness (points close to their centroid) and separation (clusters far from each other). All of them assume clusters are blobs in Euclidean space, so they systematically favour k-means-style round clusters even when the true structure is elongated or non-convex. A DBSCAN result that looks perfect visually can score low on silhouette because the metric cannot see irregular shapes.`,
      `Extrinsic evaluation (Adjusted Rand Index, Normalised Mutual Information) compares predicted clusters to ground truth labels. ARI=1 means perfect agreement; ARI=0 means agreement no better than chance. NMI measures the mutual information between the two clusterings, normalised to [0,1]. These are the right metrics for benchmarking algorithms on labelled datasets, but in production you almost never have ground truth. Extrinsic metrics are benchmarking tools, not deployment tools.`,
      `The business validity question is separate from the statistical quality question. Clusters are valid if they are: stable (similar structure on different subsets or seeds), meaningful (a domain expert can label each cluster with a coherent description), and useful (acting on cluster membership improves downstream outcomes). Statistical metrics measure only the first dimension. Missing the second or third is how teams build "clustering pipelines" that never get used.`,
      `Algorithm selection heuristic: if you need to cluster millions of points into round blobs and k is known — k-means. If k is unknown and you want to explore multiple granularities on < 10,000 points — hierarchical with Ward linkage. If clusters have arbitrary shapes or the data has genuine outliers — DBSCAN or HDBSCAN. If you need non-linear structure or the data lives on a manifold — cluster on UMAP embeddings, not in raw feature space.`,
      `The curse of dimensionality degrades every clustering algorithm: in high dimensions, distances between points converge to the same value, making "nearby" and "far" meaningless. With more than ~50 features, clustering almost always requires dimensionality reduction (PCA, UMAP) first. This is why feature engineering and preprocessing matter more for clustering than for supervised learning — the algorithm cannot learn what is relevant because there are no labels to guide it.`,
    ],
    checkQuestions: [
      {
        q: `A clustering of customers produces 5 clusters with silhouette score=0.62. A domain expert says 3 of the clusters look identical in terms of purchasing behaviour. How do you reconcile this?`,
        a: `The silhouette score measures geometric separation in feature space, but the domain expert is judging semantic similarity in purchasing behaviour — these can genuinely disagree. If 3 clusters are statistically separate (different centroids in feature space) but behaviourally identical (same RFM profile, same product affinities), it means the features used have spurious variance that drives geometric separation without capturing the business-relevant dimensions. The fix is not to trust the metric over the expert: revisit feature selection to ensure features directly encode purchasing behaviour, not noise dimensions. Re-run the clustering on the revised features. The silhouette score is a tool, not a ground truth — the expert's knowledge of what makes customers distinct is the actual success criterion.`,
      },
      {
        q: `You run k-means and DBSCAN on the same dataset. k-means silhouette=0.71, DBSCAN silhouette=0.43. Should you prefer k-means?`,
        a: `Not necessarily. Silhouette score is computed using Euclidean distances and implicitly assumes convex clusters — it is biased toward methods that produce round, compact clusters like k-means. If the true cluster shapes are non-convex (C-shapes, rings, elongated), DBSCAN may be recovering the real structure even while scoring lower on a metric that cannot see non-convex shapes. The right diagnostic: visualise both clusterings on 2D projections (PCA or UMAP), examine whether DBSCAN's noise points are genuine outliers, and test whether cluster membership from each method predicts meaningful outcomes in a downstream task. Always validate clustering against the application objective, not just the intrinsic metric.`,
      },
      {
        q: `A colleague proposes using k-means with k=150 to cluster 10,000 user-behaviour vectors with 512 features. What are the failure modes?`,
        a: `Three compounding problems. First, the curse of dimensionality: in 512 dimensions, Euclidean distances between points are nearly uniform, so the "nearest centroid" assignment is noisy and centroids converge slowly. Apply PCA or UMAP to reduce to 30-50 dimensions first. Second, k=150 with n=10,000 gives on average 67 points per cluster — many clusters will have fewer than 20 points, making centroids unstable and the assignment arbitrary. k should be at most sqrt(n/2) ≈ 70 as a rough upper bound. Third, k-means++ initialisation with k=150 takes k passes to initialise, which is slow. More fundamentally, with k this large, cluster structure will be highly sensitive to initialisation. Run with high n_init and check that WCSS converges consistently.`,
      },
      {
        q: `You need to explain to a non-technical stakeholder why you cannot report "clustering accuracy." What do you say?`,
        a: `Accuracy requires a known correct answer: accuracy = fraction of predictions that match the true label. Clustering is unsupervised — there are no true labels, so there is no ground truth to compare against. The goal of clustering is to discover structure, not to match a pre-specified structure. What you can report instead: silhouette score (how tightly packed and well-separated the clusters are, on a scale from -1 to 1), cluster stability (do you get similar clusters when you run it again or on a random subset of the data), and business validation (does each cluster map to a recognisable customer segment that your team can act on). The last criterion is the one that actually matters for the business.`,
      },
    ],
    takeaway: `The key insight is that clustering has no labels to optimise against, which means choosing a clustering algorithm is choosing a definition of what "similar" means — and the only valid final evaluation is whether the resulting clusters are actionable for the downstream business problem.`,
  },
  {
    id: 'kmeans',
    title: 'K-Means Clustering',
    subtitle: `Lloyd's algorithm, k-means++ init, silhouette, failure modes`,
    difficulty: 'foundational',
    estimatedMin: 36,
    tags: ['k-means', 'clustering', `Lloyd's algorithm`],
    summary: `K-means is the workhorse of clustering — fast, scalable, and widely understood — but it bakes in assumptions that silently break things when violated. It partitions n points into k clusters by alternating between assigning each point to its nearest centroid (the assignment step) and recomputing each centroid as the mean of its assigned points (the update step). This is Lloyd's algorithm, and it minimises the within-cluster sum of squares (WCSS). The algorithm is guaranteed to converge but only to a local optimum: different starting points produce different final clusterings. K-means++ is a smarter initialisation strategy that chooses initial centroids spread across the data, giving a provable O(log k) approximation to the global optimum. The hidden assumption is that clusters are convex, roughly spherical, and similar in size — violations produce poor or misleading clusters even when the algorithm converges perfectly. Selecting k is the hardest practical challenge: the elbow method, silhouette analysis, and gap statistic each suggest k but none are definitive. In high dimensions, k-means degrades because Euclidean distance loses discriminative power, which is why dimensionality reduction is typically applied first.`,
    keyPoints: [
      `Lloyd's algorithm (the standard k-means): (1) Initialise k centroids. (2) Assign each of n points to its nearest centroid — O(nk) per iteration. (3) Recompute each centroid as the mean of assigned points — O(n). (4) Repeat until assignments stop changing. Convergence is guaranteed because WCSS decreases at every step and there are only finitely many assignments. The catch: it converges to a local optimum, not the global one — the final result depends on initialisation.`,
      `K-means++ initialisation: choose the first centroid uniformly at random. For each subsequent centroid, choose a point with probability proportional to its squared distance to the nearest already-chosen centroid — D²(x) / sum D²(x'). This spreads centroids across the data and avoids starting with multiple centroids in the same cluster. It gives an expected WCSS within O(log k) of optimal, and in practice reaches much better solutions than random init. sklearn uses this by default.`,
      `Choosing k — the elbow method: plot WCSS vs k. WCSS always decreases as k increases (adding a centroid always reduces total distance). The "elbow" — where the curve bends from steep descent to flattening — suggests the k beyond which adding more clusters gives diminishing return. In practice, real data often has no clear elbow, or the elbow is ambiguous. Treat the elbow as a rough guide, not a definitive answer.`,
      `Choosing k — silhouette analysis: for each point i, compute a(i) = mean intra-cluster distance and b(i) = mean distance to nearest other cluster. Silhouette s(i) = (b(i) - a(i)) / max(a(i), b(i)), range [-1, 1]. Mean silhouette over all points is highest at the k that produces the best-separated clusters. Unlike the elbow method, silhouette analysis has a clear optimisation target: find the k that maximises mean silhouette. This is why it is more reliable than the elbow for most practical problems.`,
      `k-means failure mode 1 — non-spherical clusters: k-means minimises Euclidean distance, which defines clusters as Voronoi cells (convex polytopes). Two interleaved half-moons, concentric rings, or elongated ellipsoids all violate this assumption — k-means splits them incorrectly. This is not a convergence failure; the algorithm converges confidently to a wrong answer. Gaussian Mixture Models (soft, elliptical clusters), DBSCAN (arbitrary shapes), or spectral clustering are the fixes.`,
      `k-means failure mode 2 — unequal cluster density and size: k-means assigns each point to its nearest centroid. If one true cluster has 10 points and another has 1,000, the large cluster's centroid is pulled toward its dense core, leaving many peripheral points closer to a small cluster's centroid. The small cluster "steals" points from the large one, over-segmenting the large cluster and under-representing the small one. k-medoids is slightly more robust; DBSCAN handles this naturally.`,
      `k-means failure mode 3 — sensitivity to outliers: outliers are assigned to a centroid and pull that centroid toward them, distorting the cluster shape for all other members. A single extreme outlier in a well-formed cluster can shift its centroid significantly. k-medoids (which uses the actual data point closest to the cluster centre as the representative) is outlier-robust because the medoid cannot be pulled by a distant point. Alternatively, remove outliers with Isolation Forest before running k-means.`,
      `Mini-batch k-means: instead of using all n points per iteration, sample a random batch of size B each step and update only the centroids involved in that batch. Training cost drops from O(nkd) per iter to O(Bkd), enabling k-means on datasets with millions of points where full k-means is slow. Quality is slightly lower than full k-means but typically within 1-2% of WCSS for B > 1,000. sklearn's MiniBatchKMeans is the standard production implementation for large-scale clustering.`,
    ],
    checkQuestions: [
      {
        q: `K-means gives very different results on different runs on the same dataset. What is wrong and how do you fix it?`,
        a: `Different runs converge to different local optima because k-means with random initialisation starts from a different point in the WCSS landscape each time. Three fixes: (1) Use k-means++ initialisation (n_init=10, init='k-means++' in sklearn) — much less sensitive to starting positions because centroids begin spread across the data. (2) Run multiple times and keep the result with the lowest total WCSS — sklearn's KMeans does this automatically when n_init > 1. (3) If variance persists even with k-means++, it means the WCSS landscape has many equally-good local optima — which often indicates the true number of clusters is ambiguous, or the data does not have spherical structure. In that case, try hierarchical clustering or DBSCAN and see if a more stable structure emerges.`,
      },
      {
        q: `You apply k-means to 50,000 customer vectors with 200 features. Silhouette scores are uniformly low (0.08–0.12) for all k from 2 to 20. What does this tell you and what do you do?`,
        a: `Uniformly low silhouette across all k means k-means cannot find separable structure in the data. Three root causes: (1) The curse of dimensionality — in 200 dimensions, Euclidean distances between points are nearly uniform, so all centroids are roughly equidistant from all points. Apply PCA to reduce to 20-30 dimensions (capturing ~90% of variance) and re-run. (2) The data may not have cluster structure — it may be a continuous distribution without natural groups. Inspect 2D PCA projections for visual separation before assuming clusters exist. (3) The features may not encode the relevant similarity for the business purpose — if you are clustering by purchasing behaviour but features include user age and location, the geometric structure reflects those demographics, not behaviour. Feature selection or transformation is the fix here.`,
      },
      {
        q: `A k-means run with k=5 produces one cluster with 90% of the data and four clusters each with 2-3%. What likely went wrong?`,
        a: `This is k-means's failure mode on data with very unequal cluster densities or sizes. The dominant cluster likely contains most of the data because: (1) k was set too high — the data may have only 2 natural groups, and k-means is trying to carve 5 clusters where there are only 2, creating tiny splinters from the boundary regions of the real clusters. (2) There are true outliers or a heavy-tailed distribution where most points are compact but a few extreme points each get their own cluster. (3) The initialisation placed 4 starting centroids in a low-density region. Diagnose by trying k=2,3 and checking silhouette scores. If k=2 gives two large balanced clusters, the k=5 result is wrong. Also run with n_init=20 to rule out an unlucky initialisation as the cause.`,
      },
      {
        q: `What is the difference between k-means and k-medoids, and when would you choose each?`,
        a: `K-means represents each cluster by its centroid — the arithmetic mean of all points in the cluster. The centroid need not be an actual data point. K-medoids represents each cluster by its medoid — the actual data point that minimises total distance to all other cluster members. The centroid can be pulled far from the cluster by outliers; the medoid cannot, because it must be an existing point and extreme outliers are outvoted by the dense core. Choose k-medoids when: (1) The data has significant outliers that would distort centroids. (2) Your distance metric is not Euclidean (e.g., edit distance between strings, Jaccard distance between sets) — in those cases, centroid means are undefined. (3) Interpretability matters and you want to report a representative example rather than an abstract average. The cost is that k-medoids (PAM algorithm) is O(k(n-k)²) per iteration — much slower than k-means's O(nkd).`,
      },
      {
        q: `Explain why k-means is equivalent to the EM algorithm on a specific probabilistic model. What does this reveal about its assumptions?`,
        a: `K-means is the hard-assignment limit of EM applied to a Gaussian Mixture Model where all components share the same isotropic covariance σ²I and σ→0. In the EM limit with identical covariances: the E-step (computing soft responsibilities) degenerates to a hard assignment — each point belongs to the component with the highest likelihood, which is the nearest centroid. The M-step (updating means) remains computing the cluster mean. This equivalence reveals k-means's hidden assumptions: it assumes each cluster is a spherical Gaussian with identical radius (same σ for all clusters). Non-spherical clusters violate this (fix: full-covariance GMM). Different-density clusters violate this (fix: allow different σ per component). Soft cluster membership is sometimes the right answer (fix: GMM with soft assignments). Understanding k-means as hard-assignment equal-covariance EM explains every one of its failure modes.`,
      },
    ],
    takeaway: `The key insight is that k-means minimises within-cluster Euclidean distance, which forces it to assume spherical equal-size clusters — so whenever your data violates those assumptions (unequal sizes, non-convex shapes, outliers), the algorithm converges confidently to a wrong answer and the only fix is a different algorithm.`,
    interactiveId: 'kmeans_viz',
  },
  {
    id: 'hierarchical',
    title: 'Hierarchical Clustering',
    subtitle: 'Linkage criteria, dendrograms, when hierarchy beats flat clustering',
    difficulty: 'intermediate',
    estimatedMin: 34,
    tags: ['hierarchical clustering', 'dendrogram', 'linkage'],
    summary: `Hierarchical clustering is the right tool when you do not know k and want the data to tell you. It builds a dendrogram — a binary tree recording every cluster merge — from n singletons up to one all-encompassing cluster, without ever needing to specify k. At every level of the tree, you can read off a flat clustering by cutting at a given height threshold. The linkage criterion defines how distance between two clusters is computed from point-to-point distances: single linkage uses the closest pair, complete linkage uses the farthest pair, average linkage uses the mean of all pairs, and Ward linkage minimises the increase in within-cluster variance at each merge. Ward linkage is the most commonly useful choice because it produces compact, similarly-sized clusters similar to k-means — without requiring k upfront. Dendrogram height represents dissimilarity: long vertical branches indicate a large distance between merged clusters, which is where a natural cut suggests itself. The main limitation is computational: naive agglomerative clustering is O(n³) and requires O(n²) memory, making it unsuitable for n above about 10,000. The payoff is interpretability: the full merge tree is a description of your data's cluster structure at every granularity, not just at one fixed k.`,
    keyPoints: [
      `Agglomerative clustering (bottom-up): start with n clusters of one point each. At each step, merge the two clusters with the smallest inter-cluster distance according to the linkage criterion. Repeat n-1 times until one cluster remains. The dendrogram records the height (distance) at which each merge happened. Divisive (top-down) clustering is less common and typically only used for specific hierarchical applications.`,
      `Single linkage (nearest-neighbour): distance between clusters = distance between their closest points. Creates a chaining effect — if one point in cluster A is close to one point in cluster B, they merge regardless of how far apart the rest of the points are. Useful for detecting filamentary structure, but pathologically bad for compact blob-like clusters. Single linkage is highly sensitive to a single bridging point between two otherwise-distant groups.`,
      `Complete linkage (furthest-neighbour): distance between clusters = distance between their farthest points. Produces compact, roughly equal-diameter clusters because two clusters only merge when their most distant members are close. More sensitive to outliers than average linkage — one extreme outlier increases the diameter of its cluster, delaying merges. Better than single linkage for compact, spherical clusters but not as robust as Ward.`,
      `Ward linkage: at each step, merge the pair of clusters that minimises the increase in total within-cluster sum of squares (WCSS). This is equivalent to minimising the variance of the merged cluster. Ward produces compact, roughly equal-size clusters that often match k-means results — but without requiring k upfront and with the full dendrogram available for cut-height selection. Ward is the default choice in most practical applications.`,
      `Reading a dendrogram: the y-axis is the linkage distance (dissimilarity) at which each merge occurred. Long vertical lines — large height gaps between a merge and the next merge — indicate natural cluster boundaries. Draw a horizontal cut line: all branches below the line merge into clusters, and the number of lines the cut crosses is k. A good cut position is just below a long vertical line, where the dendrogram transitions from many small merges to one large merge.`,
      `Cophenetic correlation coefficient: measures how faithfully the dendrogram preserves the original pairwise distances. For each pair of points, the cophenetic distance is the dendrogram height at which they first merge. Correlation between original pairwise distances and cophenetic distances gives a score from 0 to 1. High cophenetic correlation (> 0.8) means the dendrogram is a good summary of the distance structure — a diagnostic for whether hierarchical clustering is appropriate for the data at all.`,
      `Computational constraints and approximations: naive agglomerative clustering is O(n³) time and O(n²) space (storing the pairwise distance matrix). For single linkage, SLINK reduces this to O(n²) time and O(n) space. For n > 10,000, approximate methods like BIRCH (builds a Clustering Feature tree, then applies agglomerative) or mini-batch agglomerative are needed. For millions of points, hierarchical clustering is not the right tool — use k-means or HDBSCAN instead.`,
      `When hierarchical clustering is the right choice: (1) When you need to explore multiple levels of granularity from one run — a product taxonomy, a gene family tree, a geographic region hierarchy. (2) When k is unknown and you want the data to reveal natural cut points via the dendrogram. (3) When the domain has inherent hierarchy (phylogenetics, document subcategory trees). (4) When dataset size is manageable (n < 10,000). Using hierarchical clustering as a substitute for k-means on large, unstructured data is the wrong application.`,
    ],
    checkQuestions: [
      {
        q: `You are clustering genes based on expression profiles. Why might hierarchical clustering with Ward linkage be more appropriate than k-means?`,
        a: `Gene expression clusters typically have a natural biological hierarchy — genes belong to pathways, pathways belong to regulatory modules, modules belong to broader biological processes. Hierarchical clustering reveals this multi-level structure from a single run: you can cut at different heights to get pathway-level clusters (many small groups), module-level clusters (medium groups), or process-level clusters (few large groups). K-means forces you to commit to exactly one k before seeing results, which means you either run it 10 times for different k values or guess. Ward linkage is specifically appropriate because co-expressed gene clusters tend to be compact and similarly-sized — Ward minimises within-cluster variance, producing groups that share tight co-expression profiles. The dendrogram is also directly interpretable by biologists who compare it to known pathway databases.`,
      },
      {
        q: `Two researchers use the same dataset but different linkage criteria. Researcher A uses single linkage and gets one large cluster containing 95% of the data. Researcher B uses Ward linkage and gets 5 balanced clusters. Who is right?`,
        a: `Both results are mathematically valid given their linkage criterion, but they reveal different aspects of the data's structure. Single linkage's chaining effect means the 95% cluster is held together by a chain of nearby points — remove a few bridging points and it fragments. Ward linkage ignores these bridges and instead optimises for compact, internally homogeneous groups. Whether A or B is "right" depends on what the data actually contains. If the data has elongated manifold structure (filamentary, chain-like), single linkage is capturing the true topology. If the data has 5 distinct blobs connected by sparse bridges, single linkage is being fooled by the bridges while Ward is recovering the real structure. The diagnostic: visualise the data in 2D (PCA/UMAP), check the cophenetic correlation for both linkages, and validate which clustering aligns with domain knowledge.`,
      },
      {
        q: `How do you determine the optimal cut height on a dendrogram when there is no obvious long gap?`,
        a: `When the dendrogram lacks an obvious long gap, the data may not have clear discrete clusters. Several approaches: (1) Accelerations plot: plot the second derivative of the linkage distances (the increase in distance at each merge step). Peaks in the acceleration plot correspond to large jumps in merge distance — the peak position is a more quantitative version of visually identifying long lines. (2) Silhouette analysis across different cuts: for each possible k (each cut height that gives a different k), compute mean silhouette score and choose the k that maximises it. (3) Domain knowledge: if the application requires exactly k clusters, cut at the height that produces k clusters regardless of the gap structure. (4) Mixture model comparison: fit a GMM with varying k and compare BIC — this is more principled than visual dendrogram inspection for data without clear hierarchical structure.`,
      },
      {
        q: `A colleague wants to run hierarchical clustering on 100,000 points. What do you tell them?`,
        a: `Naive agglomerative clustering on 100,000 points requires storing an n×n distance matrix of 100,000² / 2 = 5 billion pairs — roughly 40 GB of memory at 8 bytes per float. This is infeasible on a standard machine. The computation itself is O(n³), which at 100,000 points would take approximately (100,000)³ / 10^9 = 10^6 seconds — months of compute. The practical alternatives: (1) Use BIRCH to compress the data into a Clustering Feature tree first, then run hierarchical clustering on the compressed representation (a few thousand leaf nodes rather than 100,000 points). (2) Use HDBSCAN, which achieves hierarchical cluster structure in O(n log n). (3) Subsample: run hierarchical clustering on a representative 5,000-10,000 point sample, then assign remaining points to their nearest cluster. Option 2 is the best for most modern applications.`,
      },
    ],
    takeaway: `The key insight is that hierarchical clustering's dendrogram encodes cluster structure at every granularity simultaneously, which means it answers "what is the right k?" by showing you where the data's natural boundaries are — rather than requiring you to know k before you start.`,
  },
  {
    id: 'dbscan',
    title: 'DBSCAN',
    subtitle: 'Core/border/noise points, eps and minPts, non-spherical clusters',
    difficulty: 'intermediate',
    estimatedMin: 36,
    tags: ['DBSCAN', 'density-based', 'outlier detection'],
    summary: `DBSCAN is the algorithm to reach for when you expect non-convex cluster shapes, genuine outliers, or an unknown number of clusters. It defines clusters as maximal sets of mutually density-connected points, where density is measured by counting how many points fall within a radius eps of each point. A core point has at least minPts neighbours within eps — it is in a dense region. A border point is within eps of a core point but is not itself dense. A noise point (outlier) is neither a core point nor reachable from any core point. Clusters grow by collecting all points density-reachable from a core point — following density chains rather than distance to a centroid — so clusters can take any shape: crescents, rings, interleaved spirals. Because noise points are labelled explicitly rather than forced into a cluster, DBSCAN does not distort cluster shapes with outliers. The key limitation is that a single eps threshold fails when clusters have very different densities — which HDBSCAN solves by building a hierarchy of clusterings across all eps values and extracting the most stable clusters.`,
    keyPoints: [
      `Core point definition: a point p is a core point if there are at least minPts points within distance eps of p (including p itself). Border point: within eps of a core point but has fewer than minPts neighbours — it is on the edge of a dense region. Noise point: no core point within eps — it is in a sparse region. Only core points seed new clusters; border points join clusters but do not expand them. The three-way classification is DBSCAN's mechanism for outlier detection: noise points get no cluster assignment.`,
      `Density-reachability and connectivity: point q is directly density-reachable from p if q is within eps of p and p is a core point. q is density-reachable from p if there is a chain p=p₁, p₂,...,pₙ=q where each step is directly density-reachable. Two core points are density-connected if there is a core point from which both are density-reachable. A cluster is the set of all density-connected core points plus all border points within eps of any of them. This chain-following mechanism is what allows DBSCAN to grow clusters along thin, winding structures.`,
      `Parameter selection — eps: the k-distance plot plots the distance to the k-th nearest neighbour for each point (k=minPts), sorted in ascending order. Dense regions show low k-distance; sparse regions show high k-distance. The "knee" in this plot — the point where k-distance increases sharply — marks the natural transition between dense and sparse regions. Setting eps at the knee value correctly separates cluster points from noise. If there is no clear knee, the data may not have distinct density structure suitable for DBSCAN.`,
      `Parameter selection — minPts: rule of thumb is minPts ≥ ln(n) for small datasets, and minPts = 2×d (twice the feature dimension) as a lower bound. Higher minPts increases the density threshold, so sparser points qualify as noise — better for noisy datasets but risks fragmenting real clusters. Lower minPts captures more points as cluster members but makes the algorithm less robust to noise. In practice, minPts=5 is a reasonable default for 2D data; increase to 10-20 for higher-dimensional data.`,
      `DBSCAN vs k-means — when to choose DBSCAN: (1) Cluster shapes are non-convex — DBSCAN follows density chains, k-means draws Voronoi cells. (2) Outliers are present and should not distort cluster shapes — DBSCAN labels them as noise; k-means assigns them to a cluster and pulls the centroid. (3) k is unknown and you suspect very different cluster sizes — DBSCAN finds them naturally. The failure mode for DBSCAN is uniform density data where there is no density-gap to define cluster boundaries, or clusters with varying density that confuse a single eps.`,
      `DBSCAN time complexity: O(n log n) with a spatial index (k-d tree or ball tree) for low-dimensional data (d ≤ 10). O(n²) without a spatial index or in high dimensions where spatial indexes degrade. For d > 20, k-d trees become inefficient and DBSCAN degrades toward O(n²). The practical implication: DBSCAN is efficient for geographic/spatial data (2D-3D) and slow for high-dimensional text or image embeddings. Apply dimensionality reduction first.`,
      `HDBSCAN (Hierarchical DBSCAN): instead of a single eps threshold, HDBSCAN builds a hierarchy of clusterings across all eps values by computing the minimum spanning tree of mutual reachability distances. It then extracts the clusters that are most stable across a range of eps — where "stability" measures how long a cluster persists as eps changes. This makes HDBSCAN robust to varying cluster density (different clusters can have different effective eps values) and eliminates the need to manually tune eps. HDBSCAN is the modern default for density-based clustering when cluster densities may vary.`,
      `Production use cases: DBSCAN is the right tool for geographic clustering (GPS pings into visit locations), anomaly detection (noise points are anomalies by definition), image segmentation (pixel clusters of arbitrary shape), and event detection from sensor streams. The explicit noise label is valuable in production — instead of forcing ambiguous points into a cluster, you can route them to a "requires human review" queue rather than making an overconfident automated decision.`,
    ],
    checkQuestions: [
      {
        q: `K-means gives 5 circular clusters on GPS location data, but you suspect the true structure has non-circular geographic regions. How do you set up DBSCAN and validate it?`,
        a: `For GPS (2D lat/lon) data, compute the k-distance plot with k=4 (minPts=4, the rule for 2D): for each point, compute the distance to its 4th-nearest neighbour and sort ascending. Find the knee — this is the eps value separating the dense location clusters from scattered noise points. Set minPts=4 and eps at the knee. Validate: (1) Visually inspect the clusters on a map — do they follow natural geographic boundaries (neighbourhoods, parks, roads) rather than circles? (2) Check that noise points are genuinely sparse areas (highways, open water), not legitimate locations. (3) Compare cluster count and coverage against domain knowledge. (4) If some genuine locations are labelled as noise, either reduce eps or increase the minimum density threshold — GPS data often has isolated visits that should belong to a cluster but are rare.`,
      },
      {
        q: `DBSCAN with eps=0.5 and minPts=5 produces 1 cluster containing 95% of data and 200 noise points. What does this indicate and what do you try?`,
        a: `One giant cluster means eps is too large — the radius is so wide that all dense regions are connected into a single density-reachable component. The 200 noise points are genuinely isolated. To fix: reduce eps. Use the k-distance plot (k=5): find the knee more carefully. If the current eps of 0.5 is well above the knee, the knee is at a much smaller value. Halve eps incrementally (0.25, 0.1, 0.05) and inspect how the number of clusters changes. Also consider: if the data truly has no density gap between clusters — all points are in one continuous dense region — then DBSCAN is the wrong algorithm for finding subgroups. In that case try hierarchical clustering to see if the dendrogram reveals merge-distance gaps, or k-means if you are willing to assume blob-shaped clusters. The 200 noise points are a useful signal: their positions may represent genuine outliers that you should examine for anomaly patterns regardless of the clustering outcome.`,
      },
      {
        q: `You apply DBSCAN to customer embeddings in 128 dimensions and get mostly noise (90% of points labelled -1). What is happening and how do you fix it?`,
        a: `This is the curse of dimensionality applied to DBSCAN: in 128 dimensions, Euclidean distances between all pairs of points converge toward the same value. The result is that no point has "many neighbours within eps" unless eps is extremely large — because the volume of a ball in 128D space grows as r^128, meaning you need an enormous radius to capture nearby points. At any eps small enough to be discriminating, every point appears isolated and gets labelled noise. The fix is dimensionality reduction before DBSCAN: apply PCA or UMAP to reduce to 10-20 dimensions where distances are still meaningful. UMAP is preferred because it specifically preserves neighbourhood structure (which is what DBSCAN uses). Then re-run DBSCAN on the low-dimensional embeddings with appropriate eps from the k-distance plot on the reduced space.`,
      },
      {
        q: `What is the difference between noise in DBSCAN and outliers detected by Isolation Forest? When would you use each for anomaly detection?`,
        a: `DBSCAN noise is defined by density: a point is noise if it has fewer than minPts neighbours within eps — it is in a sparse region relative to the chosen density threshold. DBSCAN noise is local and density-dependent: a point that is noise in one part of the feature space might be a core point in a sparser region. Isolation Forest defines anomaly score by isolation depth: anomalies are points that require few random splits to isolate — they are in sparse, extreme regions of feature space globally. The difference: DBSCAN requires a density that makes sense for both normal points and the anomaly context; Isolation Forest is parameter-lighter (no eps to tune) and globally ranks all points by anomaly score. Use DBSCAN when you simultaneously want clusters (normal structure) and outliers (noise), especially for geographic or low-dimensional data. Use Isolation Forest when you only need anomaly scores, the data is high-dimensional, or when a continuous anomaly score (rather than binary noise label) is needed for a downstream decision threshold.`,
      },
    ],
    takeaway: `The key insight is that DBSCAN defines clusters by density-reachability chains rather than distance to a centroid, which means it can find clusters of any shape and explicitly labels outliers as noise — but a single eps threshold fails when clusters have different densities, which is why HDBSCAN is the default for production use.`,
    interactiveId: 'dbscan_viz',
  },
  {
    id: 'pca',
    title: 'PCA — Principal Component Analysis',
    subtitle: 'Eigenvectors, explained variance, when to use and when it fails',
    difficulty: 'intermediate',
    estimatedMin: 38,
    tags: ['PCA', 'dimensionality reduction', 'eigenvectors', 'variance'],
    summary: `PCA is the first thing to try for dimensionality reduction — linear, interpretable, and closed-form. It finds the directions of maximum variance in the data and projects data onto those directions. The first principal component is the unit vector that maximises variance of the projected data; the second is orthogonal to the first and maximises remaining variance; and so on. These directions are eigenvectors of the data's covariance matrix, and the variance along each is the corresponding eigenvalue. By projecting onto the top k eigenvectors, PCA produces a k-dimensional representation that retains as much variance as possible. The critical limitation is that PCA finds directions of maximum variance, not directions of maximum relevance to your task — the components it discards may contain the class-discriminative signal you actually need. PCA is also sensitive to feature scale, so standardisation before PCA is nearly always required. For non-linear structure (spiral manifolds, concentric rings), PCA cannot help — use t-SNE, UMAP, or kernel PCA instead.`,
    keyPoints: [
      `PCA mechanics: compute the covariance matrix C = (1/n) X^T X for centred X. Decompose C = V Λ V^T where columns of V are eigenvectors and Λ is the diagonal matrix of eigenvalues. Project data as Z = XV. The first column of V is the direction of maximum variance (largest eigenvalue), the second is the orthogonal direction of next-highest variance, and so on. In practice, use SVD of X rather than eigendecomposition of C for numerical stability: X = UΣV^T, where right singular vectors V are the principal component directions.`,
      `Explained variance ratio: eigenvalue λ_k / sum(all eigenvalues) is the fraction of total variance captured by component k. The explained variance curve (cumulative sum) tells you how many components are needed. Choose k where the cumulative explained variance reaches your threshold (typically 90-95% for preprocessing, 99% for lossless compression). The scree plot — eigenvalues vs component number — shows an elbow where components switch from explaining substantial variance to explaining noise.`,
      `PCA requires standardisation: PCA maximises variance, so features with larger numerical ranges dominate the first components regardless of their actual information content. Height in centimetres (range ~150-200) will dominate over a binary gender flag simply because of scale. Always apply StandardScaler (zero mean, unit variance) before PCA. The exception: if all features are already in the same units with comparable ranges (e.g., pixel intensities all in [0, 255]) or you deliberately want to weight by variance (e.g., financial returns where volatility matters), skip standardisation.`,
      `What PCA preserves and loses: PCA preserves global linear structure — large-scale variance, correlations between features. It discards local non-linear structure — clusters embedded in low-variance manifolds, non-linear relationships like concentric rings. It preserves Euclidean distances approximately (the Frobenius norm of the reconstruction error equals the sum of discarded eigenvalues). It does not preserve local neighbourhood structure in the way t-SNE or UMAP do. PCA projections often look like a cloud with no cluster structure even when the original high-dimensional data has clear clusters.`,
      `PCA for preprocessing vs PCA for visualisation: as a preprocessing step, PCA reduces dimension before a downstream model (k-means, logistic regression, SVM). This removes noise dimensions, reduces computation, and avoids the curse of dimensionality. Keep enough components to retain 90-99% of variance. For visualisation, project to 2D or 3D. The 2D PCA projection is the best linear 2D projection of the data but may miss non-linear structure — use t-SNE or UMAP if you need to visualise non-linear clusters.`,
      `Kernel PCA: applies PCA in a high-dimensional feature space implicitly defined by a kernel function k(x, x') = φ(x)·φ(x'). Using an RBF kernel, for example, maps data to an infinite-dimensional feature space and finds non-linear principal components. This allows PCA to unroll non-linear structures like Swiss rolls. The limitation: kernel PCA is O(n²) in both computation and memory (the kernel matrix is n×n), making it impractical for n > 10,000.`,
      `Incremental PCA and Randomised SVD: standard PCA requires the full data in memory to compute the covariance matrix. Incremental PCA processes data in mini-batches and updates the principal components estimate. Randomised SVD (sklearn TruncatedSVD) approximates the top k components directly using random projections, achieving O(nkd) instead of O(nd²) — dramatically faster for large n and small k. For datasets with millions of rows, randomised SVD is the practical implementation.`,
      `When PCA fails as a feature extractor: (1) When the task-relevant variation has low total variance — PCA discards low-variance components, which may contain class-discriminative signal. In face recognition, the differences between individuals are in directions of low variance relative to illumination/pose changes. Use LDA (Linear Discriminant Analysis) instead. (2) When the relationship between features and target is non-linear — PCA components are linear combinations of inputs. (3) When features have different semantic types (continuous and categorical mixed) — PCA assumes continuous features; use appropriate preprocessing or switch to methods that handle mixed types.`,
    ],
    checkQuestions: [
      {
        q: `You run PCA on a dataset with 100 features. The first component captures 85% of variance and the second captures 8%. How do you decide how many components to keep for a downstream classifier?`,
        a: `The 85% in one component suggests the data has one dominant direction of variance, and 8% for the second means most remaining variance is also captured quickly. Two steps: (1) Plot the cumulative explained variance curve — where does it flatten? If 95% is reached by component 5 and 99% by component 15, start with 15 components for the classifier. (2) Do not solely rely on variance thresholds — the direction explaining 85% of variance may be an irrelevant confound (like batch effect in genomics, or lighting variation in images). Run the classifier with components 1-15, then try excluding the dominant component 1 and see if accuracy improves. If it does, the first component was a confound, not a signal. For important applications, cross-validate at several component counts (5, 10, 15, 20, all 100) and pick based on validation performance, not explained variance alone.`,
      },
      {
        q: `A colleague skips standardisation before PCA on a dataset with features including income (range $20k-$500k), age (18-80), and binary flags (0 or 1). What goes wrong?`,
        a: `Without standardisation, PCA is dominated by the income feature because it has variance ~(240,000)² — roughly 10 billion times larger than the variance of binary flags (~0.25) and thousands of times larger than age variance. The first principal component will be almost entirely the income direction. All the information in age and binary flags will be compressed into later components that the analyst is likely to discard. The model built on these components will effectively be modelling only income, not the multivariate structure of the data. Fix: StandardScaler before PCA converts each feature to zero mean and unit variance, so income, age, and binary flags all contribute equally to the covariance matrix. The principal components then reflect the correlation structure, not the raw scale structure.`,
      },
      {
        q: `You apply PCA to reduce 1,000-dimensional text embeddings to 50 dimensions before running k-means. Cluster quality is poor. What might PCA have discarded?`,
        a: `PCA discards the directions of lowest variance. In high-dimensional text embeddings (e.g., sentence-BERT), the highest-variance directions often capture global semantic properties like document length, formality, or language style — these are not necessarily what distinguishes meaningful topic clusters. The cluster-discriminative signal (the directions along which document topics differ) may have lower absolute variance than those global properties. PCA discards those dimensions. The fix: (1) Try UMAP instead of PCA for dimensionality reduction — UMAP preserves local neighbourhood structure, which is more relevant for clustering than global variance directions. (2) Use supervised PCA or Linear Discriminant Analysis if you have some labels. (3) Try clustering in the full 1,000-dimensional space with cosine distance (which is appropriate for text embeddings and ignores magnitude) rather than Euclidean distance.`,
      },
      {
        q: `Two datasets have the same dimensions and PCA explained-variance ratios. Are they similar datasets? What would you additionally check?`,
        a: `No — explained variance ratios only tell you about the eigenvalue spectrum shape, not about the actual content or structure of the data. Two datasets can have identical eigenvalue spectra with entirely different data distributions. Additional checks: (1) Loadings (eigenvectors): which original features contribute to each principal component? If both datasets load income heavily on PC1, they share that structure; if different features dominate, the variance structure is fundamentally different. (2) Scatterplots of PC1 vs PC2: the geometric shape of the projection (linear cloud, L-shape, clusters) tells you about data structure. (3) Reconstruction error distribution: the spread and skewness of reconstruction errors tells you whether the low-d approximation is uniformly good or fails on specific subgroups. (4) Correlation between original features and principal components: the correlation matrix reveals which features are moving together, not just which directions have high variance.`,
      },
    ],
    takeaway: `The key insight is that PCA finds directions of maximum variance, not directions of maximum relevance to your task — which means the components it discards may contain the class-discriminative signal you actually need, and variance explained is not a reliable proxy for information preserved for a specific downstream objective.`,
    interactiveId: 'pca_viz',
  },
  {
    id: 'tsne_umap',
    title: 't-SNE & UMAP',
    subtitle: 'Perplexity, why t-SNE clusters mislead, UMAP vs t-SNE tradeoffs',
    difficulty: 'advanced',
    estimatedMin: 40,
    tags: ['t-SNE', 'UMAP', 'visualisation', 'manifold'],
    summary: `t-SNE and UMAP are visualisation tools — not feature extraction tools. This distinction is frequently ignored, with costly consequences. t-SNE (t-distributed Stochastic Neighbour Embedding) converts Euclidean distances in high-dimensional space into pairwise similarity probabilities using a Gaussian kernel, then places points in 2D such that a t-distribution similarity is as close as possible to the high-d similarity — minimising KL divergence between the two distributions. The t-distribution's heavy tails prevent the crowding problem where points collapse to the centre. UMAP is mathematically grounded in Riemannian geometry and fuzzy simplicial sets: it models data as a weighted graph of approximate nearest neighbours in high dimensions and optimises a low-d embedding that preserves that graph structure. UMAP is 5-100x faster than t-SNE, scales to millions of points, and better preserves global structure — inter-cluster distances in UMAP have approximate meaning, while in t-SNE they are entirely arbitrary. Both methods are non-deterministic and sensitive to their parameters, and both require careful interpretation: apparent clusters in a 2D plot can be artefacts of the algorithm, not real structure in the data.`,
    keyPoints: [
      `t-SNE objective: for each pair (i,j) in high dimensions, compute similarity p_{ij} ∝ exp(−‖x_i − x_j‖² / 2σ_i²) where σ_i is set by perplexity. In 2D, compute q_{ij} ∝ (1 + ‖y_i − y_j‖²)⁻¹ — a t-distribution with one degree of freedom. Minimise KL(P || Q) = sum_{ij} p_{ij} log(p_{ij}/q_{ij}) via gradient descent. The t-distribution's heavier tails allow distant points in 2D to remain moderately similar — preventing the crowding effect where all high-d points collapse to a small 2D region. The cost is that the t-distribution exaggerates inter-cluster distances.`,
      `Perplexity in t-SNE: perplexity ≈ effective number of neighbours each point considers. Low perplexity (5-10): σ_i is small, only very nearby points are considered similar — tight local clusters form, global structure is lost, and isolated sub-clusters may appear that are not real. High perplexity (100-500): σ_i is large, more global structure is captured at the cost of local detail. Run t-SNE at multiple perplexity values (5, 30, 100) — if the same structure appears consistently, it is likely real. If different perplexity values produce entirely different structures, the data may not have stable cluster structure.`,
      `What t-SNE distances do NOT mean: (1) Distance between clusters in the 2D plot is meaningless — t-SNE's t-distribution specifically distorts inter-cluster distances to prevent crowding, so cluster A appearing far from cluster B tells you nothing about their actual distance in high-d space. (2) Cluster size in 2D is meaningless — t-SNE inflates small dense clusters and compresses large sparse ones. A tiny dot-cluster and a large spread-out region may be the same effective size in high dimensions. (3) A single t-SNE run is unreliable — the optimisation is non-convex and different random initialisations produce different layouts.`,
      `UMAP's neighbourhood graph: UMAP constructs a k-nearest-neighbour graph of the data in high-d space, weighted by approximate geodesic distance on the data manifold. It then optimises a 2D layout that preserves this graph structure using a cross-entropy loss between high-d and low-d fuzzy set memberships. Unlike t-SNE, UMAP preserves approximate global topology: if cluster A is between clusters B and C in the neighbourhood graph, it will tend to appear between B and C in the 2D layout. This makes UMAP more informative for understanding data geometry.`,
      `UMAP parameters: n_neighbors (like perplexity — higher values capture more global structure, lower values capture more local detail, typical range 5-100), min_dist (minimum distance between embedded points — small values produce tight clusters, large values spread points out, range 0.0-1.0), and metric (default euclidean; for text embeddings, cosine; for probability distributions, hellinger). Changing n_neighbors is the primary tuning dial: start with n_neighbors=15, then explore 5, 50, 100 to see how stable the structure is.`,
      `UMAP for downstream tasks vs t-SNE: UMAP embeddings can be used as features for k-means clustering (inter-cluster distances have some meaning), for classification (global topology preserved), or for anomaly detection. Running k-means on t-SNE 2D output is a common mistake — the distorted inter-cluster distances will mislead k-means into wrong groupings. If you want to cluster in low-d space, use UMAP (preserves global structure) or PCA (linear, interpretable) — not t-SNE.`,
      `Speed and scalability: standard t-SNE is O(n²) — intractable for n > 10,000. Barnes-Hut t-SNE reduces this to O(n log n) by approximating repulsive forces, making n ~ 100,000 feasible. UMAP is O(n log n) by default and in practice runs 5-100x faster than Barnes-Hut t-SNE. For n > 100,000, UMAP with approximate nearest neighbours (using HNSW or Annoy) is the practical choice. For n > 1,000,000, dimensionality reduction to PCA-50 first, then UMAP is standard.`,
      `Interpretation pitfalls: (1) Apparent clusters in t-SNE/UMAP do not confirm that real clusters exist — run a clustering algorithm in the original space and compare. (2) Non-deterministic results: run 3-5 times and check whether the same structures appear. (3) UMAP can preserve artefact structure from preprocessing — normalisation, batch effects, or PCA initialisation can all create structure in the UMAP embedding that reflects the preprocessing, not the data. (4) Both methods emphasise local structure, so two points that appear adjacent in the plot may be very different globally. Always validate visual insights with quantitative metrics.`,
    ],
    checkQuestions: [
      {
        q: `You run t-SNE with perplexity=5 and see 50 tiny, tight clusters. You run with perplexity=100 and see 3 blobs. Which represents the "true" structure?`,
        a: `Neither run alone is definitive. Perplexity=5 shows fine-grained local groupings: with only 5 effective neighbours, each point is compared only to its immediate vicinity, producing many small tight groups that reflect local density variation. Perplexity=100 shows coarse global groupings: the algorithm is now averaging over 100 neighbours, blurring local detail into broad regions. The truth likely involves both scales of structure — 3 coarse groups, each internally subdivided. To assess: (1) Run at intermediate perplexities (15, 30, 50) and look for structures that appear consistently across runs. (2) Use UMAP (more robust to parameter choice, better global preservation) and compare. (3) Run k-means in the original high-dimensional space for k=3, k=10, k=50 and check whether those clusterings align with the t-SNE structure at the corresponding perplexity. The structures that appear in both the parametric clustering and t-SNE are most likely real.`,
      },
      {
        q: `A team visualises 50,000 single-cell RNA-seq measurements with t-SNE, sees 12 distinct clusters, and runs k-means on the 2D t-SNE embedding. Explain two problems with this approach.`,
        a: `Two fundamental problems. First, k-means on t-SNE embeddings uses Euclidean distances in 2D t-SNE space. But t-SNE distorts inter-cluster distances — the 2D distances between cluster centroids bear no consistent relationship to distances in the 50,000-dimensional gene expression space. K-means will partition the 2D space by proximity, which may correctly capture visually obvious clusters but will misgroup points at cluster boundaries where t-SNE's distortion is largest. Correct approach: run k-means (or Leiden algorithm, standard in single-cell analysis) in the original space or on PCA-reduced dimensions (30-50 components), then use t-SNE only to visualise the result. Second, the 12 apparent t-SNE clusters may be an artefact of the perplexity setting and random initialisation — different perplexity values may produce 8 or 20 clusters. Validate that the same 12 groups appear consistently across multiple t-SNE runs and perplexity values before treating them as biological cell types.`,
      },
      {
        q: `Your t-SNE plot shows two large clusters that are completely separated. Does this mean these two groups are very different from each other in the original space?`,
        a: `No. t-SNE's repulsive forces push clusters apart in 2D regardless of how far apart they are in the original space — t-SNE maximises the visual separation between any groups that are locally separated. Two groups that differ by only one gene (and are thus close in high-dimensional space) can appear as completely separate clusters in t-SNE if they have slightly different local density. Conversely, two groups that are moderately different in high-dimensional space can appear adjacent if the t-SNE optimisation happened to place them close. To determine whether two t-SNE clusters are genuinely far apart in the original space: compute their actual distance in the original space (e.g., centroid distance, average pairwise distance), run UMAP and check if the two groups are still separated (UMAP preserves global distances better), or use statistical tests (e.g., MANOVA) on original features to confirm the groups are genuinely distinct.`,
      },
      {
        q: `You need to compress 768-dimensional BERT embeddings to 2D for visualisation and downstream k-means clustering. What is your pipeline and why?`,
        a: `Two-step pipeline, not one. Step 1: dimensionality reduction for the clustering step — apply PCA to reduce from 768 to 50 dimensions (retaining 90-95% of variance). This removes noise dimensions while preserving global linear structure in a form that k-means can use with meaningful Euclidean distances. Run k-means on the 50-d PCA space, not in 768-d (too slow, curse of dimensionality) and not in 2-d UMAP (distorted distances). Step 2: visualisation only — apply UMAP to the 50-d PCA space (not raw 768-d, for speed) with n_neighbors=15 and reduce to 2D. Colour the 2D UMAP plot by k-means cluster assignments from step 1. This way the clustering is based on a proper metric in a reasonable-dimensional space, and the visualisation shows whether the UMAP structure confirms those cluster boundaries. Using UMAP directly for clustering would produce suboptimal clusters because global distances are only approximately preserved, and using t-SNE for clustering is wrong because inter-cluster distances are meaningless.`,
      },
    ],
    takeaway: `The key insight is that t-SNE and UMAP are visualisation tools that distort data geometry to reveal neighbourhood structure — t-SNE's inter-cluster distances are completely meaningless while UMAP's are approximate — which means running k-means on their 2D output is always wrong, and any apparent cluster in a 2D plot requires validation in the original high-dimensional space.`,
    interactiveId: 'tsne_viz',
  },
  {
    id: 'autoencoders_dim_reduction',
    title: 'Autoencoders for Dimensionality Reduction',
    subtitle: 'Encoder-decoder mechanics, bottleneck, VAE, anomaly detection via reconstruction error',
    difficulty: 'advanced',
    estimatedMin: 38,
    tags: ['autoencoder', 'VAE', 'dimensionality reduction', 'anomaly detection'],
    summary: `Autoencoders are the non-linear generalisation of PCA — they learn a compressed representation by being forced to reconstruct their own input through a narrow bottleneck. The encoder maps input x to a low-dimensional latent vector z; the decoder maps z back to a reconstruction x̂; training minimises reconstruction loss (MSE for continuous data, cross-entropy for binary). Because the encoder and decoder are non-linear (multiple layers with activations), autoencoders can capture curved manifold structure that PCA cannot. The Variational Autoencoder (VAE) regularises the latent space to be a smooth Gaussian distribution, enabling interpolation and generative sampling. For anomaly detection, autoencoders are trained on normal data only — anomalous inputs cannot be reconstructed well, producing high reconstruction error that serves as the anomaly score. The failure modes are overfitting (the bottleneck is too wide so the autoencoder memorises rather than compresses), representation collapse (the bottleneck is too narrow so important structure is lost), and the posterior collapse problem in VAEs where the decoder ignores the latent code.`,
    keyPoints: [
      `Autoencoder architecture: encoder f_θ: ℝ^d → ℝ^k (k << d) and decoder g_φ: ℝ^k → ℝ^d, trained to minimise reconstruction loss L(x, g_φ(f_θ(x))). For images, L is MSE or BCE on pixel values; for text, cross-entropy on token probabilities. The bottleneck forces the network to learn a compressed representation that captures the essential structure of x. Unlike PCA, the encoder and decoder can be non-linear (multiple layers with activation functions), so the bottleneck can capture curved manifolds.`,
      `Comparison to PCA: PCA finds the optimal linear projection in closed form (eigenvectors of the covariance matrix); a shallow autoencoder with linear activations and MSE loss converges to the same subspace as PCA. Adding non-linear activations makes the autoencoder equivalent to non-linear PCA — it finds a curved low-dimensional manifold fitting the data rather than a flat hyperplane. This is the key advantage: autoencoders can unroll non-linear structure (spirals, non-linear clusters) that PCA cannot.`,
      `Variational Autoencoder (VAE): instead of encoding x to a point z, the VAE encoder outputs a distribution — mean μ and log-variance log σ². Latent code is sampled: z = μ + σ · ε where ε ~ N(0,I) (the reparameterisation trick). Training minimises reconstruction loss + KL divergence between the posterior q(z|x) = N(μ, σ²) and the prior p(z) = N(0,I). The KL term forces the latent space to be compact and smooth — similar inputs should have similar latent distributions. This enables interpolation: a point between two latent vectors decodes to a meaningful intermediate reconstruction.`,
      `Anomaly detection with autoencoders: train the autoencoder exclusively on normal data. At inference time, compute reconstruction error per sample. Normal samples are reconstructed accurately (model has learned the patterns). Anomalous samples differ from anything the model has seen, so the decoder cannot reconstruct them well — reconstruction error is high. Threshold the reconstruction error to produce binary anomaly labels. The advantage over other anomaly methods: autoencoders can capture complex dependencies across all features (e.g., the correlation structure of 500 sensor readings simultaneously) that simpler methods cannot.`,
      `Choosing bottleneck dimension: too small — the autoencoder is an under-complete representation that forces information loss, useful for compression but may discard signal relevant to anomalies. Too large — the autoencoder can memorise the training data (especially with over-parameterised decoders), so even anomalies are reconstructed well, making the reconstruction error uninformative. The right size is problem-specific: start with the intrinsic dimensionality of the normal data (estimated by checking how many PCA components are needed for 95% reconstruction quality) and tune based on validation anomaly detection performance.`,
      `Denoising autoencoders: trained by corrupting the input with noise (masking, Gaussian noise, dropout) and training the network to reconstruct the clean input. This regularisation forces the autoencoder to learn which features are reliable signals vs noise, producing more robust representations than standard autoencoders. Denoising autoencoders are better at generalisation and anomaly detection in noisy environments. They are also the theoretical foundation for modern diffusion models.`,
      `Reconstruction error as anomaly score — failure modes: (1) If the training data contains anomalies (impure normal set), the autoencoder learns to reconstruct some anomalous patterns, reducing their reconstruction error. (2) If anomalies happen to lie on the learned manifold by coincidence, reconstruction error will be low even though they are anomalous. (3) High-dimensional reconstruction error can be dominated by high-variance feature dimensions that are not relevant to the anomaly type. Fix: normalise reconstruction error per feature dimension, or train a one-class classifier on the latent space rather than using raw reconstruction error.`,
      `Contrastive autoencoders and masked autoencoders: masked autoencoders (MAE, used in vision transformers) mask a large fraction of input patches (75%) and train the encoder on visible patches to reconstruct masked ones — forcing the model to learn global semantic structure rather than local pixel statistics. This is the basis of modern self-supervised pre-training. For anomaly detection, a model trained with high masking ratio is better at capturing semantic structure than pixel-level reconstruction, making it more sensitive to semantic anomalies (wrong content type) and less sensitive to superficial textural anomalies (noise, blur).`,
    ],
    checkQuestions: [
      {
        q: `You train an autoencoder for anomaly detection and find that reconstruction error is high for both normal and anomalous samples. What might be wrong?`,
        a: `High reconstruction error on normal training samples means the autoencoder failed to learn the normal data distribution — it is underfitting. Three root causes: (1) Bottleneck too small — the compression is too aggressive and the decoder cannot recover the input from the compressed code. Increase bottleneck dimension. (2) Model too small or underfit — increase network depth/width or train longer. (3) Feature preprocessing — if features have very different scales, MSE reconstruction loss is dominated by high-variance features. Apply standardisation before training. Separately, if reconstruction error is low for anomalies (rather than high), the bottleneck is too large — the autoencoder is memorising rather than generalising, so it reconstructs everything well. The right operating regime is: normal samples have low reconstruction error, anomalies have high. Tune bottleneck size to separate these two distributions.`,
      },
      {
        q: `What is the reparameterisation trick in a VAE and why is it necessary?`,
        a: `In a VAE, the encoder outputs the parameters (μ, σ) of a Gaussian distribution and the latent code z is sampled from it: z ~ N(μ, σ²). The problem is that sampling is a non-differentiable operation — you cannot backpropagate gradients through a stochastic node. The reparameterisation trick factorises the randomness out: instead of sampling z ~ N(μ, σ²) directly, write z = μ + σ · ε where ε ~ N(0,1) is a fixed noise sample drawn outside the computation graph. Now μ and σ are deterministic functions of the input (differentiable paths), and ε is external randomness not part of the parameters. Gradients of the loss with respect to μ and σ flow cleanly through z = μ + σ·ε. Without the reparameterisation trick, you cannot train the encoder by gradient descent, which is why the VAE objective requires it. The same trick applies to any distribution with a location-scale parametrisation (Laplace, logistic).`,
      },
      {
        q: `An autoencoder trained on manufacturing sensor readings is being used for anomaly detection. A maintenance engineer reports that known defective sensors are not flagged. What could cause this and how do you debug?`,
        a: `Known defective sensors not flagged means their reconstruction error is low — the autoencoder is reconstructing their patterns accurately. Four mechanisms: (1) Training data contamination: if defective sensors were present in the training data labelled as normal, the autoencoder has learned to reconstruct that defective pattern. Audit training data purity. (2) Bottleneck too large: the autoencoder is memorising and can reconstruct anything, normal or not. Reduce bottleneck dimension or add more regularisation. (3) Defect type is low-amplitude: if the defect manifests as a small deviation in a few sensors while other sensors are at normal values, reconstruction loss is dominated by the normal sensors and the defective signal is a small fraction of total MSE. Normalise per sensor and weight reconstruction error by expected sensor variance. (4) Wrong reconstruction metric: if the defect is a timing/phase anomaly rather than amplitude, MSE may miss it — try frequency-domain features or sequence-aware anomaly scoring.`,
      },
      {
        q: `Compare using a VAE latent space versus using PCA components for anomaly detection. What are the trade-offs?`,
        a: `Both methods detect anomalies by measuring how far a sample is from the learned normal representation. PCA anomaly detection computes the reconstruction error in PCA space (distance from the PCA hyperplane) or Mahalanobis distance in PCA-component space. It is fast, interpretable (each component is a linear combination of original features), and has a closed-form solution — no training instability. It works well when normal data is approximately Gaussian and linear-manifold structured. VAE anomaly detection uses the ELBO loss or reconstruction error. The VAE captures non-linear manifold structure that PCA cannot — if normal sensor data lies on a non-linear curve, PCA underestimates the "normal distance" for data on the curve, while the VAE learns the curve. However, VAEs require careful training (mode collapse, posterior collapse), hyperparameter tuning (bottleneck size, KL weight), and are harder to interpret. Use PCA for quick anomaly baselines, linear data, or when interpretability is needed. Use autoencoders/VAEs when normal data has complex non-linear dependencies (e.g., correlated time-series, images, sequential patterns).`,
      },
    ],
    takeaway: `The key insight is that autoencoders learn to reconstruct normal patterns through a compressed bottleneck, which means anything the model has not learned to reconstruct — anomalies, out-of-distribution inputs — produces high reconstruction error that serves as the anomaly score, but the bottleneck must be sized correctly or the model either memorises everything or loses the signal.`,
  },
  {
    id: 'gmm',
    title: 'Gaussian Mixture Models',
    subtitle: 'EM algorithm, soft assignments, model selection with BIC/AIC',
    difficulty: 'advanced',
    estimatedMin: 38,
    tags: ['GMM', 'EM algorithm', 'probabilistic clustering', 'soft assignment'],
    summary: `GMM is what you reach for when k-means is not flexible enough. It represents the data distribution as a weighted sum of K Gaussian components: p(x) = sum_k π_k N(x | μ_k, Σ_k). Unlike k-means, which assigns each point to exactly one cluster, GMM uses soft assignments — each point has a probability of belonging to each component (the responsibility r_{ik}). It is a generative model, so fit quality is measured by log-likelihood rather than sum of squares, enabling principled model selection with BIC/AIC. GMM is fit by the Expectation-Maximisation (EM) algorithm, which alternates between computing responsibilities given current parameters (E-step) and updating parameters to maximise expected log-likelihood given responsibilities (M-step). EM guarantees non-decreasing log-likelihood at each iteration but only converges to a local optimum, so multiple restarts are required. GMM generalises k-means in three ways: soft assignments, elliptical clusters (full covariance matrix allows any shape), and a principled probability model. The failure modes are the curse of dimensionality for full covariance matrices, sensitivity to initialisation, and the need to choose K.`,
    keyPoints: [
      `GMM generative model: for each data point, (1) sample a component k with probability π_k (mixing weight), (2) sample x from N(μ_k, Σ_k). Parameters to fit: {π_k, μ_k, Σ_k} for k=1,...,K. The log-likelihood of the data is L = sum_i log sum_k π_k N(x_i | μ_k, Σ_k). Direct maximisation of this is intractable because of the log-sum. EM solves this by introducing the latent variable z_i (which component generated x_i) and optimising a lower bound on the log-likelihood.`,
      `E-step (Expectation): compute the responsibility r_{ik} = P(z_i=k | x_i, θ) using Bayes' rule: r_{ik} = π_k N(x_i | μ_k, Σ_k) / sum_{j} π_j N(x_i | μ_j, Σ_j). Each r_{ik} ∈ [0,1] and sum_k r_{ik} = 1. This is the "soft assignment" — point i belongs to component k with probability r_{ik}. In k-means, this would be a hard 0/1 assignment to the nearest centroid.`,
      `M-step (Maximisation): update parameters using the responsibilities as weighted counts. N_k = sum_i r_{ik} (effective number of points in component k). μ_k = (sum_i r_{ik} x_i) / N_k (responsibility-weighted mean). Σ_k = (sum_i r_{ik} (x_i − μ_k)(x_i − μ_k)^T) / N_k (responsibility-weighted covariance). π_k = N_k / n (updated mixing weight). These are closed-form updates — no gradient needed. EM guarantees log-likelihood is non-decreasing, but only to a local optimum.`,
      `GMM vs k-means: k-means is the degenerate case of GMM where Σ_k = σ²I (identical isotropic covariance for all k) and σ→0 (hard assignments). Relaxing either assumption generalises k-means: allow different Σ_k to get elliptical clusters. Allow soft assignments to get probabilistic membership. GMM with spherical covariance (Σ_k = σ_k²I) gives soft k-means. GMM with diagonal covariance is faster and less prone to overfitting than full covariance. Full covariance GMM can fit any elliptical cluster orientation.`,
      `Covariance types and trade-offs: full covariance — every Σ_k is a d×d matrix, requiring d(d+1)/2 parameters per component. Captures any elliptical shape but is prone to overfitting in high dimensions (need many more samples than parameters). Diagonal covariance — Σ_k is diagonal, only d parameters per component. Assumes features are conditionally independent given component — much more robust. Spherical covariance — Σ_k = σ_k² I, one parameter per component. Closest to k-means. Tied covariance — all components share the same Σ — reduces total parameters. The right type depends on data dimension, sample size, and whether you expect correlated features within clusters.`,
      `Model selection — BIC and AIC: BIC = −2 log L + p log n (p = number of parameters). AIC = −2 log L + 2p. Both balance fit quality (log-likelihood) against model complexity (parameter count). GMM with more components always fits better (higher log-likelihood) but more parameters increases BIC/AIC penalty. Plot BIC or AIC vs K — find the K where the curve elbows or reaches minimum. BIC penalises parameters more strongly (penalty per parameter is log n vs 2 for AIC), so BIC prefers simpler models, which helps avoid overfitting. For model selection in clustering, BIC is the standard.`,
      `Initialisation sensitivity and mitigation: EM converges to a local optimum that depends heavily on initialisation. Common initialisations: k-means++ (run k-means, use centroids as initial μ_k), random restart (run EM multiple times with different initialisations, keep the run with highest final log-likelihood), and supervised initialisation using domain knowledge. GMM with full covariance is more sensitive to initialisation than diagonal covariance because the covariance matrices can take degenerate values. Degenerate solutions — a component with all points assigned, or a component that collapses to a single point with zero variance — are signs of bad initialisation or wrong K.`,
      `When to use GMM over k-means: (1) When you need probabilistic cluster membership — GMM gives P(cluster k | point i), which is useful for downstream decisions where uncertainty matters (e.g., borderline customers in a segmentation). (2) When clusters have different shapes, orientations, or densities — full or diagonal covariance GMM handles this; k-means cannot. (3) When you need principled model selection (BIC/AIC) to choose K. (4) When the data was genuinely generated by a mixture process (demographic groups, different products). Use k-means when interpretability and speed matter and the isotropic-cluster assumption is approximately valid.`,
    ],
    checkQuestions: [
      {
        q: `EM for GMM is guaranteed to not decrease log-likelihood at each step, yet it often converges to a poor local optimum. How is this possible?`,
        a: `The guarantee that EM does not decrease log-likelihood is a statement about local monotonicity, not global optimality. The log-likelihood landscape for a GMM with K components is multimodal — there are many local maxima with different log-likelihood values. EM ascends the log-likelihood hill from wherever it starts, reaching the nearest local maximum. Starting from different initial parameters produces convergence to different local maxima. The E-step computes the exact posterior over component assignments given the current parameters; the M-step finds the exact maximum of the expected complete-data log-likelihood (Q function). Both steps are optimal given the current position, but the sequence of steps is hill-climbing on the full log-likelihood, not finding the global peak. Mitigation: run EM from multiple starting points (random restarts or k-means++ initialisation) and retain the solution with the highest final log-likelihood. This increases the chance of finding the global or a high-quality local optimum.`,
      },
      {
        q: `You fit a GMM with K=5 and diagonal covariance on 10,000 points in 50 dimensions. BIC keeps decreasing as you increase K from 1 to 20. What does this suggest and what do you do?`,
        a: `BIC still decreasing at K=20 means the data has more distinct density modes than 20, or the Gaussian assumption is wrong so the model needs many components to approximate the true distribution. Possibilities: (1) The true number of clusters is genuinely large — perhaps there are 30-50 natural groups. Continue increasing K and look for a BIC plateau or minimum. (2) The data contains outliers or heavy-tailed distributions that require many Gaussian components to approximate a single non-Gaussian cluster. Preprocess to remove outliers and re-fit. (3) The feature space has high redundancy and spurious correlations — dimensionality reduction (PCA to 10-15 components, retaining 90% variance) before fitting the GMM may reveal a cleaner structure with a lower optimal K. (4) GMM is simply the wrong model for this data — consider k-means (simpler), HDBSCAN (no distributional assumption), or non-parametric density estimation.`,
      },
      {
        q: `A customer segmentation GMM has a component with mixture weight π_k = 0.001 that collapses to a tiny covariance (singular matrix). What happened and how do you fix it?`,
        a: `This is a degenerate solution: the component has latched onto one or a few points and fitted an extremely tight Gaussian around them — the covariance matrix collapses toward zero and the log-likelihood of those points under this component goes to infinity, which makes the EM update artificially inflate that component's contribution. It is a pathological local maximum. Root cause: bad initialisation placed a component centroid near an isolated point, and EM drove the covariance to zero to maximise their contribution. Fixes: (1) Add regularisation — a minimum eigenvalue constraint or a small diagonal offset to each covariance (regularization_covar in sklearn's BayesianGaussianMixture). (2) Use Bayesian GMM which places a Dirichlet prior on mixture weights — components with very small weight get penalised, preventing collapse. (3) Re-initialise with k-means++ and run again. (4) Reduce K — the degenerate component may indicate K is too large for the data.`,
      },
      {
        q: `How do you use GMM for density estimation and anomaly detection? What determines the anomaly threshold?`,
        a: `GMM for density estimation: after fitting, p(x) = sum_k π_k N(x | μ_k, Σ_k) gives the estimated density at any point x. High-density regions correspond to clusters of normal points; low-density regions are unusual. For anomaly detection: compute log p(x_i) for each sample. Samples with very low log-likelihood (in the tail of the density) are anomalies. Threshold selection: (1) Percentile-based — label the bottom τ% of training log-likelihoods as anomalies, where τ is the expected contamination rate from domain knowledge. (2) Validation-based — if some labelled anomalies are available, choose the threshold that maximises F1 or precision at a target recall on the validation set. (3) Decision-cost-based — choose the threshold at which the cost of FPs and FNs is balanced given their business costs. GMM density estimation is more interpretable than Isolation Forest (you can report which component an anomaly is far from) but makes stronger parametric assumptions (Gaussianity) that may not hold for all anomaly types.`,
      },
    ],
    takeaway: `The key insight is that GMM generalises k-means by giving each point a soft probability of belonging to each cluster and allowing clusters to be elliptical — the EM algorithm computes these assignments and updates cluster parameters iteratively, but converges to a local optimum, which means multiple restarts and principled model selection with BIC are required for reliable results.`,
    interactiveId: 'gmm_viz',
  },
  {
    id: 'anomaly_detection',
    title: 'Anomaly Detection',
    subtitle: 'Isolation Forest, one-class SVM, LOF, autoencoder-based, evaluation without labels',
    difficulty: 'intermediate',
    estimatedMin: 40,
    tags: ['anomaly detection', 'isolation forest', 'outlier', 'one-class'],
    summary: `Anomaly detection is fundamentally about choosing a definition of "unusual" — and different algorithms embed different definitions. Statistical methods define unusual as low probability under a fitted distribution. LOF defines it as low density relative to neighbours. Isolation Forest defines it as easy to isolate with random splits. Autoencoders define it as hard to reconstruct. Choosing the algorithm means choosing which definition matches your problem's anomalies. Because labelled anomalies are rare, expensive to collect, and may not cover future anomaly types, detection is typically unsupervised. Evaluation is correspondingly hard: proxy metrics include expert review of flagged samples, downstream impact (does flagging these points improve a business process), and comparison of anomaly score distributions on labelled validation data when some labels are available.`,
    keyPoints: [
      `Isolation Forest: anomalies are "few and different" — they can be isolated by fewer random partitions than normal points. The algorithm builds many random trees: at each node, select a random feature and a random split value between min and max. The depth at which point i is first isolated (alone in a leaf) is the isolation depth. Anomaly score: mean depth across all trees, normalised by the expected depth of a random sample. Short isolation depth = anomaly. Training O(n log n), scoring O(n log n). Handles high dimensions better than LOF or distance-based methods, with no assumption of Gaussianity.`,
      `Local Outlier Factor (LOF): for each point, estimate its local density as the inverse of the mean reachability distance to its k-nearest neighbours. LOF(i) = mean(local_density(neighbours)) / local_density(i). LOF ≈ 1: similar density to neighbours — normal. LOF >> 1: much lower density than neighbours — outlier in a local sense. LOF handles datasets where different regions have different density because it is locally normalised. A point in a low-density region surrounded by other low-density points gets LOF ≈ 1 (not an outlier in its context). The failure mode: O(n²) computation time makes LOF infeasible for n > 100,000.`,
      `One-class SVM: learns a tight hypersphere (or hyperplane in kernel space) around the training data. Points outside the boundary are anomalies. Parameter ν controls the fraction of training points allowed outside the boundary — effectively the expected contamination rate. With RBF kernel, one-class SVM can fit complex non-linear boundaries around normal data. Failure modes: (1) Very sensitive to feature scaling — always standardise first. (2) O(n²) to O(n³) training time, slow for large datasets. (3) Sensitive to ν: set it to the expected anomaly fraction from domain knowledge. (4) Does not produce an anomaly score, only a binary decision — less useful when you need to rank anomalies by severity.`,
      `Statistical methods — when to use: if normal data is approximately Gaussian in low dimensions (≤20 features), fit a multivariate Gaussian and use Mahalanobis distance as the anomaly score. Points beyond a Mahalanobis distance threshold (e.g., χ² critical value at 99%) are anomalies. For multimodal normal data, use a Gaussian Mixture Model (compute log p(x) as anomaly score). Statistical methods are interpretable — you can explain exactly which feature combinations cause a high anomaly score. They fail when the Gaussian assumption is badly wrong (heavy tails, discrete distributions, strong non-linearity).`,
      `Evaluation challenge without labels: most production anomaly detection has no reliable ground truth. Strategies: (1) Precision/recall on a small labelled set — if domain experts can label 200-500 examples, compute F1 at your operating threshold. (2) Expert review of top-k anomalies — ask domain experts whether the top 50 flagged samples are genuine anomalies. Precision@50 is a practical metric. (3) Downstream business metric — does blocking flagged transactions reduce fraud losses? Does alerting on flagged sensor readings reduce unplanned downtime? (4) Injection testing — inject synthetic anomalies with known properties into the stream and measure recall. The synthetic anomalies must match the expected distribution of real anomalies for this to be valid.`,
      `Semi-supervised anomaly detection: when you have labelled normal examples but few labelled anomalies, train on normals only (PatchCore, Deep SVDD, SVDD, OC-NN). The model learns a compact representation of normal patterns; anomalies are detected as points far from this compact normal representation in latent space. This is better than fully unsupervised methods because you leverage the definition of "normal" from your labelled data rather than inferring it from the data distribution, which may be contaminated with unlabelled anomalies.`,
      `Anomaly detection in time series: point anomalies (a single timestamp is anomalous), contextual anomalies (a value is anomalous given its context — a temperature reading that is normal in summer is anomalous in winter), and collective anomalies (a subsequence is anomalous even if no single point is unusual — an unusual pattern over 10 steps). Isolation Forest misses contextual and collective anomalies because it looks at each point independently. LSTM or transformer autoencoders that reconstruct sequences are more appropriate — the reconstruction error for a sequence captures the temporal context.`,
      `Contamination parameter and calibration: all anomaly detection methods require setting a contamination fraction τ (the expected fraction of anomalies in the dataset). Isolation Forest uses τ to set the decision threshold on anomaly score. Setting τ too high → too many false positives. Too low → misses anomalies. Calibration: use domain knowledge (known fraud rate, expected defect rate) to set τ. Alternatively, treat it as a hyperparameter and evaluate precision@k or downstream impact at different τ values. Contamination calibration is the most impactful single hyperparameter decision in anomaly detection deployment.`,
    ],
    checkQuestions: [
      {
        q: `You are detecting network intrusion anomalies from 1 million log events per day with 200 features. Which method do you choose and why?`,
        a: `Isolation Forest is the primary choice for this scale and dimensionality. At 1M events/day and 200 features: Isolation Forest trains in O(n log n) ≈ 20M operations, which takes seconds. Scoring new events is O(log n) per sample — real-time feasible. Handles high dimensions better than LOF (which is O(n²) — 10^12 operations for 1M events) and one-class SVM (O(n²) to O(n³)). Does not require Gaussian assumptions. Setting: contamination=0.01 (expected 1% anomaly rate from domain knowledge), n_estimators=100, max_samples=256 (subsampling per tree speeds training without much accuracy loss). Alternative if temporal pattern matters: LSTM autoencoder trained on normal traffic sequences — captures contextual anomalies (unusual sequences even if each packet looks normal). The autoencoder approach handles collective anomalies that Isolation Forest misses by treating each event independently.`,
      },
      {
        q: `LOF detects an anomaly in a dataset with two clusters of very different sizes and densities. A point in the smaller, denser cluster gets LOF=0.9 (classified as normal). A point at the edge of the larger, sparser cluster gets LOF=1.8 (classified as anomaly). Is this correct behaviour?`,
        a: `This is correct and intentional LOF behaviour. LOF is locally normalised — it compares each point's density to its neighbours' densities. The point in the dense cluster has LOF=0.9 because its local density (high) is similar to its neighbours' local densities (also high) — it is a normal member of its local neighbourhood. The point at the edge of the sparse cluster has LOF=1.8 because its local density is lower than its k-nearest neighbours' densities (which are deeper in the dense core of the cluster). From its local perspective, it is genuinely sparser than its surroundings — an outlier in its local context. This is the correct semantic: a point that is unusual relative to its neighbours is anomalous, regardless of global density. The alternative — flagging the entire dense cluster as outliers because the global density is higher there — is the global outlier problem, which LOF is specifically designed to avoid.`,
      },
      {
        q: `Your Isolation Forest model is flagging 15% of transactions as anomalies, but the fraud team says the actual fraud rate is 0.5%. What do you change?`,
        a: `The contamination parameter is set too high — it is telling Isolation Forest to treat 15% of the training data as anomalous, which sets the decision threshold at the 15th percentile of anomaly scores. At contamination=0.15, anything with score below the 85th percentile of training scores is flagged. Set contamination=0.005 (0.5% fraud rate) and recompute the threshold. The threshold will shift to the 99.5th percentile of training anomaly scores — only the most isolated points will be flagged. If the fraud team's 0.5% estimate is uncertain, sweep contamination from 0.001 to 0.01 and plot precision/recall against the small labelled set of confirmed fraud cases. Set contamination at the value where precision and recall balance appropriately for the fraud team's operational capacity (how many alerts they can review per day). The contamination parameter is the most important business-level hyperparameter in anomaly detection.`,
      },
      {
        q: `You have 1,000 labelled normal samples and 0 labelled anomalies. How do you build and evaluate an anomaly detection model?`,
        a: `With only normal labels, use a semi-supervised approach: train Isolation Forest or an autoencoder exclusively on the 1,000 normal samples, treating the entire training set as the normal distribution to model. Evaluation strategy: (1) Holdout-based anomaly injection — hold out 200 normal samples during training. Generate synthetic anomalies by: sampling uniform random points from the feature bounding box (definitely anomalous), or by applying large random perturbations to held-out normals (likely anomalous). Evaluate precision/recall on this synthetic test set. This validates that the model correctly identifies points outside the normal distribution but does not guarantee it flags real-world anomaly types. (2) Expert review — collect 50-100 samples that the model scores as most anomalous from a fresh unlabelled dataset. Have domain experts classify them as anomalous or not. Precision@50 gives a practical estimate of real-world precision. (3) Before deployment, create a labelled anomaly set via adversarial review or by collecting confirmed incidents over the first month of production use. Retrain with this data to improve recall on known anomaly types.`,
      },
    ],
    takeaway: `The key insight is that different anomaly detection algorithms embed different definitions of "unusual" — Isolation Forest says anomalies are easy to isolate, LOF says they have lower density than their neighbours, autoencoders say they are hard to reconstruct — and choosing the right algorithm means choosing which definition matches the structure of the anomalies you expect to see.`,
    interactiveId: 'anomaly_detection_viz',
  },
  {
    id: 'topic_modeling',
    title: 'Topic Modeling',
    subtitle: 'LDA mechanics, NMF, choosing K, coherence vs perplexity, production limits',
    difficulty: 'intermediate',
    estimatedMin: 38,
    tags: ['topic modeling', 'LDA', 'NMF', 'NLP'],
    summary: `Topic modeling discovers latent themes in a corpus without supervision, useful when you have thousands of documents and want to understand what they are about without reading them all. LDA (Latent Dirichlet Allocation) is a generative probabilistic model: each document is a mixture of topics, and each topic is a distribution over words. The Dirichlet priors on both document-topic and topic-word distributions enforce sparsity — most documents are primarily about a few topics, and most topics have a few characteristic words. NMF (Non-negative Matrix Factorisation) takes a linear algebra approach: decompose the term-document matrix V ≈ W H where non-negativity constraints force parts-based, additive representations that tend to be interpretable. Choosing the number of topics K is the central challenge: perplexity on held-out data measures fit but does not track human interpretability. Coherence scores (NPMI, CV) measure whether top words in a topic co-occur frequently in real text, which correlates better with human judgement. Neither metric is definitive — the true test is whether domain experts find the topics meaningful. In production, topic models face the additional challenges of topic instability (different runs produce different topics) and concept drift (new topics emerge over time that the model was not trained on).`,
    keyPoints: [
      `LDA generative process: for each document d, draw topic mixture θ_d ~ Dirichlet(α). For each word position in d: draw topic z ~ Categorical(θ_d), then draw word w ~ Categorical(φ_z) where φ_z ~ Dirichlet(β) is the topic-word distribution. The α and β hyperparameters control sparsity: small α means documents use few topics; small β means topics use few words. Inference (finding θ and φ given observed words) is intractable in closed form, requiring approximations — variational EM or collapsed Gibbs sampling.`,
      `NMF: decompose the term-document matrix V (D×V) as V ≈ W (D×K) × H (K×V) where W_{dk} is the weight of topic k in document d, and H_{kv} is the weight of word v in topic k. Non-negativity (W, H ≥ 0) enforces that topics are additive: each document is a sum of topics, each topic is a sum of words. NMF with Frobenius norm loss is equivalent to assuming Gaussian noise; NMF with KL divergence loss is equivalent to a Poisson model and closely related to LDA. NMF topics are often more coherent than LDA on short documents (tweets, product reviews) because TF-IDF already de-emphasises common words, and the parts-based representation naturally produces sparse, coherent word groups.`,
      `Perplexity vs coherence for choosing K: perplexity measures how well the model predicts held-out test words — lower perplexity = better fit. Adding more topics almost always decreases perplexity, making it unhelpful for choosing K unless you regularise heavily. Coherence scores — NPMI (Normalised Pointwise Mutual Information) between top-10 words of each topic, averaged across all topics — measure whether words the model groups together actually co-occur in real text. Coherence peaks at some K and then degrades as topics fragment into incoherent word combinations. Use coherence to choose K, not perplexity.`,
      `NPMI coherence: for a topic's top words w₁,...,wₙ, NPMI(w_i, w_j) = log[P(w_i, w_j)/P(w_i)P(w_j)] / −log P(w_i, w_j). Range [−1, 1]; NPMI=1 means words always co-occur, NPMI=0 means independent, NPMI=−1 means they never co-occur. Mean NPMI over all pairs within a topic = topic coherence. Mean across all topics = corpus coherence. Use an external reference corpus (Wikipedia) to compute co-occurrence frequencies — using the training corpus itself can inflate coherence for topics that the model has overfit to.`,
      `Preprocessing for topic modeling: (1) Lemmatisation — "running" → "run", "models" → "model" — reduces vocabulary size and consolidates related forms. (2) Stop word removal — common function words (the, a, is) appear in all topics and are uninformative. (3) Minimum document frequency — words appearing in fewer than 5 documents are rare and add noise without contributing to coherent topics. (4) Bigrams — "machine learning" is one concept, not two words. Detect frequent bigrams with Gensim's Phrases and add them as compound tokens. (5) TF-IDF weighting for NMF (de-emphasises very common words); raw counts for LDA (the Dirichlet model assumes raw word counts). Preprocessing quality is often the biggest determinant of topic quality.`,
      `Production limitations of topic models: (1) Topic instability — LDA with Gibbs sampling is non-deterministic; topics change order and content between runs. Fix: use a fixed random seed and deterministic initialisation; or stabilise by running 5 seeds and selecting the run with highest coherence. (2) No online assignment of exact same topics to new documents — fitting on a corpus snapshot means new documents about emerging topics will be forced into the most similar existing topic. (3) Scalability — LDA with Gibbs sampling is O(n_words × K × n_iterations); for large corpora use online VB LDA (Hoffman et al. 2010) which processes mini-batches. (4) Topic drift — the corpus changes over time and topics shift; periodically retrain on recent data or use dynamic topic models.`,
      `BERTopic: modern alternative to LDA/NMF. Pipeline: (1) Encode documents with sentence-BERT embeddings. (2) Reduce to 5-d with UMAP. (3) Cluster with HDBSCAN. (4) Extract topic representations using class-based TF-IDF (c-TF-IDF) — the words that best distinguish each cluster from the others. BERTopic advantages: semantically richer topics (captures meaning through embeddings), handles short texts better, no need to choose K (HDBSCAN finds it), and can track topic evolution over time. Limitation: less interpretable as a generative model (cannot compute document-topic distributions directly), requires GPU for large-scale embedding.`,
      `Evaluating topic models in practice: statistical metrics (perplexity, coherence) give a rough guide but are insufficient. The practical evaluation is: present the top-10 words for each topic to a domain expert and ask them to label it. Measure the fraction of topics that receive a coherent label (human interpretability rate). A topic model with coherence=0.55 where all topics are interpretable is better for production use than one with coherence=0.60 where half the topics are labelled "miscellaneous". Also evaluate document coverage: are all documents assigned to a meaningful topic, or are many documents spread thinly across many topics with low proportion?`,
    ],
    checkQuestions: [
      {
        q: `You train LDA with K=10 topics but the coherence score is low — words within each topic are not semantically related. List 3 concrete things to try.`,
        a: `Three targeted interventions: (1) Improve preprocessing — add bigrams for common domain phrases (topic modeling splits "machine_learning" into two tokens that dilute both topics), remove more stop words, and increase minimum document frequency to eliminate rare words that appear in few documents and add noise. The most common reason for incoherent topics is a noisy vocabulary. (2) Try a different K — K=10 may be forcing the model to subdivide natural topics or merge unrelated ones. Plot coherence vs K from K=2 to K=30 and find the peak. A typical coherence vs K curve peaks around the true number of topics and then degrades as topics fragment. (3) Switch to NMF — NMF with TF-IDF input often produces more coherent topics than LDA on short texts (product reviews, news headlines, tweets) because TF-IDF already de-emphasises common words, and the parts-based NMF representation naturally produces sparse, coherent word groups. If domain expertise matters, also try BERTopic for semantically richer topics.`,
      },
      {
        q: `A document about "machine learning in healthcare" has LDA topic proportions: topic 3 (medicine) = 0.45, topic 7 (ML) = 0.40, topic 1 (other) = 0.15. How do you use this for document retrieval vs document categorisation?`,
        a: `For document retrieval: the soft topic proportions are ideal. Instead of assigning the document to one class, represent it as a topic vector [0, 0.15, 0, 0.45, 0, 0, 0, 0.40, ...] and use cosine similarity between topic vectors to find similar documents. A query about "deep learning for diagnosis" will have high probability on both the ML and medicine topics and will correctly retrieve this document even though it is not purely about either. This is the key advantage of LDA over hard clustering for retrieval. For document categorisation: if the downstream task needs a single category, the highest-proportion topic (topic 3, medicine, 0.45) is the primary label, but report uncertainty — the document is nearly equally about medicine and ML. If the task requires a strict 1-category assignment, build a supervised classifier that uses these soft topic vectors as features, giving a calibrated prediction rather than just argmax.`,
      },
      {
        q: `You find that the top 10 words for 3 out of 10 LDA topics are nearly identical (all contain "data", "model", "analysis", "result", "method"). What does this indicate?`,
        a: `These three topics are dominated by generic methodology vocabulary rather than domain-specific content words — they represent the same underlying "generic research paper language" topic that LDA has split into three versions due to slight variation in word co-occurrence patterns. Root causes: (1) K is too high — with K=10 you are forcing 10 distinctions when the data has fewer natural topics. (2) Generic academic vocabulary was not removed from the corpus — words like "data", "model", "method" appear across all topics and should be added to the stop word list or removed by their high document frequency. Fix: increase minimum document frequency threshold to remove words appearing in more than 70% of documents (they are uninformative across topics). Then reduce K to 7 and compare coherence. The generic words should be treated like stop words — their presence in the vocabulary causes topic dilution.`,
      },
      {
        q: `What is the difference between perplexity and coherence as metrics for LDA? Why do they sometimes disagree about the optimal K?`,
        a: `Perplexity measures the model's ability to predict held-out test words — it is a generative model fitness metric. Lower perplexity = the model predicts unseen words better = better statistical fit. Coherence measures the semantic relatedness of the top words within each topic using external co-occurrence statistics — it is a human-interpretability proxy metric. They disagree about K for a systematic reason: perplexity is minimised by having many topics that precisely describe small subsets of the vocabulary — K=1,000 nearly memorises the vocabulary. Coherence is maximised by having a moderate number of topics that each focus on semantically related word clusters — too many topics fragment meaningful groups. Adding more topics always helps perplexity but starts hurting coherence after the optimal K because topics split into subsets that are semantically incoherent. The optimal K for human use is the one that maximises coherence; the optimal K for statistical fit is the one that minimises perplexity. They represent different objectives, and for topic modeling's primary use case (human interpretation), coherence is the right metric to optimise.`,
      },
    ],
    takeaway: `The key insight is that topic model quality has two independent dimensions — statistical fit (perplexity) and human interpretability (coherence) — and optimising one does not guarantee the other, which means the only reliable validation is whether domain experts find the discovered topics meaningful and actionable.`,
  },
]

export const UNSUPERVISED_MODULES = [
  {
    id: 'clustering_overview',
    title: 'Clustering Taxonomy',
    subtitle: 'Partitional vs hierarchical vs density-based, no ground truth = no accuracy',
    difficulty: 'foundational',
    estimatedMin: 15,
    tags: ['clustering', 'taxonomy', 'unsupervised'],
    summary: `Clustering groups data points into clusters based on similarity without labels. The fundamental challenge: without ground truth, "correct" clusters are undefined — evaluation must use intrinsic metrics (cohesion, separation) or domain validation. Partitional methods (k-means) assign each point to exactly one cluster. Hierarchical methods build a nested tree. Density-based methods (DBSCAN) find clusters of arbitrary shape and identify noise.`,
    keyPoints: [
      'Partitional: divide n points into k clusters. Requires k upfront. Fast O(nkd) per iteration. Assumes roughly spherical, equal-size clusters.',
      'Hierarchical: builds a dendrogram (nested cluster tree). Agglomerative (bottom-up): start with n singletons, merge closest pair. Divisive (top-down): start with one cluster, split. No k required upfront.',
      'Density-based: clusters are high-density regions separated by low-density regions. Handles arbitrary shapes, identifies outliers as noise. Requires density parameters instead of k.',
      'Evaluation without labels: silhouette score (cohesion vs separation), Davies-Bouldin index, Calinski-Harabasz index. None are ground truth — use domain expert validation as final arbiter.',
      'Evaluation with labels: Rand Index, Adjusted Rand Index (ARI), Normalised Mutual Information (NMI). Require known true cluster assignments — more useful for benchmarking than production.',
      'Cluster validity: clusters should be stable (similar results on different runs/subsets), meaningful (interpretable by domain experts), and useful (actionable for downstream tasks).',
      'The right algorithm depends on: expected cluster shape (spherical → k-means, arbitrary → DBSCAN), whether noise/outliers exist (DBSCAN handles, k-means does not), whether hierarchy is meaningful (hierarchical), and scale (k-means scales to millions, hierarchical is O(n² logn)).',
    ],
    checkQuestions: [
      { q: 'A clustering of customers produces 5 clusters with silhouette score=0.62. Is this a good clustering?', a: 'Silhouette score of 0.62 indicates reasonably good cluster separation and cohesion (range -1 to 1; >0.5 is often considered good). However: (1) Compare against k=3,4,6,7 — is 0.62 the peak? (2) Validate business interpretability — are the 5 clusters meaningfully different for the business use case? (3) Check cluster sizes — are they balanced or is one cluster capturing 95% of customers? Statistical metrics alone cannot validate that clusters are useful — domain expert review is mandatory.' },
    ],
  },
  {
    id: 'kmeans',
    title: 'K-Means Clustering',
    subtitle: 'Lloyd\'s algorithm, k-means++ init, silhouette, failure modes',
    difficulty: 'foundational',
    estimatedMin: 20,
    tags: ['k-means', 'clustering', 'Lloyd\'s algorithm'],
    summary: `K-means partitions n points into k clusters by alternating between assigning each point to its nearest centroid (E-step) and updating centroids as cluster means (M-step). This is Lloyd's algorithm and it converges to a local optimum. The objective is to minimise within-cluster sum of squares (WCSS = Σᵢ ‖xᵢ − μ_{c(i)}‖²). K-means++ provides a smarter initialisation that provably achieves O(log k) approximation to optimal WCSS.`,
    keyPoints: [
      'Lloyd\'s algorithm: (1) Initialise k centroids. (2) Assign each point to nearest centroid. (3) Recompute centroids as mean of assigned points. (4) Repeat 2-3 until convergence. Converges in finite steps but to local optima.',
      'k-means++ initialisation: choose first centroid uniformly at random; each subsequent centroid chosen with probability proportional to distance² to nearest existing centroid. Avoids pathological local optima and speeds convergence.',
      'Choosing k: elbow method (plot WCSS vs k; look for elbow), silhouette analysis (find k maximising mean silhouette), gap statistic (compare WCSS to random baseline). None are definitive — try multiple k.',
      'Silhouette score for sample i: s(i) = (b(i) - a(i)) / max(a(i), b(i)) where a = mean intra-cluster distance, b = mean nearest-cluster distance. s ∈ [-1,1]; close to 1 = well-clustered.',
      'k-means failure modes: (1) Non-spherical clusters — k-means assumes circular/spherical. (2) Varying cluster density — equal WCSS weight disadvantages small dense clusters. (3) Varying cluster size — large clusters dominate. (4) High dimensionality — distances become similar (curse of dimensionality).',
      'k-means variants: k-medoids (uses actual data points as centres, robust to outliers), mini-batch k-means (O(batch) per iteration, suitable for large n), k-modes (categorical data, uses mode instead of mean).',
      'Computational complexity: O(nkdi) per run where i=iterations. Typically i ≈ 300, d features, k clusters. Scales linearly with n — handles millions of points.',
    ],
    checkQuestions: [
      { q: 'k-means gives very different results on different runs. What is wrong and how do you fix it?', a: 'Different runs converge to different local optima due to random initialisation sensitivity. Fixes: (1) Use k-means++ initialisation (default in sklearn) — reduces variance significantly. (2) Run with multiple random seeds (n_init=10 in sklearn) and keep the run with lowest WCSS. (3) If variance persists, the data may genuinely have multiple equally-valid clusterings — consider trying different k. (4) For very sensitive cases, use a deterministic initialisation (e.g., based on data structure).' },
    ],
  },
  {
    id: 'hierarchical',
    title: 'Hierarchical Clustering',
    subtitle: 'Linkage criteria, dendrograms, when hierarchy beats flat clustering',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['hierarchical clustering', 'dendrogram', 'linkage'],
    summary: `Hierarchical clustering builds a tree of cluster merges (dendrogram) that represents the data at all levels of granularity simultaneously. Agglomerative clustering starts with each point as its own cluster and greedily merges the two most similar clusters at each step. The choice of linkage criterion (how to measure inter-cluster distance) dramatically affects the shape and quality of clusters.`,
    keyPoints: [
      'Linkage criteria: single (nearest points), complete (farthest points), average (UPGMA — average of all pairs), Ward (minimise within-cluster variance increase). Ward tends to produce compact, equal-size clusters.',
      'Single linkage: creates chaining effect — long, thin clusters following data chains. Sensitive to outliers. But useful for detecting elongated, chain-like structures.',
      'Complete linkage: compact, roughly equal-diameter clusters. Sensitive to outliers (one outlier pulls the diameter up). More suitable for compact, blob-like clusters.',
      'Ward linkage: most commonly used in practice. Greedy minimisation of WCSS increase at each merge. Produces compact clusters similar to k-means but without requiring k upfront.',
      'Dendrogram: tree where leaves are data points, height represents merge distance. Cut the dendrogram at a height h to get clusters — all branches below h merge into one cluster.',
      'Computational cost: naive implementation O(n³) time and O(n²) memory. Not suitable for n > 10,000. Approximate algorithms (SLINK for single linkage, scipy implementations) reduce to O(n² logn).',
      'When hierarchical beats flat clustering: when the number of clusters is unknown and you want to explore multiple levels of granularity; when clusters have a natural hierarchy (taxonomy, phylogenetics); when you want to visualise the merge process.',
    ],
    checkQuestions: [
      { q: 'You are clustering genes based on expression profiles. Why might hierarchical clustering with complete linkage be more appropriate than k-means?', a: 'Gene expression clusters often have a natural biological hierarchy (gene families, pathways). Hierarchical clustering reveals this structure at multiple granularities without fixing k upfront. Complete linkage produces compact clusters suitable for groups of co-expressed genes. The dendrogram enables exploration — cut at different heights to get different cluster counts. k-means would require committing to k before understanding the data, and assumes spherical clusters that may not match gene expression patterns. The dendrogram is also interpretable for biological validation.' },
    ],
  },
  {
    id: 'dbscan',
    title: 'DBSCAN',
    subtitle: 'Core/border/noise points, eps and minPts, non-spherical clusters',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['DBSCAN', 'density-based', 'outlier detection'],
    summary: `DBSCAN (Density-Based Spatial Clustering of Applications with Noise) defines clusters as contiguous high-density regions. A core point has at least minPts neighbours within radius eps. Border points are within eps of a core point but are not core points themselves. Noise points (outliers) belong to no cluster. DBSCAN discovers clusters of arbitrary shape, does not require k, and explicitly identifies outliers — making it ideal for geographic and anomaly-detection applications.`,
    keyPoints: [
      'Algorithm: for each unvisited point p: (1) find all eps-neighbours. (2) If |neighbours| ≥ minPts: p is core point, start new cluster, recursively add density-reachable points. (3) Otherwise: mark as border or noise.',
      'Core point: has ≥ minPts points within eps (including itself). Border point: within eps of a core point but not a core point itself. Noise point: not within eps of any core point.',
      'Density-reachable: point q is density-reachable from p if there is a chain of core points from p to q where each step is within eps. Clusters are sets of mutually density-connected points.',
      'Parameter sensitivity: eps controls neighbourhood radius (use k-distance plot to find natural knee), minPts controls minimum cluster density (rule of thumb: minPts ≥ ln(n) for small n; minPts=2d for d-dimensional data).',
      'Advantages over k-means: arbitrary cluster shape, automatic noise detection, no k required upfront, robust to outliers (they become noise, not distorting centroids).',
      'Disadvantages: fails with clusters of varying density (different eps needed per cluster — HDBSCAN solves this), does not scale well to high dimensions (distance becomes uniform), sensitive to eps and minPts choice.',
      'HDBSCAN: hierarchical DBSCAN. Builds a hierarchy of clusters at varying eps, then extracts stable clusters. Handles varying density, more robust to parameter choice.',
    ],
    checkQuestions: [
      { q: 'k-means gives 5 circular clusters on your GPS location data, but you suspect the true structure has non-circular geographic regions. What do you use and how do you set parameters?', a: 'Use DBSCAN. For GPS data (2D), set parameters via k-distance plot: compute distance to k-th nearest neighbour (k=minPts) for all points; plot sorted distances; the "knee" is a good eps choice. minPts ≥ 4 (2×dimensions) is a starting point — increase for noisier data. DBSCAN will find contiguous high-density geographic regions of arbitrary shape (e.g., city neighbourhoods, park boundaries) and flag isolated points as noise (outliers/erroneous GPS pings). Validate clusters against known geographic boundaries.' },
    ],
  },
  {
    id: 'dim_reduction',
    title: 'Dimensionality Reduction Taxonomy',
    subtitle: 'Linear: PCA/ICA; non-linear: t-SNE/UMAP — what each preserves',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['dimensionality reduction', 'PCA', 'UMAP', 't-SNE', 'ICA'],
    summary: `Dimensionality reduction maps high-dimensional data to a lower-dimensional representation, preserving different aspects of structure depending on the method. Linear methods (PCA, ICA) preserve global linear structure and are interpretable. Non-linear methods (t-SNE, UMAP) preserve local neighbourhood structure and reveal non-linear manifolds but sacrifice global structure. The choice depends on what structure matters for the downstream task.`,
    keyPoints: [
      'PCA: preserves maximum global variance. Components are orthogonal linear combinations. Interpretable (each component is a weighted sum of features). Best for compression and feature extraction when linear structure dominates.',
      'ICA (Independent Component Analysis): finds components that are statistically independent (not just uncorrelated). Useful for source separation (blind source separation: separate mixed audio signals). Assumes non-Gaussian sources.',
      'Non-linear manifold hypothesis: real high-dimensional data often lies on a low-dimensional non-linear manifold embedded in high-dimensional space. PCA cannot unroll a Swiss roll; UMAP/t-SNE can.',
      't-SNE preserves: local neighbourhood structure (nearby points in high-d stay nearby in low-d). Does not preserve: global distances, cluster sizes, or inter-cluster distances. Use for visualisation only, not for feature extraction.',
      'UMAP preserves: local AND approximately global structure (more than t-SNE). Faster, scales better, and is more suitable as a preprocessing step for clustering or classification.',
      'Autoencoders: non-linear dimensionality reduction via encoder-decoder neural network. Bottleneck layer is the embedding. Variational autoencoder (VAE) forces the embedding to be a smooth Gaussian distribution.',
      'When to use which: PCA for preprocessing before linear models or clustering (preserves global). t-SNE/UMAP for visualisation and exploratory analysis. Autoencoders when labelled fine-tuning is available or when reconstruction quality matters.',
    ],
    checkQuestions: [
      { q: 'You visualise 50k points in 2D with t-SNE and see 10 clear clusters. You then run k-means on the t-SNE 2D embeddings. What is wrong with this approach?', a: 'Two problems: (1) t-SNE distorts global distances — inter-cluster distances in the 2D plot are meaningless. Clusters that appear far apart in 2D may be closer in high-dimensional space. (2) t-SNE is non-deterministic and sensitive to perplexity — different runs can show different apparent structures. The correct approach: run k-means in the original high-dimensional space (or on PCA-reduced dimensions for speed). Use t-SNE only for visualisation. The t-SNE visualisation can suggest k (number of clusters), but the clustering should happen in the original or PCA-reduced space.' },
    ],
  },
  {
    id: 'tsne_umap',
    title: 't-SNE & UMAP',
    subtitle: 'Perplexity, why t-SNE clusters mislead, UMAP vs t-SNE tradeoffs',
    difficulty: 'advanced',
    estimatedMin: 25,
    tags: ['t-SNE', 'UMAP', 'visualisation', 'manifold'],
    summary: `t-SNE (t-distributed Stochastic Neighbour Embedding) and UMAP (Uniform Manifold Approximation and Projection) are the dominant non-linear dimensionality reduction methods for visualisation. t-SNE minimises KL divergence between high-d and low-d pairwise similarity distributions, using a heavy-tailed (t-distribution) low-d kernel to prevent crowding. UMAP is based on Riemannian geometry and fuzzy simplicial sets — faster, more scalable, and better at preserving global structure.`,
    keyPoints: [
      't-SNE objective: minimise KL(P||Q) where Pᵢⱼ ∝ exp(−‖xᵢ−xⱼ‖²/2σᵢ²) (high-d) and Qᵢⱼ ∝ (1+‖yᵢ−yⱼ‖²)⁻¹ (low-d t-distribution). The t-distribution\'s heavy tails prevent points from crowding the centre.',
      'Perplexity: effective number of neighbours (typically 5-50). Controls the balance between local and global structure. Low perplexity → tight local clusters. High perplexity → more global structure. No single "right" value.',
      't-SNE pitfalls: (1) Cluster sizes in 2D are meaningless — t-SNE inflates small tight clusters and compresses large spread-out ones. (2) Distances between clusters are meaningless. (3) Patterns may appear even in random data at certain perplexity. (4) Non-deterministic (random initialisation) — different runs may show different apparent structure.',
      'UMAP speed: O(n log n) vs t-SNE\'s O(n²) (with Barnes-Hut approximation O(n log n)). UMAP is 5-100× faster in practice and handles 1M+ points where t-SNE struggles.',
      'UMAP preserves global structure better: approximate geodesic distances on the manifold are preserved. t-SNE only preserves neighbourhood relationships. UMAP 2D embeddings can be used for k-means clustering; t-SNE 2D embeddings should not.',
      'UMAP parameters: n_neighbors (like perplexity), min_dist (minimum distance between points in low-d — small=tight clusters, large=spread). Metric (euclidean, cosine, etc.).',
      'For single-cell RNA-seq and other bioinformatics: UMAP is now preferred over t-SNE for both visualisation and downstream clustering. In NLP: visualise word embeddings or document clusters.',
    ],
    checkQuestions: [
      { q: 'You run t-SNE with perplexity=5 and see 50 tiny, tight clusters. You run with perplexity=100 and see 3 blobs. Which is the "true" structure?', a: 'Neither visualisation alone reveals "true" structure — t-SNE at different perplexities emphasises different scales of neighbourhood structure. Perplexity=5 shows fine-grained local groupings; perplexity=100 shows coarse global groupings. The truth likely lies between. To get a more reliable picture: (1) Run at multiple perplexity values and look for structure that appears consistently. (2) Use UMAP (more stable to parameter choice, better global preservation). (3) Validate cluster structure in the original space using k-means or hierarchical clustering and compare against t-SNE visualisation.' },
    ],
  },
  {
    id: 'anomaly_detection',
    title: 'Anomaly Detection',
    subtitle: 'Isolation forest, one-class SVM, autoencoder-based, LOF',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['anomaly detection', 'isolation forest', 'outlier', 'one-class'],
    summary: `Anomaly detection identifies data points that differ significantly from the majority. Unlike classification, it is typically unsupervised (few or no labelled anomalies). Algorithms fall into four families: statistical (fit a distribution, flag outliers), distance-based (LOF), tree-based isolation (Isolation Forest), and reconstruction-based (autoencoders). Choice depends on data type, anomaly definition, and whether patterns in normal data are learnable.`,
    keyPoints: [
      'Isolation Forest: anomalies are "few and different" — isolated quickly by random axis-aligned splits. Anomaly score = average depth to isolation across trees. Shallow isolation → anomaly. O(n log n) training, O(n log n) scoring. Robust, scalable, handles high dimensions.',
      'Local Outlier Factor (LOF): compares local density of a point to its neighbours. LOF ≈ 1: similar density to neighbours (normal). LOF >> 1: much lower density (outlier). Local — works for datasets with varying density, unlike global methods.',
      'One-class SVM: learns a tight boundary around the "normal" data in feature space. Points outside the boundary are anomalies. Sensitive to parameter ν (fraction of outliers expected) and kernel bandwidth. Slow for large n.',
      'Autoencoder-based: train autoencoder on normal data. At inference, anomalies have high reconstruction error (model cannot reconstruct patterns it has not seen). Powerful for high-dimensional data (images, time series) where other methods fail.',
      'Statistical methods: fit N(μ,σ²) to data; flag points beyond 3σ. Or fit GMM for multimodal data. Simple, interpretable, but assume a specific distribution shape.',
      'Evaluation challenge: few or no labels. Use precision/recall if some labels are available. Without labels: downstream impact (does flagging these points improve a downstream task?), expert review, or contamination rate comparison.',
      'Semi-supervised anomaly detection: train on labelled normal data only; treat all unlabelled as potential anomalies. Deep SVDD, PatchCore for images — learns a hypersphere around normal embeddings.',
    ],
    checkQuestions: [
      { q: 'You are detecting network intrusion anomalies from 1M log events per day with hundreds of features. Which anomaly detection method do you choose?', a: 'Isolation Forest is the primary choice: scales to 1M+ samples, handles high dimensions well, O(n log n) training, can score new events in real-time O(log n). Hyper-parameters: contamination fraction (estimated from domain knowledge of expected attack rate), n_estimators=100+, max_samples=256 (subsampling gives speed without accuracy loss). Alternative: autoencoder if patterns in normal traffic are complex and structured (sequential patterns, feature interactions). Avoid LOF (too slow for 1M events — O(n²)) and one-class SVM (also slow and sensitive to feature scale).' },
    ],
  },
  {
    id: 'topic_modeling',
    title: 'Topic Modeling',
    subtitle: 'LDA, NMF — choosing K, evaluating coherence, production limitations',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['topic modeling', 'LDA', 'NMF', 'NLP'],
    summary: `Topic modeling discovers latent themes in a corpus of documents without supervision. LDA (Latent Dirichlet Allocation) is a generative probabilistic model: documents are mixtures of topics, topics are distributions over words. NMF (Non-negative Matrix Factorisation) decomposes the term-document matrix as a product of two non-negative matrices. Both discover interpretable word groupings but differ in their assumptions and optimisation.`,
    keyPoints: [
      'LDA generative model: for each document d, draw topic distribution θ_d ~ Dirichlet(α). For each word position: draw topic z ~ Multinomial(θ_d), draw word w ~ Multinomial(φ_z). Inference via variational EM or collapsed Gibbs sampling.',
      'NMF: decompose TF-IDF matrix V ≈ WH where W ∈ ℝ_{+}^{D×K} (document-topic), H ∈ ℝ_{+}^{K×V} (topic-word). Non-negativity forces parts-based, additive representation — each topic is a weighted sum of words with positive weights only.',
      'Choosing K: perplexity (LDA — lower on held-out is better, but perplexity is not perfectly correlated with human interpretability), coherence score (C_v or NPMI — measures if top words in a topic co-occur frequently in real text).',
      'Topic coherence (NPMI): for top words w₁,...,wₙ of a topic: NPMI(wᵢ,wⱼ) = log[P(wᵢ,wⱼ)/P(wᵢ)P(wⱼ)] / -log P(wᵢ,wⱼ). Averaged over all pairs. Higher = more coherent topic. Correlates better with human judgement than perplexity.',
      'Preprocessing matters: lemmatisation, stop word removal, bigrams for common phrases, TF-IDF weighting or raw counts (LDA typically uses counts). High minimum document frequency for words.',
      'Production limitations: topics are not stable — re-running LDA gives different topics in different order. Topics may not map to business-meaningful categories. Dynamic topic models and online LDA handle new documents but add complexity.',
      'Modern alternatives: BERTopic uses sentence-BERT embeddings + UMAP + HDBSCAN + class-based TF-IDF. More coherent topics, handles short texts, but less interpretable as a generative model.',
    ],
    checkQuestions: [
      { q: 'You train LDA with K=10 topics but the coherence score is low — words within each topic are not semantically related. List 3 things to try.', a: '(1) Preprocess better: add bigrams/trigrams (important domain phrases split into unigrams lose meaning), remove more stop words (common function words dilute topics), increase min_df to remove rare words that add noise. (2) Try different K: use coherence vs K curve and find the K that maximises coherence — K=10 may be wrong for this corpus. (3) Try NMF instead of LDA: NMF often produces more coherent topics on short texts and when TF-IDF weighting is used. (Bonus) Try BERTopic for better semantic coherence using contextual embeddings.' },
    ],
  },
]

export const EVAL_MODULES = [
  {
    id: 'metrics_first_principles',
    title: 'Eval Metrics from First Principles',
    subtitle: 'Confusion matrix, precision, recall, F1, why accuracy fails',
    difficulty: 'foundational',
    estimatedMin: 32,
    tags: ['metrics', 'precision', 'recall', 'F1', 'confusion matrix'],
    summary: `The confusion matrix is the bedrock of all classification metrics: it counts four outcomes — TP (correctly predicted positive), FP (wrongly predicted positive), TN (correctly predicted negative), FN (missed positive). Accuracy collapses all four cells into one number, which makes it deeply misleading when classes are unbalanced. If 99% of samples are negative, a model that always predicts negative achieves 99% accuracy while finding zero true positives — a complete failure masquerading as a near-perfect system. Precision measures how trustworthy a positive prediction is (TP/(TP+FP)); recall measures how many real positives the model finds (TP/(TP+FN)). These two metrics are fundamentally opposed: lowering the decision threshold raises recall by accepting more FPs, which drops precision. F1 is the harmonic mean of the two, which punishes extreme imbalances between them — a model with P=1.0 and R=0.01 gets F1=0.02, exposing it as useless despite perfect precision. The F-beta generalisation lets you explicitly encode the cost ratio of FP vs FN into a single scalar, which is how you translate business costs into a metric.`,
    keyPoints: [
      `Accuracy is deceptive on imbalanced data: with 1% positive class, predicting all-negative gives 99% accuracy but 0% recall. The model learns nothing — yet accuracy rewards it. This is why accuracy should never be your primary metric for medical diagnosis, fraud detection, or any rare-event prediction task.`,
      `Precision answers "when you cry wolf, are you right?" and recall answers "of all actual wolves, how many did you find?" — they trade off through the decision threshold. Raising threshold increases precision (fewer FPs) but decreases recall (more FNs). The failure mode is optimising only one: high-precision/low-recall systems miss most positives; high-recall/low-precision systems flood downstream workflows with false alarms.`,
      `F1 is the harmonic mean, not the arithmetic mean, which means the lower value dominates. F1(P=0.9, R=0.1) = 0.18, not 0.5. This is intentional — it ensures you cannot compensate for dismal recall with excellent precision. Use F1 when you care equally about FPs and FNs.`,
      `F-beta score: beta > 1 weights recall more (beta=2 means recall twice as important as precision). beta < 1 weights precision more. Choosing beta is a business decision: in cancer screening, beta=2 because missing a positive (FN) is far more costly than a false alarm. In spam detection, beta=0.5 because annoying the user with false positives (FP) is the primary cost.`,
      `Macro-average computes the metric per class then takes the unweighted mean — every class counts equally regardless of size, which is correct when a rare class matters as much as a common one. Micro-average sums TP/FP/FN globally — dominated by the majority class. Weighted average weights by class frequency. The choice determines which classes your scalar metric represents.`,
      `Matthews Correlation Coefficient (MCC) = (TP·TN − FP·FN) / sqrt((TP+FP)(TP+FN)(TN+FP)(TN+FN)) uses all four confusion matrix cells symmetrically. Range −1 to 1; a random classifier gets 0. MCC is robust to class imbalance in ways that F1 is not — it is the single best scalar for binary classification, which is why it is the standard in computational biology benchmarks where imbalance is extreme.`,
      `The precision-recall curve traces the P-R trade-off as threshold varies; area under it (PR-AUC or AP) summarises the whole curve. PR-AUC is more informative than ROC-AUC for imbalanced problems because it does not use TN at all — it only measures how well the model ranks positives, which is what matters when positives are rare.`,
      `Choosing a threshold is a business decision, not a model decision. The model outputs a score; the threshold converts it to a binary prediction. Setting threshold too high produces a high-precision, low-recall system; too low does the reverse. Operating point should be chosen based on cost(FP) vs cost(FN), which requires talking to stakeholders before selecting a metric.`,
      `Label noise degrades all metrics simultaneously and its effect is asymmetric: mislabelled positives become FNs and mislabelled negatives become FPs, biasing both precision and recall downward. When offline metrics are suspiciously low for a well-designed model, audit label quality before blaming the algorithm.`,
    ],
    checkQuestions: [
      {
        q: `A cancer screening model has precision=0.95 and recall=0.40. Is this a good model?`,
        a: `Recall=0.40 means 60% of actual cancer cases are missed — those are FNs who will leave the clinic believing they are healthy. For cancer screening, a missed positive (FN) is catastrophic while a false alarm (FP) only leads to a follow-up test. You almost certainly need recall > 0.90 here and are willing to sacrifice precision to get there. Lower the decision threshold: as you accept more FPs, recall climbs. The right operating point is where the cost of extra follow-up tests is outweighed by the cost of missed diagnoses — a cost-weighted F-beta with beta=3 or beta=4 would formalise this. Precision=0.95 at recall=0.40 is a sign the model is being too conservative.`,
      },
      {
        q: `Two models: Model A has precision=0.80, recall=0.80 (F1=0.80). Model B has precision=0.99, recall=0.67 (F1=0.80). Same F1. How do you choose?`,
        a: `The choice depends on the cost structure. Model B has much higher precision: nearly every positive prediction it makes is correct — this is ideal if FPs have high cost (e.g., blocking a legitimate bank transaction). But its recall of 0.67 means it misses a third of real positives — unacceptable if FNs are costly (e.g., missed fraud, missed disease). Model A is more balanced. For fraud detection where both false blocks and missed fraud matter, Model A is safer. The key lesson is that identical F1 scores can correspond to very different operating points on the precision-recall curve, and the right choice requires knowing cost(FP) vs cost(FN) from the business.`,
      },
      {
        q: `You compute macro-F1=0.91 and micro-F1=0.78 on a 5-class classifier. What does this tell you?`,
        a: `Macro-F1 = 0.91 means classes are well-separated on average when each class is weighted equally. Micro-F1 = 0.78 means global performance (dominated by the majority class) is worse. The gap tells you that small classes are doing well (pulling macro up) but the large majority class has significant errors (pulling micro down). Investigate: the model may be over-fitted to small classes in training or the majority class may have significant within-class heterogeneity. For an imbalanced business problem where majority-class errors are the most frequent real-world failures, micro-F1=0.78 is the number that matters for production impact.`,
      },
      {
        q: `A model predicts fraud with AUC=0.97 but the operations team says too many legitimate transactions are being blocked. What metric should you change and why?`,
        a: `AUC measures ranking quality across all thresholds and says nothing about where you operate. The problem is the operating threshold is too low — it accepts too many FPs to achieve its recall target. The operations team is experiencing a precision problem. Switch to monitoring precision at a fixed recall target (e.g., precision at recall=0.80) or precision at a fixed FP-rate. Then raise the threshold until precision meets the operations team's tolerance for blocked-legitimate transactions. AUC is an offline selection metric; the production metric that matters is precision at the operating recall your business has decided to target.`,
      },
    ],
    takeaway: `The key insight is that precision and recall measure fundamentally opposite failure modes, which means the only correct way to choose a classification metric is to first quantify the business cost of FP vs FN — everything else follows from that ratio.`,
    interactiveId: 'confusion_matrix_viz',
  },
  {
    id: 'auc_roc',
    title: 'ROC Curve & AUC',
    subtitle: 'FPR/TPR, what area means, PR-AUC for imbalanced classes',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['ROC', 'AUC', 'PR-AUC', 'ranking'],
    summary: `The ROC (Receiver Operating Characteristic) curve plots TPR (true positive rate = recall) on the y-axis against FPR (false positive rate = FP/(FP+TN)) on the x-axis as the decision threshold sweeps from 1 down to 0. At threshold=1, everything is predicted negative so TPR=FPR=0; at threshold=0 everything is positive so TPR=FPR=1. A perfect classifier goes to the top-left corner (TPR=1, FPR=0). AUC-ROC is the area under this curve and has a clean probabilistic interpretation: it equals the probability that the model ranks a random positive example higher than a random negative one. AUC=0.5 is random, AUC=1.0 is perfect. The critical limitation of ROC-AUC for imbalanced datasets is that FPR uses TN in the denominator — with many negatives, even large FP counts produce small FPR, making the curve look good even when the model performs poorly on the minority class. PR-AUC eliminates TN entirely, making it a far more honest metric when positives are rare. A random classifier's PR-AUC equals the class prevalence (0.01 for 1% positive), giving a much harder baseline to beat than the ROC baseline of 0.5.`,
    keyPoints: [
      `AUC probabilistic interpretation: P(score(positive) > score(negative)) for a randomly chosen pair. This means AUC=0.85 tells you the model will rank a random positive above a random negative 85% of the time — a pure ranking quality metric independent of threshold choice.`,
      `ROC curve geometry: the diagonal line (y=x) represents a random classifier. A curve that bows toward the top-left corner is better. A curve below the diagonal means the model is inversely calibrated — you can improve it by flipping predictions. This also means AUC < 0.5 is never a property of a model in isolation, only of an incorrectly implemented scoring function.`,
      `Why AUC-ROC is optimistic for imbalanced data: FPR = FP/(FP+TN). With 10,000 negatives and 100 positives, even 500 FPs give FPR = 500/10500 ≈ 0.05 — a small number. The ROC curve stays far left, making the model look excellent. But precision = 100/(100+500) = 0.17 — terrible. PR-AUC would expose this.`,
      `PR-AUC ignores TN entirely — precision and recall both only count TP, FP, and FN. This means the metric is entirely focused on the minority class, which is exactly what you want for fraud, disease, or anomaly detection. The failure mode of using ROC-AUC here is shipping models that have high ranking quality on the full distribution but poor operational precision on the class you actually care about.`,
      `Average Precision (AP) is the area under the PR curve computed with the trapezoidal rule, weighted by recall change at each threshold. AP = sum_k (R_k − R_{k-1}) × P_k. This gives extra weight to high-precision regions because achieving precision while also achieving recall is harder. MAP (Mean Average Precision) averages AP across queries — the standard in search and information retrieval.`,
      `When to use ROC-AUC vs PR-AUC: ROC-AUC for roughly balanced classes or when you genuinely care about ranking quality across the full negative set (e.g., a credit scoring model that must rank all applicants). PR-AUC for imbalanced problems where TN performance is irrelevant to the business (fraud, medical diagnosis, anomaly detection). Using ROC-AUC on an imbalanced dataset is a common mistake that leads to overconfident model selection.`,
      `Calibration vs discrimination: AUC measures discrimination (ranking). A model with AUC=0.95 but poor calibration produces overconfident or underconfident probabilities. If you use the output probability for a decision threshold or risk score, calibration matters independently of AUC. You can have excellent AUC and terrible calibration simultaneously — always check both.`,
      `Partial AUC: you can restrict AUC computation to a specific FPR range (e.g., FPR < 0.1) when you only operate at low false-positive rates. This avoids evaluating performance at operating points you will never use, which is more meaningful for business decisions in high-stakes settings.`,
    ],
    checkQuestions: [
      {
        q: `Two models have the same AUC-ROC (0.85) on a 1% positive rate dataset. How would you further differentiate them?`,
        a: `Compare PR-AUC — for 1% positive rate, it is far more discriminative because it ignores the large TN pool. Look at precision at specific recall targets that matter for the business (e.g., precision at recall=0.80). Compare calibration — which model's P(y=1) better matches actual frequencies when you bucket predictions? Compute the lift curve — for top-k% of scored records, which model captures more true positives? Two models can have identical AUC-ROC but very different PR curves, especially in the high-recall regime where one model may be much more precise. Finally, compute the Brier score to jointly measure calibration and discrimination.`,
      },
      {
        q: `Your fraud model has AUC=0.96. The business team says the alert queue has too many false alarms. What happened and how do you fix it?`,
        a: `AUC=0.96 measures ranking quality, not operational precision at the deployed threshold. The problem is that at the chosen operating threshold, precision is too low — too many FPs relative to TPs. AUC does not tell you anything about the threshold. Fix: (1) Raise the score threshold to reduce FPs, accepting lower recall. (2) Evaluate and report precision at the recall target the business needs rather than AUC. (3) If the false alarm rate is fundamentally bad even at very high thresholds, the model may have good ranking but poor absolute calibration — recalibrate with Platt scaling or isotonic regression. The disconnect between high AUC and high false alarm rate is typical when the positive rate is very low.`,
      },
      {
        q: `AUC-ROC for a model is 0.72. A colleague argues that "since 0.72 > 0.5, the model is useful." Is that a sufficient argument?`,
        a: `No. AUC > 0.5 only means the model ranks positives above negatives more often than chance — it says nothing about whether the degree of improvement is meaningful for the business, whether the improvement holds at the specific operating threshold needed, or whether performance is uniform across important subgroups. AUC = 0.72 on a medical diagnosis task where patients need TPR > 0.90 at 5% FPR is useless. Also check: is AUC 0.72 driven by good performance in the region of the ROC curve you actually operate in? A model with AUC=0.72 that does well at low FPR may outperform one with AUC=0.80 that only does well at high FPR. Always frame AUC in context of the operating requirement.`,
      },
      {
        q: `What is the relationship between AUC-ROC and the Mann-Whitney U statistic?`,
        a: `They are equivalent. The Mann-Whitney U statistic counts the number of (positive, negative) pairs where the positive is ranked higher than the negative, divided by the total number of such pairs. This is exactly P(score(positive) > score(negative)) — the probabilistic interpretation of AUC-ROC. This equivalence has a practical implication: you can compute AUC without drawing the ROC curve at all, just by counting concordant pairs. It also implies AUC is a non-parametric test of whether the two score distributions are separable, which is why it is robust to class imbalance in the ranking sense (though not in the precision sense).`,
      },
    ],
    takeaway: `The key insight is that AUC-ROC measures ranking quality across all thresholds and denominates FPR with TN, which means it systematically overstates model quality when negatives vastly outnumber positives — in those cases you must use PR-AUC and evaluate precision at your actual operating recall.`,
    interactiveId: 'roc_curve_viz',
  },
  {
    id: 'ranking_metrics',
    title: 'Ranking Metrics',
    subtitle: 'NDCG, MAP, MRR — search and recommendation quality',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['NDCG', 'MAP', 'MRR', 'ranking', 'RecSys'],
    summary: `Ranking metrics evaluate the quality of ordered lists, used in search engines, recommendation systems, and information retrieval. Unlike classification metrics, they penalise a system for placing relevant results lower in the list — a relevant result at rank 1 is worth more than the same result at rank 5. MRR (Mean Reciprocal Rank) is the simplest: it cares only about where the first relevant result appears, making it right for question-answering systems where one correct answer is enough. MAP (Mean Average Precision) rewards finding all relevant items and finding them early, making it correct for multi-document retrieval. NDCG (Normalised Discounted Cumulative Gain) is the most general — it handles graded relevance scores, not just binary, and discounts the value of relevance at lower ranks using a logarithmic decay that models user attention drop-off. All three metrics are position-sensitive, which also means they are vulnerable to position bias in the labels used to compute them — clicks are not the same as relevance.`,
    keyPoints: [
      `MRR = (1/|Q|) sum 1/rank_first_relevant. Only the rank of the first relevant result matters. MRR is appropriate when there is one best answer (FAQ systems, navigational queries) but inappropriate when there are multiple relevant items — it ignores everything after the first hit. The failure mode is that MRR=1.0 on a query where only result 1 is correct looks the same whether results 2-10 are good or bad.`,
      `MAP (Mean Average Precision): Average Precision for a single query is (1/R) × sum_k [P@k × rel(k)] where R is total relevant items and rel(k) is 1 if rank k is relevant. AP rewards finding relevant items early AND finding all of them. MAP averages AP across queries. The failure mode: MAP equally weights each query, so a query with 1 relevant item and a query with 100 relevant items contribute equally to MAP even though the latter is far harder to optimise.`,
      `NDCG = DCG/IDCG where DCG = sum_i (2^rel(i) − 1)/log_2(i+1). IDCG is the DCG of the perfect ranking. The 2^rel formula amplifies differences in high relevance scores — a highly relevant result is worth much more than a somewhat-relevant one. NDCG normalises to [0,1] making it comparable across queries with different numbers of relevant items. This is why NDCG is the standard in commercial web search and recommendation.`,
      `The discount function log_2(i+1) means position 1 counts fully, position 2 at 63%, position 10 at 29%, position 100 at 15%. This models user attention decay: most users click on top results and ignore later ones. Choosing a different discount function (e.g., linear decay) would give different rankings of systems — the specific function should match observed user behaviour.`,
      `NDCG@K truncates at rank K — only the top K results matter. In practice K=10 for web search (one page of results), K=5 or K=20 for recommendations. Using NDCG@K forces you to decide what depth matters for your users; it also makes the metric insensitive to ordering beyond position K, which is both a feature (matches user behaviour) and a risk (ignores quality below the fold).`,
      `Position bias: users click results at position 1 more often than position 5 not just because position 1 is more relevant but because it is at position 1. Click-based labels therefore have systematic position bias. Using raw clicks to compute NDCG produces a metric that rewards returning already-popular items regardless of true relevance. Inverse Propensity Scoring (IPS) weights clicks by 1/P(click at position k) to de-bias the metric.`,
      `Graded vs binary relevance: MAP and MRR require binary relevance (relevant or not). NDCG supports continuous or ordinal relevance scores (e.g., 0=irrelevant, 1=somewhat relevant, 2=relevant, 3=highly relevant). Graded relevance is more expressive but requires human raters or implicit feedback with gradient — in practice, commercial systems use 4- or 5-point scales from human judges for offline evaluation.`,
      `Offline vs online ranking evaluation: NDCG, MAP, MRR are all offline metrics computed on historical labels. They do not capture user satisfaction directly. A model can improve NDCG by 5% while decreasing long-session engagement. Online interleaving tests (where two rankers results are merged and user clicks determine the winner) are 100x more sensitive than A/B tests and can detect ranking differences that offline metrics miss.`,
    ],
    checkQuestions: [
      {
        q: `A search engine retrieves 3 relevant documents at ranks 1, 3, 5 out of 5 total relevant documents. Compute AP.`,
        a: `AP = (1/R) × sum_k [P@k × rel(k)] where R=5. At rank 1: relevant, P@1 = 1/1 = 1.0. At rank 3: relevant, P@3 = 2/3. At rank 5: relevant, P@5 = 3/5. Non-relevant ranks contribute 0. AP = (1/5) × (1.0 + 2/3 + 3/5) = (1/5) × (1.0 + 0.667 + 0.6) = (1/5) × 2.267 = 0.453. Note: if those same 3 relevant documents had appeared at ranks 1, 2, 3, AP = (1/5)(1 + 1 + 1) = 0.60. The difference shows how much AP penalises finding relevant items later.`,
      },
      {
        q: `Your recommendation system shows NDCG@10 = 0.85 in offline evaluation. But users complain results feel irrelevant. What might explain this?`,
        a: `Several mechanisms: (1) Offline NDCG was computed using click-based relevance labels, which have position bias — high-ranked items get more clicks regardless of true quality, and the metric optimises for this spurious signal. (2) The offline held-out test set may not reflect current user intent or new content (temporal shift). (3) NDCG@10 is an average over all queries — it may be 0.98 on common queries and 0.40 on tail queries, masking poor performance for specific user segments. (4) The metric captures relevance at the item level but not at the session level — users may find each item individually relevant but the set redundant. Consider running an interleaving experiment or computing diversity metrics alongside NDCG.`,
      },
      {
        q: `MRR is 0.60 for a search system. A manager wants to improve it to 0.75. What does this mean concretely and what would you change?`,
        a: `MRR = 0.60 means on average, the first relevant result appears at rank 1/0.60 ≈ 1.67 — users typically find their first relevant result somewhere between position 1 and 2. To reach MRR = 0.75, you need to push the average to rank 1/0.75 ≈ 1.33 — essentially making position-1 the first relevant result for more queries. To improve MRR: (1) Identify query categories where first relevant result is at rank 3 or higher and focus feature engineering there. (2) Improve query understanding (entity recognition, intent classification) for queries where the top result is a semantic mismatch. (3) Improve your retrieval recall so the re-ranker has the right document available to put first. MRR only cares about the first hit, so improvements beyond position 1 will not affect it.`,
      },
      {
        q: `You have graded relevance labels (0-3) and want to compare two rankers. Should you use MAP or NDCG?`,
        a: `Use NDCG. MAP requires binary relevance — it cannot use graded scores and any binarisation (e.g., threshold at 2) discards information. NDCG with the formula 2^rel(i) amplifies differences between highly relevant (rel=3, contribution 7) and somewhat relevant (rel=1, contribution 1) items. A ranker that puts 3s at the top beats one that puts 1s at the top by a large NDCG margin, which correctly captures that the first ranker is much better. MAP with binarised relevance would show the same rank if both rankers put all rel>=2 items above rel<2 items. For graded relevance, NDCG is strictly more informative than MAP.`,
      },
    ],
    takeaway: `The key insight is that all ranking metrics embed a model of user patience — MRR says users stop after the first hit, MAP says they care equally about each relevant item, NDCG says their attention decays logarithmically — so choosing the metric means choosing which user behaviour model you believe, not just which formula is standard.`,
  },
  {
    id: 'offline_vs_online',
    title: 'Offline vs Online Evaluation',
    subtitle: 'Proxy metrics, A/B gap, how to close the offline-online divide',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['evaluation', 'A/B testing', 'online metrics', 'proxy metrics'],
    summary: `Offline evaluation uses historical data with held-out labels to compute metrics like AUC, NDCG, or F1 before the model touches users. Online evaluation — A/B tests and multi-armed bandits — measures actual user behaviour when the model is live. A model can improve offline metrics by 5% while simultaneously degrading online metrics like session length, revenue, or user satisfaction, and this offline-online gap is one of the most expensive failure modes in applied ML. The gap exists because offline labels are proxies for real outcomes (clicks are not the same as satisfaction), because the model's predictions in production change what data is collected (feedback loops), and because the held-out test set is drawn from a static historical distribution that does not match live user behaviour. Closing the gap requires two things: carefully validating that your offline proxy metric correlates historically with online outcomes, and running A/B tests as the final arbiter of value. Guardrail metrics — secondary online metrics you must not harm — prevent Goodharting a single primary metric at the expense of the overall system.`,
    keyPoints: [
      `Offline-online gap root causes: (1) Distribution shift — production traffic has different characteristics from historical test data (new users, new content, seasonal patterns). (2) Feedback loops — the model influences what data is generated; a model that always shows popular items trains the next model on popular item signals. (3) Proxy label problem — offline labels (clicks, ratings, purchases) measure something correlated with satisfaction but are not satisfaction. (4) The counterfactual problem — offline evaluation can only score items that were shown to users under the old policy.`,
      `Counterfactual offline evaluation: if you have a logged dataset from an old policy, you cannot straightforwardly evaluate a new policy because you only have reward signals for items the old policy served. Inverse Propensity Scoring (IPS) reweights observations by 1/propensity_score to correct for the old policy's bias. Used at Yahoo and Netflix. Requires knowing or estimating the logging policy's action probabilities.`,
      `Guardrail metrics are online metrics you must not harm even if the primary metric improves. E.g., improve CTR but do not let session duration drop below the control level. Guardrails prevent metric Goodharting — a model might learn to generate sensationalist content that gets clicks but causes user dissatisfaction and long-run churn. Having 3-5 guardrails and requiring the test to not harm any of them is standard practice at large platform companies.`,
      `Interleaving is 10-100x more statistically sensitive than standard A/B testing for ranking experiments. Results from two rankers are merged into a single list shown to the user (with deduplication); which rankers items get clicked determines the winner. Because each user sees both rankers simultaneously, individual user variance is eliminated — you only need hundreds of users instead of hundreds of thousands to detect the same effect size.`,
      `Proxy metric calibration: track offline metric changes alongside realised online metric changes over historical model deployments. Build a calibration map: offline NDCG +0.5% historically predicts online CTR +0.3%. This lets you set a minimum offline improvement threshold before running an A/B test. The calibration should be updated whenever the system architecture changes significantly.`,
      `Novelty and primacy effects contaminate A/B test results. Users engage more with new features simply because they are new (novelty effect) or stick with familiar features even when the new one is better (primacy effect). Both distort early test results. Standard practice: run tests for at least 2 full weeks to let novelty effects stabilise and check for non-stationarity in the treatment effect estimate over time.`,
      `Leading vs lagging indicators: CTR and clicks are leading indicators — fast feedback, but weakly correlated with long-term value. Retention, LTV, and subscription renewal are lagging indicators — strongly correlated with value, but take weeks to observe. Optimise for leading indicators in the short term but validate that improvements translate to lagging indicators on the time horizon that matters for the business.`,
      `The fundamental asymmetry of A/B testing: you can only measure the effect of models you actually test. Selection of what to test is a human decision that A/B tests cannot optimise. A rigorous offline evaluation pipeline is what filters model candidates down to a small set worth running A/B tests on — the goal of offline metrics is to kill bad models cheaply, not to prove good ones.`,
    ],
    checkQuestions: [
      {
        q: `Your RecSys shows +3% NDCG@10 offline. You run an A/B test and see -1% on session length. What do you do?`,
        a: `Session length is likely a guardrail metric — do NOT ship. The offline NDCG improvement did not translate to a real user improvement; it degraded engagement. The first investigation: did NDCG improve on click-based labels that have position bias, meaning the model learned to rank already-popular items first? Second: check if the model optimised for initial clicks but delivers content that users do not continue watching. Third: is session length confirmed as a guardrail or is it an incidental dip? If it is a confirmed guardrail violation, the model has an alignment problem — NDCG is not aligned with engagement. Go back and either redesign the offline metric using satisfaction labels if available, or redesign the model objective.`,
      },
      {
        q: `What is the difference between running an A/B test and running an interleaving experiment? When would you choose each?`,
        a: `A/B test routes each user to exactly one version — user-level randomisation. Effect is measured by comparing aggregate metrics across groups. Requires large sample sizes because between-user variance dominates. Interleaving shows each user results from both systems interleaved in one list; user preference signal (which items they click) directly measures system quality. Interleaving eliminates between-user variance, reducing required sample by 100x for the same power. Choose A/B when you need to measure absolute effects on business metrics (revenue, retention, CTR) — interleaving only measures relative ranker preference, not business impact. Choose interleaving when comparing ranking quality cheaply during model development or when you want to cheaply confirm a new ranker is better before running a full A/B test.`,
      },
      {
        q: `Your team calibrated that +1% NDCG gain historically predicts +0.4% CTR online. You see a model with +5% NDCG. Should you trust the calibration and skip the A/B test?`,
        a: `No. Calibration is based on historical model changes that may not be representative of this specific change. A +5% NDCG gain is unusually large and may indicate the model improved on historical data distribution but not on the current one. Large offline gains sometimes come from distribution mismatch, data leakage, or metric hacking. The calibration model itself has uncertainty — a +5% NDCG prediction should come with wide confidence intervals on the expected CTR gain. The A/B test is cheap relative to the cost of shipping a broken model. The correct use of calibration is to triage: only run A/B for models with offline gain above a minimum threshold; never use calibration to skip the A/B entirely.`,
      },
      {
        q: `A competitor product uses pure online bandit for model selection instead of A/B tests. What are the trade-offs?`,
        a: `Bandits adaptively route traffic to better-performing variants, reducing regret (total lost value from not always serving the best arm). A/B tests fix allocations upfront and cannot exploit interim results. Bandits are better when the cost of serving an inferior arm is high and you want to minimise harm. But bandits have significant downsides for evaluation: they cannot isolate clean causal estimates because allocation is endogenous (correlated with performance); they struggle with delayed feedback signals (cannot adapt on 30-day retention signals); and they can produce biased estimates due to adaptive stopping. For rigorous causal evaluation of model quality and business impact, A/B tests with pre-registered metrics and fixed duration are preferable. Bandits are better for production optimisation (e.g., ad creative selection) than for model validation.`,
      },
    ],
    takeaway: `The key insight is that offline metrics measure how well a model scores historical data that was collected under a different policy, which means the offline-online gap is not random noise but a systematic bias, and closing it requires either correcting for the logging policy (IPS) or running A/B tests as the true ground truth.`,
  },
  {
    id: 'validation_traps',
    title: 'Validation Set Traps & Data Leakage',
    subtitle: 'Leakage taxonomy: temporal, group, label leakage, how to audit',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['data leakage', 'validation', 'temporal split', 'label leakage'],
    summary: `Data leakage is the accidental inclusion of information from the test period or from the future into the training features, producing models that appear to perform exceptionally well offline but fail badly in production. Leakage is the single most common cause of the offline-production gap in practice — a practitioner who has not been burned by it has simply not worked on enough real-world ML projects. It takes four principal forms: temporal leakage (future information in training features), group leakage (same entity in both train and test), label leakage (a feature derived from or collected after the outcome), and preprocessing leakage (normalisation statistics computed on the full dataset before splitting). Detecting leakage requires asking one question for every feature: "Would this value be available at the time of prediction in production?" If the answer is no, it is leakage. Suspiciously high AUC (above 0.97 on a hard business problem), features with surprisingly high importance, and near-zero training error are the three strongest signals that leakage is present.`,
    keyPoints: [
      `Temporal leakage is the most common form in time-series ML: training on data from after the event you are trying to predict. A churn model trained with a random row-level split will contain future purchase data in training features for a user whose churn label was determined earlier. Always split data by time, not randomly, and ensure every feature is computed using only information available at the prediction time.`,
      `Group leakage: the same user, patient, session, or entity appears in both train and test. The model memorises entity-specific patterns (e.g., user browsing style, patient medication history) that do not generalise to new entities. Fix with group k-fold or a user-level holdout. Group leakage is especially dangerous in user engagement models where individual user patterns are strong predictors.`,
      `Label leakage: a feature is derived from or collected after the label was determined. Example: using "number of days in ICU" to predict "was the patient hospitalised?" The feature contains the answer. This is different from temporal leakage because the feature may have the right timestamp but the wrong causal direction — it is a consequence of the label, not a predictor of it. The audit question: is this feature causally upstream of the outcome, or downstream?`,
      `Preprocessing leakage: fitting a StandardScaler, mean imputer, or OneHotEncoder on the full dataset (including test) before splitting. The test set's mean and variance leak into the scaler that processes training data, making the model slightly overfit to test distribution. Fix: fit all transformers on training only; use sklearn Pipeline to enforce this. Target encoding leakage (computing target mean per category on the full dataset) is particularly severe and requires out-of-fold estimation.`,
      `Detection signals: (1) Validation AUC above 0.97 on a problem where even experts disagree — almost always leakage. (2) A feature has dramatically higher importance than all others — inspect whether that feature can be known at prediction time. (3) High Pearson correlation between a feature and the label (above 0.7) — suspect it contains the label. (4) Model accuracy is nearly identical on train and validation (no generalisation gap) — suspect the test set has been contaminated.`,
      `The point-in-time correct feature store: in production, features should be computed at the exact timestamp of prediction and logged. The training dataset is reconstructed by joining each historical event with the feature values that existed at that timestamp. Systems that do not maintain this (e.g., joining on latest feature values instead of historical) produce temporal leakage at scale. This is why feature stores with time-travel capability (Feast, Tecton, Hopsworks) are operationally important.`,
      `The leakage audit procedure: for each feature in your training data, write down (1) how it is computed, (2) what data sources it uses, (3) at what timestamp it would be available in a real production scoring scenario. Cross-reference against the label creation time. Any feature where computation time is after label time is a candidate for leakage. Automate this check in your feature engineering pipeline.`,
      `Leakage in NLP: if you preprocess a corpus (build a vocabulary, fit a TF-IDF vectoriser, or fine-tune a tokeniser) on the full dataset before splitting, the test set's vocabulary distribution leaks into the tokeniser. Similarly, if your train/test split splits documents from the same author, article series, or domain cluster, the model exploits author-style patterns rather than learning general language.`,
    ],
    checkQuestions: [
      {
        q: `You build a churn prediction model with AUC=0.97. In production, AUC drops to 0.64. What went wrong and how do you debug?`,
        a: `A drop from 0.97 to 0.64 is a strong signal of data leakage, not model degradation. Debug steps: (1) Check the train/test split — was it a random row-level split on time-series data? Re-split by time and re-evaluate. (2) Audit the top-5 most important features — are any of them computable only after the churn event? E.g., "account closed = 1" used as a feature when predicting churn. (3) Check preprocessing — was scaling or imputation fit on the full dataset? (4) Check for group leakage — does the same user appear in both train and test? (5) Look for any feature with correlation above 0.7 with the label. In production, the AUC of 0.64 is the model's true performance — the 0.97 was an illusion created by knowing the answer.`,
      },
      {
        q: `A colleague uses a random 80/20 split on a dataset of 500,000 website visits by 50,000 unique users. What is the problem and how do you fix it?`,
        a: `With a random split, each user appears in both train (80%) and test (20%) on average. The model learns user-level patterns — browsing style, device type, time patterns — that are highly predictive for that specific user but do not generalise to new users. In production, the model will score new users it has never seen, so this is direct group leakage. Fix: split 40,000 users into training, 10,000 users into test. All visits from a user go to exactly one split. The resulting test AUC will be lower than the random-split AUC and that is the correct, honest performance estimate. For time-series visit data, additionally split by time within each user-split.`,
      },
      {
        q: `What is target encoding leakage and how does k-fold target encoding fix it?`,
        a: `Target encoding replaces a categorical variable's value with the mean target (e.g., mean conversion rate) for that category. If you compute this mean using the full dataset (including the test samples), the encoding for each test sample incorporates its own label's information — the test sample literally leaks its label into its own feature. This makes the feature a near-perfect predictor, inflating train and validation metrics dramatically. K-fold target encoding fixes this by computing the encoding for each fold using only out-of-fold data: for each training sample in fold k, compute the mean target using all other folds. At test time, use the mean computed from all training data. This ensures no sample's label is used to compute its own encoding.`,
      },
      {
        q: `You join a company and are handed a model achieving AUC=0.94 on historical data. How do you quickly audit for leakage before trusting this number?`,
        a: `Three quick checks: (1) Ask how the train/test split was done — if the answer is "random rows" on time-series data, the number is probably wrong. Re-split by time and re-evaluate. (2) Print feature importances — if one feature has 60%+ importance on a problem where no single feature should dominate, inspect it. Pull up 5-10 samples and check if that feature's value could be known at prediction time. (3) Compare AUC on train vs test — if train AUC is also 0.94 with no gap, either the model is perfectly calibrated (unlikely) or the test set has contamination. In practice, leakage from a random split on event-level time series data is the most common finding in this scenario.`,
      },
    ],
    takeaway: `The key insight is that data leakage is not a modelling error but a data engineering error, which means the fix is almost always upstream in the feature computation pipeline — asking "would this feature value exist at the exact moment of prediction?" is the single question that catches most leakage.`,
  },
  {
    id: 'cross_validation',
    title: 'Cross-Validation Strategies',
    subtitle: 'k-fold, stratified, group k-fold, time-series CV, nested CV',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['cross-validation', 'k-fold', 'time series', 'nested CV'],
    summary: `Cross-validation estimates how well a model generalises to new data by training and evaluating on multiple non-overlapping subsets of the data. A single train/val split has high variance — you might get lucky or unlucky with a particular partition. K-fold CV uses the data k times, rotating which fold is held out, and averages the results, dramatically reducing that variance. But k-fold is only valid when observations are independent and identically distributed — the moment you have temporal structure, entity structure, or hierarchical structure, naive k-fold produces an overoptimistic estimate because information leaks across folds. The right CV strategy mirrors exactly how the model will be evaluated in production: if the model scores new users it has never seen, use group k-fold; if it scores future events, use time-series walk-forward CV; if you are tuning hyperparameters and evaluating simultaneously, use nested CV to prevent selection bias from contaminating the evaluation.`,
    keyPoints: [
      `Standard k-fold: partition data into k equal folds, train on k-1, test on 1, rotate. k=5 or k=10 is the standard. k=5 uses 80% of data for training on each fold (more data, lower bias) vs k=10 uses 90% (lower bias, higher variance estimate, slower). LOO (k=n) is unbiased but slow and has high estimate variance. The failure mode is using k-fold on time-series data — you are training on future data to predict the past.`,
      `Stratified k-fold preserves class proportions in each fold. With 5% positive class and 5 folds, each fold will have approximately 5% positives. Without stratification, a fold might have 0 positives by chance, producing a meaningless evaluation on that fold. Essential for imbalanced classification. In sklearn, StratifiedKFold is the default for classification tasks.`,
      `Group k-fold: all samples from the same group (user, patient, document, session) go to the same fold. This simulates what happens in production: the model is evaluated on entities it has never seen during training. Without this, group-level patterns (e.g., user browsing style) can cause massive overfitting that is invisible in naive k-fold. The key question: in production, will this model be applied to groups it has seen in training? If no, use group k-fold.`,
      `Time-series walk-forward CV: train on [0, t1], validate on [t1, t2], then train on [0, t2], validate on [t2, t3], etc. Never train on future data. Expanding window (grow training set with each fold) is standard for most applications. Rolling window (fixed training size) is used when concept drift means old data is less valuable than recent data. The failure mode is using standard k-fold — a fold trained on months 3-12 to predict month 1 is predicting the past from the future.`,
      `Purged and embargoed time-series CV: with autocorrelated features (rolling averages, lag features), even with temporal splitting, the tail of the training period influences the head of the validation period through the feature computation window. Purging removes training samples too close to the validation start; embargoing removes validation samples too close to the training end. Critical in financial ML where feature windows can span months.`,
      `Nested CV separates hyperparameter selection from generalisation estimation. Outer loop (k-fold) evaluates model quality; inner loop (another k-fold within each training fold) selects hyperparameters. Without nesting, if you tune hyperparameters on the validation set and then report metrics on the same validation set, you have selection bias — the model was implicitly optimised for that specific held-out set. Nested CV gives a conservative but unbiased estimate of the whole model-selection pipeline.`,
      `CV for large datasets: with millions of samples, k-fold is slow and variance from data is already low. A single 90/10 temporal split is often sufficient and can be re-evaluated quickly. Save k-fold for cases where data is limited (below 10,000 samples) or where the variance of a single split would be high. Time-series data always requires temporal splitting regardless of size.`,
      `Repeated CV: run the full k-fold procedure r times with different random splits, giving k×r model evaluations. Reduces variance of the CV estimate. Standard in medical ML where sample sizes are small and a single 5-fold result would have high uncertainty. Report mean ± std across repeats.`,
    ],
    checkQuestions: [
      {
        q: `You have 5 years of daily transaction data and want to build a fraud model. How do you set up CV?`,
        a: `Use time-series walk-forward CV. Structure: train on year 1, validate on months 1-3 of year 2. Then train on year 1 + Q1 of year 2, validate on Q2. Continue sliding forward. Add a 7-day purge gap between train end and validation start to prevent leakage through 7-day rolling features. Use expanding window (keep all historical data) unless you suspect strong concept drift in fraud patterns, in which case use a rolling 12-month window. Group by user_id: all transactions for a user in the validation period should not have the same user's transactions in the training folds if your goal is to detect fraud from new users. Never use standard k-fold — you would train on 2024 data to predict 2022 events.`,
      },
      {
        q: `You run 5-fold CV and get AUC scores [0.83, 0.84, 0.92, 0.85, 0.83]. The mean is 0.854. Should you report this as your model's performance?`,
        a: `The fold with AUC=0.92 is a significant outlier — about 8 points above the others. Before reporting, investigate why that fold is different. Possibilities: (1) That fold's validation set happened to contain an unusually easy subset of examples — check class distribution in each fold. (2) The training set for that fold was unusually informative. (3) There is a data quality issue in one fold. If the outlier is genuine, report mean ± std = 0.854 ± 0.035 to convey the uncertainty. The high variance itself is information — it tells you the model's performance is unstable across data subsets, which matters for production. Do not just report the mean without the standard deviation.`,
      },
      {
        q: `A colleague argues that since you are doing hyperparameter search with Optuna and reporting the best trial's validation score, you have a proper unbiased evaluation. Explain why this is wrong.`,
        a: `This is the nested CV problem. When you run Optuna for 200 trials and report the best trial's validation AUC, you have searched over 200 models and selected the one with the highest validation score. The validation AUC of the best trial is upward-biased by selection — you happened to find a configuration that did well on this specific validation set. The true generalisation performance is lower. To get an unbiased estimate: use nested CV where hyperparameter search is done in the inner loop on the training fold only, and the outer fold is a held-out test set that neither Optuna nor you ever touched during optimisation. Alternatively, hold out a separate test set before starting Optuna and only evaluate on it once, after all tuning is complete.`,
      },
    ],
    takeaway: `The key insight is that every CV strategy embeds an assumption about how the model will be deployed — temporal splitting embeds "future data is unavailable," group splitting embeds "new entities appear in production" — and using the wrong strategy produces an evaluation that tests a different deployment scenario than the real one.`,
  },
  {
    id: 'error_analysis',
    title: 'Error Analysis',
    subtitle: 'Confusion matrix drill-down, error slicing, systematic failures',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['error analysis', 'failure modes', 'slicing', 'debugging'],
    summary: `Error analysis is the systematic study of where and why a model fails. Aggregate metrics like AUC or F1 collapse all errors into a scalar that hides the structure of what went wrong — a model with F1=0.85 may be failing catastrophically on a critical subgroup. Error analysis decomposes model failures into categories: which classes are being confused, which data segments have elevated error rates, which errors are confident (and therefore most dangerous), and which are borderline ambiguous cases. The output of a good error analysis is not a number but a prioritised list: "fix X and you recover Y% of errors." It is the primary tool for deciding what to do next after getting a first model working — more data, better features, different architecture, or label cleanup. Without systematic error analysis, engineering decisions about how to improve a model are guesswork.`,
    keyPoints: [
      `Start with the confusion matrix: examine off-diagonal cells. Which class pairs are being confused? A model that confuses "cat" with "small dog" has a different failure mode than one that confuses "cat" with "car." The confusion matrix tells you the error structure; the aggregate metric tells you nothing about structure. In multi-class problems, the off-diagonal cells are more informative than the diagonal.`,
      `Error slicing: compute error rate (or your primary metric) per value of each feature — by time of day, user country, product category, input length, etc. A model with average F1=0.85 may have F1=0.40 for users in one country or for long inputs. These segments are the most actionable finding from error analysis because they point directly to what data or features to add. This is more informative than asking "why is the model wrong?" broadly.`,
      `Confidence-error calibration: plot error rate (y-axis) vs model predicted probability (x-axis). An overconfident model has high error rate even at predicted probability 0.9 — those confident wrong predictions are the most dangerous in production. Identify the confidence threshold below which accuracy degrades sharply. Consider using uncertainty estimates (MC dropout, ensemble variance) to flag high-uncertainty predictions for human review.`,
      `Manual inspection protocol: randomly sample 50-100 FPs and FNs separately. For each, assign to one of 3-5 error buckets: (a) labelling error — the true label is wrong; (b) insufficient features — the model lacks information available to a human; (c) rare pattern — the model has not seen enough examples of this type; (d) genuine ambiguity — even a human would struggle. The distribution across buckets is actionable: bucket (a) means relabel, bucket (b) means feature engineering, bucket (c) means data collection, bucket (d) means accept as irreducible error.`,
      `Systematic vs random errors: if errors are uniformly random (no feature-correlated pattern), they are likely irreducible given current features and more data will help only marginally. If errors cluster in a subgroup (e.g., all errors involve texts shorter than 20 tokens), you need targeted intervention — more data from that subgroup or a separate model for it. The distinction between systematic and random errors is the most important output of error analysis.`,
      `Error correlation and ensemble diversity: if you have two models with similar overall accuracy, check whether their errors overlap. Two models that are wrong on the same examples will produce the same ensemble as either model alone. Two models that are wrong on different examples will cancel each other's errors when ensembled. Error analysis informs model diversity — you want ensembles whose error patterns are as complementary as possible.`,
      `Production error monitoring: log each prediction, the model's confidence, the input features, and (when available through delayed feedback) the true label. Build dashboards that slice error rate by feature buckets in real time. Set alerts for any bucket where error rate exceeds a threshold (e.g., alert if error rate in any country exceeds 30% over a 1-hour window). This is error analysis applied continuously, not just once after training.`,
      `The error decomposition hierarchy: (1) compute overall metric — is there a problem? (2) slice by output class — which classes are failing? (3) slice by input features — which segments are failing? (4) examine individual errors — what kind of errors are they? (5) form hypotheses and test them with targeted experiments. Skipping levels is the mistake — going from "F1=0.75" directly to "we need a transformer" skips the diagnosis that might reveal the problem is actually a labelling error in the training set.`,
    ],
    checkQuestions: [
      {
        q: `Your NLP classifier has 88% overall accuracy but stakeholders are unhappy. How do you diagnose what is wrong?`,
        a: `88% overall hides per-class and per-segment failures. Step 1: compute per-class precision and recall — which classes have low recall? A class with recall=0.40 means 60% of instances are misclassified, which stakeholders feel even if average accuracy is 88%. Step 2: slice by text length, domain, source, and date — any segment with significantly lower accuracy? Step 3: sample 100 errors and manually categorise into label errors, insufficient features, rare patterns, and genuine ambiguity. Step 4: check the confusion matrix — are errors concentrated between specific class pairs? Step 5: check confidence distribution on errors — are most errors low-confidence (model knows it does not know) or high-confidence (model is systematically wrong)? Each finding points to a different fix.`,
      },
      {
        q: `You sample 100 FPs from a fraud detection model and find that 60% involve transactions from a new merchant category launched 2 months ago. What do you do?`,
        a: `This is a systematic failure from distribution shift — the model was trained before this merchant category existed and has no representation for it. 60% is far too high to dismiss as random. Actions: (1) Collect labelled examples from this merchant category and add to training data. (2) Add a feature indicating "merchant category is new" as a signal that the model should be more uncertain. (3) Consider a separate lightweight model or rule for this category while the main model retrains. (4) Check if other new merchant categories also have elevated FP rates — if so, temporal weighting of training data (more weight to recent transactions) is the fix. (5) Add "new merchant category" as a monitored slice in production dashboards.`,
      },
      {
        q: `You have two models both with F1=0.82. How do you choose which to deploy using error analysis?`,
        a: `Identical F1 does not mean identical behaviour. Run error analysis on both: (1) Compare confusion matrices — does one model fail on a different class than the other? (2) Compare error slices — does one perform better on the segment that is most critical to the business? (3) Compare error confidence — which model is less overconfident on its errors (safer for downstream decision-making)? (4) Compare error correlation — if their errors are largely different, they may be worth ensembling. (5) Compare calibration — which model's predicted probabilities better match actual frequencies? Identical aggregate F1 with different error structures means the models are solving the problem differently and the choice should be guided by business risk tolerance for specific failure modes.`,
      },
      {
        q: `What is the difference between error slicing and subgroup fairness analysis? When does one become the other?`,
        a: `Error slicing is a debugging technique: slice error rate by any feature to find model blind spots, with the goal of fixing them. Subgroup fairness analysis is a normative assessment: check whether error rates are equitable across protected demographic groups (race, gender, age). They use the same technical procedure but have different intent and consequence. Error slicing asks "where is the model wrong so I can fix it?" Fairness analysis asks "is the model wrong more for certain groups in a way that causes harm?" When your error slices are by protected attributes and you find significantly different error rates, you have moved from debugging into fairness territory with legal and ethical implications. At that point the response changes from "collect more data" to also involving policy decisions, legal review, and potentially redesigning the model objective.`,
      },
    ],
    takeaway: `The key insight is that aggregate metrics tell you whether you have a problem, but error slicing tells you which specific subpopulation has the problem — and in practice, a model with good average metrics often has catastrophic failures concentrated in a small slice that the average hides completely.`,
  },
  {
    id: 'calibration',
    title: 'Calibration & Brier Score',
    subtitle: 'Reliability diagrams, Platt scaling, isotonic regression, Brier decomposition',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['calibration', 'Brier score', 'Platt scaling', 'reliability diagram'],
    summary: `Calibration measures whether a model's predicted probabilities match observed frequencies: a well-calibrated model that predicts P(y=1)=0.7 should be correct 70% of the time for those predictions. Discrimination (AUC) and calibration are independent properties — a model can rank positives above negatives perfectly (AUC=1.0) while being systematically overconfident or underconfident. Calibration matters enormously in decision-making contexts: if a doctor's risk model says 90% mortality probability but actual mortality at that score bin is 40%, the model is misleading clinical decisions. The Brier score is the mean squared error of probability predictions and jointly measures discrimination and calibration. Reliability diagrams visualise calibration by bucketing predictions and plotting mean prediction vs mean outcome. Post-hoc recalibration (Platt scaling, isotonic regression) can fix a discriminating but miscalibrated model without retraining. Modern neural networks are typically overconfident out of the box — temperature scaling is the standard fix.`,
    keyPoints: [
      `A model is calibrated if P(Y=1 | predicted_prob=p) = p for all p. Calibration is a marginal property — it does not require the model to be right on any individual prediction, only that p% of the predictions of probability p are correct. High-calibration does not imply high-discrimination; a model that always predicts the base rate is perfectly calibrated but useless.`,
      `Reliability diagrams: bin predictions into 10 equal-width or equal-frequency buckets (e.g., [0, 0.1], [0.1, 0.2], ...). For each bucket, plot mean predicted probability (x-axis) vs fraction of actual positives (y-axis). A perfectly calibrated model lies on the diagonal. Points above the diagonal: model is underconfident (predicts lower than actual). Points below: overconfident (predicts higher than actual). Neural networks typically show overconfidence.`,
      `Expected Calibration Error (ECE) = sum_b (n_b/n) |acc_b - conf_b| where n_b is bucket size, acc_b is bucket accuracy, conf_b is mean confidence. Weighted average absolute deviation from perfect calibration. Low ECE is necessary but not sufficient for good calibration — it can be low if all buckets are near the average even with individual miscalibration.`,
      `Brier score = (1/n) sum (predicted_i - y_i)^2 — mean squared error of probability predictions. Range [0, 1]; 0 is perfect. Brier score decomposes into resolution (how much predicted probs vary — discrimination) + reliability (calibration error) - uncertainty (irreducible error from base rate). A model with good AUC but poor calibration has high reliability component of Brier score. Use Brier skill score = 1 - BS/BS_ref to get a relative measure.`,
      `Platt scaling: train a logistic regression on top of model scores using held-out data. This adds two parameters (slope and intercept) that rescale the model's raw output to calibrated probabilities. Works well for sigmoid-shaped calibration errors, which are common in SVMs and boosting models. Requires a separate calibration set — do not calibrate on training data.`,
      `Isotonic regression for calibration: fits a non-decreasing step function mapping raw scores to calibrated probabilities. More flexible than Platt scaling — can correct non-monotonic miscalibration. Requires more calibration data (risk of overfitting on small datasets). Use Platt for small calibration sets, isotonic regression for large ones.`,
      `Temperature scaling (neural networks): divide logits by temperature T before the softmax. T > 1 produces softer (more uniform) probabilities (reduces overconfidence). T < 1 sharpens predictions. Optimal T is found by minimising NLL on a validation set. Simple one-parameter recalibration that does not change predictions (argmax is preserved), only probabilities. The standard recalibration method for neural networks.`,
      `When calibration matters most: (1) Medical and financial risk models where the probability itself is the output used in decision-making. (2) Downstream models that take the probability as an input feature. (3) Cost-sensitive decision making where different probability thresholds have different costs. Calibration matters less when you only care about ranking (e.g., top-k recommendation), where AUC is sufficient.`,
    ],
    checkQuestions: [
      {
        q: `A model has AUC = 0.91 and ECE = 0.15. What does this mean and what do you do?`,
        a: `AUC=0.91 means the model discriminates very well — it ranks positives above negatives 91% of the time. ECE=0.15 means the model's probabilities are systematically off by 15 percentage points on average — this is severe miscalibration. If the model predicts 0.8, actual probability is likely around 0.65 or 0.95. For any use case where the probability itself drives a decision (medical risk, pricing, fraud threshold), this is a serious problem. Fix: apply Platt scaling or isotonic regression on a held-out calibration set. Re-check ECE after recalibration. The high AUC tells you the underlying model is strong; the recalibration just adjusts the probability scale without changing rankings.`,
      },
      {
        q: `Your reliability diagram shows the model is overconfident at high probabilities (0.8-1.0 bucket shows actual positive rate of 0.55). What could cause this and how do you fix it?`,
        a: `Overconfidence in the high-probability bucket means when the model is very confident, it is actually correct only 55% of the time — its confidence is not warranted. Causes: (1) Model was trained with log loss which rewards confident predictions even when training data is noisy. (2) Neural network — modern NNs are systematically overconfident without calibration. (3) Insufficient training data in the high-probability regime. (4) Class imbalance causing the model to push all scores to extremes. Fix: (1) Temperature scaling — find T > 1 that brings the high-probability bucket down to match actual frequencies. (2) Label smoothing during training prevents the model from predicting extreme probabilities. (3) For tree models, use Platt scaling on a held-out validation set.`,
      },
      {
        q: `Two models: Brier score for Model A = 0.08, for Model B = 0.12 on the same dataset. Model B has higher AUC. How do you interpret this?`,
        a: `The Brier score jointly measures discrimination and calibration. Model A has lower Brier score (better) despite lower AUC than Model B — this means Model A is significantly better calibrated, which outweighs Model B's ranking advantage in terms of mean squared probability error. For applications where probability quality matters (risk scoring, cost-sensitive decisions), Model A may be preferred despite lower AUC. For applications where only ranking matters (recommendation, top-k retrieval), Model B is better. You can decompose the Brier score into resolution (discrimination) and reliability (calibration) components to see which effect is larger and whether recalibrating Model B would close the gap.`,
      },
    ],
    takeaway: `The key insight is that AUC measures whether a model ranks correctly while calibration measures whether its probability estimates are honest, and these are independent — so for any application where the probability output drives a real-world decision, calibration must be evaluated and fixed separately from discrimination.`,
  },
  {
    id: 'ablation',
    title: 'Ablation Studies & Baselines',
    subtitle: 'Designing ablations, good baselines, isolating contributions',
    difficulty: 'intermediate',
    estimatedMin: 26,
    tags: ['ablation', 'baselines', 'experiment design'],
    summary: `Ablation studies systematically remove or disable components of a model or pipeline to measure each component's contribution. They answer the essential engineering question: "how much does each piece matter, and what can I remove without paying a performance cost?" A rigorous ablation study is required for credible ML research and is equally important in production engineering — a complex pipeline with an unknown contribution per component is fragile and expensive to maintain. The counterpart to ablation is baseline selection: the baseline defines the bar a complex model must clear to justify its complexity. Choosing too weak a baseline makes a contribution look larger than it is; choosing the strongest relevant baseline forces honest measurement. Good ablation and baseline practice are the difference between ML engineering that produces actionable insights and ML engineering that produces spurious paper gains.`,
    keyPoints: [
      `Ablation design: start from the full system, remove exactly one component per experiment. The performance gap between full system and ablated system is that component's contribution. Removing two components simultaneously makes attribution impossible — you cannot tell which removal caused the change. Ablation is the method of scientific control applied to ML systems.`,
      `Confounding in ablations: the most common error is changing the architecture and hyperparameters simultaneously. If you remove a layer and also retune learning rate, you cannot attribute the performance change. Run all ablations with the same hyperparameters. This means ablated models may be suboptimal for their specific architecture — that is acceptable; you are measuring the value of the component to the full system, not optimising each variant.`,
      `Baselines hierarchy: random -> majority class -> simple rule-based -> linear model -> single tree -> gradient boosting -> your model. In research: also compare against (1) best published result on the same benchmark, (2) strong reimplemented baseline. The most common malpractice in ML research is comparing against an undertuned or outdated baseline. If your method only beats a weak baseline, the contribution may not exist against a properly tuned competitor.`,
      `Feature ablation: remove one feature group at a time and measure performance impact. This is not the same as feature importance from a tree model — permutation-based feature importance estimates marginal contribution assuming other features are present, while ablation captures the contribution including all interaction effects with removed features. Use ablation to decide which feature groups to invest in and which to sunset.`,
      `Data ablation (learning curves): train with 10%, 25%, 50%, 75%, 100% of training data and plot performance vs dataset size. If the curve is still steep at 100%, more data will help significantly — data collection is the bottleneck. If the curve has flattened, more data will not help much — model capacity or features are the bottleneck. Learning curves are the most informative analysis for deciding where to invest engineering effort next.`,
      `Negative result ablations: if removing a component does not hurt performance (or improves it), that component is dead weight. Remove it. Simpler models are faster to train, faster to serve, cheaper to maintain, and often generalise better because they have fewer ways to overfit. Publishing negative ablations ("this technique we tried did not help") is scientifically valuable even when not publishable on its own.`,
      `Seed variance: run each ablation condition with 3-5 different random seeds and report mean ± std. A result that does not survive seed variance is not a real result. A claimed +2% improvement with std=3% is noise. In deep learning, seed variance is high enough that single-run ablations are unreliable. This is especially important for NLP tasks where random initialisation can dominate small dataset effects.`,
    ],
    checkQuestions: [
      {
        q: `Your paper claims that adding a cross-attention layer improves NDCG by 2%. A reviewer asks for an ablation. What do you run?`,
        a: `Run four conditions: (1) Full model with cross-attention — the reported number. (2) Full model without cross-attention — baseline contribution. (3) Full model with self-attention replacing cross-attention of equal parameter count — to test whether cross-attention specifically is needed vs just added parameters. (4) Full model with a feedforward layer of equal parameters — to test whether any additional capacity helps, ruling out the "more parameters = better" explanation. All four run with the same hyperparameters (no retuning), same train/test split, same evaluation protocol, reported as mean ± std over 3 seeds. If condition 1 beats conditions 3 and 4 significantly, you have demonstrated that cross-attention specifically contributes. If conditions 3 or 4 match condition 1, the contribution is capacity, not architecture.`,
      },
      {
        q: `You run a feature ablation and find removing "user age" drops F1 from 0.85 to 0.78 (a large drop). But age is a protected attribute. How do you reason about this?`,
        a: `The feature ablation tells you age is highly predictive in the current model — but predictive does not mean it should be used. Three separate questions: (1) Is using age legally permitted in this application? (2) Is age the true causal driver, or is it a proxy for other features (credit history length, income stability) that should be modelled directly? (3) What are the fairness implications — does the model treat people differently by age in ways that cause disproportionate harm? If legal, explore whether the F1 drop is coming from age per se or from correlated features that age is proxying for. Add those features explicitly and re-run the ablation to see if age then becomes less predictive. If the age contribution persists after controlling for non-protected proxies, you face a genuine fairness-accuracy trade-off that requires policy input.`,
      },
      {
        q: `You remove component X from your system and AUC goes up from 0.82 to 0.84. What do you conclude and what do you do next?`,
        a: `Component X is either hurting performance (contributing noise or introducing a regularisation mismatch) or is redundant (its information is already captured by other components). Before concluding: (1) Is the improvement within seed variance? Run 5 seeds and check if the gap is consistent. (2) Is component X designed for a different objective — e.g., a diversity penalty that hurts AUC but improves user satisfaction? (3) Check whether removing X makes the system simpler without harming other metrics. If the improvement is real and consistent, remove X — the simpler system is better. Report this as a negative ablation result: the component was expected to help but did not, and removal improved performance. This is scientifically interesting because it challenges the assumption behind X's inclusion.`,
      },
    ],
    takeaway: `The key insight is that ablation studies are the only way to know what is actually contributing to your model's performance versus what is cargo-culted complexity, which means skipping ablations is how teams end up maintaining components that have been hurting performance for months.`,
  },
  {
    id: 'evaluation_in_prod',
    title: 'Evaluation in Production',
    subtitle: 'Shadow mode, champion-challenger, guardrail metrics, significance at scale',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['production evaluation', 'shadow mode', 'A/B testing', 'champion-challenger'],
    summary: `Production evaluation goes beyond offline metrics to measure the real impact of a model on user behaviour and business outcomes. Shadow mode runs a new model on live traffic, logging predictions alongside the champion's, without serving the new model's results to users — it validates infrastructure, checks for prediction errors, and flags distribution shifts before anyone is affected. Champion-challenger A/B tests route a fraction of live traffic to the new model and compare business metrics. At the scale of millions of users, any non-zero effect will be statistically significant, making practical significance (effect size) more important than p-values. Sequential testing frameworks allow stopping early when results are clearly positive or negative without inflating Type I error. The ramp strategy — 1% then 10% then 50% then 100% — catches catastrophic failures cheaply. Long-run evaluation catches novelty effects and ensures the model's gains are durable.`,
    keyPoints: [
      `Shadow mode: the new model receives the same production requests as the champion and generates predictions, but only the champion's predictions are served to users. New model predictions are logged for analysis. Use shadow mode to: (1) Validate that the serving infrastructure works correctly under production load. (2) Detect cases where the new model fails or produces unexpected output. (3) Compare prediction distributions between models — large differences on the same inputs indicate the models behave very differently and warrant investigation before A/B.`,
      `Champion-challenger A/B test: traffic is split (typically 90/10 or 95/5) between the current champion and the challenger. Metrics are measured for both groups and compared using statistical tests. Require Bonferroni correction if testing multiple metrics simultaneously — testing 10 metrics at p < 0.05 each gives a 40% chance of at least one false positive without correction. Set the primary metric, secondary metrics, and guardrails before the test starts.`,
      `Statistical significance vs practical significance at scale: with 10M daily users, a 0.01% CTR lift is statistically significant (p < 0.0001) with 1 day of data. But 0.01% on 10M events = 1,000 extra clicks — may or may not be worth the engineering cost to deploy and maintain a new model. Always define the minimum detectable effect (MDE) based on business value before starting the test. "Is this effect real?" and "Is this effect worth acting on?" are different questions.`,
      `Sample Ratio Mismatch (SRM): if you intend a 50/50 split but observe a 55/45 split in the data, the experimental randomisation has failed. SRM invalidates the A/B test because the groups are no longer comparable — some systematic factor affected who was assigned to which group. Always run a chi-squared test on the observed vs expected traffic split as the first check before interpreting any metric results.`,
      `Sequential testing: standard A/B test assumes fixed sample size determined upfront. Peeking at results during the test and stopping early inflates Type I error dramatically — you can reject a null with p=0.05 when the true p > 0.3, just by stopping at the right moment. mSPRT (mixture Sequential Probability Ratio Test) and Bayesian online decision-making frameworks provide valid inference at any stopping point. Used at Spotify, Microsoft, and Booking.com to enable early stopping without inflating error rates.`,
      `Ramp strategy for deployment: start at 1% of traffic, then 5%, 10%, 50%, 100%. At each step, run guardrail checks and monitor for degradation. The value of ramping: a catastrophic failure at 1% traffic affects 1% of users — catching it early prevents a full rollout disaster. Automated ramp-back triggers (if error rate exceeds threshold in any 15-minute window, reduce traffic to 0%) are standard at high-traffic systems.`,
      `Long-run evaluation: tests run for only a few days may overstate a model's impact due to novelty effects (users engage more with new things) or understate it due to primacy effects (users initially prefer the familiar). Plan tests for at least 2 full business cycles (14 days minimum, 28 for products with strong weekly patterns). Track whether the treatment effect estimate is stable over time — a declining effect suggests novelty, a growing effect suggests network effects or learning.`,
      `Holdout groups: maintain a small fraction (1-5%) of users permanently on the old system as a long-term holdout. This lets you measure the long-term cumulative impact of a series of model improvements against a never-updated baseline — short A/B tests only measure marginal improvements, while holdouts measure total accumulated value. Netflix and LinkedIn use long-term holdouts for this reason.`,
    ],
    checkQuestions: [
      {
        q: `Your A/B test on a recommendation system shows +2% CTR (p=0.001) after 3 days. Should you ship?`,
        a: `Not yet. Check five things before shipping: (1) Guardrail metrics — has session length, revenue per session, or explicit rating dropped? CTR gain coming at the expense of session quality is a net loss. (2) Test duration — 3 days is too short; novelty effect inflates CTR early. Run for at least 14 days. (3) SRM check — does the observed traffic split match the intended split? If not, the test is invalid. (4) Practical significance — is +2% CTR worth deploying and maintaining a new model? What is the business value? (5) Segment breakdown — is the lift uniform across countries, platforms, and user types, or driven by one segment that happens to be overrepresented? Ship only after all guardrails pass, 14+ days of data, and segment analysis confirms broadly positive results.`,
      },
      {
        q: `A/B test results show +1% revenue lift (p=0.0001) after 30 days. A colleague says "p < 0.05 means we should ship." What is missing from this reasoning?`,
        a: `p < 0.05 confirms the effect is unlikely to be zero, not that it is large enough to matter. Missing: (1) Effect size and practical significance — is +1% revenue worth the deployment cost? Check absolute dollar value and whether it meets the MDE you set before the test. (2) Guardrail metrics — is any guardrail metric degraded? A revenue gain from aggressive upselling might harm long-term retention. (3) Confidence interval — what is the range of plausible effects? (4) Multiple testing correction — if you tested 20 metrics and one is p=0.0001, Bonferroni would require p < 0.0025 for significance. (5) Heterogeneous treatment effects — is the +1% uniform or driven by a small segment that could be specifically targeted?`,
      },
      {
        q: `You are running 5 simultaneous A/B tests on your platform. A product manager notices that tests 2 and 4 seem to interact. How do you handle this?`,
        a: `Simultaneous A/B tests on the same users can interact if the tests affect the same user behaviour. The standard fix is mutual exclusion: each user is in at most one test. But mutual exclusion limits total test capacity. To diagnose interaction: look at users in both tests simultaneously and compare their metrics against users in only one test. If the interaction is genuine (significant difference), the test results for interacting cells are contaminated and cannot be interpreted. Going forward: use a traffic allocation system that enforces mutual exclusion for tests that touch similar parts of the product, and use factorial design only when you can formally test and confirm statistical independence between tests. At minimum, document which tests ran simultaneously and flag their results as potentially confounded.`,
      },
      {
        q: `What is a holdout group and why is it more valuable than a series of A/B tests for measuring long-term model value?`,
        a: `A holdout group is a permanently withheld fraction of users who receive no model updates over a long period (months to years). A/B tests measure marginal lift from one model version to the next. If you deploy 12 model improvements per year and each shows +1% CTR, does that mean you have +12% CTR from the starting point? Not necessarily — some improvements may cannibalise each other, or the product would have drifted +8% just from seasonal effects. A holdout group exposes users to the original baseline model continuously, so you can compare the current production system against the counterfactual of never having made any of those improvements. This gives true cumulative causal impact — the total value of the ML work over time. The cost is that holdout users are permanently under-served, which raises an ethical question in high-stakes applications.`,
      },
    ],
    takeaway: `The key insight is that production evaluation is the only source of ground truth for model impact, which means the entire offline evaluation pipeline exists only to filter candidates cheaply enough that you run A/B tests on the handful worth testing — every ship/no-ship decision should ultimately be backed by a controlled experiment.`,
  },
]

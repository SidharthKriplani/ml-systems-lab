export const EVAL_MODULES = [
  {
    id: 'metrics_first_principles',
    title: 'Eval Metrics from First Principles',
    subtitle: 'Confusion matrix, precision, recall, F1, why accuracy fails',
    difficulty: 'foundational',
    estimatedMin: 32,
    tags: ['metrics', 'precision', 'recall', 'F1', 'confusion matrix'],
    summary: `In an interview, the moment you reach for accuracy on an imbalanced problem, you've already lost the plot. Every classification metric comes from four numbers — TP, FP, TN, FN — and the first thing a practitioner learns is that collapsing all four into one accuracy number destroys the information you actually need. A fraud model that never fires achieves 99.9% accuracy and catches nothing. Precision tells you whether your positives are trustworthy (TP/(TP+FP)); recall tells you whether you're finding the real ones (TP/(TP+FN)). These two metrics trade off against each other through the decision threshold — and that tradeoff is a business decision, not a model tuning knob. F1 uses the harmonic mean specifically because it refuses to let one number compensate for a catastrophically bad other. F-beta formalises the cost ratio of FP vs FN so you can encode it once and optimise consistently.`,
    keyPoints: [
      `Accuracy on imbalanced data is a trap. With 1% positive class, predicting all-negative gives 99% accuracy and 0% recall. The model is useless — it has learned nothing — and the metric is rewarding that. Never use accuracy as your primary metric for fraud, medical diagnosis, or any rare-event task.`,
      `Precision and recall answer different questions. Precision: when you raise an alarm, how often are you right? Recall: of all the real alarms, how many did you catch? Raising your decision threshold makes you more selective (higher precision, fewer FPs) but causes you to miss more real cases (lower recall, more FNs). The failure mode is optimising only one: a high-precision/low-recall system misses most real positives; a high-recall/low-precision system floods downstream workflows.`,
      `F1 is harmonic, not arithmetic. F1(P=0.9, R=0.1) = 0.18, not 0.5. That's intentional — a model with dismal recall cannot hide behind excellent precision. F1 also equals 0.18 for the reverse case P=0.1, R=0.9. Use F1 when you care roughly equally about FPs and FNs.`,
      `F-beta is a business-driven parameter. beta > 1 means recall matters more (beta=2: recall twice as important as precision). beta < 1 weights precision more. Cancer screening uses beta=2 or higher because a missed positive is a missed cancer. Spam filtering might use beta=0.5 because a wrongly blocked legitimate email is a significant UX failure. Choosing beta is a conversation with stakeholders, not a grid search.`,
      `Macro-average treats every class equally — the rare class gets the same weight as the common one. Micro-average is dominated by the majority class. Weighted average follows class frequency. The choice tells interviewers whether you've thought about which failures actually matter — don't report macro vs micro without explaining the tradeoff.`,
      `MCC (Matthews Correlation Coefficient) uses all four confusion matrix cells symmetrically. Range -1 to 1; random gets 0. It doesn't reward models that just predict the majority class. This is why computational biology uses it as the standard — extreme class imbalance is the norm there and F1 can be gamed by class imbalance in ways MCC cannot.`,
      `PR-AUC and ROC-AUC tell you very different things on imbalanced data. ROC-AUC denominates FPR with TN — when you have millions of negatives, even 10,000 FPs look like a small FPR. PR-AUC ignores TN entirely, so it exposes poor precision directly. For rare-event problems, PR-AUC is the honest metric; ROC-AUC will flatter you.`,
      `The threshold is not part of the model — it's a business decision. The model outputs a score; you pick where to draw the line based on cost(FP) vs cost(FN). This conversation has to happen before you pick a metric, not after you train the model.`,
      `When your offline metrics are suspiciously low on a well-designed model, look at label quality before blaming the algorithm. Mislabelled positives become FNs; mislabelled negatives become FPs. Both drag precision and recall down. Label noise is asymmetric and its effect accumulates across metrics.`,
    ],
    checkQuestions: [
      {
        q: `A cancer screening model has precision=0.95 and recall=0.40. Is this a good model?`,
        a: `Recall=0.40 means 60% of actual cancer cases are missed — those are FNs who will leave the clinic believing they are healthy. For cancer screening, a missed positive (FN) is catastrophic while a false alarm (FP) only leads to a follow-up test. You almost certainly need recall > 0.90 here and are willing to sacrifice precision to get there. Lower the decision threshold: as you accept more FPs, recall climbs. The right operating point is where the cost of extra follow-up tests is outweighed by the cost of missed diagnoses — a cost-weighted F-beta with beta=3 or beta=4 would formalise this. Precision=0.95 at recall=0.40 is a sign the model is being too conservative.`,
      },
      {
        q: `Two models: Model A has precision=0.80, recall=0.80 (F1=0.80). Model B has precision=0.99, recall=0.67 (F1=0.80). Same F1. How do you choose?`,
        a: `The choice depends on the cost structure. Model B has much higher precision: nearly every positive prediction it makes is correct — this is ideal if FPs have high cost (e.g., blocking a legitimate bank transaction). But its recall of 0.67 means it misses a third of real positives — unacceptable if FNs are costly (e.g., missed fraud, missed disease). Model A is more balanced. For fraud detection where both false blocks and missed fraud matter, Model A is safer. The key lesson is that identical F1 scores can correspond to very different operating points on the precision-recall curve, and the right choice requires knowing cost(FP) vs cost(FN) from the business.`,
      },
      {
        q: `You compute macro-F1=0.91 and micro-F1=0.78 on a 5-class classifier. What does this tell you?`,
        a: `Macro-F1 = 0.91 means classes are well-separated on average when each class is weighted equally. Micro-F1 = 0.78 means global performance (dominated by the majority class) is worse. The gap tells you that small classes are doing well (pulling macro up) but the large majority class has significant errors (pulling micro down). Investigate: the model may be over-fitted to small classes in training or the majority class may have significant within-class heterogeneity. For an imbalanced business problem where majority-class errors are the most frequent real-world failures, micro-F1=0.78 is the number that matters for production impact.`,
      },
      {
        q: `A model predicts fraud with AUC=0.97 but the operations team says too many legitimate transactions are being blocked. What metric should you change and why?`,
        a: `AUC measures ranking quality across all thresholds and says nothing about where you operate. The problem is the operating threshold is too low — it accepts too many FPs to achieve its recall target. The operations team is experiencing a precision problem. Switch to monitoring precision at a fixed recall target (e.g., precision at recall=0.80) or precision at a fixed FP-rate. Then raise the threshold until precision meets the operations team's tolerance for blocked-legitimate transactions. AUC is an offline selection metric; the production metric that matters is precision at the operating recall your business has decided to target.`,
      },
    ],
    takeaway: `The key insight is that precision and recall measure fundamentally opposite failure modes, which means the only correct way to choose a classification metric is to first quantify the business cost of FP vs FN — everything else follows from that ratio.`,
    interactiveId: 'confusion_matrix_viz',
  },
  {
    id: 'auc_roc',
    title: 'ROC Curve & AUC',
    subtitle: 'FPR/TPR, what area means, PR-AUC for imbalanced classes',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['ROC', 'AUC', 'PR-AUC', 'ranking'],
    summary: `AUC-ROC is probably the metric most often cited in ML papers and most often misapplied in production. The clean probabilistic reading — P(model ranks a random positive above a random negative) — makes it an excellent offline ranking metric. The trap is that ROC-AUC denominates FPR with TN: with 10,000 negatives in the pool, even 500 FPs produce an FPR of ~0.05. The curve stays far upper-left, the model looks excellent, and precision = 100/600 = 0.17 is quietly catastrophic. PR-AUC fixes this by ignoring TN entirely — it only cares about whether you're ranking positives correctly in the context of your actual positive class, not relative to an enormous sea of negatives. Knowing when to use each one, and why, is the litmus test for whether someone has actually shipped a model on real imbalanced data.`,
    keyPoints: [
      `AUC has a clean probabilistic interpretation: P(score(positive) > score(negative)) for a randomly drawn pair. AUC=0.85 means the model ranks a random positive above a random negative 85% of the time. It's a pure ranking quality metric, entirely threshold-independent.`,
      `A curve below the diagonal (AUC < 0.5) is not a bad model — it's a backwards-scored model. Flip the decision and you get a model with AUC > 0.5. AUC < 0.5 always signals an implementation error in the scoring direction, not a fundamental capability problem.`,
      `The FPR denominator is why ROC-AUC flatters you on imbalanced data. With 10,000 negatives and 100 positives: 500 FPs give FPR = 500/10500 ≈ 0.05 — looks fine on the ROC curve. But precision = 100/600 = 0.17. PR-AUC would expose this instantly because it removes TN from the picture completely.`,
      `PR-AUC is the right metric whenever your TN count is large relative to your positive class. Fraud detection, medical diagnosis, anomaly detection — in all of these, the business doesn't care about the TN sea. The failure mode of insisting on ROC-AUC here is shipping models that look great in eval and perform terribly in the alert queue.`,
      `Average Precision (AP) is the area under the PR curve, weighted by recall change at each threshold. Map (Mean AP) averages AP over queries — the standard in search and information retrieval. The weighting gives extra credit for achieving high precision while also achieving high recall.`,
      `When to use which: ROC-AUC for roughly balanced classes or when you genuinely care about ranking quality across the full negative set (credit scoring of all applicants). PR-AUC for imbalanced problems where TN performance is irrelevant to the business. Picking ROC-AUC for an imbalanced problem is a standard mistake that leads to overconfident model selection.`,
      `Calibration and discrimination are independent properties. A model with AUC=0.95 but poor calibration ranks positives correctly but produces overconfident or underconfident probabilities. If you're using the probability output to set a threshold or compute a risk score, calibration matters as much as AUC — always check both.`,
      `Partial AUC lets you restrict evaluation to a specific FPR range (e.g., FPR < 0.1). This makes sense when you'll never operate at high FPR in production — evaluating the full curve includes operating points you'll never use, which dilutes the signal about performance where it actually matters.`,
    ],
    checkQuestions: [
      {
        q: `Two models have the same AUC-ROC (0.85) on a 1% positive rate dataset. How would you further differentiate them?`,
        a: `Compare PR-AUC — for 1% positive rate, it is far more discriminative because it ignores the large TN pool. Look at precision at specific recall targets that matter for the business (e.g., precision at recall=0.80). Compare calibration — which model's P(y=1) better matches actual frequencies when you bucket predictions? Compute the lift curve — for top-k% of scored records, which model captures more true positives? Two models can have identical AUC-ROC but very different PR curves, especially in the high-recall regime where one model may be much more precise. Finally, compute the Brier score to jointly measure calibration and discrimination.`,
      },
      {
        q: `Your fraud model has AUC=0.96. The business team says the alert queue has too many false alarms. What happened and how do you fix it?`,
        a: `AUC=0.96 measures ranking quality, not operational precision at the deployed threshold. The problem is that at the chosen operating threshold, precision is too low — too many FPs relative to TPs. AUC does not tell you anything about the threshold. Fix: (1) Raise the score threshold to reduce FPs, accepting lower recall. (2) Evaluate and report precision at the recall target the business needs rather than AUC. (3) If the false alarm rate is fundamentally bad even at very high thresholds, the model may have good ranking but poor absolute calibration — recalibrate with Platt scaling or isotonic regression. The disconnect between high AUC and high false alarm rate is typical when the positive rate is very low.`,
      },
      {
        q: `AUC-ROC for a model is 0.72. A colleague argues that "since 0.72 > 0.5, the model is useful." Is that a sufficient argument?`,
        a: `No. AUC > 0.5 only means the model ranks positives above negatives more often than chance — it says nothing about whether the degree of improvement is meaningful for the business, whether the improvement holds at the specific operating threshold needed, or whether performance is uniform across important subgroups. AUC = 0.72 on a medical diagnosis task where patients need TPR > 0.90 at 5% FPR is useless. Also check: is AUC 0.72 driven by good performance in the region of the ROC curve you actually operate in? A model with AUC=0.72 that does well at low FPR may outperform one with AUC=0.80 that only does well at high FPR. Always frame AUC in context of the operating requirement.`,
      },
      {
        q: `What is the relationship between AUC-ROC and the Mann-Whitney U statistic?`,
        a: `They are equivalent. The Mann-Whitney U statistic counts the number of (positive, negative) pairs where the positive is ranked higher than the negative, divided by the total number of such pairs. This is exactly P(score(positive) > score(negative)) — the probabilistic interpretation of AUC-ROC. This equivalence has a practical implication: you can compute AUC without drawing the ROC curve at all, just by counting concordant pairs. It also implies AUC is a non-parametric test of whether the two score distributions are separable, which is why it is robust to class imbalance in the ranking sense (though not in the precision sense).`,
      },
    ],
    takeaway: `The key insight is that AUC-ROC measures ranking quality across all thresholds and denominates FPR with TN, which means it systematically overstates model quality when negatives vastly outnumber positives — in those cases you must use PR-AUC and evaluate precision at your actual operating recall.`,
    interactiveId: 'roc_curve_viz',
  },
  {
    id: 'ranking_metrics',
    title: 'Ranking Metrics',
    subtitle: 'NDCG, MAP, MRR — search and recommendation quality',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['NDCG', 'MAP', 'MRR', 'ranking', 'RecSys'],
    summary: `Ranking metrics encode a theory of user attention. That's the thing most candidates miss — you're not just measuring "how many relevant results did we find," you're claiming something about how users scan lists. MRR says users read until they find the first relevant result and stop. MAP says they care about finding every relevant result and finding it early. NDCG says attention decays logarithmically with rank — position 1 is worth twice position 3, roughly. If your actual users behave differently (they scroll more, they click multiple things, they use infinite scroll), your metric is wrong even if the math is right. The other thing that kills offline ranking metrics in production: click-based labels have position bias baked in. Results ranked first get clicked more because they're first, not because they're relevant. Measuring NDCG against those labels means you're partly measuring your existing ranker, not ground truth relevance.`,
    keyPoints: [
      `MRR = (1/|Q|) sum 1/rank_first_relevant. Only the rank of the first relevant result matters. This is the right metric for navigational queries and FAQ systems where one answer is what users need. The failure mode: two rankers with identical MRR can have completely different result quality at positions 2-10, and MRR won't notice.`,
      `MAP's Average Precision for a single query sums P@k × rel(k) over all ranks where a relevant item appears, then divides by the total number of relevant items. It rewards finding relevant items early AND finding all of them. The catch: MAP weights each query equally regardless of difficulty. A query with 1 relevant item and a query with 100 relevant items look the same to MAP.`,
      `NDCG = DCG/IDCG where DCG = sum (2^rel(i) − 1)/log_2(i+1). The 2^rel formula is important — it makes a highly relevant item (rel=3) worth 7 units while a marginally relevant one (rel=1) is worth 1 unit. The normalisation by IDCG makes scores comparable across queries with different numbers of relevant items. This is why NDCG is the commercial search and recommendation standard.`,
      `The log_2(i+1) discount means: position 1 counts fully, position 2 at 63%, position 10 at 29%, position 100 at 15%. This is a model of attention decay, not an arbitrary formula. If your users have different scan patterns — say, they're using a horizontal carousel — you'd want a different discount function that matches the observed behaviour.`,
      `NDCG@K ignores everything after rank K. For web search K=10 (one page). For recommendations K=5 or K=20. The truncation is both realistic (matches user behaviour) and a risk: if two systems are identical in the top K but differ below it, NDCG@K won't help you choose. Make sure K matches the actual depth users engage with in your product.`,
      `Position bias is the silent killer of click-based ranking metrics. Users click position 1 more than position 5 not just because position 1 is more relevant, but because it's at position 1. Raw click labels carry this bias. If you train and evaluate on click-based NDCG, you're partly optimising to reproduce whatever your current ranker already ranks first. Inverse Propensity Scoring (IPS) — weighting clicks by 1/P(click at position k) — removes this but requires a click propensity model.`,
      `MAP and MRR require binary relevance (relevant/not). NDCG supports graded relevance (0-3 or 0-4), which is more expressive. If you have human raters using a 4-point scale, throwing that away to binarise for MAP is losing information. Use NDCG when you have graded labels; use MAP/MRR for simpler binary-relevant retrieval tasks.`,
      `Offline NDCG, MAP, and MRR are all computed on historical labels that don't capture user satisfaction directly. A ranker can improve NDCG by 5% while decreasing long-session engagement. Online interleaving tests — where two rankers' results are merged and user clicks determine the winner — are 100x more sensitive than standard A/B tests and can detect ranking differences that offline metrics miss entirely.`,
    ],
    checkQuestions: [
      {
        q: `A search engine retrieves 3 relevant documents at ranks 1, 3, 5 out of 5 total relevant documents. Compute AP.`,
        a: `AP = (1/R) × sum_k [P@k × rel(k)] where R=5. At rank 1: relevant, P@1 = 1/1 = 1.0. At rank 3: relevant, P@3 = 2/3. At rank 5: relevant, P@5 = 3/5. Non-relevant ranks contribute 0. AP = (1/5) × (1.0 + 2/3 + 3/5) = (1/5) × (1.0 + 0.667 + 0.6) = (1/5) × 2.267 = 0.453. Note: if those same 3 relevant documents had appeared at ranks 1, 2, 3, AP = (1/5)(1 + 1 + 1) = 0.60. The difference shows how much AP penalises finding relevant items later.`,
      },
      {
        q: `Your recommendation system shows NDCG@10 = 0.85 in offline evaluation. But users complain results feel irrelevant. What might explain this?`,
        a: `Several mechanisms: (1) Offline NDCG was computed using click-based relevance labels, which have position bias — high-ranked items get more clicks regardless of true quality, and the metric optimises for this spurious signal. (2) The offline held-out test set may not reflect current user intent or new content (temporal shift). (3) NDCG@10 is an average over all queries — it may be 0.98 on common queries and 0.40 on tail queries, masking poor performance for specific user segments. (4) The metric captures relevance at the item level but not at the session level — users may find each item individually relevant but the set redundant. Consider running an interleaving experiment or computing diversity metrics alongside NDCG.`,
      },
      {
        q: `MRR is 0.60 for a search system. A manager wants to improve it to 0.75. What does this mean concretely and what would you change?`,
        a: `MRR = 0.60 means on average, the first relevant result appears at rank 1/0.60 ≈ 1.67 — users typically find their first relevant result somewhere between position 1 and 2. To reach MRR = 0.75, you need to push the average to rank 1/0.75 ≈ 1.33 — essentially making position-1 the first relevant result for more queries. To improve MRR: (1) Identify query categories where first relevant result is at rank 3 or higher and focus feature engineering there. (2) Improve query understanding (entity recognition, intent classification) for queries where the top result is a semantic mismatch. (3) Improve your retrieval recall so the re-ranker has the right document available to put first. MRR only cares about the first hit, so improvements beyond position 1 will not affect it.`,
      },
      {
        q: `You have graded relevance labels (0-3) and want to compare two rankers. Should you use MAP or NDCG?`,
        a: `Use NDCG. MAP requires binary relevance — it cannot use graded scores and any binarisation (e.g., threshold at 2) discards information. NDCG with the formula 2^rel(i) amplifies differences between highly relevant (rel=3, contribution 7) and somewhat relevant (rel=1, contribution 1) items. A ranker that puts 3s at the top beats one that puts 1s at the top by a large NDCG margin, which correctly captures that the first ranker is much better. MAP with binarised relevance would show the same rank if both rankers put all rel>=2 items above rel<2 items. For graded relevance, NDCG is strictly more informative than MAP.`,
      },
    ],
    takeaway: `The key insight is that all ranking metrics embed a model of user patience — MRR says users stop after the first hit, MAP says they care equally about each relevant item, NDCG says their attention decays logarithmically — so choosing the metric means choosing which user behaviour model you believe, not just which formula is standard.`,
  },
  {
    id: 'offline_vs_online',
    title: 'Offline vs Online Evaluation',
    subtitle: 'Proxy metrics, A/B gap, how to close the offline-online divide',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['evaluation', 'A/B testing', 'online metrics', 'proxy metrics'],
    summary: `The most expensive ML mistake is shipping a model that beats its offline benchmark and makes the product worse. The offline-online gap is not a fluke — it's structural. Offline labels are proxies (clicks ≠ satisfaction), the test set is static (it doesn't reflect live traffic), and once you deploy, the model changes what data gets collected (feedback loops). The gap is systematic bias, not noise. Closing it requires two things: validating historically that your offline metric correlates with online outcomes (proxy metric calibration), and treating A/B tests as the final arbiter of value. Guardrail metrics — secondary online metrics you must not harm — are your protection against Goodharting. A model that raises CTR by gaming sensationalism while destroying session length is worse than no model, and guardrails are what catch it.`,
    keyPoints: [
      `The offline-online gap has four root causes: distribution shift (production traffic differs from historical test data), feedback loops (the model influences what data future models train on), proxy label problem (clicks measure something correlated with satisfaction, not satisfaction), and the counterfactual problem (offline evaluation can only score items that the old policy actually served).`,
      `Counterfactual offline evaluation: you can't just score a new policy on logged data from an old policy because you only have reward signals for items the old policy served. Inverse Propensity Scoring (IPS) reweights observations by 1/propensity_score to correct for the old policy's selection bias. Used at Yahoo and Netflix. Requires knowing or estimating the logging policy's action probabilities.`,
      `Guardrail metrics are online metrics you must not harm even if the primary metric improves — for example, improve CTR but don't let session duration fall below the control level. Three to five guardrails that must all pass is standard practice at large platform companies. Without guardrails, models learn to Goodhart the primary metric at the expense of the overall system.`,
      `Interleaving is 10-100x more statistically sensitive than standard A/B testing for ranking experiments. Each user sees results from both rankers merged into one list; clicks determine the winner. Individual user variance is eliminated because both systems are evaluated on the same user simultaneously. This is why you can run ranking experiments with hundreds of users instead of hundreds of thousands.`,
      `Proxy metric calibration: track offline metric changes alongside realised online metric changes over historical model deployments. Build a calibration map — offline NDCG +0.5% historically predicts online CTR +0.3%. This tells you the minimum offline improvement worth the cost of an A/B test. The calibration needs to be updated whenever the system architecture changes significantly.`,
      `Novelty and primacy effects contaminate early A/B test results. Users engage more with new features simply because they're new (novelty) or cling to familiar ones even when the new version is better (primacy). Both effects distort results in the first few days. Standard practice: run tests for at least two full weeks and check that the treatment effect estimate is stable over time before calling it.`,
      `CTR and clicks are leading indicators — fast to measure, weakly correlated with long-term value. Retention and LTV are lagging indicators — slow to observe, strongly correlated with what the business actually cares about. Optimise for leading indicators in the short term, but validate that they translate to lagging indicators on the time horizon that matters.`,
      `A/B tests can only measure the models you actually test. The selection of what to test is a human judgment that no experiment can optimise. Offline metrics exist to kill bad candidates cheaply so you run A/B tests on only the handful worth testing — not to prove models are good.`,
    ],
    checkQuestions: [
      {
        q: `Your RecSys shows +3% NDCG@10 offline. You run an A/B test and see -1% on session length. What do you do?`,
        a: `Session length is likely a guardrail metric — do NOT ship. The offline NDCG improvement did not translate to a real user improvement; it degraded engagement. The first investigation: did NDCG improve on click-based labels that have position bias, meaning the model learned to rank already-popular items first? Second: check if the model optimised for initial clicks but delivers content that users do not continue watching. Third: is session length confirmed as a guardrail or is it an incidental dip? If it is a confirmed guardrail violation, the model has an alignment problem — NDCG is not aligned with engagement. Go back and either redesign the offline metric using satisfaction labels if available, or redesign the model objective.`,
      },
      {
        q: `What is the difference between running an A/B test and running an interleaving experiment? When would you choose each?`,
        a: `A/B test routes each user to exactly one version — user-level randomisation. Effect is measured by comparing aggregate metrics across groups. Requires large sample sizes because between-user variance dominates. Interleaving shows each user results from both systems interleaved in one list; user preference signal (which items they click) directly measures system quality. Interleaving eliminates between-user variance, reducing required sample by 100x for the same power. Choose A/B when you need to measure absolute effects on business metrics (revenue, retention, CTR) — interleaving only measures relative ranker preference, not business impact. Choose interleaving when comparing ranking quality cheaply during model development or when you want to cheaply confirm a new ranker is better before running a full A/B test.`,
      },
      {
        q: `Your team calibrated that +1% NDCG gain historically predicts +0.4% CTR online. You see a model with +5% NDCG. Should you trust the calibration and skip the A/B test?`,
        a: `No. Calibration is based on historical model changes that may not be representative of this specific change. A +5% NDCG gain is unusually large and may indicate the model improved on historical data distribution but not on the current one. Large offline gains sometimes come from distribution mismatch, data leakage, or metric hacking. The calibration model itself has uncertainty — a +5% NDCG prediction should come with wide confidence intervals on the expected CTR gain. The A/B test is cheap relative to the cost of shipping a broken model. The correct use of calibration is to triage: only run A/B for models with offline gain above a minimum threshold; never use calibration to skip the A/B entirely.`,
      },
      {
        q: `A competitor product uses pure online bandit for model selection instead of A/B tests. What are the trade-offs?`,
        a: `Bandits adaptively route traffic to better-performing variants, reducing regret (total lost value from not always serving the best arm). A/B tests fix allocations upfront and cannot exploit interim results. Bandits are better when the cost of serving an inferior arm is high and you want to minimise harm. But bandits have significant downsides for evaluation: they cannot isolate clean causal estimates because allocation is endogenous (correlated with performance); they struggle with delayed feedback signals (cannot adapt on 30-day retention signals); and they can produce biased estimates due to adaptive stopping. For rigorous causal evaluation of model quality and business impact, A/B tests with pre-registered metrics and fixed duration are preferable. Bandits are better for production optimisation (e.g., ad creative selection) than for model validation.`,
      },
    ],
    takeaway: `The key insight is that offline metrics measure how well a model scores historical data that was collected under a different policy, which means the offline-online gap is not random noise but a systematic bias, and closing it requires either correcting for the logging policy (IPS) or running A/B tests as the true ground truth.`,
  },
  {
    id: 'validation_traps',
    title: 'Validation Set Traps & Data Leakage',
    subtitle: 'Leakage taxonomy: temporal, group, label leakage, how to audit',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['data leakage', 'validation', 'temporal split', 'label leakage'],
    summary: `Data leakage is probably the single most common cause of the offline-production gap in practice. Every practitioner who has worked on enough real-world ML has a leakage war story. The pattern is always the same: suspiciously high offline AUC, model ships, production performance is terrible. Leakage takes four main forms: temporal (future information in training features), group (same entity in both train and test), label (a feature derived from or collected after the outcome), and preprocessing (normalisation fit on the full dataset before splitting). The diagnostic question is simple and needs to be applied to every feature: "Would this value exist at the exact moment of prediction in production?" If no, it's leakage. Suspicious AUC above 0.97 on a hard problem, a single feature dominating importance, or training and validation error nearly identical — any of these should trigger a leakage audit before you do anything else.`,
    keyPoints: [
      `Temporal leakage is the most common form in time-series ML. A churn model trained with a random row-level split will contain future purchase data in training features for a user whose churn label was determined earlier. The fix is always the same: split by time, not by row, and verify that every feature is computed using only data available before the prediction timestamp.`,
      `Group leakage: the same user, patient, or entity in both train and test. The model memorises entity-specific patterns — user browsing style, patient baseline — that don't generalise to new entities. The fix is group k-fold or a user-level holdout. Group leakage is especially nasty in user engagement models because individual user patterns are very strong predictors and the model will appear to generalise when it's just memorising.`,
      `Label leakage is different from temporal leakage: the feature may have the right timestamp but the wrong causal direction — it's a consequence of the label, not a predictor. "Number of days in ICU" to predict "was the patient hospitalised?" is the textbook case. The audit question isn't "when was this feature computed?" but "is this feature causally upstream of the outcome, or downstream?"`,
      `Preprocessing leakage: fitting a StandardScaler or mean imputer on the full dataset (including test) before splitting. The test set's statistics leak into the training pipeline. Fix: fit all transformers on training only. Use sklearn Pipeline to enforce this automatically. Target encoding is particularly severe — computing target mean per category on the full dataset directly leaks the label into the feature for every test sample.`,
      `Four detection signals: (1) Validation AUC above 0.97 on a problem where experts disagree — almost always leakage. (2) One feature has dramatically higher importance than all others — check if that feature can be known at prediction time. (3) High Pearson correlation between a feature and the label (above 0.7) — suspect label leakage. (4) Train and validation accuracy nearly identical with no generalisation gap — the test set has likely been contaminated.`,
      `Point-in-time correct feature stores: in production, features should be computed at the exact prediction timestamp and logged. Training datasets are reconstructed by joining each historical event with the feature values that existed at that timestamp. Systems that join on latest feature values instead of historical values produce temporal leakage at scale — this is the operational case for feature stores with time-travel capability (Feast, Tecton, Hopsworks).`,
      `The leakage audit procedure: for each feature, document (1) how it's computed, (2) what data sources it uses, (3) when it would be available in production scoring. Cross-reference against label creation time. Any feature where computation time is after label time is a leakage candidate. Automate this check in your feature engineering pipeline — manual audits miss things.`,
      `NLP leakage: if you build a vocabulary or fit a TF-IDF vectoriser on the full dataset before splitting, the test set's token distribution leaks into the tokeniser. Similarly, splitting documents from the same author or article series means the model exploits stylistic patterns rather than learning general language understanding.`,
    ],
    checkQuestions: [
      {
        q: `You build a churn prediction model with AUC=0.97. In production, AUC drops to 0.64. What went wrong and how do you debug?`,
        a: `A drop from 0.97 to 0.64 is a strong signal of data leakage, not model degradation. Debug steps: (1) Check the train/test split — was it a random row-level split on time-series data? Re-split by time and re-evaluate. (2) Audit the top-5 most important features — are any of them computable only after the churn event? E.g., "account closed = 1" used as a feature when predicting churn. (3) Check preprocessing — was scaling or imputation fit on the full dataset? (4) Check for group leakage — does the same user appear in both train and test? (5) Look for any feature with correlation above 0.7 with the label. In production, the AUC of 0.64 is the model's true performance — the 0.97 was an illusion created by knowing the answer.`,
      },
      {
        q: `A colleague uses a random 80/20 split on a dataset of 500,000 website visits by 50,000 unique users. What is the problem and how do you fix it?`,
        a: `With a random split, each user appears in both train (80%) and test (20%) on average. The model learns user-level patterns — browsing style, device type, time patterns — that are highly predictive for that specific user but do not generalise to new users. In production, the model will score new users it has never seen, so this is direct group leakage. Fix: split 40,000 users into training, 10,000 users into test. All visits from a user go to exactly one split. The resulting test AUC will be lower than the random-split AUC and that is the correct, honest performance estimate. For time-series visit data, additionally split by time within each user-split.`,
      },
      {
        q: `What is target encoding leakage and how does k-fold target encoding fix it?`,
        a: `Target encoding replaces a categorical variable's value with the mean target (e.g., mean conversion rate) for that category. If you compute this mean using the full dataset (including the test samples), the encoding for each test sample incorporates its own label's information — the test sample literally leaks its label into its own feature. This makes the feature a near-perfect predictor, inflating train and validation metrics dramatically. K-fold target encoding fixes this by computing the encoding for each fold using only out-of-fold data: for each training sample in fold k, compute the mean target using all other folds. At test time, use the mean computed from all training data. This ensures no sample's label is used to compute its own encoding.`,
      },
      {
        q: `You join a company and are handed a model achieving AUC=0.94 on historical data. How do you quickly audit for leakage before trusting this number?`,
        a: `Three quick checks: (1) Ask how the train/test split was done — if the answer is "random rows" on time-series data, the number is probably wrong. Re-split by time and re-evaluate. (2) Print feature importances — if one feature has 60%+ importance on a problem where no single feature should dominate, inspect it. Pull up 5-10 samples and check if that feature's value could be known at prediction time. (3) Compare AUC on train vs test — if train AUC is also 0.94 with no gap, either the model is perfectly calibrated (unlikely) or the test set has contamination. In practice, leakage from a random split on event-level time series data is the most common finding in this scenario.`,
      },
    ],
    takeaway: `The key insight is that data leakage is not a modelling error but a data engineering error, which means the fix is almost always upstream in the feature computation pipeline — asking "would this feature value exist at the exact moment of prediction?" is the single question that catches most leakage.`,
  },
  {
    id: 'cross_validation',
    title: 'Cross-Validation Strategies',
    subtitle: 'k-fold, stratified, group k-fold, time-series CV, nested CV',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['cross-validation', 'k-fold', 'time series', 'nested CV'],
    summary: `K-fold CV exists because a single train/val split has high variance — you might get lucky or unlucky with a particular partition. But k-fold is only valid when your observations are iid, and the moment you have temporal structure, entity groups, or hierarchical clusters, naive k-fold produces an overoptimistic estimate because information leaks across folds. The most important mental model: every CV strategy embeds an assumption about how the model will be deployed. Group k-fold embeds "the model will be applied to entities it has never seen." Time-series walk-forward embeds "future data is unavailable at training time." Pick the wrong one and you're evaluating a fantasy deployment scenario, not your real one. Nested CV is the thing most people skip — if you're tuning hyperparameters and reporting metrics on the same validation set, selection bias is inflating your numbers.`,
    keyPoints: [
      `Standard k-fold: k=5 or k=10 is standard. k=5 uses 80% of data per fold (lower variance estimate, less training data per fold). k=10 uses 90% (lower bias, higher variance of the estimate, slower). LOO (k=n) is unbiased but has high estimate variance and is very slow. Never use standard k-fold on time-series data — you'll be training on future observations to predict the past.`,
      `Stratified k-fold preserves class proportions across folds. With 5% positive class and 5 folds, each fold has approximately 5% positives. Without stratification, a fold might have zero positives by chance — giving a meaningless evaluation. sklearn's StratifiedKFold is the default for classification; use it unless you have a specific reason not to.`,
      `Group k-fold: all samples from the same entity — user, patient, document — go to the same fold. In production, the model scores entities it's never seen during training. Without this, group-level patterns can cause massive apparent generalisation that disappears in production. The test: in production, will this model be applied to groups it saw during training? If no, use group k-fold.`,
      `Time-series walk-forward CV: train on [0, t1], validate on [t1, t2]. Then train on [0, t2], validate on [t2, t3]. Expanding window is standard — keep all historical data. Rolling window — fixed-size training — makes sense when old data is less relevant due to concept drift. The failure mode is any k-fold that lets a model train on month 3-12 to predict month 1. You're predicting the past from the future.`,
      `Purged and embargoed CV for autocorrelated features: rolling averages and lag features computed on the tail of the training period can bleed into the head of the validation period. Purging removes training samples too close to the validation start; embargoing removes validation samples too close to the training end. Critical in financial ML where feature windows can span months.`,
      `Nested CV is the fix for selection bias in hyperparameter tuning. Outer loop evaluates model quality; inner loop selects hyperparameters. Without nesting: if you tune on the validation set and report metrics on the same set, you've implicitly optimised for that specific held-out partition. The result is upward-biased. Nested CV is slower but gives a conservative unbiased estimate of the entire model-selection pipeline.`,
      `Large dataset CV: with millions of samples, k-fold is slow and the variance from a single temporal split is already low. A single 90/10 temporal split is often sufficient. Save k-fold for limited data (below ~10,000 samples) or cases where partition variance would be high. Time-series data always needs temporal splitting regardless of size.`,
      `Repeated CV: run the full k-fold r times with different random splits, giving k×r evaluations. Reduces variance of the estimate. Standard in medical ML with small samples where a single 5-fold result would have high uncertainty. Report mean ± std across repeats.`,
    ],
    checkQuestions: [
      {
        q: `You have 5 years of daily transaction data and want to build a fraud model. How do you set up CV?`,
        a: `Use time-series walk-forward CV. Structure: train on year 1, validate on months 1-3 of year 2. Then train on year 1 + Q1 of year 2, validate on Q2. Continue sliding forward. Add a 7-day purge gap between train end and validation start to prevent leakage through 7-day rolling features. Use expanding window (keep all historical data) unless you suspect strong concept drift in fraud patterns, in which case use a rolling 12-month window. Group by user_id: all transactions for a user in the validation period should not have the same user's transactions in the training folds if your goal is to detect fraud from new users. Never use standard k-fold — you would train on 2024 data to predict 2022 events.`,
      },
      {
        q: `You run 5-fold CV and get AUC scores [0.83, 0.84, 0.92, 0.85, 0.83]. The mean is 0.854. Should you report this as your model's performance?`,
        a: `The fold with AUC=0.92 is a significant outlier — about 8 points above the others. Before reporting, investigate why that fold is different. Possibilities: (1) That fold's validation set happened to contain an unusually easy subset of examples — check class distribution in each fold. (2) The training set for that fold was unusually informative. (3) There is a data quality issue in one fold. If the outlier is genuine, report mean ± std = 0.854 ± 0.035 to convey the uncertainty. The high variance itself is information — it tells you the model's performance is unstable across data subsets, which matters for production. Do not just report the mean without the standard deviation.`,
      },
      {
        q: `A colleague argues that since you are doing hyperparameter search with Optuna and reporting the best trial's validation score, you have a proper unbiased evaluation. Explain why this is wrong.`,
        a: `This is the nested CV problem. When you run Optuna for 200 trials and report the best trial's validation AUC, you have searched over 200 models and selected the one with the highest validation score. The validation AUC of the best trial is upward-biased by selection — you happened to find a configuration that did well on this specific validation set. The true generalisation performance is lower. To get an unbiased estimate: use nested CV where hyperparameter search is done in the inner loop on the training fold only, and the outer fold is a held-out test set that neither Optuna nor you ever touched during optimisation. Alternatively, hold out a separate test set before starting Optuna and only evaluate on it once, after all tuning is complete.`,
      },
    ],
    takeaway: `The key insight is that every CV strategy embeds an assumption about how the model will be deployed — temporal splitting embeds "future data is unavailable," group splitting embeds "new entities appear in production" — and using the wrong strategy produces an evaluation that tests a different deployment scenario than the real one.`,
    interactiveId: 'cross_validation_viz',
  },
  {
    id: 'error_analysis',
    title: 'Error Analysis',
    subtitle: 'Confusion matrix drill-down, error slicing, systematic failures',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['error analysis', 'failure modes', 'slicing', 'debugging'],
    summary: `A model with F1=0.85 might be failing catastrophically on the subgroup that matters most. Aggregate metrics hide this. Error analysis is how you find out what's actually broken and what to do about it — without it, decisions about whether to collect more data, engineer new features, or change architecture are guesswork. The output of good error analysis is a prioritised intervention list: "fix X and you recover Y% of errors." Manual inspection of 50-100 errors — and actually categorising them — is more informative than running 10 more experiments on the full dataset. The key finding that changes what you do: systematic errors (all from one segment, one feature range, one pattern) require targeted intervention. Random errors that correlate with nothing are irreducible and more data won't help much.`,
    keyPoints: [
      `Start with the confusion matrix: which class pairs are being confused? "Cat" confused with "small dog" is a fundamentally different failure from "cat" confused with "car." In multi-class problems, the off-diagonal cells tell you the error structure; the aggregate metric tells you nothing about it.`,
      `Error slicing is the most actionable step: compute error rate per value of each feature — by time of day, user country, product category, input length. A model with average F1=0.85 may have F1=0.40 for users in one country. Those segments point directly to where data or features need to improve. Broad questions like "why is the model wrong?" are much harder to act on.`,
      `Overconfident wrong predictions are the most dangerous in production. Plot error rate vs predicted probability. An overconfident model has high error rate at predicted probability 0.9 — those are systematic misclassifications the model is certain about. Consider using uncertainty estimates (MC dropout, ensemble variance) to flag high-uncertainty predictions for human review.`,
      `Manual inspection protocol: sample 50-100 FPs and FNs separately. Assign each to one of: (a) labelling error — true label is wrong; (b) insufficient features — model lacks info a human would have; (c) rare pattern — not enough training examples; (d) genuine ambiguity — humans disagree. The distribution across buckets is directly actionable: bucket (a) means relabel, bucket (b) means feature engineering, bucket (c) means data collection, bucket (d) means accept as irreducible.`,
      `Systematic vs random errors is the most important output of error analysis. If errors cluster by feature value, subgroup, or pattern, you need targeted intervention — more data from that subgroup, a separate model for it, or a specific feature. If errors look uniformly random, more data helps only marginally and you're likely near the irreducible error floor for the current feature set.`,
      `Error correlation is the basis for ensemble design. Two models with similar accuracy that fail on the same examples ensemble into neither model. Two models that fail on different examples cancel each other's errors. Error analysis informs whether ensembling is worth it — you want maximum error complementarity, not error overlap.`,
      `Production error monitoring means applying error analysis continuously, not just once. Log each prediction, confidence, and features. Feed true labels back when available through delayed feedback. Slice error rate by feature buckets in real time. Alert when any slice exceeds a threshold — "error rate in country X exceeded 30% over the last hour" is a much better signal than a global accuracy metric degrading slowly.`,
      `The error decomposition hierarchy: (1) Overall metric — is there a problem? (2) Slice by output class — which classes are failing? (3) Slice by input features — which segments? (4) Inspect individual errors — what kind? (5) Form hypotheses and test. Skipping from "F1=0.75" to "we need a transformer" skips the diagnosis that might reveal the problem is a labelling error in one category.`,
    ],
    checkQuestions: [
      {
        q: `Your NLP classifier has 88% overall accuracy but stakeholders are unhappy. How do you diagnose what is wrong?`,
        a: `88% overall hides per-class and per-segment failures. Step 1: compute per-class precision and recall — which classes have low recall? A class with recall=0.40 means 60% of instances are misclassified, which stakeholders feel even if average accuracy is 88%. Step 2: slice by text length, domain, source, and date — any segment with significantly lower accuracy? Step 3: sample 100 errors and manually categorise into label errors, insufficient features, rare patterns, and genuine ambiguity. Step 4: check the confusion matrix — are errors concentrated between specific class pairs? Step 5: check confidence distribution on errors — are most errors low-confidence (model knows it does not know) or high-confidence (model is systematically wrong)? Each finding points to a different fix.`,
      },
      {
        q: `You sample 100 FPs from a fraud detection model and find that 60% involve transactions from a new merchant category launched 2 months ago. What do you do?`,
        a: `This is a systematic failure from distribution shift — the model was trained before this merchant category existed and has no representation for it. 60% is far too high to dismiss as random. Actions: (1) Collect labelled examples from this merchant category and add to training data. (2) Add a feature indicating "merchant category is new" as a signal that the model should be more uncertain. (3) Consider a separate lightweight model or rule for this category while the main model retrains. (4) Check if other new merchant categories also have elevated FP rates — if so, temporal weighting of training data (more weight to recent transactions) is the fix. (5) Add "new merchant category" as a monitored slice in production dashboards.`,
      },
      {
        q: `You have two models both with F1=0.82. How do you choose which to deploy using error analysis?`,
        a: `Identical F1 does not mean identical behaviour. Run error analysis on both: (1) Compare confusion matrices — does one model fail on a different class than the other? (2) Compare error slices — does one perform better on the segment that is most critical to the business? (3) Compare error confidence — which model is less overconfident on its errors (safer for downstream decision-making)? (4) Compare error correlation — if their errors are largely different, they may be worth ensembling. (5) Compare calibration — which model's predicted probabilities better match actual frequencies? Identical aggregate F1 with different error structures means the models are solving the problem differently and the choice should be guided by business risk tolerance for specific failure modes.`,
      },
      {
        q: `What is the difference between error slicing and subgroup fairness analysis? When does one become the other?`,
        a: `Error slicing is a debugging technique: slice error rate by any feature to find model blind spots, with the goal of fixing them. Subgroup fairness analysis is a normative assessment: check whether error rates are equitable across protected demographic groups (race, gender, age). They use the same technical procedure but have different intent and consequence. Error slicing asks "where is the model wrong so I can fix it?" Fairness analysis asks "is the model wrong more for certain groups in a way that causes harm?" When your error slices are by protected attributes and you find significantly different error rates, you have moved from debugging into fairness territory with legal and ethical implications. At that point the response changes from "collect more data" to also involving policy decisions, legal review, and potentially redesigning the model objective.`,
      },
    ],
    takeaway: `The key insight is that aggregate metrics tell you whether you have a problem, but error slicing tells you which specific subpopulation has the problem — and in practice, a model with good average metrics often has catastrophic failures concentrated in a small slice that the average hides completely.`,
  },
  {
    id: 'calibration',
    title: 'Calibration & Brier Score',
    subtitle: 'Reliability diagrams, Platt scaling, isotonic regression, Brier decomposition',
    difficulty: 'intermediate',
    estimatedMin: 28,
    tags: ['calibration', 'Brier score', 'Platt scaling', 'reliability diagram'],
    summary: `AUC tells you whether a model ranks correctly. Calibration tells you whether the probabilities it outputs are honest. These are completely independent properties — a model can have AUC=1.0 (perfect ranking) while being systematically 30 percentage points overconfident. In pure ranking problems (top-k recommendations) you can ignore calibration. But the moment a probability output drives an actual decision — a doctor reading a 90% mortality risk, a pricing engine setting rates, an alert system setting thresholds — calibration is as important as discrimination. Modern neural networks are almost always overconfident out of the box. The standard fix is temperature scaling: one parameter, tuned on validation data, that compresses the output probabilities without changing the ranking. It's the first thing you should try before reaching for anything more complex.`,
    keyPoints: [
      `A calibrated model satisfies P(Y=1 | predicted_prob=p) = p for all p. It's a marginal property — you don't need to be right on any individual prediction, only that p% of predictions at probability p are correct. Importantly, good calibration doesn't imply good discrimination. A model that always predicts the base rate is perfectly calibrated but completely useless.`,
      `Reliability diagrams are the standard visual check: bin predictions into 10 buckets. For each bucket, plot mean predicted probability (x-axis) vs fraction of actual positives (y-axis). Perfectly calibrated models lie on the diagonal. Points below the diagonal mean the model is overconfident (predicts higher than actual). Neural networks almost always show points below the diagonal — systematic overconfidence.`,
      `ECE (Expected Calibration Error) = sum_b (n_b/n) |acc_b - conf_b| — the weighted average absolute deviation from perfect calibration. Low ECE is necessary but not sufficient. A model can have low ECE if all buckets cluster near the mean even with significant miscalibration in specific probability ranges. Always look at the reliability diagram, not just the scalar.`,
      `Brier score = (1/n) sum (predicted_i - y_i)^2 — mean squared error of probability predictions. It decomposes into resolution (discrimination), reliability (calibration error), and uncertainty (irreducible from base rate). A model with good AUC but poor calibration shows high reliability component in the Brier decomposition. Use Brier skill score = 1 - BS/BS_ref for a relative measure.`,
      `Platt scaling: train a logistic regression on top of model scores on held-out data. Two parameters (slope and intercept) rescale the raw output. Works well for sigmoid-shaped miscalibration, which is common in SVMs and boosting. Don't calibrate on training data — you need a separate calibration set.`,
      `Isotonic regression for calibration fits a non-decreasing step function mapping raw scores to calibrated probabilities. More flexible than Platt — can correct non-monotonic miscalibration. Needs more calibration data; risks overfitting on small sets. Rule of thumb: Platt for small calibration sets, isotonic for large ones.`,
      `Temperature scaling for neural networks: divide logits by T before softmax. T > 1 softens probabilities (reduces overconfidence). T < 1 sharpens them. Find optimal T by minimising NLL on a validation set. Critically, temperature scaling doesn't change predictions (argmax is preserved) — only the probabilities. This is the standard first-pass recalibration for any neural network.`,
      `Calibration matters most when: the probability itself is the model output used in decisions (medical risk, fraud scoring, pricing), downstream models take the probability as an input feature, or decision thresholds have different costs. It matters less when you only care about ranking — for top-k recommendation, AUC is sufficient and calibration is irrelevant.`,
    ],
    checkQuestions: [
      {
        q: `A model has AUC = 0.91 and ECE = 0.15. What does this mean and what do you do?`,
        a: `AUC=0.91 means the model discriminates very well — it ranks positives above negatives 91% of the time. ECE=0.15 means the model's probabilities are systematically off by 15 percentage points on average — this is severe miscalibration. If the model predicts 0.8, actual probability is likely around 0.65 or 0.95. For any use case where the probability itself drives a decision (medical risk, pricing, fraud threshold), this is a serious problem. Fix: apply Platt scaling or isotonic regression on a held-out calibration set. Re-check ECE after recalibration. The high AUC tells you the underlying model is strong; the recalibration just adjusts the probability scale without changing rankings.`,
      },
      {
        q: `Your reliability diagram shows the model is overconfident at high probabilities (0.8-1.0 bucket shows actual positive rate of 0.55). What could cause this and how do you fix it?`,
        a: `Overconfidence in the high-probability bucket means when the model is very confident, it is actually correct only 55% of the time — its confidence is not warranted. Causes: (1) Model was trained with log loss which rewards confident predictions even when training data is noisy. (2) Neural network — modern NNs are systematically overconfident without calibration. (3) Insufficient training data in the high-probability regime. (4) Class imbalance causing the model to push all scores to extremes. Fix: (1) Temperature scaling — find T > 1 that brings the high-probability bucket down to match actual frequencies. (2) Label smoothing during training prevents the model from predicting extreme probabilities. (3) For tree models, use Platt scaling on a held-out validation set.`,
      },
      {
        q: `Two models: Brier score for Model A = 0.08, for Model B = 0.12 on the same dataset. Model B has higher AUC. How do you interpret this?`,
        a: `The Brier score jointly measures discrimination and calibration. Model A has lower Brier score (better) despite lower AUC than Model B — this means Model A is significantly better calibrated, which outweighs Model B's ranking advantage in terms of mean squared probability error. For applications where probability quality matters (risk scoring, cost-sensitive decisions), Model A may be preferred despite lower AUC. For applications where only ranking matters (recommendation, top-k retrieval), Model B is better. You can decompose the Brier score into resolution (discrimination) and reliability (calibration) components to see which effect is larger and whether recalibrating Model B would close the gap.`,
      },
    ],
    takeaway: `The key insight is that AUC measures whether a model ranks correctly while calibration measures whether its probability estimates are honest, and these are independent — so for any application where the probability output drives a real-world decision, calibration must be evaluated and fixed separately from discrimination.`,
    interactiveId: 'calibration_curve_viz',
  },
  {
    id: 'ablation',
    title: 'Ablation Studies & Baselines',
    subtitle: 'Designing ablations, good baselines, isolating contributions',
    difficulty: 'intermediate',
    estimatedMin: 26,
    tags: ['ablation', 'baselines', 'experiment design'],
    summary: `Ablation studies answer the question every honest engineer should be asking: "how much of this actually matters?" Without ablations, complex pipelines accumulate dead weight — components added because they seemed plausible, never removed because nobody measured their contribution. The counterpart to ablation is baseline selection: the baseline sets the bar a complex model has to clear to justify its complexity. Picking a weak baseline inflates your apparent gain. The most common malpractice in ML research is comparing against an undertuned or outdated competitor. In production, the same principle applies — if your fancy model doesn't beat a well-tuned gradient boosted tree, the fancy model isn't earning its inference cost.`,
    keyPoints: [
      `Ablation design: start from the full system, remove exactly one component per experiment. The performance gap between full and ablated system is that component's contribution. Remove two components simultaneously and you can't attribute what caused the change. This is scientific control applied to ML systems.`,
      `The most common ablation error: changing architecture and hyperparameters at the same time. Remove a layer and also retune learning rate — you can't interpret the result. Run all ablations with the same hyperparameters. Ablated models may be suboptimal for their specific configuration; that's fine. You're measuring the component's value to the full system, not optimising each variant independently.`,
      `Baselines hierarchy: random → majority class → simple rule-based → linear model → single tree → gradient boosting → your model. In research, also include the best published result on the same benchmark and a reimplemented strong baseline. If your method only beats a weak baseline, the contribution may not exist against a properly tuned competitor.`,
      `Feature ablation is different from permutation importance. Permutation importance estimates marginal contribution assuming other features are present. Feature ablation captures contribution including all interaction effects with the removed features. Use ablation to decide which feature groups to invest in and which to sunset — it gives the full picture, not just the marginal one.`,
      `Learning curves (data ablation): train with 10%, 25%, 50%, 75%, 100% of data and plot performance vs dataset size. Curve still steep at 100%? More data will help — data collection is the bottleneck. Curve flattened? Model capacity or features are the bottleneck. This is the most informative single analysis for deciding where to invest next.`,
      `Negative ablation results are scientifically valuable. If removing a component doesn't hurt performance (or improves it), that component is dead weight. Remove it. Simpler models train faster, serve faster, cost less to maintain, and often generalise better. The willingness to publish or act on negative ablations distinguishes rigorous ML practice from cargo-culting.`,
      `Seed variance: run each ablation with 3-5 different random seeds and report mean ± std. A result that doesn't survive seed variance isn't real. A claimed +2% improvement with std=3% is noise. In deep learning, single-run ablations are unreliable — especially on small datasets where random initialisation can dominate the effect you're trying to measure.`,
    ],
    checkQuestions: [
      {
        q: `Your paper claims that adding a cross-attention layer improves NDCG by 2%. A reviewer asks for an ablation. What do you run?`,
        a: `Run four conditions: (1) Full model with cross-attention — the reported number. (2) Full model without cross-attention — baseline contribution. (3) Full model with self-attention replacing cross-attention of equal parameter count — to test whether cross-attention specifically is needed vs just added parameters. (4) Full model with a feedforward layer of equal parameters — to test whether any additional capacity helps, ruling out the "more parameters = better" explanation. All four run with the same hyperparameters (no retuning), same train/test split, same evaluation protocol, reported as mean ± std over 3 seeds. If condition 1 beats conditions 3 and 4 significantly, you have demonstrated that cross-attention specifically contributes. If conditions 3 or 4 match condition 1, the contribution is capacity, not architecture.`,
      },
      {
        q: `You run a feature ablation and find removing "user age" drops F1 from 0.85 to 0.78 (a large drop). But age is a protected attribute. How do you reason about this?`,
        a: `The feature ablation tells you age is highly predictive in the current model — but predictive does not mean it should be used. Three separate questions: (1) Is using age legally permitted in this application? (2) Is age the true causal driver, or is it a proxy for other features (credit history length, income stability) that should be modelled directly? (3) What are the fairness implications — does the model treat people differently by age in ways that cause disproportionate harm? If legal, explore whether the F1 drop is coming from age per se or from correlated features that age is proxying for. Add those features explicitly and re-run the ablation to see if age then becomes less predictive. If the age contribution persists after controlling for non-protected proxies, you face a genuine fairness-accuracy trade-off that requires policy input.`,
      },
      {
        q: `You remove component X from your system and AUC goes up from 0.82 to 0.84. What do you conclude and what do you do next?`,
        a: `Component X is either hurting performance (contributing noise or introducing a regularisation mismatch) or is redundant (its information is already captured by other components). Before concluding: (1) Is the improvement within seed variance? Run 5 seeds and check if the gap is consistent. (2) Is component X designed for a different objective — e.g., a diversity penalty that hurts AUC but improves user satisfaction? (3) Check whether removing X makes the system simpler without harming other metrics. If the improvement is real and consistent, remove X — the simpler system is better. Report this as a negative ablation result: the component was expected to help but did not, and removal improved performance. This is scientifically interesting because it challenges the assumption behind X's inclusion.`,
      },
    ],
    takeaway: `The key insight is that ablation studies are the only way to know what is actually contributing to your model's performance versus what is cargo-culted complexity, which means skipping ablations is how teams end up maintaining components that have been hurting performance for months.`,
  },
  {
    id: 'evaluation_in_prod',
    title: 'Evaluation in Production',
    subtitle: 'Shadow mode, champion-challenger, guardrail metrics, significance at scale',
    difficulty: 'intermediate',
    estimatedMin: 30,
    tags: ['production evaluation', 'shadow mode', 'A/B testing', 'champion-challenger'],
    summary: `Every ship/no-ship decision should ultimately be backed by a controlled experiment — that's the guiding principle for production evaluation. Shadow mode is the right first step for any high-stakes deployment: run the new model on live traffic, log its predictions, but serve only the champion's results. You're validating infrastructure and catching distribution surprises before anyone sees the model's outputs. Champion-challenger A/B tests are the actual measurement. At the scale of millions of users, statistical significance is trivially achieved on tiny effects — a 0.01% CTR lift will be p < 0.0001 with one day of data. That's a scale problem, not a reason to ship. Practical significance (effect size, business value) is what matters. The ramp strategy (1% → 10% → 50% → 100%) is your circuit breaker: catch catastrophic failures before they affect everyone.`,
    keyPoints: [
      `Shadow mode: the new model receives the same production requests as the champion and generates predictions, but only the champion's predictions are served. Use it to (1) validate serving infrastructure under real load, (2) detect unexpected failures or output distributions, (3) compare prediction distributions between models — large disagreements on the same inputs warrant investigation before any A/B test.`,
      `Champion-challenger A/B test: traffic split is typically 90/10 or 95/5. Require Bonferroni correction if testing multiple metrics simultaneously — 10 metrics at p < 0.05 each gives ~40% probability of at least one false positive without correction. Set the primary metric, secondary metrics, and guardrails before the test starts, not after looking at the results.`,
      `Statistical significance vs practical significance at scale: with 10M daily users, a 0.01% CTR lift is statistically significant with one day of data. That's 1,000 extra clicks. Ask whether deploying and maintaining a new model is worth 1,000 extra clicks per day. "Is this effect real?" and "Is this effect worth acting on?" are different questions with different answers.`,
      `Sample Ratio Mismatch (SRM): if you intend a 50/50 split but observe 55/45 in the data, the randomisation has failed. SRM invalidates the A/B test — some systematic factor affected assignment. Always run a chi-squared test on observed vs expected traffic split as the first check before interpreting any metric results.`,
      `Sequential testing with mSPRT allows valid inference at any stopping point. Standard A/B tests assume fixed sample size determined upfront. Peeking at results during the test and stopping early when p < 0.05 inflates Type I error dramatically. Sequential testing frameworks (used at Spotify, Microsoft, Booking.com) let you stop early when results are clearly positive or negative without corrupting the Type I error rate.`,
      `Ramp strategy: start at 1% of traffic, then 5%, 10%, 50%, 100%. At each step, run guardrail checks. A catastrophic failure at 1% affects 1% of users. Automated ramp-back triggers — if error rate exceeds threshold in any 15-minute window, reduce to 0% — are standard at high-traffic systems. The ramp is your insurance policy.`,
      `Novelty and primacy effects require at least two full business cycles to resolve. Novelty: users engage more with new things simply because they're new. Primacy: users initially prefer familiar features even when the new one is better. Both distort early results. Plan for at least 14 days minimum, 28 for products with strong weekly patterns. Check whether the treatment effect estimate is stable over time.`,
      `Holdout groups: maintain 1-5% of users permanently on the old system. This lets you measure cumulative impact of a series of improvements against a never-updated baseline. Short A/B tests only measure marginal gains. A holdout measures total accumulated value over months. Netflix and LinkedIn use long-term holdouts for this reason.`,
    ],
    checkQuestions: [
      {
        q: `Your A/B test on a recommendation system shows +2% CTR (p=0.001) after 3 days. Should you ship?`,
        a: `Not yet. Check five things before shipping: (1) Guardrail metrics — has session length, revenue per session, or explicit rating dropped? CTR gain coming at the expense of session quality is a net loss. (2) Test duration — 3 days is too short; novelty effect inflates CTR early. Run for at least 14 days. (3) SRM check — does the observed traffic split match the intended split? If not, the test is invalid. (4) Practical significance — is +2% CTR worth deploying and maintaining a new model? What is the business value? (5) Segment breakdown — is the lift uniform across countries, platforms, and user types, or driven by one segment that happens to be overrepresented? Ship only after all guardrails pass, 14+ days of data, and segment analysis confirms broadly positive results.`,
      },
      {
        q: `A/B test results show +1% revenue lift (p=0.0001) after 30 days. A colleague says "p < 0.05 means we should ship." What is missing from this reasoning?`,
        a: `p < 0.05 confirms the effect is unlikely to be zero, not that it is large enough to matter. Missing: (1) Effect size and practical significance — is +1% revenue worth the deployment cost? Check absolute dollar value and whether it meets the MDE you set before the test. (2) Guardrail metrics — is any guardrail metric degraded? A revenue gain from aggressive upselling might harm long-term retention. (3) Confidence interval — what is the range of plausible effects? (4) Multiple testing correction — if you tested 20 metrics and one is p=0.0001, Bonferroni would require p < 0.0025 for significance. (5) Heterogeneous treatment effects — is the +1% uniform or driven by a small segment that could be specifically targeted?`,
      },
      {
        q: `You are running 5 simultaneous A/B tests on your platform. A product manager notices that tests 2 and 4 seem to interact. How do you handle this?`,
        a: `Simultaneous A/B tests on the same users can interact if the tests affect the same user behaviour. The standard fix is mutual exclusion: each user is in at most one test. But mutual exclusion limits total test capacity. To diagnose interaction: look at users in both tests simultaneously and compare their metrics against users in only one test. If the interaction is genuine (significant difference), the test results for interacting cells are contaminated and cannot be interpreted. Going forward: use a traffic allocation system that enforces mutual exclusion for tests that touch similar parts of the product, and use factorial design only when you can formally test and confirm statistical independence between tests. At minimum, document which tests ran simultaneously and flag their results as potentially confounded.`,
      },
      {
        q: `What is a holdout group and why is it more valuable than a series of A/B tests for measuring long-term model value?`,
        a: `A holdout group is a permanently withheld fraction of users who receive no model updates over a long period (months to years). A/B tests measure marginal lift from one model version to the next. If you deploy 12 model improvements per year and each shows +1% CTR, does that mean you have +12% CTR from the starting point? Not necessarily — some improvements may cannibalise each other, or the product would have drifted +8% just from seasonal effects. A holdout group exposes users to the original baseline model continuously, so you can compare the current production system against the counterfactual of never having made any of those improvements. This gives true cumulative causal impact — the total value of the ML work over time. The cost is that holdout users are permanently under-served, which raises an ethical question in high-stakes applications.`,
      },
    ],
    takeaway: `The key insight is that production evaluation is the only source of ground truth for model impact, which means the entire offline evaluation pipeline exists only to filter candidates cheaply enough that you run A/B tests on the handful worth testing — every ship/no-ship decision should ultimately be backed by a controlled experiment.`,
  },
]

export const EVAL_MODULES = [
  {
    id: 'metrics_first_principles',
    title: 'Eval Metrics from First Principles',
    subtitle: 'Confusion matrix, precision, recall, F1, why accuracy fails',
    difficulty: 'foundational',
    estimatedMin: 20,
    tags: ['metrics', 'precision', 'recall', 'F1', 'confusion matrix'],
    summary: `The confusion matrix is the foundation of all classification metrics. For binary classification, it has four cells: TP (predicted positive, actually positive), FP (predicted positive, actually negative), TN, FN. Accuracy = (TP+TN)/N is misleading when classes are imbalanced. Precision = TP/(TP+FP) measures how often positive predictions are correct; Recall = TP/(TP+FN) measures how many actual positives are found. F1 = 2·P·R/(P+R) is their harmonic mean.`,
    keyPoints: [
      'Accuracy is deceptive: with 1% positive class, predicting all-negative gives 99% accuracy. The model learns nothing — yet accuracy metric rewards it.',
      'Precision answers "when you cry wolf, are you right?". Recall answers "of all actual wolves, how many did you find?". They trade off against each other via threshold.',
      'F1 = harmonic mean of P and R. Harmonic mean is dominated by the smaller value — F1 is low if either P or R is low. This is intentional.',
      'F_β score: β>1 weights recall higher (β=2: recall twice as important). β<1 weights precision higher (β=0.5). Choose β based on business cost of FP vs FN.',
      'Macro-average: compute metric per class, take unweighted mean. Weights each class equally regardless of size. Micro-average: compute globally by summing TP/FP/FN. Weighted average: weight by class frequency.',
      'For multi-class problems, precision/recall/F1 are computed per class then averaged. The choice of averaging method matters significantly for imbalanced multi-class problems.',
      'Matthew Correlation Coefficient (MCC): single metric for binary classification that handles imbalance well. MCC = (TP·TN − FP·FN) / √((TP+FP)(TP+FN)(TN+FP)(TN+FN)). Range [-1,1]; 1=perfect.',
    ],
    checkQuestions: [
      { q: 'A cancer screening model has precision=0.95 and recall=0.40. Is this a good model?', a: 'Depends on the business context, but recall=0.40 means 60% of actual cancer cases are missed (FN). For cancer screening, missing a positive case (FN) is far more costly than a false alarm (FP). You would want recall >> precision. Lower the decision threshold to increase recall at the cost of precision. A recall of 0.40 would likely be unacceptable — you want >0.90 recall and then maximise precision within that constraint.' },
    ],
  },
  {
    id: 'auc_roc',
    title: 'ROC Curve & AUC',
    subtitle: 'FPR/TPR, what area means, PR-AUC for imbalanced classes',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['ROC', 'AUC', 'PR-AUC', 'ranking'],
    summary: `The ROC (Receiver Operating Characteristic) curve plots TPR (recall) vs FPR (= FP/(FP+TN)) as the classification threshold varies from 1 to 0. AUC-ROC is the area under this curve — a threshold-independent measure of ranking quality. AUC = 0.5 means random; AUC = 1.0 means perfect ranking. For highly imbalanced datasets, AUC-ROC is overly optimistic because TN dominates FPR — PR-AUC is more informative.`,
    keyPoints: [
      'AUC interpretation: probability that a randomly chosen positive example is ranked higher than a randomly chosen negative example by the model. Pure ranking metric.',
      'ROC curve sweeps threshold from 1→0. At threshold=1: all predicted negative (TPR=0, FPR=0). At threshold=0: all predicted positive (TPR=1, FPR=1). Perfect classifier: goes to top-left corner.',
      'Why AUC-ROC is optimistic for imbalanced data: FPR = FP/(FP+TN). With many negatives, even large FP counts give small FPR, making the curve look good in the upper-left region.',
      'PR-AUC (Precision-Recall AUC): plots precision vs recall. For imbalanced data, a random classifier\'s PR-AUC = prevalence (e.g., 0.01 for 1% positive). Hard to game — more informative baseline.',
      'PR curve has no TN: precision and recall both ignore true negatives. For minority class detection, this is exactly what you want — you do not care about the large TN pool.',
      'Average Precision (AP): area under PR curve using trapezoidal rule. MAP (Mean Average Precision) averages AP across queries (ranking/retrieval tasks).',
      'When to use which: AUC-ROC for roughly balanced classes, or when you care about ranking across the full threshold range. PR-AUC for highly imbalanced classes or when FP cost ≈ FN cost and the minority class is what matters.',
    ],
    checkQuestions: [
      { q: 'Two models have the same AUC-ROC (0.85) on a 1% positive rate dataset. How would you further differentiate them?', a: 'Compare PR-AUC — it is more discriminative for imbalanced classes. Also look at: (1) Calibration — which model\'s P(y=1) better matches actual frequencies? (2) Precision at the operating recall you care about (e.g., P@recall=0.9). (3) Lift curve — for top-decile scoring, which model captures more positives? (4) Business metric at the intended operating threshold. Two models can have identical AUC-ROC but very different PR curves, especially near high-recall regime.' },
    ],
  },
  {
    id: 'ranking_metrics',
    title: 'Ranking Metrics',
    subtitle: 'NDCG, MAP, MRR — search and recommendation quality',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['NDCG', 'MAP', 'MRR', 'ranking', 'RecSys'],
    summary: `Ranking metrics evaluate the quality of ordered lists — used in search engines, recommendation systems, and information retrieval. Unlike classification metrics, ranking metrics care about position: a relevant result at rank 1 is better than the same result at rank 10. NDCG, MAP, and MRR are the three standard metrics, each capturing different aspects of ranking quality.`,
    keyPoints: [
      'MRR (Mean Reciprocal Rank): MRR = (1/|Q|)Σ 1/rank_first_relevant. Only counts the first relevant result. Good for QA ("I want one right answer"), bad for multiple relevant items.',
      'MAP (Mean Average Precision): Average Precision@k for each query, averaged over queries. AP = (1/R) Σₖ P@k · rel(k) where rel(k)=1 if rank k is relevant. Rewards finding all relevant items early.',
      'NDCG (Normalised Discounted Cumulative Gain): DCG = Σᵢ rel(i)/log₂(i+1). NDCG = DCG/IDCG where IDCG is DCG of perfect ranking. Handles graded relevance (not just binary).',
      'Discount function in NDCG: log₂(i+1) means position 1 counts fully, position 2 counts at 63%, position 10 counts at 29%. Models the attention decay of a real user.',
      'NDCG@K: truncate at rank K. Standard in practice — users rarely look past page 1. K=10 or K=20 common for web search, K=100 for offline offline retrieval evaluation.',
      'Graded relevance: NDCG supports relevance scores 0-3 or 0-4 (e.g., from human raters: irrelevant/somewhat/relevant/highly relevant). MAP and MRR require binary relevance.',
      'Position bias: users click results based on position, not just relevance. Inverse Propensity Scoring (IPS) corrects for this in training. Counterfactual LTR accounts for logged position bias.',
    ],
    checkQuestions: [
      { q: 'A search engine retrieves 3 relevant documents at ranks 1, 3, 5 out of 5 total relevant documents. Compute AP.', a: 'AP = (1/R)Σ P@k·rel(k) where R=5 (total relevant). P@1=1/1=1 (rank 1 relevant), P@3=2/3 (rank 3 relevant), P@5=3/5 (rank 5 relevant). Other ranks: not relevant, contribute 0. AP = (1/5)(1 + 2/3 + 3/5) = (1/5)(1 + 0.667 + 0.6) = (1/5)(2.267) = 0.453.' },
    ],
  },
  {
    id: 'offline_vs_online',
    title: 'Offline vs Online Evaluation',
    subtitle: 'Proxy metrics, A/B gap, how to close the offline-online divide',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['evaluation', 'A/B testing', 'online metrics', 'proxy metrics'],
    summary: `Offline evaluation uses historical data with held-out labels; online evaluation (A/B tests) measures real user behaviour in production. A model can improve offline metrics (AUC, NDCG) yet degrade online metrics (CTR, revenue, engagement) — this gap is the core challenge of applied ML. Closing the gap requires carefully choosing proxy metrics that correlate with online outcomes.`,
    keyPoints: [
      'Offline-online gap causes: (1) distribution shift between train/test and production. (2) Feedback loops — model affects what data is collected. (3) Offline labels proxy real outcomes poorly. (4) Counterfactual issue — you only observe one treatment.',
      'Counterfactual evaluation: evaluate offline as if items were served in logged positions. Requires inverse propensity scoring to correct for logging policy bias. Used in Yahoo/Netflix recommendation evaluation.',
      'Guardrail metrics: online metrics you must not harm even if the primary metric improves. E.g., improve CTR but do not reduce session length below X. Prevents metric Goodharting.',
      'Interleaving: a more sensitive alternative to A/B testing for ranking. Interleave results from two rankers, observe which items get clicked. Can detect improvements with 100× fewer users than standard A/B.',
      'Proxy metric correlation: track historical offline metric changes alongside online metric changes. Build a calibration curve: offline Δ = +0.5 AUC typically corresponds to +X% CTR. Use this to set minimum offline bar before running A/B.',
      'Novelty/position effects in A/B: users may click more on new things simply because they\'re new (novelty effect). Run tests long enough (2+ weeks) to see novelty wear off.',
      'Metric selection framework: leading vs lagging indicators. CTR is leading (fast feedback); LTV/retention is lagging (weeks/months). Need both — optimising only CTR can inflate clicks while decreasing long-term engagement.',
    ],
    checkQuestions: [
      { q: 'Your RecSys shows +3% NDCG@10 offline. You run an A/B test and see -1% on session length. What do you do?', a: 'Session length is likely a guardrail metric — do NOT ship the model. The offline NDCG improvement did not translate to a real user experience improvement; in fact it hurt engagement. Investigate why: (1) Did offline evaluation use unbiased labels? (2) Is the model optimising for clicks on items that do not satisfy the user? (3) Check CTR, dwell time, explicit ratings, return rate. The disconnect suggests a proxy metric problem — NDCG on click labels is not aligned with long-term engagement. Revise the offline evaluation setup.' },
    ],
  },
  {
    id: 'validation_traps',
    title: 'Validation Set Traps & Data Leakage',
    subtitle: 'Leakage taxonomy: temporal, group, label leakage, how to audit',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['data leakage', 'validation', 'temporal split', 'label leakage'],
    summary: `Data leakage is the accidental inclusion of information from the test set (or future) in the training features, causing overly optimistic offline metrics that collapse in production. Leakage is the most common cause of the offline-online gap in practice. It takes many subtle forms — temporal leakage, group leakage, label leakage, preprocessing leakage — and requires systematic auditing to detect.`,
    keyPoints: [
      'Temporal leakage: training on data from after the prediction time. E.g., using features computed at t=10 to predict an event at t=5. Always use point-in-time correct features.',
      'Group leakage: same user/entity appears in both train and test. Models learn user-specific patterns that do not generalise to new users. Fix: group k-fold or user holdout split.',
      'Label leakage: a feature is correlated with the label because it was derived from or collected after the label was determined. E.g., using "days in hospital" to predict "was the patient hospitalised." The feature contains the answer.',
      'Preprocessing leakage: fitting a scaler, imputer, or encoder on the full dataset (including test) before splitting. The test set\'s statistics leak into training. Fix: fit all transformers on training only; apply to test.',
      'Target encoding leakage: computing target mean per category using the full dataset. Fix: k-fold target encoding using out-of-fold data only.',
      'The leakage audit: for each feature, ask "would this value be available at prediction time in production?" If not, it is leakage. This temporal feasibility check is the gold standard.',
      'Detection signal: if model accuracy seems too good (near-perfect), or if a feature has much higher importance than expected, suspect leakage. Check correlation of each feature with the label — suspiciously high correlation is a red flag.',
    ],
    checkQuestions: [
      { q: 'You build a churn prediction model with AUC=0.97. In production, AUC drops to 0.64. What went wrong and how do you debug?', a: 'AUC drop from 0.97 to 0.64 is a strong signal of data leakage or training/serving skew. Debug steps: (1) Check for temporal leakage — was training data split randomly by row (mixing future and past) instead of by time? (2) Audit high-importance features — are any correlated >0.8 with the label? Could indicate label leakage. (3) Check preprocessing — was scaling/imputation fit on full dataset? (4) Check if any feature is computed post-outcome (e.g., account closed = churn proxy used as feature). (5) Ensure production feature pipeline matches training pipeline exactly.' },
    ],
  },
  {
    id: 'cross_validation',
    title: 'Cross-Validation Strategies',
    subtitle: 'k-fold, stratified, group k-fold, time-series CV, nested CV',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['cross-validation', 'k-fold', 'time series', 'nested CV'],
    summary: `Cross-validation (CV) provides a more reliable estimate of generalisation performance than a single train/val split by using the data multiple times. Standard k-fold partitions data into k equal folds, training on k-1 and testing on 1, rotating through all folds. Stratified k-fold preserves class ratios. Group k-fold prevents entity leakage. Time-series CV respects temporal order. Nested CV separates hyperparameter tuning from evaluation.`,
    keyPoints: [
      'Standard k-fold: k=5 or k=10 balances bias (small k) and variance (large k). k=n is leave-one-out (LOO) — unbiased but slow and high variance.',
      'Stratified k-fold: each fold has the same class proportion as the full dataset. Essential for imbalanced classes (a fold with no positives would give meaningless metrics).',
      'Group k-fold: all samples from a group (user, patient, session) go to the same fold. Prevents group leakage — simulates the real scenario of predicting on new unseen groups.',
      'Time-series CV (walk-forward validation): train on [0,t], validate on [t, t+Δ], slide forward. Never look at the future. Expanding window (grow train set) or rolling window (fixed train size).',
      'Purged time-series CV: add a gap between train and validation to prevent temporal leakage from autocorrelated features (e.g., rolling averages that span the boundary). Used in financial ML.',
      'Nested CV: outer loop (k-fold for evaluation), inner loop (k-fold for hyperparameter tuning). Gives unbiased estimate of model selection + training. Without nesting, hyperparameter search leaks into evaluation.',
      'CV for large datasets: stratified sampling of large folds. Or use a single large validation split (80/20) — CV is most valuable when data is scarce.',
    ],
    checkQuestions: [
      { q: 'You have 5 years of daily transaction data and want to build a fraud model. How do you set up CV?', a: 'Use time-series walk-forward CV. Set up expanding windows: train on year 1, validate on month 1 of year 2; train on years 1+2m1, validate on month 2 of year 2; etc. Add a gap (e.g., 7 days) between train and validation to prevent leakage from features with lag (e.g., 7-day rolling fraud rate). Never use standard k-fold — it would allow training on future data to predict the past. Group by user_id within each fold if same user appearing in both is a concern.' },
    ],
  },
  {
    id: 'error_analysis',
    title: 'Error Analysis',
    subtitle: 'Confusion matrix drill-down, error slicing, systematic failures',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['error analysis', 'failure modes', 'slicing', 'debugging'],
    summary: `Error analysis is the systematic study of where and why a model fails. Rather than aggregating all errors into a single metric, it slices errors by subgroup, input type, prediction confidence, and data source to identify systematic failure patterns. In production, error analysis is the primary tool for deciding what to improve next — it turns a generic "model performs poorly" into a specific actionable diagnosis.`,
    keyPoints: [
      'Start with the confusion matrix: which off-diagonal cells are largest? Are FPs or FNs more common? Are errors concentrated in specific classes?',
      'Error slicing: segment errors by feature values (e.g., time of day, user country, product category) and check if error rate spikes in specific segments. A model with average F1=0.85 may have F1=0.4 for a critical segment.',
      'Confidence calibration in errors: plot error rate vs predicted confidence. Overconfident errors (high confidence, wrong) are most dangerous. Identify the confidence threshold where accuracy starts degrading.',
      'Manual inspection of errors: randomly sample 50-100 FPs and FNs. Categorise errors into buckets (e.g., "ambiguous label", "rare pattern", "data quality issue", "model limitation"). The distribution of error buckets guides next steps.',
      'Systematic vs random errors: if errors are random (no pattern), more data helps. If errors cluster in a subgroup, you need targeted data collection or feature engineering for that subgroup.',
      'Error correlation across models: two models with the same accuracy but different error patterns can be ensembled — their errors partially cancel. Error analysis informs model diversity.',
      'Production error monitoring: log prediction, confidence, and (when available) labels. Build dashboards that slice error rate by feature buckets in real time. Set alerts for error rate spikes in any segment.',
    ],
    checkQuestions: [
      { q: 'Your NLP classifier has 88% overall accuracy but stakeholders are unhappy. How do you diagnose what is wrong?', a: '88% overall may hide severe per-class or per-segment failures. Steps: (1) Compute per-class precision/recall — which classes have low recall? (2) Slice by text length, domain, date — any segment with much lower accuracy? (3) Sample 100 errors and manually label root causes: data quality, label ambiguity, missing features, out-of-distribution input? (4) Check confusion matrix — are errors concentrated between specific pairs of classes (easily confused)? (5) Confidence-accuracy curve — is the model overconfident on errors? Each finding points to a different fix: more data, better features, re-labelling, or model architecture changes.' },
    ],
  },
  {
    id: 'explainability',
    title: 'Model Explainability',
    subtitle: 'SHAP values, LIME, partial dependence plots, global vs local',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['SHAP', 'LIME', 'explainability', 'interpretability', 'XAI'],
    summary: `Model explainability methods help understand why a model makes specific predictions. Global explanations (PDP, feature importance) describe the model overall; local explanations (SHAP, LIME) explain individual predictions. SHAP (SHapley Additive exPlanations) is grounded in game theory and provides theoretically principled feature attributions that sum to the model output. LIME fits a local linear surrogate model around each prediction.`,
    keyPoints: [
      'SHAP values: based on Shapley values from cooperative game theory. φⱼ = average marginal contribution of feature j across all possible coalitions of features. Properties: efficiency (sum to prediction), symmetry, dummy (unused feature gets 0).',
      'SHAP for tree models (TreeSHAP): exact polynomial-time computation. For neural nets and black-box models: DeepSHAP (gradient-based) or KernelSHAP (model-agnostic, slow).',
      'SHAP summary plot: beeswarm of SHAP values — x-axis is SHAP value (impact on output), colour is feature value. Shows global feature importance AND direction of effect.',
      'SHAP interaction values: φᵢⱼ = share of prediction attributable to interaction between features i and j. More expensive but catches interaction effects missed by marginal SHAP.',
      'LIME (Local Interpretable Model-agnostic Explanations): perturb input, get model predictions on neighbours, fit weighted linear model. Explanation is coefficients of the local linear model. Fast but not theoretically grounded — can be unstable.',
      'Partial Dependence Plots (PDP): fix all features except j, vary j across its range, plot average prediction. Shows marginal effect of feature j. ICE plots show individual-level effects (PDP = mean of ICE lines).',
      'Post-hoc vs inherent interpretability: decision trees and linear models are inherently interpretable. SHAP/LIME are post-hoc — they explain a black-box model after the fact. Post-hoc explanations can be unfaithful if the approximation is poor.',
    ],
    checkQuestions: [
      { q: 'A SHAP analysis shows "age" as the most important feature, with high age → high credit risk score. A lawyer asks if this constitutes illegal age discrimination. How do you respond?', a: 'SHAP values show correlation-based attribution, not causation. Age may be important because it is correlated with truly causal features (length of credit history, income stability) that are not in the model. Steps: (1) Compute SHAP values for age when controlling for other features — are the residual age effects explainable by non-protected proxies? (2) Run a disparate impact analysis — do model outcomes differ significantly by protected group when controlling for creditworthiness? (3) For legal compliance, consult fairness metrics (demographic parity, equalised odds) and legal counsel. SHAP is not a legal fairness tool.' },
    ],
  },
  {
    id: 'ablation',
    title: 'Ablation Studies & Baselines',
    subtitle: 'Designing ablations, good baselines, isolating contributions',
    difficulty: 'intermediate',
    estimatedMin: 15,
    tags: ['ablation', 'baselines', 'experiment design'],
    summary: `Ablation studies systematically remove components of a model or pipeline and measure the performance impact of each removal. They answer "how much does each component contribute?" and are required for credible ML research and rigorous engineering. A good baseline is the simplest model that is still competitive — it sets the bar that a complex model must beat to justify its complexity.`,
    keyPoints: [
      'Ablation design: start with the full system. Remove one component at a time (holding all else fixed). The gap between full system and ablated system is that component\'s contribution.',
      'Confounding in ablations: changing two things at once makes it impossible to attribute the improvement. Each ablation should change exactly one thing.',
      'Baselines hierarchy: random → majority class → simple rule-based → linear model → single decision tree → XGBoost → your model. Never compare against only the strongest baseline.',
      'Selecting baselines in research: compare against (1) the best published result on the same dataset, (2) a strong reimplemented baseline, (3) an ablated version of your own model. Avoid comparing against old/weak baselines.',
      'Feature ablation: remove one feature group at a time. Quantifies which feature groups drive performance. Different from feature importance — ablation captures interaction effects.',
      'Ablation for data: train with 10%, 25%, 50%, 75%, 100% of data. Learning curve shows how much more data would help and whether data collection is the bottleneck.',
      'Negative result ablation: if removing a component does not hurt performance, it is dead weight. Simplify the model — simpler models are faster, cheaper, easier to maintain, and often generalise better.',
    ],
    checkQuestions: [
      { q: 'Your paper claims that adding a cross-attention layer improves NDCG by 2%. A reviewer asks for an ablation. What do you run?', a: 'Run the full model with the cross-attention layer, and separately: (1) remove cross-attention layer entirely (to show contribution), (2) replace with simpler attention (to show that cross-attention specifically is needed), (3) replace with a fully-connected layer of equal parameters (to rule out parameter count as the reason for improvement). All ablations run with the same hyperparameters, seeds, and evaluation protocol. Report mean ± std over 3+ seeds. This isolates whether cross-attention specifically contributes vs just added capacity.' },
    ],
  },
  {
    id: 'evaluation_in_prod',
    title: 'Evaluation in Production',
    subtitle: 'Shadow mode, champion-challenger, guardrail metrics, significance at scale',
    difficulty: 'intermediate',
    estimatedMin: 20,
    tags: ['production evaluation', 'shadow mode', 'A/B testing', 'champion-challenger'],
    summary: `Production evaluation goes beyond offline metrics to measure real user impact. Shadow mode runs a new model on live traffic without affecting users — useful for validating infrastructure and catching prediction errors before A/B. Champion-challenger A/B tests route a fraction of traffic to the new model and compare business metrics. At scale, even tiny effects are detectable, raising the importance of practical significance alongside statistical significance.`,
    keyPoints: [
      'Shadow mode: new model receives production requests and generates predictions logged alongside the champion\'s live predictions — but only the champion\'s predictions are served. Validates correctness without user impact.',
      'Champion-challenger: split traffic (e.g., 90/10) between current model and new model. Measure primary and guardrail metrics. Requires Bonferroni correction if testing multiple metrics simultaneously.',
      'Statistical significance at scale: with millions of users, tiny effects (0.01% lift) are statistically significant but not practically meaningful. Always set minimum detectable effect (MDE) based on business value before starting the test.',
      'Sequential testing (always-valid p-values): standard A/B tests require fixing sample size upfront — peeking inflates Type I error. mSPRT and mixture sequential probability ratio tests allow stopping early with controlled error rates.',
      'Interaction effects: if multiple A/B tests run simultaneously on the same users, they may interact. Mutual exclusion (no user in two tests) prevents this but limits test capacity. Interaction testing at the end validates independence.',
      'Ramp strategy: soft launch at 1% traffic → 10% → 50% → 100%. At each step, check guardrail metrics. Allows catching catastrophic failures early with minimal user impact.',
      'Long-run evaluation: some effects only appear after weeks (novelty wears off, users adapt). Plan evaluation horizon based on expected effect duration — at least 2 business cycles.',
    ],
    checkQuestions: [
      { q: 'Your A/B test on a recommendation system shows +2% CTR (p=0.001) after 3 days. Should you ship?', a: 'Not yet. (1) Check guardrail metrics: session length, revenue, explicit ratings — is CTR gain coming at their expense? (2) Test has only run 3 days — novelty effect may inflate CTR. Run for at least 2 weeks. (3) Check segment breakdowns — is the lift uniform or driven by one segment? (4) Is p=0.001 driven by the effect size or by massive sample size? Check absolute CTR lift — is 2% meaningful for the business? (5) Ensure SRM (Sample Ratio Mismatch) check passes — does traffic split match intended ratio? Only ship after all guardrails pass and test has run long enough to exclude novelty.' },
    ],
  },
]
